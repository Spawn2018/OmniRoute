# BENCH-66.0 inbound-extract-hitl

**Źródło:** plaster `66.0`, delta `docs/deltas/archived/66.0-inbound-extract.md`
**Typ:** pogłębienie M-20
**Commit:** `0224f1b`

## Zadanie

Treść `inbound_message` idzie do `extract_to_draft` (HITL). Przycisk na `/mail`.
Nie accept, nie zapis `rate_line` z poczty, nie IMAP, nie blob.

## Kontekst wejściowy

- `docs/state/CURRENT.md` (S3)
- `docs/deltas/archived/66.0-inbound-extract.md`
- `backend/alembic/versions/` migracje `inbound_message`
- serwis i API `inbound_messages`

## Wynik referencyjny

**Powinny powstać / ruszyć się:** API extract z wiadomości, UI `/mail`, testy
kontraktu (brak `rate_line` na extract), izolacja już z 64.0.

**Nie wolno:** `AcceptExtractionToRates` z poczty; import `rate_lines` /
`quotations` z `ExtractionService`; Graph/IMAP; auto-accept HITL.

## Metryki referencyjne

| Metryka | Wartość |
|---|---|
| test izolacji `inbound_message` | tak (`test_inbound_message_isolation.py`) |
| FORCE ROW LEVEL SECURITY | tak (026) |
| extract nie przyjmuje stawek | tak (`test_inbound_extract_api_does_not_accept_rates`) |

## Pułapka tego przypadku

Agent łączy extract z accept i zapisuje `rate_line` „bo to ten sam ekran poczty”.
