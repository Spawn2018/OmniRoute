# Plaster 235.0 · T3 `si_cutoff_at` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [234.0](../archived/234.0-container-free-time-dest.md) leftover `si_cutoff_at` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 234.0 `free_time_dest_h`
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie VGM cutoff. Nie PIN. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `si_cutoff_at`/AMS/CY/CFS/VGM/PIN/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked (`vgm_cutoff_at` razem z VGM). leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (Decimal). leftover `ams_cutoff_at` / `cy_cutoff_at` / `cfs_cutoff_at` parked (następne pola). leftover `last_survey_at` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `si_cutoff_at` (`timestamptz`, ISO-8601 ze strefą, dana HITL cutoffu instrukcji SI). POST bez pola zostaje `null`. Zła wartość → 400 (`si`). `require_si_cutoff_at` w `app.domain.container` — nie import `stop` / `require_eta_*`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["235.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `174_container_si_cutoff` (po `173_container_free_time_dest`). `_WriteBox` — dodać `cut`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.eta_physical` / `eta_legal` (194.0, wymagane ETA, inny BC). ISTNIEJE `groupage_line.cutoff_local` (155.0, inny obiekt). BRAK `container.si_cutoff_at`. PODOBNE: parse ISO ze strefą. Nie kopiować importem z `stop`. Nie live HTTP armatora. Nie AMS/CY/CFS w tym plasterze.

**Odrzucone:**

- Live HTTP / scrape cutoffu z portalu.
- `vgm_cutoff_at` / VGM Decimal.
- `ams_cutoff_at` / `cy_cutoff_at` / `cfs_cutoff_at` w tym plasterze.
- PIN / `booking_no` / FK / import `stop`.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `174_container_si_cutoff` (po `173_container_free_time_dest`): `si_cutoff_at` nullable `DateTime(timezone=True)`.
2. POST opcjonalny `si_cutoff_at`. Zła wartość → 400. Serwis nie importuje stops/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

AMS/CY/CFS · VGM cutoff · PIN · booking · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown

## Kryteria akceptacji

- [x] POST z ISO `si_cutoff_at` zapisuje czas, bez live HTTP i bez PIN
- [x] POST bez pola zostaje `null`
- [x] zły / bez strefy → 400 (`si`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape / import `stop`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `si_cutoff_at`; 400 `si`; omit → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable timestamptz; brak live HTTP |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `ams_cutoff_at`; PIN/VGM parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
