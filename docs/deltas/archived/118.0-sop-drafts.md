# Plaster 118.0 · S58 drafty po SOP (M-57)

**Spec źródłowa:** [docs/spec/ai-copilot.md](../../spec/ai-copilot.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S58  
**Zależy od:** 76.0 `mail_draft` · 73.0 `blocks_auto` · 81.0 świadomy mailto · 11.0 `customer_sop`

**Status:** Zamknięty `/noc` 2026-09-04. Ten sam żywy ID **M-57**. Kat. M-60 = to pogłębienie, bez nowej tabeli. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** `/ai` **czyta** istniejące `customer_sop` (`fetchCustomerSops`) obok szkiców `mail_draft`. Operator widzi `blocks_auto` zanim kliknie „Wyślij w kliencie”. Zapis SOP zostaje na `/customer-sops`. Zero nowej tabeli. Zero Graph/SMTP. Zero auto-send. `SHIPPED_CHARGE_ROUTES["118.0"]` = `/ai`. OPS_JOBS 54 → 55.

**Łowca:** ISTNIEJE 76.0/81.0 szkic + mailto na `/ai`. ISTNIEJE 73.0 `blocks_auto` + `GET /customer-sops/auto-block`. BRAK listy SOP na `/ai`. PODOBNE: 116.0 wieża czyta szkice. Dwa joby: zapis SOP vs odczyt przy szkicu.

**Odrzucone:**

- Auto-send / auto-przelew / auto-booking.
- Graph HTTP / SMTP / nowa tabela `mail_send`.
- Import `customer_sops` w `MailDraftService`.
- Nowy żywy M-60 / czat / LLM piszący SOP.
- F9.1.

## Zakres

1. `/ai`: sekcja SOP, `data-ai-copilot="sop"`, `fetchCustomerSops`, `blocks_auto`. Link do `/customer-sops`.
2. Test powierzchni 118.0 (zostaw 47.0/76.0/81.0). How-to w `katalogi.md` bez nowej linii (bullet SOP / szkic).
3. `SHIPPED_CHARGE_ROUTES["118.0"]` = `/ai`. OPS_JOBS = 55.

## Poza zakresem

Graph send · auto-send · S59 OTel · Auth0 · F9.1

## Ustalenia

- Token szkicu zostaje `mail_draft`. Token flagi zostaje `blocks_auto`.
- 81.0: `blocks_auto` nie blokuje świadomego `mailto:`. S58 tylko pokazuje flagę.
- LLM nie pisze SOP. `charge` zostaje prawdą o marży.

## Schemat

Bez nowej tabeli.

## Kryteria akceptacji

- [x] `/ai` listuje SOP `blocks_auto`; brak smtp / graph.microsoft / auto-send
- [x] Zero nowej migracji
- [x] OPS_JOBS = 55; `118.0` → `/ai`
- [x] `katalogi.md` ≤ 40

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/ai` czyta `blocks_auto`; zero auto-send |
| Szybkość | N/A ten plaster | odczyt listy, bez budżetu AGENTS |
| SQL | N/A | brak nowego SELECT/INSERT |
| N+1 | OK | jeden `fetchCustomerSops` |
| Dług / człowiek | OK | `SopAutoFacts` na `ol`; nie klon listy szkiców |
| Proza operatora | N/A ten plaster | tablica-odczyt; `katalogi.md` wtopione |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
