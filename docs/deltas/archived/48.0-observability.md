# Plaster 48.0 · M-68 Obserwowalność (`observability`)

**Spec źródłowa:** [docs/spec/observability.md](../../spec/observability.md)  
**Zależy od:** 0.24 `/ready` + `fetchHealth`

**Status:** Zamknięty `/noc` 2026-09-02. Nie OTel. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane:** M-58–M-60 nie mają żywej nazwy w PLAN. Fala 10 portale **parked**. Następna nazwana nie-parked pozycja = **M-68 obserwowalność**. Operator widzi status API (`fetchHealth`) na `/health`. Token `observability` (UI, nie tabela). `/` pulpit zostaje jobami. Nie OTel.

**Łowca:** BRAK `span` / `trace` / OTel. ISTNIEJE: `fetchHealth`, `/ready`. PODOBNE: pulpit healthLabel.

**Odrzucone:**

- Temporal / OTel / Hatchet — cel, nie runtime (AGENTS).
- Zgadywanie nazwy M-58.
- Portale (parked).
- Druga sonda poza `fetchHealth`.

## Zakres

Read-only `/health`. `data-observability="board"`. `fetchHealth`. Link do `/`.

## Poza zakresem

OTel · k6 · mapa · ExtractionService

## Ustalenia

- Token: `observability` (GLOSSARY, UI).
- OpenFGA: health już publiczny / istniejące.
- RLS: bez nowej tabeli.
- Job: `/health`. `SHIPPED_CHARGE_ROUTES["48.0"]` = `/health`. OPS_JOBS +1 → 45.
- `just dup` ≤ 3% (2,84%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/health` ma `data-observability="board"` i woła `fetchHealth`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["48.0"]` = `/health`
- [x] `just dup` ≤ 3% (2,84%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | status API; brak OTel |
| Szybkość | N/A ten plaster | health |
| Dług w diffie | leftover | OTel |
| Docs/OS | PRZESZŁO | spec `observability.md`; CURRENT → Plan M-69 |

## Pytania

Brak. OTel leftover. M-58 bez nazwy świadomie pominięte.
