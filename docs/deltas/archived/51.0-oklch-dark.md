# Plaster 51.0 · U-oklch-dark (tokeny)

**Spec źródłowa:** [docs/spec/oklch-dark.md](../../spec/oklch-dark.md)  
**Zależy od:** ADR-0003 + `frontend/src/index.css`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 10. Nie nowa tabela. Nie zmiana jobów operatora.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN § Wave FE leftover, po zamknięciu Fali 11):** hex w `:root` → `oklch()`. Klasa `.dark` z parą tło/tekst AA. Tokeny zostają w `index.css`. OS `prefers-color-scheme: dark` dodaje klasę na `html`. Żywy ID leftover **U-oklch-dark**, plaster **51.0**.

**Łowca:** BRAK `.dark` / `oklch(` przed 51.0. ISTNIEJE: paleta ADR-0002 w `index.css`; test `:focus-visible`.

**Odrzucone:**

- Nowa tabela / ustawienie motywu w bazie.
- Druga paleta poza `index.css`.
- Base UI.

## Zakres

`index.css`: `oklch()`, blok `.dark`. `main.tsx` dodaje `.dark` z `prefers-color-scheme`. Test w `a11y.test.ts`. Zero migracji. Zero nowej trasy.

## Poza zakresem

U-money-align · U-condensed · Playwright axe · mapa

## Ustalenia

- Tokeny zostają CSS, nie tabela.
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy. `SHIPPED_CHARGE_ROUTES` bez 51.0.
- `just dup` ≤ 3% (2,85%).
- LLM nie liczy marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `:root` i `.dark` używają `oklch(` na parach tło/tekst
- [x] Test pali hex-only na tych parach i brak `.dark`
- [x] Brak nowej migracji i nowej trasy
- [x] `just dup` ≤ 3% (2,85%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | oklch + `.dark`; OS prefers-color-scheme |
| Szybkość | N/A ten plaster | CSS |
| Dług w diffie | leftover | U-money-align |
| Docs/OS | PRZESZŁO | spec `oklch-dark.md`; CURRENT → Plan U-money-align |

## Pytania

Brak. U-money-align leftover.
