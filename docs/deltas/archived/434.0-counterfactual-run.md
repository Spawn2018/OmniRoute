# Plaster 434.0 · AI1.2 `counterfactual_run` HITL

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.2

**Zależy od:** 433.0 `outcome_ledger` (AI1.1 DONE)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `counterfactual_run` per tenant — append-only HITL. Wiersz = `run_code` (snake 2–32, nazwany scenariusz) + `baseline_label` (punkt odniesienia, tekst 1–256) + `levers_label` (przestawione dźwignie, tekst 1–256) + `result_label` (wynik jako dana, tekst 1–256, nie kwota) + `source_ref`. Operator wpisuje przebieg „co by było gdyby”. Serwis nie woła LLM i nie liczy what-if.
- **Wybrane:** etykiety jako dane — odtwarzalność to ten sam wiersz, nie silnik. Kwota oszczędności zostaje w AI1.3 `benefit_ledger`.
- **Odrzucone:** rozszerzenie `what_if_mark` — tam rodzaj scenariusza (`fuel|port|bankruptcy`), nie przebieg.
- **Odrzucone:** rozszerzenie `plan_snapshot` / `circle_sim` — migawka planu i kółko to inne BC.
- **Odrzucone:** silnik AI4.1 · JSON dźwigni · FK · `benefit_ledger` · słowniki AI1.4 · trzy BC autosave.
- **Odrzucone:** kwota / marża / float · UPDATE/DELETE · zapis LLM.

**Łowca:** PODOBNE `plan_snapshot` (kod + dane, nie silnik). PODOBNE `what_if_mark` (rodzaj, nie przebieg). PODOBNE `circle_sim` (nazwany przebieg HITL). BRAK `counterfactual_run` w backend i frontend.

## Zakres

HITL `counterfactual_run`. Migracja `344` (revises `343_outcome_ledger`). UI `/counterfactual-runs`.
OpenFGA `can_manage_counterfactual_runs`. `SHIPPED["434.0"]`. OPS_JOBS length 251.

## Poza zakresem

silnik what-if (AI4.1) · benefit_ledger · słowniki AI1.4 · trzy BC autosave · FK · JSON dźwigni · mutate plan_snapshot / circle_sim / what_if_mark

## Kryteria akceptacji

- [x] POST z run_code, baseline_label, levers_label, result_label, source_ref
- [x] amount / margin / extra pole → 422; zły run_code / source_ref / pusta etykieta → 400
- [x] izolacja RLS; REQUIRED `/counterfactual-runs`
- [x] OPS_JOBS length 251; SHIPPED `"434.0"`
- [x] craft_style funkcje>40: 0

## Leftover

AI1.3 benefit_ledger · AI1.4 słowniki · trzy BC piszą do substratu · silnik AI4.1 · CRPS liczone (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL katalog + RLS + UI + OpenFGA |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | list/order bez nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | analog outcome_ledger; same etykiety |
| Proza operatora | 5–15 zdań | `docs/operator/counterfactual-run.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI billing limit jak 433 |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
