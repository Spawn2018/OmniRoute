# Plaster 220.0 · T3 `voyage_no` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [219.0](../archived/219.0-container-vessel.md) leftover `voyage_no` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 219.0 `vessel_name`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `voyage_no`/PIN/VGM/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `voyage_no` (1–32 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`rejs`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["220.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `159_container_voyage`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `vessel_name`. BRAK `voyage_no`. BRAK PIN. BRAK VGM. BRAK `booking_no`.

**Odrzucone:**

- `booking_no` / live HTTP armatora.
- `pin_code` / ciphertext.
- VGM / TARA.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `159_container_voyage` (po `158_container_vessel`): `voyage_no` nullable `String(32)`.
2. POST opcjonalny `voyage_no`. Zły token → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `voyage_no` zapisuje numer rejsu, bez PIN i bez booking
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`rejs`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `voyage_no`; 400 `rejs`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(32)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie booking; leftover T3 PIN/VGM/`remarks`; C901 czysty |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
