# Plaster 138.0 · O4 checkboxy + N× `mail_draft` + ranking SQL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O4 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § O4
**Zależy od:** 76.0 `mail_draft` · 3.0 `organization_setting` · 136.0 `carrier_inquiry` batch
**Status:** Zamknięty `/noc` 2026-09-08. Żywe ID **M-57** + **M-03** + odczyt M-30. Nie O5. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki O4):** ta sama tabela `mail_draft`. CHECK `subject_kind`: `extraction_draft` \| `carrier_inquiry`. Batch: N wierszy (jeden `subject_id` = `carrier_inquiry.id`), treść z requestu, nie LLM. Ustawienie `inquiry_default_n` (int 1–20) w M-03. Ranking SQL na `carrier_inquiry`: `COUNT(*) FILTER (status=answered)` per `network_member_id`, zero na końcu. UI `/networks`: zaznacza top N z rankingu; „Zapisz szkice”. Job `/mail` + `/networks`. OpenFGA bez nowej relacji. Send zostaje 81.0 `mailto:` — nie w tym plasterze.

**Łowca:** ISTNIEJE `mail_draft` (tylko extract). ISTNIEJE 136.0 checkbox + batch inquiry. ISTNIEJE M-03 allowlista kluczy. PODOBNE POST pojedynczy szkicu. BRAK `carrier_inquiry` na szkicu, default N, rankingu SQL, batchu szkiców.

**Odrzucone:**

- Tabela `party_lane_scorecard` (O5).
- Jeden szkic z wieloma `To`.
- Auto-send / Graph HTTP.
- Treść z LLM.
- Import `app.services.quotations` / `mail_drafts` w serwisie buy.

## Zakres

1. Migracja `080_mail_draft_inquiry`: CHECK subject_kind.
2. POST `/mail-drafts/batch`. GET ranking na `/carrier-inquiries/ranking`.
3. Klucz `inquiry_default_n` w M-03.
4. UI `/networks` + lista szkiców. `SHIPPED_CHARGE_ROUTES["138.0"]` = `/networks`.
5. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

O5–O8 · S18 send · Graph · NBP

## Kryteria akceptacji

- [x] `subject_kind=party` → 400
- [x] Batch trzech inquiry = trzy szkice `draft`
- [x] `inquiry_default_n=0` → 400
- [x] Ranking: więcej `answered` wyżej; zero na końcu
- [x] Serwis mail_drafts bez importu quotations / HTTP
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | party=400; batch 3 szkice; N=0 → 400; ranking SQL answered |
| Szybkość | N/A ten plaster | GROUP BY bez budżetu AGENTS |
| SQL | leftover | nowy SELECT rankingu; bez EXPLAIN w tej sesji |
| N+1 | OK | `create_batch` jeden `add_many` / flush |
| Dług / człowiek | OK | ta sama tabela; bez importu quotations |
| Proza operatora | 5–15 zdań | `katalogi.md` wtopione, 40 linii |
| Docs/OS | PRZESZŁO | spec M-57 + M-03 · CURRENT · `just docs` |
| Pushy do zielonego | N/A | billing GitHub zablokowany |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
