# Plaster 258.0 · T2c `subcontractor_party_id` na `trip`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover T2c · [257.0](257.0-trip-actual-distance.md) leftover `party` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 · [242.0](242.0-container-carrier-party.md) wzór FK
**Zależy od:** 257.0 `actual_distance_km`
**Status:** Zamknięty `/noc` 2026-09-10. Nie `/fleet`. Nie GPS. Nie C8 409. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2c `party`/`/fleet` + leftover T1b + leftover `appointment_status` + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover `/fleet` parked. leftover T1b parked. leftover `appointment_status` / `no_show_at` parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. leftover C8 409 parked. Żywy plaster: **pogłębienie istniejącej** `trip` — opcjonalny `subcontractor_party_id` (UUID HITL podwykonawcy). Złożone FK `(organization_id, subcontractor_party_id)` → `party` jak `container.carrier_party_id`. POST bez pola zostaje `null`. Nieznany UUID → 404. Zły token → 400 (`podwykonawca`). `require_trip_subcontractor_party_id` w `app.domain.trip` — kształt `require_carrier_party_id`, komunikat `podwykonawca`, nie import `container`. API składa `PartyService.get_party` (wzorzec 242.0) — **TripService nie importuje `parties`**. UI `/shipments` panel przejazdu. `SHIPPED_CHARGE_ROUTES["258.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `194_trip_subcontractor` (po `193_trip_actual_distance`). `record_trip` zostaje ≤ 40 linii. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.carrier_party_id` (242.0, bind w API). ISTNIEJE `require_trip_resource_id` (UUID opcjonalny, inny komunikat). BRAK `trip.subcontractor_party_id`. BRAK `/fleet`.

**Odrzucone:**

- `/fleet` / HW / mapa / GPS / live HTTP.
- Import `parties` w `TripService`.
- 409 C8 / `party_document` / scoring osoby / T5 / S53 / float.

## Zakres

1. Migracja `194_trip_subcontractor`: `subcontractor_party_id` nullable UUID + FK `(organization_id, subcontractor_party_id)` → `party`. Nie `create_table`.
2. POST opcjonalny. Nieznany → 404. Zły token → 400 (`podwykonawca`). Serwis nie importuje parties/charges i nie woła HTTP.
3. UI `/shipments` panel przejazdu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`/fleet` · GPS · mapa · C8 409 · T7b · T5 · Auth0 · live HTTP · float · scoring

## Kryteria akceptacji

- [x] POST z UUID istniejącego `party` zapisuje FK, bez live HTTP i bez `/fleet`
- [x] POST bez pola zostaje `null`
- [x] nieznany UUID → 404; zły token → 400 (`podwykonawca`)
- [x] RLS istniejącego `trip`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `parties` w serwisie

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny FK; 404 nieznany; 400 `podwykonawca`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | FK złożone; brak nowego SELECT resolve w serwisie |
| N+1 | OK | GET jedna lista; POST = get_party + INSERT |
| Dług / człowiek | OK | wzór 242.0 bind w API; leftover `/fleet`; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T2, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
