# Plaster 608.0 · T3 EXP1 `vessel_imo` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover EXP1 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container · [607.0](../archived/607.0-container-grade.md)
**Zależy od:** 153.0 `container`; 219.0 `vessel_name`
**Status:** Zamknięty `/noc` 2026-09-21. Nie AIS. Nie alliance.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 EXP1 `vessel_imo`/`alliance_service` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** leftover `alliance_service` parked. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `vessel_imo` (`String(16)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Nie-tekst / > 16 → 400 (`imo`). `require_vessel_imo` w `app.domain.container` — wzór `require_voyage_no`, **nie** cyfra kontrolna IMO, **nie** AIS / live class. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["608.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `483_container_vessel_imo` (po `482_container_grade`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.vessel_name` (219.0). ISTNIEJE `container.voyage_no` (220.0). ISTNIEJE `container.grade` (607.0). BRAK `container.vessel_imo`. BRAK `alliance_service`.

**Odrzucone:**

- `alliance_service` / HW / UXCL / N3 / AI3 / G0-SH / cyfra kontrolna IMO / AIS / float / marża / mapa / Auth0.

## Zakres

1. Migracja `483_container_vessel_imo`: `vessel_imo` nullable String(16). Nie `create_table`.
2. POST opcjonalny `vessel_imo`. Zła wartość → 400 (`imo`). Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

alliance_service · AIS · cyfra kontrolna · HW · UXCL · N3 · mapa · Auth0 · live HTTP · float

## Kryteria akceptacji

- [x] POST z `vessel_imo` zapisuje IMO HITL, bez AIS
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / > 16 → 400 (`imo`)
- [x] RLS istniejącego `container`; izolacja bez nowej tabeli
- [x] Zero float / marży / AIS / import charges

## Leftover

`alliance_service` · HW · UXCL · N3 · AI3 · G0-SH

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `vessel_imo`; 400 `imo`; pusty → null; CI po push |
| Szybkość | N/A | brak ścieżki z budżetu AGENTS |
| SQL | N/A | add_column; bez nowego SELECT |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | `_box_unchanged` ≤40; wzór voyage_no |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
