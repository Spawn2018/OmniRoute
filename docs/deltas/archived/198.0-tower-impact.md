# Plaster 198.0 · V6 `tower_impact` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) § V6 · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md) § `impact_scenario` / `sla_clause` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP0.1
**Zależy od:** 0.3 tenancy (katalog per tenant) · 94.0 `/watchtower` (tablica wyjątków zostaje odczytem)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie scoring osoby. Nie silnik EBITDA.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover V6 stock→EBITDA + CI5 `sla_clause` + V8 what-if + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b `exchange_message` parked. leftover V4 AIS wieży (S32) parked. leftover V2b myto→`charge` parked. leftover C3 live VIES parked. leftover C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO parked. leftover F2b–F4 parked. leftover `circle_sim` parked. leftover kg parked. F1 / C1 / C8 409 parked. leftover N3 countdown parked. Tabela `sla_clause` i kara SQL zostają leftover CI5 (po CI9). Silnik stock→produkcja→sprzedaż→EBITDA i „co jeśli” zostają leftover V6b / V8. Żywy plaster: jedna tabela `tower_impact` per tenant — **HITL znacznik etapu łańcucha jako dane**, nie wieża EBITDA i nie scoring. Wiersz = `chain_stage` (`stock` | `production` | `sales` | `ebitda`) + `contract_data_status` (`missing` | `recorded`) + `source_ref` (`tenant:manual` albo `fixture://tower-impact/…`). `missing` pokazuje operatorowi stały tekst **„brak danych umowy”** — bez odczytu `sla_clause` i bez kary. Operator wpisuje etap i status umowy; serwis nie mnoży EBITDA, nie woła HTTP, nie woła LLM, nie czyta `charge` / `party_scorecard` / `customer_contract` i nie składa `/watchtower`. Zły etap → 400 (`etap`). Zły status umowy → 400 (`umowa`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/tower-impacts` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["198.0"]` = `/tower-impacts`. OpenFGA: `can_manage_tower_impacts`. Migracja `138_tower_impact`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `/watchtower` 94.0 / 116.0 / 124.0 — tablica wyjątków + pending S11 + liczniki, **nie** łańcuch skutków. ISTNIEJE `operational_exception` — wyjątek na zleceniu, **nie** etap stock/EBITDA. ISTNIEJE `party_scorecard` / `party_lane_scorecard` — snapshot oferty, **nie** scoring osoby i **nie** impact. ISTNIEJE `prediction_ledger` — CRPS/MAE, **nie** EBITDA. ISTNIEJE `entity_event` — append-only job, **nie** łańcuch. ISTNIEJE pojęcie `sla_clause` w GLOSSARY — CI, **brak tabeli**. BRAK tabeli `tower_impact`. BRAK `impact_scenario`. BRAK `sla_clause`. BRAK kalkulatora EBITDA.

**Odrzucone:**

- Silnik stock→produkcja→sprzedaż→EBITDA i mnożenie w Pythonie / SQL / JS.
- Tabela `sla_clause` / `customer_contract` / kara / ciphertext (leftover CI5).
- What-if na `plan_snapshot` (leftover V8).
- Scoring osoby / karta kontrahenta jako „impact”.
- Klejenie CT2 / auto „zatrzymaj produkcję”.
- Wpisywanie łańcucha w kafelki `/watchtower` (tablica wyjątków zostaje).
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `138_tower_impact` (po `137_telematics_connector`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist stage + status umowy.
2. POST/GET. Zły etap / status → 400. GET przy `missing` niesie tekst „brak danych umowy”. Serwis nie importuje charges / parties / extraction / watchtower i nie woła HTTP.
3. UI `/tower-impacts`. `just api-types`. How-to `docs/operator/wieza-impact.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `ImpactDesk` / `impactWrite` / `persistImpactMark`.

## Poza zakresem

silnik EBITDA · sla_clause / kara CI5 · what-if V8 · scoring osoby · kafelki watchtower · live GPS · AIS · myto → charge · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [x] POST zapisuje `tower_impact` z `chain_stage` i `contract_data_status`, bez HTTP
- [x] zły `chain_stage` → 400 (`etap`)
- [x] zły `contract_data_status` → 400 (`umowa`)
- [x] `missing` → odpowiedź zawiera „brak danych umowy”
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / scoringu / live HTTP / scrape / EBITDA

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST etap + status; 400 `etap` / `umowa` / `obce`; GET `missing` → „brak danych umowy” |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_tower_impact_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover silnik EBITDA / sla_clause / V8; formularz radio (nie select) — jscpd < 3% |
| Proza operatora | 5–15 zdań | `wieza-impact.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN V6, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
