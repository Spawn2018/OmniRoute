# Plaster 80.0 · S17 ingest IMAP na inbound_message (M-32)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S17  
**Zależy od:** 64.0 / 78.0 `inbound_message` · 79.0 outbox · 0.3 RLS

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID M-32. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-32**, ta sama tabela. Ingest IMAP to zapis **już pobranej** wiadomości: `source_ref` zaczyna się od `imap://`, `external_id` ten sam unikat per tenant co Graph. Drugi POST z tym samym `external_id` zwraca istniejący wiersz. Status `draft`. OpenFGA: `can_manage_inbound_messages`. Ekran `/mail`: formularz ingest skrzynki. Serwis nie woła IMAP/EmailEngine — w pliku serwisu nie ma słowa `imap`. API poczty składa outbox `inbound_message_saved`. Sekret skrzynki nie wchodzi do bazy.

**Łowca:** ISTNIEJE `external_id` + `graph://` (78.0). ISTNIEJE ingest-graph + outbox (79.0). BRAK `imap://` / EmailEngine. PODOBNE: ingest Graph.

**Odrzucone:**

- Live IMAP / EmailEngine HTTP.
- Zmiana Graph / drugi `external_id`.
- Send (S18).
- Nowa tabela / nowy żywy ID.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `040_inbound_imap_ingest`: CHECK dopuszcza `imap://`.
2. `POST /inbound-messages/ingest-imap` + outbox.
3. UI `/mail`: formularz ingest skrzynki.

## Poza zakresem

Live IMAP · EmailEngine · sekret · send · F9.1

## Ustalenia

- Żywy ID: **M-32**. Token: `inbound_message`.
- HITL extract bez zmian. LLM nie liczy.
- How-to: `docs/operator/katalogi.md`.

## Schemat

CHECK `source_ref ~ '^(fixture|synth|graph|imap)://'`.

## Kryteria akceptacji

- [x] `imap://` przechodzi; live host/http nie
- [x] Dwa ingest z tym samym `external_id` = jeden wiersz
- [x] Tenant B nie widzi A
- [x] Serwis inbound bez `imap` / `httpx` / `emailengine` w pliku
- [x] Brak sekretu w diffie
- [x] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | ten sam `external_id` = jeden wiersz |
| Szybkość | N/A ten plaster | ingest, nie wycena |
| SQL | N/A | CHECK + lookup `external_id` |
| N+1 | OK | jeden lookup, jeden zapis |
| Dług / człowiek | OK | live skrzynka leftover; send S18 |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT, `just docs` |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
