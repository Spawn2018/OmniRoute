# Plaster 79.0 · S16 outbox, zdarzenie wiadomość zapisana (M-02)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S16  
**Zależy od:** 64.0 / 78.0 `inbound_message` · 0.3 RLS · 0.4 OpenFGA

**Status:** Zamknięty `/noc` 2026-09-03. Żywy ID **M-02**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** żywy ID **M-02**, tabela `outbox_event` per tenant. Pierwsze zdarzenie: `event_kind = inbound_message_saved` („wiadomość zapisana”). `subject_id` = UUID wiadomości **bez FK** do `inbound_message` (skład w API, nie import inbound w serwisie outbox). Status tylko `pending` — nie ma konsumenta. Unikat `(organization_id, event_kind, subject_id)`: ten sam zapis wiadomości = ten sam wiersz. Zapis zdarzenia składa **API poczty** po create fixture albo ingest `graph://` — nie `InboundMessageService` (zakaz importu innych BC). OpenFGA: `can_manage_outbox_events` = member. Ekran `/outbox`: lista. How-to: `katalogi.md`.

**Łowca:** BRAK tabeli `outbox` / `outbox_event` / `event_kind`. ISTNIEJE: 64.0/78.0 zapis `inbound_message` (to jest zdarzenie). ISTNIEJE: 74.0 `subject_id` bez FK. PODOBNE: unikat pending na decyzji — tu unikat zdarzenia na subject. 0.4 to OpenFGA, nie outbox.

**Odrzucone:**

- Temporal / Hatchet / worker / dispatcher (teatr bez konsumenta).
- JSON `payload` (zgadywanie schematu; wystarczy `subject_id`).
- Import `inbound_messages` w serwisie outbox albo outbox w serwisie inbound.
- Status `processed` / `published` (brak konsumenta).
- FK do `inbound_message`.
- Live HTTP / send / IMAP.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `039_outbox_event_rls`: tabela + RLS FORCE + unikat zdarzenia. Test izolacji.
2. `GET/POST /outbox-events`. Create z API poczty po zapisie wiadomości. Ponowny ten sam `subject_id` = ten sam wiersz.
3. UI `/outbox`: lista. Nie licz w JS. Nie dispatch.

## Poza zakresem

Konsument · Temporal · send · IMAP · payload JSON · F9.1

## Ustalenia

- Żywy ID: **M-02**. Token: `outbox_event`.
- HITL extract bez zmian. LLM nie liczy. `charge` zostaje prawdą o marży.
- How-to: `docs/operator/katalogi.md`.
- Konsument / dispatch = leftover z „dlaczego”: nie ma odbiorcy poza HTTP.

## Schemat

`outbox_event`: `id`, `organization_id`, `event_kind` (`inbound_message_saved`), `subject_id` UUID, `status` (`pending`), `source_ref`, timestamps. Unique `(organization_id, event_kind, subject_id)`.

## Kryteria akceptacji

- [x] RLS FORCE + izolacja; B nie widzi A
- [x] Create wiadomości / ingest Graph składa jeden `inbound_message_saved`
- [x] Drugi zapis tej samej wiadomości = ten sam wiersz outbox
- [x] Serwis outbox nie importuje inbound / quotations / extraction
- [x] Brak Temporal / worker / `httpx` w serwisie
- [x] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | ten sam subject = jeden wiersz; B nie widzi A |
| Szybkość | N/A ten plaster | zapis zdarzenia, nie wycena |
| SQL | N/A | SELECT po kind+subject + INSERT; bez JOIN |
| N+1 | OK | jeden lookup, jeden zapis |
| Dług / człowiek | OK | konsument leftover; IMAP S17; send S18 |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT, `just docs` |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
