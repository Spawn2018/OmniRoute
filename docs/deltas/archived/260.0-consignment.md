# Plaster 260.0 · N1 `consignment`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N1 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) § N1 · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D2 (paczka ≠ przesyłka)
**Zależy od:** 90.0 `shipment` · 150.0 `stop` (poza zapisem) · 156.0 `shipment_package` (nie FK w tym plasterze)
**Status:** Zamknięty `/noc` 2026-09-10. Nie mapa. Nie SQL na `charge`. Nie WMS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover N1 `consignment` + leftover T1b + leftover T6 mapa + leftover T4b SQL `charge` + leftover T5 + leftover T7b + leftover N3 + S53 + PIN):** leftover T1b parked. leftover T6 parked. leftover T4b parked. leftover T5 parked. leftover T7b parked. leftover N3 parked. S53 parked. PIN parked. leftover FTL=1 (409 drugiego wiersza przy FTL) parked — na `shipment` nie ma trybu FTL/LTL. leftover D2b FK paczki na przesyłkę parked. leftover wiązanie `stop` parked. Żywy plaster: **nowa tabela** `consignment` per tenant obok `shipment` — HITL przesyłka (Qargo: obiekt między zleceniem a punktem), nie paczka i nie podzlecenie. Wiersz = `consignment_ref` (2–64, `[A-Za-z0-9._:-]+`) + FK złożone `(organization_id, shipment_id)` → `shipment` + `source_ref` (`tenant:manual` albo `fixture://consignment/…`). N wierszy na jedno zlecenie (LTL/LCL=N). Unique `(organization_id, consignment_ref)` i `(organization_id, source_ref)`. POST bez `shipment_id` → 400 (`zlecenie`). Nieznany UUID zlecenia → 404. Zły token UUID → 400 (`zlecenie`). Zły `consignment_ref` → 400 (`przesyłka`). Obce `source_ref` → 400 (`obce`). GET listy SQL. API składa `ShipmentService` — **ConsignmentService nie importuje shipments / stops / packages / charges**. UI `/consignments`. `SHIPPED_CHARGE_ROUTES["260.0"]` = `/consignments`. Nowy job OPS (108). OpenFGA: `can_manage_consignments`. Migracja `195_consignment` (po `194_trip_subcontractor`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `shipment` — zlecenie z wyceny, **nie** przesyłka N1. ISTNIEJE `parent_shipment_id` (210.0) — drzewo zleceń, **nie** consignment. ISTNIEJE `shipment_package` — sztuka QR na zleceniu, **nie** grupa przesyłki. ISTNIEJE `stop` — punkt trasy, **nie** consignment. ISTNIEJE `ocean_bill` UI `consignmentToken` = alias `shipment_id`, **nie** tabela N1; nie ruszamy. ISTNIEJE słownik `consignment` — **nie** zbudowane. BRAK tabeli `consignment`. BRAK `/consignments`.

**Odrzucone:**

- unique `(organization_id, shipment_id)` (FTL=1) — zablokowałoby LTL/LCL=N.
- FK `shipment_package.consignment_id` / przenoszenie paczek.
- `stop_id` na przesyłce / mapa / T6.
- SQL marży na `charge` / widok T4b.
- Kwota / Decimal / float / live HTTP / WMS / GPS.
- Zmiana `consignmentToken` na `/ocean-bills`.

## Zakres

1. Migracja `195_consignment`: RLS FORCE, izolacja, indeks `(organization_id, shipment_id)`, unique ref + `source_ref`. Nie kolumny na `shipment`.
2. POST/GET. Nieznany `shipment_id` → 404. Zły token → 400 (`zlecenie` / `przesyłka` / `obce`). Serwis nie importuje shipments/charges i nie woła HTTP.
3. UI `/consignments`. `just api-types`. How-to `docs/operator/przesylka.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Job OPS 108. OpenFGA `can_manage_consignments`.

## Poza zakresem

FTL=1 409 · D2b FK paczki · `stop` na przesyłce · T6 mapa · T4b SQL `charge` · T5 · T1b · Auth0 · live HTTP · WMS · kwota · `consignmentToken` ocean

## Kryteria akceptacji

- [x] POST zapisuje `consignment` na zleceniu tenanta, N wierszy na `shipment` wolno
- [x] nieznany `shipment_id` → 404; zły token → 400 (`zlecenie` albo `przesyłka`)
- [x] GET zwraca przesyłki tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / mapy / live HTTP / unique FTL=1 / zapisu `shipment_package`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET N na zlecenie; 404 nieznany; 400 `zlecenie`/`przesyłka`/`obce`; izolacja |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN leftover | indeks `ix_consignment_org_shipment`; lista RLS; EXPLAIN ANALYZE nie w tym plasterze |
| N+1 | OK | GET jedna lista; POST = get_shipment + INSERT |
| Dług / człowiek | OK | wzór ocean_bill bind w API; leftover FTL=1 / D2b / stop |
| Proza operatora | 5–15 zdań | `przesylka.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN N1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

Import API: `consignments` przed `containers` (ruff I001). Lista UI nie używa `ParcelRows` z D2 — próg `just dup` 3,0%.
