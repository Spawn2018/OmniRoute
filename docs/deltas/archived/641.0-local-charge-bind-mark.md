# Plaster 641.0 · leftover P4c bind stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 · [166.0](../archived/166.0-local-charge.md) · [640.0](../archived/640.0-local-charge-match-mark.md) · [635.0](../archived/635.0-local-charge-warning-mark.md)
**Zależy od:** 640.0 `local_charge_match_mark` już jest
**Status:** Zamknięty `/noc 21` 2026-09-23. Nie FK UUID. Nie warning-jako-fakt. Nie matching SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P4c „bind charge|quote”):** katalog `local_charge` zostaje prawdą o dopłacie Decimal. Warning stance = `local_charge_warning_mark` (635). Matching stance = `local_charge_match_mark` (640). Silnik matching SQL / warning-jako-fakt / FK UUID do `charge`/`quotation` / 409 wyceny zostają park. Żywy plaster: **nowa tabela** `local_charge_bind_mark` per tenant — HITL stance wiązania dopłaty lokalnej. Wiersz = `mark_code` (snake 1–32) + `bind_kind` (`charge`|`quote`|`other`) + `source_ref` (`tenant:manual` albo `fixture://local-charge-bind/…`). Operator zapisuje, do czego odnosi dopłatę lokalną (wiersz `charge` / wycena / inne); serwis **nie** wiąże UUID, nie czyta `local_charge` / `charge` / `quotation` i nie zwraca 409. Zły kind → 400 (`wiązanie`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `local_charges` / `local_charge_warning_marks` / `local_charge_match_marks` / charges / quotations. UI `/local-charge-bind-marks`. `SHIPPED_CHARGE_ROUTES["641.0"]` = `/local-charge-bind-marks`. OpenFGA: `can_manage_local_charge_bind_marks`. Migracja `513_local_charge_bind_mark` po `512_local_charge_match_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 166.0 / 640.0 P4c bind stance — tu tylko HITL stance, nie FK i nie silnik. Źródło: `docs/deltas/archived/166.0-local-charge.md`; PLAN wiersz P4; `docs/ops/docs-debt.md` § 640.0.

**Łowca:** ISTNIEJE `local_charge` — dopłata Decimal, nie stance. ISTNIEJE `local_charge_warning_mark` / `local_charge_match_mark` — warning/match, nie bind. ISTNIEJE `handover_bind_mark` / `sales_bind_mark` — wzorzec stance bind. BRAK `local_charge_bind_mark`. BRAK `/local-charge-bind-marks`. BRAK FK UUID charge/quote.

**Odrzucone:**

- FK UUID do `local_charge` / `charge` / `quotation`
- matching SQL / warning-jako-fakt / 409 na wycenie
- auto-linia `charge` / float / marża / live HTTP

## Zakres

1. Migracja `513_local_charge_bind_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `bind_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/local-charge-bind-marks` + zdanie w how-to dopłat lokalnych.

## Poza zakresem

FK UUID · matching SQL · warning-jako-fakt · 409 wyceny · float · live HTTP

## Plan plików

- `backend/alembic/versions/513_local_charge_bind_mark.py`
- `backend/app/models/local_charge_bind_mark.py`
- `backend/app/domain/local_charge_bind_mark.py`
- `backend/app/repositories/local_charge_bind_marks/`
- `backend/app/services/local_charge_bind_marks/` (+ AGENTS.md)
- `backend/app/api/local_charge_bind_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/local-charge-bind-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `bind_kind=charge` zapisuje wiersz; `local_charge` i `charge` bez zmian
- [x] kind spoza `charge`|`quote`|`other` → 400 (`wiązanie`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `local_charges` / `charges` / `quotations` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / FK UUID

## Leftover

warning-jako-fakt (compose) · matching SQL · FK charge/quote · P5c · live HTTP

## Karta po-plastrze

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kryteria delty |
| Szybkość | N/A | |
| SQL | N/A | list/insert katalog |
| N+1 | OK | |
| Dług / człowiek | OK | wzorzec bind stance |
| Proza operatora | 5-15 | how-to bind + zdanie w oplata-lokalna |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
