# Plaster 530.0 · N2 — HITL `trip_bill_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N2 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) · po T2 [152.0](../archived/152.0-trip.md) · po [529.0](../archived/529.0-handover-sbar-mark.md)

**Zależy od:** tenancy RLS · `trip` (poza zapisem) · F1 live KSeF zostaje park

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `trip_bill_marks`. Tabela `trip_bill_mark`: `mark_code` + `bill_kind` `ready`|`held`|`billed`|`other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja N2 — **znacznik gotowości przejazdu do fakturowania jako dana HITL**, nie lista SQL na `charge` i nie live KSeF.
- **Wybrane:** POST/GET `/api/v1/trip-bill-marks`. OpenFGA `can_manage_trip_bill_marks`. UI `/trip-bill-marks`. `SHIPPED["530.0"]`.
- **Wybrane:** how-to `docs/operator/trip-bill-mark.md`. Alembic `420_trip_bill_mark` (`down_revision = "419_handover_sbar_mark"`).
- **Odrzucone:** widok SQL `trips_to_bill` łączący `trip`×`charge` (`REJECTED` na tym wierszu — leftover; druga marża ryzykiem).
- **Odrzucone:** F1 live FA(3) / KSeF HTTP · auto INSERT `sales_invoice` · UPDATE `trip.status` (`REJECTED` — park F1 / leftover).
- **Odrzucone:** FK do `trip` / `sales_invoice` · kwota / marża / float / UPDATE/DELETE.

**Łowca:** ISTNIEJE `trip`. ISTNIEJE `sales_invoice`. ISTNIEJE `collective_invoice`. PODOBNE katalog HITL stance. BRAK `trip_bill_mark`. BRAK `trips_to_bill`.

## Zakres

HITL `trip_bill_mark` — migracja 420 + warstwy + OpenFGA + FE + how-to + SHIPPED 530.0.

## Poza zakresem

widok SQL trips×charge · F1 KSeF live · auto FV · FK trip · matching do FV

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zły kind / obce source_ref → 400; extra amount → 422
- [x] UI `/trip-bill-marks`; `SHIPPED["530.0"]`
- [x] craft_style; how-to

## Leftover

widok SQL `trips_to_bill` · FK do `trip` · F1 live · auto linia FV
