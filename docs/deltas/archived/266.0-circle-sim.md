# Plaster 266.0 · G2.20 `circle_sim` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.19–G2.22 leftover `circle_sim` · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) § G2.19–G2.22 · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) (brak osobnej tabeli pól kółka — karta G2)
**Zależy od:** 0.3 tenancy · 185.0 `lane_pattern` (para origin/destination już jest — to nie ten obiekt)
**Status:** Zamknięty `/noc` 2026-09-10. Nie silnik ≥500k. Nie km. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.20–G2.22 kółka/km/P + leftover F9 Optima + leftover T8 + leftover what-if V8 + leftover 8 silników):** leftover km ładowny/pusty/dolot parked (G2.21 — Decimal na korytarzu, nie ten katalog). leftover P=`n_overlap`/`n_similar` i lista przewoźników parked (G2.22). leftover silnik ≥500k w Postgres parked. leftover F9 Optima fixture parked. leftover T8 parked. leftover V8 what-if parked. leftover fizyka twinów parked. Żywy plaster: **nowa tabela** `circle_sim` per tenant — HITL kółko jako dane, nie silnik nakładania i nie VRP. Wiersz = `sim_code` (snake 2–32) + `unload_unlocode` + `load_unlocode` (token UN/LOCODE 5 znaków przez istniejące `normalize_unlocode`, bez resolve `port`; karta: constraint unload A ∩ load B **jako para danych**, nie przecięcie w SQL) + `source_ref` (`tenant:manual` albo `fixture://circle-sim/…`). To nie jest `lane_pattern` (tam origin→destination korytarza). To nie jest `plan_snapshot` (tam wersja planu). To nie jest `twin_mark` kind `plan`. Operator wpisuje nazwane kółko i parę rozładunek/załadunek; serwis nie liczy km, nie woła HTTP, nie woła LLM, nie czyta `charge` / `trip` / `shipment` / `resource` / `tender` / `geography` i nie liczy przecięcia. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE (nie ledger append-only). Zły kod → 400 (`kolko`). Zły rozładunek → 400 (`rozladunek`). Zły załadunek → 400 (`zaladunek`). Te same końce → 400 (`para`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, sim_code)`, `(organization_id, source_ref)` i `(organization_id, unload_unlocode, load_unlocode)`. GET listy SQL. UI `/circle-sims`. `SHIPPED_CHARGE_ROUTES["266.0"]` = `/circle-sims`. OpenFGA: `can_manage_circle_sims`. Migracja `199_circle_sim` (po `198_plan_snapshot`). Job OPS 112. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `lane_pattern` 185.0 — para origin/destination UN/LOCODE, **nie** unload∩load kółka. ISTNIEJE `plan_snapshot` 265.0 — wersja planu, UUID bez FK, **nie** kółko. ISTNIEJE `twin_mark` 199.0 — kind m.in. `plan`, **nie** symulacja kółek. ISTNIEJE `normalize_unlocode` — format tokenu; **nie** drugi parser. ISTNIEJE `tender_lane` — para na partii przetargu. BRAK tabeli `circle_sim` przed tym plasterem. BRAK `/circle-sims`. BRAK silnika ≥500k. BRAK km ładowny/pusty/dolot.

**Odrzucone:**

- silnik `circle_sim` ≥500k w Postgres (leftover G2.20 engine).
- km ładowny / pusty / dolot Decimal (leftover G2.21).
- P=`n_overlap`/`n_similar` / lista przewoźników (leftover G2.22).
- LLM-VRP / copy „natychmiast” / p95 stage.
- what-if V8 / F9 Optima fixture / T8 / fizyka twinów.
- import trips / shipments / resources / charges / geography / tenders.
- kwota / marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `199_circle_sim`: RLS FORCE, izolacja, unique kod + `source_ref` + para UN/LOCODE, CHECK końce różne + format UN/LOCODE. Nie kolumny km. Nie silnik.
2. POST/GET. Zły token → 400 (`kolko` / `rozladunek` / `zaladunek` / `para` / `obce`). Serwis nie importuje trips/shipments/resources/charges/geography/tenders i nie woła HTTP. Brak UPDATE/DELETE.
3. UI `/circle-sims`. How-to `docs/operator/circle-sim.md`; fold w `katalogi.md` (40 linii). Job OPS 112. OpenFGA `can_manage_circle_sims`. Nazwy: `CircleDesk` / `circle-form.tsx` / `persistCircleSim`.

## Poza zakresem

silnik ≥500k · km ładowny/pusty/dolot · P overlap · lista P · LLM-VRP · F9 Optima · T8 · what-if · fizyka · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `circle_sim` z kodem, parą UN/LOCODE unload/load i `source_ref`, bez km i bez silnika
- [x] zły kod → 400 (`kolko`); zły UN/LOCODE → 400 (`rozladunek` / `zaladunek`); te same końce → 400 (`para`)
- [x] GET zwraca kółka tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / km / silnika 500k / LLM / live HTTP / importu trips/shipments/charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; RLS + unique kod/`source_ref`/para; zero km |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); EXPLAIN ANALYZE nie w tym plasterze |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela unique; leftover km / P / silnik 500k |
| Proza operatora | 5–15 zdań | `circle-sim.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.20, GLOSSARY, MODULES G2.20, spec |
| Pushy do zielonego | N/A | GitHub watch po pushu |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (266.0)

- Migracja `199_circle_sim` po `198_plan_snapshot`. RLS FORCE.
- `CircleSimService.persist_circle_sim` — katalog unique, bez importu trips/shipments/charges/geography/tenders.
- UI `/circle-sims`. `SHIPPED_CHARGE_ROUTES["266.0"]` = `/circle-sims`. Job OPS 112.

## Leftover po 266.0

km ładowny/pusty/dolot (G2.21). P=`n_overlap` / lista P (G2.22). silnik ≥500k. F9 Optima fixture. T8. what-if V8. 8 silników fizyki.
