# Plaster 463.0 · BR3.2 dyspozytor drobnicy

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR3.2 · D1 `groupage_line` · D5 `groupage_tariff`

**Zależy od:** tenancy RLS · wzorzec HITL `campaign_mark` (462.0) · katalog linii D1 już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `groupage_dispatcher_marks`. Tabela `groupage_dispatcher_mark`: `mark_code` + `dispatcher_kind` `line|hub|cutoff|consol|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — katalog stance dyspozytora, nie tablica robocza.
- **Wybrane:** POST/GET `/api/v1/groupage-dispatcher-marks`. OpenFGA `can_manage_groupage_dispatcher_marks` (member). UI `/groupage-dispatcher-marks`. `SHIPPED["463.0"]` → `/groupage-dispatcher-marks`.
- **Wybrane:** Alembic `368_groupage_disp_mark` (`down_revision = "367_campaign_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `groupage_line` / `groupage_tariff` (`REJECTED` — linia i cennik zostają katalogiem D1/D5, nie dyspozycją).
- **Odrzucone:** rozszerzenie `cutoff_mark` (`REJECTED` — EXP2.8 to cutoff booking/document/gate, nie dyspozytor).
- **Odrzucone:** klej FK do `groupage_line` / `shipment` / `consignment` / `stop` (`REJECTED` na tym wierszu).
- **Odrzucone:** silnik hubów · OR konsolidacji · matching cutoff SQL · live · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `groupage_line` (D1 linia LTL). ISTNIEJE `groupage_tariff` (D5 cennik). ISTNIEJE `cutoff_mark` (EXP2.8 cutoff). PODOBNE katalog HITL. BRAK `groupage_dispatcher_mark`. BRAK silnika hubów.

## Zakres

How-to: jedno zdanie — to katalog stance dyspozytora drobnicy; silnik hubów i konsolidacja zostają leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

silnik hubów · OR konsolidacji · matching cutoff · FK do linii · Valhalla BR3.0 leftover · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

silnik hubów · OR konsolidacji · matching cutoff · FK do `groupage_line` · BR3.1 załadunek

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS dwa tenanty; amount/engine 422/400 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | INSERT/SELECT katalogu |
| N+1 | OK | jeden SELECT listy |
| Dług / człowiek | OK | osobny BC obok `groupage_line`; bez FK |
| Proza operatora | 5–15 zdań | `docs/operator/groupage-dispatcher-mark.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–462 |
| Minuty plastra | 45 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
