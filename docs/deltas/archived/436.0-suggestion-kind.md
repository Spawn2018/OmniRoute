# Plaster 436.0 · AI1.4 `suggestion_kind` HITL (słownik otwarty)

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 435.0 `benefit_ledger` (AI1.3 DONE) · 432.0 `suggestion_ledger` (kolumna `suggestion_kind` zostaje CHECK)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `suggestion_kind` per tenant — append-only HITL. Wiersz = `kind_code` (snake 2–32, **bez** listy CHECK/ENUM) + `source_ref`. Nowy rodzaj podpowiedzi = INSERT, nie migracja (VISION B.3).
- **Wybrane:** pierwszy z czterech słowników AI1.4 — `suggestion_kind` jest wzorcem w B.3.
- **Odrzucone:** cztery tabele w jednym plasterze.
- **Odrzucone:** `data_source` (licencja, AI5) · `autonomy_level` (per klient) · tabela `twin_kind` (twin_mark + CHECK = leftover C.2).
- **Odrzucone:** jedna tabela z `axis` w CHECK · zmiana CHECK na `suggestion_ledger` / FK · JSON · UPDATE/DELETE · float.

**Łowca:** PODOBNE `twin_mark` (rodzaj z CHECK — nie rozszerzać). PODOBNE kolumna `suggestion_ledger.suggestion_kind`. BRAK tabeli słownikowej bez listy CHECK.

## Zakres

HITL `suggestion_kind`. Migracja `346` (revises `345_benefit_ledger`). UI `/suggestion-kinds`.
OpenFGA `can_manage_suggestion_kinds`. `SHIPPED["436.0"]`. OPS_JOBS length 253.

## Poza zakresem

twin_kind tabela · data_source · autonomy_level · FK z suggestion_ledger · zdjęcie CHECK · CRPS · silnik AI4.1

## Kryteria akceptacji

- [x] POST z kind_code (dowolny snake 2–32) + source_ref
- [x] extra amount/margin/score → 422; zły kod / source_ref → 400
- [x] brak CHECK `eta|rate|route` w migracji; izolacja RLS; REQUIRED `/suggestion-kinds`
- [x] OPS_JOBS length 253; SHIPPED `"436.0"`
- [x] craft_style funkcje>40: 0

## Leftover

tabele `twin_kind` / `data_source` / `autonomy_level` · zdjęcie CHECK na `suggestion_ledger` i `twin_mark` + FK · trzy BC piszą do substratu · silnik AI4.1 · CRPS liczone (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL otwarty słownik + RLS + UI + OpenFGA |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | list/order bez nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | analog counterfactual bez etykiet |
| Proza operatora | 5–15 zdań | `docs/operator/suggestion-kind.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI billing limit |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
