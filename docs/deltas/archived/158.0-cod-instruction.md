# Plaster 158.0 · D4 `cod_instruction`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D4 · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D4
**Zależy od:** 90.0 `shipment` · 92.0 `shipment_document` już jest (poza tym plasterem)
**Status:** Zamknięty `/noc` 2026-09-08. Nie kwota. Nie Fala F.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z D4):** jedna tabela `cod_instruction` per tenant — **znacznik pobrania na zleceniu**, bez kwoty. Rozliczenie pobrań = Fala F (`charge` zostaje prawdą o marży). Wiersz = `instruction_code` (snake 2–32) + FK złożone do `shipment` + `collection_status` (`noted` | `advised` | `collected` | `refused`) + `source_ref`. `collected` to status operacyjny, nie zapis pieniędzy. GET listy SQL. API składa odczyt `shipment`; serwis COD nie importuje shipments / charges / sales_invoices. UI `/cod`. `SHIPPED_CHARGE_ROUTES["158.0"]` = `/cod`. OpenFGA: `can_manage_cod_instructions`. Migracja `098_cod_instruction`.

**Łowca:** ISTNIEJE `shipment_document` (`noted`/`attached`/`other`) — wskazanie dokumentu, nie pobranie. ISTNIEJE GLOSSARY `POD` = `destination_port_id` — **inny** byt niż proof of delivery. ISTNIEJE `bank_payment` / `sales_invoice` — płatność i FV, nie COD. BRAK `cod_instruction`. BRAK `/cod`. BRAK statusu pobrania bez kwoty.

**Odrzucone:**

- Kwota / Decimal / rozliczenie pobrania (Fala F).
- POD/ROD jako `document_kind` na `shipment_document` (leftover D4b; `pod` koliduje z portem wyładunku; kind ma 16 znaków).
- Bajty skanu POD / kamera.
- Live HTTP / marża / druga tabela pieniędzy.

## Zakres

1. Migracja `098_cod_instruction`: RLS, izolacja, indeks `(organization_id, shipment_id)`.
2. POST/GET. Zły status / pusty kod / obce `source_ref` → 400.
3. UI `/cod`. `just api-types`. How-to `docs/operator/pobranie.md`.

## Poza zakresem

D4b POD/ROD · Fala F rozliczenie · kwota · WMS · mapa · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje instrukcję pobrania na zleceniu bez kwoty
- [x] `collection_status` spoza allowlisty → 400
- [x] GET zwraca instrukcje tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / kwoty na wierszu

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET, 400 na status/kod/`source_ref`, RLS, zero `amount` |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | EXPLAIN | `ix_cod_instruction_org_shipment` Index Scan na `(organization_id, shipment_id)` |
| N+1 | OK | GET jedna lista; POST = get shipment + INSERT |
| Dług / człowiek | OK | radio statusu, nie klon `ScanCell`; leftover D4b/F w docs-debt |
| Proza operatora | 5–15 zdań | `pobranie.md`; `katalogi.md` wtopione, 40 linii |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D4, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
