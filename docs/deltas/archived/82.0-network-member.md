# Plaster 82.0 · S19 katalog network_member (M-12)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S19  
**Zależy od:** 9.0 `network` · 0.3 RLS · 0.4 OpenFGA

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-12**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** tabela `network_member` — ręczny katalog agenta w sieci tenanta. FK `network_id`. Unikat `(organization_id, network_id, member_code)`. Bez FK party. RLS FORCE + izolacja. OpenFGA `can_manage_networks`. `/networks/{id}/members`. UI `/networks`.

**Odrzucone:** portal WCA · RapidFuzz · FK party · Excel · F9.1

## Zakres

1. Migracja `042_network_member_rls`.
2. `GET/POST /networks/{network_id}/members`.
3. UI `/networks`.

## Poza zakresem

Portal · RapidFuzz · FK party · Excel · M-30 · F9.1

## Ustalenia

- Token: `network_member`. How-to: `katalogi.md`. Portal = leftover.

## Schemat

`network_member`: `id`, `organization_id`, `network_id`, `member_code`, `legal_name`, `source_ref`, timestamps.

## Kryteria akceptacji

- [x] RLS FORCE + izolacja
- [x] Nieznana sieć / duplikat → 400
- [x] `can_manage_networks`; 403
- [x] Serwis bez `httpx` / `requests` / `scrap`
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | ręczny członek; B nie widzi A |
| Szybkość | N/A ten plaster | katalog |
| SQL | N/A | INSERT/SELECT |
| N+1 | OK | list po network_id |
| Dług / człowiek | OK | portal / RapidFuzz leftover |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
