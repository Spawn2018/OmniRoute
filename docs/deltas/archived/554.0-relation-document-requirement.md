# Plaster 554.0 · C8 leftover — HITL `relation_document_requirement`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C8 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C8 `party_document` (obok, nie to samo)

**Status:** zamknięty `/noc` — archived.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C8 leftover. Nowa tabela `relation_document_requirement`: `requirement_code` (snake 2–32) + `relation_kind` `domestic|international|waste|other` + `source_ref`. Append-only. RLS. Katalog stancji wymaganego zestawu dokumentów per rodzaj relacji — nie 409 na shipment.
- **Wybrane:** POST/GET `/api/v1/relation-document-requirements`. OpenFGA `can_manage_relation_document_requirements`. UI `/relation-document-requirements`. `SHIPPED["554.0"]`.
- **Odrzucone:** `blocks_create` / 409 na `POST shipment` (`REJECTED` → leftover). Para `document_kind` / matching do `party_document` (`REJECTED` → leftover). Kwota / float / scoring osoby (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / bind filing shipment / PUESC.

**Łowca:** BRAK `relation_document_requirement`. ISTNIEJE `party_document` (rodzaj na party, nie brama). ISTNIEJE `document_checklist_rule` (incoterm×trade_side×mode, nie relation_kind). ISTNIEJE `document_dispatch_rule` (adresat).

## Zakres

1. Migracja `434_rel_doc_requirement` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["554.0"]` → `/relation-document-requirements`.

## Poza zakresem

409 · blocks_create · document_kind matching · PUESC · AI3 · G0 · kwota

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount / 409

## Leftover

blocks_create · 409 shipment · document_kind matching · scoring
