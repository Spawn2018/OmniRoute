# Plaster 219.0 · T3 `vessel_name` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [218.0](../archived/218.0-container-seal3.md) leftover `vessel_name` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 218.0 `seal_no_3`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie `voyage_no`. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `vessel_name`/`voyage_no`/PIN/VGM + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `voyage_no` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `vessel_name` (1–128 znaków po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`statek`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["219.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `158_container_vessel`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container` + trzy plomby. BRAK `vessel_name`. BRAK `voyage_no`. BRAK PIN. BRAK VGM. ISTNIEJE `ocean_bill` — HBL/MBL, nie nazwa statku.

**Odrzucone:**

- `voyage_no` (kolejny plaster).
- `pin_code` / ciphertext.
- VGM / TARA.
- `booking_no` / live HTTP armatora.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `158_container_vessel` (po `157_container_seal3`): `vessel_name` nullable `String(128)`.
2. POST opcjonalny `vessel_name`. Zły token → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · `voyage_no` · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `vessel_name` zapisuje nazwę statku, bez rejsu i bez PIN
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`statek`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `vessel_name`; 400 `statek`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(128)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie rejs; leftover T3 PIN/VGM/`voyage_no`; C901 czysty |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
