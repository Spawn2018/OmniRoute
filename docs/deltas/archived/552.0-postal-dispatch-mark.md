# Plaster 552.0 · F11 leftover — HITL `postal_dispatch_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala F · F11

**Zależy od:** tenancy · wzorzec HITL mark · EXP2.19 `e_delivery_mark` (obok, nie to samo)

**Status:** zamknięty `/noc` — archived.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy F11. Nowa tabela `postal_dispatch_mark`: `mark_code` (snake 2–32) + `dispatch_kind` `en|uss|epo|other` + `source_ref`. Append-only. RLS. Katalog stancji książki PP (EN/USS/EPO) — nie live Poczta Polska i nie e-Doręczenia.
- **Wybrane:** POST/GET `/api/v1/postal-dispatch-marks`. OpenFGA `can_manage_postal_dispatch_marks`. UI `/postal-dispatch-marks`. `SHIPPED["552.0"]`.
- **Odrzucone:** live PP HTTP / książka EN+USS+EPO silnik (`REJECTED` → leftover). `postal_epo` osobna tabela (`REJECTED` → leftover). e-Doręczenia / ADE (`REJECTED` — EXP2.19). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13 / ranking SQL F10.

**Łowca:** BRAK `postal_dispatch_mark` / `postal_epo`. ISTNIEJE `e_delivery_mark` / `e_doreczenia_mark` (e-Doręczenia, nie PP). ISTNIEJE `document_dispatch_rule` (adresat dokumentów, nie PP).

## Zakres

1. Migracja `432_postal_dispatch_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["552.0"]` → `/postal-dispatch-marks`.

## Poza zakresem

live PP · `postal_epo` · e-Doręczenia · kwota · AI3 · G0 · ranking F10

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount

## Leftover

live PP · `postal_epo` · książka EN+USS+EPO silnik
