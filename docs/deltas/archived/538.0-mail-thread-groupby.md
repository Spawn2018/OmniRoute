# Plaster 538.0 · O8 leftover — `/mail` group_by=thread po rfc822

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O8 leftover · [536.0](../archived/536.0-inbound-rfc822.md) · [142.0](../archived/142.0-mail-group-by.md)

**Zależy od:** `inbound_message.rfc822_message_id` / `in_reply_to` (536.0) · allowlista `table_view` już ma `thread` · UI `/mail` group_by (142.0)

**Status:** zamknięty `/noc` (kod; CI po push).

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover O8 po 536.0. FE `mail-groups`: allowlista + `group_by=thread`. Klucz wątku: `in_reply_to` gdy podany, inaczej `rfc822_message_id`, inaczej `—`. Zero SQL, zero migracji. Select na `/mail`. `SHIPPED["538.0"]` → `/mail`.
- **Wybrane:** domena `require_mail_group_by("thread")` już ISTNIEJE — nie ruszać buy-desk POL/POD (533.0).
- **Odrzucone:** live Graph fill Message-ID (`REJECTED`). Self-FK wątku (`REJECTED`). Buy-desk group-by po rfc822 (`REJECTED` na tym wierszu — desk zostaje lane).
- **Odrzucone:** drugi czat / Temporal / float (`REJECTED`).

**Łowca:** ISTNIEJE kolumny rfc822 (536.0). ISTNIEJE `thread` w `table_view` domain. ISTNIEJE UI group-by party|country|status. BRAK `thread` w `MAIL_GROUP_BY` FE. BRAK klucza z Message-ID.

## Zakres

1. `mail-groups.ts` + vitest.
2. Select opcja Wątek na `/mail`.
3. How-to `poczta.md` / `katalogi.md`.

## Poza zakresem

buy-desk rfc822 · live Graph · self-FK · nowy czat

## Kryteria akceptacji

- [x] `group_by=thread` grupuje po In-Reply-To albo Message-ID
- [x] brak nagłówków → klucz `—`
- [x] zero migracji / float / marży
- [x] SHIPPED `538.0` → `/mail`

## Leftover

buy-desk group-by po rfc822 · live Graph fill · self-FK wątku

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | thread key FE |
| Szybkość | N/A ten plaster | grupowanie w pamięci |
| SQL | N/A | zero migracji |
| N+1 | OK | jedna lista |
| Dług / człowiek | OK | helper mailThreadGroupKey |
| Proza operatora | 5–15 | poczta + katalogi |
| Docs/OS | PRZESZŁO | CURRENT · PLAN · SHIPPED |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
