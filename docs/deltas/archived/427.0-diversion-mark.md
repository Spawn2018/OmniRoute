# Plaster 427.0 Â· EXP1 `diversion_mark` HITL

**Status:** Zamknięty `/noc`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `diversion_mark` â€” `mark_code` + `stance_kind` `diversion|reroute|other` + `source_ref`. EXP1 `diversion_of_shipment_id` bez FK self-shipment.
- **Odrzucone:** FK `diversion_of_shipment_id` na shipment â€” leftover osobny.
- **Odrzucone:** `cargo_value` â€” kwota.
- **Odrzucone:** temperature/logger â€” osobny leftover (reefer_mark juĹĽ pokrywa czÄ™Ĺ›Ä‡).

## Zakres

HITL `diversion_mark`: mark_code + stance_kind diversion|reroute|other + source_ref.
Migracja `337` (revises `336_haulier_role_mark`). UI `/diversion-marks`.
OpenFGA `can_manage_diversion_marks` (exp4).

## Poza zakresem

FK shipment Â· mapa Â· kwota Â· auto-reroute

## Kryteria

- [ ] POST z kodem, stance_kind, source_ref
- [ ] zĹ‚y kod/kind/source_ref â†’ 400; amount/margin/score â†’ 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/diversion-marks`
- [ ] OPS_JOBS length 244; SHIPPED `"427.0"`

## Leftover

FK diversion_of_shipment_id Â· temperature fields

