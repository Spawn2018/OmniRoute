# Plaster 274.0 · CI9 `tenant_contract_kek` HITL (znacznik owijki, nie klucz)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CI / CI9 · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md) (`tenant_contract_kek`) · [GLOSSARY.md](../../GLOSSARY.md) znacznik KEK · [273.0](273.0-customer-contract-ciphertext.md)
**Zależy od:** 0.3 tenancy · 272.0 nagłówek · 273.0 opaque present/absent
**Status:** Zamknięty `/noc` 2026-09-10. To jest znacznik, nie klucz. 273.0 nadal nie jest szyfrowaniem. Nie KMS. Nie unwrap. Nie materiał.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CI9 KEK mark / pin 2026-09-08c: katalog HITL, że owijka istnieje jako znacznik; nie klucz i nie live p44):** leftover KMS / unwrap / `wrapped_dek` / materiał klucza parked (zakaz Omni-master, zakaz Fernet/AES, zakaz BYTEA/ciphertext na tej tabeli). leftover super-admin plaintext parked (nie ma decode — 273.0 nadal present/absent). leftover CI1 `sla_clause` / extract LLM parked (CI9 przed CI1). leftover CT7 p44 / FourKites / Shippeo parked live. leftover Langfuse na PDF parked. Żywy plaster: **nowa tabela** `tenant_contract_kek` per tenant — HITL katalog znacznika owijki jako dane, nie klucz i nie szyfr. GLOSSARY: **znacznik KEK** = `tenant_contract_kek` (nie `kek_helper`, nie kolumny sekretu na `customer_contract`). Wiersz = `kek_code` (snake 2–32) + `wrap_kind` (`password` | `kms` — token **danych**, nie wywołanie) + `source_ref` (`tenant:manual` albo `fixture://kek/…`). Zero BYTEA. Zero ciphertext. Zero hasła. Zero FK do `customer_contract` w tym Q. To jest znacznik, nie klucz; 273.0 nadal nie jest szyfrowaniem. Operator wpisuje kod i token owijki; serwis nie woła KMS, nie unwrapuje, nie woła HTTP, nie woła LLM, nie importuje `customer_contracts` / `charges` / `extraction` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły `wrap_kind` → 400 (`owijka`). Obce `source_ref` → 400 (`obce`). Body z extra `password` / `ciphertext` / `key` (oraz `wrapped_dek` / `unwrap` / `plaintext` / `secret`) → 422 (`extra=forbid`). Unique `(organization_id, kek_code)` i `(organization_id, source_ref)`. GET nigdy nie zwraca materiału klucza (nie ma kolumn). UI `/tenant-contract-keks`. `SHIPPED_CHARGE_ROUTES["274.0"]` = `/tenant-contract-keks`. OpenFGA: `can_manage_tenant_contract_keks`. Migracja `207_tenant_contract_kek` (po `206_customer_contract_blob`). Job OPS 119. ExtractionService nie importuje tego BC. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `tenant_contract_kek`. BRAK `kek_code` / `wrap_kind` / `wrapped_dek` w schemacie. BRAK nazwy w GLOSSARY (`tenant_contract_kek`) — ten plan dopisuje **znacznik KEK**. ISTNIEJE karta CI (`wrap KMS/hasło tenanta`, brak plaintext platformy) — ten plaster **nie** zapisuje wrapu. ISTNIEJE `customer_contract` 272.0/273.0 — nagłówek + opaque present/absent, **nie** rozszerzać o KEK / sekrety. ISTNIEJE `idp_connector` 270.0 / `erp_connector` 268.0 / `exchange_connector` 271.0 — katalog HITL, **inny** job (IdP / ERP / giełda). ISTNIEJE `party_document` — kind na `party`, nie owijka. PODOBNE: kod + kind + `source_ref` jak F9/S53. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- KMS HTTP / unwrap API / Fernet / AES / klucz Omni-master.
- materiał klucza / `wrapped_dek` / BYTEA / ciphertext / hasło / sekret.
- kolumny sekretu na `customer_contract`.
- FK do `customer_contract` (nie ten Q).
- twierdzenie, że szyfrowanie albo KEK „istnieje” jako kryptografia.
- CT7 live HTTP p44 / FourKites / Shippeo.
- CI1 extract LLM / `sla_clause` / kara.
- super-admin decode / impersonate=decrypt.
- import customer_contracts/charges/extraction; kwota / marża / float / HTTP.

## Zakres

1. Migracja `207_tenant_contract_kek` po `206_customer_contract_blob`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK `wrap_kind IN ('password', 'kms')`. Zero BYTEA / ciphertext / hasła.
2. POST/GET. Zły token → 400 (`oznaczenie` / `owijka` / `obce`). Extra `password` / `ciphertext` / `key` → 422. GET bez materiału klucza. Serwis nie importuje customer_contracts/charges/extraction i nie woła HTTP. Brak UPDATE/DELETE. ExtractionService nie importuje BC. Zero `float()`.
3. UI `/tenant-contract-keks`. How-to `docs/operator/znacznik-kek.md`; fold w `katalogi.md` (40 linii). Job OPS 119. OpenFGA `can_manage_tenant_contract_keks`. Nazwy: `TenantContractKekDesk` / `tenant-contract-kek-form.tsx` / `persistTenantContractKek`. Brak pól sekretu / hasła / unwrap.

## Poza zakresem

KMS · unwrap · materiał klucza · `wrapped_dek` · Omni-master · Fernet/AES · CT7 live · CI1 extract · `sla_clause` · FK umowy · szyfrowanie · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `tenant_contract_kek` z kodem, `wrap_kind` (`password`|`kms`) i `source_ref`, bez HTTP i bez materiału
- [x] zły kod → 400 (`oznaczenie`); zły `wrap_kind` → 400 (`owijka`); obce `source_ref` → 400 (`obce`)
- [x] POST z extra `password` / `ciphertext` / `key` **nie** zapisuje sekretu (422)
- [x] GET zwraca wpisy tenanta i **nie** zawiera materiału klucza / ciphertext / hasła
- [x] Test izolacji nowej tabeli
- [x] ExtractionService / `extraction-no-rates` nie importuje `tenant_contract_keks`
- [x] `customer_contract` bez nowych kolumn sekretu; serwis nie importuje `customer_contracts`
- [x] Zero float / marży / BYTEA / KMS / unwrap / twierdzenia o szyfrowaniu

## Ustalenia

- To jest znacznik, nie klucz. 273.0 nadal nie jest szyfrowaniem.
- `password` i `kms` to tokeny `wrap_kind` (dane), nie live call i nie pole sekretu.
- Extra pole JSON `password` ≠ token `wrap_kind=password`.
- Nie edytować migracji `205_customer_contract` / `206_customer_contract_blob`.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; 422 na extra `password`/`ciphertext`/`key`; RLS + unique kod/`source_ref`; GET bez materiału |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); izolacja SELECT pod RLS |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela unique; leftover `wrapped_dek` / CT7 live; select owijki zamiast klonu IdP |
| Proza operatora | 5–15 zdań | `znacznik-kek.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN CI9, GLOSSARY, MODULES, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (274.0)

- Migracja `207_tenant_contract_kek` po `206_customer_contract_blob`. RLS FORCE.
- `TenantContractKekService.persist_kek_mark` — katalog unique, bez importu customer_contracts/charges/extraction.
- UI `/tenant-contract-keks`. `SHIPPED_CHARGE_ROUTES["274.0"]` = `/tenant-contract-keks`. Job OPS 119.

## Leftover po 274.0

`wrapped_dek` / KMS unwrap (gdy umowa + test, nie ten Q). Super-admin = bytea. CI1 `sla_clause`. CT7 p44/FourKites/Shippeo parked live (TO_VERIFY umowa). Reszta pinu 2026-09-08c.
