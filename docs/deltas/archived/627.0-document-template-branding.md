# Plaster 627.0 · D9f `branding_ref` HITL na szablonie wydruku

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D9 · [162.0](../archived/162.0-document-template.md)
**Zależy od:** 162.0 `document_template` (`layout_ref` + `html_print`)
**Status:** Zamknięty `/noc 7` 2026-09-22. Nie PDF. Nie ZPL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D9f branding + pdf/zpl):** layout i `html_print` już są. PDF i ZPL zostają parked (brak silnika bajtów). Żywy plaster: opcjonalne `branding_ref` (tekst snake 2–64, nullable) na istniejącej `document_template`. POST bez pola zostaje `null`. Pusty string → `null`. Zły kształt → 400 (`branding`). Wskazanie marki/logo tenanta jako dana, nie plik i nie URL HTTP. `SHIPPED_CHARGE_ROUTES["627.0"]` = `/document-templates`. OpenFGA bez nowej relacji (`can_manage_document_templates`). Migracja `499_document_template_branding` po `498_network_print_requirement`. Marża zostaje na `charge`. Zero bajtów. Zero float.

**REQUIREMENT:** leftover 162.0 „`branding_ref` / `pdf`/`zpl`”. Węższy kawałek = samo `branding_ref` HITL. Źródło: `docs/deltas/archived/162.0-document-template.md` · `docs/ops/docs-debt.md` § 162.0.

**Łowca:** ISTNIEJE `document_template` z `layout_ref` / `output_kind=html_print`. ISTNIEJE `network_print_requirement` (626.0). BRAK kolumny `branding_ref`. BRAK `output_kind` `pdf`/`zpl`.

**Odrzucone:**

- `output_kind` `pdf` / `zpl` (leftover D9f park; nie silnik bajtów)
- BYTEA logo / upload pliku / live URL brandingu
- HTTP 409 wyjazdu (D9c park)
- QR bitmap / skan HITL (D9d / D9e park)
- kwota / marża / float

## Zakres

1. Kolumna nullable + walidacja snake jak `layout_ref` (gdy podane).
2. POST z brandingiem i bez niego; GET zwraca pole.
3. Pole na formularzu `/document-templates` i zdanie w how-to szablonu.

## Poza zakresem

PDF · ZPL · QR · 409 · upload · C8 · WMS · mapa · kwota

## Plan plików

- `backend/alembic/versions/499_document_template_branding.py`
- `backend/app/domain/document_template.py`
- `backend/app/models/document_template.py`
- serwis i API szablonu (pole opcjonalne)
- testy domeny i HTTP
- `frontend/src/features/document-template/sheet-panel.tsx`
- `frontend/src/features/ops/ops-index.ts` (`"627.0"`)
- `docs/operator/szablon-wydruku.md`
- GLOSSARY · CURRENT · PLAN · VISION · PROGRESS

## Kryteria akceptacji

- [x] POST bez `branding_ref` zostaje `null`; z `tenant-logo` zapisuje tekst
- [x] zły kształt → 400 (`branding`); `layout_ref` / `output_kind` bez zmian
- [x] zero bajtów PDF/ZPL / marży / float / 409

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST z brandingiem i bez; 400 na złym kształcie |
| Szybkość | N/A ten plaster | katalog INSERT |
| SQL | N/A | kolumna nullable + CHECK |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | to samo snake co `layout_ref` |
| Proza operatora | 5–15 zdań | `szablon-wydruku.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D9, GLOSSARY, VISION |
| Pushy do zielonego | N/A | CI po tym pushu |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
