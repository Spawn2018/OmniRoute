# Plaster 425.0 Â· EXP1 `language_code_mark` HITL

**Status:** Zamknięty `/noc`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `language_code_mark` â€” `mark_code` + `locale_kind` `pl|en|de|other` + `source_ref`. EXP1 `language_code` na shipment bez kolumny na zleceniu.
- **Odrzucone:** `cargo_value`+waluta â€” kwota; nie katalog bez Decimal.
- **Odrzucone:** mutacja i18n UI / `preferred_language` na party â€” osobne leftover; ten plaster nie rusza frontowego i18n ani party.
- **Odrzucone:** `booked_carrier` / diversion â€” ciÄ™ĹĽsze FK; wÄ™ĹĽszy Q = locale katalog.

## Zakres

HITL `language_code_mark`: mark_code + locale_kind pl|en|de|other + source_ref.
Migracja `335` (revises `334_payment_terms_mark`). UI `/language-code-marks`.
OpenFGA `can_manage_language_code_marks` (exp4).

## Poza zakresem

kolumna na shipment Â· party.preferred_language Â· i18n UI Â· tĹ‚umaczenie LLM

## Kryteria

- [ ] POST z kodem, locale_kind, source_ref
- [ ] zĹ‚y kod/kind/source_ref â†’ 400; amount/margin/score â†’ 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/language-code-marks`
- [ ] OPS_JOBS length 242; SHIPPED `"425.0"`

## Leftover

FK language na shipment Â· sync z i18n UI

