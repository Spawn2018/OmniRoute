# Plaster 575.0 · T3 leftover — HITL `container.volume_m3`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`weight_kg` / `volume_m3`) · [574.0](../archived/574.0-container-weight-kg.md)

**Zależy od:** 153.0 `container` · 574.0 `weight_kg`

**Status:** Zamknięty `/noc` 2026-09-19. Nie kalkulator CBM. Nie `weight_kg`. Nie quantity. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po wadze karta T3 zostawia `volume_m3` na kontenerze. W `backend/app` kolumny nie ma. Objętość HITL to ten sam `Numeric(14,4)` co kilogramy kontenera, przez `_require_positive_kg` z etykietą `objętość`: Decimal, > 0, odrzut float. POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`objętość`). Nie liczy CBM z wymiarów. Nie mutuje `weight_kg` / `quantity`.
- **Wybrane:** `require_container_volume_m3` woła istniejące `_require_positive_kg`. UI `/shipments`, string jak waga (`optionalToken`). `SHIPPED["575.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `455_container_volume` (po `454_container_weight`). Pole w istniejącej linii `_write_from_body`. Bez CHECK w bazie.
- **Odrzucone:** kalkulator CBM (`REJECTED`). `weight_kg` (`REJECTED`). quantity / TEU (`REJECTED`). float (`REJECTED`). ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `volume_m3` w `backend/app`. ISTNIEJE `_require_positive_kg`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `455_container_volume`: kolumna nullable `Numeric(14,4)`.
2. Domain `require_container_volume_m3` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["575.0"]`.

## Poza zakresem

kalkulator CBM · `weight_kg` · quantity · TEU · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `volume_m3="12.5"` zapisuje Decimal
- [x] float / ≤0 / bool / nie-liczba → 400 (`objętość`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero kalkulatora CBM / mutacja wagi

## Leftover

daty SPEED (`pickup_date` i reszta) · `temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL volume_m3 Decimal |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Numeric(14,4) |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | reuse `_require_positive_kg`; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
