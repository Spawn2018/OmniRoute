# Plaster 109.0 · S47 odcinek kolejowy (M-49)

**Spec źródłowa:** [docs/spec/intermodal-rail.md](../../spec/intermodal-rail.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S47  
**Zależy od:** 42.0 `/rail` · 108.0 `shipment_leg` · 4.0 `port` · 4.1 `location`

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-49**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama tabela `shipment_leg`. Migracja 066 rozszerza CHECK `leg_kind IN ('road', 'rail')`. Operator na `/rail` **zapisuje** odcinek kolejowy: `shipment_id` + dwie lokalizacje `unlocode` + `source_ref` + `leg_kind=rail`. Port podpięty pod lokalizację musi mieć `rail` w `function_flags`. Jeden odcinek `rail` na zlecenie (unikat org+zlecenie+kind zostaje — `road` i `rail` mogą współistnieć). API składa: odczyt `shipment`, odczyt dwóch `location`, odczyt `port` (`get_port` jeśli brak), zapis nogi. Serwis nogi nadal nie importuje geography. `SHIPPED_CHARGE_ROUTES["109.0"]` = `/rail`. OPS_JOBS długość 50. POST: `leg_kind` opcjonalny, domyślnie `road` — formularz 108.0 bez zmian.

**Łowca:** ISTNIEJE 108.0 `shipment_leg` + `/shipment-legs` + `can_manage_shipment_legs`. ISTNIEJE 42.0 `/rail` + `railPorts`. ISTNIEJE 4.0 `function_flags`. BRAK `leg_kind=rail` w CHECK. PODOBNE: 108.0 druga akcja na istniejącym ekranie. Dwa joby (katalog portów vs odcinek) = `/ports` vs `/rail`.

**Odrzucone:**

- Druga tabela `rail_leg` / wagon / CIM.
- Druga mapa / GPS / ETA.
- `china_rail` / `ocean_lcl` — S48–S49.
- Kwota / marża.
- Auto-INSERT przy zapisie zlecenia.
- F9.1.

## Zakres

1. Migracja `066_shipment_leg_rail_kind` (po `065_shipment_leg_rls`): DROP `ck_shipment_leg_kind`, nowy CHECK `leg_kind IN ('road', 'rail')`. Bez nowej tabeli.
2. POST `/shipment-legs` przyjmuje `leg_kind` (`road` | `rail`, default `road`). `rail`: lokalizacja nie-`unlocode` albo port bez flagi `rail` → 400. `road` zostaje jak 108.0. Nieznane zlecenie / lokalizacja / port → 404.
3. UI `/rail`: lista odcinków `rail` + „Zapisz odcinek kolejowy”. `data-intermodal-rail="board"` i inwentarz 42.0 zostają. `SHIPPED_CHARGE_ROUTES["109.0"]` = `/rail`. OPS_JOBS = 50.
4. Test izolacji nie nowy (tabela ta sama) — asercja CHECK + HTTP `rail`. How-to `docs/operator/kolej.md`.

## Poza zakresem

S48–S49 · mapa · GPS · wagon · CIM · flota · kwota · F9.1

## Ustalenia

- Token: `shipment_leg` (tabela). UI zostaje `intermodal_rail`. How-to: `kolej.md`.
- `source_ref` bez zmian: `fixture://shipment-leg/` albo `tenant:manual`.
- Serwis nogi bez `httpx` / bez importu rodziców. ExtractionService nie importuje nogi.
- LLM nie liczy trasy. HITL extract bez zmian. `charge` zostaje prawdą o marży.
- Formularz: `data-intermodal-rail="leg-form"` + `grid gap-2`. Nie `flex flex-col gap-2` z 108.0, nie `CatalogCreateForm`.
- OPS_JOBS zostaje długości 50.

## Schemat

Bez nowej tabeli. `shipment_leg.leg_kind`: `road` | `rail`.

## Kryteria akceptacji

- [x] CHECK dopuszcza `rail`; `road` nadal działa
- [x] Brak `amount` / leaflet; zero nowej tabeli
- [x] Port bez `rail` albo `postal_zone` jako koniec → 400; brak uprawnienia → 403
- [x] `/rail` zapisuje i listuje odcinki `rail`; `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CHECK `rail`, HTTP 400 bez flagi, zapis na `/rail` |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | N/A | bez nowego SELECT poza listą nóg |
| N+1 | OK | rail = get zlecenie + dwie lokalizacje + dwa porty + INSERT |
| Dług / człowiek | OK | formularz kolejowy nie kopiuje `Input` z `/road` (jscpd) |
| Proza operatora | 5–15 zdań | `kolej.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
