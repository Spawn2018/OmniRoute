# Plaster 572.0 · T3 leftover — HITL `container.teu`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [571.0](571.0-container-payload-kg.md) · docs-debt 153.0 (zostaje TEU, bez nazwy kolumny w kodzie)

**Zależy od:** 153.0 `container` · 571.0 `payload_kg`

**Status:** Zamknięty `/noc` 2026-09-18. Nie quantity. Nie kalkulator z `iso_size_type`. Nie `payload_kg`. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ostatni nazwany leftover T3 z docs-debt 153.0 mówi „TEU”, a w `backend/app` nie ma kolumny. Pogłębienie istniejącej `container`: opcjonalny `teu` (`Numeric(14,4)`, ten sam typ co `tare_kg` / `payload_kg`). Decimal HITL, > 0, nie float. 45 stóp bywa 2.25 TEU — Integer by to obciął, więc nie Integer. POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`teu`). Nie wylicza TEU z `iso_size_type`. Nie mutuje `payload_kg` / `tare_kg` / `pin_code`.
- **Wybrane:** `require_teu` woła istniejące `_require_positive_kg` z etykietą `teu` (bez drugiego parsera, bez zmiany wspólnego przymiotnika). UI zostaje `/shipments`. `SHIPPED["572.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `452_container_teu` (po `451_container_payload`). `_write_from_body` na suficie 40 linii — pole w istniejącej linii argumentów.
- **Odrzucone:** kalkulator TEU z typu ISO (`REJECTED`). float (`REJECTED`). `quantity` (`REJECTED` → park). Integer obcinający 2.25 (`REJECTED`). ciphertext / AI3-payload / G0-SH (`REJECTED` → park). Import `charges`.

**Łowca:** BRAK `teu` w `backend/app`. ISTNIEJE `iso_size_type` (typ, nie TEU). ISTNIEJE `_require_positive_kg`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `452_container_teu`: kolumna nullable `Numeric(14,4)`.
2. Domain `require_teu` + API POST opcjonalny + GET.
3. UI `/shipments` (`teu` jako string). How-to dopisek. `SHIPPED["572.0"]`.

## Poza zakresem

quantity · kalkulator z ISO · suma TEU · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `teu="2.25"` zapisuje Decimal
- [x] float / ≤0 / bool / nie-liczba → 400 (`teu`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero float / wyliczenie z `iso_size_type` / mutacja payload i tary

## Leftover

`quantity` na kontenerze · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL TEU Decimal |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Numeric(14,4) |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | reuse `_require_positive_kg`; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
