# Plaster 0.15 · M-01 — hasła + rotacja refresh

**Faza:** leftover po 0.12  
**Zależy od:** 0.3 `app_user`, 0.12 JWT HS256  
**Status:** archived 2026-08-31  
**Spec:** `docs/spec/tenancy.md`

## Zakres

- Logowanie: `POST /api/v1/session/token` przyjmuje `email` + `password` (nie UUID).
- `app_user.password_hash` — argon2id; plaintext nigdy w JSON, logach, JWT.
- Access JWT bez zmian (HS256, `sub` + `org`).
- Tabela `refresh_token` + RLS + rotacja: użycie refresh unieważnia poprzedni.
- Seed/dev: hasło znane lokalnie, nie hello-po-UUID.

## Poza zakresem

OAuth/OIDC, PKCE, IdP, zmiana algorytmu access tokena, OpenFGA model, `rate_line`.

## Kryteria akceptacji

- [x] `POST /session/token` z `email`+`password` → 201, `access_token` + `refresh_token`; bez `password` / `password_hash`
- [x] `POST /session/token` z `organization_id`+`user_id` (hello 0.12) → 422
- [x] Hasło inne niż zapisane → 401, bez `access_token` (property: dowolne inne hasło)
- [x] `POST /session/refresh` rotuje: nowy refresh, stary → 401
- [x] Kolumna `password_hash` na `app_user` (nie plaintext)
- [ ] RLS na `refresh_token` — test integration w CI (`just test-integration`); lokalnie PG wisiał

## Pętla po plasterze

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | **PRZESZŁO** | unit HTTP email/password/refresh; nie IdP |
| Szybkość | **N/A ten plaster** | auth, nie ścieżka z budżetu AGENTS.md |
| Dług w diffie | **OK** | C901=0 na plikach 0.15; RLS integration = leftover CI |
| Docs/OS | **PRZESZŁO** | CURRENT=0.15, następny D0 (nie OAuth) |
