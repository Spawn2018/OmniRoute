# Plaster 293.0 · `/refaktor` CatalogSourceRefHintField (Q-E1 leftover)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Cel jakości + Q-E1 `catalog-parts`  
**Zależy od:** `CatalogSourceRefField` (292.0)

**Status:** Zamknięty 2026-09-11. Nie nowy M-xx. Nie mixin modeli. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** trzecie powtórzenie paper-form `source_ref` (`<p>` hint + mono input `max-w-lg text-xs`). `just dup` ~8 klonów z `cash-discount/paper-form.tsx`.

**Łowca:** ISTNIEJE: `CatalogSourceRefField` (label). PODOBNE: pozostałe label-form HITL. BRAK: mixin modeli.

**Odrzucone:**

- Scalanie hint+label w jeden komponent z flagą — inny DOM/klasy.
- Mixin modeli.
- Parsery `*-api.ts`.

## Zakres

1. `CatalogSourceRefHintField` + `cash-discount/paper-form.tsx`
2. `kreptd-licence/licence-form.tsx`
3. `party-document/paper-form.tsx`

## Poza zakresem

pozostałe paper-form (lane-pattern, monitoring-scheme, tender-*) · label-form circle/lane-km · F9.1

## Ustalenia

- C901 czyste. `just dup` total 2,92% → 2,90% (tsx 2,56% → 2,51%; klony 243 → 240).
- `refactor_ratio` 1,4% / 10% — slot spłaca chrome.
- Unit + vitest OK. Zachowanie / aria bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `CatalogSourceRefHintField` w `catalog-parts`
- [x] Trzy paper-form bez lokalnego `<p>`+input `source_ref`
- [x] Testy bez zmiany asercji
- [x] Brak mixin modeli

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | paper-form `source_ref` na `catalog-parts` |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | brak zapytania |
| N+1 | OK | brak pętli |
| Dług / człowiek | leftover | pozostałe paper-form (lane-pattern, monitoring, tender-*); label-form circle/lane-km |
| Proza operatora | N/A | brak nowego jobu |
| Docs/OS | PRZESZŁO | CURRENT → leftover CT/pin Plan; `just docs` |
| Pushy do zielonego | N/A | po pushu |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. Następny: leftover Fala CT / pin — `/plan-modul`.
