# Plaster 81.0 · S18 świadomy mailto po S11+S10 (M-33)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S18  
**Zależy od:** 26.0 `mailto:` · 73.0 `blocks_auto` · 74.0 / 77.0 `operator_decision` · 76.0 `mail_draft`

**Status:** Zamknięty `/noc` 2026-09-03. Żywy ID **M-33**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** żywy ID **M-33** (token `mail_client`). Wysyłka = operator klika i otwiera **swój** klient poczty. Ta sama tabela `mail_draft`: CHECK `status` ∈ (`draft`, `sent`), kolumna `to_address`. `POST /mail-drafts/{id}/dispatch-mailto` tylko gdy jest **accepted** `operator_decision`. SOP `blocks_auto` nie blokuje mailto. Odpowiedź zwraca `mailto:`. OpenFGA: `can_manage_mail_drafts`. Ekran `/ai`. Serwis bez `smtp` / `graph.microsoft` / `httpx`. API składa decyzję + SOP.

**Łowca:** ISTNIEJE 26.0 `mailto:` na `/mail`. ISTNIEJE 76.0 `mail_draft`. BRAK dispatch / status `sent`.

**Odrzucone:**

- Live Graph / SMTP / IMAP send.
- Auto-send.
- Nowa tabela `mail_send`.
- Office.js.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `041_mail_draft_sent`.
2. `POST /mail-drafts/{id}/dispatch-mailto`.
3. UI `/ai`: „Wyślij w kliencie”.

## Poza zakresem

Graph HTTP · SMTP · auto-send · Office.js · F9.1

## Ustalenia

- Żywy ID: **M-33**. Token UI: `mail_client`. Tabela `mail_draft` (M-57).
- How-to: `docs/operator/katalogi.md`.
- Graph send = leftover.

## Schemat

`mail_draft.status` ∈ (`draft`, `sent`). `to_address` nullable do chwili send.

## Kryteria akceptacji

- [x] Brak accepted → 400; accepted + `to_address` → `sent` + `mailto:`
- [x] Drugi dispatch → 400
- [x] `blocks_auto` nie blokuje mailto; brak endpointu auto-send
- [x] Tenant B nie widzi szkicu A (RLS)
- [x] Serwis `mail_drafts` bez `smtp` / `graph.microsoft` / `httpx`
- [x] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | accepted → `sent` + `mailto:`; bez accept 400 |
| Szybkość | N/A ten plaster | dispatch, nie wycena |
| SQL | N/A | UPDATE status + lookup accepted |
| N+1 | OK | jeden get, jeden accepted, jeden mark |
| Dług / człowiek | OK | Graph HTTP leftover |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT, `just docs` |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
