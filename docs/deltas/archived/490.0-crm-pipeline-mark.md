# Plaster 490.0 · BR6.0 leftover etap CRM — katalog HITL

**Status:** Zamknięty `/noc 8` 2026-09-15.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 / E Fala BR · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § BR6.0 leftover etap · leftover po **489.0**

**Zależy od:** tenancy RLS · wzorzec HITL `crm_opportunity` (456.0) · `crm_activity` (489.0) · nie silnik lejka

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `crm_pipeline_marks`. Tabela `crm_pipeline_mark`: `mark_code` + `pipeline_kind` `stage|won|lost|hold|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/crm-pipeline-marks`. OpenFGA `can_manage_crm_pipeline_marks` (member). UI `/crm-pipeline-marks`. `SHIPPED["490.0"]` → `/crm-pipeline-marks`.
- **Wybrane:** Alembic `395_crm_pipeline_mark` (`down_revision = "394_crm_activity"`). Id ≤ 32 znaków.
- **Wybrane:** how-to `docs/operator/crm-pipeline-mark.md`.
- **Odrzucone:** silnik lejka CRM (`REJECTED` — osobny silnik, nie ten wiersz).
- **Odrzucone:** FK do `crm_opportunity` / `crm_activity` / `crm_lead` / `party` (`REJECTED` na tym wierszu).
- **Odrzucone:** kwota / marża / float · UPDATE/DELETE.

**Łowca:** ISTNIEJE `crm_lead`. ISTNIEJE `crm_opportunity`. ISTNIEJE `crm_activity`. ISTNIEJE `funnel_mark`. BRAK `crm_pipeline_mark`.

## Zakres

How-to + OPS_JOB. FE: `pipeline_kind` jako radio.

## Poza zakresem

silnik lejka · FK lead/opportunity/activity/party · cold-send · dedup NIP · HubSpot live

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zły kind → 400; amount → 422
- [x] craft_style; jscpd < 3.0%
- [x] operator how-to

## Leftover

FK · dedup NIP · cold-send · silnik lejka (park)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL crm_pipeline_mark POST/GET + RLS |
| Szybkość | N/A | ten plaster |
| SQL | N/A | list/insert |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | clone campaign_mark |
| Proza operatora | 5-15 | docs/operator/crm-pipeline-mark.md |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | lokalny gate |
| Minuty plastra | N/A | /noc 8 |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
