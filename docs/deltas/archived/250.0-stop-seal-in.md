# Plaster 250.0 · T1 EXP1 `seal_in` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [249.0](249.0-stop-pack.md) leftover plomba · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 249.0 `packaging_code`
**Status:** Zamknięty `/noc` 2026-09-10. Nie `seal_out`. Nie awizacja. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 plomba/awizacja + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover `seal_out`/awizacja/waiting/POD parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `seal_in` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`plomba`). `require_stop_seal_in` w `app.domain.stop` — nie import `container` / `require_seal_no_1`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["250.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `186_stop_seal_in` (po `185_stop_pack`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.packaging_code` (249.0). ISTNIEJE `container.seal_no_1` (216.0, inny BC). BRAK `stop.seal_in`. BRAK `seal_out`. BRAK `appointment_ref` na `stop` (D3 to osobna tabela).

**Odrzucone:**

- `seal_out` / para in+out w jednym plasterze.
- Awizacja / waiting / POD / D3.
- Import `container` / PIN / `charge` / float / km / mapa / T5 / S53 / live HTTP.

## Zakres

1. Migracja `186_stop_seal_in`: `seal_in` nullable String(32).
2. POST opcjonalny `seal_in`. Zły token → 400 (`plomba`). Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`seal_out` · awizacja · waiting · POD · tabela `stop_group` · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `seal_in` zapisuje plombę wjazdu, bez `seal_out`
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / za długi → 400 (`plomba`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `container`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `seal_in`; 400 `plomba`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | String(32); brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | `require_stop_seal_in` bez importu container; leftover `seal_out`; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
