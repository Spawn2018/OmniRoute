# Plaster 638.0 · leftover P5b trip variance stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P5 · [167.0](../archived/167.0-trip-expected-buy.md)
**Zależy od:** 167.0 `expected_buy` na `trip` już jest
**Status:** Zamknięty `/noc 17` 2026-09-23. Nie SQL na `charge`. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P5b „wariancja SQL na charge”):** `trip.expected_buy_*` zostaje snapshotem freeze. P5c actual-from-charge oraz silnik wariancji SQL vs `charge` zostają park. Żywy plaster: **nowa tabela** `trip_variance_mark` per tenant — HITL stance wariancji przejazdu. Wiersz = `mark_code` (snake 1–32) + `variance_kind` (`expected`|`actual`|`gap`|`other`) + `source_ref` (`tenant:manual` albo `fixture://trip-variance/…`). Operator zapisuje, jak traktuje rozjazd expected/actual (oczekiwany / faktyczny / luka / inne); serwis **nie** czyta `charge`, nie liczy różnicy i nie mutuje `trip`. Zły kind → 400 (`wariancja`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `trips` / charges / quotations. UI `/trip-variance-marks`. `SHIPPED_CHARGE_ROUTES["638.0"]` = `/trip-variance-marks`. OpenFGA: `can_manage_trip_variance_marks`. Migracja `510_trip_variance_mark` po `509_handover_bind_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 167.0 P5b — tu tylko HITL stance, nie SQL wariancji. Źródło: `docs/deltas/archived/167.0-trip-expected-buy.md`; PLAN wiersz P5; `docs/ops/docs-debt.md` § 167.0.

**Łowca:** ISTNIEJE `trip.expected_buy_amount` — freeze Decimal, nie stance. ISTNIEJE `handover_bind_mark` — wzorzec stance bind. BRAK `trip_variance_mark`. BRAK `/trip-variance-marks`. BRAK widoku wariancji vs `charge`.

**Odrzucone:**

- SQL wariancji na `charge` / widok expected−actual
- P5c actual odczytany z `charge`
- FK UUID do `trip` / `charge`
- druga marża / float / live HTTP

## Zakres

1. Migracja `510_trip_variance_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `variance_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/trip-variance-marks` + zdanie w how-to przejazdu / zlecenia.

## Poza zakresem

SQL wariancji · P5c · FK · druga marża · float · live HTTP

## Plan plików

- `backend/alembic/versions/510_trip_variance_mark.py`
- `backend/app/models/trip_variance_mark.py`
- `backend/app/domain/trip_variance_mark.py`
- `backend/app/repositories/trip_variance_marks/`
- `backend/app/services/trip_variance_marks/` (+ AGENTS.md)
- `backend/app/api/trip_variance_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/trip-variance-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `variance_kind=expected` zapisuje wiersz; `trip` i `charge` bez zmian
- [x] kind spoza `expected`|`actual`|`gap`|`other` → 400 (`wariancja`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `trips` / `charges` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / SQL na `charge`

## Leftover

P5b SQL wariancji · P5c actual-from-charge · FK trip
