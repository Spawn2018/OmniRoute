# Plaster 123.0 · leftover S12 filtr 27.0 (M-34)

**Spec źródłowa:** [docs/spec/operator-notice.md](../../spec/operator-notice.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S12  
**Zależy od:** 27.0 tablica odczytu · 75.0 tabela `operator_notice`

**Status:** Zamknięty `/noc` 2026-09-04. Żywy ID **M-34**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** tablica 27.0 na `/notifications` dostaje **filtr rodzaju** (wszystkie / szkice HITL / wyceny pending). Helper `operatorNotices()` zostaje składem odczytu. Tabela `operator_notice` nie jest źródłem tych wierszy. Zero auto-INSERT z HITL/wyceny. Zero nowej tabeli. Serwis inbox nie importuje quotations / extraction. `SHIPPED_CHARGE_ROUTES["123.0"]` = `/notifications`. OPS_JOBS 58 → 59.

**Łowca:** ISTNIEJE `operatorNotices()` + `PendingWorkBoard`. BRAK przełącznika kind. PODOBNE: 120.0 compose-read.

**Odrzucone:**

- Zapis pending HITL/wycen do tabeli (auto-INSERT = leftover send / 75.0).
- Import `quotations` / `extraction` w serwisie inbox.
- Send / Graph / SMTP / outbox / Temporal.
- Auth0 · portale · OTel · katalog 71–212.

## Zakres

1. UI `/notifications`: filtr tablicy 27.0 po `kind`. Asercje 27.0/75.0 zostają.
2. Test powierzchni 123.0. How-to: wtopić w bullet powiadomień w `katalogi.md` (bez nowej linii).
3. `SHIPPED_CHARGE_ROUTES["123.0"]` = `/notifications`. OPS_JOBS = 59.

## Poza zakresem

Auto-INSERT · send · F9.1

## Ustalenia

- Token zostaje `operator_notice`. HITL accept zostaje na `/extractions`.
- LLM nie liczy i nie pisze treści. `charge` zostaje prawdą o marży.

## Schemat

Bez nowej tabeli. Bez migracji.

## Kryteria akceptacji

- [x] Filtr kind na tablicy 27.0; tabela inbox nietknięta jako źródło pending
- [x] Zero auto-INSERT; serwis bez importu quotations
- [x] OPS_JOBS = 59; `123.0` → `/notifications`
- [x] `katalogi.md` ≤ 40

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | filtr kind na tablicy 27.0; inbox bez auto-INSERT |
| Szybkość | N/A ten plaster | filtr w pamięci po istniejących GET |
| SQL | N/A | zero nowego zapytania |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | helper filtra; bez `as` na kind |
| Proza operatora | N/A ten plaster | job zapisu już był; wtopione w `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
