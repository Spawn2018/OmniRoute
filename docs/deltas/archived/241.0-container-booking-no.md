# Plaster 241.0 · T3 `booking_no` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [240.0](../archived/240.0-container-last-survey.md) leftover `booking_no` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 240.0 `last_survey_at`
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie PIN. Nie S21.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `booking_no`/PIN/FK + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover `carrier_party_id` parked (FK). leftover `shipment_leg_id` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `booking_no` (tekst HITL, max 64, dana numeru bookingu). POST bez pola zostaje `null`. Pusty po trim → `null`. Zła wartość → 400 (`booking`). `require_booking_no` w `app.domain.container` — wzorzec `require_voyage_no`, nie import `booking_instruction` / S21. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["241.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `180_container_booking_no` (po `179_container_last_survey`). `_WriteBox` — dodać `book`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.voyage_no` (220.0, inny token, ten sam BC — reuse trim/max). ISTNIEJE `booking_instruction` (148.0, status instrukcji, nie numer). BRAK `container.booking_no`. PODOBNE: opcjonalny string. Nie import I4. Nie live HTTP armatora. Nie PIN.

**Odrzucone:**

- Live HTTP / Selenium / S21 booking u armatora.
- PIN / `carrier_party_id` FK / `shipment_leg_id`.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `180_container_booking_no` (po `179_container_last_survey`): `booking_no` nullable `String(64)`.
2. POST opcjonalny `booking_no`. Zła wartość → 400. Serwis nie importuje booking_instructions/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · S21 · FK · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown

## Kryteria akceptacji

- [x] POST z `booking_no` zapisuje token, bez live HTTP i bez PIN
- [x] POST bez pola zostaje `null`
- [x] za długi / nie tekst → 400 (`booking`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / PIN / live HTTP / scrape / import I4

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `booking_no`; 400 `booking`; omit → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable String(64); brak live HTTP |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `carrier_party_id` FK; PIN parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
