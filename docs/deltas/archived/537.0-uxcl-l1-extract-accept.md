# Plaster 537.0 · UXCL-L1 — L0 privacy + allowlista extract-accept

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § UXCL-L1 · [PROC-UXCL](../../ops/ux-continuous-loop.md) P2 · skill `ux-privacy-instrument`

**Zależy od:** `track()` + kolejka `/extractions` · PostHog `frontend/src/lib/analytics.ts` · D2 job extract-accept (2026-09-16)

**Status:** zamknięty `/noc` (kod; CI po push).

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover UXCL-L1. Nie nowe eventy jobu — **ISTNIEJĄ** na `/extractions`: `extraction_draft_created` / `accepted` / `rejected` / `patched` / `undone`. Plaster = brama L0: allowlista tych pięciu nazw, zero properties z treścią szkicu, replay wyłączony, `person_profiles: never`, `capture_pageview: false` (vanity). Test fixture `capture` bez NIP / e-mail / `input_text`.
- **Wybrane:** `SHIPPED["537.0"]` → `/extractions`. OpenFGA bez nowej relacji. Zero Alembic.
- **Odrzucone:** Job#2 `charge` (`REJECTED` — PROC-UXCL). FullStory / heatmap / session replay (`REJECTED`). `ab_sus_mark` jako A/B (`REJECTED`). Live OpenAI / scoring osoby / auto-L3 (`REJECTED`).
- **Odrzucone:** nowy dashboard PostHog / DAU (`REJECTED`).
- **Odrzucone:** zgoda per operator w UI (`REJECTED` na tym wierszu — leftover: klucz `VITE_POSTHOG_KEY` = opt-in tenanta przez env, nie cookie banner).

**Łowca:** ISTNIEJE `track()` i pięć eventów na `queue-page.tsx`. ISTNIEJE `initAnalytics` z `identified_only` + `capture_pageview: true`. BRAK allowlisty. BRAK testu payloadu bez PII. BRAK wyłączenia replay. PODOBNE: `tracking_consent` (BR2.2, GPS) — nie ten job.

## Checklist L0 (P2)

| Pozycja | Werdykt | Notatka |
|---|---|---|
| Cel pomiaru | TAK | completion / drop jobu extract-accept |
| Consent / opt-in | TAK (env) | brak klucza = zero sieci |
| Mask inputs | TAK | `track` bez properties |
| Cookieless / profiles | TAK | `person_profiles: never` |
| Replay osobna zgoda | TAK | `disable_session_recording` |
| Zero scoringu osoby | TAK | same nazwy statusu szkicu |
| Nazwy z GLOSSARY | TAK | `extraction_draft_*` |

## Zakres

1. `analytics.ts`: init bez pageview, bez replay, `person_profiles: never`; `track` tylko allowlista, bez properties.
2. Test: payload fixture bez PII; nazwa spoza listy nie jest eventem jobu.
3. How-to fold na `/extractions`.

## Poza zakresem

Job#2 charge · banner zgody operatora · session replay · A/B · live PostHog w CI · zmiana HITL accept

## Kryteria akceptacji

- [x] bez `VITE_POSTHOG_KEY` — zero `init` / `capture`
- [x] `app_loaded` / paleta / table_view — no-op
- [x] pięć eventów extract-accept z kolejki, bez properties
- [x] test fixture bez NIP / e-mail / `input_text`

## Leftover

banner zgody operatora · Job#2 charge · czas trwania jobu · SH-R16-11 Closure ≤14 dni egzekucja · eventy table_view / paleta

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | allowlista + init L0 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | zero migracji |
| N+1 | OK | brak SQL |
| Dług / człowiek | OK | jedna allowlista |
| Proza operatora | 5–15 | `ekstrakcja.md` |
| Docs/OS | PRZESZŁO | CURRENT · PLAN · SHIPPED |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
