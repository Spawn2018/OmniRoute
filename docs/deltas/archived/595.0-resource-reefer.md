# Plaster 595.0 · T2 leftover — HITL `resource.reefer`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [594.0](../archived/594.0-resource-adr-certified.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`reefer`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie `tail_lift`. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `adr_certified`. Opcjonalny `reefer` na `resource` (bool HITL, nie float). POST bez pola → `null`. `true` / `false` zapisuje. Inny typ → 400. Zmiana flagi przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / inwentarz / kg / LDM / m³ / ADR). Nie matching ładunku. Nie 409. Nie `container.reefer`.
- **Wybrane:** UI `/fleet`. `SHIPPED["595.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `471_resource_reefer` (po `470_resource_adr_certified`). Bez nowej tabeli.
- **Odrzucone:** `tail_lift` (`REJECTED` w tym plasterze). telefon / karta kierowcy (`REJECTED`). HW (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.reefer`. ISTNIEJE `container.reefer` (inny BC, nie reuse). PODOBNE: `adr_certified` + supersede.

## Zakres

1. Migracja `471_resource_reefer`.
2. Domain `require_reefer` + serwis + API + GET.
3. UI `/fleet` flaga chłodni. How-to. `SHIPPED["595.0"]`.

## Poza zakresem

`tail_lift` · telefon · karta kierowcy · matching reefer · temperatura · HW · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `reefer=true` zapisuje bool
- [x] zły typ → 400
- [x] zmiana flagi → supersede
- [x] izolacja RLS (tabela `resource`)
- [x] zero float / import charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | HITL `reefer` + supersede |
| Szybkość | N/A | |
| SQL | N/A | INSERT jak 594 |
| N+1 | OK | |
| Dług / człowiek | OK | ten sam wzorzec co `adr_certified` |
| Proza operatora | 5-15 | katalogi + zlecenie |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |

## Leftover

`tail_lift` · telefon · karta kierowcy · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
