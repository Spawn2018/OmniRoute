# Plaster 10.0 · M-13 Karta wyników kontrahenta (`party_scorecard`)

**Spec źródłowa:** [docs/spec/party-scorecard.md](../../spec/party-scorecard.md)  
**Zależy od:** 5.0 `party` · 0.4 OpenFGA · 0.6 DataTableShell · 5.1 `quotation.party_id` (czytanie **nie** w tym plasterze)

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. Nie M-14.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z M-13):** jedna bieżąca karta per kontrahent — tabela `party_scorecard` ze snapshotem wskaźników (Decimal, nie float). Operator zapisuje kartę ręcznie (`source_ref` = `tenant:manual`). Lista rankingowa sortuje istniejące karty — to powierzchnia „kieruj zapytanie do najlepszych”, **bez** wysyłki RFQ. Żywy ID **M-13**. Ten sam BC `app.services.parties` (jak `party_contact` / 8.0) — nie drugi serwis.

**Odrzucone:**

- Silnik liczący `response_rate` / medianę z `quotation` — wycena 5.1 nie ma znaczników odpowiedzi ani RFQ (to M-30). Formuła z niepełnych danych = zgadywanie.
- `quote_invoice_match_rate` z faktury — M-40/M-41 nie istnieją. Kolumna snapshotu wolno; wyliczenie = leftover.
- Pozycja cenowa **per relacja** (lane POL/POD) — osobne ziarno; 10.0 = jedna karta na `party`. Lane = leftover.
- Automatyczny credit scoring `natural_person` / JDG — **M-14 + AI Act**. `party` nie ma `legal_form`; 10.0 tego nie dokłada i nie czyta `credit_limit` jako scoringu.
- Nadpisanie M-10 `party`, M-08 `charge`, M-11 `resolve_email`, M-12 `network`.
- LLM / ExtractionService liczące wskaźniki. Routing zapytań (M-30). Scraping sieci. Portal. Auth0. M-02.

## Zakres

Snapshot karty wyników per tenant+`party`. Lista (ranking), odczyt po `party_id`, zapis/upsert. RLS FORCE. OpenFGA `can_manage_parties` (bez nowej relacji). UI: ranking `/party-scorecards` + panel na `/parties`.

## Poza zakresem

M-14 scoring osoby/JDG · M-16 SOP · M-18 opłaty portowe · M-19 live · M-25 negocjacja · M-30 RFQ · M-40/M-41 faktura vs oferta · `network_member*` · SQL-refresh z `quotation`/`charge` · karta per lane · ExtractionService→parties · LLM liczący wskaźniki · zmiana `credit_limit`

## Ustalenia

- Żywy ID: **M-13**. Token: `party_scorecard` (GLOSSARY). Archiwum: obiekt `party_scorecard`; funkcje pełnego modułu — w 10.0 jako **kolumny snapshotu**, nie jako pipeline.
- Unikat `(organization_id, party_id)` — jedna bieżąca karta. FK złożone do `party`.
- Wskaźniki udziałowe: `Numeric(7,4)`, zakres `[0, 1]`, NULL = nieznany.
- `source_ref` obowiązkowy; ręczny wpis = `tenant:manual`. Klient nie wysyła `source_ref`.
- OpenFGA: `can_manage_parties` = member.
- RLS FORCE + test izolacji.
- Migracja `021_party_scorecard_rls`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

`party_scorecard`: `id`, `organization_id`, `party_id`, `response_rate`, `median_response_hours`, `price_position`, `quote_invoice_match_rate`, `rollover_count`, `sample_size`, `window_days`, `computed_at`, `source_ref`, timestamps.

## Kryteria akceptacji

- [x] Migracja `021_party_scorecard_rls`: tabela + RLS FORCE; w górę i w dół
- [x] Test izolacji tenantów
- [x] Unikat `(organization_id, party_id)`; FK do `party`
- [x] GET listy rankingowej; GET po `party_id`; brak karty = `UnknownPartyScorecard`
- [x] POST upsert; zły zakres = `InvalidPartyScorecard`; obcy `party` = `UnknownParty`
- [x] OpenFGA `can_manage_parties` na endpointach; brak deklaracji = deny
- [x] UI `/party-scorecards` (DataTableShell ranking) + panel karty na `/parties`
- [x] Extraction / quotations / charges / rate_lines nie importują `parties`
- [x] `just api-types` po zmianie OpenAPI
- [x] Brak auto-scoringu osoby; brak zapisu do `charge` / `quotation`

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | RLS FORCE + izolacja; ranking; upsert; deny OpenFGA; UI `/party-scorecards` + panel `/parties` |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS.md |
| Dług w diffie | leftover | `just dup` 2,97%; SQL-refresh / lane / M-14 / M-30 zostają |
| Docs/OS | PRZESZŁO | spec `party-scorecard.md`; CURRENT → Plan M-16 |
