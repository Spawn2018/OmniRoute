# Plaster 270.0 · S53 `idp_connector` HITL (Auth0 fixture)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Auth0 · wiersz **S53** · [MODULES.md](../../MODULES.md) M-04
**Zależy od:** 0.3 tenancy · 0.12 JWT hello · 0.15 email+hasło · 268.0 `erp_connector` (wzorzec katalogu HITL)
**Status:** Zamknięty `/noc` 2026-09-10. JWT hello zostaje; ta tabela nie jest logowaniem. Nie live Auth0 HTTP. Nie portale.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover S53: katalog HITL fixture Auth0 jako dane; nie I1/I2 live):** leftover I1 BFF+PKCE+cookie live parked (brak tenanta, brak sekretu, brak testu live). leftover I2 RS256 JWKS live parked. leftover Organizations feature parked. leftover Infisical parked. leftover portale / diada / S55 / Fala X parked (oś: S53 **przed** portalami). leftover hello OAuth / „0.12 = IdP” parked — **JWT hello i email+hasło zostają sesją; ten plaster ich nie rusza.** Żywy plaster: **nowa tabela** `idp_connector` per tenant — HITL katalog konektora IdP jako dane, nie login i nie BFF. GLOSSARY nie miał `idp_connector` / `auth0_connector`; kanon jak `erp_connector`: **konektor IdP** = `idp_connector` (nie `auth0_helper`, nie `idp_utils`, nie kolumny na `app_user`). Wiersz = `connector_code` (snake 2–32) + `provider_code` (`auth0` — jedyny token w tym Q) + opcjonalny `public_domain` (tekst hosta, nie live issuer, nie JWKS) + `source_ref` (`tenant:manual` albo `fixture://auth0/…`). Zero kolumn sekretu / ciphertext / client_secret / URL fetch. Operator wpisuje kod i token `auth0`; serwis nie woła HTTP, nie pobiera JWKS, nie woła Auth0 SDK, nie woła LLM, nie importuje `tenancy` / `session` / `charges` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły provider → 400 (`dostawca`). Zła domena → 400 (`domena`). Obce `source_ref` → 400 (`obce`). Body z polem sekretu (`client_secret`, `ciphertext`, `jwks_uri`) → 422 (`extra=forbid`). Unique `(organization_id, connector_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/idp-connectors`. `SHIPPED_CHARGE_ROUTES["270.0"]` = `/idp-connectors`. OpenFGA: `can_manage_idp_connectors`. Migracja `203_idp_connector` (po `202_terminal_slot_connector`). Job OPS 116. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `idp_connector`. BRAK `auth0_connector`. BRAK `auth0` / `oauth` / `jwks` / `pkce` w `backend/` i `frontend/src`. BRAK nazwy w GLOSSARY (`idp_connector`, `auth0_connector`) — ten plan dopisuje **konektor IdP**. ISTNIEJE `session` 0.12/0.15 (`backend/app/api/session.py`, `SessionService`, JWT HS256 + email+hasło + refresh) — **to nie jest ta tabela**; nie rozszerzać, nie zastępować, nie dodawać przycisku logowania. ISTNIEJE GLOSSARY `token sesji` / `token odświeżający` — login zostaje. ISTNIEJE OpenFGA hello 0.4 — SoT ról, **nie** IdP. ISTNIEJE `erp_connector` 268.0 / `telematics_connector` 197.0 / `terminal_slot_connector` 269.0 — katalog adaptera HITL, **inny** job (ERP / GPS / slot). PODOBNE: kod + kind + `source_ref` jak F9. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- I1 BFF + PKCE + cookie httpOnly live.
- I2 RS256 JWKS live / fetch JWKS / Auth0 SDK login.
- hello OAuth / placeholder, który twierdzi że jest IdP.
- zmiana tras hasło / JWT / refresh (`/session`).
- Organizations feature Auth0.
- Infisical / sekrety w kodzie / `client_secret` / ciphertext.
- pięć IdP w jednym plasterze (Okta / Azure / Google / Cognito).
- portale / diada / S55 / Fala X / G2.22 P/lista / T8 live.
- import tenancy/session/charges; kwota / marża / float / HTTP.

JWT hello zostaje; ta tabela nie jest logowaniem.

## Zakres

1. Migracja `203_idp_connector`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK `provider_code = 'auth0'`. Zero kolumn sekretu / ciphertext / URL. `public_domain` nullable tekst hosta.
2. POST/GET. Zły token → 400 (`oznaczenie` / `dostawca` / `domena` / `obce`). Body z sekretem → 422. Serwis nie importuje tenancy/session/charges i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/idp-connectors`. How-to `docs/operator/konektor-idp.md`; fold w `katalogi.md` (40 linii). Job OPS 116. OpenFGA `can_manage_idp_connectors`. Nazwy: `IdpConnectorDesk` / `idp-connector-form.tsx` / `persistIdpConnector`. Brak przycisku logowania / redirect OAuth.

## Poza zakresem

live Auth0 HTTP · BFF+PKCE · JWKS · zmiana `/session` · Organizations · Infisical · pięć IdP · portale · S55 · G2.22 · T8 live · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `idp_connector` z kodem, `provider_code=auth0` i `source_ref`, bez HTTP
- [x] zły kod → 400 (`oznaczenie`); zły provider (m.in. `okta`) → 400 (`dostawca`); zła domena (scheme/URL) → 400 (`domena`); obce `source_ref` → 400 (`obce`)
- [x] POST z `client_secret` / `ciphertext` / `jwks_uri` **nie** zapisuje sekretu (422)
- [x] GET zwraca wpisy tenanta
- [x] Test izolacji nowej tabeli
- [x] `/session` (hasło + JWT hello) bez zmian w tym plasterze
- [x] Zero float / marży / ciphertext / live HTTP / importu tenancy/session/charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; 422 na sekrety; RLS + unique kod/`source_ref`; `/session` nietknięte |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); EXPLAIN ANALYZE nie w tym plasterze |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela unique; leftover I1/I2 live / portale; `just dup` 2.99% |
| Proza operatora | 5–15 zdań | `konektor-idp.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN S53, GLOSSARY, MODULES S53, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (270.0)

- Migracja `203_idp_connector` po `202_terminal_slot_connector`. RLS FORCE.
- `IdpConnectorService.persist_idp_connector` — katalog unique, bez importu tenancy/session/charges.
- UI `/idp-connectors`. `SHIPPED_CHARGE_ROUTES["270.0"]` = `/idp-connectors`. Job OPS 116.

## Leftover po 270.0

I1/I2 live (gdy będzie tenant + sekret + test). Portale/diada **S55** / Fala X (następny na osi `/noc`). Organizations. Infisical. Inne IdP.
