# Plaster 61.0 · Q-E2 testy przez Alembic + pomiar wyceny

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka Q-E2  
**Zależy od:** leftover 4.1 (`create_all` vs migracje) · leftover 58.0 (EXPLAIN, 0 wierszy `rate_line`) · `QUOTE_FROM_CURRENT_SQL` · migracja `010_quotation_rls`

**Status:** Zamknięty `/noc` 2026-09-02. Nie nowy M-xx. Nie seed 50k. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** schemat testowy (`omniroute_test`) powstaje z `alembic upgrade head` na URL-u testowym, nie z `Base.metadata.create_all`. Indeks częściowy `ix_rate_line_current_charge_code` żyje tylko w migracji 010 — ORM go nie ma, więc dzisiejsze testy nie widzą planu produkcyjnego. Pomiar wyceny: `EXPLAIN` tego SELECT-a plus `COUNT(*)` z `rate_line`; p95 < 300 ms tylko przy ≥ 50k wierszy, inaczej jawne N/A z liczbą.

**Łowca:** ISTNIEJE: `create_all` + `_apply_postal_zone_ddl` + `_apply_rls_policies` w `backend/tests/conftest.py`; `QUOTE_FROM_CURRENT_SQL`; leftover 4.1 i 58.0. PODOBNE: `just migrate` na `DATABASE_URL_SYNC` (żywa `omniroute`, nie test). BRAK: Alembic na `omniroute_test`; `pytest-alembic`; p95 przy 50k.

**Odrzucone:**

- Seed 50k fikcyjnych `rate_line`, żeby ogłosić p95 — teatr; anti-cel PLAN.
- k6 jako DoD (`just perf` = size-limit; k6 nadal echo).
- `alembic stamp` po `create_all` — fałszywy head.
- Mixin modeli, Q-E3 (how-to/C4), Q-E4 (CodeQL), F9.1, S1, Auth0, portale.

## Zakres

1. Fixture `engine`: zrzut schematu testowego, `alembic upgrade head` z nadpisaniem URL na `omniroute_test` (nigdy żywa `omniroute`). GRANT dla `tenant_tester` / `omniroute_app` zostaje — migracje nie nadają ról testowych.
2. Usunąć `create_all`, `_apply_postal_zone_ddl` i ręczne `_apply_rls_policies` — RLS, `postal_range` / `postal_span` / exclusion idą z migracji.
3. Test: `alembic_version` = head; `EXPLAIN` SELECT-a wyceny używa `ix_rate_line_current_charge_code`; karta pomiaru z liczbą wierszy i werdyktem N/A albo p95.

## Poza zakresem

Q-E3 · Q-E4 · seed 50k · k6 · zmiana `QUOTE_FROM_CURRENT_SQL` · pętla `quote_batch` (leftover 20.0) · mixin modeli · F9.1 · S1 · nowa tabela · nowy ekran

## Ustalenia

- Alembic w teście: `ALEMBIC_DATABASE_URL` + `command.upgrade`. Zakaz `settings.database_url_sync` (to `omniroute`).
- Jedna migracja na proces pytest, potem TRUNCATE; nie `stamp`.
- Job operatora: brak (ops, nie zapis). OpenFGA: bez zmian. Nowa tabela: brak.
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL bez zmian.

## Schemat

Brak nowej tabeli. Indeks `ix_rate_line_current_charge_code` z 010 — testy go dostają.

## Kryteria akceptacji

- [x] Po fixturze `engine` tabela `alembic_version` ma rewizję head
- [x] `EXPLAIN` SELECT-a z `QUOTE_FROM_CURRENT_SQL` (bez INSERT) na `omniroute_test` zawiera `ix_rate_line_current_charge_code`
- [x] `_apply_postal_zone_ddl` i `_apply_rls_policies` nie są wołane; `create_all` nie buduje schematu testowego
- [x] Karta pomiaru: `COUNT(*)` z `rate_line`, fragment EXPLAIN, werdykt **N/A + liczba wierszy** gdy < 50k
- [x] Brak nowej migracji, nowej trasy i seedu 50k
- [x] Istniejące testy izolacji RLS przechodzą na politykach z migracji (117 integration)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | Alembic na `omniroute_test`; karta N/A przy 0 wierszach |
| Szybkość | N/A ten plaster | p95 50k nie obowiązuje; 0 wierszy `rate_line`; anti-cel PLAN |
| SQL | EXPLAIN | Index Scan Backward `ix_rate_line_current_charge_code` |
| N+1 | leftover | pętla `quote_batch` max 20 = leftover 20.0; ten plaster jej nie rusza |
| Dług / człowiek | leftover | 4.1 `create_all` spłacone; mixin modeli i Q-E3/E4 zostają |
| Proza operatora | N/A | brak jobu zapisu |
| Docs/OS | PRZESZŁO | CURRENT → Q-E3 Plan; `just docs` |

## Pytania

Brak. Następny: Q-E3 `/plan-modul`.
