# Plaster 540.0 · N1 leftover — 409 gdy FTL=1 już ma przesyłkę

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N1 leftover FTL=1 · [260.0](../archived/260.0-consignment.md)

**Zależy od:** `consignment` HITL (260.0) · `shipment`

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** na `POST /consignments` opcjonalne `load_kind` = `ftl` | `ltl`. Gdy `ftl` i zlecenie ma już ≥1 `consignment` → **409** PL. Bez pola albo `ltl` — N wierszy jak 260.0. **Nie** kolumna trybu na `shipment` (260.0: brak FTL/LTL na zleceniu).
- **Wybrane:** compose w API (`consignments.py`), count w repo. Domena: `ConsignmentFtlLimit` → 409.
- **Odrzucone:** unique `(org, shipment_id)` w DB (`REJECTED` — zabiłoby LTL/LCL=N). Kolumna FTL na `shipment` (`REJECTED` na tym wierszu). D2b FK paczki / `stop` (`REJECTED` — inne leftovery).

**Łowca:** ISTNIEJE `consignment` N na shipment. BRAK egzekucji FTL=1.

## Zakres

1. `ConsignmentFtlLimit` + asercja domeny.
2. Repo `count_for_shipment`; API przed INSERT gdy `load_kind=ftl`.
3. Test HTTP 409 / 201 bez kind / 201 ltl z drugim wierszem.
4. How-to `przesylka.md`. `SHIPPED["540.0"]` → `/consignments`.

## Poza zakresem

D2b · stop · tryb na shipment · mapa · kwota

## Kryteria akceptacji

- [x] POST bez `load_kind` → N wierszy jak dziś
- [x] POST `load_kind=ftl` na puste zlecenie → 201; drugi `ftl` → 409
- [x] POST `load_kind=ltl` przy istniejącym wierszu → 201
- [x] zero unique FTL w DB / float / D2b

## Leftover

D2b FK paczki · `stop` na przesyłce · kolumna trybu na `shipment`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | load_kind=ftl → 409 |
| Szybkość | N/A | |
| SQL | N/A | count_for_shipment |
| N+1 | OK | jeden count |
| Dług / człowiek | OK | API compose |
| Proza operatora | 5–15 | przesylka.md |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | |
