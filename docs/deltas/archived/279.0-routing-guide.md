# Plaster 279.0 · CT4 `routing_guide` HITL (katalog, bez 409 egzekucji)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`routing_guide` → 409)
**Zależy od:** 0.3 tenancy · 278.0 CT1 `asn` (oś pin; ten Q nie rusza PO)
**Status:** Zamknięty `/noc` 2026-09-11. To nie jest egzekucja 409 na shipment. To nie jest auto shipment. To nie jest CT2 (klej CI5 — CI5 brak).

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover Fala CT / pin 2026-09-08c po zamknięciu CT1 asn):** leftover CT1 auto `shipment` (U1) **parked z powodem** — `shipment` wymaga `quotation_id`; BC `purchase_orders` zakazuje zapisu/`FK` do `shipment`; „auto” bez HITL łamie HC. Osobny Q compose API gdy będzie decyzja. leftover CT2 klej CI5 **parked** — CI5 (`sla_clause` kara SQL) jeszcze nie żyje; V6 `tower_impact.contract_data_status` już niesie „brak danych umowy”. leftover CT5 EDI live parked. leftover CT8 AIS live parked. leftover CT3 OTIF parked (metryka, nie ten Q). Żywy plaster: **nowa tabela** `routing_guide` per tenant — HITL katalog przewodnika routingu jako dane, nie 409 i nie mapa. GLOSSARY: **przewodnik routingu** = `routing_guide`. Wiersz = `guide_code` + opcjonalne `lane_label` / `mode_label` + `source_ref`. Zero kolumn 409 / kwoty. Katalog unique, POST INSERT, GET lista. UI `/routing-guides`. OpenFGA `can_manage_routing_guides`. Migracja `212_routing_guide`. Job OPS 124. Nowy BC `routing_guides`.

**Łowca:** BRAK tabeli `routing_guide`. ISTNIEJE `trip.route_label` — nie ten job. ISTNIEJE `asn` 278.0 — nie ten Q.

**Odrzucone:** 409 na shipment · auto shipment · CT2 CI5 · OTIF · live EDI · AIS · kwota · float · HTTP · mapa · UPDATE/DELETE.

## Zakres

1. Migracja `212_routing_guide` po `211_asn`: RLS FORCE, izolacja.
2. POST/GET `/routing-guides`. OpenFGA. Nowy BC.
3. UI `/routing-guides`. How-to. Job OPS 124.

## Poza zakresem

409 na shipment · auto shipment · CT2 CI5 · OTIF · live EDI · AIS · kwota · float · mapa

## Kryteria akceptacji

- [x] POST zapisuje `routing_guide` z kodem, opcjonalnymi etykietami i `source_ref`
- [x] zły kod → 400 (`oznaczenie`); obce `source_ref` → 400 (`obce`)
- [x] POST z `shipment_id` / `amount` / `blocks_dispatch` → 422
- [x] GET zwraca wpisy tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / live HTTP / 409 egzekucji
- [x] Zero twierdzenia, że auto shipment albo CI5 żyje

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400; 422; RLS; OpenFGA; deny-list extract |
| Szybkość | N/A ten plaster | INSERT/GET katalog |
| SQL | EXPLAIN | lista po organization_id |
| N+1 | OK | jeden SELECT / INSERT |
| Dług / człowiek | OK | nowy BC; leftover 409 / auto shipment / CT2 |
| Proza operatora | 5–15 zdań | `przewodnik-routingu.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN, GLOSSARY, MODULES |
| Pushy do zielonego | N/A | lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Leftover po 279.0

Egzekucja 409 z `routing_guide`. CT1 auto shipment (compose + quotation). CT2 klej CI5. CT3 OTIF. CT5 EDI live. CT8 AIS live. CI1 extract.
