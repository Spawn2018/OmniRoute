# Plaster 85.0 · S23 wynik negocjacji (M-25)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S23  
**Zależy od:** 18.0 ślad `offer_negotiation` · 13.0 `channel_quote` · 2.0 `quotation`

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-25**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** wynik negocjacji = **wskazanie** oferty kanału na wycenie, nie nowa kwota. Nullable `quotation.negotiated_channel_quote_id`. PATCH zapisuje id. Kwota wyceny i `charge.margin` **nie** ruszają. 18.0 resolve zostaje odczytem. 84.0 `charge` zostaje prawdą o marży.

**Łowca:** ISTNIEJE 18.0 panel + `resolveChannelQuote`. BRAK kolumny wyniku. PODOBNE: 84.0 zapis `charge` (inne BC).

**Odrzucone:**

- Druga kwota / nadpisanie `quotation.amount`.
- Spread poza `margin()`.
- won/lost (S24).
- Live HTTP.
- F9.1.

## Zakres

1. Migracja `044_quotation_negotiated_channel`: kolumna UUID NULL, bez FK (API składa).
2. `PATCH /quotations/{id}/negotiate` — `channel_quote_id`. OpenFGA `can_manage_quotations`.
3. UI `/quotations` w panelu 18.0: „Zapisz wynik”. Zero odejmowania.

## Poza zakresem

won/lost · mutacja kwoty · nowy `margin` · live HTTP · F9.1

## Ustalenia

- Token: `offer_negotiation`. How-to: `ścieżka-pieniędzy.md`.
- Serwis `quotations` nie importuje `channel_quotes` / `charges`.

## Schemat

`quotation.negotiated_channel_quote_id` UUID NULL.

## Kryteria akceptacji

- [x] Kolumna bez kwoty; izolacja zostaje na `quotation`
- [x] Nieznany kanał → 400
- [x] Brak `-` na amount w UI
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | PATCH zapisuje wskazanie; kwota nietknięta; 400 przy nieznanym kanale |
| Szybkość | N/A ten plaster | jeden PATCH, nie wycena 50k |
| SQL | N/A | UPDATE kolumny; brak nowego SELECT silnika |
| N+1 | OK | get quote + get quotation, bez pętli |
| Dług / człowiek | OK | won/lost leftover S24; bez FK tenanta (API składa) |
| Proza operatora | 5–15 zdań | `ścieżka-pieniędzy.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
