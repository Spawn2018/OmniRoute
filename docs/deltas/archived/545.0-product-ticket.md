# Plaster 545.0 · Plat-HD-flow — HITL wpis ticketu produktu

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Plat-HD-flow · [480.0](../archived/480.0-product-ticket-mark.md)

**Zależy od:** `product_ticket_mark` katalog kind (480.0) — poza zapisem

**Status:** zamknięty `/noc` — archiwum.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover Plat-HD-flow. Nowa tabela `product_ticket` — jeden wpis zgłoszenia: `ticket_code` + `title` (1–200) + `body` (1–2000) + `ticket_kind` `report|triage|owner_ok|other` + `source_ref`. Append-only INSERT. RLS. Katalog kind z 480.0 zostaje osobno (nie FK).
- **Wybrane:** POST/GET `/api/v1/product-tickets`. OpenFGA `can_manage_product_tickets`. UI `/product-tickets`. `SHIPPED["545.0"]` → `/product-tickets`.
- **Odrzucone:** workflow silnik / UPDATE kind w miejscu (`REJECTED`). Auto-naprawa / agent auto-PR (`REJECTED`). CAPA / `operator_notice` (`REJECTED`). Mob Expo (`REJECTED`). FK do `product_ticket_mark` (`REJECTED`). Kwota (`REJECTED`).
- **Odrzucone teraz:** AI3-payload (żywy OpenAI CI = park), G0-SH (host/IdP), SH-R16-13.

**Łowca:** ISTNIEJE `product_ticket_mark` (kind). BRAK tabeli z tytułem/treścią ticketu.

## Zakres

1. Migracja `426_product_ticket` + RLS + izolacja.
2. Model → repo → serwis → API.
3. OpenFGA · FE · how-to.
4. `SHIPPED["545.0"]` → `/product-tickets`.

## Poza zakresem

auto-fix · S11 owner gate · Mob · FK mark · CAPA · agent PR

## Kryteria akceptacji

- [x] POST z title/body/kind + source_ref → 201
- [x] pusty title / zły kind / brak source_ref → 400
- [x] izolacja tenantów
- [x] zero LLM / auto-fix / UPDATE

## Leftover

S11 owner · FK mark · Mob · auto-fix / agent PR

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | HITL product_ticket |
| Szybkość | N/A | |
| SQL | N/A | CREATE + RLS |
| N+1 | OK | |
| Dług / człowiek | OK | parse rozbity <40 |
| Proza operatora | 5–15 | product-ticket.md |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | `--close` |
