# Plaster 202.0 · W4 `executive_mark` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala W · [karty-pol-fala-w.md](../../analysis/karty-pol-fala-w.md) § W4
**Zależy od:** 0.3 tenancy (katalog per tenant) · 15.0 / 117.0 tablica finansowa (odczyt zostaje; ten plaster nie sumuje)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie suma LLM. Nie EBITDA.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover W4 zdania z agregatów SQL + leftover W5 ranking + leftover V7 tacho + V8 what-if + leftover V6 EBITDA + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7 tacho TO_VERIFY parked. leftover V8 what-if parked. leftover V6 silnik EBITDA / `sla_clause` parked. leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b parked. leftover V4 AIS parked. leftover V2b myto parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. leftover W5 ranking + auto-award parked. Zdania z agregatów SQL (wzorzec M-15 / 117.0) zostają leftover. Żywy plaster: jedna tabela `executive_mark` per tenant — **HITL znacznik rodzaju pytania zarządu jako dane**, nie narracja z LLM i nie suma. Wiersz = `question_kind` (`loss` | `lane` | `risk` | `cash` | `other`) + `source_ref` (`tenant:manual` albo `fixture://executive-mark/…`). Operator wpisuje rodzaj pytania; serwis nie woła LLM, nie czyta `charge` / `quotation` / tablicy finansowej, nie sumuje EBITDA i nie woła HTTP. Zły rodzaj → 400 (`pytanie`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/executive-marks` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["202.0"]` = `/executive-marks`. OpenFGA: `can_manage_executive_marks`. Migracja `142_executive_mark`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `finance_board` M-15 / 117.0 — odczyt marży i narracja po SQL, **nie** katalog pytań. ISTNIEJE `tower_impact` — etap łańcucha, **nie** pytanie zarządu. ISTNIEJE `prediction_ledger` — CRPS/MAE, **nie** Executive AI. ISTNIEJE `memory_edge` — rodzaj krawędzi, **nie** zarząd. BRAK tabeli `executive_mark`. BRAK sumowania LLM. BRAK silnika zdań.

**Odrzucone:**

- Zdania z agregatów SQL / generator narracji / LLM sumuje EBITDA.
- Ranking W5 / auto-award / szkice maila z rankingu.
- Silnik EBITDA / fizyka twinów / N8 / RAG.
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `142_executive_mark` (po `141_memory_edge`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind.
2. POST/GET. Zły rodzaj → 400. Serwis nie importuje charges / quotations / extraction i nie woła HTTP.
3. UI `/executive-marks`. `just api-types`. How-to `docs/operator/executive.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `BriefDesk` / `briefWrite` / `persistBriefMark`. Formularz radio (nie `<select>` klonów HITL).

## Poza zakresem

narracja SQL · suma LLM · EBITDA · ranking W5 · auto-award · RAG · N8 · drugi czat · silnik fizyki · live GPS · AIS · myto → charge · kwota · T-SQL

## Kryteria akceptacji

- [x] POST zapisuje `executive_mark` z `question_kind`, bez HTTP
- [x] zły `question_kind` → 400 (`pytanie`)
- [x] obce `source_ref` → 400 (`obce`)
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / sumy LLM / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST `question_kind`; 400 `pytanie` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_executive_mark_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover suma LLM / 117.0 / W5; formularz radio + lista `dl` |
| Proza operatora | 5–15 zdań | `executive.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN W4, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
