# Plaster 292.0 · `/refaktor` CatalogSourceRefField (Q-E1 leftover)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Cel jakości + Q-E1 `catalog-parts`  
**Zależy od:** `components/catalog/catalog-parts.tsx` (60.0)

**Status:** Zamknięty 2026-09-11. Nie nowy M-xx. Nie mixin modeli. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** trzecie powtórzenie chrome `source_ref` (label + mono input) w formularzach HITL Grupa A. `just dup` pokazywał ~15 klonów z `carbon-method/method-form.tsx`.

**Łowca:** ISTNIEJE: `CatalogHeading`, `CatalogError`, `CatalogCreateForm`. PODOBNE: wariant paper-form (`<p>` + input bez label). BRAK: mixin `organization_id`.

**Odrzucone:**

- Mixin wszystkich modeli SQLAlchemy.
- `CatalogLoadedTable` na erp/idp/terminal-slot — zmieniłby loading (pusty shell → „Ładowanie…”).
- Parsery `*api.ts` (`parseMethod` / `parseClock`) — osobny dług; nie chrome katalogu.
- Wariant paper-form w tym slocie — inny DOM; leftover.

## Zakres

Trzy ruchy, zachowanie bez zmian, testy bez zmiany asercji.

1. `CatalogSourceRefField` + `carbon-method/method-form.tsx`
2. `free-time-clock/clock-form.tsx`
3. `telematics-connector/connector-form.tsx`

## Poza zakresem

paper-form source_ref · mixin modeli · leftover CT / pin · F9.1

## Ustalenia

- C901 czyste. `just dup` total 2,95% → 2,92% (tsx 2,66% → 2,56%; klony 247 → 243).
- `refactor_ratio` 1,4% / 10% za 4 tyg. — slot spłaca chrome.
- OpenFGA / RLS / API: bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży.
- Unit 3111 + vitest 335 OK. Integration: `test_resource_list_uses_org_kind_index` — planner bez `ix_resource_org_kind` (poza diffem FE); leftover w docs-debt.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `CatalogSourceRefField` w `catalog-parts`
- [x] Trzy formularze HITL bez lokalnego label+input `source_ref`
- [x] Testy bez zmiany asercji; unit + frontend po ruchach
- [x] Brak mixin modeli

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | chrome `source_ref` na `catalog-parts`; mutacje bez zmian |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS w diffie |
| SQL | N/A | brak SELECT/INSERT/UPDATE/resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | leftover | paper-form `source_ref`; pozostałe ~12 label-form; EXPLAIN `ix_resource_org_kind` poza diffem |
| Proza operatora | N/A | brak nowego jobu zapisu |
| Docs/OS | PRZESZŁO | CURRENT → leftover CT/pin Plan; `just docs` |
| Pushy do zielonego | N/A | po pushu |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. Następny: leftover Fala CT / pin — `/plan-modul`.
