# Plaster 581.0 · T3 leftover — HITL `container.temp_min`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [580.0](../archived/580.0-container-unload-date.md) · [229.0](../archived/229.0-container-reefer.md)

**Zależy od:** 153.0 `container` · 229.0 `reefer` · 580.0 `unload_date`

**Status:** Zamknięty `/noc` 2026-09-19. Nie `temp_max`. Nie `needs_external_power`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po pięciu datach SPEED karta T3 zostawia `temp_min` (Decimal °C). Temperatura może być ujemna — **nie** `_require_positive_kg`. Helper `_require_temp_c` + `require_container_temp_min`. POST bez pola → `null`. Nie egzekwuje `reefer=true` w tym plasterze.
- **Wybrane:** UI `/shipments`. `SHIPPED["581.0"]` → `/shipments`. Migracja `461_container_temp_min`.
- **Odrzucone:** `temp_max` / `needs_external_power` / float / 409 vs reefer / N3 / ciphertext / AI3 / G0-SH.

**Łowca:** BRAK `temp_min` przed plasterem. ISTNIEJE `reefer` 229.0.

## Zakres

1. Migracja `461_container_temp_min`: `Numeric(14, 4)` nullable.
2. Domain + API + UI + SHIPPED.

## Poza zakresem

`temp_max` · `needs_external_power` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `temp_min="-18"` zapisuje Decimal
- [x] float / bool / zły token → 400 (`temp. min`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero float / countdown / mutacja `reefer`

## Leftover

`temp_max` · `needs_external_power` · egzekucja „tylko gdy reefer” · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL temp_min Decimal |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Numeric |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | `_require_temp_c` / `_require_dates` |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
