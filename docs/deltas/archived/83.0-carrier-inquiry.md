# Plaster 83.0 · S20 zapytanie do agenta (M-30)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S20  
**Zależy od:** 23.0 ślad `carrier_inquiry` · 82.0 `network_member` · 0.3 RLS · 0.4 OpenFGA

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-30**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** tabela `carrier_inquiry` — ślad kupna: operator zapisuje, że zapytał agenta (`network_member`). Nie `customer_rfq`. 23.0 zostaje tablicą `channel_quote` przy lane. FK tenanta do `network_member`. Status `draft`. `source_ref = tenant:manual`. OpenFGA `can_manage_networks`. `GET/POST /carrier-inquiries`. UI `/networks`. Serwis bez importu innych BC.

**Łowca:** ISTNIEJE 23.0 token UI + 82.0 `network_member` + 67.0 `customer_rfq` (sell). BRAK tabeli zapytania do agenta.

**Odrzucone:** live HTTP · `customer_rfq` · kwoty · FK quotation/party · scraping · F9.1

## Zakres

1. Migracja `043_carrier_inquiry_rls`.
2. `GET/POST /carrier-inquiries`.
3. UI `/networks`.

## Poza zakresem

Live HTTP · S21 kanał · porównanie M-31 · kwoty · scraping · F9.1

## Ustalenia

- Token: `carrier_inquiry` (tabela). How-to: `katalogi.md`. HTTP = leftover.

## Schemat

`carrier_inquiry`: `id`, `organization_id`, `network_member_id`, `source_ref`, `status = draft`, timestamps.

## Kryteria akceptacji

- [x] RLS FORCE + izolacja
- [x] Nieznany członek → 400; brak `amount`
- [x] `can_manage_networks`; 403
- [x] Serwis bez `httpx` / `requests` / `scrap`
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | zapis draft do członka; B nie widzi A; 403 |
| Szybkość | N/A ten plaster | katalog, nie wycena |
| SQL | N/A | INSERT/SELECT; indeks org+member i org+created |
| N+1 | OK | jedno select listy; create to jeden INSERT |
| Dług / człowiek | OK | live HTTP leftover; 23.0 ślad zostaje |
| Proza operatora | 5–15 zdań | `katalogi.md` — Zapisz zapytanie |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
