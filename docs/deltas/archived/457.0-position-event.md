# Plaster 457.0 · BR2.0 zdarzenie pozycji

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR2.0

**Zależy od:** tenancy RLS · wzorzec HITL (V5 `telematics_connector` / M-36 `tracking_event` jako odrębne BC)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `position_events`. Tabela `position_event`: `event_code` + `source_kind` `gps|manual|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/position-events`. OpenFGA `can_manage_position_events` (member). UI `/position-events`. `SHIPPED["457.0"]` → `/position-events`.
- **Wybrane:** Alembic `362_position_event` (`down_revision = "361_crm_opportunity"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny lat/lng / float (`REJECTED` — leftover współrzędnych jako dane, nie live).
- **Odrzucone:** klej FK do `tracking_event` / `resource` / `trip` / `telematics_connector` (`REJECTED` na tym wierszu).
- **Odrzucone:** live GPS poll · GBOX/IKOL HTTP · mapa · countdown.
- **Odrzucone:** rozszerzenie `tracking_event` o GPS (`REJECTED` — M-36 nie jest pozycją).
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `tracking_event` (M-36 ślad na zleceniu, nie GPS). ISTNIEJE `telematics_connector` (V5 adapter, nie event). ISTNIEJE `demo_gps_mark` (demo, nie pozycja). PODOBNE katalog HITL. BRAK `position_event`. BRAK live poll.

## Zakres

How-to: jedno zdanie — to katalog zdarzenia pozycji bez współrzędnych; live GPS zostaje leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

lat/lng · poll · `tracking_event` · mapa · AI5 · pipeline CRM

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount/lat → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

współrzędne jako dane · `observed_at` · FK do resource/trip · live poll · BR2.1 urządzenie

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS dwa tenanty; amount/lat/lng 422 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | INSERT/SELECT katalogu |
| N+1 | OK | jeden SELECT listy |
| Dług / człowiek | OK | osobny BC obok `tracking_event`; bez FK |
| Proza operatora | 5–15 zdań | `docs/operator/position-event.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–456 |
| Minuty plastra | 45 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
