# Plaster 438.0 · AI1.4 leftover `autonomy_level` HITL (słownik otwarty)

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · B.4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 437.0 `twin_kind` (drugi słownik DONE)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `autonomy_level` per tenant — append-only HITL. Wiersz = `level_code` (snake 2–32, **bez** listy CHECK/ENUM 0–5) + `source_ref` (`tenant:manual` albo `fixture://autonomy-level/…`). Nowy poziom = INSERT, nie migracja (VISION B.3).
- **Wybrane:** trzeci z czterech słowników AI1.4 — węższy leftover niż `data_source` (licencja, AI5).
- **Wybrane:** bez FK do `party` / klienta — poziom jest daną katalogu tenanta. Per klient = leftover.
- **Odrzucone:** `data_source` (licencja przy wierszu, AI5).
- **Odrzucone:** CHECK / ENUM poziomów 0–5 z B.4 · kolumna na `app_user` / `party` · silnik L3+ · JSON · UPDATE/DELETE · float / kwota.
- **Odrzucone:** jedna tabela z `axis` w CHECK · zmiana CHECK na innych tabelach.

**Łowca:** PODOBNE `suggestion_kind` / `twin_kind` (wzorzec otwartego słownika — klon). PODOBNE taksonomia B.4 (sześć nazw — nie CHECK). BRAK tabeli `autonomy_level`.

## Zakres

HITL `autonomy_level`. Migracja `348` (revises `347_twin_kind`). UI `/autonomy-levels`.
OpenFGA `can_manage_autonomy_levels`. `SHIPPED["438.0"]`. OPS_JOBS length 255.

## Poza zakresem

data_source · FK party · egzekucja L3+ · zdjęcie CHECK na twin_mark / suggestion_ledger · CRPS · silnik AI4.1

## Kryteria akceptacji

- [x] POST z level_code (dowolny snake 2–32, np. observer) + source_ref
- [x] extra amount/margin/score → 422; zły kod / source_ref → 400
- [x] brak CHECK listy 0–5 w migracji; izolacja RLS; REQUIRED `/autonomy-levels`
- [x] OPS_JOBS length 255; SHIPPED `"438.0"`
- [x] craft_style funkcje>40: 0

## Leftover

data_source (AI5) · FK per klient · zdjęcie CHECK na twin_mark i suggestion_ledger + FK · trzy BC piszą · silnik AI4.1 · CRPS (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL otwarty słownik + RLS + UI + OpenFGA |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | list/order bez nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | analog twin_kind |
| Proza operatora | 5–15 zdań | `docs/operator/autonomy-level.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI billing limit |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
