# Plaster 166.0 · P4 `local_charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 · [karty-pol-fala-p.md](../../analysis/karty-pol-fala-p.md) § P4
**Zależy od:** 1.2 `charge` (marża zostaje) · 12.0 `port_surcharge` już jest (inny job)
**Status:** Zamknięty `/noc` 2026-09-08. Nie warning braków. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z P4):** jedna tabela `local_charge` per tenant — **dopłata lokalna jako dane + kwota Decimal**, nie macierz armator×port i nie warning braków. Wiersz = `charge_kind` (`thc` | `isps` | `seal` | `amendment`) + `amount` (`Numeric(14,4)` > 0) + `currency` (`CHAR(3)`) + `source_ref` (`tenant:manual` albo `fixture://local-charge/…`). `charge_kind` spoza allowlisty → 400 (detail zawiera `rodzaj`). `amount` ≤ 0 / float / bool → 400 (detail zawiera `kwota`). GET listy SQL. Serwis nie importuje port_surcharges / channel_quotes / charges / quotations / geography. UI `/local-charges`. `SHIPPED_CHARGE_ROUTES["166.0"]` = `/local-charges`. OpenFGA: `can_manage_local_charges`. Migracja `106_local_charge`. Marża zostaje na `charge`. Warning ≠ fakt zostaje leftover.

**Łowca:** ISTNIEJE `port_surcharge` — extra **portowe** + `applies_when`; **nie** rozszerzamy o bibliotekę THC/ISPS. ISTNIEJE `channel_quote` — oferta kanału; **nie** rozszerzamy. ISTNIEJE `rate_card` — warunek + Decimal; inna oś. ISTNIEJE `charge` — jedyna marża. BRAK `local_charge`. BRAK `/local-charges`.

**Odrzucone:**

- Macierz armator × port × serwis × typ kontenera / FK (leftover P4b).
- Warning braku dopłaty (leftover P4c; warning ≠ fakt).
- Kolumny na `port_surcharge` / `channel_quote`.
- Zapis do `charge` / float / T-SQL / live HTTP.

## Zakres

1. Migracja `106_local_charge`: RLS, izolacja, indeks `(organization_id, charge_kind)`.
2. POST/GET. Zły rodzaj / kwota / `source_ref` → 400.
3. UI `/local-charges`. `just api-types`. How-to `docs/operator/oplata-lokalna.md`.

## Poza zakresem

P4b macierz · P4c warning · P5 expected vs actual · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `local_charge` THC/ISPS/seal/amendment z Decimal, bez warning
- [x] `charge_kind` spoza allowlisty → 400
- [x] GET zwraca dopłaty tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / warning-jako-fakt / live HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET, 400 na rodzaj/kwotę/`source_ref`, RLS, Decimal bez warning |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | EXPLAIN | `ix_local_charge_org_kind` Index Scan na `organization_id`+`charge_kind` |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | `LocalDesk` / `levyWrite`; leftover P4b–c |
| Proza operatora | 5–15 zdań | `oplata-lokalna.md`; `katalogi.md` 40 linii |
| Docs/OS | PRZESZŁO | CURRENT, PLAN P4, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
