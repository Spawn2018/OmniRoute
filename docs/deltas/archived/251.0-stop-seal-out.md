# Plaster 251.0 · T1 EXP1 `seal_out` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [250.0](250.0-stop-seal-in.md) leftover `seal_out` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 250.0 `seal_in`
**Status:** Zamknięty `/noc` 2026-09-10. Nie awizacja. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 `seal_out`/awizacja + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover awizacja/waiting/POD parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `seal_out` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`plomba`). `require_stop_seal_out` deleguje do `require_stop_seal_in` (jak `seal_no_2` → `seal_no_1` na kontenerze) — nie import `container`. `record_stop` jest na 39 liniach — spakować opcjonalne HITL do `_HitlTail`, żeby dodać `seal_out` bez przekroczenia 40. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["251.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `187_stop_seal_out` (po `186_stop_seal_in`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.seal_in` (250.0). ISTNIEJE `container.seal_no_2` (217.0, inny BC). BRAK `stop.seal_out`. BRAK `appointment_ref` na `stop`.

**Odrzucone:**

- Awizacja / waiting / POD / D3.
- Import `container` / PIN / `charge` / float / km / mapa / T5 / S53 / live HTTP.

## Zakres

1. Migracja `187_stop_seal_out`: `seal_out` nullable String(32).
2. POST opcjonalny `seal_out`. Zły token → 400 (`plomba`). `_HitlTail` na `record_stop`. Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

awizacja · waiting · POD · tabela `stop_group` · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `seal_out` zapisuje plombę wyjazdu, bez awizacji
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / za długi → 400 (`plomba`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `container`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `seal_out`; 400 `plomba`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | String(32); brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | `_HitlTail`; `require_stop_seal_out` → `seal_in`; leftover awizacja; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
