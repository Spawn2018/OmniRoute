# Plaster 56.0 · U-playwright-axe (`e2e_axe_route`)

**Spec źródłowa:** [docs/spec/playwright-axe.md](../../spec/playwright-axe.md)  
**Zależy od:** ADR-0003 §8 + U-oklch-dark (51.0)

**Status:** Zamknięty `/noc` 2026-09-02. Nie nowa tabela. Nie live accept HITL. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** Playwright Chromium + axe na `/session`→`/rate-lines`, `/extractions`, `/quotations`. `just frontend-e2e` po `perf`. CI instaluje Chromium.

**Łowca:** BRAK e2e przed 56.0. ISTNIEJE: vitest, `just perf`.

**Odrzucone:**

- Firefox / WebKit.
- Live login + klik Akceptuj.
- Nowa tabela / nowa trasa.

## Zakres

Trzy specy + axe. `playwright.config.ts` preview port 4173.

## Poza zakresem

U-print · pełny accept HITL w E2E

## Ustalenia

- Token: `e2e_axe_route` (GLOSSARY).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3% (2,82%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `just code-gate` odpala Playwright
- [x] Trzy ścieżki: `/session`→`/rate-lines`, `/extractions`, `/quotations`
- [x] axe na każdej (impact serious/critical = fail)
- [x] CI instaluje Chromium
- [x] Test nie klika Akceptuj i nie woła `parseFloat`
- [x] `just dup` ≤ 3% (2,82%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | 3 ścieżki + axe w gate i CI |
| Szybkość | N/A ten plaster | E2E nie p95 wyceny |
| Dług w diffie | leftover | U-print |
| Docs/OS | PRZESZŁO | spec `playwright-axe.md`; CURRENT → Plan U-print |

## Pytania

Brak. U-print leftover.
