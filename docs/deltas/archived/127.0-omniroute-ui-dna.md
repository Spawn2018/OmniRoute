# Plaster 127.0 · leftover DNA `--ink` + `/refaktor` `_blank_to_none`

**Spec źródłowa:** [docs/design/omniroute-ui.html](../../design/omniroute-ui.html) — tylko `#v-quote` i `#v-ship`  
**Zależy od:** ADR-0003 + leftover U-oklch-dark (51.0) · 5.0 `party` · 125.0 `_blank_to_none`

**Status:** Zamknięty `/noc` 2026-09-04. Nie nowy M-xx. Nie mixin 40 modeli.

## Wybrane / odrzucone / dlaczego

**Wybrane:** mock nazywa rail `--ink` / `--on-ink`. Żywy `index.css` miał te same OKLCH pod `--sidebar`. Alias, nie druga paleta. Trzy ruchy w `PartyService`: kontakt, rachunek, wyjątek opłaty biorą `_blank_to_none` zamiast trzeciej kopii ternara. Zero zmiany asercji.

**Łowca:** ISTNIEJE hue 165, `--s1`…`--s6`, IBM Plex, header `h-12`, `_blank_to_none` (125.0). BRAK nazwy `--ink` w CSS. PODOBNE: mixin nagłówków modeli — nie ten slot.

**Odrzucone:** mixin 40 modeli · chrome `/quotations` (testy źródła) · unifikacja `*-leg-board.tsx` (jscpd celowo rozjechany) · Auth0 · AIS · katalog 71–212.

## Zakres

1. `--ink` / `--on-ink` w `:root` i `.dark`; `--sidebar: var(--ink)`. Literały `--banner-from` / `--banner-to` bez zmian (a11y).
2. Trzy wywołania `_blank_to_none` w `create_contact` / `create_bank_account` / `create_charge_override`.

## Poza zakresem

S4 RFQ · ikonowy rail 3.35rem · nowa trasa · k6 · zmiana asercji

## Ustalenia

- Druga paleta poza `index.css` zakazana.
- LLM nie liczy. `<Money/>` zostaje stringiem. `charge` zostaje prawdą o marży.

## Schemat

Bez migracji.

## Kryteria akceptacji

- [x] `--ink` i `--on-ink` w `frontend/src/index.css`; rail = `var(--ink)`
- [x] Trzy metody PartyService bez ternara `strip() or None`; testy bez edycji

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | token `--ink`; trzy ruchy bez zmiany zachowania |
| Szybkość | N/A ten plaster | nie wycena 50k |
| SQL | N/A | zero nowego zapytania |
| N+1 | OK | brak pętli SQL w diffie |
| Dług / człowiek | OK | mixin modeli leftover; `/quotations` chrome leftover |
| Proza operatora | N/A ten plaster | brak nowego jobu zapisu |
| Docs/OS | PRZESZŁO | CURRENT, PLAN Wave FE |
| Pushy do zielonego | N/A | GitHub billing |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
