# Plaster 159.0 · D5 `groupage_tariff`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D5 · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D5
**Zależy od:** 4.1 `location` (odczyt `postal_zone`) · 155.0 `groupage_line` już jest (poza tym plasterem)
**Status:** Zamknięty `/noc` 2026-09-08. Nie silnik P1. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z D5):** jedna tabela `groupage_tariff` per tenant — **próg wagi na strefie taryfowej**, kwota Decimal, nie matching. Wiersz = `tariff_code` (snake 2–32) + FK złożone do `location` (`kind=postal_zone`) + `chargeable_weight` (`Numeric(14,4)`, próg jako dane, nie wyliczenie) + `amount` (`Numeric(14,4)`) + `currency` (`CHAR(3)`) + `source_ref`. `location.kind` inny niż `postal_zone` → 400 (detail zawiera `strefa`). `chargeable_weight` ≤ 0 albo `amount` ≤ 0 albo float → 400. GET listy SQL. API składa odczyt `location`; serwis cennika nie importuje geography / charges / rate_lines. UI `/groupage-tariffs`. `SHIPPED_CHARGE_ROUTES["159.0"]` = `/groupage-tariffs`. OpenFGA: `can_manage_groupage_tariffs`. Migracja `099_groupage_tariff`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `rate_line` — stawka kupna po `charge_code`, oś wyceny 50k; **nie** strefa LTL, **nie** rozszerzamy (silnik = P1). ISTNIEJE `groupage_line` — cutoff/TT, nie kwota. ISTNIEJE `location` / `location_zone_member` — strefa, nie cennik. ISTNIEJE `port_surcharge` / `channel_quote` — extra portowe i oferta kanału, nie LTL. ISTNIEJE `charge` — jedyna marża. BRAK `groupage_tariff`. BRAK `/groupage-tariffs`. BRAK progu `chargeable_weight` jako danych.

**Odrzucone:**

- Silnik dopasowania WHEN/IF/MIN/MAX (P1).
- T-SQL / matching w Pythonie / LLM liczący `chargeable_weight`.
- Objętość, paleta, FSC (leftover D5b / P3).
- Kolumny na `rate_line` (inna oś; wycena zostaje).
- Float / druga tabela marży / live HTTP.

## Zakres

1. Migracja `099_groupage_tariff`: RLS, izolacja, indeks `(organization_id, location_id)`.
2. POST/GET. Zła strefa / waga / kwota / `source_ref` → 400.
3. UI `/groupage-tariffs`. `just api-types`. How-to `docs/operator/cennik-drobnicy.md`.

## Poza zakresem

P1 silnik · D5b objętość/paleta/FSC · T-SQL · WMS · mapa · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje próg `chargeable_weight` na `location` `postal_zone` z kwotą Decimal
- [x] `location.kind` ≠ `postal_zone` → 400
- [x] GET zwraca cenniki tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / matching

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET, 400 na nie-strefę i kwotę ≤0, RLS, Decimal |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | EXPLAIN | `ix_groupage_tariff_org_location` Index Scan |
| N+1 | OK | GET jedna lista; POST = get location + INSERT |
| Dług / człowiek | OK | `<Money/>` na liście; leftover D5b/P1/P3 w docs-debt |
| Proza operatora | 5–15 zdań | `cennik-drobnicy.md`; `katalogi.md` 40 linii |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D5, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
