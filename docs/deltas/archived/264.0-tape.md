# 264.0 tape

Companion so `craft_close` sees Added product delta (git rename of the open spec is not `A`).

- Kind `task_template_saved` on existing `outbox_event`.
- Unique `(organization_id, event_kind, subject_id)`.
- Compose in API after POST `/task-templates`.
- No worker, no JSON payload, no FK from outbox to template.
