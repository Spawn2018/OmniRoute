# Plaster 553.0 · C1 leftover — HITL `shipment_monitoring_filing`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C1 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C7 `monitoring_scheme` (obok, nie to samo)

**Status:** zamknięty `/noc` — archived.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C1. Nowa tabela `shipment_monitoring_filing`: `filing_code` (snake 2–32) + `status_kind` `open|filed|closed|other` + `source_ref`. Append-only. RLS. Katalog stancji zgłoszenia SENT/BDO — nie live PUESC i nie XML.
- **Wybrane:** POST/GET `/api/v1/shipment-monitoring-filings`. OpenFGA `can_manage_shipment_monitoring_filings`. UI `/shipment-monitoring-filings`. `SHIPPED["553.0"]`.
- **Odrzucone:** FK `shipment_id` / `monitoring_scheme_id` (`REJECTED` → leftover bind). PUESC HTTP / SENT XML / SENT-GEO / `geo_required` / `valid_from`/`valid_to` (`REJECTED` → leftover). `relation_document_requirement` / 409 na shipment (`REJECTED` — C8 leftover). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13 / live PP / postal_epo.

**Łowca:** BRAK `shipment_monitoring_filing`. ISTNIEJE `monitoring_scheme` (C7 katalog schematu, nie zgłoszenie). ISTNIEJE `filing_scheme_mark` (G12 ICS2/CBAM, nie SENT na zleceniu). ISTNIEJE `party_document` (C8, nie filing). BRAK `relation_document_requirement`.

## Zakres

1. Migracja `433_shipment_monitoring_filing` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["553.0"]` → `/shipment-monitoring-filings`.

## Poza zakresem

PUESC · SENT XML · GEO · FK shipment/scheme · 409 · relation_document_requirement · kwota · AI3 · G0

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount

## Leftover

bind shipment/scheme · PUESC/XML/GEO · relation_document_requirement · 409
