# Plaster 55.0 · U-i18n-structure (`ui_message`)

**Spec źródłowa:** [docs/spec/i18n-structure.md](../../spec/i18n-structure.md)  
**Zależy od:** ADR-0003 §7 + trasy `/quality` `/rollout`

**Status:** Zamknięty `/noc` 2026-09-02. Nie nowa tabela. Nie drugi język. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** `t()` + katalog `pl` w `frontend/src/lib/i18n.ts`. Token `ui_message`. Nagłówki `/quality` i `/rollout` przez klucze. `formatInstant` + `Intl` `pl-PL`. Kwoty zostają na osi 52.0.

**Łowca:** BRAK i18next. ISTNIEJE: `CatalogHeading`. PODOBNE: heading dostaje string z `t()`.

**Odrzucone:**

- i18next / EN / migracja wszystkich katalogów.
- `Number` na kwocie.
- Nowa tabela / nowa trasa.

## Zakres

Katalog `pl`, `t(key)`, `formatInstant`. Nagłówki quality i rollout. Test.

## Poza zakresem

U-playwright-axe · U-print · EN · grouping `<Money/>`

## Ustalenia

- Token: `ui_message` (GLOSSARY).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3% (2,82%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `t("extraction_quality.title")` zwraca polski napis z katalogu
- [x] Brak klucza pali test
- [x] `/quality` i `/rollout` nie mają hardcoded tytułu w JSX (jest `t(`)
- [x] `formatInstant` formatuje ISO przez `pl-PL`; `i18n.ts` bez `parseFloat` / `Number(`
- [x] Brak i18next w `package.json`
- [x] `just dup` ≤ 3% (2,82%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | klucze pl na quality/rollout; format dat Intl |
| Szybkość | N/A ten plaster | katalog stringów |
| Dług w diffie | leftover | Playwright axe |
| Docs/OS | PRZESZŁO | spec `i18n-structure.md`; CURRENT → Plan U-playwright-axe |

## Pytania

Brak. U-playwright-axe leftover.
