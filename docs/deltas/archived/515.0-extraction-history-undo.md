# Plaster 515.0 · AI3.1 leftover undo z `payload.history`

**Status:** DONE 2026-09-15 `/noc 15`.

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI3.1 leftover · po [503.0](503.0-extraction-draft-history.md)

**Zależy od:** 503.0 `payload.history` · `can_review_extractions`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `extraction`. `POST /api/v1/extractions/{id}/undo` na pending: zdejmij **ostatni** wpis z `payload.history`, przywróć jego `candidates` i `revision`; bez nowego snapshotu przy undo.
- **Wybrane:** domena `undo_extraction_history` — pusta historia = `InvalidExtractionDraft`. Tylko `rate_line` / `carrier_quote` / `tender_rfp` (jak PATCH).
- **Wybrane:** UI `/extractions` — przycisk „Cofnij” gdy `history.length > 0`. How-to `docs/operator/ekstrakcja.md`.
- **Wybrane:** OpenFGA bez zmian. `SHIPPED["515.0"]` → `/extractions`. Bez Alembic.
- **Odrzucone:** undo do dowolnej wersji z listy · diff UI · tabela historii (`REJECTED`).
- **Odrzucone:** live vision · DocLayNet · żywy OpenAI w CI (`REJECTED`).

**Łowca:** ISTNIEJE `append_extraction_history` + lista w UI. BRAK endpointu undo / przywrócenia kandydatów.

## Zakres

Domena + serwis + HTTP undo + testy + FE przycisk + how-to + SHIPPED 515.0.

## Poza zakresem

Undo do wskazanej rewizji · Instructor · live vision · DocLayNet · nowa tabela

## Kryteria akceptacji

- [x] po PATCH: undo przywraca poprzednie `candidates` i `revision`; `history` krótsza o 1
- [x] undo przy pustej historii → 4xx domenowe
- [x] UI: przycisk Cofnij tylko gdy jest historia
- [x] craft_style; SHIPPED 515.0 length 457

## Leftover

wiring Instructor (`07`) · live vision · DocLayNet · żywy OpenAI w CI

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST undo z historii |
| Szybkość | N/A ten plaster | |
| SQL | N/A | ten sam UPDATE JSONB |
| N+1 | OK | |
| Dług / człowiek | OK | domena undo_extraction_history |
| Proza operatora | 5–15 | ekstrakcja.md |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
