# Plaster 445.0 · AI2.1 leftover `version_window` — średnie per dzień

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI2.1 leftover dryf

**Zależy od:** 444.0 `version_score` · 443.0 `interval_score` · 432.0 `suggestion_ledger.created_at`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `version_window` — widok `security_invoker` jak `version_score`, plus oś czasu z już istniejącego `suggestion_ledger.created_at`. Postgres liczy `pair_count`, `avg_mae`, `avg_crps` (`Numeric(14,4)`) per `model_version` i dzień UTC `created_on`. Operator widzi szereg, nie wpisuje go.
- **Wybrane:** `created_on` = `(s.created_at AT TIME ZONE 'UTC')::date`. To obcięcie istniejącego `timestamptz`, nie nowa kolumna biznesowa i nie `window_date` z doku.
- **Wybrane:** tylko GET. Brak POST. Brak flagi „to jest dryf”. Brak progu.
- **Wybrane:** nowy widok, nie rozbudowa `version_score` o dzień (ranking wszystkich par zostaje rankingiem).
- **Odrzucone:** detektor / próg / alert (`REJECTED` — człowiek patrzy na szereg).
- **Odrzucone:** auto-przełączenie modelu (`REJECTED`).
- **Odrzucone:** ziarnistość tydzień/miesiąc · Brier · zapis `prediction_ledger` · float · druga marża · SQL na `charge` · import serwisów ledgerów · kolumna `window_date` (to awizacja doku).

**Łowca:** PODOBNE `version_score` (444.0) — średnie **bez** dnia. PODOBNE `interval_score` (443.0) — metryka pary. PODOBNE `dock_appointment.window_date` — okno magazynu, nie model. BRAK GROUP BY dnia z `created_at`. `just dup` bez wyodrębnienia.

## Zakres

Migracja `355` (revises `354_version_score`). UI `/version-windows` tylko odczyt. OpenFGA `can_manage_version_windows`. `SHIPPED["445.0"]`. OPS_JOBS +1.

## Poza zakresem

auto-champion · próg dryfu · Brier · zapis `prediction_ledger` · trzy BC · `data_source` · AI4.1

## Kryteria akceptacji

- [x] GET zwraca `created_on`/`avg_mae`/`avg_crps`/`pair_count` z SQL (nie z body)
- [x] POST → 405
- [x] izolacja RLS widoku; brak `float`; craft_style funkcje>40: 0
- [x] brak UPDATE `prediction_ledger` i brak flagi dryf/champion

## Leftover

zapis `prediction_ledger` (**446.0**) · Brier · auto-champion · detektor/próg · trzy BC · `data_source` (AI5)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | widok GROUP BY dzień UTC + średnie SQL; GET; POST 405 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | leftover | widok `security_invoker`; EXPLAIN nie w tym plasterze |
| N+1 | OK | jeden SELECT widoku |
| Dług / człowiek | OK | brak Pythona na średnich; nowy BC zamiast flagi dryfu |
| Proza operatora | 5–15 zdań | `docs/operator/version-window.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–444 |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
