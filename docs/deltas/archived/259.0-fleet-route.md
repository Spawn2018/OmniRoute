# Plaster 259.0 · T2c trasa `/fleet`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover T2c · [151.0](../archived/151.0-resource.md) leftover `/fleet` · [112.0](../archived/112.0-fleet-named-park.md) named park · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2
**Zależy od:** 151.0 `resource` · 258.0 `subcontractor_party_id`
**Status:** Zamknięty `/noc` 2026-09-10. Nie GPS. Nie HW. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2c `/fleet` + leftover T1b + leftover `appointment_status` + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b parked. leftover `appointment_status` parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. leftover pojemności / `document_expiries` parked. leftover HW parked. Żywy plaster: **trasa `/fleet`** na istniejącym katalogu `resource` — strona owija `FleetResourcePanel` (HITL pojazd/kierowca/naczepa). Panel zostaje też na `/shipments`. Brak migracji. `SHIPPED_CHARGE_ROUTES["259.0"]` = `/fleet`. Nowy job OPS (107). `BUSINESS_LISTS.fleet` + `REQUIRED_BUSINESS_LIST_ROUTES`. Sidebar i paleta. OpenFGA bez nowej relacji (`can_manage_shipments` jak 151.0). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `resource` (151.0). ISTNIEJE `FleetResourcePanel` na `/shipments`. BRAK trasy `/fleet`. BRAK jobu floty.

**Odrzucone:**

- własne HW / GPS / mapa / live HTTP / poll.
- nowa tabela / pojemności Decimal / `document_expiries`.
- T1b · T7b · T5 · N1 `consignment` · S53.

## Zakres

1. `frontend/src/routes/fleet.tsx` + strona z `FleetResourcePanel`. Nie `create_table`.
2. `BUSINESS_LISTS` / `REQUIRED_BUSINESS_LIST_ROUTES` / job OPS 107 / `SHIPPED_CHARGE_ROUTES["259.0"]` = `/fleet`.
3. Sidebar, paleta, how-to. Job OPS 151.0 zostaje `/shipments`.

## Poza zakresem

HW · GPS · mapa · pojemności · `document_expiries` · T7b · T5 · Auth0 · live HTTP · `consignment`

## Kryteria akceptacji

- [x] GET `/fleet` pokazuje zapis floty HITL (`data-resource="fleet"`), bez GPS
- [x] Panel na `/shipments` zostaje
- [x] `SHIPPED_CHARGE_ROUTES["259.0"]` = `/fleet`; job OPS ma tę trasę
- [x] Zero nowej tabeli / float / marży / mapy / live HTTP / HW

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/fleet` owija `FleetResourcePanel`; panel zostaje na `/shipments` |
| Szybkość | N/A ten plaster | nowa trasa, nie ścieżka z budżetu AGENTS |
| SQL | N/A | brak migracji i zapytania |
| N+1 | OK | brak pętli SQL |
| Dług / człowiek | OK | reuse panelu 151.0; leftover HW / pojemności / `document_expiries` |
| Proza operatora | 5–15 zdań | `katalogi.md`; fold w `zlecenie.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T2, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
