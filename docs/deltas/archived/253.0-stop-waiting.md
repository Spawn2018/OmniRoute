# Plaster 253.0 · T1 EXP1 `waiting_free_minutes` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [252.0](252.0-stop-appointment-ref.md) leftover waiting · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 252.0 `appointment_ref`
**Status:** Zamknięty `/noc` 2026-09-10. Nie countdown. Nie POD. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 waiting/POD + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover `waiting_started_at` / POD parked. leftover `appointment_status` / `no_show_at` parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `waiting_free_minutes` (`Integer` HITL, ≥ 0, pusty → `null`). POST bez pola zostaje `null`. JSON float / bool → 400 (`oczekiwanie`). `require_stop_waiting_free_minutes` w `app.domain.stop` — wzór `quantity`, nie import `free_time_clocks`. `_HitlTail` z 251.0 dostaje jedno pole; `record_stop` zostaje ≤ 40. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["253.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `189_stop_waiting` (po `188_stop_appointment_ref`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.appointment_ref` (252.0). ISTNIEJE `stop.quantity` (248.0, Integer HITL). ISTNIEJE `free_time_clock` (196.0) — zegar D&D, inny BC. BRAK `stop.waiting_free_minutes`. BRAK `waiting_started_at`.

**Odrzucone:**

- `waiting_started_at` / countdown remaining / szkic `charge`.
- POD / D3 / T8 / PIN / import `free_time_clocks` / km / mapa / T5 / S53 / live HTTP / float.

## Zakres

1. Migracja `189_stop_waiting`: `waiting_free_minutes` nullable Integer. Nie `create_table`.
2. POST opcjonalny `waiting_free_minutes`. Zły token → 400 (`oczekiwanie`). Serwis nie importuje free_time_clocks/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`waiting_started_at` · countdown · POD · tabela `stop_group` · D3 · T8 · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `waiting_free_minutes` zapisuje minuty wolnego oczekiwania, bez countdown
- [x] POST bez pola / pusty trim zostaje `null`; `0` jest legalne
- [x] float / bool / ujemna / nie-int → 400 (`oczekiwanie`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `free_time_clocks`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `waiting_free_minutes`; 400 `oczekiwanie`; 0 legalne; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | Integer + CHECK ≥ 0; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | wzór `quantity`; leftover `waiting_started_at`; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
