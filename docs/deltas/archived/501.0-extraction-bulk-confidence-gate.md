# Plaster 501.0 · AI3.0 leftover próg 70% przed akceptacją zbiorczą

**Status:** DONE 2026-09-15 `/noc 15`.

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI3.0 · leftover po [500.0](500.0-extraction-patch-quote-rfp.md) · [VISION.md](../../VISION.md) § zakazy AI · ui-04

**Zależy od:** `extraction_draft` · `confidence_text` (448.0) · `can_review_extractions` · accept 1.3

## Wybrane / odrzucone / dlaczego

- **Wybrane:** brama domenowa na `confidence_text` przed **akceptacją zbiorczą** szkicu `rate_line`. Próg stały `Decimal("0.70")` w domenie (nie float, nie kolumna Numeric na kandydacie). Zbiorcza = accept gdy w `candidates` jest **≥2** wiersze — każdy musi przejść bramkę, inaczej błąd domeny → HTTP 409. Jeden kandydat = akceptacja indywidualna HITL **bez** bramki (ui-04: niski próg wymaga osobnego kliknięcia, nie blokuje pojedynczego accept).
- **Wybrane:** parser `confidence_text`: ułamek `0.xx` / procent `xx%` przez `Decimal`; etykiety pasma `green`|`yellow`|`high` = OK zbiorczo; `orange`|`hold`|`low`|`poor` = nie; pusty / nierozpoznany = nie do zbiorczego. Zgodne z katalogiem `field_confidence_mark` (487.0) bez FK.
- **Wybrane:** ten sam `POST .../accept`; FE `/extractions` wyłącza Akceptuj przy ≥2 i którymś poniżej progu + komunikat; how-to dopisek. Trasa `/extractions` już pod `SHIPPED["447.0"]` (bez nowej pozycji mapy — jscpd). Bez nowej tabeli / migracji.
- **Odrzucone:** auto-accept / optimistic accept (`REJECTED` — HC-04 / AI Act).
- **Odrzucone:** float / Numeric pewności w bazie · klucze M-03 `hitl_confidence_*` w tym plasterze (`REJECTED` — leftover X9 / M-03; próg 0,70 stały w domenie).
- **Odrzucone:** partial write (część kandydatów → `rate_line`, reszta pending) (`REJECTED` na 501.0 — leftover).
- **Odrzucone:** live vision · przebudowa canvas ui-04 · bramka na `carrier_quote`/`tender_rfp` (jeden payload, nie lista stawek).

**Łowca:** ISTNIEJE `confidence_text` + accept całości szkicu. ISTNIEJE `field_confidence_mark` (katalog pasm, nie egzekucja). BRAK bramy 70% na accept zbiorczy.

## Zakres

Domena + testy + egzekucja w ścieżce accept `rate_line` + UI komunikat + how-to.

## Poza zakresem

partial write · M-03 progi per tenant · live vision · historia PATCH · bbox PDF · auto-accept · kwota / marża

## Kryteria akceptacji

- [x] accept `rate_line` z ≥2 kandydatami, któryś `confidence_text` &lt; 0,70 (lub pusty/orange/hold) → 409; zero `rate_line`
- [x] accept z jednym kandydatem poniżej progu → 200 (indywidualny HITL)
- [x] accept z ≥2, wszystkie ≥0,70 / green|yellow|high → 200 + stawki
- [x] brak `float()` w domenie; craft_style OK; how-to dopisek

## Leftover

partial write · M-03 `hitl_confidence_*` · live vision · AI3.1 historia wierszy / prompt jako dana

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | brama Decimal 0,70 + UI + testy |
| Szybkość | N/A ten plaster | brak ścieżki budżetu |
| SQL | N/A | bez nowego SELECT |
| N+1 | OK | brak pętli SQL |
| Dług / człowiek | OK | domena + lustro FE |
| Proza operatora | 5–15 | `docs/operator/ekstrakcja.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
