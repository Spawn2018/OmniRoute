# Plaster 561.0 · C1 leftover — HITL `filing_fk_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C1 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C1 `filing_bind_mark` (obok, nie to samo)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C1 leftover. Nowa tabela `filing_fk_mark`: `mark_code` (snake 2–32) + `fk_kind` `shipment|scheme|other` + `source_ref`. Append-only. RLS. Katalog stancji celu FK zgłoszenia — nie live FK UUID do shipment/scheme.
- **Wybrane:** POST/GET `/api/v1/filing-fk-marks`. OpenFGA `can_manage_filing_fk_marks`. UI `/filing-fk-marks`. `SHIPPED["561.0"]`.
- **Odrzucone:** live FK `shipment_id` / `monitoring_scheme_id` (`REJECTED` → leftover). PUESC HTTP / SENT XML (`REJECTED` → park). Kwota / float / scoring (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live 409 blocks_create.

**Łowca:** BRAK `filing_fk_mark`. ISTNIEJE `filing_bind_mark` (wiązanie, nie cel FK). ISTNIEJE `shipment_monitoring_filing` (katalog filing). ISTNIEJE `monitoring_scheme`.

## Zakres

1. Migracja `441_filing_fk_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["561.0"]` → `/filing-fk-marks`.

## Poza zakresem

live FK UUID · PUESC · SENT XML · AI3 · G0 · kwota · 409

## Kryteria akceptacji

- [x] POST zły kind / obce source_ref / pusty kod → 400
- [x] izolacja tenantów
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / FK UUID

## Leftover

live FK shipment/scheme · PUESC · scoring
