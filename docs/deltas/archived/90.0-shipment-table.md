# Plaster 90.0 · S28 tabela shipment (M-35)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S28  
**Zależy od:** 28.0 tablica `/shipments` · 5.1 `quotation.party_id` · 86.0 accept oferty (nie bramka w tym plasterze) · 0.3 RLS · 0.4 OpenFGA

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-35**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** jedna tabela `shipment` per tenant (= default arch. M-89). Operator **zapisuje** zlecenie z istniejącej wyceny z `party_id`. Jeden wiersz na wycenę. API składa BC: odczyt `quotation`, zapis `shipment`. Serwis zlecenia nie importuje `quotations` / `charges` / `operator_decisions`. Tablica 28.0 przestaje udawać, że wycena to zlecenie. Wzorzec jak S4 (`customer_rfq` z wiadomości).

**Łowca:** ISTNIEJE 28.0 `/shipments` + `data-shipment="board"` + `fetchQuotations` / `quotationAcceptancePending` + `quotation.party_id`. BRAK tabeli `shipment` / `shipment_leg` w modelach i Alembic. PODOBNE: 67.0 `customer_rfq` (FK tenanta + unikat na źródle); 83.0 `carrier_inquiry` (`status = draft`, `source_ref`). Dwa joby (oferta vs zlecenie) = dwa ekrany (`/quotations` vs `/shipments`), **nie** dwie tabele.

**Odrzucone:**

- Druga tabela `booking` (arch. M-89) — PLAN: dwa obiekty tylko przy dwóch jobach; tu jeden job zapisu zlecenia.
- Auto-INSERT przy Przyjmij (S11) — świadomy zapis operatora, nie skutek uboczny decyzji.
- Bramka „tylko accepted” / import `operator_decisions` — leftover; S24 zostaje na `/quotations`.
- Kwoty, marża, kopia `charge` na zleceniu — prawda zostaje w `charge`.
- Numer `GD/2026/…` / licznik / LLM nadający numer — S8 leftover.
- `shipment_leg`, tracking (S29), dokumenty (S30), wyjątki (S31), EDI, SOP przy zapisie, bramka sankcji/kredytu.
- Live HTTP, Temporal, outbox, F9.1.

## Zakres

1. Migracja `049_shipment_rls` (po `048_party_sanctions_screen`): tabela `shipment`, `organization_id`, RLS ENABLE+FORCE, `uq_quotation_org_id` na `quotation` (nośnik FK złożonego), unikat `(organization_id, quotation_id)`.
2. `GET/POST /shipments` — OpenFGA `can_manage_shipments` = member. POST: `{quotation_id, source_ref}`. `party_id` z wyceny, nie z ciała. Brak `party_id` na wycenie → 400. Drugi wiersz na tę samą wycenę → konflikt domenowy. Nieznana wycena → 404.
3. UI `/shipments`: lista wierszy tabeli + „Zapisz zlecenie”. `data-shipment="board"` zostaje. Lista **nie** idzie z `fetchQuotations`. `SHIPPED_CHARGE_ROUTES["90.0"]` = `/shipments`. Asercje 28.0 bez tabeli — zaktualizować w tym plasterze.
4. Test izolacji tenantów. `just api-types`. How-to `docs/operator/zlecenie.md`.

## Poza zakresem

S29 tracking · S30 dokumenty · S31 wyjątki · EDI · numeracja · kwoty / marża · auto z S11 · bramka sankcji · `shipment_leg` · F9.1

## Ustalenia

- Token: `shipment` (GLOSSARY, tabela). How-to: `zlecenie.md`.
- `source_ref` obowiązkowy (pochodzenie **zapisu** zlecenia, nie stawki). Pusty po trim = 400. Max 256. Testy: `fixture://shipment/…`.
- `status = draft` (CHECK). Brak kwoty w API.
- Serwis bez `httpx` / `requests`. ExtractionService nie importuje `shipment`.
- LLM nie liczy i nie nadaje numeru. HITL extract bez zmian. `charge` zostaje prawdą o marży.

## Schemat

`shipment` (nowa): `id`, `organization_id`, `quotation_id`, `party_id`, `source_ref`, `status`, timestamps, `created_by`. FK tenanta do `quotation` i `party`. Unikat org+quotation.

## Kryteria akceptacji

- [x] Tabela z `organization_id`, RLS FORCE, test izolacji
- [x] Jeden `shipment` na wycenę; brak `amount` / marży w API
- [x] Wycena bez `party_id` → 400; brak `can_manage_shipments` → 403
- [x] `/shipments` zapisuje i listuje tabelę; zero `shipment_leg` / HBL
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | RLS, 403, jeden wiersz na wycenę, `/shipments` zapisuje tabelę |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT, nie wycena 50k |
| SQL | N/A | lista `ORDER BY created_at`; unikat org+quotation; 0 wierszy — nie udajemy p95 |
| N+1 | OK | jedno select listy; create = get wyceny + INSERT |
| Dług / człowiek | OK | C901 czyste; tracking leftover S29 |
| Proza operatora | 5–15 zdań | `zlecenie.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
