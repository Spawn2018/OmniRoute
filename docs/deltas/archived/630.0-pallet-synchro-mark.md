# Plaster 630.0 · D7c leftover synchro stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D7 · [629.0](../archived/629.0-pallet-ledger.md) · [161.0](../archived/161.0-pallet-balance.md)
**Zależy od:** 161.0 `pallet_balance` · 629.0 `pallet_ledger`
**Status:** Zamknięty `/noc 7` 2026-09-22. Nie auto-UPDATE salda. Nie giełda.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D7c „synchro salda↔ledger”):** saldo i ledger zostają osobnymi tabelami. Auto-UPDATE `pallet_balance.unit_count` z sumy ledgeru zostaje park. Giełda D7b zostaje park live. Żywy plaster: **nowa tabela** `pallet_synchro_mark` per tenant — HITL stance zgodności salda z ledgerem. Wiersz = `mark_code` (snake 1–32) + `synchro_kind` (`aligned`|`drift`|`held`|`other`) + `source_ref` (`tenant:manual` albo `fixture://pallet-synchro/…`). Operator zapisuje, czy uważa saldo i ledger za zgodne / rozjechane / wstrzymane; serwis **nie** sumuje `delta_count`, **nie** UPDATE `pallet_balance` i nie porównuje SQL. Zły kind → 400 (`synchro`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `pallet_balances` / `pallet_ledgers` / parties / charges. UI `/pallet-synchro-marks`. `SHIPPED_CHARGE_ROUTES["630.0"]` = `/pallet-synchro-marks`. OpenFGA: `can_manage_pallet_synchro_marks`. Migracja `502_pallet_synchro_mark` po `501_pallet_ledger`. Marża zostaje na `charge`. Zero float. Zero HTTP giełdy.

**REQUIREMENT:** leftover 629.0 / docs-debt „synchro stance” (nie auto-UPDATE). Źródło: `docs/deltas/archived/629.0-pallet-ledger.md`; PLAN wiersz D7; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `pallet_balance` — snapshot ≥ 0. ISTNIEJE `pallet_ledger` — ruch ze znakiem, bez mutacji salda. ISTNIEJE `pallet_pool_mark` — rodzaj puli, nie stance synchro. ISTNIEJE `dual_ledger_mark` — ops|finance|tax, nie palety. BRAK `pallet_synchro_mark`. BRAK `/pallet-synchro-marks`. BRAK stance zgodności salda↔ledger.

**Odrzucone:**

- auto-UPDATE / UPDATE `pallet_balance` z sumy ledgeru
- giełda Chep/LPR/EPAL i live HTTP (D7b)
- FK UUID do `pallet_balance` / `pallet_ledger` / `party`
- kwota / Decimal / depozyt na `charge`
- SQL porównania / scoring / float

## Zakres

1. Migracja `502_pallet_synchro_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `synchro_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/pallet-synchro-marks` + zdanie w `docs/operator/palety.md`.

## Poza zakresem

D7b giełda live · auto-UPDATE salda · FK UUID · kwota · WMS · mapa · float

## Plan plików

- `backend/alembic/versions/502_pallet_synchro_mark.py`
- `backend/app/models/pallet_synchro_mark.py`
- `backend/app/domain/pallet_synchro_mark.py`
- `backend/app/repositories/pallet_synchro_marks/`
- `backend/app/services/pallet_synchro_marks/` (+ AGENTS.md)
- `backend/app/api/pallet_synchro_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/pallet-synchro-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- `docs/operator/palety.md` · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS

## Kryteria akceptacji

- [x] POST z `synchro_kind=aligned` zapisuje wiersz; saldo i ledger bez zmian
- [x] kind spoza `aligned`|`drift`|`held`|`other` → 400 (`synchro`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `pallet_balances` / `pallet_ledgers` / `charges` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / giełdy / auto-UPDATE

## Leftover

D7b giełda park live · auto-UPDATE salda park · D9c 409 / D9d/e/f · matching T5 · FK kontekstu · T8 live · fx×FV · 528 clone U1 carry

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST aligned; 400 synchro; amount 422 |
| Szybkość | N/A ten plaster | katalog INSERT |
| SQL | N/A | create_table + RLS; bez sumy |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | BC osobny od saldo/ledger |
| Proza operatora | 5–15 zdań | palety.md |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D7, GLOSSARY, VISION |
| Pushy do zielonego | N/A | CI po tym pushu |
| Minuty plastra | N/A | /noc 7 |
| Bench rzemiosła | PRZESZŁO | factory_cycle.py --close |
