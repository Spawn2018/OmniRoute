# Plaster 0.8 · instructor + llm-guard

**Faza:** C  
**Zależy od:** 0.7 HITL  
**GROUNDING:** HC-04, HC-03, zasada 4 (model nie liczy), zasada 10 (wejście niezaufane)  
**Status:** archived 2026-08-31

## Zakres
- `ExtractionInputGuard` przed modelem (skanery w stylu llm-guard: injection + sekrety)
- `InstructorExtractor` — wymusza `ExtractionPayload` (Pydantic); `source_ref` z żądania, nie z modelu
- Provider: `EXTRACTION_PROVIDER=mock` (CI default) | `instructor` (klucz API wymagany)
- Testy bez płatnego LLM (fałszywy `complete`)

## Poza zakresem (kolejne)
docling A/B (0.9), presidio, żywe wywołanie OpenAI w CI, langfuse/promptfoo CI (0.10), zapis `rate_line`

## Kryteria akceptacji
- [x] Guard blokuje injection i sekrety; treść sekretu nie wraca w wyjątku
- [x] Instructor wymusza schemat; nadpisuje `source_ref` z requestu
- [x] Bez klucza `instructor` nie startuje
- [x] Default w CI nadal MockExtractor
- [x] HITL bez zmian (accept nie tworzy `rate_line`)
