# Plaster 607.0 · T3 EXP1 `grade` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover EXP1 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container · [606.0](606.0-stop-weigh-out.md) (T1 EXP1 stop DONE)
**Zależy od:** 153.0 `container`; 223.0 `packaging_code`
**Status:** Zamknięty `/noc` 2026-09-21. Nie lista branży. Nie VGM. Nie alliance.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 EXP1 `grade`/`alliance_service`/`vessel_imo` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** leftover `alliance_service` parked. leftover `vessel_imo` parked. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `grade` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Nie-tekst / > 32 → 400 (`stopień`). `require_container_grade` w `app.domain.container` — wzór `require_packaging_code`, **nie** CHECK listy branży, **nie** import `charges` / live class. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["607.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `482_container_grade` (po `481_stop_weigh_out`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.iso_size_type` (153.0) — typ ISO, nie stopień. ISTNIEJE `container.packaging_code` (223.0) — opakowanie, nie grade. ISTNIEJE `container.remarks` (221.0). BRAK `container.grade`. BRAK `alliance_service`. BRAK `vessel_imo`.

**Odrzucone:**

- `alliance_service` / `vessel_imo` / HW / UXCL / N3 / AI3 / G0-SH / CHECK branży / ENUM / live class HTTP / VGM / float / marża / mapa / Auth0.

## Zakres

1. Migracja `482_container_grade`: `grade` nullable String(32). Nie `create_table`.
2. POST opcjonalny `grade`. Zła wartość → 400 (`stopień`). Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

alliance_service · vessel_imo · lista branży live · VGM · HW · UXCL · N3 · mapa · Auth0 · live HTTP · float

## Kryteria akceptacji

- [x] POST z `grade` zapisuje stopień HITL, bez listy branży
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / > 32 → 400 (`stopień`)
- [x] RLS istniejącego `container`; izolacja bez nowej tabeli
- [x] Zero float / marży / live class / import charges

## Leftover

`alliance_service` · `vessel_imo` · HW · UXCL · N3 · AI3 · G0-SH

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `grade`; 400 `stopień`; pusty → null; gate success |
| Szybkość | N/A ten plaster | kolumny INSERT/GET |
| SQL | N/A | ADD COLUMN String(32) |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | wzór `packaging_code` |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
