# 1.3 — accept HITL → rate_line, jedna transakcja HTTP (M-20)

**Status:** archived 2026-09-01  
**Moduł:** M-20 (join M-07)  
**Spec:** PROGRAM-12M Charge 1.3 · [docs/spec/extraction.md](../../spec/extraction.md)

## Zakres

- `AcceptExtractionToRates` w warstwie API: `ExtractionService.accept` + `RateLineService.create_buy_rate` w tym samym request/transakcji
- `rate_line` = kupno, `source_ref` ze szkicu, `charge_code` z katalogu; nieznany token / zła kwota / brak kandydata = rollback + komunikat PL
- `ExtractionService` nie importuje rates/charges (import-linter + asercja AST)
- HITL: status po accept + link `/rate-lines` (U-routes-breadth, nie Exit Wave FE)

## Poza zakresem

outbox, Temporal, `charge`/sell z LLM, Auth0 I1, Wave FE U-*.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | unit: ta sama transakcja (brak commit przy fail), fail-closed, brak importu rates; HTTP 400 PL |
| Szybkość | N/A ten plaster | join accept, bez ścieżki budżetu AGENTS |
| Dług w diffie | OK | C901 czyste; jscpd 2.18% |
| Docs/OS | PRZESZŁO | CURRENT = Auth0 I1; Wave FE U-* leftover; isolation = CI |
