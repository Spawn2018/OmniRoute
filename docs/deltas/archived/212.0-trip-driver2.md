# Plaster 212.0 · T2c `driver2_id` na `trip`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover · [152.0](../archived/152.0-trip.md) leftover T2c · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2
**Zależy od:** 152.0 `trip` + `driver_id` już jest · 151.0 `resource`
**Status:** Zamknięty `/noc` 2026-09-09. Nie km. Nie `/fleet`. Nie `party`. Nie freeze kosztu.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2c multi-manning/naczepa/`party`/km + leftover T7b override na `charge` + leftover T5 outbox + leftover T6 mapa + leftover T8 live API + S53):** leftover T7b na `charge` parked. leftover T5 outbox parked. leftover T6 mapa parked. leftover T8 live API parked. leftover T2c km/`party`/`/fleet`/floating trailer parked. S53 Auth0 parked. Żywy plaster: **pogłębienie istniejącej** `trip` — opcjonalny `driver2_id` (FK tenanta na `resource`, ten sam slot `driver` co `driver_id`), nie drugi przebieg i nie km. POST bez pola zostaje `null`. Ten sam UUID co `driver_id` → 400 (`kierowca`). Rodzaj zasobu inny niż `driver` → 400 (`rodzaj`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["212.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `151_trip_driver2`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `trip.driver_id` — pierwszy kierowca, **nie** drugi. ISTNIEJE `resource` kind `driver`. ISTNIEJE `trailer_id` — naczepa, **nie** drugi kierowca. BRAK `driver2_id`. BRAK km. BRAK `/fleet`.

**Odrzucone:**

- `planned_distance_km` / Decimal km / freeze kosztu.
- `subcontractor_party_id` / floating trailer / `/fleet` / HW.
- T7b na `charge` · T5 outbox · T6 mapa · T8 API · S53 Auth0.
- Import resources / charges w serwisie tripa (API może złożyć odczyt kind jak 152.0).

## Zakres

1. Migracja `151_trip_driver2` (po `150_shipment_parent`): `driver2_id` nullable, FK `(organization_id, driver2_id)` → `resource`, CHECK `driver2_id IS NULL OR driver_id IS NULL OR driver2_id <> driver_id` (`NULL <> UUID` jest UNKNOWN).
2. POST opcjonalny `driver2_id`. Zły zestaw → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments` panel przejazdu. Fold w how-to przejazdu. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

km · party · fleet · T7b · T5 · T6 · T8 · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST z `driver2_id` zapisuje FK tenanta, bez km
- [x] POST bez pola zostaje `null`
- [x] `driver2_id` = `driver_id` → 400 (`kierowca`); kind ≠ `driver` → 400 (`rodzaj`)
- [x] FK tego samego tenanta; izolacja
- [x] Zero float / marży / km / `/fleet` / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `driver2_id`; 400 `kierowca`/`rodzaj`; FK tenanta; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | FK `(organization_id, driver2_id)`; CHECK distinct; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `trip`, nie km; leftover T2c km/`party`/`/fleet`; C901 czysty; bez nowego OPS job |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T2, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
