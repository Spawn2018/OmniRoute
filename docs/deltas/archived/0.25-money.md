# 0.25 — Money Decimal + `<Money/>`

**Status:** archived 2026-09-01  
**Moduł:** domain / UI  
**Spec:** PROGRAM-12M Charge 0.25

## Zakres

- `Money` + `Currency`: `Decimal` + ISO 4217 CHAR(3), skala Numeric(14,4)
- Odrzut `float` / `bool` / gołej liczby
- React `<Money/>` + `parseMoney` / `formatMoney` (string, nigdy `number`)
- HITL recenzja: kandydat pokazuje `<Money/>` (draft `amount_text`, LLM nie liczy)

## Poza zakresem

`rate_line`, tabela `charge`, Auth0 I1, Wave FE U-*, 0.24 pip-audit.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | invariant Decimal+waluta; `<Money/>` na HITL |
| Szybkość | N/A ten plaster | value object, bez ścieżki budżetu AGENTS |
| Dług w diffie | OK | brak nowej tabeli; C901/jscpd na diffie |
| Docs/OS | PRZESZŁO | CURRENT = Charge 1.0 `charge_code`; U-routes-breadth nie claim |
