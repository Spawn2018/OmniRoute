# Plaster 280.0 · CT3 `otif_mark` HITL (katalog zakresu, bez metryki %)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT3 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`OTIF pickup / delivery / SKU`)
**Zależy od:** 0.3 tenancy · 279.0 CT4 `routing_guide` (oś pin; ten Q nie rusza 409)
**Status:** Zamknięty `/noc` 2026-09-11. To nie jest wyliczanie OTIF%. To nie jest 409. To nie jest auto shipment.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover Fala CT / pin 2026-09-08c po 279.0):** leftover CT1 auto `shipment` **parked z powodem** (quotation_id + zakaz FK w BC PO; „auto” bez HITL łamie HC). leftover CT2 klej CI5 **parked** (CI5 brak). leftover egzekucja 409 z `routing_guide` parked. leftover CT5 EDI live / CT8 AIS live parked. Żywy plaster: **nowa tabela** `otif_mark` per tenant — HITL katalog zakresu OTIF jako dane (`pickup` | `delivery` | `sku`), nie silnik % i nie SQL na `stop`/`po_line`. GLOSSARY: **znacznik OTIF** = `otif_mark`. Wiersz = `mark_code` + `scope_kind` + `source_ref`. Zero kolumn procentu / kwoty / `shipment_id`. Katalog unique, POST INSERT, GET lista. UI `/otif-marks`. OpenFGA `can_manage_otif_marks`. Migracja `213_otif_mark`. Job OPS 125. Nowy BC `otif_marks`.

**Łowca:** BRAK tabeli `otif_mark` / `otif` w backendzie i GLOSSARY. ISTNIEJE `tower_impact` V6 — etap łańcucha, **nie** OTIF. ISTNIEJE `po_line` / `asn` — CT1, **nie** metryka. ISTNIEJE `routing_guide` 279.0 — CT4, **nie** ten Q. PODOBNE: kind + `source_ref` jak `free_time_clock` / `rank_mark`. Nie wyodrębniać wspólnej warstwy.

**Odrzucone:** OTIF% / CRPS / SQL scoring · 409 · auto shipment · CT2 CI5 · live EDI · AIS · kwota · float · HTTP · UPDATE/DELETE · FK shipment/stop/po_line.

## Zakres

1. Migracja `213_otif_mark` po `212_routing_guide`: RLS FORCE, izolacja, unique kod + `source_ref`, check `scope_kind`.
2. POST/GET `/otif-marks`. OpenFGA. Nowy BC. Deny-list extract.
3. UI `/otif-marks`. How-to. Job OPS 125.

## Poza zakresem

OTIF% · scoring SQL · 409 · auto shipment · CT2 · live EDI · AIS · kwota · float · mapa

## Kryteria akceptacji

- [x] POST zapisuje `otif_mark` z kodem, `scope_kind` (pickup|delivery|sku) i `source_ref`
- [x] zły kod → 400 (`oznaczenie`); zły scope → 400 (`zakres`); obce `source_ref` → 400 (`obce`)
- [x] POST z `shipment_id` / `amount` / `otif_pct` / `percent` → 422
- [x] GET zwraca wpisy tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / live HTTP / wyliczania OTIF%
- [x] Zero twierdzenia, że auto shipment albo CI5 żyje

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400; 422; RLS; OpenFGA; deny-list extract |
| Szybkość | N/A ten plaster | INSERT/GET katalog |
| SQL | EXPLAIN | lista po organization_id |
| N+1 | OK | jeden SELECT / INSERT |
| Dług / człowiek | OK | nowy BC; leftover OTIF% / 409 / auto shipment / CT2 |
| Proza operatora | 5–15 zdań | `otif.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN, GLOSSARY, MODULES |
| Pushy do zielonego | N/A | lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Leftover po 280.0

OTIF% SQL / egzekucja na stop/SKU. Egzekucja 409 z `routing_guide`. CT1 auto shipment (compose + quotation). CT2 klej CI5. CT5 EDI live. CT8 AIS live. CI1 extract. CT6 SAP HITL. CT12 CAPA.
