# Plaster 54.0 · U-primitives-json (`shadcn_radix_base`)

**Spec źródłowa:** [docs/spec/shadcn-radix-base.md](../../spec/shadcn-radix-base.md)  
**Zależy od:** ADR-0003 §1 + istniejące `@radix-ui/*`

**Status:** Zamknięty `/noc` 2026-09-02. Nie nowa tabela. Nie migracja na Base UI. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** pin CLI w `frontend/components.json` — pole `base: "radix"`. Token `shadcn_radix_base`. Żywy ID leftover **U-primitives-json**, plaster **54.0**. Button/Input na Radix Slot zostają.

**Łowca:** BRAK `components.json` przed 54.0. ISTNIEJE: `@radix-ui/react-slot`. Zero `@base-ui`.

**Odrzucone:**

- Base UI / dwa primitywy.
- `shadcn init` nadpisujący tokeny OKLCH.
- Nowa tabela / nowa trasa.

## Zakres

`frontend/components.json` z `"base": "radix"`. Test pinu.

## Poza zakresem

U-i18n-structure · nowe komponenty CLI

## Ustalenia

- Token: `shadcn_radix_base` (GLOSSARY, plik).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3% (2,83%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `frontend/components.json` ma `"base": "radix"`
- [x] Test pali brak pliku albo `base-ui` jako base
- [x] Brak `@base-ui` w `package.json`
- [x] `just dup` ≤ 3% (2,83%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | pin radix; nie Base UI |
| Szybkość | N/A ten plaster | plik JSON |
| Dług w diffie | leftover | i18n |
| Docs/OS | PRZESZŁO | spec `shadcn-radix-base.md`; CURRENT → Plan U-i18n-structure |

## Pytania

Brak. U-i18n-structure leftover.
