# Plaster 148.0 · I4 `booking_instruction`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § I4 · [karty-pol-fala-i.md](../../analysis/karty-pol-fala-i.md) § I4
**Zależy od:** 28.0 `shipment` · I1 `booking_scope` · I2 role
**Status:** Zamknięty `/noc` 2026-09-08. Nie send. Nie S21.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z I4):** jedna tabela per tenant. Wiersz = `shipment_id` + `booking_scope` + `target_role` + `status` + `source_ref`. Scope z I1: `precarriage` | `ocean` | `oncarriage` | `contact_exchange` | `none`. Rola z I2 (party na zleceniu, nie kolumny EXP1): `shipper` | `consignee` | `origin_agent` | `dest_agent` | `ocean_carrier` | `omni_customs` | `client_customs`. Status: `suggested` | `accepted` | `sent` | `confirmed` | `rejected` — to dane, nie silnik maila. Zmiana statusu/scope/roli = nowy wiersz + `superseded_by`. GET SQL po `shipment_id` (bieżące). Złożone FK `(organization_id, shipment_id)` jak `cargo_claim`. Brak `party_id` na tym wierszu — kontrahent zostaje na `shipment_stakeholder`.

UI `/shipments`. `SHIPPED_CHARGE_ROUTES["148.0"]` = `/shipments`. OpenFGA: `can_manage_shipments`. Migracja `088_booking_instruction`.

**Łowca:** ISTNIEJE `incoterm_responsibility.booking_scope` (I1) — **inny** agregat (katalog macierzy), nie JOIN w tym BC. ISTNIEJE `shipment_stakeholder` (I2) — party per rola. ISTNIEJE `carrier_inquiry` — ślad zapytania do sieci, nie instrukcja bookingu. ISTNIEJE GLOSSARY `booking_instruction`. BRAK tabeli. BRAK auto-suggest SQL z macierzy. BRAK HTTP armatora.

**Odrzucone:**

- Auto-INSERT `suggested` z JOIN do I1 (import serwisu macierzy). Operator zapisuje wiersz.
- Dwa `mail_draft` przy DAP `contact_exchange` — leftover Fali O / S18.
- Live HTTP S21 / booking armatora.
- `party_id` na tej tabeli (duplikat I2).
- Import serwisów shipments / incoterm_responsibilities / mail_drafts / charges.

## Zakres

1. Migracja `088_booking_instruction`: RLS, izolacja, złożone FK, indeks `(organization_id, shipment_id)`.
2. POST/GET. Override = nowy wiersz. Obcy scope / rola / status → 400.
3. UI `/shipments`. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

mail_draft DAP · S21 · U4 · C8 · NBP · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje wiersz; `booking_scope=ocean_booking` albo `status=queued` → 400
- [x] Ta sama para zlecenie×scope×rola z innym statusem = nowy wiersz, stary `superseded_by`
- [x] GET po `shipment_id` zwraca bieżące wiersze
- [x] Serwis bez importu shipments / incoterm_responsibilities / mail_drafts / charges
- [x] Test izolacji tabeli
- [x] Zero float / marży / HTTP armatora

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST 400 na ocean_booking/queued; override = nowy wiersz; GET bieżące; izolacja |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | EXPLAIN | `ix_booking_instruction_org_shipment` (GET po zleceniu + filtr `superseded_by IS NULL`) |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | jeden BC; brak party_id; S21 leftover |
| Proza operatora | 5–15 zdań | wtopione w katalogi.md, 40 linii |
| Docs/OS | PRZESZŁO | CURRENT · just docs |
| Pushy do zielonego | N/A | billing GitHub zablokowany — zgoda sesji `/noc 17` |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
