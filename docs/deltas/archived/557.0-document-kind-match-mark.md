# Plaster 557.0 Â· C8 leftover â€” HITL `document_kind_match_mark`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala C Â· C8

**ZaleĹĽy od:** tenancy Â· wzorzec HITL mark Â· C8 `party_document` / `relation_document_requirement` (obok)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy C8 leftover. Nowa tabela `document_kind_match_mark`: `mark_code` (snake 2â€“32) + `match_kind` `exact|alias|missing|other` + `source_ref`. Append-only. RLS. Katalog stancji dopasowania rodzaju dokumentu â€” nie live matching do `party_document`.
- **Wybrane:** POST/GET `/api/v1/document-kind-match-marks`. OpenFGA `can_manage_document_kind_match_marks`. UI `/document-kind-match-marks`. `SHIPPED["557.0"]`.
- **Odrzucone:** live matching SQL do `party_document` (`REJECTED` â†’ leftover). 409 / blocks_create egzekucja (`REJECTED` â†’ leftover). Kwota / float / scoring (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / ranking SQL / PUESC / bind filing.

**Ĺowca:** BRAK `document_kind_match_mark`. ISTNIEJE `party_document` (rodzaj na party). ISTNIEJE `relation_document_requirement` (zestaw relacji). ISTNIEJE `create_block_mark` (brama create).

## Zakres

1. Migracja `437_doc_kind_match_mark` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["557.0"]` â†’ `/document-kind-match-marks`.

## Poza zakresem

matching SQL Â· party_document live Â· 409 Â· PUESC Â· AI3 Â· G0 Â· kwota

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / matching

## Leftover

matching SQL Â· party_document bind Â· 409 egzekucja
