# Plaster 521.0 · AI9.0 — `article50_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI9.0 · po [520.0](../archived/520.0-impact-edge-mark.md)

**Zależy od:** AI9.1 `automation_bias_mark` · AI9.2 `compliance_program_mark` zostają osobno

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `article50_marks`. Tabela `article50_mark`: `mark_code` + `label_kind` `generated|exempt|human|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja AI9.0 — **etykieta art. 50** jako dane HITL (VISION), nie przebudowa UI U-art50.
- **Wybrane:** POST/GET `/api/v1/article50-marks`. OpenFGA `can_manage_article50_marks`. UI `/article50-marks`. `SHIPPED["521.0"]`.
- **Wybrane:** how-to `docs/operator/article50-mark.md`.
- **Odrzucone:** mutacja `automation_bias_mark` / `compliance_program_mark` / `field_confidence_mark` (`REJECTED` — osobne BC).
- **Odrzucone:** U-art50 przebudowa UI / live LLM label / scoring (`REJECTED`).
- **Odrzucone:** float / amount (`REJECTED`).

**Łowca:** ISTNIEJE `automation_bias_mark` · `compliance_program_mark` · `field_confidence_mark`. BRAK `article50_mark`.

## Zakres

HITL `article50_mark` — migracja 411 + warstwy + OpenFGA + FE + how-to + SHIPPED 521.0.

## Poza zakresem

U-art50 UI · live LLM label · scoring · kwota · UPDATE/DELETE

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zły kind → 400; amount → 422
- [x] UI `/article50-marks`; `SHIPPED["521.0"]`
- [x] craft_style; how-to

## Leftover

U-art50 UI · live label na treści modelu
