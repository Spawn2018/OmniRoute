# Plaster 32.0 · M-39 EDI (`edi_message` tablica odczytu)

**Spec źródłowa:** [docs/spec/edi-message.md](../../spec/edi-message.md)  
**Zależy od:** 13.0 `channel_quote` · 23.0 `quotationCarrierInquiries` · 5.1 lane na `quotation`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie X12.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 5 M-39):** EDI **nie ma tabeli komunikatów** w repo. Operator widzi `channel_quote` dopasowane do lane wyceny — jedyny znany komunikat z kanału. Token `edi_message` (UI, nie tabela). Żywy ID **M-39**. 23.0 zostaje śladem na `/quotations`; 32.0 = jeden odczyt.

**Łowca:** BRAK `edi` / EDIFACT / X12 / IFTMIN w modelach. ISTNIEJE: `fetchChannelQuotes`, `quotationLanes`, `quotationCarrierInquiries`. PODOBNE: 23.0 zapytanie do armatora, 31.0 `source_ref` własnej wyceny.

**Odrzucone:**

- Tabela EDI / parser X12 — zgadywanie schematu (CURRENT).
- Live HTTP do armatora — leftover 13.0.
- LLM składający komunikat.

## Zakres

Read-only `/edi`. `data-edi-message="board"`. Brak migracji. `quotationCarrierInquiries` + `<Money/>`. Linki do `/channel-quotes` i `/quotations`.

## Poza zakresem

Tabela EDI · X12 / EDIFACT · live HTTP · Temporal · ExtractionService

## Ustalenia

- Token: `edi_message` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations` i uprawnienia `channel_quote`.
- RLS: bez nowej tabeli.
- Job: `/edi`. `SHIPPED_CHARGE_ROUTES["32.0"]` = `/edi`. OPS_JOBS +1 → 29.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/edi` ma `data-edi-message="board"` i woła `quotationCarrierInquiries`
- [x] Brak nowej migracji; brak `x12` / `edifact` / `iftmin` w ekranie
- [x] `SHIPPED_CHARGE_ROUTES["32.0"]` = `/edi`
- [x] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `channel_quote` na lane; brak parsera i live HTTP |
| Szybkość | N/A ten plaster | odczyt lokalnych list |
| Dług w diffie | leftover | X12 / EDIFACT / live HTTP zostaje |
| Docs/OS | PRZESZŁO | spec `edi-message.md`; CURRENT → Plan M-40 |

## Knowledge

Nie RAG. Nie parser EDI.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Parser EDI i live HTTP świadomie leftover.
