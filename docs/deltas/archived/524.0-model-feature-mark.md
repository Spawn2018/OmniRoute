# Plaster 524.0 Â· AI5.1 â€” `model_feature_mark` HITL

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ AI5.1 Â· po [523.0](../archived/523.0-ingest-gate-mark.md)

**ZaleĹĽy od:** AI5.0 `data_source` + `ingest_gate_mark` Â· cechy jako etykiety, nie live train

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `model_feature_marks`. Tabela `model_feature_mark`: `mark_code` + `feature_kind` `numeric|categorical|derived|other` + `source_ref`. Append-only INSERT. RLS + izolacja. WÄ™ĹĽsza opcja AI5.1 â€” **etykieta cechy modelu jako dana HITL**, nie live train / feature store.
- **Wybrane:** POST/GET `/api/v1/model-feature-marks`. OpenFGA `can_manage_model_feature_marks`. UI `/model-feature-marks`. `SHIPPED["524.0"]`.
- **Wybrane:** how-to `docs/operator/model-feature-mark.md`.
- **Odrzucone:** live train Â· feature store SQL Â· embedding / pgvector Â· auto-champion (`REJECTED`).
- **Odrzucone:** float / amount / score cech / FK do `data_source` w tym plasterze (`REJECTED`).

**Ĺowca:** ISTNIEJE `data_source` Â· `ingest_gate_mark` Â· `suggestion_kind`. BRAK `model_feature_mark`.

## Zakres

HITL `model_feature_mark` â€” migracja 414 + warstwy + OpenFGA + FE + how-to + SHIPPED 524.0.

## Poza zakresem

live train Â· AI6 SQL grafu Â· KPI wzĂłr z modelu Â· float

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zĹ‚y kind â†’ 400; amount â†’ 422
- [x] UI `/model-feature-marks`; `SHIPPED["524.0"]`
- [x] craft_style; how-to

## Leftover

live train Â· wiring cech do modelu Â· AI6 leftover SQL

