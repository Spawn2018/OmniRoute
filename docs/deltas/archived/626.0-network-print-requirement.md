# Plaster 626.0 · D9c katalog wymogu wydruku sieci

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D9 · [162.0](../archived/162.0-document-template.md)
**Zależy od:** 162.0 `document_template` (`own_label`|`cmr`) zostaje osobno
**Status:** Zamknięty — plaster 626.0 (`/noc 17` 2026-09-22).

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D9c 409 sieci):** szablon wydruku już jest. Etykieta sieci i blokada wyjazdu zostają rozdzielone. Żywy plaster: tabela `network_print_requirement` per tenant — `requirement_code` + `network_label` (tekst 1–64) + `source_ref`. Nowy wiersz = INSERT. Brak 409 na zleceniu i na wyjeździe. Brak bajtów PDF. `SHIPPED_CHARGE_ROUTES["626.0"]` = `/network-print-requirements`. OpenFGA: `can_manage_network_print_requirements`. RLS jak plaster 0.3. Marża zostaje na `charge`.

**REQUIREMENT:** leftover 162.0 „`network_print_requirement` / 409 wyjazdu”. Ten plaster = sam katalog. 409 = poza. Źródło: `docs/deltas/archived/162.0-document-template.md`.

**Łowca:** ISTNIEJE `document_template`. ISTNIEJE `document_checklist_rule`. BRAK `network_print_requirement`.

**Odrzucone:**

- HTTP 409 na wyjeździe / zleceniu
- PDF / QR / ZPL
- rozszerzenie `template_kind` o etykietę sieci
- kwota / marża / float

## Zakres

1. Migracja z RLS i testem izolacji.
2. POST/GET katalogu. Zły kod albo pusta etykieta → 400.
3. Ekran `/network-print-requirements` i zdanie operatora.

## Poza zakresem

409 · PDF · QR · ZPL · C8 · WMS · mapa

## Plan plików

- migracja `498_network_print_requirement` po `497_groupage_tariff_volume`
- model, domena, repozytorium, serwis, API
- test izolacji + HTTP
- frontend katalog + trasa w `ops-index`
- `docs/operator/` jedno how-to

## Kryteria akceptacji

- [x] POST zapisuje kod i etykietę; GET zwraca wiersze tenanta
- [x] obcy tenant nie widzi wiersza
- [x] brak 409 na shipment; zero bajtów / marży / float
