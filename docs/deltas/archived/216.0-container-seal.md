# Plaster 216.0 · T3 `seal_no_1` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [153.0](../archived/153.0-container.md) leftover plomby · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 153.0 `container`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie booking. Nie `seal_no_2`/`seal_no_3`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 plomby/PIN/VGM + leftover T1b tabela + leftover T1 EXP1 + leftover T2c km/`party`/`/fleet` + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T1 EXP1 parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN (sekret) parked. leftover T3 VGM/cutoffy parked. leftover `seal_no_2`/`seal_no_3` parked. Karta T3: `seal_no_1` = SPEED pierwsza plomba HITL, **nie** PIN i **nie** waga. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `seal_no_1` (1–32 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token (nie tekst, za długi) → 400 (`plomba`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["216.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `155_container_seal`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container` — ISO 6346 + typ + opcjonalne zlecenie, **nie** plomba. ISTNIEJE `stop.notes_for_driver` — notatka punktu, **nie** plomba kontenera. BRAK `seal_no_1`. BRAK PIN. BRAK VGM.

**Odrzucone:**

- `pin_code` / ciphertext (leftover T3 PIN, zakaz BC).
- VGM / TARA / Decimal waga (leftover EXP1).
- `seal_no_2` / `seal_no_3` (kolejny plaster).
- `booking_no` / live HTTP armatora.
- T1b tabela · T1 EXP1 na `stop` · T2c km · T7b `charge` · T5 outbox · S53 Auth0.

## Zakres

1. Migracja `155_container_seal` (po `154_stop_notes`): `seal_no_1` nullable `String(32)`.
2. POST opcjonalny `seal_no_1`. Zły token → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · `seal_no_2` · `seal_no_3` · booking · km · mapa · T7b · T5 · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST z `seal_no_1` zapisuje plombę, bez PIN
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`plomba`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / `/fleet` / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `seal_no_1`; 400 `plomba`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(32)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie PIN; leftover T3 PIN/VGM/`seal_no_2`; C901 czysty; bez nowego OPS job |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
