# Plaster 444.0 · AI2.1 `version_score` — ranking wersji modelu

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI2.1

**Zależy od:** 443.0 `interval_score` · 432.0 `suggestion_ledger`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `version_score` — widok `security_invoker` grupujący `interval_score` × `suggestion_ledger.model_version`. Postgres liczy `pair_count`, `avg_mae`, `avg_crps` (`Numeric(14,4)`). Operator widzi ranking, nie wpisuje go.
- **Wybrane:** tylko GET. Brak POST. Brak flagi „to jest champion”.
- **Wybrane:** nowy widok, nie rozbudowa `interval_score` o agregat (para zostaje parą).
- **Odrzucone:** auto-przełączenie modelu / zapis champion (`REJECTED` — człowiek decyduje).
- **Odrzucone:** dryf (`REJECTED` na 444.0 — brak osi czasu na widoku; leftover **445.0**).
- **Odrzucone:** Brier · zapis `prediction_ledger` · float · druga marża · SQL na `charge` · import serwisów ledgerów.

**Łowca:** PODOBNE `interval_score` (443.0) — metryka **pary**. PODOBNE `prediction_ledger` — wpisane CRPS per wiersz. BRAK agregatu per `model_version`. Nie klon ręcznego champion.

## Zakres

Migracja `354` (revises `353_interval_score`). UI `/version-scores` tylko odczyt. OpenFGA `can_manage_version_scores`. `SHIPPED["444.0"]`. OPS_JOBS +1.

## Poza zakresem

dryf · auto-champion · Brier · zamknięcie zapisu `prediction_ledger` · trzy BC · `data_source` · AI4.1

## Kryteria akceptacji

- [x] GET zwraca `avg_mae`/`avg_crps`/`pair_count` z SQL (nie z body)
- [x] POST → 405
- [x] izolacja RLS widoku; brak `float`; craft_style funkcje>40: 0
- [x] brak UPDATE `prediction_ledger` i brak flagi champion

## Leftover

dryf (**445.0**) · auto-champion · Brier · zapis `prediction_ledger` · trzy BC · `data_source` (AI5)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | widok GROUP BY + średnie SQL; GET; POST 405 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | leftover | widok `security_invoker`; EXPLAIN nie w tym plasterze |
| N+1 | OK | jeden SELECT widoku |
| Dług / człowiek | OK | brak Pythona na średnich; nowy BC zamiast flagi champion |
| Proza operatora | 5–15 zdań | `docs/operator/version-score.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441/442/443 |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
