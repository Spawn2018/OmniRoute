# Plaster 263.0 · T5 `task_template` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T5 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T5 · [decyzja-operatora-wizja.md](../../analysis/decyzja-operatora-wizja.md) #8
**Zależy od:** 0.3 tenancy · 12.0 `port_surcharge.applies_when` (wzorzec danych, nie import)
**Status:** Zamknięty `/noc` 2026-09-10. Nie matching SQL. Nie instancja `task`. Nie outbox. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover lookup/KSeF TE + leftover T5 + leftover outbox T5 + leftover rollup drzewa + leftover T7b + leftover N3 + S53 + PIN):** leftover C3 live VIES/GUS parked (HTTP). leftover F1 live FA(3)/KSeF parked (HTTP). leftover instancja `task` (shipment/trip/stop, assignee, status) parked. leftover matching SQL `applies_when` parked. leftover outbox/async parked. leftover M-71 done na zadaniu parked. leftover rollup parked. leftover T7b parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **nowa tabela** `task_template` per tenant — HITL szablon zadania jako dane, nie silnik i nie worker. Wiersz = `template_code` (snake 2–32) + `applies_when` (tekst 1–512, dana jak extra portowe, nie parser AST) + `source_ref` (`tenant:manual` albo `fixture://task-template/…`). Operator wpisuje kod i warunek; serwis nie ewaluuje warunku, nie tworzy zadania na zleceniu, nie woła HTTP, nie woła LLM i nie czyta `charge` / `shipment` / `outbox_event`. Zły kod → 400 (`szablon`). Zły warunek → 400 (`warunek`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, template_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/task-templates`. `SHIPPED_CHARGE_ROUTES["263.0"]` = `/task-templates`. OpenFGA: `can_manage_task_templates`. Migracja `196_task_template` (po `195_consignment`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `operator_notice` — inbox, **nie** szablon zadania. ISTNIEJE `operator_decision` — szyna Akceptuj/Zmień/Odrzuć, **nie** task engine. ISTNIEJE `document_checklist_rule` — trójka Incoterms, **nie** T5. ISTNIEJE `charge_template` — kolekcja kodów opłat, **nie** zadanie. ISTNIEJE `port_surcharge.applies_when` — extra portowe, **nie** T5. ISTNIEJE SOP `blocks_auto` — nie generator zadań. BRAK tabeli `task_template`. BRAK tabeli `task`. BRAK workera.

**Odrzucone:**

- live VIES/GUS (C3) · live KSeF FA(3) (F1).
- tabela `task` / FK `shipment_id` / `trip_id` / `stop_id` / assignee.
- ewaluacja `applies_when` w SQL albo parser AST.
- outbox / Temporal / konsument M-02.
- JSONB warunków · LLM piszący reguły · kwota / marża / float.

## Zakres

1. Migracja `196_task_template`: RLS FORCE, izolacja, unique kod + `source_ref`. Nie kolumny na `shipment`.
2. POST/GET. Zły token → 400 (`szablon` / `warunek` / `obce`). Serwis nie importuje shipments/charges/outbox i nie woła HTTP.
3. UI `/task-templates`. How-to; fold w `katalogi.md` (40 linii). Job OPS 110. OpenFGA `can_manage_task_templates`.

## Poza zakresem

instancja `task` · matching SQL · outbox · live VIES · live KSeF · M-71 · Auth0 · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `task_template` z `template_code` + `applies_when` + `source_ref`, bez HTTP
- [x] zły kod → 400 (`szablon`); zły warunek → 400 (`warunek`)
- [x] GET zwraca szablony tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / matchingu / instancji `task` / live HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL szablon; 400 `szablon`/`warunek`/`obce`; izolacja RLS |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | indeks `ix_task_template_organization_id`; test izolacji woła EXPLAIN listy |
| N+1 | OK | GET jedna lista; POST jeden INSERT |
| Dług / człowiek | OK | wzór HITL katalogu; leftover instancja `task` / matching / outbox |
| Proza operatora | 5–15 zdań | `szablon-zadania.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T5, GLOSSARY, spec `task-template.md` |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (263.0)

- Migracja `196_task_template` po `195_consignment`; FORCE RLS; unique kod + `source_ref`.
- `TaskTemplateService.record_template` waliduje snake / warunek / `source_ref` w domenie.
- OpenFGA `can_manage_task_templates`. UI `/task-templates`. Job OPS 110.
- `SHIPPED_CHARGE_ROUTES["263.0"]` = `/task-templates`.

## Leftover po 263.0

Instancja `task` (FK zlecenie/przejazd/punkt, assignee, status). Matching SQL `applies_when`. Outbox / konsument M-02. Live VIES i live KSeF zostają parked HTTP.
