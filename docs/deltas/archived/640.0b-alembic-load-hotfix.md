# Hotfix 640.0b · alembic load

**Status:** Zamknięty `/noc 17` 2026-09-23.

Usunięto `sa.SchemaItem` z annotacji `_table_constraints` w `512_local_charge_match_mark`
(AttributeError przy `alembic upgrade` w CI). Zero nowej tabeli. Zero matching SQL.
