# Plaster 254.0 · T1 EXP1 `waiting_started_at` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [253.0](253.0-stop-waiting.md) leftover `waiting_started_at` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 253.0 `waiting_free_minutes`
**Status:** Zamknięty `/noc` 2026-09-10. Nie countdown. Nie POD. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 `waiting_started_at`/POD + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover POD parked. leftover `appointment_status` / `no_show_at` parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `waiting_started_at` (`DateTime(timezone=True)` HITL, ISO-8601 ze strefą, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`początek`). `require_stop_waiting_started_at` w `app.domain.stop` — opcjonalny zegar, reużywa `_require_eta_clock`, nie import `free_time_clocks`. `_HitlTail` dostaje jedno pole. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["254.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `190_stop_waiting_started` (po `189_stop_waiting`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.waiting_free_minutes` (253.0). ISTNIEJE `stop.eta_physical` / `eta_legal` (194.0). ISTNIEJE `free_time_clock` (196.0, inny BC). BRAK `stop.waiting_started_at`. BRAK `pod_quality`.

**Odrzucone:**

- Countdown remaining / remaining minutes w Pythonie / szkic `charge`.
- POD / D3 / T8 / PIN / import `free_time_clocks` / km / mapa / T5 / S53 / live HTTP / float.

## Zakres

1. Migracja `190_stop_waiting_started`: `waiting_started_at` nullable timestamptz. Nie `create_table`.
2. POST opcjonalny `waiting_started_at`. Zły token → 400 (`początek`). Serwis nie importuje free_time_clocks/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

countdown · POD · tabela `stop_group` · D3 · T8 · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `waiting_started_at` zapisuje start oczekiwania HITL, bez odliczania
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / bez strefy / nie ISO → 400 (`początek`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `free_time_clocks`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `waiting_started_at`; 400 `początek`; pusty → null; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | timestamptz nullable; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | reużywa `_require_eta_clock`; leftover POD; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
