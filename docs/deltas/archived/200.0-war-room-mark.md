# Plaster 200.0 · W2 `war_room_mark` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala W · [karty-pol-fala-w.md](../../analysis/karty-pol-fala-w.md) § W2 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) § N8
**Zależy od:** 0.3 tenancy (katalog per tenant) · 94.0 `/watchtower` (tablica wyjątków zostaje odczytem) · 198.0 `tower_impact` (łańcuch zostaje osobnym katalogiem)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie drugi czat. Nie N8. Nie T8 live API.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover W2 widok M-37+S11+V6+T8+mapa + N8 koalescencja + leftover V7 tacho + V8 what-if + leftover V6 EBITDA + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7 tacho TO_VERIFY parked. leftover V8 what-if parked. leftover V6 silnik EBITDA / `sla_clause` parked. leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b parked. leftover V4 AIS parked. leftover V2b myto parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. Widok sklejający wyjątki + pending + impact + T8 + mapa zostaje leftover. Koalescencja alertów N8 zostaje leftover. T8 `confirmed` tylko z API zostaje leftover. Żywy plaster: jedna tabela `war_room_mark` per tenant — **HITL znacznik incydentu sali jako dane**, nie drugi czat i nie silnik alertów. Wiersz = `incident_kind` (`weather` | `congestion` | `labor` | `carrier` | `credit` | `other`) + `source_ref` (`tenant:manual` albo `fixture://war-room-mark/…`). Operator wpisuje rodzaj incydentu; serwis nie scala alertów, nie woła HTTP, nie woła LLM, nie czyta `charge` / `operational_exception` / `tower_impact` / `operator_decision` i nie składa `/watchtower`. Zły rodzaj → 400 (`incydent`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/war-room-marks` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["200.0"]` = `/war-room-marks`. OpenFGA: `can_manage_war_room_marks`. Migracja `140_war_room_mark`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `/watchtower` 94.0 / 116.0 / 124.0 — wyjątki + pending S11 + liczniki + leniwa mapa, **nie** sala. ISTNIEJE `operational_exception` — wyjątek na zleceniu, **nie** incydent sali. ISTNIEJE `tower_impact` — etap łańcucha, **nie** war room. ISTNIEJE `operator_notice` / `operator_decision` — inbox i szyna, **nie** koalescencja. ISTNIEJE `twin_mark` — rodzaj bliźniaka, **nie** incydent. BRAK tabeli `war_room_mark`. BRAK silnika N8. BRAK T8 slot API.

**Odrzucone:**

- Widok sklejający M-37 + pending S11 + V6 + T8 + mapa lazy.
- Koalescencja alertów N8 / drugi czat.
- T8 slot `confirmed` tylko z live API / Selenium.
- Silnik EBITDA / sla_clause / scoring osoby / fizyka twinów.
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `140_war_room_mark` (po `139_twin_mark`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind.
2. POST/GET. Zły rodzaj → 400. Serwis nie importuje exceptions / tower_impacts / charges / extraction i nie woła HTTP.
3. UI `/war-room-marks`. `just api-types`. How-to `docs/operator/war-room.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `RoomDesk` / `roomWrite` / `persistRoomMark`. Formularz radio (nie `<select>` klonów HITL).

## Poza zakresem

N8 koalescencja · drugi czat · T8 live API · mapa · watchtower glue · silnik EBITDA · fizyka twinów · live GPS · AIS · myto → charge · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [x] POST zapisuje `war_room_mark` z `incident_kind`, bez HTTP
- [x] zły `incident_kind` → 400 (`incydent`)
- [x] obce `source_ref` → 400 (`obce`)
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / koalescencji / live HTTP / scrape / drugiego czatu

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST `incident_kind`; 400 `incydent` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_war_room_mark_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover N8 / T8 / widok; formularz radio (nie select) |
| Proza operatora | 5–15 zdań | `war-room.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN W2, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
