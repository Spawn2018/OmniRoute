# Plaster 244.0 · leftover 2a `inquiry_queued` z API

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Oś leftoverów `/noc` (2a/2b) · [133.0](../archived/133.0-entity-event.md) · [136.0](../archived/136.0-carrier-inquiry-batch.md) · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) B0a
**Zależy od:** 133.0 `entity_event` · 136.0 `carrier_inquiry`
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie outbox. Nie Temporal.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover eventy 2a/2b + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 Auth0 parked. PIN parked. leftover 2b parked (`inquiry_sent` / `quote_recorded` / batch / `channel_quote`). Żywy plaster: **składanie w warstwie API** — po udanym POST `/carrier-inquiries` (jeden wiersz), gdy `status=queued`, API woła `EntityEventService.create_event` (`event_kind=inquiry_queued`, `subject_kind=carrier_inquiry`, `subject_id` = id zapytania, `source_ref` z wiersza). Wzorzec: `inbound_messages` → `OutboxEventService`. **CarrierInquiryService nie importuje `entity_events`.** EntityEventService nie importuje inquiries. Domyślny `draft` nie pisze zdarzenia. Brak nowej tabeli / migracji. OpenFGA bez nowej relacji — zostaje `can_manage_networks` na POST zapytania. UI: ledger `/entity-events`. `SHIPPED_CHARGE_ROUTES["244.0"]` = `/entity-events`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `entity_event` (133.0, allowlista kind + subject). ISTNIEJE POST `/carrier-inquiries` (136.0). ISTNIEJE składanie API `inbound_messages` → outbox. BRAK append `inquiry_queued` przy zapisie zapytania. PODOBNE: składanie w API, nie import BC↔BC. Nie live HTTP. Nie nowa tabela.

**Odrzucone:**

- Import `entity_events` w `CarrierInquiryService`.
- 2b: `inquiry_sent` / `quote_recorded` / batch / POST `channel_quote`.
- FK `entity_event.subject_id` → `carrier_inquiry` (133.0: wskazanie UUID bez FK).
- T1b · T2c km · T7b · T5 · N3 · S53 · PIN · live HTTP · Temporal.

## Zakres

1. API `create_carrier_inquiry`: po INSERT, gdy status `queued`, append `entity_event` w tej samej transakcji (commit jak dziś).
2. Test HTTP: queued → jeden INSERT zdarzenia; draft → zero zdarzeń; serwis inquiries bez importu entity_events.
3. Fold w how-to `entity-event.md`. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

2b · batch · `channel_quote` · FK subject · outbox consumer · Temporal · PIN · km · mapa · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST zapytania ze `status=queued` dopisuje `inquiry_queued` na `carrier_inquiry`
- [x] POST bez statusu / `draft` nie dopisuje zdarzenia
- [x] Zero importu `entity_events` w serwisie inquiries; zero importu inquiries w serwisie zdarzeń
- [x] RLS istniejących tabel; izolacja bez nowej tabeli
- [x] Zero float / marży / live HTTP / Temporal / JSON payload

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | queued → `inquiry_queued`; draft → zero; składanie API |
| Szybkość | N/A ten plaster | jeden INSERT zdarzenia, nie ścieżka z budżetu AGENTS |
| SQL | N/A | brak nowej tabeli / migracji |
| N+1 | OK | jeden append po jednym INSERT |
| Dług / człowiek | OK | leftover 2b; PIN parked |
| Proza operatora | 5–15 zdań | fold w `entity-event.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN B0a, how-to |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
