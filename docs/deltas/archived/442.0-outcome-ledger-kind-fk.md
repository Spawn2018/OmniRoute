# Plaster 442.0 · AI1.4 leftover FK `outcome_ledger` → `outcome_kind`

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 441.0 `outcome_kind` · 433.0 `outcome_ledger` · wzorzec 439.0

## Wybrane / odrzucone / dlaczego

- **Wybrane:** zdjąć CHECK `eta|rate|route|other` z `outcome_ledger.outcome_kind`. Walidacja wobec słownika = FK `(organization_id, outcome_kind)` → `outcome_kind (organization_id, kind_code)` `ON DELETE RESTRICT` (VISION B.3). Węższa opcja z kolejki Q — ten sam ruch co **439.0**, nie nowa tabela.
- **Wybrane:** kolumna zostaje tekstem `kind_code` (snake), nie UUID. Szerokość 32 jak słownik (dziś ledger ma 16).
- **Wybrane:** backfill DISTINCT starych kodów jako wiersze `outcome_kind` per organizacja, która już ma ledger — żeby upgrade nie padł.
- **Wybrane:** serwis ledgeru **nie** importuje `outcome_kinds` (AGENTS). Brak wiersza słownika = `IntegrityError` → 400 (`fk_outcome_ledger_kind`).
- **Odrzucone:** `data_source` (AI5) · CRPS (AI2) · silnik L3+ · trzy BC autosave · FK do `suggestion_ledger`.
- **Odrzucone:** import `app.services.outcome_kinds` · seed globalny poza tenantem · UPDATE/DELETE ledgeru.

**Łowca:** PODOBNE 439.0 (FK `suggestion_ledger`) i 440.0 (FK `twin_mark`). PODOBNE CHECK na `outcome_ledger` (433.0) i otwarty słownik `outcome_kind` (441.0). BRAK FK. Nie klon nowej tabeli.

## Zakres

Migracja `352` (revises `351_outcome_kind`). Domain: rodzaj = snake, bez listy. Test: `tender` przechodzi w parse/HTTP unit; brak wiersza słownika na bazie → IntegrityError. UI ledgeru: pole tekstowe zamiast select czterech.

## Poza zakresem

data_source · CRPS · trzy BC autosave · FK do suggestion_ledger · AI4.1

## Kryteria akceptacji

- [x] brak `IN ('eta'` w modelu/migracji 352; jest FK RESTRICT
- [x] POST ledger `tender` (parse + HTTP unit) → 201
- [x] INSERT ledger bez wiersza słownika → IntegrityError
- [x] izolacja RLS bez zmian semantyki; craft_style funkcje>40: 0

## Leftover

`data_source` (AI5) · trzy BC · AI4.1 · CRPS (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CHECK zdjęty, FK RESTRICT, backfill, UI otwarty |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | backfill DISTINCT + FK; brak nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | IntegrityError mapowany po nazwie constraint |
| Proza operatora | 5–15 zdań | `docs/operator/outcome-ledger.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | | po pushu |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
