# Plaster 230.0 · T3 `pickup_terminal` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [229.0](../archived/229.0-container-reefer.md) leftover party/terminal · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 229.0 `reefer`
**Status:** Zamknięty `/noc` 2026-09-10. Nie FK. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 party/terminal/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK do `party`). leftover `return_terminal` parked. leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `pickup_terminal` (1–32 znaki po trim, jak `packaging_code`), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`terminal`). HITL tekst, nie FK do `terminal` i nie import geography w serwisie. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["230.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `169_container_pickup_terminal` (po `168_container_reefer`). `_WriteBox` — dodać `dock`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container` + `reefer`. BRAK kolumny `pickup_terminal` / `pickup_terminal_id` / `carrier_party_id` (zero trafień w `backend/app/models/container.py`). PODOBNE: `require_packaging_code` (opcjonalny token 1–32). Nie duplikować BC geography `terminal`.

**Odrzucone:**

- FK `pickup_terminal_id` / `return_terminal_id` / `carrier_party_id` w tym plasterze.
- PIN / VGM / `booking_no` / live HTTP.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `169_container_pickup_terminal` (po `168_container_reefer`): `pickup_terminal` nullable `String(32)`.
2. POST opcjonalny `pickup_terminal`. Zły token → 400. Serwis nie importuje geography/parties/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

FK terminal/party · return · PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `pickup_terminal` zapisuje token terminalu pobrania, bez FK i bez PIN
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`terminal`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `pickup_terminal`; 400 `terminal`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(32)`; brak FK; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `return_terminal`; PIN/VGM parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
