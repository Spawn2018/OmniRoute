# Plaster 469.0 · BR4.3 NAC i agent nominowany

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR4.3 · I2 `shipment_stakeholder` · I3 `document_dispatch_rule`

**Zależy od:** tenancy RLS · wzorzec HITL `lcl_console_mark` (468.0) · moduł I (incoterm / dispatch) już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `nac_marks`. Tabela `nac_mark`: `mark_code` + `nac_kind` `nac|nominated|agent|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance NAC / agenta nominowanego w przepływie dokumentowym, nie FK do `shipment_stakeholder` i nie live NAC HTTP.
- **Wybrane:** POST/GET `/api/v1/nac-marks`. OpenFGA `can_manage_nac_marks` (member). UI `/nac-marks`. `SHIPPED["469.0"]` → `/nac-marks`.
- **Wybrane:** Alembic `374_nac_mark` (`down_revision = "373_lcl_console_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumna na `shipment_stakeholder` (`REJECTED` — I2 zostaje rola + party_id, nie NAC).
- **Odrzucone:** klej FK do `shipment_stakeholder` / `document_dispatch_rule` / `party` (`REJECTED` na tym wierszu).
- **Odrzucone:** live NAC HTTP · auto-send dokumentów · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `shipment_stakeholder` (I2 rola na zleceniu). ISTNIEJE `document_dispatch_rule` (I3 adresat). ISTNIEJE `haulier_role_mark` (booked/actual). ISTNIEJE `collaboration_mark` / `three_way_mark` (role współpracy). PODOBNE katalog HITL roli. BRAK `nac_mark`. BRAK katalogu NAC / agenta nominowanego.

## Zakres

How-to: jedno zdanie — to katalog stance NAC i agenta nominowanego; live NAC i auto-dispatch zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (radio fieldset jak groupage_dispatcher, nie klon 468) pod jscpd < 3%.

## Poza zakresem

live NAC HTTP · auto-send · FK do `shipment_stakeholder` · FK do `document_dispatch_rule` · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live NAC HTTP · FK do `shipment_stakeholder` / `document_dispatch_rule` · auto-send dokumentów · BR4.4 Jedwabny Szlak
