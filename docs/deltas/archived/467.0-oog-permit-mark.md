# Plaster 467.0 · BR4.1 ładunki ponadnormatywne: zezwolenie i pilotaż

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR4.1 · G5 `oog_mark`

**Zależy od:** tenancy RLS · wzorzec HITL `ferry_booking_mark` (466.0) · `oog_mark` już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `oog_permit_marks`. Tabela `oog_permit_mark`: `mark_code` + `permit_kind` `permit|pilot|route|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance zezwolenia/pilotażu/trasy, nie osie G5 i nie wymiary Decimal.
- **Wybrane:** POST/GET `/api/v1/oog-permit-marks`. OpenFGA `can_manage_oog_permit_marks` (member). UI `/oog-permit-marks`. `SHIPPED["467.0"]` → `/oog-permit-marks`.
- **Wybrane:** Alembic `372_oog_permit_mark` (`down_revision = "371_ferry_booking_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `oog_mark` (`REJECTED` — G5 zostaje oog/lashing/escort, nie zezwolenie).
- **Odrzucone:** klej FK do `oog_mark` / `trip` / `shipment` (`REJECTED` na tym wierszu).
- **Odrzucone:** wymiary Decimal · live zezwolenie HTTP · escort party FK · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `oog_mark` (G5 oog/lashing/escort). ISTNIEJE `load_order_mark` (kolejność). PODOBNE katalog HITL. BRAK `oog_permit_mark`. BRAK katalogu zezwolenia/pilotażu OOG.

## Zakres

How-to: jedno zdanie — to katalog stance zezwolenia OOG; wymiary i live urząd zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (lista + select, nie klon 464/465) pod jscpd < 3%.

## Poza zakresem

wymiary Decimal · live zezwolenie · escort FK · Valhalla · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

wymiary Decimal · live zezwolenie · escort party FK · FK do `oog_mark` · BR4.2 LCL

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS dwa tenanty; amount/metres 422/400 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | INSERT/SELECT katalogu |
| N+1 | OK | jeden SELECT listy |
| Dług / człowiek | OK | osobny BC obok `oog_mark`; bez FK |
| Proza operatora | 5–15 zdań | `docs/operator/oog-permit-mark.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–466 |
| Minuty plastra | 40 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
