# Plaster 287.0 · CT4 żywy 409 na shipment poza `routing_guide`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 leftover · [286.0](../archived/286.0-asn-routing-guide-409.md)
**Zależy od:** 286.0 ASN 409 · 285.0 `routing_guide_enforcement` · 279.0 `routing_guide` · M-35 `shipment`
**Status:** Zamknięty `/noc` 2026-09-11. Nie live EDI. Nie matching lane/mode.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama bramka co ASN: gdy tenant ma `routing_guide_enforcement.enforcement_kind=block_409`, POST `/shipments` wymaga `guide_code` z katalogu `routing_guide`; brak / nieznany → **HTTP 409** (`RoutingGuideOffGuide`). Składanie w API (BC `shipments` nie importuje services przewodnika). Opcjonalna kolumna `shipment.guide_code`. Reużywa `assert_asn_on_routing_guide` albo wspólny alias w `routing_guide_gate`. `record_only` = brak blokady.

**Łowca:** ISTNIEJE gate 286.0 na ASN + katalogi CT4. BRAK egzekucji 409 na shipment.

**Odrzucone:** matching lane/mode · live EDI · auto shipment z ASN · float · kwota.

## Zakres

1. Migracja `220_shipment_guide_code`: nullable `guide_code` + check snake.
2. Domain / gate (wspólny z ASN) + handler 409 już jest.
3. API `POST /shipments`: odczyt enforcements + guides, assert, potem `ShipmentService`.
4. FE pole `guide_code` na formularzu zlecenia; testy; how-to.

## Kryteria akceptacji

- [x] bez `block_409` → POST shipment bez guide_code = 201
- [x] z `block_409` i brak/nieznany guide_code → 409
- [x] z `block_409` i znany guide_code → 201 + zapis guide_code
- [x] BC shipments bez importu `routing_guide*` services
- [x] zero float; RLS bez zmiany polityki (ta sama tabela shipment)

## Leftover po 287.0

Matching lane/mode. Live EDI. Auto shipment. Wspólny SELECT CT11.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | 409/201 na POST shipments; API compose |
| Szybkość | N/A | bramka |
| SQL | N/A | list guides/enforcement istniejące |
| N+1 | OK | dwa listy przed INSERT |
| Dług / człowiek | OK | reużywa gate ASN |
| Proza operatora | 5–15 zdań | docs/operator/zlecenie.md |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | lokalny gate /noc |
| Minuty plastra | N/A | noc |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
