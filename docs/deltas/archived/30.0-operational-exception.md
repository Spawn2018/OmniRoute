# Plaster 30.0 · M-37 Wyjątki (`operational_exception` tablica odczytu)

**Spec źródłowa:** [docs/spec/operational-exception.md](../../spec/operational-exception.md)  
**Zależy od:** 5.1 POL/POD nullable na `quotation` · 28.0 `/shipments` · 29.0 `quotationLanes`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie AIS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 5 M-37):** wyjątek operacyjny **nie ma tabeli zdarzeń** w repo. Operator widzi wyceny z `party_id`, którym brakuje POL albo POD — nie wchodzą na `/tracking`. Token `operational_exception` (UI, nie tabela). Żywy ID **M-37**. 27.0 zostaje HITL/pending; 29.0 zostaje pełnym lane. Nie mylić z `party_charge_override` (wyjątek stawki).

**Łowca:** BRAK `exception` / `exception_event` / hold / delay / AIS w modelach. ISTNIEJE: `quotation` z `party_id` i nullable `origin_port_id` / `destination_port_id`, `quotationLanes` (pełny lane), `quotationAcceptancePending`. PODOBNE: 27.0 `/notifications`, 28.0 `/shipments`, 29.0 `/tracking`. Watchtower mapa parked (ADR-0003).

**Odrzucone:**

- Tabela wyjątków / ETA / hold — zgadywanie schematu (CURRENT).
- Mapa / Watchtower — parked; nie initial JS.
- Duplikat 27.0 (HITL pending) na tym ekranie.
- `party_charge_override` — inny byt (stawka, nie zlecenie).
- LLM klasyfikujący wyjątek.

## Zakres

Read-only `/exceptions`. `data-operational-exception="board"`. Brak migracji. Helper `quotationOperationalExceptions` (party + brak POL albo POD). Linki do `/shipments`, `/tracking` i `/quotations`.

## Poza zakresem

Tabela wyjątków · AIS · mapa · `shipment_leg` · M-38 dokumenty · EDI · ExtractionService

## Ustalenia

- Token: `operational_exception` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations`.
- RLS: bez nowej tabeli.
- Job: `/exceptions`. `SHIPPED_CHARGE_ROUTES["30.0"]` = `/exceptions`. OPS_JOBS +1 → 27.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/exceptions` ma `data-operational-exception="board"` i woła `quotationOperationalExceptions`
- [x] Brak nowej migracji; brak mapy; brak `eta` / `ais` w ekranie
- [x] `SHIPPED_CHARGE_ROUTES["30.0"]` = `/exceptions`
- [x] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | wyceny z party bez pełnego POL/POD; brak AIS i mapy |
| Szybkość | N/A ten plaster | odczyt lokalnych list |
| Dług w diffie | leftover | tabela zdarzeń / AIS / mapa zostaje |
| Docs/OS | PRZESZŁO | spec `operational-exception.md`; CURRENT → Plan M-38 |

## Knowledge

Nie RAG. Nie silnik wyjątków.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela zdarzeń, AIS i mapa świadomie leftover.
