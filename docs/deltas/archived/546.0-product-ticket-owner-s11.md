# Plaster 546.0 Â· Plat-HD leftover â€” S11 owner na `product_ticket`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Plat-HD-flow leftover Â· [545.0](../archived/545.0-product-ticket.md) Â· [544.0](../archived/544.0-margin-floor-s11.md)

**ZaleĹĽy od:** `product_ticket` HITL (545.0) Â· `operator_decision` M-71 (74.0 / 121.0 / 544.0)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy leftover Plat-HD S11 owner. `POST /product-tickets` z `ticket_kind=owner_ok` wymaga `reviewed_ticket_id` (istniejÄ…cy ticket tenanta). Bez accepted `owner_decision_id`: API tworzy/wznawia pending `operator_decision` (`subject_kind=product_ticket`, `subject_id=reviewed_ticket_id`) i zwraca **409** z `decision_id`. Z accepted `owner_decision_id` dla tego ticketu â†’ INSERT `owner_ok`.
- **Wybrane:** inne kind (`report|triage|other`) bez zmian; `owner_decision_id` / `reviewed_ticket_id` wtedy zabronione.
- **Wybrane:** compose w `product_tickets.py` (jak 544 charges) â€” nie import charges/decisions w `ProductTicketService`.
- **Odrzucone:** auto-fix / agent PR (`REJECTED`). UPDATE ticketu w miejscu (`REJECTED`). Mob Expo (`REJECTED`). FK do `product_ticket_mark` (`REJECTED`). CAPA (`REJECTED`).
- **Odrzucone teraz:** AI3-payload (park OpenAI CI), G0-SH (host/IdP), SH-R16-13 (Dependabot).

**Ĺowca:** ISTNIEJE `product_ticket` Â· ISTNIEJE `operator_decision` + pending/unique Â· ISTNIEJE wzorzec 544. BRAK `subject_kind=product_ticket`. BRAK 409 owner przy `owner_ok`.

## Zakres

1. Alembic `427_od_product_ticket`: CHECK + domena `_SUBJECTS`.
2. API create: `owner_ok` â†’ pending + 409 / accepted override â†’ 201.
3. Testy HTTP: 409+decision_id Â· retry po accept â†’ 201 Â· regresja report bez decyzji.
4. How-to `product-ticket.md`.
5. `SHIPPED["546.0"]` â†’ `/product-tickets`.

## Poza zakresem

auto-fix Â· Mob Â· FK mark Â· CAPA Â· mail Â· AI3-payload Â· G0-SH

## Kryteria akceptacji

- [x] POST `owner_ok` + `reviewed_ticket_id` bez decyzji â†’ 409 + pending + `decision_id`
- [x] drugi POST (ten sam reviewed) â†’ ten sam pending, 409
- [x] POST z `owner_decision_id` accepted â†’ 201
- [x] POST `report` bez pĂłl owner â†’ 201 (regresja 545)
- [x] zero auto-fix / UPDATE / float

## Leftover

FK mark Â· Mob Â· auto-fix / agent PR
