# Plaster 482.0 · AI9.1 automation bias — katalog HITL

**Status:** Zamknięty `/noc 20` 2026-09-14.

**Spec źródłowa:** [VISION.md](../../VISION.md) § D.5 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI9.1

**Zależy od:** tenancy RLS · wzorzec HITL mark · nie U-art50 label · nie AI9.2 rejestr

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `automation_bias_marks`. Tabela `automation_bias_mark`: `mark_code` + `bias_kind` `confirm|delay|review|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z AI9.1 — katalog HITL **stancji mitygacji** automation bias (potwierdzenie / opóźnienie / ponowny przegląd), nie przebudowa całego HITL UI i nie scoring osoby.
- **Wybrane:** POST/GET `/api/v1/automation-bias-marks`. OpenFGA `can_manage_automation_bias_marks` (member). UI `/automation-bias-marks`. `SHIPPED["482.0"]` → `/automation-bias-marks`.
- **Wybrane:** Alembic `387_automation_bias_mark` (`down_revision = "386_risk_register_mark"`).
- **Odrzucone:** przebudowa ui-04 / U-art50 (`REJECTED` — label już DONE; tu katalog stancji).
- **Odrzucone:** scoring osoby · auto-accept · L3 write (`REJECTED` — AI Act / HC-04).
- **Odrzucone:** AI8.2 brama L3 pełna (`REJECTED` — osobny wiersz leftover).

**Łowca:** ISTNIEJE U-art50. ISTNIEJE `risk_register_mark`. BRAK `automation_bias_mark`.

## Zakres

How-to + OPS_JOB. FE: `bias_kind` jako `<select>` z jawnymi `<option>` (bez `.map`), kolejność pól source→kod→kind; model: `bias_kind` przed `mark_code` (jscpd < 3.0%).

## Poza zakresem

ui-04 przebudowa · U-art50 · L3 silnik · scoring · kwota

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zły kind → 400; amount → 422
- [x] craft_style; jscpd < 3.0%

## Leftover

ui-04 pewność per pole · AI8.2 brama L3 · AI9.2 program PDF

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL automation_bias_mark POST/GET + RLS |
| Szybkość | N/A | ten plaster |
| SQL | N/A | list/insert |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | select FE vs jscpd |
| Proza operatora | 5-15 | docs/operator/automation-bias-mark.md |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | lokalny gate |
| Minuty plastra | N/A | /noc 20 |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |