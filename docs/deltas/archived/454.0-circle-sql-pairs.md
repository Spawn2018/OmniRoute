# Plaster 454.0 · AI4.2 kółka w SQL

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI4.2 · 266.0 `circle_sim` · 453.0 widok powtórki

**Zależy od:** 266.0 `circle_sim` HITL

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `circle_sims`. Widok SQL `circle_sim_pair` — samozłączenie dwóch wierszy HITL tego tenanta, gdy `a.unload_unlocode = b.load_unlocode` i `a.load_unlocode = b.unload_unlocode` oraz `a.id < b.id` (para raz). `WITH (security_invoker = true)`.
- **Wybrane:** GET `/api/v1/circle-sim-pairs` — odczyt widoku, to samo OpenFGA `can_manage_circle_sims`. Bez +1 OPS_JOB. UI zostaje na `/circle-sims` (lista par uzupełniających).
- **Wybrane:** `SHIPPED["454.0"]` → `/circle-sims`. How-to: para zamyka się w SQL; silnik 500k i km zostają leftover.
- **Wybrane:** Alembic `359_circle_pair` (`down_revision = "358_cf_run_snap"`).
- **Odrzucone:** pętla w Pythonie po kółkach (`REJECTED`).
- **Odrzucone:** generator 500k / VRP / OR-Tools / LLM-VRP (`REJECTED` na tym wierszu).
- **Odrzucone:** km / Haversine / `lane_km` / float / kwota / marża / `n_overlap`.
- **Odrzucone:** import `geography` / `lane_kms` / `plan_snapshots` · UPDATE/DELETE · live HTTP · nowy BC.

**Łowca:** ISTNIEJE `circle_sim` HITL (266.0). ISTNIEJE unique pary. ISTNIEJE widok `security_invoker`. PODOBNE 453.0. BRAK widoku pary. BRAK silnika 500k.

## Zakres

OpenFGA to samo. Widok bez kwoty / km / float.

## Poza zakresem

silnik 500k · km · VRP · `lane_km` · what-if liczby · mapa

## Kryteria akceptacji

- [x] migracja `varchar(32)`; down zdejmuje widok
- [x] dwa uzupełniające kółka tenanta → GET 200 z jedną parą; tenant B nie widzi
- [x] widok bez amount / km / float; craft_style funkcje>40: 0
- [x] brak pętli Python po `circle_sim`

## Leftover

silnik 500k wariantów · km ładowny/pusty · `n_overlap` · VRP · EXPLAIN p95 na 500k

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | dwa HITL wiersze → jedna para; RLS; POST 405 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS; 500k leftover |
| SQL | leftover | widok JOIN; EXPLAIN 500k leftover |
| N+1 | OK | jeden SELECT widoku |
| Dług / człowiek | OK | ten sam BC; bez generatora |
| Proza operatora | 5–15 zdań | `docs/operator/circle-sim.md` — para w SQL |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–453 |
| Minuty plastra | 45 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
