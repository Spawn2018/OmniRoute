# Plaster 45.0 · M-53 Sankcje (`sanctions`)

**Spec źródłowa:** [docs/spec/sanctions.md](../../spec/sanctions.md)  
**Zależy od:** 5.0 `party.tax_id` + `country_code`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 9. LLM nie liczy. Nie nowa tabela. Nie OFAC.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 8 M-53):** sankcje **nie mają tabeli listy**. Operator widzi aktywnych kontrahentów z `tax_id` i `country_code` — identyfikatory do ręcznego przeglądu. Token `sanctions` (UI, nie tabela). Żywy ID **M-53**. M-52 `dangerous_good` zostaje katalogiem UN. 5.0 zostaje katalogiem party.

**Łowca:** BRAK `sanction` / OFAC / embargo. ISTNIEJE: `fetchParties`, `tax_id`, `country_code`, `is_active`. PODOBNE: 5.0 `/parties`; 25.0 tablica odczytu bez IMAP. Postgres: zero kolumn `sanction`/`ofac`.

**Odrzucone:**

- Tabela hitów OFAC / EU — zgadywanie schematu (CURRENT).
- HTTP do OFAC/EU/UN — leftover sekretów.
- Lista krajów w kodzie — konfiguracja ma być danymi.
- Scoring dopasowania / auto-blokada.

## Zakres

Read-only `/sanctions`. `data-sanctions="board"`. Brak migracji. `sanctionsParties(parties)`. Link do `/parties`.

## Poza zakresem

Tabela sankcji · HTTP list · auto-match · ExtractionService

## Ustalenia

- Token: `sanctions` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia parties.
- RLS: bez nowej tabeli.
- Job: `/sanctions`. `SHIPPED_CHARGE_ROUTES["45.0"]` = `/sanctions`. OPS_JOBS +1 → 42.
- `just dup` ≤ 3% (2,86%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 45.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/sanctions` ma `data-sanctions="board"` i woła `sanctionsParties`
- [x] Filtr `is_active`; widać `tax_id` i `country_code`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["45.0"]` = `/sanctions`
- [x] `just dup` ≤ 3% (2,86%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | aktywni party; brak OFAC |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | OFAC HTTP / tabela hitów zostaje |
| Docs/OS | PRZESZŁO | spec `sanctions.md`; CURRENT → Plan M-54 |

## Knowledge

Nie RAG. Nie silnik OFAC.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. HTTP list i auto-match świadomie leftover.
