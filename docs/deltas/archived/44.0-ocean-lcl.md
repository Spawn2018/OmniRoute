# Plaster 44.0 · M-51 Drobnica morska (`ocean_lcl`)

**Spec źródłowa:** [docs/spec/ocean-lcl.md](../../spec/ocean-lcl.md)  
**Zależy od:** 4.0 `port.is_seaport`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie CFS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 7 M-51):** drobnica morska **nie ma tabeli LCL**. Operator widzi porty z `is_seaport` (UN/LOCODE: flaga `port`; ingest ustawia boolean). Token `ocean_lcl` (UI, nie tabela). Żywy ID **M-51**. 4.0 zostaje katalogiem; 42.0 zostaje koleją.

**Łowca:** BRAK `lcl` / CFS / konsolidacji. ISTNIEJE: `fetchPorts`, `is_seaport`. PODOBNE: 42.0 `railPorts`; 4.0 `/ports`. Postgres: brak kolumn `lcl`/`fcl`/`equipment`.

**Odrzucone:**

- Tabela rezerwacji LCL / CFS / konsolidacji — zgadywanie schematu (CURRENT).
- Rozróżnienie LCL vs FCL — brak kolumny.
- Filtrowanie wycen po POL/POD jako drobnica — to Fala 3, nie modal.
- HTTP do konsolidatora — leftover.

## Zakres

Read-only `/lcl`. `data-ocean-lcl="board"`. Brak migracji. `oceanLclPorts(ports)`. Link do `/ports`.

## Poza zakresem

Tabela LCL · CFS · CBM w JS · ExtractionService

## Ustalenia

- Token: `ocean_lcl` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia geography.
- RLS: bez nowej tabeli.
- Job: `/lcl`. `SHIPPED_CHARGE_ROUTES["44.0"]` = `/lcl`. OPS_JOBS +1 → 41.
- `just dup` ≤ 3% (2,87%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 44.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/lcl` ma `data-ocean-lcl="board"` i woła `oceanLclPorts`
- [x] Filtr `is_seaport`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["44.0"]` = `/lcl`
- [x] `just dup` ≤ 3% (2,87%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | tylko `is_seaport`; brak tabeli LCL |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | CFS / LCL vs FCL zostaje |
| Docs/OS | PRZESZŁO | spec `ocean-lcl.md`; CURRENT → Plan M-53 |

## Knowledge

Nie RAG. Nie silnik CFS.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. CFS i rozróżnienie LCL/FCL świadomie leftover.
