# Plaster 0.14 · HTTP happy-path ekstrakcji

**Faza:** C leftover  
**Zależy od:** 0.7 API, 0.11 XOR, 0.12 JWT  
**Status:** archived 2026-08-31

## Zakres
- HTTP POST extract (`input_text` / `document_base64`) → 201
- list / accept / reject; niepoprawny base64 → 400
- JWT Bearer + AllowAll OpenFGA w teście; serwis zastąpiony stubem (nie live PG w unit)
- Bez OAuth, bez k6, bez zapisu `rate_line`

## Kryteria akceptacji
- [x] 201: `organization_id` z claims tokena
- [x] accept/reject zmienia status; payload bez `rate_line`
- [x] Pętla `docs/ops/post-plaster.md`

## Pętla po plasterze

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | 6 testów HTTP extract/list/accept/reject + invalid base64; 67 unit; stub `ExtractionService`, nie live PG |
| Szybkość | N/A ten plaster | HTTP unit stub — nie ścieżka z budżetu AGENTS.md (p95 wycena/lista/LCP) |
| Dług w diffie | OK | C901=0 na `test_extraction_http.py`; jscpd 0 klonów / 0.00% na pliku 0.14; gate jscpd 1.40% < 3%; `api/extractions.py` 97% (linia 78 = XOR, poza 0.14) |
| Docs/OS | PRZESZŁO | CURRENT=0.14, następny OAuth; delta archived; PROGRESS linia 0.14 |
