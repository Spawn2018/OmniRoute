# Plaster 62.0 · Q-E3 how-to jobów zapisu + C4

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka Q-E3  
**Zależy od:** leftover 58.0 (proza operatora tylko `ścieżka-pieniędzy.md`) · [ARCHITECTURE.md](../../ARCHITECTURE.md) bez C4

**Status:** Zamknięty `/noc` 2026-09-02. Nie nowy M-xx. Nie 70 stubów. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** dwa how-to zapisu, których brakuje obok ścieżki pieniądza: **kontrahent** (zapis po `tax_id` / potwierdzeniu lookupu) oraz **katalogi** (jeden plik na wspólny wzorzec INSERT, nie plik na każdy ekran). W ARCHITECTURE: C4 **context + container** w mermaid — bez poziomu Code na każdy BC.

**Łowca:** ISTNIEJE: `docs/operator/ścieżka-pieniędzy.md` (extract → `rate_line` → `charge` → `quotation`). PODOBNE: drzewo katalogów w ARCHITECTURE (lista folderów, nie C4). BRAK: how-to kontrahenta i katalogów; diagram C4.

**Odrzucone:**

- How-to na każdą tablicę-odczyt (zlecenie, tracking, FV jako odczyt).
- 70 plików stub.
- C4 component/code na każdy moduł.
- Zmiana kodu produktu, Q-E4, F9.1, S1.

## Zakres

1. `docs/operator/kontrahent.md` — 5–15 zdań: resolve po `tax_id`, lookup = szkic, INSERT tylko po potwierdzeniu.
2. `docs/operator/katalogi.md` — 5–15 zdań: które ekrany naprawdę zapisują; Decimal / `source_ref`; nie liczyć w UI.
3. C4 context + container w `docs/ARCHITECTURE.md` (mermaid). Drzewo warstw zostaje.

## Poza zakresem

Q-E4 · seed 50k · k6 · nowa tabela · nowy ekran · how-to tablic-odczytów · mixin modeli · F9.1 · S1

## Ustalenia

- Job operatora: dokumentacja istniejących jobów, nie nowy ekran.
- OpenFGA / RLS: bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL bez zmian.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `docs/operator/kontrahent.md` istnieje i mówi, że lookup nie zapisuje `party`
- [x] `docs/operator/katalogi.md` istnieje i nie jest listą 70 stubów
- [x] `docs/ARCHITECTURE.md` ma mermaid C4 context i container
- [x] Brak zmiany backendu, migracji i `frontend/src` (poza testem docs)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | how-to kontrahent+katalogi; C4 context/container |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS w diffie |
| SQL | N/A | brak SELECT/INSERT/UPDATE/resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | leftover | how-to tablic-odczytów świadomie nie; C4 component/code nie |
| Proza operatora | 5–15 zdań | dwa pliki zapisu; ścieżka pieniądza bez zmian |
| Docs/OS | PRZESZŁO | CURRENT → Q-E4 Plan; `just docs` |

## Pytania

Brak. Następny: Q-E4 `/plan-modul`.
