# Plaster 639.0 · leftover N6 matching stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N6 · [527.0](../archived/527.0-margin-floor.md) · [539.0](../archived/539.0-margin-floor-409.md)
**Zależy od:** 527.0 `margin_floor` + 539.0 compose 409 już są
**Status:** Zamknięty `/noc 17` 2026-09-23. Nie matching SQL lane. Nie auto charge.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover N6 „matching lane na wycenie”):** `margin_floor` zostaje Decimal + UN/LOCODE. Egzekucja 409/S11 na POST `charge` zostaje w compose API (539/544). Żywy plaster: **nowa tabela** `margin_match_mark` per tenant — HITL stance dopasowania podłogi. Wiersz = `mark_code` (snake 1–32) + `match_kind` (`match`|`hold`|`waive`|`other`) + `source_ref` (`tenant:manual` albo `fixture://margin-match/…`). Operator zapisuje, jak traktuje dopasowanie lane do podłogi (dopasuj / wstrzymaj / zwolnij / inne); serwis **nie** porównuje UN/LOCODE, nie czyta `charge` i nie zwraca 409. Zły kind → 400 (`dopasowanie`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `margin_floors` / charges / sales_lanes. UI `/margin-match-marks`. `SHIPPED_CHARGE_ROUTES["639.0"]` = `/margin-match-marks`. OpenFGA: `can_manage_margin_match_marks`. Migracja `511_margin_match_mark` po `510_trip_variance_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 527.0 „matching lane” — tu tylko HITL stance, nie silnik matching. Źródło: `docs/deltas/archived/527.0-margin-floor.md`; PLAN wiersz N6; `docs/ops/docs-debt.md` § 527.0.

**Łowca:** ISTNIEJE `margin_floor` — Decimal + para UN/LOCODE. ISTNIEJE compose 409/S11 (539/544). ISTNIEJE `local_charge_warning_mark` / `trip_variance_mark` — wzorzec stance. BRAK `margin_match_mark`. BRAK `/margin-match-marks`. BRAK matching SQL lane.

**Odrzucone:**

- matching SQL lane vs `margin_floor` / wycena
- auto-linia `charge` / druga marża
- FK UUID do `margin_floor` / `charge` / `quotation`
- float / live HTTP

## Zakres

1. Migracja `511_margin_match_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `match_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/margin-match-marks` + zdanie w how-to podłogi marży.

## Poza zakresem

matching SQL · auto charge · FK · float · live HTTP

## Plan plików

- `backend/alembic/versions/511_margin_match_mark.py`
- `backend/app/models/margin_match_mark.py`
- `backend/app/domain/margin_match_mark.py`
- `backend/app/repositories/margin_match_marks/`
- `backend/app/services/margin_match_marks/` (+ AGENTS.md)
- `backend/app/api/margin_match_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/margin-match-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `match_kind=match` zapisuje wiersz; `margin_floor` i `charge` bez zmian
- [x] kind spoza `match`|`hold`|`waive`|`other` → 400 (`dopasowanie`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `margin_floors` / `charges` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / matching SQL

## Leftover

matching SQL lane · auto charge · FK margin_floor
