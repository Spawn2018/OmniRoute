# Plaster 654.0 · leftover EXP1 quotation mqc_teu HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `quotation` · [646.0](../archived/646.0-rate-line-allotment-teu.md) · [653.0](../archived/653.0-quotation-revision-no.md)
**Zależy od:** M- wycena `quotation`; `revision_no` (653.0); `allotment_teu` na `rate_line` (646.0) = wzorzec Decimal TEU, inny BC
**Status:** Zamknięte `/noc 7` 2026-09-25. Nie float. Nie MQC SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 kolumn MQC na wycenie):** tabela `quotation` zostaje prawdą o ofercie. Katalog `mqc_mark` (EXP3.5) zostaje katalogiem stance — **nie** FK. Żywy plaster: **opcjonalny** `mqc_teu` (`Numeric(14,4)` nullable) na `quotation` — zobowiązanie TEU HITL. Operator ustawia Decimal ≥ 0 (jak `allotment_teu`); serwis nie liczy MQC vs actual, nie mnoży NBP, nie tworzy `charge`, nie woła HTTP/LLM, nie wiże z `mqc_window`. POST/PATCH bez pola → `null`. Float / ujemna → 400. Istniejące wiersze: `null`. GET listy SQL. UI `/quotations`. `SHIPPED_CHARGE_ROUTES["654.0"]` = `/quotations`. OpenFGA bez nowej relacji. Migracja `525_quotation_mqc_teu` po `524_quotation_revision_no`. Marża zostaje na `charge`. Zero float.

**Dlaczego tylko `mqc_teu`, nie para z oknem:** leftover EXP1 wymienia `mqc_teu` + `mqc_window`. Najwęższy plaster = jeden Decimal TEU; `mqc_window` = osobny leftover (park w CURRENT).

**REQUIREMENT:** leftover EXP1 po 653.0. Źródło: CURRENT Następny **654.0**; docs-debt 653.0.

**Łowca:** ISTNIEJE `quotation`. ISTNIEJE `revision_no` (653.0). ISTNIEJE `rate_line.allotment_teu` (646.0). ISTNIEJE `mqc_mark` (stance). BRAK `mqc_teu` na `quotation`. BRAK float / MQC SQL.

**Odrzucone:**

- `mqc_window` w tym plasterze (leftover / park)
- FK do `mqc_mark`
- MQC SQL / actual / gap engine
- float / NBP / mnożenie / Deadline Engine
- zapis `charge` / auto-award
- live HTTP
- `supersedes_id` / spot_or_contract na quotation (park)

## Zakres

1. Migracja `525_quotation_mqc_teu`: kolumna `Numeric(14,4)` nullable.
2. Domain `require_mqc_teu`; API create/update opcjonalnie; UI `/quotations`.
3. How-to / fold. `just api-types`. `SHIPPED_CHARGE_ROUTES["654.0"]`.

## Poza zakresem

mqc_window · supersedes_id · spot_or_contract na quotation (park) · MQC SQL · NBP · float · charge

## Plan plików

- `backend/alembic/versions/525_quotation_mqc_teu.py`
- model / domain / serwis / API / testy quotations
- `frontend/src/features/quotations/` + API client
- `ops-index` + `routes-breadth`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST/PATCH z `mqc_teu` Decimal ≥ 0 zapisuje; bez pola → null
- [x] float / ujemna / zła wartość → 400; serwis nie liczy MQC SQL i nie tworzy charge
- [x] RLS: tenant B nie widzi wycen A; zero float / marży
- [x] `revision_no` / `valid_until` / `mqc_mark` bez regresji

## Leftover

mqc_window · supersedes_id · spot_or_contract na quotation (park) · matching · Deadline Engine
