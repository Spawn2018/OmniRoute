# Plaster 245.0 · leftover 2b `inquiry_sent` z API

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Oś leftoverów `/noc` (2a/2b) · [244.0](../archived/244.0-inquiry-queued-event.md) leftover 2b · [133.0](../archived/133.0-entity-event.md)
**Zależy od:** 244.0 `inquiry_queued` z API
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie Temporal.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover 2b `inquiry_sent`/`quote_recorded`/batch + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. `quote_recorded` parked. batch parked. Żywy plaster: **to samo składanie API co 244.0** — po POST `/carrier-inquiries` (jeden wiersz), gdy `status=sent`, API woła `EntityEventService.create_event` (`event_kind=inquiry_sent`, `subject_kind=carrier_inquiry`). Rozszerzyć `_append_queued_event` (albo cienki mapper status→kind). **CarrierInquiryService nie importuje `entity_events`.** `draft` i `queued` bez zmian (queued zostaje 244.0). Brak nowej tabeli / migracji. `SHIPPED_CHARGE_ROUTES["245.0"]` = `/entity-events`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE 244.0 `_append_queued_event` (tylko `queued`). ISTNIEJE kind `inquiry_sent` w allowliście 133.0. BRAK append przy `sent`. PODOBNE: ten sam helper. Nie nowa tabela. Nie import BC.

**Odrzucone:**

- Import `entity_events` w serwisie inquiries.
- `quote_recorded` / batch / PATCH status / `channel_quote`.
- T1b · T2c · T7b · T5 · N3 · S53 · PIN · live HTTP · Temporal.

## Zakres

1. API: `sent` → `inquiry_sent` w tej samej transakcji co POST.
2. Test HTTP: sent → jeden INSERT; queued nadal `inquiry_queued`; draft zero.
3. Fold w `entity-event.md`. Job OPS zostaje 106.

## Poza zakresem

`quote_recorded` · batch · PATCH · FK subject · outbox consumer · Temporal · PIN · km · mapa · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST ze `status=sent` dopisuje `inquiry_sent`
- [x] POST `queued` nadal `inquiry_queued`; `draft` zero
- [x] Zero importu `entity_events` w serwisie inquiries
- [x] Zero float / marży / live HTTP / Temporal

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | sent → `inquiry_sent`; queued bez zmian; draft → zero |
| Szybkość | N/A ten plaster | jeden INSERT zdarzenia, nie ścieżka z budżetu AGENTS |
| SQL | N/A | brak nowej tabeli / migracji |
| N+1 | OK | jeden append po jednym INSERT |
| Dług / człowiek | OK | leftover `quote_recorded`; PIN parked |
| Proza operatora | 5–15 zdań | fold w `entity-event.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN B0a, how-to |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
