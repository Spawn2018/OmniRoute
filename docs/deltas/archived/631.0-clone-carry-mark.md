# Plaster 631.0 · leftover 528 clone U1 carry HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N9 · [528.0](../archived/528.0-shipment-clone-mark.md) · [144.0](../archived/144.0-u1-u5-carry-checklist.md)
**Zależy od:** 528.0 `shipment_clone_mark` · 144.0 `field_carry_forward`
**Status:** Zamknięty `/noc 7` 2026-09-23. Nie auto-copy. Nie similar SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover 528 „kopiowanie allowlisty pól przez U1”):** `shipment_clone_mark` zostaje intencją klonu. `field_carry_forward` zostaje snapshotem pól oferty→zlecenie. Auto INSERT `field_carry_forward` / ciche kopiowanie zlecenia i silnik similar SQL zostają park. Żywy plaster: **nowa tabela** `clone_carry_mark` per tenant — HITL stance, czy przy klonie operator chce przenieść allowlistę U1. Wiersz = `mark_code` (snake 1–32) + `carry_kind` (`carry`|`held`|`skip`|`other`) + `source_ref` (`tenant:manual` albo `fixture://clone-carry/…`). Operator zapisuje stance; serwis **nie** INSERT `field_carry_forward`, **nie** kopiuje `shipment`/`stop`/`charge` i nie woła matching SQL. Zły kind → 400 (`carry`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `shipment_clone_marks` / `field_carry_forwards` / shipments / charges. UI `/clone-carry-marks`. `SHIPPED_CHARGE_ROUTES["631.0"]` = `/clone-carry-marks`. OpenFGA: `can_manage_clone_carry_marks`. Migracja `503_clone_carry_mark` po `502_pallet_synchro_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 528.0 „kopiowanie allowlisty pól z poprzedniego zlecenia przez U1” — tu tylko HITL stance, nie egzekucja copy. Źródło: `docs/deltas/archived/528.0-shipment-clone-mark.md`; PLAN wiersz N9; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `shipment_clone_mark` — intencja klonu, nie carry. ISTNIEJE `field_carry_forward` — snapshot pól, nie stance przy klonie. ISTNIEJE `shipment_clone_marks` UI. BRAK `clone_carry_mark`. BRAK `/clone-carry-marks`. BRAK stance carry|held|skip przy klonie.

**Odrzucone:**

- auto-copy / INSERT `field_carry_forward` z poprzedniego zlecenia
- silnik similar SQL / LLM matching
- F2b Decimal skonta przy klonie FV
- FK UUID do `shipment` / `shipment_clone_mark` / `field_carry_forward`
- drugi SoR / UPDATE `shipment` / kwota / float

## Zakres

1. Migracja `503_clone_carry_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `carry_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/clone-carry-marks` + zdanie w how-to klonu / U1.

## Poza zakresem

auto-copy U1 · similar SQL · F2b Decimal · FK UUID · drugi SoR · live HTTP

## Plan plików

- `backend/alembic/versions/503_clone_carry_mark.py`
- `backend/app/models/clone_carry_mark.py`
- `backend/app/domain/clone_carry_mark.py`
- `backend/app/repositories/clone_carry_marks/`
- `backend/app/services/clone_carry_marks/` (+ AGENTS.md)
- `backend/app/api/clone_carry_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/clone-carry-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `carry_kind=carry` zapisuje wiersz; `field_carry_forward` i `shipment` bez zmian
- [x] kind spoza `carry`|`held`|`skip`|`other` → 400 (`carry`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `shipment_clone_marks` / `field_carry_forwards` / `charges` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / auto-copy / similar SQL

## Leftover

auto-copy allowlisty U1 przy klonie (compose API) · similar SQL · F2b Decimal · D9c 409 / D9d/e/f · matching T5 · FK kontekstu · T8 live · D7b · auto-UPDATE salda

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST carry; 400 carry; amount 422 |
| Szybkość | N/A ten plaster | katalog INSERT |
| SQL | N/A | create_table + RLS |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | BC osobny od clone/carry |
| Proza operatora | 5–15 zdań | shipment-clone-mark.md |
| Docs/OS | PRZESZŁO | CURRENT, PLAN N9, GLOSSARY, VISION |
| Pushy do zielonego | N/A | CI po tym pushu |
| Minuty plastra | N/A | /noc 7 |
| Bench rzemiosła | PRZESZŁO | factory_cycle.py --close |
