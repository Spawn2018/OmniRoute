# Plaster 0.5 · Frontend Shell 2026

**ADR:** docs/adr/0002-frontend-platform-2026.md  
**Zależy od:** 0.3 RLS, 0.4 OpenFGA

## Zakres
- Vite + React 19 + React Compiler + Tailwind v4 + shadcn
- TanStack Router + Query + Form
- Design tokens (neutral, compact) + layout shell (sidebar) wg satnaing/shadcn-admin
- Command palette ⌘K
- PostHog bootstrap
- Ekran `/tenancy/users` (read-only lista przez API) — bez ColumnEditor (to 0.6)

## Poza zakresem
DataTableShell ColumnEditor, `table_view` DB, Faza C AI, IdP/JWT

## Kryteria akceptacji
- [x] `pnpm dev` + proxy do API (`vite.config.ts` → `:8000`)
- [x] Typy z OpenAPI (openapi-ts) lub tymczasowy klient typed (`src/lib/api.ts`)
- [x] Zero AI-slop (tokens zatwierdzone — neutral enterprise w `index.css`)
- [x] PostHog event `app_loaded` (`src/lib/analytics.ts`)
- [x] `just gate` / CI nie łamie backendu (`frontend-typecheck` w gate + pnpm w CI)

## Dług świadomy → spłata później
- openapi-ts zamiast ręcznego `lib/api.ts` → **0.6** lub plaster kontraktu
- lazy-load PostHog (chunk blisko 250 kB gzip) → slot wydajności
- JWT/IdP zamiast localStorage → poza B.5/B.6
