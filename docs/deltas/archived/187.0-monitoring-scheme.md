# Plaster 187.0 · C7 `monitoring_scheme`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md) § C7
**Zależy od:** 0.3 tenancy (katalog per tenant)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie live PUESC. Nie „SENT-Europa”.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.20–G2.22 + F + C7):** leftover `circle_sim`/km/P (G2.20–G2.22) zostaje leftover — nie silnik 500k w Pythonie. leftover kg/CBAM zostaje leftover. F1 live FA(3) parked (live HTTP; numer sesji to już S35 `ksef_ref`). C1 `shipment_monitoring_filing` / XML PUESC parked (live HTTP). Żywy plaster: jedna tabela `monitoring_scheme` per tenant — **HITL kod schematu monitoringu z `source_ref`**, nie zgłoszenie SENT i nie wymyślony klon w kraju bez urzędu. Wiersz = `scheme_code` (snake 2–32) + `source_ref` (`tenant:manual` albo `fixture://monitoring-scheme/…`). Operator wpisuje, że tenant zna ten schemat (np. `sent`, `ekaer`); serwis nie woła sieci, nie woła LLM i nie czyta `charge` / `shipment`. Zły kod → 400 (`schemat`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, scheme_code)`. GET listy SQL. UI `/monitoring-schemes` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["187.0"]` = `/monitoring-schemes`. OpenFGA: `can_manage_monitoring_schemes`. Migracja `127_monitoring_scheme`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `sales_invoice.ksef_ref` — S35 numer sesji, **nie** F1 live FA(3). ISTNIEJE `kreptd_licence` — numer licencji na `party`, **nie** schemat SENT. BRAK tabeli `monitoring_scheme`. BRAK `shipment_monitoring_filing`. BRAK `/monitoring-schemes`. BRAK klienta HTTP PUESC.

**Odrzucone:**

- `circle_sim` / km / P (leftover G2.20–G2.22).
- kg / CBAM (leftover G2.15–G2.18).
- F1 live FA(3) / XML KSeF / VAT matrix (live HTTP; S35 już jest).
- C1 SENT XML/XSD / SENT-GEO / `shipment_monitoring_filing`.
- Seed „SENT w każdym kraju UE” / wymyślony adapter.
- C8 `party_document` / 409 na shipment.
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `127_monitoring_scheme`: RLS, izolacja, unique `(organization_id, scheme_code)`.
2. POST/GET. Zły `scheme_code` / `source_ref` → 400. Serwis nie importuje shipments / charges / extraction / geography i nie woła HTTP.
3. UI `/monitoring-schemes`. `just api-types`. How-to `docs/operator/schemat-monitoringu.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `SchemeDesk` / `schemeWrite` / `persistSchemeMark`.

## Poza zakresem

G2.20–G2.22 circle_sim · G2.15–G2.18 kg · F1 live KSeF · C1 filing XML · C8 `party_document` · API/GEO/urząd na wierszu · seed mapy UE · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `monitoring_scheme` z kodem i `source_ref`, bez zgłoszenia SENT
- [x] zły `scheme_code` → 400
- [x] GET zwraca schematy tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP / scrape / wymyślonego klona SENT

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST kod + `source_ref`; 400 `schemat` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_monitoring_scheme_organization_id` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover C1 filing XML / GEO/API/urząd; leftover C8 `party_document`; leftover F1 live FA(3); leftover G2.20–G2.22 |
| Proza operatora | 5–15 zdań | `schemat-monitoringu.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN C7, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
