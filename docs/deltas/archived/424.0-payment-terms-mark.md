# Plaster 424.0 Â· EXP1 `payment_terms_mark` HITL

**Status:** Zamknięty `/noc`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `payment_terms_mark` â€” `mark_code` + `terms_kind` `net|prepaid|other` + `source_ref`. Odpowiada EXP1 `payment_terms_id` na shipment bez FK na zlecenie.
- **Odrzucone:** `cargo_value`+waluta â€” kwota/Decimal na katalogu; koliduje z zakazem drugiej marĹĽy i z `high_value_mark` (protokĂłĹ‚ bez wartoĹ›ci).
- **Odrzucone:** kolumna `payment_terms_days` na `party` â€” juĹĽ istnieje; ten plaster nie mutuje party ani nie liczy dni.
- **Odrzucone:** `language_code` / `booked_carrier` â€” osobne leftover; wÄ™ĹĽszy Q z kolejki = terms.

## Zakres

HITL `payment_terms_mark`: mark_code + terms_kind net|prepaid|other + source_ref.
Migracja `334` (revises `333_high_value_mark`). UI `/payment-terms-marks`.
OpenFGA `can_manage_payment_terms_marks` (exp4 catalog).

## Poza zakresem

kolumna / FK na shipment Â· party.payment_terms_days Â· skonto F2 Â· kwota Â· DSO

## Kryteria

- [ ] POST z kodem, terms_kind, source_ref
- [ ] zĹ‚y kod/kind/source_ref â†’ 400; amount/margin/score/days â†’ 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] `__init__.py` w services; `/payment-terms-marks` w REQUIRED_BUSINESS_LIST_ROUTES
- [ ] OPS_JOBS length 241; SHIPPED `"424.0"`

## Leftover

FK payment_terms na shipment Â· egzekucja skonta Â· ERP
