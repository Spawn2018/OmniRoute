# Plaster 567.0 · V3 leftover — HITL `container.detention_free_days`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · V3 · leftover z [566.0](archived/566.0-container-demurrage-days.md) · [196.0](archived/196.0-free-time-clock.md)

**Zależy od:** 153.0 `container` · 566.0 `demurrage_free_days` (dni demurrage — obok, nie to samo)

**Status:** Zamknięty `/noc` 2026-09-18. Nie countdown N3. Nie szkic charge. Nie mixed.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover V3 po demurrage. Pogłębienie istniejącej `container`: opcjonalny `detention_free_days` (`Integer` nullable, ≥ 0, dana w **dniach**, nie `now() − gate_out`). POST bez pola → `null`. Zła wartość → 400 (`dni`). Nie mutuje `free_time_clock` / `demurrage_free_days`.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["567.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1.
- **Odrzucone:** countdown N3 / remaining (`REJECTED` → park). Szkic `charge` D&D (`REJECTED` → park). `mixed_dd_days` w tym plasterze (`REJECTED` → leftover). Import `free_time_clocks` (`REJECTED`). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live armator / PIN / VGM.

**Łowca:** BRAK `detention_free_days`. ISTNIEJE `demurrage_free_days` (566.0). ISTNIEJE `free_time_origin_h` / `free_time_dest_h` (godziny). ISTNIEJE `free_time_clock.free_days` (katalog V3).

## Zakres

1. Migracja `447_container_detention` (po `446_container_demurrage`): kolumna nullable Integer.
2. Domain `require_detention_free_days` + API POST opcjonalny + GET.
3. UI `/shipments` panel kontenera. How-to dopisek. `SHIPPED["567.0"]`.

## Poza zakresem

countdown N3 · mixed_dd · szkic charge · free_time_clock mutacja · AI3 · kwota

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `detention_free_days=5` zapisuje dni
- [x] ujemne / float / bool → 400 (`dni`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero float / live HTTP / import free_time_clocks

## Leftover

`mixed_dd_days` · N3 countdown · szkic charge · live armator

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (gate po push) | kolumna HITL dni; czekaj `gate` success |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | wyodrębnione `_require_clocks` / `_clock_kwargs` |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` kontener |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
