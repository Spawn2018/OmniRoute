# Plaster 558.0 Â· C1 leftover â€” HITL `filing_bind_mark`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala C Â· C1

**ZaleĹĽy od:** tenancy Â· wzorzec HITL mark Â· C1 `shipment_monitoring_filing` (obok)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy C1 leftover. Nowa tabela `filing_bind_mark`: `mark_code` (snake 2â€“32) + `bind_kind` `shipment|scheme|both|other` + `source_ref`. Append-only. RLS. Katalog stancji wiÄ…zania zgĹ‚oszenia â€” nie FK `shipment` / `monitoring_scheme`, nie PUESC.
- **Wybrane:** POST/GET `/api/v1/filing-bind-marks`. OpenFGA `can_manage_filing_bind_marks`. UI `/filing-bind-marks`. `SHIPPED["558.0"]`.
- **Odrzucone:** FK `shipment_id` / `monitoring_scheme_id` (`REJECTED` â†’ leftover). PUESC HTTP / SENT XML (`REJECTED` â†’ leftover). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / ranking SQL / 409 egzekucja.

**Ĺowca:** BRAK `filing_bind_mark`. ISTNIEJE `shipment_monitoring_filing` (stancja zgĹ‚oszenia). ISTNIEJE `monitoring_scheme` (C7 schemat). ISTNIEJE `filing_scheme_mark` (G12 ICS2/CBAM).

## Zakres

1. Migracja `438_filing_bind_mark` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["558.0"]` â†’ `/filing-bind-marks`.

## Poza zakresem

FK shipment/scheme Â· PUESC Â· SENT XML Â· AI3 Â· G0 Â· kwota

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / FK

## Leftover

FK shipment/scheme Â· PUESC Â· SENT XML
