# Plaster 26.0 · M-33 Dodatek do Outlooka (`mail_client` z `mailto:` na `/mail`)

**Spec źródłowa:** [docs/spec/mail-client.md](../../spec/mail-client.md)  
**Zależy od:** 25.0 `/mail` · 5.0 `party_contact.email`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie Office.js.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 4 M-33):** dodatek Outlook **nie istnieje w repo**. Operator otwiera znany mail w kliencie poczty przez `mailto:` na `/mail`. Token `mail_client` (UI, nie tabela). Żywy ID **M-33**. 25.0 zostaje tablicą odczytu; 26.0 = link `mailto:` przy kontakcie.

**Łowca:** BRAK Office.js / manifestu / Graph. ISTNIEJE: `/mail` + `party_contact.email`. PODOBNE: 25.0 board.

**Odrzucone:**

- Office add-in / `manifest.xml` / COM — zgadywanie schematu (CURRENT).
- Microsoft Graph / OAuth Outlook — Auth0 odroczone; sekrety leftover HC.
- IMAP wysyłka — leftover 25.0.
- LLM piszący treść maila.

## Zakres

`mailto:` przy `party_contact.email` na `/mail`. `data-mail-client="mailto"`. Brak migracji.

## Poza zakresem

Office.js · Graph · IMAP · M-34 powiadomienia · ExtractionService · LLM

## Ustalenia

- Token: `mail_client` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_parties`.
- RLS: bez nowej tabeli.
- Job: ten sam `/mail`. `SHIPPED_CHARGE_ROUTES["26.0"]` = `/mail`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/mail` ma `data-mail-client="mailto"` i `mailto:` przy adresie kontaktu
- [x] Brak `office.js` / `manifest.xml` / `graph.microsoft` w ekranie
- [x] Brak nowej migracji
- [x] `just dup` ≤ 3% (2,69%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `mailto:` z `party_contact.email`; brak Office.js |
| Szybkość | N/A ten plaster | link lokalny |
| Dług w diffie | leftover | `just dup` 2,69%; dodatek Office zostaje |
| Docs/OS | PRZESZŁO | spec `mail-client.md`; CURRENT → Plan M-34 |

## Knowledge

Nie RAG. Nie dodatek COM.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Office add-in świadomie leftover.
