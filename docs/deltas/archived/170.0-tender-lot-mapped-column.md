# 170.0 follow-up · mapped_column type_

**Status:** Zamknięty z 170.0. Nie nowy plaster.

`TenderLot.organization_id` używa `ForeignKey` pozycyjnie i `type_=UUID(as_uuid=True)`.
Pozycyjny `UUID()` po FK pada w SQLAlchemy (`SchemaItem expected, got UUID()`).
Słowo kluczowe `type_` zachowuje typ kolumny i łamie klon `bank_payment` pod jscpd < 3%.
