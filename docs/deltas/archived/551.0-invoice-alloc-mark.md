# Plaster 551.0 Â· F10 leftover â€” HITL `invoice_alloc_mark`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala F Â· F10 leftover po 550.0

**ZaleĹĽy od:** tenancy Â· `purchase_invoice` (549.0) Â· `invoice_match_mark` (550.0)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy leftover F10. Nowa tabela `invoice_alloc_mark`: `mark_code` (snake 2â€“32) + `alloc_kind` `line|header|batch|other` + `source_ref`. Append-only. RLS. Katalog stancji alokacji FV zakupu â€” nie `purchase_invoice_allocation` z kwotÄ… i nie SQL.
- **Wybrane:** POST/GET `/api/v1/invoice-alloc-marks`. OpenFGA `can_manage_invoice_alloc_marks`. UI `/invoice-alloc-marks`. `SHIPPED["551.0"]`.
- **Odrzucone:** tabela `purchase_invoice_allocation` z kwotÄ… / FK charge (`REJECTED` â†’ leftover). Ranking SQL / `invoice_match_candidate` (`REJECTED` â†’ leftover). Auto-link do `charge` / `purchase_invoice` (`REJECTED`). Kwota / marĹĽa / float (`REJECTED`). Live ERP (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13.

**Ĺowca:** BRAK `invoice_alloc_mark` / `purchase_invoice_allocation` w models. ISTNIEJE `invoice_match_mark` (stancja match, nie allocation). ISTNIEJE `cost_allocation_mark` (koszt ABC, nie FV zakupu). ISTNIEJE `purchase_invoice` (ingest).

## Zakres

1. Migracja `431_invoice_alloc_mark` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["551.0"]` â†’ `/invoice-alloc-marks`.

## Poza zakresem

`purchase_invoice_allocation` Â· ranking SQL Â· `invoice_match_candidate` Â· auto-link Â· kwota Â· live ERP Â· AI3 Â· G0

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / FK purchase_invoice

## Leftover

`purchase_invoice_allocation` Â· ranking SQL Â· `invoice_match_candidate` Â· auto-link Â· kwota Â· live ERP

