# Plaster 582.0 · T3 leftover — HITL `container.temp_max`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [581.0](../archived/581.0-container-temp-min.md)

**Zależy od:** 153.0 `container` · 581.0 `temp_min`

**Status:** Zamknięty `/noc` 2026-09-19. Nie `needs_external_power`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** para SPEED po `temp_min`: `temp_max` Decimal °C. Reuse `_require_temp_c` + `require_container_temp_max`. Pusty → `null`. Float/bool → 400 (`temp. max`). Nie porównuje z `temp_min`.
- **Wybrane:** UI `/shipments`. `SHIPPED["582.0"]` → `/shipments`. Migracja `462_container_temp_max`.
- **Odrzucone:** `needs_external_power` / min≤max / 409 vs reefer / float / N3 / ciphertext / AI3 / G0-SH.

**Łowca:** BRAK `temp_max` przed plasterem. ISTNIEJE `_require_temp_c`.

## Zakres

1. Migracja `462_container_temp_max`: `Numeric(14, 4)` nullable.
2. Domain + API + UI + SHIPPED.

## Poza zakresem

`needs_external_power` · min≤max · 409 vs reefer · N3 · ciphertext · AI3 · G0-SH

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `temp_max="2"` zapisuje Decimal
- [x] float / bool → 400 (`temp. max`)
- [x] izolacja RLS
- [x] zero mutacji `temp_min`

## Leftover

`needs_external_power` · min≤max · 409 vs reefer · N3 · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL temp_max Decimal |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Numeric |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | reuse `_require_temp_c` |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
