# Plaster 232.0 · T3 `bl_kind` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [231.0](../archived/231.0-container-return-terminal.md) leftover `bl_kind` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 231.0 `return_terminal`
**Status:** Zamknięty `/noc` 2026-09-10. Nie ocean_bill. Nie HBL/MBL. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `bl_kind`/party/PIN/VGM/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `bl_kind` z allowlistą EXP1 `original` / `seawaybill` / `telex` / `express` (nie `hbl`/`mbl`). POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`list`). `require_container_bl_kind` w `app.domain.container` — nie wołać `require_bill_kind` z ocean_bill. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["232.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `171_container_bl_kind` (po `170_container_return_terminal`). `_WriteBox` — dodać `bill`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `ocean_bill.bill_kind` (`hbl`/`mbl`) na zleceniu. BRAK `container.bl_kind`. PODOBNE nazwa, INNA allowlista. Nie import ocean_bill / nie druga tabela listu. Nie duplikować `shipment_document`.

**Odrzucone:**

- `hbl`/`mbl` na kontenerze (to 160.0 `ocean_bill`).
- FK `carrier_party_id` / live HTTP / numer HBL.
- PIN / VGM / `booking_no`.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `171_container_bl_kind` (po `170_container_return_terminal`): `bl_kind` nullable `String(16)`.
2. POST opcjonalny `bl_kind`. Zły token → 400. Serwis nie importuje ocean_bills/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

ocean_bill · HBL/MBL · numer listu · PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `bl_kind=original` (albo seawaybill/telex/express) zapisuje token, bez numeru listu i bez PIN
- [x] POST bez pola zostaje `null`
- [x] `hbl` / inny token → 400 (`list`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape / import ocean_bill

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `bl_kind`; 400 `list` na `hbl`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(16)`; allowlista w domenie; brak FK |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `free_time_origin_h`; PIN/VGM parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
