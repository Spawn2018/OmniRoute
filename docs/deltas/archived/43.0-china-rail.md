# Plaster 43.0 · M-50 Kolej z Chin (`china_rail`)

**Spec źródłowa:** [docs/spec/china-rail.md](../../spec/china-rail.md)  
**Zależy od:** 4.0 `port.country_code` + `function_flags` · 42.0 `railPorts`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie korytarz.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 7 M-50):** kolej z Chin **nie ma tabeli korytarza**. Operator widzi porty `country_code = CN` z flagą `rail`. Token `china_rail` (UI, nie tabela). Żywy ID **M-50**. 42.0 zostaje całą koleją; 4.0 zostaje katalogiem.

**Łowca:** BRAK `belt_road` / korytarza CN. ISTNIEJE: `fetchPorts`, `railPorts`, `country_code`. PODOBNE: 42.0 wszystkie flagi rail.

**Odrzucone:**

- Tabela Nowego Jedwabnego Szlaku / rozkładu CN — zgadywanie schematu (CURRENT).
- HTTP do China Railway — leftover.
- Filtrowanie bez `rail` — to nie kolej.

## Zakres

Read-only `/china-rail`. `data-china-rail="board"`. Brak migracji. `chinaRailPorts(ports)`. Link do `/rail` i `/ports`.

## Poza zakresem

Tabela korytarza · CIM · live HTTP · ExtractionService

## Ustalenia

- Token: `china_rail` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia geography.
- RLS: bez nowej tabeli.
- Job: `/china-rail`. `SHIPPED_CHARGE_ROUTES["43.0"]` = `/china-rail`. OPS_JOBS +1 → 40.
- `just dup` ≤ 3% (2,88%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 43.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/china-rail` ma `data-china-rail="board"` i woła `chinaRailPorts`
- [x] Filtr `CN` + `rail`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["43.0"]` = `/china-rail`
- [x] `just dup` ≤ 3% (2,88%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CN + rail; brak korytarza |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | korytarz / HTTP CR zostaje |
| Docs/OS | PRZESZŁO | spec `china-rail.md`; CURRENT → Plan M-51 |

## Knowledge

Nie RAG. Nie silnik CR.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Korytarz i HTTP świadomie leftover.
