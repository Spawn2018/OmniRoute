# Plaster 33.0 · M-40 Fakturowanie (`sales_invoice` tablica odczytu)

**Spec źródłowa:** [docs/spec/sales-invoice.md](../../spec/sales-invoice.md)  
**Zależy od:** 1.2 `charge` sell · 15.0 `/finance`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie KSeF.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 6 M-40):** faktura **nie ma tabeli** w repo. Operator widzi `sell_amount` z `charge` na `/invoices` — jedyna znana sprzedaż do zafakturowania. Token `sales_invoice` (UI, nie tabela). Żywy ID **M-40**. 15.0 zostaje tablicą marży/NBP; 1.2 zostaje katalogiem opłat. KSeF leftover.

**Łowca:** BRAK `invoice` / KSeF / FV w modelach. ISTNIEJE: `fetchCharges`, `sell_amount`, `<Money/>`, `/finance`. PODOBNE: 15.0 tablica finansowa, 1.2 `/charges`.

**Odrzucone:**

- Tabela faktur / numer FV / KSeF — zgadywanie schematu (CURRENT).
- JS odejmujący buy/sell — marża zostaje w `charge`.
- LLM piszący treść faktury.

## Zakres

Read-only `/invoices`. `data-sales-invoice="board"`. Brak migracji. `fetchCharges` + `<Money/>` z `sell_amount`. Linki do `/charges` i `/finance`.

## Poza zakresem

Tabela faktur · KSeF · U-print · M-41 rozliczenie · ExtractionService

## Ustalenia

- Token: `sales_invoice` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia `charge`.
- RLS: bez nowej tabeli.
- Job: `/invoices`. `SHIPPED_CHARGE_ROUTES["33.0"]` = `/invoices`. OPS_JOBS +1 → 30.
- `just dup` ≤ 3% (2,90%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/invoices` ma `data-sales-invoice="board"` i woła `fetchCharges`
- [x] Brak nowej migracji; brak `ksef` / odejmowania buy/sell w ekranie
- [x] `SHIPPED_CHARGE_ROUTES["33.0"]` = `/invoices`
- [x] `just dup` ≤ 3% (2,90%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | sell z charge; brak KSeF i nowej tabeli |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | KSeF / numer FV zostaje |
| Docs/OS | PRZESZŁO | spec `sales-invoice.md`; CURRENT → Plan M-41 |

## Knowledge

Nie RAG. Nie silnik KSeF.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. KSeF i numer FV świadomie leftover.
