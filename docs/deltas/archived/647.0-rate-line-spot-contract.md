# Plaster 647.0 · leftover EXP1 rate_line spot_or_contract HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [646.0](../archived/646.0-rate-line-allotment.md) · [428.0](../archived/428.0-spot-contract-mark.md)
**Zależy od:** M-07 `rate_line` (646.0 allotment_teu)
**Status:** Zamknięty `/noc 7` 2026-09-24. Nie matching. Nie float. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 spot_or_contract na stawce):** tabela `rate_line` zostaje prawdą o stawce kupna. Katalog `spot_contract_mark` (428.0) zostaje osobnym słownikiem tenantowym — **nie** zastępuje kolumny na wierszu stawki. Żywy plaster: **pogłębienie istniejącej** `rate_line` — opcjonalny `spot_or_contract` (`spot`|`contract`|`other`, nullable). Operator oznacza, czy stawka jest spot / kontrakt / other; serwis nie dopasowuje marków SQL, nie tworzy `charge`, nie woła sieci, nie woła LLM. POST bez pola → `null`. Istniejące wiersze: `null`. Zmiana = supersede. GET listy SQL. UI `/rate-lines`. `SHIPPED_CHARGE_ROUTES["647.0"]` = `/rate-lines`. OpenFGA bez nowej relacji. Migracja `519_rate_line_spot_contract` po `518_rate_line_allotment`. Marża zostaje na `charge`. Zero float. Zero HTTP. Trigger niemutowalności obejmuje też `allotment_teu` + `spot_or_contract`.

**Dlaczego kolumna na `rate_line`, nie tylko mark:** karty EXP1 mówią „rate_line/charge: spot_or_contract”. Mark katalogowy nie wiąże stance z konkretną stawką; nullable token na niemutowalnym wierszu jest węższy.

**REQUIREMENT:** leftover EXP1 po 646.0. Źródło: `docs/analysis/karty-pol-fala-exp.md` § EXP1; CURRENT Następny **647.0**.

**Łowca:** ISTNIEJE `rate_line` — amount + source_ref + allotment_teu, **nie** spot_or_contract. ISTNIEJE `spot_contract_mark` — katalog deal_kind, **nie** kolumna na stawce. ISTNIEJE quotation leftover spot_or_contract (park). BRAK kolumny `spot_or_contract` na `rate_line`. BRAK matching. BRAK float.

**Odrzucone:**

- float / scoring oferty
- zapis `charge` / druga marża
- matching WHEN/IF / T-SQL / FK do `spot_contract_mark`
- kolumna na `quotation` w tym plasterze (osobny leftover)
- live HTTP / MQC
- mutacja w miejscu (tylko supersede)
- index_id FSC (następny leftover)

## Zakres

1. Migracja `519_rate_line_spot_contract`: kolumna `spot_or_contract` TEXT nullable + CHECK (`spot`|`contract`|`other`); trigger niemutowalności.
2. POST opcjonalnie przyjmuje `spot_or_contract`. Zła wartość → 400. Serwis nie importuje charges / quotations / extraction / spot_contract_marks.
3. UI `/rate-lines` (opcjonalne pole). How-to / fold. `just api-types`.

## Poza zakresem

index_id FSC · kolumna na quotation · matching · float · charge · FK mark · live HTTP · mutacja w miejscu

## Plan plików

- `backend/alembic/versions/519_rate_line_spot_contract.py`
- model / domain / serwis / API / testy rate_lines
- `frontend/src/features/rate-lines/` + API client
- `ops-index` + `routes-breadth` (`647.0` → `/rate-lines`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `spot_or_contract` ∈ {spot,contract,other} zapisuje wiersz; bez pola → null
- [x] zły token → 400
- [x] serwis nie importuje charges / quotations / extraction / spot_contract_marks i nie woła HTTP
- [x] RLS: tenant B nie widzi stawek tenanta A; zero float / marży / matching

## Leftover

index_id FSC na rate_line · spot_or_contract na quotation · matching · live MQC

## Pętla po plasterze

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | spot_or_contract HITL; pytest + vitest |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN + CHECK + REPLACE trigger |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | domain ≤40 |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
