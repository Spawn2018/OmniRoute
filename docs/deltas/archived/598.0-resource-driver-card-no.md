# Plaster 598.0 · T2 leftover — HITL `resource.driver_card_no`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [597.0](../archived/597.0-resource-phone.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`driver_card_no`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-20. Nie tacho live. Nie DDD. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `phone`. Opcjonalny `driver_card_no` na `resource` (`String(32)`, tekst HITL, nie float). Szerokość jak `inventory_no` / `registration_no`. POST bez pola → `null`. Pusty string → `null`. Tekst dłuższy niż 32 → 400. Zmiana numeru przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / inwentarz / kg / LDM / m³ / ADR / chłodnia / winda / telefon). Nie tacho live. Nie DDD. Nie `party`.
- **Wybrane:** UI `/fleet`. `SHIPPED["598.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `474_resource_driver_card_no` (po `473_resource_phone`). Bez nowej tabeli.
- **Odrzucone:** HW (`REJECTED`). tacho live / DDD parse (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.driver_card_no`. BRAK kolumny w całym backendzie. PODOBNE: `inventory_no` + `phone` + supersede.

## Zakres

1. Migracja `474_resource_driver_card_no`.
2. Domain `require_driver_card_no` + serwis + API + GET.
3. UI `/fleet` pole numeru karty kierowcy. How-to. `SHIPPED["598.0"]`.

## Poza zakresem

HW · tacho live · DDD · `party` · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `driver_card_no` zapisuje tekst
- [x] pusty string → `null`; >32 → 400
- [x] zmiana numeru → supersede
- [x] izolacja RLS (tabela `resource`)
- [x] zero float / import charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | HITL `driver_card_no` + supersede |
| Szybkość | N/A | |
| SQL | N/A | INSERT jak 597 |
| N+1 | OK | |
| Dług / człowiek | OK | ten sam wzorzec co `inventory_no` |
| Proza operatora | 5-15 | katalogi + zlecenie |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |

## Leftover

HW · `vehicle_profile` · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
