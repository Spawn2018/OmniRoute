# Plaster 257.0 · T2c `actual_distance_km` na `trip`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover T2c · [256.0](256.0-trip-planned-distance.md) leftover actual · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2
**Zależy od:** 256.0 `planned_distance_km`
**Status:** Zamknięty `/noc` 2026-09-10. Nie GPS. Nie `/fleet`. Nie `party`. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2c actual/`party`/`/fleet` + leftover T1b + leftover `appointment_status` + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover `subcontractor_party_id` parked. leftover `/fleet` parked. leftover T1b parked. leftover `appointment_status` / `no_show_at` parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `trip` — opcjonalny `actual_distance_km` (`Numeric(14,4)` HITL, ≥ 0, pusty → `null`). POST bez pola zostaje `null`. JSON float / bool / ujemna → 400 (`km`). `require_trip_actual_distance_km` w `app.domain.trip` — delegacja do `require_trip_planned_distance_km` (ten sam parser Decimal; nie druga kopia). Nie liczenie z GPS. UI `/shipments` panel przejazdu. `SHIPPED_CHARGE_ROUTES["257.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `193_trip_actual_distance` (po `192_trip_planned_distance`). CHECK `ck_trip_actual_distance`. `record_trip` zostaje ≤ 40 linii (dwa argumenty w jednej linii). Marża zostaje na `charge`. W plasterze: zakaz BC trip „km” → zakaz **liczenia** km / GPS; pole HITL wolno.

**Łowca:** ISTNIEJE `trip.planned_distance_km` (256.0). ISTNIEJE `require_trip_planned_distance_km`. BRAK `trip.actual_distance_km`. BRAK `/fleet`. BRAK `subcontractor_party_id`.

**Odrzucone:**

- liczenie z GPS / freeze kosztu / wariancja plan vs actual w SQL.
- `subcontractor_party_id` / `/fleet` / HW / import `parties` / mapa / T5 / S53 / live HTTP / float.

## Zakres

1. Migracja `193_trip_actual_distance`: `actual_distance_km` nullable Numeric(14,4) + CHECK ≥ 0. Nie `create_table`.
2. POST opcjonalny `actual_distance_km`. Zły token → 400 (`km`). Serwis nie importuje charges/parties i nie woła HTTP.
3. UI `/shipments` panel przejazdu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`subcontractor_party_id` · `/fleet` · GPS · mapa · T7b · T5 · Auth0 · live HTTP · float · wariancja SQL

## Kryteria akceptacji

- [x] POST z `actual_distance_km` zapisuje odległość wykonaną HITL, bez liczenia
- [x] POST bez pola / pusty trim zostaje `null`; `0` jest legalne
- [x] float / bool / ujemna / nie-Decimal → 400 (`km`)
- [x] RLS istniejącego `trip`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `parties`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `actual_distance_km`; 400 `km`; 0 legalne; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | Numeric + CHECK ≥ 0; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | delegacja do `require_trip_planned_distance_km`; leftover `party`/`/fleet`; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T2, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
