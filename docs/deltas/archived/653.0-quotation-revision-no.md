# Plaster 653.0 · leftover EXP1 quotation revision_no HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `quotation` · [431.0](../archived/431.0-quote-validity-mark.md) · [652.0](../archived/652.0-quotation-valid-until.md)
**Zależy od:** M- wycena `quotation`; `valid_until` (652.0); katalog `quote_validity_mark` (431.0) zostaje osobno
**Status:** Zamkni�te `/noc 7` 2026-09-25. Nie float. Nie NBP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 kolumn ważności wyceny):** tabela `quotation` zostaje prawdą o ofercie. Katalog `quote_validity_mark` (431.0) zostaje katalogiem stance — **nie** FK. Żywy plaster: **opcjonalny** `revision_no` (`INTEGER` nullable) na `quotation` — numer rewizji oferty HITL. Operator ustawia liczbę całkowitą ≥ 1; serwis nie liczy sekwencji, nie mnoży NBP, nie tworzy `charge`, nie woła HTTP/LLM, nie wiąże z `supersedes_id`. POST/PATCH bez pola → `null`. Zła wartość → 400. Istniejące wiersze: `null`. GET listy SQL. UI `/quotations`. `SHIPPED_CHARGE_ROUTES["653.0"]` = `/quotations`. OpenFGA bez nowej relacji. Migracja `524_quotation_revision_no` po `523_quotation_valid_until`. Marża zostaje na `charge`. Zero float.

**Dlaczego tylko `revision_no`, nie para:** leftover 431.0 wymienia trzy kolumny. 652.0 zabrał `valid_until`; najwęższy następny = jeden numer rewizji; `supersedes_id` = osobny leftover (park w CURRENT).

**REQUIREMENT:** leftover EXP1 po 652.0 / 431.0. Źródło: CURRENT Następny **653.0**; docs-debt 652.0.

**Łowca:** ISTNIEJE `quotation`. ISTNIEJE `valid_until` (652.0). ISTNIEJE `quote_validity_mark` (stance). BRAK `revision_no` na `quotation`. BRAK float / NBP / supersede chain.

**Odrzucone:**

- `supersedes_id` w tym plasterze (leftover / park)
- FK do `quote_validity_mark`
- auto-inkrement rewizji / sekwencja Postgres zamiast HITL
- float / NBP / mnożenie / Deadline Engine
- zapis `charge` / auto-award
- live HTTP

## Zakres

1. Migracja `524_quotation_revision_no`: kolumna `INTEGER` nullable.
2. Domain `require_revision_no`; API create/update opcjonalnie; UI `/quotations`.
3. How-to / fold. `just api-types`. `SHIPPED_CHARGE_ROUTES["653.0"]`.

## Poza zakresem

supersedes_id · spot_or_contract na quotation (park) · Deadline Engine · NBP · float · charge · auto-inkrement

## Plan plików

- `backend/alembic/versions/524_quotation_revision_no.py`
- model / domain / serwis / API / testy quotations
- `frontend/src/features/quotations/` + API client
- `ops-index` + `routes-breadth`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST/PATCH z `revision_no` ≥ 1 zapisuje; bez pola → null
- [ ] zła wartość (float, 0, ujemna, string) → 400; serwis nie mnoży NBP i nie tworzy charge
- [ ] RLS: tenant B nie widzi wycen A; zero float / marży
- [ ] `quote_validity_mark` / `valid_until` bez regresji

## Leftover

supersedes_id · spot_or_contract na quotation (park) · matching · Deadline Engine
