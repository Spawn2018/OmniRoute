# Plaster 464.0 · BR3.1 planowanie załadunku

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR3.1 · G6 `load_plan_mark`

**Zależy od:** tenancy RLS · wzorzec HITL `groupage_dispatcher_mark` (463.0) · G6 `load_plan_mark` już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `load_order_marks`. Tabela `load_order_mark`: `mark_code` + `order_kind` `sequence|stack|door|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — kolejność załadunku jako stance, nie osie G6 i nie solver.
- **Wybrane:** POST/GET `/api/v1/load-order-marks`. OpenFGA `can_manage_load_order_marks` (member). UI `/load-order-marks`. `SHIPPED["464.0"]` → `/load-order-marks`.
- **Wybrane:** Alembic `369_load_order_mark` (`down_revision = "368_groupage_disp_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `load_plan_mark` (`REJECTED` — G6 zostaje osie/tunel, nie kolejność).
- **Odrzucone:** klej FK do `load_plan_mark` / `trip` / `shipment` / `resource` (`REJECTED` na tym wierszu).
- **Odrzucone:** solver OR · LLM-VRP · wymiary Decimal · osie Decimal · live · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `load_plan_mark` (G6 osie/tunel). ISTNIEJE `bin_pack_mark` (EXP2.16 objętość/waga). ISTNIEJE `oog_mark` (G5). PODOBNE katalog HITL. BRAK `load_order_mark`. BRAK solvera załadunku.

## Zakres

How-to: jedno zdanie — to katalog kolejności załadunku; solver i wymiary zostają leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

solver OR · wymiary Decimal · osie Decimal · FK do G6 · Valhalla · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

solver OR · wymiary Decimal · osie Decimal · FK do `load_plan_mark` · BR3.3 tacho w planie

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS dwa tenanty; amount/solver 422/400 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | INSERT/SELECT katalogu |
| N+1 | OK | jeden SELECT listy |
| Dług / człowiek | OK | osobny BC obok `load_plan_mark`; bez FK |
| Proza operatora | 5–15 zdań | `docs/operator/load-order-mark.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–463 |
| Minuty plastra | 50 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
