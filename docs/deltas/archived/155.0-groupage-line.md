# Plaster 155.0 · D1 `groupage_line`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D1 · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D1
**Zależy od:** 4.1 `location` · T1 `stop` już jest (poza tym plasterem)
**Status:** Zamknięty `/noc` 2026-09-08. Nie WMS. Nie OR hubów.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z D1):** jedna tabela katalogu linii drobnicy per tenant. Wiersz = `line_code` + dwa `location` (start i koniec, FK złożone) + `cutoff_local` (TIME, godzina odcięcia — dana, nie wyliczenie) + `transit_days` (int ≥ 1) + `operating_dows` (ISODOW 1–7, tablica, nie kalendarz U4) + `source_ref`. Override tego samego `line_code` = nowy wiersz + `superseded_by`. GET listy SQL (bieżące). Końce: `postal_zone` albo `address` (nie UN/LOCODE). API składa odczyt `location`; serwis linii nie importuje geography. UI `/groupage`. `SHIPPED_CHARGE_ROUTES["155.0"]` = `/groupage`. OpenFGA: `can_manage_groupage_lines`. Migracja `095_groupage_line`.

**Łowca:** ISTNIEJE `channel_quote.transit_days` — **inny** agregat (oferta kanału), nie linia sieci. ISTNIEJE `location`. ISTNIEJE `organization_calendar` / `is_working_day` — **inny** (święta kraju), nie dni linii. ISTNIEJE `shipment_leg` road — odcinek na zleceniu, nie katalog linii. BRAK `groupage_line`. BRAK `/groupage`. BRAK cutoff na katalogu.

**Odrzucone:**

- OR wielu hubów na jednym końcu (leftover D1b).
- `shipment_package` / skan (D2).
- cross-dock / WMS (D3).
- cennik LTL / FSC (D5 / P1).
- live HTTP / optymalizator.

## Zakres

1. Migracja `095_groupage_line`: RLS, izolacja, indeks `(organization_id, line_code)`.
2. POST/GET. Strefa UN/LOCODE / `transit_days=0` / pusty ISODOW → 400.
3. UI `/groupage`. `just api-types`. How-to wtopione (katalogi ≤ 40).

## Poza zakresem

D2 paczka · D3 WMS · OR hubów · kwota · mapa · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje linię; UN/LOCODE jako koniec → 400
- [x] Ten sam `line_code` = nowy wiersz, stary `superseded_by`
- [x] GET zwraca bieżące
- [x] Test izolacji tabeli
- [x] Zero float / marży / WMS

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET, 400 na UN/LOCODE i TT=0, RLS + EXPLAIN indeks |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | EXPLAIN | `ix_groupage_line_org_code` na `(organization_id, line_code)` |
| N+1 | OK | GET jedna lista; POST = get dwóch location + INSERT |
| Dług / człowiek | OK | panel `fieldset` nie kopiuje `/air`; leftover OR hubów w docs-debt |
| Proza operatora | 5–15 zdań | `linia.md`; `katalogi.md` wtopione, 40 linii |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
