# Plaster 474.0 · BR1.0 WMS — katalog przepływu magazynowego

**Status:** Zamknięte `/noc` 2026-09-14. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 1 · § A.4 Trade-Tech · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR1.0 · BR1.1 · BR1.2

**Zależy od:** tenancy RLS · wzorzec HITL `inventory_position_mark` (EXP3.3) · `dock_appointment` (157.0 magazyn spedycyjny, nie WMS operacyjny)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `wms_flow_marks`. Tabela `wms_flow_mark`: `mark_code` + `flow_kind` `receipt|location|pick|ship|count|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR5.2 — pierwszy katalog HITL WMS (przyjęcie / lokalizacja / kompletacja / wydanie / inwentaryzacja jako dane), nie live WMS HTTP i nie ewidencja ilościowa.
- **Wybrane:** POST/GET `/api/v1/wms-flow-marks`. OpenFGA `can_manage_wms_flow_marks` (member). UI `/wms-flow-marks`. `SHIPPED["474.0"]` → `/wms-flow-marks`.
- **Wybrane:** Alembic `379_wms_flow_mark` (`down_revision = "378_exchange_connector_kinds"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `inventory_position_mark` (`REJECTED` — EXP3.3 to znacznik pozycji zapasu plant/sku, nie operacja WMS).
- **Odrzucone:** rozszerzenie `dock_appointment` / `shipment_package` (`REJECTED` — D2/D3 to zlecenie spedycyjne, nie WMS Trade-Tech).
- **Odrzucone:** live Manhattan / SAP EWM / Infios HTTP · RFID poll · qty float · lokalizacja bin jako FK (`REJECTED` — BR1.1/BR1.2 leftover; CURRENT park live HTTP).
- **Odrzucone:** FK do `purchase_order` / `po_line` / wycena zapasu BR1.2 (`REJECTED` — BR5.1 katalog zostaje, klej PO = leftover).
- **Odrzucone:** kwota / marża / float / UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `inventory_position_mark` (stock_kind position/plant/sku). ISTNIEJE `dock_appointment` (okno doku spedycyjnego). ISTNIEJE `shipment_package` (paczka na zleceniu). ISTNIEJE `po_plant_mark` / `po_batch_mark` (PO plant/batch). ISTNIEJE `bonded_mark` (procedura magazynowa). PODOBNE katalog HITL stance per tenant. BRAK `wms_flow_mark`. BRAK katalogu operacji WMS (przyjęcie–inwentaryzacja).

## Zakres

How-to: jedno zdanie — to katalog rodzaju operacji WMS; lokalizacja bin, RFID i live WMS zostają leftover. +1 OPS_JOB. Klient FE: `flow_kind` jako segmented control (nie radio 469, nie toggle 470, nie select 471).

## Poza zakresem

live WMS HTTP · RFID · qty · FK location/bin · wycena zapasu BR1.2 · BR1.1 · Inventory Release · sekrety · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live Manhattan / SAP EWM / Infios · RFID/bramki · lokalizacja bin · qty · wycena zapasu BR1.2 · FK PO · Inventory Release · Trade-Tech zastaw
