# Plaster 205.0 · P1b matching GET `rate_card`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P1 leftover · [163.0](../archived/163.0-rate-card.md) leftover P1b · [69.0](../archived/69.0-port-surcharge-when.md)
**Zależy od:** 163.0 `rate_card` (karta + `applies_when` już jest) · 69.0 extra portowe (wzorzec równości SQL; ten plaster nie importuje port_surcharges)
**Status:** Zamknięty `/noc` 2026-09-09. Nie parser WHEN/IF. Nie T-SQL. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P1b matching + P1c parser + P1d exclusion + leftover D9c–f + V7–V8 + C3–C9 + F2b–F4 + G2.20):** leftover D9c–f / V7–V8 / V6 / V5 / V4 / V2b / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. leftover P1c WHEN/IF/CALC parked. leftover P1d exclusion parked. Żywy plaster: **pogłębienie istniejącej** `rate_card` — GET listy po dokładnej równości `applies_when` w SQL, jak 69.0 extra portowe, nie parser AST. Query `applies_when` (1–512). GET `/rate-cards` bez query = dotychczasowa lista. GET `/matching` bez query albo z pustym warunkiem → 400 (`warunek`). Indeks `(organization_id, applies_when)`. UI `/rate-cards` „Dopasuj warunek” bez `<Money/>` sumy i bez `parseFloat`. `SHIPPED_CHARGE_ROUTES["205.0"]` = `/rate-cards`. OpenFGA bez nowej relacji (`can_manage_rate_cards`). Migracja `145_rate_card_match`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `port_surcharge.list_matching` — równość `applies_when` + `port_id`, **nie** karta stawek. ISTNIEJE `rate_card` POST/GET cała lista. ISTNIEJE how-to „matching SQL jak extra portowe” jako poza zakresem. BRAK GET match na `rate_card`. BRAK parsera WHEN/IF. BRAK exclusion daterange.

**Odrzucone:**

- Parser WHEN/IF/CALC/MIN/MAX (P1c).
- Daty ważności / exclusion GiST (P1d / P2c).
- Import `port_surcharges` / zapis `charge` / `rate_line`.
- T-SQL / live HTTP / scrape / float / druga marża.

## Zakres

1. Migracja `145_rate_card_match` (po `144_shipment_ref`): indeks `(organization_id, applies_when)`.
2. GET `/matching` — równość SQL. Serwis nie importuje port_surcharges / charges / extraction i nie woła HTTP.
3. UI `/rate-cards`. `just api-types`. How-to `karta-stawek.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

P1c parser · P1d exclusion · T-SQL · FSC · charge · live HTTP · float

## Kryteria akceptacji

- [x] GET bez query zwraca karty tenanta (jak 163.0)
- [x] GET z `applies_when` zwraca tylko równość SQL
- [x] zły / pusty query gdy podany → 400 (`warunek`)
- [x] EXPLAIN używa indeksu org+when albo unique
- [x] Test izolacji (match nie widzi karty tenanta B)
- [x] Zero parsera / float / marży / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | GET `/matching` równość SQL; 400 `warunek`; GET listy bez query jak 163.0 |
| Szybkość | N/A ten plaster | indeks equality, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `ix_rate_card_org_when` albo Index Scan na org+when |
| N+1 | OK | GET matching = jeden SELECT; lista = jeden SELECT |
| Dług / człowiek | OK | `fetch_equal_when` / `WhenEqualStrip`; leftover P1c–d parser/exclusion; bez nowego OPS job |
| Proza operatora | 5–15 zdań | `karta-stawek.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN P1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
