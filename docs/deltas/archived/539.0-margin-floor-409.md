# Plaster 539.0 · N6 leftover — 409 gdy marża < `margin_floor`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N6 leftover · [527.0](../archived/527.0-margin-floor.md)

**Zależy od:** `margin_floor` HITL (527.0) · `charge` + `margin()` · para UN/LOCODE

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover N6. Na `POST /charges` opcjonalne `origin_unlocode` + `destination_unlocode` (tylko do sprawdzenia — **nie** kolumny na `charge`). Gdy para podana: API czyta `margin_floor` tej samej waluty i pary; jeśli `sell−buy` < `floor_amount` → **409** PL. Bez pary — zachowanie 527.0 (zero egzekucji).
- **Wybrane:** składanie w API (`charges.py`), nie w `ChargeService` / `MarginFloorService` (HC granic BC). Domena: `MarginFloorBreach` → 409.
- **Odrzucone:** S11 auto-mail (`REJECTED` — leftover). Matching „podobnego” lane / Haversine (`REJECTED`). Kolumny lane na `charge` (`REJECTED`). Float (`REJECTED`).
- **Odrzucone:** import `margin_floors` w `charges` service (`REJECTED`).

**Łowca:** ISTNIEJE `margin_floor` + `margin()`. ISTNIEJE 409 dla konfliktów w `main.py`. BRAK egzekucji podłogi. `charge` bez UN/LOCODE — check tylko z body.

## Zakres

1. `MarginFloorBreach` + asercja domeny.
2. Repo/odczyt pary+waluta; API create przed INSERT.
3. Test HTTP 409 / 201 bez pary / 201 powyżej podłogi.
4. How-to `margin-floor.md` + `ścieżka-pieniędzy` fold.
5. `SHIPPED["539.0"]` → `/charges`.

## Poza zakresem

S11 · matching fuzzy · kolumna lane na charge · UI nowego formularza UN (opcjonalne pola gdy FE ładuje floors)

## Kryteria akceptacji

- [x] POST bez UN → 201 jak dziś
- [x] POST z parą + margin < floor → 409 PL
- [x] POST z parą + margin ≥ floor → 201
- [x] zero float / S11 / zapis floor z charge

## Leftover

S11 zamiast 409 · matching lane na wycenie · pola UN na UI `/charges`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | 409 + opcjonalna para |
| Szybkość | N/A | brak ścieżki budżetu |
| SQL | N/A | find_for_lane Index-ready WHERE; bez EXPLAIN w tym plasterze |
| N+1 | OK | jeden SELECT przed INSERT |
| Dług / człowiek | OK | compose w API |
| Proza operatora | 5–15 | `margin-floor.md` |
| Docs/OS | PRZESZŁO | CURRENT + VISION + PLAN |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `--close` |
