# Plaster 448.0 · AI3.1 wersja szkicu + `bbox` + pewność

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI3.1

**Zależy od:** 447.0 `PATCH` `candidates` · `can_review_extractions`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `extraction`. Bez nowej tabeli. `ExtractedChargeCandidate` dostaje opcjonalne `bbox_text` i `confidence_text` (tekst, jak `amount_text`). `extra=forbid`.
- **Wybrane:** `payload.revision` liczba całkowita w JSONB: 0 przy tworzeniu szkicu, +1 przy każdym `PATCH` pending/`rate_line`. Nie kolumna Alembic.
- **Wybrane:** GET i PATCH niosą nowe pola. Accept 1.3 nadal czyta tylko kod / `amount_text` / walutę. Serwis nie zapisuje `rate_line`.
- **Wybrane:** UI `/extractions` — pola ramki i pewności przy poprawce; widać numer wersji. How-to `docs/operator/ekstrakcja.md`.
- **Odrzucone:** tabela historii / `parent_id` (`REJECTED` — ślad wersji to licznik, nie kopia wiersza).
- **Odrzucone:** zmiana `draft_kind` (`REJECTED` — już jest).
- **Odrzucone:** `float` na pewności · cztery floaty ramki · rysowanie na PDF (`REJECTED` — AI3.2).
- **Odrzucone:** PATCH quote/rfp · Excel · golden · zapis z modelu · auto-accept.

**Łowca:** ISTNIEJE PATCH 447.0 i schemat kandydata. PODOBNE `amount_text` jako dana, nie wynik. BRAK `bbox_text` / `confidence_text` / `payload.revision`. `just dup` bez wyodrębnienia.

## Zakres

OpenFGA to samo `can_review_extractions`. `SHIPPED["448.0"]` → `/extractions`. Bez +1 OPS_JOB.

## Poza zakresem

AI3.2 obraz wprost · AI3.3 golden · AI3.4 Excel · historia wierszy · quote/rfp PATCH

## Kryteria akceptacji

- [x] nowy szkic ma `payload.revision` = 0; PATCH pending podnosi o 1
- [x] PATCH przyjmuje `bbox_text` i `confidence_text`; `confidence` float / extra pole → 422
- [x] GET pokazuje ramkę, pewność i wersję; accept nie woła extractora
- [x] UI pokazuje wersję i pozwala wpisać ramkę/pewność przed accept
- [x] brak `float`; craft_style funkcje>40: 0

## Leftover

historia szkicu · PATCH quote/rfp · AI3.2–3.4 · Brier

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | revision 0→1; bbox/pewność tekst; 422 na float |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | leftover | ten sam UPDATE JSONB; EXPLAIN nie w tym plasterze |
| N+1 | OK | jeden get + flush |
| Dług / człowiek | OK | pola na istniejącym schemacie; bez tabeli historii |
| Proza operatora | 5–15 zdań | `docs/operator/ekstrakcja.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–447 |
| Minuty plastra | 15 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
