# Plaster 29.0 · M-36 Tracking (`tracking` tablica odczytu)

**Spec źródłowa:** [docs/spec/tracking.md](../../spec/tracking.md)  
**Zależy od:** 5.1 POL/POD na `quotation` · `quotationLanes` · 4.0 `port` · 28.0 `/shipments`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 5 M-36):** tracking **nie ma tabeli zdarzeń** w repo. Operator widzi znany lane wyceny (POL→POD) na `/tracking`. Token `tracking` (UI, nie tabela). Żywy ID **M-36**. 28.0 zostaje tablicą zlecenia; 29.0 = odczyt lane.

**Łowca:** BRAK `tracking` / milestone / AIS / `shipment_leg`. ISTNIEJE: `quotationLanes`, `fetchPorts`. PODOBNE: 23.0 ślad kanału na lane, 28.0 `/shipments`.

**Odrzucone:**

- Tabela zdarzeń / ETA / AIS — zgadywanie schematu (CURRENT).
- `shipment_leg` — leftover 28.0.
- Mapa / Watchtower — parked; nie initial JS.
- LLM liczący ETA albo odległość.

## Zakres

Read-only `/tracking`. `data-tracking="board"`. Brak migracji. `quotationLanes` + UN/LOCODE z `port`. Linki do `/shipments` i `/quotations`.

## Poza zakresem

Tabela trackingu · odcinki · AIS · mapa · M-37 wyjątki · ExtractionService

## Ustalenia

- Token: `tracking` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations`.
- RLS: bez nowej tabeli.
- Job: `/tracking`. `SHIPPED_CHARGE_ROUTES["29.0"]` = `/tracking`. OPS_JOBS +1.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/tracking` ma `data-tracking="board"` i woła `quotationLanes` oraz `fetchPorts`
- [x] Brak nowej migracji; brak mapy; brak `eta` w ekranie
- [x] `SHIPPED_CHARGE_ROUTES["29.0"]` = `/tracking`
- [x] `just dup` ≤ 3% (2,73%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | lane POL/POD + UN/LOCODE; brak AIS i mapy |
| Szybkość | N/A ten plaster | odczyt lokalnych list |
| Dług w diffie | leftover | `just dup` 2,73%; tabela zdarzeń / AIS / mapa zostaje |
| Docs/OS | PRZESZŁO | spec `tracking.md`; CURRENT → Plan M-37 |

## Knowledge

Nie RAG. Nie silnik AIS.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela zdarzeń, AIS i mapa świadomie leftover.
