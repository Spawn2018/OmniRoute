# Plaster 527.0 · N6 — `margin_floor` HITL Decimal

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N6 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) · po [526.0](../archived/526.0-kpi-definition-mark.md)

**Zależy od:** P0 `charge.source_ref` · P1 rate_card (poza zapisem) · `charge` + `margin()` · para UN/LOCODE jak `sales_lane`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `margin_floors`. Tabela `margin_floor`: `floor_code` + para `origin_unlocode` / `destination_unlocode` + `floor_amount` Decimal + `floor_currency` CHAR(3) + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja N6 — **podłoga marży jako dana HITL**, nie egzekucja 409/S11 w tym plasterze.
- **Wybrane:** POST/GET `/api/v1/margin-floors`. OpenFGA `can_manage_margin_floors`. UI `/margin-floors`. `SHIPPED["527.0"]`.
- **Wybrane:** how-to `docs/operator/margin-floor.md`.
- **Odrzucone:** 409 na POST `charge` / S11 auto (`REJECTED` na tym wierszu — leftover).
- **Odrzucone:** float · druga kolumna marży na `charge` · LLM liczący podłogę · FK do `sales_lane` / `charge` (`REJECTED`).

**Łowca:** ISTNIEJE `margin()` · GET SQL `sell−buy` (262.0) · `sales_lane` (para UN/LOCODE). BRAK `margin_floor`.

## Zakres

HITL `margin_floor` — migracja 417 + warstwy + OpenFGA + FE + how-to + SHIPPED 527.0. Kwota tylko Decimal.

## Poza zakresem

409 na charge · S11 · matching lane przy wycenie · float · live HTTP

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST float/amount-obok → 422; zły UN/LOCODE → 400
- [x] UI `/margin-floors`; `SHIPPED["527.0"]`
- [x] craft_style; how-to

## Leftover

409 gdy `sell−buy` < floor na POST charge · S11 zamiast twardego 409 · matching lane na wycenie

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO* | *po `gate` success na SHA close |
| Szybkość | N/A | brak ścieżki budżetu |
| SQL | N/A | list/add bez nowego resolve |
| N+1 | OK | brak pętli z session |
| Dług / człowiek | OK | klon benefit_ledger + sales_lane UN/LOCODE |
| Proza operatora | 5–15 | `docs/operator/margin-floor.md` |
| Docs/OS | PRZESZŁO | CURRENT + archiwum + bench |
| Pushy do zielonego | N/A | po pushu |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
