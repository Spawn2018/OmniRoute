# Plaster 573.0 · T3 leftover — HITL `container.quantity`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [572.0](../archived/572.0-container-teu.md) · `stop.quantity` w `backend/app/models/stop.py` (inny BC, Integer, CHECK ≥ 0)

**Zależy od:** 153.0 `container` · 572.0 `teu`

**Status:** Zamknięty `/noc` 2026-09-18. Nie `stop.quantity`. Nie TEU. Nie kalkulator. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** leftover po TEU mówi quantity na kontenerze. W `container` kolumny nie ma. `stop.quantity` jest Integer i CHECK `quantity IS NULL OR quantity >= 0` — to wzorzec typu, nie ta kolumna i nie zapis `stop`. Opcjonalny `quantity` Integer na `container`. POST bez pola → `null`. Ujemna / bool / float / nie-int → 400 (`ilość`). Zero zostaje (jak CHECK na stop). Bez sufitu 3650 — to nie dni. Nie mutuje `teu` / `payload_kg`.
- **Wybrane:** `require_container_quantity` osobno od `_require_dd_days` (tam komunikat „dni” i sufit dni). UI `/shipments`, liczba jak dni (`optionalHours`), nie tekst kg. `SHIPPED["573.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `453_container_quantity` (po `452_container_teu`). Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** FK / zapis `stop` (`REJECTED`). TEU (`REJECTED`). Decimal / float (`REJECTED`). Sufit dni (`REJECTED`). Kalkulator sztuk (`REJECTED`). ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `quantity` na `container`. ISTNIEJE `stop.quantity` (T1, inny BC). ISTNIEJE `_require_dd_days` (dni, nie sztuki). Nie nowa tabela.

## Zakres

1. Migracja `453_container_quantity`: Integer nullable, CHECK null albo ≥ 0.
2. Domain `require_container_quantity` + API POST opcjonalny + GET (int).
3. UI `/shipments`. How-to dopisek. `SHIPPED["573.0"]`.

## Poza zakresem

`stop.quantity` · TEU · kalkulator · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `quantity=3` zapisuje int
- [x] float / bool / ujemna → 400 (`ilość`)
- [x] zero przechodzi
- [x] izolacja RLS
- [x] zero zapisu `stop` / mutacja `teu`

## Leftover

`weight_kg` na kontenerze (karta T3, nie `stop.weight_kg`) · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL quantity Integer |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Integer + CHECK |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | osobny `require_container_quantity`; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
