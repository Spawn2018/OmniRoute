# Plaster 77.0 · S14 lock optymistyczny na decyzji (M-71)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S14  
**Zależy od:** 74.0 `operator_decision` · 76.0 `subject_kind=mail_draft`

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID M-71. Arch. M-187 nie dostaje osobnego kodu. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-71**. Kolumna `lock_version` INTEGER NOT NULL, create = 0. Dwa okna, dwa Akceptuj: POST decide wysyła `lock_version` z wiersza; jeden `UPDATE … WHERE id AND status = pending AND lock_version = oczekiwany` z inkrementem. Trafienie 0 wierszy = konflikt (nie cichy overwrite). Werdykt nadal tylko `accepted` | `rejected`. OpenFGA bez nowej relacji. Ekran `/decisions` wysyła wersję z listy. Serwis nie importuje inbound / quotations / extraction / mail_drafts. Nie send. Nie nowa tabela.

**Łowca:** BRAK `lock_version` / ETag / `UPDATE … WHERE status = pending`. ISTNIEJE: `require_pending_before_decide` po GET — wyścig dwóch pending. ISTNIEJE: unikat pending na `(organization_id, subject_kind, subject_id)` — to create, nie decide. PODOBNE: 74.0 drugi decide po zapisie = błąd w Pythonie, nie atomowy UPDATE.

**Odrzucone:**

- Nowy żywy ID / nowa tabela (arch. M-187 scala się w M-71).
- SELECT FOR UPDATE / pesymistyczny lock (S14 = optymistyczny).
- Zapis `changed` (zostaje leftover).
- Send / Graph / outbox.
- FK do inbound / mail_draft.
- Accept extractu 1.3.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `037_operator_decision_lock`: kolumna `lock_version`. Test: dwa decide z tą samą wersją — jeden wygrywa.
2. POST `/{id}/decide` wymaga `lock_version`. Konflikt = błąd domenowy, nie overwrite.
3. UI `/decisions`: Akceptuj/Odrzuć niesie `lock_version` wiersza. Nie licz w JS.

## Poza zakresem

`changed` · send · outbox · S15 Graph ingest · F9.1

## Ustalenia

- Żywy ID: **M-71**. Token: `operator_decision`. Arch. M-187 bez osobnego kodu.
- HITL extract bez zmian. LLM nie liczy.
- How-to: `docs/operator/katalogi.md`.

## Schemat

`operator_decision.lock_version` INTEGER NOT NULL DEFAULT 0.

## Kryteria akceptacji

- [x] Create = `lock_version` 0
- [x] Dwa decide z wersją 0: jeden accepted/rejected, drugi konflikt
- [x] Decide bez / ze złą wersją nie nadpisuje
- [x] UI `/decisions` wysyła `lock_version`
- [x] Serwis nie importuje inbound / quotations / extraction
- [x] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | drugi claim z wersją 0 = None |
| Szybkość | N/A ten plaster | jeden UPDATE, nie wycena |
| SQL | N/A | UPDATE po id+status+wersja; bez JOIN |
| N+1 | OK | jeden claim |
| Dług / człowiek | OK | `changed` leftover; send S18 |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT, `just docs` |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
