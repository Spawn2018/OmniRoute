# Plaster 181.0 · G2.11 `tender_bid_stance`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.11 · leftover bid/no-bid
**Zależy od:** 169.0 `tender` (nagłówek już jest)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.11–G2.18):** jedna tabela `tender_bid_stance` per tenant — **HITL postawa bid/no-bid z `source_ref`**, nie silnik scoringu i nie four-eyes. Wiersz = `tender_id` (FK tenanta do `tender`) + `stance_code` (`bid` | `no_bid`) + `source_ref` (`tenant:manual` albo `fixture://tender-bid-stance/…`). Operator wpisuje, czy idziemy w przetarg; serwis nie woła LLM, nie mutuje `tender.status`, nie zapisuje `tender_win_loss` i nie czyta `charge`. Zły kod → 400 (`udział`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id)` — jedna postawa na przetarg. GET listy SQL. API składa odczyt nagłówka (`TenderService.get_board`). UI `/tender-bid-stances` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["181.0"]` = `/tender-bid-stances`. OpenFGA: `can_manage_tender_bid_stances`. Migracja `121_tender_bid_stance`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tender_win_loss.outcome` (`won` | `lost` | `no_bid`) — wynik po fakcie, **nie** decyzja udziału. ISTNIEJE `tender.status` (`no_bid`) — nagłówek; **nie** mutujemy. ISTNIEJE `operator_decision` — szyna Akceptuj/Zmień/Odrzuć, nie bid/no-bid G2. BRAK `tender_bid_stance`. BRAK `/tender-bid-stances`.

**Odrzucone:**

- Award four-eyes / TED capability / CO₂ (leftover G2.12–G2.18).
- Auto-award / scrape / UPDATE `tender.status` / INSERT `tender_win_loss`.
- Kwota / druga marża / scoring.
- Float / T-SQL / live HTTP.

## Zakres

1. Migracja `121_tender_bid_stance`: RLS, izolacja, indeks `(organization_id, stance_code)`, unique `(organization_id, tender_id)`, CHECK `stance_code`.
2. POST/GET. Zły `stance_code` / `source_ref` → 400. API składa odczyt `tender`, serwis nie importuje tenders / win_losses / charges / extraction.
3. UI `/tender-bid-stances`. `just api-types`. How-to `docs/operator/udzial-przetargu.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `StanceDesk` / `stanceWrite` / `persistStanceMark`.

## Poza zakresem

G2.12–G2.18 four-eyes/TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · scrape · LLM · kwota na wierszu · mutacja `tender.status` · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `tender_bid_stance` z postawą i `source_ref` na istniejącym `tender`, bez auto-award
- [x] zły `stance_code` → 400
- [x] GET zwraca postawy tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP / scrape / UPDATE `tender.status`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST bid/no-bid + `source_ref`; 400 `udział` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `(organization_id, stance_code)` — `ix_tender_bid_stance_org_stance` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover G2.12–G2.18; jscpd na plikach plastra |
| Proza operatora | 5–15 zdań | `udzial-przetargu.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.11, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
