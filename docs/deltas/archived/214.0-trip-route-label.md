# Plaster 214.0 · T2 `route_label` na `trip`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover · [152.0](../archived/152.0-trip.md) leftover T2c · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2
**Zależy od:** 152.0 `trip` + 212.0 `driver2_id`
**Status:** Zamknięty `/noc` 2026-09-09. Nie km. Nie `/fleet`. Nie `party`. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2 `route_label`/km/`party`/`/fleet` + leftover T1b tabela + leftover T1 EXP1 + leftover T7b na `charge` + leftover T5 + S53):** leftover T2c km/`party`/`/fleet` parked. leftover T1b tabela parked. leftover T1 EXP1 parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. Karta T2: `route_label` = etykieta trasy HITL (SPEED „GDYNIA (PL) - BŁONIE (PL)"), **nie** odległość. Żywy plaster: **pogłębienie istniejącej** `trip` — opcjonalny `route_label` (1–128 znaków po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token (nie tekst, za długi) → 400 (`trasa`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["214.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `153_trip_route_label`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `trip` — numer + status + flota + snapshot + `driver2`. ISTNIEJE `stop` — punkt, **nie** etykieta przejazdu. BRAK `route_label`. BRAK `planned_distance_km`. BRAK `/fleet`.

**Odrzucone:**

- `planned_distance_km` / `actual_distance_km` / Decimal km (leftover T2c).
- `subcontractor_party_id` / `/fleet` / HW.
- T1b tabela · T1 EXP1 · T7b `charge` · T5 outbox · mapa · S53 Auth0.

## Zakres

1. Migracja `153_trip_route_label` (po `152_stop_group`): `route_label` nullable `String(128)`.
2. POST opcjonalny `route_label`. Zły token → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments` panel przejazdu. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

km · party · fleet · T1b · EXP1 · T7b · T5 · mapa · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST z `route_label` zapisuje etykietę, bez km
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`trasa`)
- [x] RLS istniejącego `trip`; izolacja
- [x] Zero float / marży / km / `/fleet` / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `route_label`; 400 `trasa`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(128)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `trip`, nie km; leftover T2c km/`party`/`/fleet`; C901 czysty; bez nowego OPS job |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T2, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
