# Plaster 655.0 · leftover EXP1 quotation mqc_window HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `quotation` · [654.0](../archived/654.0-quotation-mqc-teu.md)
**Zależy od:** M- wycena `quotation`; `mqc_teu` (654.0)
**Status:** Zamknięte `/noc 7` 2026-09-25. Nie float. Nie MQC SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 pary MQC):** po `mqc_teu` (654.0) żywy plaster = **opcjonalny** `mqc_window` (`VARCHAR(64)` nullable) na `quotation` — etykieta okna zobowiązania HITL (np. `CY2026`, `Q1`). Operator wpisuje tekst 1–64; serwis nie liczy okna, nie mnoży NBP, nie tworzy `charge`, nie woła HTTP/LLM, nie FK do `mqc_mark`. POST/PATCH bez pola → `null`. Pusty po trim → `null`. Zbyt długi / nie-tekst → 400. Istniejące wiersze: `null`. GET listy SQL. UI `/quotations`. `SHIPPED_CHARGE_ROUTES["655.0"]` = `/quotations`. OpenFGA bez nowej relacji. Migracja `526_quotation_mqc_window` po `525_quotation_mqc_teu`. Marża zostaje na `charge`. Zero float.

**Dlaczego tekst, nie daty:** leftover mówi `mqc_window` obok TEU — okno jako dana HITL, nie silnik zakresu dat (Deadline Engine = park).

**REQUIREMENT:** leftover EXP1 po 654.0. Źródło: CURRENT Następny **655.0**.

**Łowca:** ISTNIEJE `quotation.mqc_teu` (654.0). ISTNIEJE `mqc_mark` (stance). BRAK `mqc_window` na `quotation`. BRAK float / MQC SQL.

**Odrzucone:**

- Deadline Engine / zakres dat SQL
- FK do `mqc_mark`
- MQC SQL / actual
- float / NBP / charge
- supersedes_id / spot_or_contract na quotation (park)

## Zakres

1. Migracja `526_quotation_mqc_window`: kolumna `VARCHAR(64)` nullable.
2. Domain `require_mqc_window`; API create/update opcjonalnie; UI `/quotations`.
3. How-to / fold. `just api-types`. `SHIPPED_CHARGE_ROUTES["655.0"]`.

## Poza zakresem

supersedes_id · spot_or_contract na quotation (park) · MQC SQL · Deadline Engine · NBP · float · charge

## Plan plików

- `backend/alembic/versions/526_quotation_mqc_window.py`
- model / domain / serwis / API / testy quotations
- `frontend/src/features/quotations/` + API client
- `ops-index` + `routes-breadth`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST/PATCH z `mqc_window` 1–64 znaków zapisuje; bez pola → null
- [x] zła wartość → 400; serwis nie liczy MQC SQL i nie tworzy charge
- [x] RLS: tenant B nie widzi wycen A; zero float / marży
- [x] `mqc_teu` / `revision_no` bez regresji

## Leftover

supersedes_id · spot_or_contract na quotation (park) · matching · Deadline Engine
