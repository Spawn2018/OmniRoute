# Plaster 542.0 · N1 leftover — opcjonalny `stop_id` na przesyłce

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N1 leftover `stop` · [260.0](../archived/260.0-consignment.md)

**Zależy od:** `consignment` · `stop`

**Status:** zamknięty `/noc` — archiwum.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nullable `stop_id` na `consignment` + złożone FK `(organization_id, shipment_id, stop_id)` → `stop` (jak paczka). POST opcjonalnie; API sprawdza stop na tym samym zleceniu.
- **Odrzucone:** required stop (`REJECTED`). Mapa / WMS (`REJECTED`).

**Łowca:** ISTNIEJE `consignment` + `stop`. BRAK `stop_id` na przesyłce.

## Zakres

1. Migracja `423_consignment_stop`.
2. Model + serwis + API + domena.
3. Test HTTP bind / mismatch / omit.
4. How-to `przesylka.md`. `SHIPPED["542.0"]` → `/consignments`.

## Kryteria akceptacji

- [x] POST bez stop_id → 201
- [x] POST stop tej samej trasy → 201
- [x] POST stop obcego zlecenia → 400

## Leftover

auto-link · mapa

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | optional stop_id |
| Szybkość | N/A | |
| SQL | N/A | ALTER + FK |
| N+1 | OK | |
| Dług / człowiek | OK | |
| Proza operatora | 5–15 | przesylka.md |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | |
