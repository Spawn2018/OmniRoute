# Plaster 569.0 · T3 leftover — HITL `container.tare_kg`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [239.0](archived/239.0-container-vgm.md) (VGM obok, nie to samo) · [568.0](archived/568.0-container-mixed-dd-days.md)

**Zależy od:** 153.0 `container` · 239.0 `vgm_kg` (VGM SOLAS — obok, nie tara)

**Status:** Zamknięty `/noc` 2026-09-18. Nie PIN. Nie TEU. Nie kalkulator VGM=tara+ładunek. Nie countdown N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover T3 po V3 mixed D&D. Pogłębienie istniejącej `container`: opcjonalny `tare_kg` (`Numeric(14,4)`, Decimal HITL tara pustego kontenera, > 0, nie float, nie waluta, nie `charge`). POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`tara`). Nie mutuje `vgm_kg` / `vgm_method` / `vgm_cutoff_at`. Nie liczy VGM w Pythonie/JS.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["569.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1. Migracja `449_container_tare` (po `448_container_mixed_dd`).
- **Odrzucone:** kalkulator tara+ładunek=VGM (`REJECTED`). float / `parseFloat` (`REJECTED`). PIN (`REJECTED` → park). TEU (`REJECTED`). countdown N3 / szkic charge (`REJECTED` → park). Import `money` / `groupage_tariff` (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live armator / `payload_kg` / `quantity` na kontenerze.

**Łowca:** BRAK `tare_kg`. ISTNIEJE `vgm_kg` (239.0, VGM SOLAS, inna dana). ISTNIEJE `stop.weight_kg` (247.0, inny BC). ISTNIEJE `groupage_tariff.chargeable_weight` (D5, inny BC). PODOBNE: `require_vgm_kg` — reuse wzorca Decimal, osobny komunikat `tara`.

## Zakres

1. Migracja `449_container_tare`: kolumna nullable Numeric(14,4).
2. Domain `require_tare_kg` + API POST opcjonalny + GET.
3. UI `/shipments` panel kontenera (`tare_kg` jako string). How-to dopisek. `SHIPPED["569.0"]`.

## Poza zakresem

PIN · TEU · kalkulator VGM · `payload_kg` · countdown N3 · szkic charge · AI3 · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `tare_kg="2200.5"` zapisuje Decimal
- [x] float / ≤0 / bool / nie-liczba → 400 (`tara`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero float / live HTTP / mutacja VGM / import money

## Leftover

PIN · `payload_kg` / `quantity` na kontenerze · N3 countdown · szkic charge · live armator · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL tara Decimal |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Numeric |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | wspólne `_require_positive_kg` z VGM |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
