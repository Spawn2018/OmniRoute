# Plaster 154.0 · U3 `air`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § U3 · [karty-pol-fala-u.md](../../analysis/karty-pol-fala-u.md) § U3
**Zależy od:** 111.0 `shipment_leg` · 4.0 `port.function_flags` (`airport`)
**Status:** Zamknięty `/noc` 2026-09-08. Nie live IATA. Nie HAWB.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z U3):** ta sama tabela `shipment_leg`. Migracja `094_shipment_leg_air_kind` rozszerza CHECK o `air`. Operator na `/air` zapisuje odcinek: `shipment_id` + dwie lokalizacje `unlocode` + `source_ref` + `leg_kind=air`. Port podpięty pod lokalizację musi mieć `airport` w `function_flags` (UN/LOCODE pozycja 4, już w 4.0). Jeden odcinek `air` na zlecenie (unikat org+zlecenie+kind). API składa odczyt shipment + location + port; serwis nogi nie importuje geography. `SHIPPED_CHARGE_ROUTES["154.0"]` = `/air`. OpenFGA: `can_manage_shipment_legs`. POST: `leg_kind` opcjonalny, default `road`.

**Łowca:** ISTNIEJE `shipment_leg` road/rail/china_rail/ocean_lcl. ISTNIEJE `function_flags` z tokenem `airport`. BRAK `leg_kind=air`. BRAK `/air`. BRAK HAWB/MAWB na nodze.

**Odrzucone:**

- HAWB/MAWB / pule M-03 (leftover U3b).
- e-rates / `channel_quote` IATA (leftover).
- live HTTP IATA / booking.
- druga tabela lotu / AWB.
- kwota / marża / mapa.

## Zakres

1. Migracja `094_shipment_leg_air_kind`: CHECK `air`.
2. POST `/shipment-legs` `air`. Końce: `unlocode` + `airport` w flags. Inny kind / strefa → 400.
3. UI `/air`. `just api-types`. How-to wtopione (katalogi ≤ 40).

## Poza zakresem

HAWB · MAWB · IATA live · pule · mapa · kwota

## Kryteria akceptacji

- [x] CHECK dopuszcza `air`; poprzednie kind działają
- [x] Port bez `airport` albo `postal_zone` jako koniec → 400
- [x] `/air` zapisuje i listuje odcinki `air`
- [x] Zero float / marży / IATA HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CHECK `air`, HTTP 400 bez `airport`, zapis na `/air` |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | N/A | CHECK; bez nowego SELECT poza listą nóg |
| N+1 | OK | air = get zlecenie + dwie lokalizacje + dwa porty + INSERT |
| Dług / człowiek | OK | panel `fieldset` nie kopiuje `/lcl`; leftover HAWB w docs-debt |
| Proza operatora | 5–15 zdań | `lot.md`; `katalogi.md` wtopione, 40 linii |
| Docs/OS | PRZESZŁO | CURRENT, PLAN U3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
