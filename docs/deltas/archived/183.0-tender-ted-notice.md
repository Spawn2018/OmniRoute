# Plaster 183.0 · G2.13 `tender_ted_notice`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.13 · leftover TED capability
**Zależy od:** 169.0 `tender` (nagłówek już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.13–G2.18):** jedna tabela `tender_ted_notice` per tenant — **HITL numer ogłoszenia TED z `source_ref`**, nie klient HTTP do TED.europa.eu i nie parser HTML. Wiersz = `tender_id` (FK tenanta do `tender`) + `notice_number` (tekst wpisany przez operatora) + `source_ref` (`tenant:manual` albo `fixture://tender-ted-notice/…`). Operator zapisuje, że przetarg ma numer ogłoszenia TED; serwis nie woła sieci, nie woła LLM, nie mutuje `tender.status` i nie czyta `charge`. Zły numer → 400 (`ogłoszenie`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id)` — jeden numer TED na przetarg. GET listy SQL. API składa odczyt nagłówka (`TenderService.get_board`). UI `/tender-ted-notices` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["183.0"]` = `/tender-ted-notices`. OpenFGA: `can_manage_tender_ted_notices`. Migracja `123_tender_ted_notice`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tender.kind` (`open` | `restricted` | …) — tryb nagłówka, **nie** numer TED. ISTNIEJE `tender_rfp_intake` — przyjęcie RFP HITL, nie ogłoszenie TED. BRAK `tender_ted_notice`. BRAK `/tender-ted-notices`. BRAK klienta HTTP TED.

**Odrzucone:**

- CO₂ / CBAM (leftover G2.14–G2.18).
- Scrape TED.europa.eu / live HTTP / parser HTML / auto-uzupełnianie z sieci.
- Auto-award / UPDATE `tender.status` / INSERT `tender_win_loss`.
- Kwota / druga marża / scoring.
- Float / T-SQL.

## Zakres

1. Migracja `123_tender_ted_notice`: RLS, izolacja, indeks `(organization_id, notice_number)`, unique `(organization_id, tender_id)`.
2. POST/GET. Zły `notice_number` / `source_ref` → 400. API składa odczyt `tender`, serwis nie importuje tenders / charges / extraction i nie woła HTTP.
3. UI `/tender-ted-notices`. `just api-types`. How-to `docs/operator/ogloszenie-ted.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `TedDesk` / `tedWrite` / `persistTedMark`.

## Poza zakresem

G2.14–G2.18 CO₂ · G2.19–G2.22 · G2.23 · auto-award · scrape TED · LLM · kwota na wierszu · mutacja `tender.status` · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `tender_ted_notice` z numerem ogłoszenia i `source_ref` na istniejącym `tender`, bez scrape
- [x] zły `notice_number` → 400
- [x] GET zwraca ogłoszenia tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP / scrape / UPDATE `tender.status`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST numer TED + `source_ref`; 400 `ogłoszenie` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `(organization_id, notice_number)` — `ix_tender_ted_notice_org_notice` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover G2.14–G2.18 CO₂; jscpd total 2.98% |
| Proza operatora | 5–15 zdań | `ogloszenie-ted.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.13, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
