# Plaster 242.0 · T3 `carrier_party_id` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [241.0](../archived/241.0-container-booking-no.md) leftover `carrier_party_id` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 241.0 `booking_no`
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie PIN. Nie S21.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `carrier_party_id`/PIN/`shipment_leg_id` + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover `shipment_leg_id` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `carrier_party_id` (UUID HITL armatora). Złożone FK `(organization_id, carrier_party_id)` → `party` jak `pallet_balance` / `shipment_id` na tym samym wierszu. POST bez pola zostaje `null`. Nieznany UUID → 404. `require_carrier_party_id` jak `require_container_shipment_id`. API składa `PartyService.get_party` (wzorzec I4/`pallet_balance`) — **serwis** nie importuje `parties`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["242.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `181_container_carrier_party` (po `180_container_booking_no`). `_WriteBox` — dodać `carrier`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.shipment_id` (złożone FK + bind w API). ISTNIEJE `pallet_balance.party_id` (złożone FK + `PartyService` w API). BRAK `container.carrier_party_id`. PODOBNE: bind w warstwie API. Nie import `app.services.parties` w BC container. Nie live HTTP.

**Odrzucone:**

- Import `parties` w `ContainerService`.
- PIN / `shipment_leg_id` / S21 / live HTTP.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `181_container_carrier_party`: `carrier_party_id` nullable UUID + FK `(organization_id, carrier_party_id)` → `party`.
2. POST opcjonalny. Nieznany → 404. Serwis nie importuje parties/charges.
3. UI `/shipments`. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · `shipment_leg_id` · S21 · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown

## Kryteria akceptacji

- [x] POST z UUID istniejącego `party` zapisuje FK, bez live HTTP i bez PIN
- [x] POST bez pola zostaje `null`
- [x] nieznany UUID → 404
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / PIN / live HTTP / scrape / import `parties` w serwisie

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `carrier_party_id`; 404 nieznany; omit → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable UUID + złożone FK; brak live HTTP |
| N+1 | OK | GET jedna lista; POST = bind + INSERT |
| Dług / człowiek | OK | leftover T3 `shipment_leg_id`; PIN parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
