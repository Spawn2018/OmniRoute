# Plaster 4.0 · M-05 Geografia (`port`)

**Spec źródłowa:** [docs/spec/geography.md](../../spec/geography.md), sekcja 4.0  
**Zależy od:** 0.3 RLS · 0.4 OpenFGA · 0.6 DataTableShell · 1.0 `charge_code` (wzorzec katalogu)

**Status:** Zamknięty 2026-09-01. Kod na origin.

## Zakres

Katalog `port` per tenant: ingest z `cristan/improved-un-locodes` (pin SHA przy implementacji), aliasy na wierszu, `resolve` tokenu (DoD: `Gdingen` → `PLGDY`), UI `/ports`. `organization_id` obowiązkowy — archiwalny PK-only / NULL odrzucony.

## Poza zakresem

`location`, `location_zone_member`, `terminal`, World Port Index, OSM, ISPS, podpięcie `rate_line` / `quotation`, Q2 M-10, live fetch GitHub na endpoint, druga tabela aliasów, RapidFuzz / pgvector, `container_type`, Auth0, M-02, Exit Wave FE.

## Ustalenia

- Żywy ID modułu: **M-05** (brak kolizji z żywym M-07/M-08). Archiwalne 1.1/1.2 nie używać.
- Całe M-05 = **4.0 → 4.1 → 4.2**, potem Q2. Nie skakać do M-10 po samym 4.0.
- Źródło seed: `cristan/improved-un-locodes` (współrzędne + aliasy); `datasets/un-locode` = rodowód UNECE w `source_ref`. Pełny UN/LOCODE, nie filtr samych morskich; `is_seaport` z danych.
- Ingest poza requestem HTTP: idempotentny upsert per `(organization_id, unlocode)`. CI = fixture (kilka wierszy + alias Gdingen), bez sieci, bez 670k SQL w git.
- OpenFGA: `can_manage_geography` = `member` (jedna relacja na 4.0–4.2).
- RLS FORCE + test izolacji dwóch orgów. Runtime `omniroute_app` NOBYPASSRLS.
- Aliasy na wierszu (`text[]`), nie osobna tabela. Szukanie w Postgres (`pg_trgm` / GIN OK).
- Port nieoficjalny: `is_official = false`, `source_ref = tenant:manual`.
- `lat`/`lng`: Numeric, nie float.
- Job operatora w tym plastrze: `/ports` (DataTableShell): lista, szukaj, dodaj nieoficjalny, resolve.
- `pg_trgm` **poza 4.0**: DoD `resolve` to dokładne dopasowanie po znormalizowanym aliasie, wystarczy GIN na `aliases`. Rozszerzenie dokładamy dopiero przy realnym wyszukiwaniu przybliżonym.
- Alias wskazujący dwa porty tego samego tenanta: pierwszeństwo ma trafienie w `unlocode`, potem wiersz oficjalny; dalsza niejednoznaczność = błąd domenowy `AmbiguousPortToken`, nigdy pierwszy z brzegu.
- `function_flags` jako `text[]` z nazwanymi wartościami (`port`, `rail`, `airport`, `icd`) — dekodowane przy ingest, nie surowy ośmioznakowy ciąg UN/LOCODE.

## Kryteria akceptacji

- [x] Tabela `port` + RLS FORCE + test izolacji tenantów — `012_port_rls`, `test_port_isolation.py`
- [x] Unikalność `(organization_id, unlocode)` — `uq_port_org_unlocode`, dwa tenanty trzymają ten sam kod
- [x] `resolve("Gdingen")` → `PLGDY` na fixture (bez sieci) — `test_port_resolve.py`
- [x] OpenFGA `can_manage_geography`; endpoint z `require_permission`; brak deklaracji = deny
- [x] UI `/ports` (DataTableShell): lista + dodanie nieoficjalnego + resolve
- [x] Ingest idempotentny; `source_ref` z pinem źródła albo `tenant:manual`
- [x] Brak `organization_id` NULL; brak live GitHub w requestcie — test skanuje `app/api/ports.py`

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | 307 testów backendu (45 integracyjnych na żywym PG 16) + 41 vitest; `resolve` i izolacja RLS dowiedzione na bazie, nie na stubie |
| Szybkość | N/A ten plaster | katalog portów nie leży na żadnej ścieżce z budżetu AGENTS.md; initial JS 125,7 kB gzip < 250 kB |
| Dług w diffie | OK | `just complexity` czysto; `just dup` 3,97% → 2,87% po wyodrębnieniu `components/catalog/catalog-parts.tsx` (próg 3%, baseline sprzed plastra 2,93%) |
| Docs/OS | PRZESZŁO | CURRENT → 4.1 Plan, PROGRESS, `just docs` |

Środowisko lokalne: instalator EDB odrzuca argumenty winget (kod 1, bez własnego logu), więc PG 16 stoi jako klaster przenośny w `tools/pg16` + `tools/pgdata` (gitignore). `scripts/dev-native.ps1` nadal zakłada instalację systemową — rozjazd zapisany w `docs/ops/docs-debt.md`.
