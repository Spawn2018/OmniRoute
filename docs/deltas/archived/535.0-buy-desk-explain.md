# Plaster 535.0 · leftover O7/O8 — EXPLAIN JOIN buy-desk

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O7/O8 leftover EXPLAIN · [532.0](../archived/532.0-agent-country-filter.md) · [533.0](../archived/533.0-buy-desk-groupby.md) · wzorzec [test_trip_isolation](../../backend/tests/trips/test_trip_isolation.py)

**Zależy od:** 532.0 filtr `country_code` · 533.0 `list_desk` JOIN `network_member` × `party` · indeksy `ix_carrier_inquiry_org_created` / `ix_carrier_inquiry_org_member` / `ix_party_organization_id`

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover SQL z O7/O8. Dwa testy **integration** z `EXPLAIN` + `enable_seqscan=off`: (1) lista `carrier_inquiry` po `organization_id` (ścieżka desk / `ix_carrier_inquiry_org_created`); (2) JOIN `network_member` × `party` z filtrem `party.country_code` (ścieżka O7). Katalog `pg_indexes` jako źródło prawdy przy pustej tabeli (jak trip).
- **Wybrane:** zero nowej migracji, dopóki plan pokazuje Index Scan / nazwę indeksu. Dokument: krótki wpis w [quote-budget.md](../../ops/quote-budget.md) albo fold w how-to sieci — N/A pełnego p95 (brak 50k wierszy).
- **Odrzucone:** nowy indeks „na zapas” bez Seq Scan w EXPLAIN (`REJECTED`).
- **Odrzucone:** rfc822 · live HTTP · zmiana API/UI (`REJECTED` — osobne leftovery).
- **Odrzucone:** k6 / seed 50k (`REJECTED` — anti-cel PLAN).

**Łowca:** ISTNIEJE `list_desk` OUTER JOIN (`carrier_inquiry_repository.py`). ISTNIEJE filtr country JOIN (`network_member_repository.py`). ISTNIEJE wzorzec EXPLAIN w `test_trip_isolation`. BRAK EXPLAIN na buy-desk JOIN. PODOBNE: kopiuj asercję Index Scan, nie nowa tabela.

## Zakres

1. Testy integration EXPLAIN desk + members×country.
2. Notatka ops (EXPLAIN przy pustej tabeli = N/A budżet AGENTS).
3. PLAN/CURRENT: zdejmij leftover EXPLAIN O7/O8; zostaje rfc822.

## Poza zakresem

rfc822 · nowy indeks bez Seq Scan · seed 50k · zmiana list_desk SQL · UI

## Kryteria akceptacji

- [x] EXPLAIN listy inquiry: Index Scan / `ix_carrier_inquiry_org_*` (albo katalog indeksu + Index Scan)
- [x] EXPLAIN JOIN members×party z `country_code`: Index Scan / indeks party/member
- [x] Zero migracji bez dowodu Seq Scan
- [x] Zero float / marży / live HTTP

## Leftover

rfc822 / in_reply_to · seed 50k p95 desk

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | dwa EXPLAIN integration zielone |
| Szybkość | N/A ten plaster | pusty desk ≠ budżet 50k AGENTS |
| SQL | EXPLAIN | `ix_carrier_inquiry_org_created` + JOIN party; Index Scan |
| N+1 | OK | brak nowych zapytań w pętli |
| Dług / człowiek | OK | wzorzec trip isolation |
| Proza operatora | N/A | brak nowego jobu zapisu; notatka quote-budget |
| Docs/OS | PRZESZŁO | CURRENT · PLAN · quote-budget |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
