# 0.24 — pip-audit, pin SHA, /ready, request-id

**Status:** archived 2026-09-01  
**Moduł:** ops  
**Spec:** PROGRAM-12M Charge 0.24

## Zakres

- `/ready` publiczne: 200 gdy `probe_database` (SELECT 1, timeout 2 s), 503 `not_ready` gdy PG nie odpowiada
- `X-Request-ID`: UUID z klienta albo nowy; nie-UUID odrzucane
- Actions w `gate.yml` / `pr-nudge.yml` przypięte do SHA (nie `@v4`)
- `just audit` = `python -m pip_audit --skip-editable .` (nie echo); CI woła po `just gate`

## Poza zakresem

Auth0 I1/I2. k6 / vulture nadal echo. Image Dockera bez digestu. OpenFGA nie w `/ready`. `just audit` nie w lokalnym `just gate` (~80 s + PyPI).

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | /ready 200/503; request-id; SHA pin; audit nie echo |
| Szybkość | N/A ten plaster | ops / liveness |
| Dług w diffie | OK | C901 czyste; jscpd 2.23%; audit poza local gate |
| Docs/OS | PRZESZŁO | CURRENT = 2.1–2.2; Auth0 nadal odroczone |
