# Plaster 255.0 · T1 EXP1 `pod_quality` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [254.0](254.0-stop-waiting-started.md) leftover POD · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 254.0 `waiting_started_at`
**Status:** Zamknięty `/noc` 2026-09-10. Nie kamera. Nie bajty. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 POD + leftover T1b + leftover `appointment_status`/`no_show_at` + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b parked. leftover `appointment_status` / `no_show_at` parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `pod_quality` (`String(12)` HITL, allowlista `ok` / `retake` / `missing`, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`pod`). `require_stop_pod_quality` w `app.domain.stop` — wzór `stop_kind` (zbioru), nie import `shipment_documents`. `_HitlTail` dostaje jedno pole; `record_stop` zostaje ≤ 40. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["255.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `191_stop_pod_quality` (po `190_stop_waiting_started`). CHECK `ck_stop_pod_quality`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.waiting_started_at` (254.0). ISTNIEJE `stop.stop_kind` (allowlista). ISTNIEJE `shipment_document` (M-38, inny BC). BRAK `stop.pod_quality`. BRAK tabeli `stop_group`.

**Odrzucone:**

- Kamera / bajty zdjęcia / OCR / skan M-20.
- `appointment_status` / `no_show_at` / T1b tabela / km / D3 / T8 / PIN / import `shipment_documents` / mapa / T5 / S53 / live HTTP / float.

## Zakres

1. Migracja `191_stop_pod_quality`: `pod_quality` nullable String(12) + CHECK allowlista. Nie `create_table`.
2. POST opcjonalny `pod_quality`. Zły token → 400 (`pod`). Serwis nie importuje shipment_documents/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

kamera · bajty · tabela `stop_group` · `appointment_status` · D3 · T8 · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `pod_quality=ok|retake|missing` zapisuje jakość POD HITL, bez kamery
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / nieznany token → 400 (`pod`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / import `shipment_documents`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `pod_quality`; 400 `pod`; pusty → null; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | String + CHECK allowlista; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | wzór `stop_kind`; leftover T1b; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
