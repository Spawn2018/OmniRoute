# Plaster 560.0 · C8 leftover — HITL `blocks_create_enforcement_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C8 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C8 `create_block_mark` (obok, nie to samo)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C8 leftover. Nowa tabela `blocks_create_enforcement_mark`: `mark_code` (snake 2–32) + `enforcement_kind` `block_409|warn_only|record_only|other` + `source_ref`. Append-only. RLS. Katalog trybu egzekucji bramy create — nie live 409 na `POST shipment`.
- **Wybrane:** POST/GET `/api/v1/blocks-create-enforcement-marks`. OpenFGA `can_manage_blocks_create_enforcement_marks`. UI `/blocks-create-enforcement-marks`. `SHIPPED["560.0"]`.
- **Odrzucone:** live HTTP 409 na `POST shipment` / wiring `blocks_create` (`REJECTED` → leftover). Mutacja `create_block_mark` (`REJECTED`). Kwota / float / scoring osoby (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / ranking SQL / PUESC / FK filing.

**Łowca:** BRAK `blocks_create_enforcement_mark`. ISTNIEJE `create_block_mark` (stancja bramy, nie tryb egzekucji). ISTNIEJE `routing_guide_enforcement` (CT4 przewodnik, nie C8 create). ISTNIEJE `relation_document_requirement`.

## Zakres

1. Migracja `440_blocks_create_enf_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["560.0"]` → `/blocks-create-enforcement-marks`.

## Poza zakresem

409 shipment · live blocks_create · wiring create_block_mark · PUESC · AI3 · G0 · kwota

## Kryteria akceptacji

- [x] POST zły kind / obce source_ref / pusty kod → 400
- [x] izolacja tenantów
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / 409

## Leftover

live 409 shipment · wiring blocks_create · scoring
