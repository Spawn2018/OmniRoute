# Plaster 121.0 · leftover S11 `changed` (M-71)

**Spec źródłowa:** [docs/spec/operator-decision.md](../../spec/operator-decision.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S11  
**Zależy od:** 74.0 `operator_decision` · 77.0 `lock_version` · CHECK już ma `changed`

**Status:** Zamknięty `/noc` 2026-09-04. Ten sam żywy ID **M-71**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** POST decide przyjmuje trzeci werdykt `changed` przy tym samym `lock_version` (pending → zapis). Przycisk **Zmień** na `/decisions` (i pending na wieży / wycenie, ten sam POST). Zero nowej tabeli. Zero migracji — CHECK już dopuszcza `changed`. `SHIPPED_CHARGE_ROUTES["121.0"]` = `/decisions`. OPS_JOBS 56 → 57.

**Łowca:** ISTNIEJE CHECK `pending|accepted|changed|rejected`. ISTNIEJE lock 77.0. BRAK: allowlista domeny (dziś `require_decide_status` odrzuca `changed`) i przycisk. PODOBNE: Akceptuj / Odrzuć.

**Odrzucone:**

- Nadpisanie już zapisanego werdyktu (zostaje: tylko z `pending`).
- LLM / extract HITL jako `changed`.
- Send / Graph / nowa tabela / FK.
- Auth0 · portale · OTel · katalog 71–212 · UN na RFQ.

## Zakres

1. Domena + HTTP: `changed` jest legalnym decide; test 77.0 „odrzuca changed” → teraz przyjmuje.
2. UI: przycisk Zmień z `lock_version`. How-to: wtopić w bullet `/decisions` w `katalogi.md` (bez nowej linii).
3. `SHIPPED_CHARGE_ROUTES["121.0"]` = `/decisions`. OPS_JOBS = 57.

## Poza zakresem

UN na RFQ · send · accept extractu · F9.1

## Ustalenia

- Token zostaje `operator_decision`. HITL extract bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży.
- 120.0 karta nadal pokazuje tylko przyjęte / odrzucone oferty — `changed` nie jest wygraną.

## Schemat

Bez nowej tabeli. Bez migracji.

## Kryteria akceptacji

- [x] Decide `changed` z lock przechodzi; drugi decide = konflikt
- [x] UI Zmień na `/decisions`; brak openai / parseFloat / acceptExtraction
- [x] Zero nowej migracji
- [x] OPS_JOBS = 57; `121.0` → `/decisions`
- [x] `katalogi.md` ≤ 40

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | decide `changed` + przycisk Zmień; lock bez zmian |
| Szybkość | N/A ten plaster | jeden UPDATE claim_pending |
| SQL | N/A | bez nowego SELECT/INSERT; ten sam claim |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | `DecideStatusButtons` zamiast trzeciej kopii przycisków |
| Proza operatora | N/A ten plaster | job zapisu już miał how-to; wtopione `Zmień` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
