# Plaster 178.0 · G2.9 `tender_rfp_intake`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.9 · leftover extract RFP HITL
**Zależy od:** 169.0 `tender` · 0.8 `extraction_draft` (szkic zostaje na `/ai`; ten plaster nie woła LLM)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.9–G2.18):** jedna tabela `tender_rfp_intake` per tenant — **HITL przyjęcie RFP z `source_ref`**, nie auto-award i nie zapis z LLM. Wiersz = `tender_id` (FK tenanta do `tender`) + `intake_code` (snake 2–32) + `source_ref` (`tenant:manual` albo `fixture://tender-rfp-intake/…`). Operator wpisuje przyjęcie po przeczytaniu RFP; serwis nie woła LLM, nie czyta `charge` i nie importuje extraction. Zły kod → 400 (`przyjęcie`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id, intake_code)`. GET listy SQL. API składa odczyt nagłówka (`TenderService.get_board`). UI `/tender-rfp-intakes` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["178.0"]` = `/tender-rfp-intakes`. OpenFGA: `can_manage_tender_rfp_intakes`. Migracja `118_tender_rfp_intake`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `extraction_draft.draft_kind` (`rate_line` | `carrier_quote`) — HITL stawek/ofert, **nie** RFP. ISTNIEJE `tender_playbook` — teza operatora, nie przyjęcie RFP. ISTNIEJE `tender` — nagłówek; **nie** dokładamy RFP jako JSON tam. BRAK `tender_rfp_intake`. BRAK `/tender-rfp-intakes`. BRAK `draft_kind=tender_rfp` (leftover).

**Odrzucone:**

- `draft_kind=tender_rfp` + gałąź `AcceptExtractionToRates` (leftover G2.9b / 179.0; nic z LLM do bazy bez akceptu).
- Prospecting HITL / bid/no-bid silnik / award four-eyes / TED / CO₂ (leftover G2.10–G2.18).
- Auto-award / scrape TED / kwota / druga marża / UPDATE `tender.status`.
- Float / T-SQL / live HTTP.

## Zakres

1. Migracja `118_tender_rfp_intake`: RLS, izolacja, indeks `(organization_id, tender_id)`, unique `(organization_id, tender_id, intake_code)`, CHECK `intake_code`.
2. POST/GET. Zły `intake_code` / `source_ref` → 400. API składa odczyt `tender`, serwis nie importuje tenders / extraction / charges.
3. UI `/tender-rfp-intakes`. `just api-types`. How-to `docs/operator/przyjecie-rfp.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `IntakeDesk` / `intakeWrite` / `persistIntakeMark`.

## Poza zakresem

G2.9b `draft_kind=tender_rfp` · G2.10–G2.18 prospecting/four-eyes/TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · LLM zapis · kwota na wierszu · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `tender_rfp_intake` z kodem i `source_ref` na istniejącym `tender`, bez auto-award
- [x] zły `intake_code` → 400
- [x] GET zwraca przyjęcia tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST kod + `source_ref`; 400 `przyjęcie` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `(organization_id, tender_id)` — `ix_tender_rfp_intake_org_tender` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover G2.9b–G2.18; jscpd 0 na plikach plastra |
| Proza operatora | 5–15 zdań | `przyjecie-rfp.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.9, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
