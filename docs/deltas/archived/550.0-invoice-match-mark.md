# Plaster 550.0 · F10 leftover — HITL `invoice_match_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala F · F10 leftover po 549.0

**Zależy od:** tenancy · `purchase_invoice` (549.0) · wzorzec HITL mark (`freight_audit_mark`)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover F10. Nowa tabela `invoice_match_mark`: `mark_code` (snake 2–32) + `match_kind` `candidate|rank|allocate|other` + `source_ref`. Append-only. RLS. Katalog stancji dopasowania FV zakupu — nie ranking SQL i nie kandydata z score.
- **Wybrane:** POST/GET `/api/v1/invoice-match-marks`. OpenFGA `can_manage_invoice_match_marks`. UI `/invoice-match-marks`. `SHIPPED["550.0"]`.
- **Odrzucone:** tabela `invoice_match_candidate` / ranking SQL / score (`REJECTED` teraz → leftover). `purchase_invoice_allocation` (`REJECTED` → leftover). Auto-link do `charge` / `purchase_invoice` / `sales_invoice` (`REJECTED`). Kwota / marża / float (`REJECTED`). Live ERP / KSeF (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13.

**Łowca:** BRAK `invoice_match_mark` / `invoice_match_candidate` w models. ISTNIEJE `purchase_invoice` (ingest HITL). ISTNIEJE `freight_audit_mark` (rodzaj audytu, nie match FV). ISTNIEJE `quote_invoice_settlement` (wiązanie wyceny↔FV sprzedaży). ISTNIEJE `party_scorecard.quote_invoice_match_rate` (metryka karty, nie katalog match).

## Zakres

1. Migracja `430_invoice_match_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["550.0"]` → `/invoice-match-marks`.

## Poza zakresem

`invoice_match_candidate` · ranking SQL · allocation · auto-link · kwota · live ERP · AI3 · G0

## Kryteria akceptacji

- [x] POST zły kind / obce source_ref / pusty kod → 400
- [x] izolacja tenantów
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / FK purchase_invoice

## Leftover

`invoice_match_candidate` · ranking SQL · `purchase_invoice_allocation` · auto-link · kwota · live ERP
