# Plaster 465.0 · BR3.3 tacho jako ograniczenie planu

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR3.3 · EXP4.10 `tacho_office_mark`

**Zależy od:** tenancy RLS · wzorzec HITL `load_order_mark` (464.0) · `tacho_office_mark` już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `tacho_plan_marks`. Tabela `tacho_plan_mark`: `mark_code` + `constraint_kind` `plan|window|rest|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance ograniczenia planu, nie biuro tachografu i nie godziny SQL.
- **Wybrane:** POST/GET `/api/v1/tacho-plan-marks`. OpenFGA `can_manage_tacho_plan_marks` (member). UI `/tacho-plan-marks`. `SHIPPED["465.0"]` → `/tacho-plan-marks`.
- **Wybrane:** Alembic `370_tacho_plan_mark` (`down_revision = "369_load_order_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `tacho_office_mark` (`REJECTED` — EXP4.10 zostaje office/card/ddd, nie ograniczenie planu).
- **Odrzucone:** klej FK do `trip` / `resource` / `route_plan_mark` (`REJECTED` na tym wierszu).
- **Odrzucone:** live tacho · parse DDD · Driver Time Solver · godziny SQL · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `tacho_office_mark` (EXP4.10 office/card/ddd). ISTNIEJE `ferry_art9_mark` (odpoczynek promu). ISTNIEJE `route_plan_mark` (BR3.0 trasa). PODOBNE katalog HITL. BRAK `tacho_plan_mark`. BRAK tacho jako constraint planu.

## Zakres

How-to: jedno zdanie — to katalog ograniczenia tacho w planie; live DDD i solver godzin zostają leftover. +1 OPS_JOB na nową listę. Klient FE od razu rozróżniony (jak CSRD / tabela, nie klon `asJson`) pod jscpd < 3%.

## Poza zakresem

live tacho · DDD parse · Driver Time Solver · FK do trip · Valhalla · AI5

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live DDD · Driver Time Solver · godziny SQL · FK do `trip` / `resource` / `route_plan_mark` · BR4.0 promy

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS dwa tenanty; amount/hours 422/400 |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | INSERT/SELECT katalogu |
| N+1 | OK | jeden SELECT listy |
| Dług / człowiek | OK | osobny BC obok `tacho_office_mark`; bez FK |
| Proza operatora | 5–15 zdań | `docs/operator/tacho-plan-mark.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–464 |
| Minuty plastra | 55 | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
