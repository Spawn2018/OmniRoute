# Plaster 273.0 · CI9 `customer_contract` HITL (opaque blob, nie szyfr)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CI / CI9 · [272.0](272.0-customer-contract.md) · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md) (tabele — nie cała fala) · [GLOSSARY.md](../../GLOSSARY.md) umowa klienta
**Zależy od:** 0.3 tenancy · 272.0 `customer_contract` nagłówek
**Status:** Zamknięty `/noc` 2026-09-10. 272.0 = nagłówek; 273.0 = opaque blob present/absent, nie szyfr. Nie KMS. Nie unwrap. Nie KEK.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CI9 ciphertext/KEK / pin 2026-09-08c: obecność opaque BYTEA jako dana HITL; nie zero-knowledge i nie live p44):** leftover `tenant_contract_kek` / `wrapped_dek` / KMS / unwrap parked (zakaz Omni-master, zakaz Fernet/AES „na teraz”, zakaz tabeli sekretów). leftover super-admin plaintext parked (nie ma decode — GET nie zwraca bajtów). leftover CI1 `sla_clause` / extract LLM parked (CI9 przed CI1). leftover CT7 p44 / FourKites / Shippeo parked live. leftover Langfuse na PDF parked. Żywy plaster: **kolumna** nullable `blob_ciphertext` `BYTEA` na istniejącej `customer_contract` — HITL opaque fixture bytes, nie szyfrowanie. Operator może POSTować bajty fixture (`source_ref` nadal wymagane). GET lista/szczegół **nigdy** nie zwraca bajtów — tylko `has_ciphertext` (bool). Zero kolumn `wrapped_dek` / `tenant_contract_kek` / `body` / `plaintext`. Zero funkcji decrypt. GLOSSARY zostaje **umowa klienta** = `customer_contract` (jedna tabela). Migracja `206_customer_contract_blob` po `205_customer_contract`. RLS FORCE już jest — nie ruszać polityki. Unique kod + `source_ref` bez zmian. POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zła etykieta → 400 (`załadowca` / `odbiorca`). Obce `source_ref` → 400 (`obce`). Zły opaque blob → 400 (`opakowanie`). Body z `unwrap` / `wrapped_dek` / `plaintext` / `body` / `kek` / `decrypt` / polem JSON `blob_ciphertext` → 422 (`extra=forbid`). UI `/customer-contracts` — checkbox fixture, nigdy hex z powrotem. `SHIPPED_CHARGE_ROUTES["273.0"]` = `/customer-contracts`. OpenFGA bez nowej relacji. ExtractionService nie importuje tego BC. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `customer_contract` 272.0 — **nie** druga tabela umowy. BRAK kolumny `blob_ciphertext` przed tym plasterem. BRAK `has_ciphertext` w JSON. BRAK `tenant_contract_kek` / `wrapped_dek` / decrypt. ISTNIEJE `party_document` 188.0 — kind na `party`, nie BYTEA umowy. ISTNIEJE `tender_data_room` — NDA przetargu, nie bajty. ISTNIEJE `shipment_document` / `document_template` — wskazanie / layout, nie BYTEA. ISTNIEJE `idp_connector` / `erp_connector` / `exchange_connector` — zakaz sekretów ciphertext, **inny** job. PODOBNE: leftover-kolumna jak 214.0 na istniejącej tabeli. Nie wyodrębniać wspólnej warstwy.

**Odrzucone:**

- KMS / unwrap API / Fernet / AES w Pythonie / klucz Omni-master.
- tabela `tenant_contract_kek` / kolumna `wrapped_dek` / materiał klucza.
- zwrot `blob_ciphertext` albo plaintext do przeglądarki („żeby operator skopiował”).
- Langfuse na PDF / CI1 extract LLM / AI na umowach.
- CT7 live HTTP p44 / FourKites / Shippeo.
- `sla_clause` / kara / `spend_leakage` / druga tabela umowy.
- super-admin decode / impersonate=decrypt.
- import charges/parties/extraction; kwota / marża / float / HTTP.

## Zakres

1. Migracja `206_customer_contract_blob` po `205_customer_contract`: nullable `blob_ciphertext` BYTEA. RLS FORCE zostaje. Izolacja: tenant A nie czyta BYTEA tenanta B.
2. POST: opcjonalny `opaque_fixture` (bool) albo `opaque_blob` (base64 fixture, 1–4096 B). `source_ref` wymagane. GET: `has_ciphertext`, nigdy bajty / `blob_ciphertext` / plaintext. Zły token → 400 (`opakowanie` + istniejące). Extra unwrap → 422. Brak funkcji decrypt. Brak UPDATE/DELETE. ExtractionService nie importuje BC.
3. UI `/customer-contracts`: checkbox fixture, kolumna Tak/Nie. How-to `docs/operator/umowa-klienta.md`. Fold w `katalogi.md`. Bez nowej relacji OpenFGA.

## Poza zakresem

KMS · unwrap · KEK · `wrapped_dek` · Omni-master · Fernet/AES · Langfuse PDF · CI1 extract · `sla_clause` · CT7 live · super-admin plaintext · kwota · float

## Kryteria akceptacji

- [x] POST z fixture bytes zapisuje BYTEA; POST bez pola zostawia `NULL`
- [x] GET JSON ma `has_ciphertext` i **nie** ma `blob_ciphertext` / plaintext / hex
- [x] extra pole `unwrap` (oraz `wrapped_dek` / `plaintext` / `body` / `kek` / `decrypt` / JSON `blob_ciphertext`) → 422
- [x] zły opaque blob → 400 (`opakowanie`); istniejące 400 zostają
- [x] test izolacji: tenant A nie widzi wiersza ani BYTEA tenanta B
- [x] ExtractionService / `extraction-no-rates` nie importuje `customer_contracts`
- [x] Zero funkcji decrypt / KEK / KMS / float / marży / live HTTP / importu charges/parties/extraction

## Ustalenia

- 272.0 = nagłówek; 273.0 = opaque blob present/absent, nie szyfr.
- Nie edytować migracji `205_customer_contract`.
- UI nie renderuje bajtów z powrotem (checkbox + token fixture, nie hex).

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST fixture bytes → BYTEA; GET `has_ciphertext`; 422 unwrap; 400 `opakowanie`; RLS nie wycieka BYTEA |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); izolacja SELECT BYTEA pod RLS |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | kolumna na istniejącej tabeli; leftover KEK mark / CT7 live; C901 czysty |
| Proza operatora | 5–15 zdań | `umowa-klienta.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN CI9, GLOSSARY, MODULES, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (273.0)

- Migracja `206_customer_contract_blob` po `205_customer_contract`. RLS FORCE zostaje.
- `resolve_opaque_blob` — fixture flag albo base64; brak decrypt.
- UI `/customer-contracts` checkbox + kolumna Tak/Nie. `SHIPPED_CHARGE_ROUTES["273.0"]` = `/customer-contracts`.

## Leftover po 273.0

CI9 KEK mark (bez materiału klucza; park KMS/unwrap; zakaz Omni-master). `wrapped_dek` / `tenant_contract_kek`. CI1 `sla_clause`. CT7 p44/FourKites/Shippeo parked live (TO_VERIFY umowa). Reszta pinu 2026-09-08c.
