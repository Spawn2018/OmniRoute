# Plaster 548.0 · N14 — HITL `self_billing_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N14 · po F9+D

**Zależy od:** tenancy · wzorzec HITL mark (G16 `billing_mark` = SaaS, nie ten BC)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy N14. Nowa tabela `self_billing_mark`: `mark_code` + `billing_kind` `self|subcontractor|other` + `source_ref`. Append-only. RLS. Osobny od `billing_mark` (SaaS Omni).
- **Wybrane:** POST/GET `/api/v1/self-billing-marks`. OpenFGA `can_manage_self_billing_marks`. UI `/self-billing-marks`. `SHIPPED["548.0"]`.
- **Odrzucone:** live self-billing HTTP / JPK / auto FV (`REJECTED`). Kwota / marża (`REJECTED`). FK do `party` / `sales_invoice` (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13.

**Łowca:** ISTNIEJE `billing_mark` (SaaS seat/usage). BRAK znacznika self-billing podwykonawcy.

## Zakres

1. Migracja `428_self_billing_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["548.0"]` → `/self-billing-marks`.

## Poza zakresem

live self-billing · JPK · auto FV · kwota · FK party · AI3 · G0

## Kryteria akceptacji

- [x] POST zły kind / obce source_ref → 400
- [x] izolacja tenantów
- [x] UI + SHIPPED
- [x] zero float / live HTTP

## Leftover

live self-billing · FK party · JPK
