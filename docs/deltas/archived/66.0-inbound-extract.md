# Plaster 66.0 · S3 treść inbound_message → extract HITL (M-20)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S3  
**Zależy od:** 65.0 `inbound_message` · M-20 `extract_to_draft` · HITL `/extractions`

**Status:** Zamknięty `/noc` 2026-09-02. Nie IMAP. Nie zapis `rate_line` z serwisu ekstrakcji. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** operator na `/mail` wysyła **temat + treść** istniejącej wiadomości do `ExtractionService.extract_to_draft`. `source_ref` = pin wiadomości (`fixture://` / `synth://`). Szkic ląduje na istniejącej kolejce HITL `/extractions`. Accept zostaje tam, nie na poczcie. API składa BC; `ExtractionService` nie importuje `inbound_messages`.

**Łowca:** ISTNIEJE: `extract_to_draft`, guard, `/extractions` accept. PODOBNE: extract z wklejonego tekstu. BRAK: przycisk z poczty; blob załącznika.

**Odrzucone:**

- Załącznik blob / PDF z maila (poza S1; parser dokumentu już jest inną ścieżką).
- Accept / zapis `rate_line` z poczty albo z `ExtractionService`.
- Import `inbound_messages` w `app.services.extraction`.
- Auto-extract przy INSERT fixture.
- IMAP / Graph / send.
- F9.1.

## Zakres

1. `POST /inbound-messages/{id}/extract` — `can_manage_inbound_messages`; 403 bez tuple.
2. Wejście = `subject` + `body_text`; `source_ref` z wiersza; guard jak przy zwykłym extract.
3. `/mail`: przycisk „Extract HITL” na wierszu; zero nowego ekranu.
4. Test: serwis extraction nie importuje inbound; accept z tego endpointu nie istnieje; nieznana wiadomość = 404.

## Poza zakresem

Załączniki · IMAP · Graph · send · auto-accept · `party_id` wymagane · outbox · LLM liczący kwoty

## Ustalenia

- EN: extract z `inbound_message`. PL: wyciąg ze wiadomości (szkic HITL).
- HITL zostaje. ExtractionService nie zapisuje `rate_line`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Bez nowej tabeli. Korelacja przez `source_ref`.

## Kryteria akceptacji

- [x] POST extract tworzy `extraction_draft` pending z tekstem wiadomości
- [x] Brak importu inbound w `app.services.extraction`
- [x] Brak accept / `rate_line` na tym endpoincie
- [x] `/mail` przycisk; zero Graph/IMAP
- [x] Guard nadal skanuje tekst

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST extract → pending draft; 404; 403; zero accept/`rate_line` na poczcie |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS.md |
| SQL | N/A | bez nowej tabeli; `extract_to_draft` bez nowego SELECT w tym BC |
| N+1 | OK | jedno get + jeden extract, bez pętli |
| Dług / człowiek | OK | API składa dwa BC; C901 czyste; `just dup` 2,74% total |
| Proza operatora | 5–15 zdań | `docs/operator/poczta.md` — Extract HITL |
| Docs/OS | PRZESZŁO | spec mail + extraction, CURRENT, `just docs` |
