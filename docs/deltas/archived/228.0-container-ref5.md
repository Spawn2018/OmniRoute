# Plaster 228.0 · T3 `ref_5` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [227.0](../archived/227.0-container-ref4.md) leftover `ref_5` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 227.0 `ref_4`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `ref_5`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `ref_5` (ta sama reguła co `ref_1`: 1–64 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`referencja`). `require_container_ref_5` deleguje do `require_container_ref_1`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["228.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `167_container_ref5`. `_WriteBox` już jest — dodać `mark5`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `ref_1`…`ref_4`. BRAK `ref_5`. BRAK PIN. BRAK VGM. BRAK `booking_no`.

**Odrzucone:**

- PIN / VGM / `booking_no` / live HTTP.
- `quantity` / `weight_kg` / TARA.
- `pin_code` / ciphertext.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `167_container_ref5` (po `166_container_ref4`): `ref_5` nullable `String(64)`.
2. POST opcjonalny `ref_5`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `ref_5` zapisuje piątą referencję, bez PIN i bez wagi
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`referencja`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `ref_5`; 400 `referencja`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(64)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `reefer` bez temperatury; PIN/VGM parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
