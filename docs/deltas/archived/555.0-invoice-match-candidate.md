# Plaster 555.0 Â· F10 leftover â€” HITL `invoice_match_candidate`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala F Â· F10

**ZaleĹĽy od:** tenancy Â· wzorzec HITL mark Â· F10 `invoice_match_mark` (obok, nie to samo)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy F10 leftover. Nowa tabela `invoice_match_candidate`: `candidate_code` (snake 2â€“32) + `candidate_kind` `proposed|held|rejected|other` + `source_ref`. Append-only. RLS. Katalog stancji kandydata dopasowania FV â€” nie ranking SQL i nie auto-link do `charge`.
- **Wybrane:** POST/GET `/api/v1/invoice-match-candidates`. OpenFGA `can_manage_invoice_match_candidates`. UI `/invoice-match-candidates`. `SHIPPED["555.0"]`.
- **Odrzucone:** ranking SQL / score / auto-link (`REJECTED` â†’ leftover). FK `purchase_invoice` / `charge` (`REJECTED` â†’ leftover). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / blocks_create 409.

**Ĺowca:** BRAK `invoice_match_candidate`. ISTNIEJE `invoice_match_mark` (stancja match, nie candidate). ISTNIEJE `invoice_alloc_mark` (alokacja). ISTNIEJE `purchase_invoice` (nagĹ‚Ăłwek FV).

## Zakres

1. Migracja `435_invoice_match_candidate` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["555.0"]` â†’ `/invoice-match-candidates`.

## Poza zakresem

ranking SQL Â· score Â· auto-link Â· FK FV/charge Â· kwota Â· AI3 Â· G0

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / ranking

## Leftover

ranking SQL Â· score Â· auto-link Â· FK purchase_invoice/charge
