# Plaster 606.0 · T1 EXP1 `weigh_out_kg` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [605.0](605.0-stop-weigh-in.md) leftover · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 605.0 `weigh_in_kg`; 247.0 `weight_kg`
**Status:** Zamknięty `/noc` 2026-09-21. Nie VGM. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `weigh_out_kg` + leftover HW + leftover UXCL + Job#2 + N3 + AI3 + G0-SH):** leftover HW parked. leftover UXCL parked. Job#2 parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `weigh_out_kg` (`Numeric(14,4)` Decimal HITL, ≥ 0, pusty → `null`). POST bez pola zostaje `null`. JSON float / bool / ujemna → 400 (`waga wyjazdu`). `require_stop_weigh_out_kg` w `app.domain.stop` — wzór `require_stop_weigh_in_kg`, komunikat `waga wyjazdu`, **nie** import `container` / VGM / `charges`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["606.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `481_stop_weigh_out` (po `480_stop_weigh_in`). CHECK `ck_stop_weigh_out`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.weigh_in_kg` (605.0). ISTNIEJE `stop.weight_kg` (247.0). ISTNIEJE `container.vgm_kg` (T3, inny BC). BRAK `stop.weigh_out_kg`.

**Odrzucone:**

- HW / UXCL / Job#2 / N3 / AI3 / G0-SH / VGM / kalkulator / float / import `container` / T8 / PIN / mapa / Auth0 / live HTTP / para wymagana weigh_in↔weigh_out.

## Zakres

1. Migracja `481_stop_weigh_out`: `weigh_out_kg` nullable Numeric(14,4) + CHECK ≥ 0. Nie `create_table`.
2. POST opcjonalny `weigh_out_kg` (string jak `weigh_in_kg`). Zła wartość → 400 (`waga wyjazdu`). Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

VGM · dok / T8 · HW · UXCL · PIN · mapa · Auth0 · live HTTP · float · wymóg pary wjazd/wyjazd

## Kryteria akceptacji

- [x] POST z Decimal `weigh_out_kg` zapisuje wagę wyjazdu HITL, bez VGM
- [x] POST bez pola / pusty trim zostaje `null`
- [x] float / ujemna / nie-liczba → 400 (`waga wyjazdu`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / doku / live HTTP / import `container`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `weigh_out_kg`; 400 `waga wyjazdu`; pusty → null; gate success |
| Szybkość | N/A ten plaster | kolumny INSERT/GET |
| SQL | N/A | Numeric + CHECK |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | wzór `weigh_in_kg`; leftover HW/UXCL |
| Proza operatora | 5–15 zdań | `zlecenie.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
