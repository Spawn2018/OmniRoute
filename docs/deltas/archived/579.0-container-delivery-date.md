# Plaster 579.0 · T3 leftover — HITL `container.delivery_date`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`pickup_date` / `return_date` / `gate_in_date` / `delivery_date` / `unload_date`, typ `date`) · [578.0](../archived/578.0-container-gate-in-date.md)

**Zależy od:** 153.0 `container` · 578.0 `gate_in_date`

**Status:** Zamknięty `/noc` 2026-09-19. Nie `unload_date`. Nie cutoff timestamptz. Nie `temp_min`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po `gate_in_date` karta T3 zostawia `delivery_date`. W `backend/app` kolumny nie ma. Ten sam typ `date` przez `_require_calendar_day` z etykietą `data dostawy`. Pusty string → `null`, POST bez pola → `null`. Zła wartość → 400. Nie liczy countdown. Nie mutuje `gate_in_date`.
- **Wybrane:** `require_container_delivery_date`. UI `/shipments`, string (`optionalToken`). `SHIPPED["579.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `459_container_delivery_date` (po `458_container_gate_in_date`). Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `unload_date` (`REJECTED` w tym plasterze). Reuse cutoffu timestamptz (`REJECTED`). `temp_min` (`REJECTED`). N3 countdown (`REJECTED` → park). float / ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `delivery_date` w `backend/app`. ISTNIEJE `_require_calendar_day`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `459_container_delivery_date`: kolumna nullable `Date`.
2. Domain `require_container_delivery_date` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["579.0"]`.

## Poza zakresem

`unload_date` · cutoff timestamptz · `temp_min` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `delivery_date="2026-09-22"` zapisuje date
- [x] data z godziną / zły format / nie-string → 400 (`data dostawy`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero countdown / mutacja `gate_in_date`

## Leftover

`unload_date` · `temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL delivery_date date |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Date |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | `_require_calendar_day`; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
