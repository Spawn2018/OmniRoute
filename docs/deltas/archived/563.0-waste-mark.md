# Plaster 563.0 · C6 leftover — HITL `waste_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C6 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C8 `relation_document_requirement` (obok, nie to samo)

**Status:** zamknięty `/noc` — HITL katalog `waste_mark`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C6 leftover. Nowa tabela `waste_mark`: `mark_code` (snake 2–32) + `waste_kind` `bdo|kpo|wsr|other` + `source_ref`. Append-only. RLS. Katalog stancji odpadów BDO/KPO/WSR — nie MOS live, nie kolumna `shipment.is_waste`.
- **Wybrane:** POST/GET `/api/v1/waste-marks`. OpenFGA `can_manage_waste_marks`. UI `/waste-marks`. `SHIPPED["563.0"]`.
- **Odrzucone:** MOS/BDO HTTP / API live (`REJECTED` → park). Kolumna `shipment.is_waste` (`REJECTED` → leftover). Kwota / float / scoring (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / PUESC / live 409.

**Łowca:** BRAK `waste_mark`. ISTNIEJE `relation_document_requirement` (relation_kind może waste). ISTNIEJE `filing_scheme_mark`. ISTNIEJE `dangerous_good`.

## Zakres

1. Migracja `443_waste_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["563.0"]` → `/waste-marks`.

## Poza zakresem

MOS live · BDO HTTP · `shipment.is_waste` · AI3 · G0 · kwota

## Kryteria akceptacji

- [x] POST zły kind / obce source_ref / pusty kod → 400
- [x] izolacja tenantów
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / MOS

## Leftover

MOS/BDO live · `shipment.is_waste` · scoring

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (gate po push) | HITL katalog; czekaj `gate` success |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | INSERT/SELECT standard RLS |
| N+1 | OK | brak pętli z query |
| Dług / człowiek | OK | wzorzec HITL mark |
| Proza operatora | 5–15 zdań | `docs/operator/waste-mark.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
