# Plaster 580.0 · T3 leftover — HITL `container.unload_date`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (pięć dat SPEED) · [579.0](../archived/579.0-container-delivery-date.md)

**Zależy od:** 153.0 `container` · 579.0 `delivery_date`

**Status:** Zamknięty `/noc` 2026-09-19. Nie `temp_min`. Nie cutoff timestamptz. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ostatnia data SPEED z karty T3: `unload_date`. W `backend/app` kolumny nie było. Ten sam typ `date` przez `_require_calendar_day` z etykietą `data rozładunku`. Pusty string → `null`, POST bez pola → `null`. Zła wartość → 400. Nie liczy countdown. Nie mutuje `delivery_date`.
- **Wybrane:** `require_container_unload_date`. UI `/shipments`, string (`optionalToken`). `SHIPPED["580.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `460_container_unload_date` (po `459_container_delivery_date`). Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `temp_min` (`REJECTED` w tym plasterze — osobna karta). Reuse cutoffu timestamptz (`REJECTED`). N3 countdown (`REJECTED` → park). float / ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `unload_date` w `backend/app` przed plasterem. ISTNIEJE `_require_calendar_day`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `460_container_unload_date`: kolumna nullable `Date`.
2. Domain `require_container_unload_date` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["580.0"]`.

## Poza zakresem

`temp_min` · cutoff timestamptz · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `unload_date="2026-09-23"` zapisuje date
- [x] data z godziną / zły format / nie-string → 400 (`data rozładunku`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero countdown / mutacja `delivery_date`

## Leftover

`temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL unload_date date |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Date |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | `_require_calendar_day`; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
