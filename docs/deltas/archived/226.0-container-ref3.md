# Plaster 226.0 · T3 `ref_3` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [225.0](../archived/225.0-container-ref2.md) leftover `ref_3` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 225.0 `ref_2`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `ref_3`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `ref_4`…`ref_5` parked. leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `ref_3` (ta sama reguła co `ref_1`: 1–64 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`referencja`). `require_container_ref_3` deleguje do `require_container_ref_1`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["226.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `165_container_ref3`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `ref_1` / `ref_2`. BRAK `ref_3`. BRAK PIN. BRAK VGM. BRAK `booking_no`.

**Odrzucone:**

- `ref_4`…`ref_5` w tym plasterze.
- `booking_no` / live HTTP.
- `quantity` / `weight_kg` / VGM / TARA.
- `pin_code` / ciphertext.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `165_container_ref3` (po `164_container_ref2`): `ref_3` nullable `String(64)`.
2. POST opcjonalny `ref_3`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · `ref_4`…`ref_5` · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `ref_3` zapisuje trzecią referencję, bez PIN i bez wagi
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`referencja`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `ref_3`; 400 `referencja`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(64)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | leftover T3 `ref_4`; PIN/VGM parked |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
