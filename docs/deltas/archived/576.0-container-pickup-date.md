# Plaster 576.0 · T3 leftover — HITL `container.pickup_date`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`pickup_date` / `return_date` / `gate_in_date` / `delivery_date` / `unload_date`, typ `date`) · [575.0](../archived/575.0-container-volume-m3.md)

**Zależy od:** 153.0 `container` · 575.0 `volume_m3`

**Status:** Zamknięty `/noc` 2026-09-19. Nie cztery pozostałe daty. Nie cutoff timestamptz. Nie `temp_min`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** karta T3 ma pięć dat SPEED. Węższa z kolejki to sama `pickup_date`. W `backend/app` kolumny nie ma. Typ karty to `date`, nie `timestamptz` cutoffów (`last_survey_at` / `si_cutoff_at`). Opcjonalna data kalendarzowa: ISO `YYYY-MM-DD` przez `date.fromisoformat`, pusty string → `null`, POST bez pola → `null`. Zła wartość → 400 (`data odbioru`). Nie liczy countdown. Nie mutuje `volume_m3`.
- **Wybrane:** `require_container_pickup_date` w domenie kontenera (ten sam kształt co data kalendarzowa w innym BC, bez importu tamtego serwisu). UI `/shipments`, string (`optionalToken`). `SHIPPED["576.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `456_container_pickup_date` (po `455_container_volume`), kolumna `Date` nullable. Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `return_date` / `gate_in_date` / `delivery_date` / `unload_date` (`REJECTED` w tym plasterze — węższa data). Reuse cutoffu timestamptz (`REJECTED` — karta mówi `date`). `temp_min` (`REJECTED`). N3 countdown (`REJECTED` → park). float / ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `pickup_date` w `backend/app`. ISTNIEJE `require_last_survey_at` (timestamptz). ISTNIEJE `date.fromisoformat` w innym BC — nie import. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `456_container_pickup_date`: kolumna nullable `Date`.
2. Domain `require_container_pickup_date` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["576.0"]`.

## Poza zakresem

`return_date` · `gate_in_date` · `delivery_date` · `unload_date` · cutoff timestamptz · `temp_min` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `pickup_date="2026-09-19"` zapisuje date
- [x] data z godziną / zły format / nie-string → 400 (`data odbioru`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero countdown / mutacja `volume_m3`

## Leftover

`return_date` i reszta dat SPEED · `temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL pickup_date date |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Date |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | ISO date lokalnie; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
