# Plaster 440.0 · AI1.4 leftover FK `twin_mark` → `twin_kind`

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · C.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 437.0 `twin_kind` · 199.0 `twin_mark` · wzorzec 439.0

## Wybrane / odrzucone / dlaczego

- **Wybrane:** zdjąć CHECK ośmiu wartości z `twin_mark.twin_kind`. Walidacja wobec słownika = FK `(organization_id, twin_kind)` → `twin_kind (organization_id, kind_code)` `ON DELETE RESTRICT` (VISION B.3 · C.2).
- **Wybrane:** kolumna zostaje tekstem `kind_code` (snake), nie UUID. Szerokość 32 jak słownik.
- **Wybrane:** backfill Distinct starych kodów jako wiersze `twin_kind` per organizacja, która już ma znacznik — żeby upgrade nie padł.
- **Wybrane:** serwis `twin_marks` **nie** importuje `twin_kinds` (AGENTS). Brak wiersza słownika = `IntegrityError` → 400.
- **Odrzucone:** zmiana `outcome_ledger` · `data_source` (AI5) · CRPS (AI2) · silnik L3+ · fizyka / `plan_snapshot`.
- **Odrzucone:** import `app.services.twin_kinds` · seed globalny poza tenantem · UPDATE/DELETE znacznika.

**Łowca:** PODOBNE 439.0 (FK ledgeru). PODOBNE CHECK na `twin_mark` (199.0) i otwarty słownik `twin_kind` (437.0). BRAK FK. Nie klon nowej tabeli.

## Zakres

Migracja `350` (revises `349_suggestion_ledger_kind_fk`). Domain: rodzaj = snake, bez listy ośmiu. Test: `tender` przechodzi w parse/HTTP unit; brak wiersza słownika na bazie → IntegrityError. UI znacznika: pole tekstowe.

## Poza zakresem

outcome_kind CHECK · data_source · CRPS · trzy BC autosave · fizyka bliźniaka

## Kryteria akceptacji

- [x] brak `IN ('vehicle'` w modelu/migracji 350; jest FK RESTRICT
- [x] POST znacznik `tender` (parse + HTTP unit) → 201
- [x] INSERT znacznik bez wiersza słownika → IntegrityError
- [x] izolacja RLS bez zmian semantyki; craft_style funkcje>40: 0

## Leftover

`data_source` (AI5) · `outcome_ledger.outcome_kind` CHECK · trzy BC · AI4.1 · CRPS (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CHECK zdjęty, FK RESTRICT, backfill, UI otwarty |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | backfill DISTINCT + FK; brak nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | IntegrityError mapowany po nazwie constraint |
| Proza operatora | 5–15 zdań | `docs/operator/twin.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI billing limit |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
