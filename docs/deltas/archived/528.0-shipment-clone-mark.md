# Plaster 528.0 · N9 — HITL `shipment_clone_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N9 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) · po [527.0](../archived/527.0-margin-floor.md) · z U1 [144.0](../archived/144.0-u1-u5-carry-checklist.md)

**Zależy od:** U1 `field_carry_forward` (poza zapisem) · `shipment` (poza zapisem)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `shipment_clone_marks`. Tabela `shipment_clone_mark`: `mark_code` + `clone_kind` `last_similar`|`manual_pick`|`other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja N9 — **znacznik intencji klonu jako dana HITL**, nie kopiowanie zlecenia i nie drugi SoR.
- **Wybrane:** POST/GET `/api/v1/shipment-clone-marks`. OpenFGA `can_manage_shipment_clone_marks`. UI `/shipment-clone-marks`. `SHIPPED["528.0"]`.
- **Wybrane:** how-to `docs/operator/shipment-clone-mark.md`.
- **Odrzucone:** auto INSERT `shipment` / cichy UPDATE / kopiowanie `charge` (`REJECTED` — drugi SoR).
- **Odrzucone:** silnik „podobne” SQL · LLM matching · FK do `shipment` w tym plasterze · F2b Decimal skonta (`REJECTED` na tym wierszu — leftover F2 / osobny).
- **Odrzucone:** float · marża · live HTTP.

**Łowca:** ISTNIEJE `field_carry_forward` (U1) · ISTNIEJE `shipment` · ISTNIEJE `cash_discount` (F2 kind). BRAK `shipment_clone_mark`. BRAK auto-klonu zlecenia.

## Zakres

HITL `shipment_clone_mark` — migracja 418 + warstwy + OpenFGA + FE + how-to + SHIPPED 528.0.

## Poza zakresem

kopiowanie wierszy shipment/stop/charge · matching podobieństwa · F2 Decimal · U1 nowe klucze · live HTTP

## Kryteria akceptacji

- [x] migracja + FORCE RLS + izolacja
- [x] POST zły kind / obce source_ref → 400; extra amount → 422
- [x] UI `/shipment-clone-marks`; `SHIPPED["528.0"]`
- [x] craft_style; how-to

## Leftover

kopiowanie allowlisty pól z poprzedniego zlecenia przez U1 · silnik similar SQL · F2b Decimal na skonto przy klonie FV
