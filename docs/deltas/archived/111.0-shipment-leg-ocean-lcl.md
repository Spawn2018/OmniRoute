# Plaster 111.0 · S49 odcinek drobnicy morskiej (M-51)

**Spec źródłowa:** [docs/spec/ocean-lcl.md](../../spec/ocean-lcl.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S49  
**Zależy od:** 44.0 `/lcl` · 110.0 `shipment_leg` · 4.0 `port.is_seaport`

**Status:** Zamknięty `/noc` 2026-09-04. Ten sam żywy ID **M-51**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama tabela `shipment_leg`. Migracja 068 rozszerza CHECK `leg_kind IN ('road', 'rail', 'china_rail', 'ocean_lcl')`. Operator na `/lcl` **zapisuje** odcinek: `shipment_id` + dwie lokalizacje `unlocode` + `source_ref` + `leg_kind=ocean_lcl`. Port podpięty pod lokalizację musi mieć `is_seaport`. Jeden odcinek `ocean_lcl` na zlecenie (unikat org+zlecenie+kind zostaje — modały mogą współistnieć). API składa: odczyt `shipment`, odczyt dwóch `location`, odczyt `port`, zapis nogi. Serwis nogi nadal nie importuje geography. `SHIPPED_CHARGE_ROUTES["111.0"]` = `/lcl`. OPS_JOBS długość 50. POST: `leg_kind` opcjonalny, domyślnie `road` — formularze 108.0–110.0 bez zmian.

**Łowca:** ISTNIEJE 108.0–110.0 `shipment_leg` + `/shipment-legs` + `can_manage_shipment_legs`. ISTNIEJE 44.0 `/lcl` + `oceanLclPorts`. ISTNIEJE 4.0 `is_seaport`. BRAK `leg_kind=ocean_lcl` w CHECK. PODOBNE: 109.0/110.0 druga akcja na istniejącym ekranie. Dwa joby (katalog vs odcinek) = `/ports` vs `/lcl`.

**Odrzucone:**

- Druga tabela LCL / CFS / konsolidacji / CBM.
- Rozróżnienie LCL vs FCL (brak kolumny).
- Druga mapa / GPS / HTTP konsolidatora.
- Kwota / marża / CBM w Pythonie albo JS.
- Auto-INSERT przy zapisie zlecenia.
- F9.1.

## Zakres

1. Migracja `068_shipment_leg_ocean_lcl_kind` (po `067_shipment_leg_china_rail_kind`): DROP `ck_shipment_leg_kind`, nowy CHECK z `ocean_lcl`. Bez nowej tabeli.
2. POST `/shipment-legs` przyjmuje `ocean_lcl`. Końce: `unlocode` + `is_seaport`. Port śródlądowy albo strefa pocztowa → 400. Nieznane zlecenie / lokalizacja / port → 404.
3. UI `/lcl`: lista odcinków `ocean_lcl` + zapis. `data-ocean-lcl="board"` i inwentarz 44.0 zostają. `SHIPPED_CHARGE_ROUTES["111.0"]` = `/lcl`. OPS_JOBS = 50.
4. Test izolacji nie nowy — asercja CHECK `ocean_lcl` dopuszczony (były odrzut) + HTTP. How-to `docs/operator/drobnica.md`. `katalogi.md` zostaje ≤ 40 linii (wtopić w istniejący bullet nóg).

## Poza zakresem

S50 flota · CFS · LCL vs FCL · CBM · mapa · HTTP · kwota · F9.1

## Ustalenia

- Token: `shipment_leg` (tabela). UI zostaje `ocean_lcl`. How-to: `drobnica.md`.
- `source_ref` bez zmian: `fixture://shipment-leg/` albo `tenant:manual`.
- Serwis nogi bez `httpx` / bez importu rodziców. ExtractionService nie importuje nogi.
- LLM nie liczy CBM. HITL extract bez zmian. `charge` zostaje prawdą o marży.
- Formularz: `data-ocean-lcl="leg-form"`. Nie klon `/road`, `/rail`, `/china-rail`, nie `CatalogCreateForm`.
- OPS_JOBS zostaje długości 50.

## Schemat

Bez nowej tabeli. `shipment_leg.leg_kind`: `road` | `rail` | `china_rail` | `ocean_lcl`.

## Kryteria akceptacji

- [x] CHECK dopuszcza `ocean_lcl`; poprzednie kind nadal działają
- [x] Brak `amount` / leaflet / CBM; zero nowej tabeli
- [x] Port bez `is_seaport` albo `postal_zone` jako koniec → 400; brak uprawnienia → 403
- [x] `/lcl` zapisuje i listuje odcinki `ocean_lcl`; `just api-types`; `katalogi.md` ≤ 40 linii

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | CHECK `ocean_lcl`, HTTP 400 śródlądowy, zapis na `/lcl` |
| Szybkość | N/A ten plaster | katalog INSERT/SELECT |
| SQL | N/A | bez nowego SELECT poza listą nóg |
| N+1 | OK | ocean_lcl = get zlecenie + dwie lokalizacje + dwa porty + INSERT |
| Dług / człowiek | OK | formularz `dl` nie kopiuje `/road` `/rail` `/china-rail` |
| Proza operatora | 5–15 zdań | `drobnica.md`; `katalogi.md` wtopione, 40 linii |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
