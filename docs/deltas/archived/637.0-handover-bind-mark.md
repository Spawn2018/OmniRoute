# Plaster 637.0 · leftover 543 handover bind stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N11 · [543.0](../archived/543.0-handover-note.md) · [529.0](../archived/529.0-handover-sbar-mark.md)
**Zależy od:** 543.0 `handover_note` już jest
**Status:** Zamknięty `/noc 17` 2026-09-23. Nie FK UUID. Nie T6 live.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover 543 „FK mark/T6”):** `handover_note` zostaje treścią S/B/A/R. Live T6 / N8 / auto SBAR / FK UUID zostają park. Żywy plaster: **nowa tabela** `handover_bind_mark` per tenant — HITL stance wiązania przekazania. Wiersz = `mark_code` (snake 1–32) + `bind_kind` (`note`|`board`|`shift`|`other`) + `source_ref` (`tenant:manual` albo `fixture://handover-bind/…`). Operator zapisuje, do czego odnosi przekazanie (wpis / tablica / zmiana / inne); serwis **nie** wiąże UUID do `handover_note` / trip / shipment i nie czyta T6. Zły kind → 400 (`wiązanie`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `handover_notes` / `handover_sbar_marks` / trips / charges. UI `/handover-bind-marks`. `SHIPPED_CHARGE_ROUTES["637.0"]` = `/handover-bind-marks`. OpenFGA: `can_manage_handover_bind_marks`. Migracja `509_handover_bind_mark` po `508_dt_output_kind`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 543.0 / 529.0 „wiązywanie z tablicą T6 / FK mark” — tu tylko HITL stance, nie FK i nie live board. Źródło: `docs/deltas/archived/543.0-handover-note.md`; `docs/ops/docs-debt.md` § 529.0.

**Łowca:** ISTNIEJE `handover_note` — treść S/B/A/R. ISTNIEJE `handover_sbar_mark` — kind SBAR. ISTNIEJE `clone_carry_mark` / `crm_link_mark` — wzorzec stance bind. BRAK `handover_bind_mark`. BRAK `/handover-bind-marks`.

**Odrzucone:**

- FK UUID do `handover_note` / trip / shipment / mark
- T6 live board / N8 / auto SBAR z LLM
- drugi czat
- kwota / marża / float

## Zakres

1. Migracja `509_handover_bind_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `bind_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/handover-bind-marks` + zdanie w how-to handover.

## Poza zakresem

FK UUID · T6 live · N8 · auto SBAR · LLM · float

## Plan plików

- `backend/alembic/versions/509_handover_bind_mark.py`
- `backend/app/models/handover_bind_mark.py`
- `backend/app/domain/handover_bind_mark.py`
- `backend/app/repositories/handover_bind_marks/`
- `backend/app/services/handover_bind_marks/` (+ AGENTS.md)
- `backend/app/api/handover_bind_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/handover-bind-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `bind_kind=note` zapisuje wiersz; `handover_note` bez zmian
- [x] kind spoza `note`|`board`|`shift`|`other` → 400 (`wiązanie`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `handover_notes` / trips / charges i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / FK UUID

## Leftover

FK UUID · T6 live · N8 · auto SBAR
