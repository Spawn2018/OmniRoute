# Plaster 612.0 · T3 `destination_city` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T3 · [611.0](611.0-container-pod-unlocode.md)
**Zależy od:** 153.0 `container`; 611.0 `pod_unlocode`
**Status:** Zamknięty `/noc` 2026-09-21. Nie mapa. Nie FK.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z karty T3 `destination_city` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** `pol_unlocode` jest w 610.0, `pod_unlocode` w 611.0 (gate success). FK `port` odrzucone — serwis kontenera nie importuje geography. Miasto nie jest UN/LOCODE. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `destination_city` (`String(64)` HITL). POST bez pola zostaje `null`. Pusty trim → `null`. Nie-tekst albo dłuższy niż 64 → 400 (`miasto`). Trim bez składania wielkości liter. `require_destination_city` w `app.domain.container` — wzór `require_pickup_terminal` (sufit 64), **nie** geokoder, **nie** mapa. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["612.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `487_container_destination_city` (po `486_container_pod_unlocode`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.pod_unlocode` (611.0). ISTNIEJE `container.pickup_terminal` (tekst terminala, nie miasto). BRAK `container.destination_city`.

**Odrzucone:**

- FK `port` / geokoder / mapa / HW / UXCL / N3 / AI3 / G0-SH / float / marża / Auth0.

## Zakres

1. Migracja `487_container_destination_city`: `destination_city` nullable String(64). Nie `create_table`.
2. POST opcjonalny `destination_city`. Zła wartość → 400 (`miasto`). Serwis nie importuje charges ani geography.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

FK portu · mapa · geokoder · HW · UXCL · N3 · Auth0 · live HTTP · float

## Kryteria akceptacji

- [x] POST z `destination_city` zapisuje tekst do 64 znaków, bez geokodera
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / powyżej 64 → 400 (`miasto`)
- [x] RLS istniejącego `container`; izolacja bez nowej tabeli
- [x] Zero float / marży / live HTTP / import charges / import geography

## Leftover

HW · UXCL · N3 · AI3 · G0-SH

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | CI po push | POST opcjonalny `destination_city`; 400 `miasto`; puste → null. PRZESZŁO dopiero gdy gate na SHA = success |
| Szybkość | N/A | brak ścieżki z budżetu AGENTS |
| SQL | N/A | add_column; bez nowego SELECT |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | sufit 64 jak ref, nie geokoder |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | po `--close` | `factory_cycle.py --close` |
