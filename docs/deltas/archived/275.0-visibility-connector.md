# Plaster 275.0 · CT7 `visibility_connector` HITL (p44 fixture)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT7 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`visibility vendor capability`)
**Zależy od:** 0.3 tenancy · 271.0 `exchange_connector` / 270.0 `idp_connector` / 197.0 `telematics_connector` (wzorzec katalogu HITL)
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie sekrety. Nie AIS. Nie track-and-trace.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CT7 parked live / pin 2026-09-08c: katalog HITL fixture jednego vendora widoczności jako dane; nie live p44 i nie trzy marki):** leftover live p44 / FourKites / Shippeo HTTP parked (TO_VERIFY umowa — park live, nie teatr API, nie feed). leftover scrape ocean parked (zakaz na stałe). leftover CT8 AIS / kongestia parked (TO_VERIFY licencja — nie ten Q). leftover CI1 `sla_clause` / extract LLM parked. leftover `wrapped_dek` / KMS parked (274.0 = znacznik, nie klucz). leftover drugi vendor jako prawda parked (karta CT7). Żywy plaster: **nowa tabela** `visibility_connector` per tenant — HITL katalog konektora widoczności jako dane, nie live track i nie mapa. GLOSSARY: **konektor widoczności** = `visibility_connector` (nie `track_helper`, nie `p44_utils`, nie kolumny na `tracking_event` / `telematics_connector`). Token `system_kind` = `p44` — pierwsza nazwana marka w wierszu CT7. Nie trzy tokeny w jednym CHECK. Wiersz = `connector_code` (snake 2–32) + `system_kind` (`p44`) + `source_ref` (`tenant:manual` albo `fixture://visibility/…`). Zero kolumn sekretu / ciphertext / api_key / URL. Operator wpisuje kod i token `p44`; serwis nie woła HTTP, nie scrapuje, nie poluje AIS, nie woła LLM, nie importuje `charges` / `tracking_events` / `telematics_connectors` / `extraction` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły kind → 400 (`system`). Obce `source_ref` → 400 (`obce`). Body z polem sekretu (`api_key`, `ciphertext`, `base_url`) → 422 (`extra=forbid`). Unique `(organization_id, connector_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/visibility-connectors`. `SHIPPED_CHARGE_ROUTES["275.0"]` = `/visibility-connectors`. OpenFGA: `can_manage_visibility_connectors`. Migracja `208_visibility_connector` (po `207_tenant_contract_kek`). Job OPS 120. Marża zostaje na `charge`. To nie jest żywy feed widoczności.

**Łowca:** BRAK tabeli `visibility_connector`. BRAK `track_connector`. BRAK `p44` / FourKites / Shippeo w schemacie. BRAK nazwy w GLOSSARY — ten plan dopisał **konektor widoczności**. ISTNIEJE `tracking_event` 91.0 — ślad zlecenia, **nie** vendor. ISTNIEJE `telematics_connector` 197.0 — GPS, **nie** p44. ISTNIEJE `exchange_connector` 271.0 / `idp_connector` 270.0 / `erp_connector` 268.0 — inny job. ISTNIEJE `watchtower` — nie feed p44. PODOBNE: kod + kind + `source_ref` jak S55. Nie wyodrębniać wspólnej warstwy.

**Odrzucone:**

- live HTTP p44 / FourKites / Shippeo / track-and-trace feed.
- scrape ocean / AIS / CT8 kongestia live.
- trzy marki w jednym CHECK (`fourkites`, `shippeo` obok `p44`).
- kolumny sekretu / ciphertext / `api_key` / `base_url` / live URL.
- CI1 extract / `sla_clause` / `wrapped_dek` / KMS / twierdzenie o szyfrowaniu.
- import charges/tracking_events/telematics/extraction; kwota / marża / float / HTTP.
- mapa / live ETA board / drugi vendor jako prawda.

## Zakres

1. Migracja `208_visibility_connector` po `207_tenant_contract_kek`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK `system_kind = 'p44'`. Zero kolumn sekretu / URL / kwoty.
2. POST/GET. Zły token → 400 (`oznaczenie` / `system` / `obce`). Body z sekretem → 422. Serwis nie importuje charges/tracking_events/telematics/extraction i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/visibility-connectors`. How-to `docs/operator/konektor-widocznosci.md`; fold w `katalogi.md` (40 linii). Job OPS 120. OpenFGA `can_manage_visibility_connectors`. Nazwy: `VisibilityConnectorDesk` / `visibility-connector-form.tsx` / `persistVisibilityConnector`. Brak przycisku „Śledź live” / mapy / ETA.

## Poza zakresem

live p44 · FourKites · Shippeo · scrape ocean · CT8 AIS · CI1 extract · KMS · sekrety · kwota · float · mapa

## Kryteria akceptacji

- [x] POST zapisuje `visibility_connector` z kodem, `system_kind=p44` i `source_ref`, bez HTTP
- [x] zły kod → 400 (`oznaczenie`); zły kind (m.in. `fourkites`, `shippeo`) → 400 (`system`); obce `source_ref` → 400 (`obce`)
- [x] POST z `api_key` / `ciphertext` / `base_url` **nie** zapisuje sekretu (422)
- [x] GET zwraca wpisy tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / ciphertext / live HTTP / importu charges/tracking_events/telematics/extraction
- [x] Zero twierdzenia, że żywy feed widoczności albo KEK/szyfr istnieje

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; 422 na sekrety; RLS + unique kod/`source_ref`; zero live HTTP |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); izolacja SELECT pod RLS |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela unique; leftover live p44 / FourKites / Shippeo; radio + parse_visibility_row zamiast klonu S55; `just dup` 2.96% |
| Proza operatora | 5–15 zdań | `konektor-widocznosci.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN CT7, GLOSSARY, MODULES, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (275.0)

- Migracja `208_visibility_connector` po `207_tenant_contract_kek`. RLS FORCE.
- `VisibilityConnectorService.persist_visibility_fixture` — katalog unique, bez importu charges/tracking_events/telematics/extraction.
- UI `/visibility-connectors`. `SHIPPED_CHARGE_ROUTES["275.0"]` = `/visibility-connectors`. Job OPS 120.

## Leftover po 275.0

Live p44 (gdy umowa + sekret + test). FourKites / Shippeo jako osobne tokeny. CT8 AIS live (TO_VERIFY licencja). CT1 HITL `purchase_order` / reszta pinu. CI1 extract. `wrapped_dek` / KMS.
