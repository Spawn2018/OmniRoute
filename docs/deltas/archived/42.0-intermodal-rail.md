# Plaster 42.0 · M-49 Kolej intermodalna (`intermodal_rail`)

**Spec źródłowa:** [docs/spec/intermodal-rail.md](../../spec/intermodal-rail.md)  
**Zależy od:** 4.0 `port.function_flags`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie wagon.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 7 M-49):** kolej **nie ma tabeli wagonów**. Operator widzi porty, których `function_flags` zawiera `rail` (flaga UN/LOCODE już w 4.0). Token `intermodal_rail` (UI, nie tabela). Żywy ID **M-49**. 4.0 zostaje pełnym katalogiem portów; 41.0 zostaje drogą.

**Łowca:** BRAK `rail_leg` / wagon / tabeli kolejowej. ISTNIEJE: `fetchPorts`, `function_flags` z tokenem `rail`. PODOBNE: 4.0 `/ports`; 41.0 filtr `kind`.

**Odrzucone:**

- Tabela wagonu / rozkładu / CIM — zgadywanie schematu (CURRENT).
- Live GPS / AIS — leftover trackingu.
- Filtrowanie `is_seaport` jako kolej — to morze.

## Zakres

Read-only `/rail`. `data-intermodal-rail="board"`. Brak migracji. `railPorts(ports)`. Link do `/ports`.

## Poza zakresem

Tabela wagonów · CIM · GPS · ExtractionService

## Ustalenia

- Token: `intermodal_rail` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia geography.
- RLS: bez nowej tabeli.
- Job: `/rail`. `SHIPPED_CHARGE_ROUTES["42.0"]` = `/rail`. OPS_JOBS +1 → 39.
- `just dup` ≤ 3% (2,89%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 42.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/rail` ma `data-intermodal-rail="board"` i woła `railPorts`
- [x] Filtr `function_flags` zawiera `rail`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["42.0"]` = `/rail`
- [x] `just dup` ≤ 3% (2,89%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | tylko flagą rail; brak wagonu |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | wagon / CIM / GPS zostaje |
| Docs/OS | PRZESZŁO | spec `intermodal-rail.md`; CURRENT → Plan M-50 |

## Knowledge

Nie RAG. Nie silnik CIM.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Wagon i CIM świadomie leftover.
