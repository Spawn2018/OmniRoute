# Plaster 151.0 · T2 `resource`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 · S50 = T2
**Zależy od:** M-01 tenancy
**Status:** Zamknięty `/noc` 2026-09-08. Nie trip. Nie HW.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z T2):** jedna tabela katalogu floty per tenant. Wiersz = `resource_kind` (`vehicle` | `driver` | `trailer`) + `display_name` + opcjonalny `registration_no` + `source_ref`. Override tej samej pary kind×nazwa z innym nr = nowy wiersz + `superseded_by`. GET listy SQL (bieżące), opcjonalny filtr kind. Jedna tabela, nie trzy. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["151.0"]` = `/shipments`. OpenFGA: `can_manage_shipments`. Migracja `091_resource`.

**Łowca:** ISTNIEJE `stop` / `shipment_leg` — inny agregat. ISTNIEJE 112.0 named park S50 bez tabeli. BRAK `resource`. BRAK `trip`. BRAK `/fleet`.

**Odrzucone:**

- `trip` / przypisanie vehicle+trailer+kierowca I/II (leftover T2b).
- `party_id` podwykonawcy (M-10).
- pojemności / Decimal km / snapshot kosztu (Fala P / T6).
- `document_expiries` / ważności przy planowaniu.
- własne HW / mapa / `/fleet`.

## Zakres

1. Migracja `091_resource`: RLS, izolacja, indeks `(organization_id, resource_kind)`.
2. POST/GET. Obcy kind / brak source_ref → 400.
3. UI `/shipments`. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

trip · multi-manning · naczepa floating · Decimal km · party · mapa · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje pojazd; `resource_kind=truck` → 400
- [x] Ta sama para kind×nazwa z innym nr = nowy wiersz, stary `superseded_by`
- [x] GET zwraca bieżące; filtr kind działa
- [x] Serwis bez importu shipments / parties / charges
- [x] Test izolacji tabeli
- [x] Zero float / marży / mapy

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST 400 na truck; override = nowy wiersz; GET bieżące + filtr kind; izolacja RLS |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | EXPLAIN | `ix_resource_org_kind` (GET po kind + `superseded_by IS NULL`) |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | jeden BC katalogu; trip leftover T2b |
| Proza operatora | 5–15 zdań | wtopione w katalogi.md, 40 linii |
| Docs/OS | PRZESZŁO | CURRENT · just docs |
| Pushy do zielonego | N/A | billing GitHub zablokowany — zgoda sesji `/noc 17` |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
