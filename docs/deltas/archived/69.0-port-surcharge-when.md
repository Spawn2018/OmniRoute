# Plaster 69.0 · S6 ewaluacja `applies_when` w SQL (M-18)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S6  
**Zależy od:** 12.0 `port_surcharge` · 4.0 `port` · 0.4 OpenFGA

**Status:** Zamknięty `/noc` 2026-09-03. Nie zapis marży do `charge`. LLM nie liczy. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam katalog `port_surcharge`. Postgres filtruje `port_id` + `applies_when` po znormalizowanym tokenie (`normalize_applies_when`). `GET /port-surcharges/matching`. Zero nowej tabeli. Zero INSERT `charge` / `quotation`. Kwota extra zostaje na wierszu katalogu — panel pokazuje `<Money/>`, nie sumuje w JS. OpenFGA bez nowej relacji: `can_manage_geography`. Ekran: ten sam `/port-surcharges`.

**Łowca:** ISTNIEJE: `PortSurchargeService.resolve(port_id, code)`, `normalize_applies_when`, lista i create, UI `/port-surcharges`. PODOBNE: `find_by_port_and_code` (inny klucz). BRAK: SELECT po warunku; endpoint matching; UI „które extra pasują”.

**Odrzucone:**

- Parser AST / dzień tygodnia / typ kontenera w Pythonie (12.0 świadomie zostawił tekst; S6 = SQL, nie nowy język).
- Zapis do `charge` / `rate_line` / `quotation` (kolejka: nie marża tutaj).
- Auto-dopięcie extra przy wycenie 68.0.
- Filtrowanie listy w pętli Pythona.
- Nowa tabela warunków.
- F9.1 · Auth0 · portale · M-02.

## Zakres

1. Indeks `(organization_id, port_id, applies_when)` — migracja `030_port_surcharge_when`. Bez nowej tabeli, bez nowego RLS.
2. `list_matching(port_id, applies_when)` w SQL. `GET /port-surcharges/matching`.
3. Test: extra tenanta B nie wraca; zły port = błąd; warunek musi być dokładny po normalizacji; brak `charge` w serwisie.
4. `/port-surcharges`: formularz port + warunek, lista pasujących, `<Money/>`.

## Poza zakresem

Zapis `charge` / marża · auto na wycenie · parser AST · live taryfa · IMAP · F9.1

## Ustalenia

- Żywy ID: **M-18**. Token `applies_when` zostaje tekstem-danymi; ewaluacja = równość w SQL po `normalize_applies_when`.
- HITL extract bez zmian. LLM nie liczy. `charge` zostaje prawdą o marży.
- How-to: dopisać do `docs/operator/katalogi.md` (job odczytu matching, nie nowy zapis extra).

## Schemat

Bez nowej tabeli. Indeks na istniejącym `port_surcharge`.

## Kryteria akceptacji

- [x] Matching w SQL, nie pętla po wszystkich extra
- [x] Izolacja: extra B nie wraca dla tenanta A
- [x] Serwis nie importuje `charges` / `quotations` / `rate_lines`
- [x] `can_manage_geography`; 403 bez tuple
- [x] UI matching na `/port-surcharges`; zero sumy w JS
- [x] `just api-types` po zmianie OpenAPI

## Pytania

Brak. `/noc` akceptuje deltę.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | SQL równość `applies_when`; izolacja; UI Dopasuj; zero `charge` |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS.md |
| SQL | N/A | `WHERE port_id` + `applies_when`; indeks `ix_port_surcharge_org_port_when` |
| N+1 | OK | jedno SELECT matching |
| Dług / człowiek | OK | parser AST zostaje poza zakresem |
| Proza operatora | 5–15 zdań | `katalogi.md` — Dopasuj warunek |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT, `just docs` |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | zakaz optymalizacji pod liczbę |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
