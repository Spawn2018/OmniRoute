# Plaster 599.0 · T2 leftover — HITL `resource.vehicle_profile`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [598.0](../archived/598.0-resource-driver-card-no.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`vehicle_profile`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-20. Nie FK słownik live. Nie HERE. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `driver_card_no`. Opcjonalny `vehicle_profile` na `resource` (`String(64)`, tekst HITL etykiety profilu, nie float, nie FK). POST bez pola → `null`. Pusty string → `null`. Tekst dłuższy niż 64 → 400. Zmiana etykiety przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / inwentarz / kg / LDM / m³ / ADR / chłodnia / winda / telefon / karta). Nie silnik routingu. Nie matching.
- **Wybrane:** UI `/fleet`. `SHIPPED["599.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `475_resource_vehicle_profile` (po `474_resource_driver_card_no`). Bez nowej tabeli.
- **Odrzucone:** FK słownik / HERE live (`REJECTED`). HW (`REJECTED` → park). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.vehicle_profile`. BRAK kolumny w backendzie. PODOBNE: `phone` (64) + `inventory_no` + supersede.

## Zakres

1. Migracja `475_resource_vehicle_profile`.
2. Domain `require_vehicle_profile` + serwis + API + GET.
3. UI `/fleet` pole profilu. How-to. `SHIPPED["599.0"]`.

## Poza zakresem

HW · HERE · FK słownik · matching · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `vehicle_profile` zapisuje tekst
- [x] pusty string → `null`; >64 → 400
- [x] zmiana etykiety → supersede
- [x] izolacja RLS (tabela `resource`)
- [x] zero float / import charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | HITL vehicle_profile + supersede |
| Szybkość | N/A | |
| SQL | N/A | INSERT jak 598 |
| N+1 | OK | |
| Dług / człowiek | OK | ten sam wzorzec co phone |
| Proza operatora | 5-15 | katalogi + zlecenie |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |

## Leftover

HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
