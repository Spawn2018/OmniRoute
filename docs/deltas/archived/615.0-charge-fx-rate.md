# Plaster 615.0 · T7b nadpisanie kursu na `charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T7 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T7 · [211.0](211.0-fx-rate-basis.md)
**Zależy od:** 1.2 `charge`; 211.0 klucze `fx_rate_*` w ustawieniach
**Status:** Zamknięty `/noc` 2026-09-21. Tokeny na charge są. Mnożenie zostaje.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T7b override na charge, po T4 614.0):** polityka tenanta jest w `organization_setting` (211.0). Nadpisanie per opłata to trzy opcjonalne kolumny tekstowe na `charge`: `fx_rate_basis`, `fx_rate_offset_days`, `fx_rate_table` — te same tokeny co allowlista. Walidacja przez funkcje domeny `normalize_fx_rate_*` (DomainError 400 z `kurs` / `dni`). Puste / brak = `null` (dziedziczy politykę tenanta poza tym plasterem). Zero mnożenia `nbp_rate`. Serwis `charges` nie importuje `app.services.nbp_rates` ani `organization_settings`. UI `/charges`. `SHIPPED_CHARGE_ROUTES["615.0"]` = `/charges`. OpenFGA bez zmian. Migracja `490_charge_fx_rate`.

**Łowca:** ISTNIEJE `normalize_fx_rate_*` w domenie. ISTNIEJE KV ustawień. BRAK kolumn fx na `charge`. BRAK SQL mnożenia.

**Odrzucone:**

- mnożenie kursem / przeliczenie przy FV / tabela B live
- override na `shipment` · T7c dzień roboczy U4
- HW · UXCL · N3 · AI3 · G0-SH · float · Auth0

## Zakres

1. Migracja `490`: trzy nullable `String` na `charge` + CHECK tokenów (albo walidacja w domenie przy INSERT).
2. POST opcjonalne pola. Zły token → 400. Stare wiersze `null`.
3. UI `/charges` trzy pola opcjonalne. Fold how-to.

## Poza zakresem

Mnożenie NBP · T7c U4 · override na shipment · live HTTP · float

## Kryteria akceptacji

- [x] POST z poprawnymi tokenami zapisuje trzy pola; brak / puste → `null`
- [x] zły basis/tabela → 400 (`kurs`); zły offset → 400 (`dni`)
- [x] serwis nie importuje `nbp_rates` / `organization_settings` i nie mnoży
- [x] RLS istniejącego `charge`; zero nowej tabeli
- [x] Zero float / drugiej marży / live HTTP

## Leftover

T7c U4 D-1 · SQL mnożenia przy FV · HW · UXCL · N3

## Post-plaster

| Check | Wynik | Dowód |
|---|---|---|
| Skuteczność | PRZESZŁO | tokeny etd/-1/nbp_a; 400 `kurs`; puste → null. CI po push |
| Szybkość | N/A | brak ścieżki z budżetu AGENTS |
| SQL | N/A | add_column + CHECK; bez mnożenia |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | T7c i SQL mnożenia zostają |
| Proza operatora | 5–15 zdań | `tablica-finansowa.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | CI po push | charges http+fx unit OK |
