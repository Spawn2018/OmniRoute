# Plaster 248.0 · T1 EXP1 `quantity` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [247.0](247.0-stop-weight.md) leftover ilość · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1
**Zależy od:** 247.0 `weight_kg`
**Status:** Zamknięty `/noc` 2026-09-10. Nie opakowanie. Nie plomba. Nie mapa. Nie VGM.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 ilość/opakowanie/plomba/awizacja + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover opakowanie/plomba/awizacja/waiting/POD parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `quantity` (`Integer` HITL, ≥ 0, nie float). POST bez pola zostaje `null`. Zła ilość → 400 (`ilość`). `require_stop_quantity` w `app.domain.stop`. `record_stop` jest na 39 liniach — nowe pole przez `_PackedPoint`; sygnatura bez przekroczenia 40 (gdy trzeba, spakować opcjonalne HITL). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["248.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `184_stop_quantity` (po `183_stop_weight`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.weight_kg` (247.0, Decimal kg). ISTNIEJE `sequence_no` (Lp, nie ilość ładunku). BRAK `stop.quantity`. BRAK `packaging_code` / plomby / awizacji na `stop`.

**Odrzucone:**

- Opakowanie / plomba / awizacja / waiting / POD.
- Tabela `stop_group`.
- Float / `charge` / VGM / km / mapa / T5 / S53 / live HTTP.

## Zakres

1. Migracja `184_stop_quantity`: `quantity` nullable Integer, CHECK ≥ 0 albo NULL.
2. POST opcjonalny `quantity`. Zła wartość → 400 (`ilość`). Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

opakowanie · plomba · awizacja · waiting · POD · tabela `stop_group` · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `quantity` ≥ 0 zapisuje ilość, bez opakowania
- [x] POST bez pola zostaje `null`
- [x] float / ujemna / bool → 400 (`ilość`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / `/fleet` / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `quantity`; 400 `ilość`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | CHECK `>= 0`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | `_persist` żeby `record_stop` został ≤ 40; leftover EXP1 reszta; C901 czysty |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
