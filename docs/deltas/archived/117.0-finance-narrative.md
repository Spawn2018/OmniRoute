# Plaster 117.0 · S57 narracja po SQL (M-15)

**Spec źródłowa:** [docs/spec/finance-board.md](../../spec/finance-board.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S57  
**Zależy od:** 15.0 `/finance` · 106.0 FV na tablicy · 1.2 `charge.margin`

**Status:** Zamknięty `/noc` 2026-09-04. Ten sam żywy ID **M-15**. Kat. M-59 = to pogłębienie, bez nowej tabeli. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** `/finance` dokłada **narrację po SQL**: sekcja `data-finance="narrative"` składa polskie zdania z pól już zwróconych przez istniejące GET (`charge_code`, `margin_amount`/`margin_currency` przez `<Money/>`, `invoice_ref`, kurs `currency`+`rate_date`). Zero nowej tabeli. Zero wywołania modelu. Zero sumy. `SHIPPED_CHARGE_ROUTES["117.0"]` = `/finance`. OPS_JOBS 53 → 54.

**Łowca:** ISTNIEJE 15.0 tablica faktów + 106.0 FV. BRAK akapitu „po SQL”. PODOBNE: 116.0 wieża czyta `mail_draft`. Dwa joby: zapis FV na `/invoices` vs odczyt narracji na `/finance`.

**Odrzucone:**

- Wywołanie LLM / czat / komentarz modelu do marży (liczenie albo zgadywanie kwot).
- Nowa tabela `finance_*` / żywy M-59.
- Suma faktur / `parseFloat` / odejmowanie buy−sell.
- Silnik limitu / auto-scoring.
- F9.1.

## Zakres

1. `/finance`: `NarrativeSection`, `data-finance="narrative"`. Kwoty tylko `<Money/>` z API. Strona rozbita na funkcje ≤40 linii (dziś `FinanceBoardPage` jest za długa).
2. Test powierzchni 117.0 (zostaw asercje 15.0/106.0). How-to `tablica-finansowa.md`. `katalogi.md` bez nowej linii (footer już ma tablicę).
3. `SHIPPED_CHARGE_ROUTES["117.0"]` = `/finance`. OPS_JOBS = 54.

## Poza zakresem

Silnik limitu · suma FV · asystent LLM · Auth0 · F9.1 · S58

## Ustalenia

- Token UI zostaje `finance_board`. Żywy ID **M-15**.
- LLM nie liczy i nie składa kwot. `charge` zostaje prawdą o marży.
- Pusta lista: zdanie kanonu zostaje („marża na `charge`, bez sumy”).

## Schemat

Bez nowej tabeli.

## Kryteria akceptacji

- [x] Narracja na `/finance`; brak openai / parseFloat / sell_amount
- [x] Zero nowej migracji
- [x] OPS_JOBS = 54; `117.0` → `/finance`
- [x] `katalogi.md` ≤ 40

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/finance` składa zdania z GET; zero LLM / sumy |
| Szybkość | N/A ten plaster | odczyt listy, bez budżetu AGENTS |
| SQL | N/A | brak nowego SELECT/INSERT |
| N+1 | OK | te same fetchę co 106.0 |
| Dług / człowiek | OK | sekcje wycięte; `NarrativeSection` na `p` |
| Proza operatora | 5–15 zdań | `tablica-finansowa.md` (delta; panel-odczyt) |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
