# Plaster 25.0 · M-32 Integracja pocztowa (`mail_integration` tablica odczytu)

**Spec źródłowa:** [docs/spec/mail-integration.md](../../spec/mail-integration.md)  
**Zależy od:** 5.0 `party_contact` / `party_email_domain` · 8.0 `resolve_email` · 0.6 DataTableShell

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie IMAP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 4 M-32):** integracja pocztowa **to dziś odczyt znanych adresów**. Ekran `/mail` składa `party_email_domain`, `party_contact.email` i `resolve_email` dla wybranego kontrahenta. Token `mail_integration` (UI, nie tabela). Żywy ID **M-32**. Matcher 8.0 zostaje na `/parties`; 25.0 = osobny job operatora bez skrzynki.

**Łowca:** BRAK IMAP / SMTP / `mailbox` / sekretu skrzynki. ISTNIEJE: `fetchContacts`, `fetchEmailDomains`, `resolvePartyEmail`. PODOBNE: 15.0 `/finance` tablica odczytu; panele maila na `/parties` — nie duplikuj zapisu, tylko odczyt.

**Odrzucone:**

- Tabela skrzynki / credential IMAP — zgadywanie schematu (CURRENT); sekrety tenanta = leftover HC.
- Live IMAP/SMTP — brak magazynu kluczy.
- Outlook (M-33) · powiadomienia (M-34).
- Auto-INSERT kontaktu · LLM czytający mail.
- Pętla fetch po wszystkich party (N+1) — jeden wybrany kontrahent.

## Zakres

Read-only `/mail`. `data-mail-integration="board"`. Brak migracji. Brak endpointu IMAP.

## Poza zakresem

IMAP · SMTP · sekrety · M-33 · M-34 · ExtractionService · LLM

## Ustalenia

- Token: `mail_integration` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_parties` na kontaktach, domenach i resolve-email.
- RLS: bez nowej tabeli.
- Job: `/mail`. `SHIPPED_CHARGE_ROUTES["25.0"]` = `/mail`. OPS_JOBS 23.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/mail` ma `data-mail-integration="board"` i woła `fetchEmailDomains` + `fetchContacts` + `resolvePartyEmail`
- [x] Brak `imap` / `smtp` w ekranie; brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["25.0"]` = `/mail`
- [x] `just dup` ≤ 3% (2,69%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/mail` odczyt domen i kontaktów; resolve_email; brak IMAP |
| Szybkość | N/A ten plaster | odczyt istniejących API |
| Dług w diffie | leftover | `just dup` 2,69%; live IMAP i sekrety zostają |
| Docs/OS | PRZESZŁO | spec `mail-integration.md`; CURRENT → Plan M-33 |

## Knowledge

Nie RAG. Wzorzec = 15.0 tablica odczytu. Nie skrzynka.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Live IMAP i sekrety świadomie leftover.
