# Plaster 428.0 Â· EXP1 `spot_contract_mark` HITL

**Status:** Zamknięty `/noc`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `spot_contract_mark` â€” `mark_code` + `deal_kind` `spot|contract|other` + `source_ref`. EXP1 quotation `spot_or_contract`.
- **Odrzucone:** `temp_control_mark` â€” duplikat `reefer_mark` (juĹĽ `setpoint|genset`).
- **Odrzucone:** `cargo_value` â€” kwota/waluta.
- **Odrzucone:** `mqc_teu` â€” pokryte `mqc_mark`; nie tu.

## Zakres

HITL `spot_contract_mark`: mark_code + deal_kind spot|contract|other + source_ref.
Migracja `338` (revises `337_diversion_mark`). UI `/spot-contract-marks`.
OpenFGA `can_manage_spot_contract_marks` w `_organization_exp1_shipment_catalog_relations` (albo nowy helper â‰¤40 linii).

## Poza zakresem

kolumna na quotation Â· matching SQL Â· marĹĽa Â· MQC egzekucja

## Kryteria

- [ ] POST z kodem, deal_kind, source_ref
- [ ] zĹ‚y kod/kind/source_ref â†’ 400; amount/margin/score â†’ 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/spot-contract-marks`
- [ ] OPS_JOBS length 245; SHIPPED `"428.0"`
- [ ] craft_style funkcje>40: 0 (OpenFGA â€” nie puchnij jednej funkcji)

## Leftover

kolumna spot_or_contract na quotation Â· bid_decision

