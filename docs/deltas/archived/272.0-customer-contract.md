# Plaster 272.0 · CI9 `customer_contract` HITL (nagłówek, bez ciphertext)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CI / CI9 · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md) (tabele — nie cała fala) · [GLOSSARY.md](../../GLOSSARY.md) umowa klienta
**Zależy od:** 0.3 tenancy · 271.0 `exchange_connector` (wzorzec katalogu HITL)
**Status:** Zamknięty `/noc` 2026-09-10. Nie ciphertext. Nie KEK. Nie live CT7.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CI9 / pin 2026-09-08c: katalog HITL nagłówka umowy jako dane; nie zero-knowledge blob i nie live p44):** leftover CT7 p44 / FourKites / Shippeo parked (TO_VERIFY umowa — park live HTTP, nie teatr API, nie scrape ocean). leftover `blob_ciphertext` / `wrapped_dek` / `tenant_contract_kek` parked (zakaz Omni-master, zakaz wymyślonego KMS/unwrap). leftover CI1 `sla_clause` / extract LLM parked (CI9 przed CI1; extract nie na umowach). leftover super-admin plaintext parked (test super-admina = leftover bytea, nie ten Q). leftover Langfuse na PDF parked. leftover CI2 `spend_leakage` / CI5 kara / CI6 `impact_scenario` / G2.22 parked. Żywy plaster: **nowa tabela** `customer_contract` per tenant — HITL katalog nagłówka umowy jako dane, nie treść umowy i nie szyfr. GLOSSARY: **umowa klienta** = `customer_contract`. Wiersz = `contract_code` (snake 2–32) + `shipper_label` (tekst 1–128, etykieta, **nie** FK `party`) + `their_customer_label` (tekst 1–128, etykieta, **nie** FK `party`) + `source_ref` (`tenant:manual` albo `fixture://contract/…`). Zero kolumn `blob_ciphertext` / `wrapped_dek` / `body` / `pdf` / kary SLA / kwoty. Operator wpisuje kod i dwie etykiety; serwis nie woła HTTP, nie woła LLM, nie importuje `charges` / `parties` / `extraction` / `sla_clause` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. GET **nigdy** nie zwraca treści umowy — kolumny treści nie ma. Zły kod → 400 (`oznaczenie`). Zła etykieta załadowcy → 400 (`załadowca`). Zła etykieta odbiorcy → 400 (`odbiorca`). Obce `source_ref` → 400 (`obce`). Body z polem treści/szyfru (`body`, `blob_ciphertext`, `wrapped_dek`, `plaintext`) → 422 (`extra=forbid`). Unique `(organization_id, contract_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/customer-contracts`. `SHIPPED_CHARGE_ROUTES["272.0"]` = `/customer-contracts`. OpenFGA: `can_manage_customer_contracts`. Migracja `205_customer_contract` (po `204_exchange_connector`). Job OPS 118. Deny-list: `extraction-no-rates` dostaje `app.services.customer_contracts` / `app.models.customer_contract`. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `customer_contract`. BRAK `sla_clause` / `tenant_contract_kek` / `blob_ciphertext` w kodzie (tylko karta CI). ISTNIEJE nazwa w GLOSSARY (`umowa klienta` → `customer_contract`) — ten plaster **nie** dopisuje ciphertext. ISTNIEJE `party_document` 188.0 — kind dokumentu na `party`, nie umowa CI. ISTNIEJE `tender_data_room` — NDA przetargu, nie umowa klienta. ISTNIEJE `document_template` / `shipment_document` — layout / wskazanie, nie treść umowy. ISTNIEJE `exchange_connector` 271.0 — katalog HITL, **inny** job. ISTNIEJE `customer_rfq` — zapytanie ofertowe, nie umowa. PODOBNE: kod + etykiety + `source_ref` jak F9/S55. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- CT7 live HTTP p44 / FourKites / Shippeo / scrape ocean.
- `blob_ciphertext` / `wrapped_dek` / `tenant_contract_kek` / KMS / unwrap / klucz Omni-master.
- Langfuse na PDF / CI1 extract LLM / AI na umowach.
- super-admin plaintext / impersonate=decrypt.
- `sla_clause` / kara Decimal / `spend_leakage` / `impact_scenario` / EBITDA.
- G2.22 P/lista; FK `party` w tym Q; treść umowy w DB / logach / promptach.
- import charges/parties/extraction; kwota / marża / float / HTTP.

## Zakres

1. Migracja `205_customer_contract`: RLS FORCE, izolacja, unique kod + `source_ref`. Zero kolumn treści / ciphertext / kary / kwoty.
2. POST/GET. Zły token → 400 (`oznaczenie` / `załadowca` / `odbiorca` / `obce`). Body z treścią/szyfrem → 422. Serwis nie importuje charges/parties/extraction i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`. ExtractionService nie importuje tego BC (`extraction-no-rates`).
3. UI `/customer-contracts`. How-to `docs/operator/umowa-klienta.md`; fold w `katalogi.md` (40 linii). Job OPS 118. OpenFGA `can_manage_customer_contracts`. Nazwy: `CustomerContractDesk` / `header-form.tsx` / `persistCustomerContract`. Brak podglądu PDF / przycisku unwrap.

## Poza zakresem

CT7 live · KEK/KMS · ciphertext · Langfuse PDF · CI1 extract · `sla_clause` · super-admin plaintext · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `customer_contract` z kodem, dwiema etykietami i `source_ref`, bez treści umowy
- [x] zły kod → 400 (`oznaczenie`); zła etykieta → 400 (`załadowca` / `odbiorca`); obce `source_ref` → 400 (`obce`)
- [x] POST z `body` / `blob_ciphertext` / `wrapped_dek` / `plaintext` **nie** zapisuje treści (422)
- [x] GET zwraca wpisy tenanta i **nie** zawiera pola treści umowy (kolumny nie ma)
- [x] Test izolacji nowej tabeli
- [x] ExtractionService / `extraction-no-rates` nie importuje `customer_contracts`
- [x] Zero float / marży / ciphertext / live HTTP / importu charges/parties/extraction

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; 422 na treść/szyfr; RLS + unique kod/`source_ref`; deny-list extract; zero treści umowy |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique); EXPLAIN ANALYZE w teście izolacji (Index Scan / unique) |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela unique; leftover ciphertext/KEK / CT7 live; UI inny kształt niż giełda (formularz nad tabelą, inne nazwy pól) |
| Proza operatora | 5–15 zdań | `umowa-klienta.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN CI9, GLOSSARY, MODULES CI9, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (272.0)

- Migracja `205_customer_contract` po `204_exchange_connector`. RLS FORCE.
- `CustomerContractService.persist_customer_contract` — katalog unique, bez importu charges/parties/extraction.
- UI `/customer-contracts`. `SHIPPED_CHARGE_ROUTES["272.0"]` = `/customer-contracts`. Job OPS 118.

## Leftover po 272.0

CI9 ciphertext + `wrapped_dek` + `tenant_contract_kek` (park KMS/unwrap, zakaz Omni-master, test super-admin = leftover bytea). CI1 `sla_clause`. CT7 p44/FourKites/Shippeo parked live (TO_VERIFY umowa). Reszta pinu 2026-09-08c.
