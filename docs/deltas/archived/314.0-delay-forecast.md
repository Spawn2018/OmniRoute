# Plaster 314.0 · CI4 `delay_forecast` HITL (katalog, bez wróżby)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CI / CI4 · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md)
**Zależy od:** 0.3 tenancy · 313.0 `sla_clause` (oś pin)
**Status:** Zamknięty `/noc` 2026-09-11. Nie wróżba punktowa. Nie GPS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CI4 / pin):** leftover CI2 spend (SQL FV vs charge) parked. leftover CI3 409/notice parked. leftover CI5 kara parked. Żywy plaster: tabela `delay_forecast` — HITL katalog prognozy opóźnienia jako dane. `forecast_code` + `horizon_hours` (1–168) + `p_late` (Decimal 0–1, dana, nie liczenie) + `source_ref`. UI `/delay-forecasts`. OpenFGA `can_manage_delay_forecasts`. Migracja `226_delay_forecast`. Job OPS. Deny-list extract.

**Łowca:** BRAK `delay_forecast`. ISTNIEJE `prediction_ledger` — CRPS/MAE, inny job. ISTNIEJE `sla_clause` — klauzula, nie prognoza.

**Odrzucone:** wróżba punktowa · GPS/AIS · float · marża · HTTP · UPDATE/DELETE · CI5 kara.

## Zakres

1. Migracja `226_delay_forecast` RLS FORCE.
2. POST/GET `/delay-forecasts`. OpenFGA. Deny-list.
3. UI `/delay-forecasts`. How-to. `SHIPPED_CHARGE_ROUTES["314.0"]`.

## Kryteria akceptacji

- [x] POST z kodem, horizon_hours, p_late, source_ref
- [x] zły kod → 400; zły horyzont → 400; złe p_late → 400; obce source_ref → 400
- [x] POST z amount/eta → 422
- [x] izolacja RLS; zero float/HTTP

## Leftover po 314.0

wróżba punktowa · GPS/AIS · CI2 spend · CI3 409 · CI5 kara · CI6 remedacja z kwotą

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET + RLS + OpenFGA + UI |
| Szybkość | N/A ten plaster | katalog HITL, nie ścieżka budżetu |
| SQL | N/A | list/order_by; brak nowego resolve |
| N+1 | OK | bez pętli z zapytaniem |
| Dług / człowiek | OK | wzorzec katalogu; nie mixin |
| Proza operatora | 5–15 zdań | `docs/operator/prognoza-opoznienia.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing Actions; lokalny gate |
| Minuty plastra | N/A | noc |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
