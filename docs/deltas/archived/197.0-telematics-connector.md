# Plaster 197.0 · V5 `telematics_connector` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) § V5 · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § 3.3
**Zależy od:** 0.3 tenancy (katalog per tenant) · 151.0 `resource` (powiązanie leftover)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie live GPS. Nie sekrety w plaintext.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover V5 hub + V5b + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V4 AIS wieży (S32) parked. leftover V2b myto→`charge` parked. leftover C3 live VIES parked. leftover C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO parked. leftover F2b–F4 parked. leftover `circle_sim` parked. leftover kg parked. F1 / C1 / C8 409 parked. leftover N3 countdown parked. `position_event` (lat/lng, poll, trip window) zostaje leftover. `resource_telematics_link` + silnik 3 dni roboczych zostaje leftover. `credential_ciphertext` / live GBOX/IKOL/Flotis/Wialon zostaje leftover. Żywy plaster: jedna tabela `telematics_connector` per tenant — **HITL znacznik adaptera GPS jako dane**, nie hub i nie track. Wiersz = `observation_kind` (`omni_telematic` | `external_api`) + `provider_code` (`gbox` | `ikol` | `flotis` | `wialon` | `other`) + `source_ref` (`tenant:manual` albo `fixture://telematics-connector/…`). Operator wpisuje reżim i dostawcę; serwis nie woła HTTP, nie pollowuje floty, nie woła LLM, nie liczy grace U4 i nie czyta `charge` / `resource` / `trip` / `position_event`. Zły reżim → 400 (`reżim`). Zły dostawca → 400 (`dostawca`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/telematics-connectors` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["197.0"]` = `/telematics-connectors`. OpenFGA: `can_manage_telematics_connectors`. Migracja `137_telematics_connector`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tracking_event` — ślad zlecenia, **nie** GPS. ISTNIEJE `observation_kind` w GLOSSARY — nazwa reżimu, **nie** tabela. ISTNIEJE `resource` 151.0 — flota, **nie** konto GPS. ISTNIEJE `entity_event` — append-only job, **nie** pozycja. ISTNIEJE `weather_observation` — pogoda, **nie** telematyka. BRAK tabeli `telematics_connector`. BRAK `position_event`. BRAK ciphertext adaptera. BRAK poll HTTP.

**Odrzucone:**

- `position_event` lat/lng / prędkość / paliwo / live track.
- Poll GBOX / IKOL / Flotis / Wialon / aggregator HTTP.
- `credential_ciphertext` i sekrety w logach.
- `resource_telematics_link` + `observation_status` idle/active/grace/stopped + 3 dni robocze U4.
- V4 AIS wieży (leftover S32).
- V5b `exchange_message` / quoted_amount.
- Myto → `charge` (leftover V2b).
- C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `137_telematics_connector` (po `136_free_time_clock`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind + provider.
2. POST/GET. Zły reżim / dostawca → 400. Serwis nie importuje resources / trips / charges / extraction i nie woła HTTP.
3. UI `/telematics-connectors`. `just api-types`. How-to `docs/operator/telematyka.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `ConnectorDesk` / `connectorWrite` / `persistConnectorMark`.

## Poza zakresem

position_event · live GPS · sekrety · 3 dni robocze · AIS · myto → charge · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [x] POST zapisuje `telematics_connector` z `observation_kind` i `provider_code`, bez HTTP
- [x] zły `observation_kind` → 400 (`reżim`)
- [x] zły `provider_code` → 400 (`dostawca`)
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / lat/lng / live HTTP / scrape / ciphertext

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST reżim + dostawca; 400 `reżim` / `dostawca` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_telematics_connector_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover position_event / ciphertext / 3 dni U4 |
| Proza operatora | 5–15 zdań | `telematyka.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN V5, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
