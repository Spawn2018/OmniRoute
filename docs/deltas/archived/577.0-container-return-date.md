# Plaster 577.0 · T3 leftover — HITL `container.return_date`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`pickup_date` / `return_date` / `gate_in_date` / `delivery_date` / `unload_date`, typ `date`) · [576.0](../archived/576.0-container-pickup-date.md)

**Zależy od:** 153.0 `container` · 576.0 `pickup_date`

**Status:** Zamknięty `/noc` 2026-09-19. Nie trzy pozostałe daty. Nie cutoff timestamptz. Nie `temp_min`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po `pickup_date` karta T3 zostawia `return_date`. W `backend/app` kolumny nie ma. Ten sam typ `date`: ISO `YYYY-MM-DD`, pusty string → `null`, POST bez pola → `null`. Zła wartość → 400 (`data zwrotu`). Nie liczy countdown. Nie mutuje `pickup_date`.
- **Wybrane:** `require_container_return_date` w domenie kontenera (ten sam kształt co `require_container_pickup_date`, osobna etykieta). UI `/shipments`, string (`optionalToken`). `SHIPPED["577.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `457_container_return_date` (po `456_container_pickup_date`), kolumna `Date` nullable. Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `gate_in_date` / `delivery_date` / `unload_date` (`REJECTED` w tym plasterze — węższa data). Reuse cutoffu timestamptz (`REJECTED`). `temp_min` (`REJECTED`). N3 countdown (`REJECTED` → park). float / ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `return_date` w `backend/app`. ISTNIEJE `require_container_pickup_date`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `457_container_return_date`: kolumna nullable `Date`.
2. Domain `require_container_return_date` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["577.0"]`.

## Poza zakresem

`gate_in_date` · `delivery_date` · `unload_date` · cutoff timestamptz · `temp_min` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `return_date="2026-09-20"` zapisuje date
- [x] data z godziną / zły format / nie-string → 400 (`data zwrotu`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero countdown / mutacja `pickup_date`

## Leftover

`gate_in_date` i reszta dat SPEED · `temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL return_date date |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN Date |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | ISO date lokalnie; sufit 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
