# Plaster 177.0 · G2.8 `tender_consortium_member`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.8 · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) leftover consortium
**Zależy od:** 169.0 `tender` · `party` (członek konsorcjum)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.8–G2.18):** jedna tabela `tender_consortium_member` per tenant — **fotel konsorcjum z `source_ref`**, nie extract RFP HITL i nie TED live HTTP. Wiersz = `tender_id` (FK tenanta do `tender`) + `party_id` (FK tenanta do `party`) + `seat_code` (`lead` | `member`) + `source_ref` (`tenant:manual` albo `fixture://tender-consortium-member/…`). Operator wpisuje członka; serwis nie woła LLM i nie czyta `charge`. Zły `seat_code` → 400 (`fotel`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id, party_id)`. GET listy SQL. Serwis nie importuje tenders / parties / charges / quotations / extraction. API składa odczyt nagłówka i kontrahenta (`TenderService.get_board`, `PartyService.get_party`). UI `/tender-consortium-members` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["177.0"]` = `/tender-consortium-members`. OpenFGA: `can_manage_tender_consortium_members`. Migracja `117_tender_consortium_member`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `network_member` — członek sieci, nie konsorcjum przetargu. ISTNIEJE `shipment_stakeholder` — strona zlecenia, nie fotel G2. ISTNIEJE `tender` — nagłówek; **nie** dokładamy członków jako JSON tam. BRAK `tender_consortium_member`. BRAK `/tender-consortium-members`.

**Odrzucone:**

- Extract RFP HITL (leftover G2.9+; nic z LLM do bazy bez człowieka).
- Prospecting HITL / bid/no-bid silnik / award four-eyes / TED / CO₂ (leftover G2.9–G2.18).
- Auto-award / kwota / druga marża.
- Float / T-SQL / live HTTP / scrape TED.

## Zakres

1. Migracja `117_tender_consortium_member`: RLS, izolacja, indeks `(organization_id, tender_id)`, unique `(organization_id, tender_id, party_id)`, CHECK `seat_code`.
2. POST/GET. Zły `seat_code` / `source_ref` → 400. API składa odczyt `tender` i `party`, serwis nie importuje tenders ani parties.
3. UI `/tender-consortium-members`. `just api-types`. How-to `docs/operator/konsorcjum-przetargu.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `SeatDesk` / `seatWrite` / `persistSeatMark`.

## Poza zakresem

G2.9–G2.18 extract/four-eyes/TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · LLM · kwota na wierszu · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `tender_consortium_member` z fotelem i `source_ref` na istniejącym `tender` i `party`, bez auto-award
- [x] zły `seat_code` → 400
- [x] GET zwraca fotele tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST fotel + `source_ref`; 400 `fotel` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `(organization_id, tender_id)` — `ix_tender_consortium_member_org_tender` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover G2.9–G2.18; jscpd 0 na plikach plastra |
| Proza operatora | 5–15 zdań | `konsorcjum-przetargu.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.8, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
