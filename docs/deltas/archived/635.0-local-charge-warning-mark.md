# Plaster 635.0 ? leftover P4c warning stance HITL

**Spec ¼ród³owa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 ? [166.0](../archived/166.0-local-charge.md) ? [633.0](../archived/633.0-local-charge-carrier-service.md)
**Zale¿y od:** 633.0 `carrier_label`/`service_label` ju¿ s±
**Status:** Zamkniêty `/noc 7` 2026-09-23. Nie warning-jako-fakt. Nie matching.

## Wybrane / odrzucone / dlaczego

**Wybrane (wê¿sze z leftover P4c ?warning braku dop³aty ? fakt?):** katalog `local_charge` zostaje prawd± o dop³acie Decimal. Silnik ?brak THC = fakt / 409 / auto-linia? oraz P5b?c SQL na `charge` zostaj± park. ¯ywy plaster: **nowa tabela** `local_charge_warning_mark` per tenant ? HITL stance ostrze¿enia o lukach dop³aty. Wiersz = `mark_code` (snake 1?32) + `warning_kind` (`warn`|`hold`|`waived`|`other`) + `source_ref` (`tenant:manual` albo `fixture://local-charge-warning/?`). Operator zapisuje, jak traktuje brak dop³aty (ostrze¿ / wstrzymaj / zwolnij / inne); serwis **nie** porównuje do `local_charge`, nie zwraca 409 i nie tworzy `charge`. Z³y kind ? 400 (`ostrzezenie`). Obce `source_ref` ? 400 (`obce`). Duplikat `mark_code` w tenancie ? 409. GET listy SQL. Serwis nie importuje `local_charges` / charges / quotations. UI `/local-charge-warning-marks`. `SHIPPED_CHARGE_ROUTES["635.0"]` = `/local-charge-warning-marks`. OpenFGA: `can_manage_local_charge_warning_marks`. Migracja `507_local_charge_warning_mark` po `506_party_contact_consent`. Mar¿a zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 166.0 / 633.0 ?P4c warning ? fakt? ? tu tylko HITL stance, nie silnik braków. ¬ród³o: `docs/deltas/archived/166.0-local-charge.md`; PLAN wiersz P4; `docs/ops/docs-debt.md`.

**£owca:** ISTNIEJE `local_charge` ? dop³ata Decimal, nie stance. ISTNIEJE `network_print_gate_mark` ? brama D9c, nie P4. BRAK `local_charge_warning_mark`. BRAK `/local-charge-warning-marks`. BRAK warning-jako-fakt.

**Odrzucone:**

- warning braku dop³aty jako fakt / auto-linia `charge`
- matching SQL vs `local_charge` / 409 na wycenie
- P5b?c SQL wariancji na `charge`
- FK UUID do `local_charge` / `quotation`
- float / mar¿a / live HTTP

## Zakres

1. Migracja `507_local_charge_warning_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `warning_kind`.
2. POST/GET. Z³y kind / `source_ref` ? 400. Duplikat kodu ? 409. Amount ? 422.
3. UI `/local-charge-warning-marks` + zdanie w how-to dop³at lokalnych.

## Poza zakresem

warning-jako-fakt ? matching ? 409 wyceny ? P5b?c ? FK ? live HTTP ? float

## Plan plików

- `backend/alembic/versions/507_local_charge_warning_mark.py`
- `backend/app/models/local_charge_warning_mark.py`
- `backend/app/domain/local_charge_warning_mark.py`
- `backend/app/repositories/local_charge_warning_marks/`
- `backend/app/services/local_charge_warning_marks/` (+ AGENTS.md)
- `backend/app/api/local_charge_warning_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/local-charge-warning-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to ? GLOSSARY ? CURRENT ? PLAN ? VISION ? PROGRESS ? docs-debt

## Kryteria akceptacji

- [x] POST z `warning_kind=warn` zapisuje wiersz; `local_charge` i `charge` bez zmian
- [x] kind spoza `warn`|`hold`|`waived`|`other` ? 400 (`ostrzezenie`); amount ? 422; duplikat kodu ? 409
- [x] serwis nie importuje `local_charges` / `charges` / `quotations` i nie wo³a HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / mar¿y / warning-jako-fakt

## Leftover

warning-jako-fakt (compose) ? matching braków ? P5b?c SQL ? FK ? live HTTP
