# Plaster 193.0 · B0b/V1 `prediction_ledger`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § B0b · § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) § N7
**Zależy od:** 0.3 tenancy (katalog per tenant) · 133.0 `entity_event` (append-only jest, nie ten ledger)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie silnik CRPS. Nie scoring osoby.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover C3–C9 + F2b–F4 + G2.20–G2.22 + F1 + C1 + C8 409 + B0b + V1):** leftover C3 live VIES/GUS parked (HTTP). leftover C4 eCMR parked. leftover C9 Trans.eu parked. leftover C2 AIS/PUESC parked. leftover C6 BDO parked. leftover F2b Decimal parked. leftover F3 period lock parked. leftover F4 CAMT parked. leftover `circle_sim` parked. leftover kg/CBAM parked. leftover packing group / live IMO parked. F1 live FA(3) parked. C1 filing parked. C8 409 parked. B0b `plan_snapshot` / własne TT z actuals zostają leftover (V8 / T1). Indeks paliwa już jest (`fuel_index` P3). Żywy plaster: jedna tabela `prediction_ledger` per tenant — **HITL wiersz predykcji z przedziałem N7 i metryką CRPS/MAE jako dane**, nie punktowa wróżba i nie silnik, który liczy CRPS. Wiersz = `prediction_kind` (`eta` | `transit` | `disrupt`) + `horizon_code` (`h1h` | `h6h` | `h24h` | `h7d`) + `interval_low` / `interval_high` (`Numeric(14,4)`, minuty, `high >= low`) + `crps` + `mae` (`Numeric(14,4)`, `>= 0`, NOT NULL) + `model_code` (snake 2–32) + `source_ref` (`tenant:manual` albo `fixture://prediction-ledger/…`). Operator wpisuje przedział i metrykę po fakcie; serwis nie liczy CRPS/MAE, nie woła sieci, nie woła LLM, nie scoruje osoby i nie czyta `charge` / `trip` / `stop`. Brak albo ujemne CRPS → 400 (`crps`). Brak MAE → 400 (`mae`). Zły kind → 400 (`rodzaj`). Zły horyzont → 400 (`horyzont`). `interval_high` < `interval_low` → 400 (`przedział`). Obce `source_ref` → 400 (`obce`). Append-only INSERT. Unique `(organization_id, source_ref)`. GET listy SQL. UI `/prediction-ledgers` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["193.0"]` = `/prediction-ledgers`. OpenFGA: `can_manage_prediction_ledgers`. Migracja `133_prediction_ledger`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `entity_event` — append-only zdarzenie inquiry/quote, **nie** predykcja i **nie** CRPS. ISTNIEJE `fuel_index` — katalog FSC/BAF/CAF, **nie** ledger ETA. ISTNIEJE `outbox_event` — szyna między BC, **nie** scorecard. ISTNIEJE `party_scorecard` — snapshot kontrahenta, **nie** kalibracja modelu. BRAK tabeli `prediction_ledger`. BRAK `plan_snapshot`. BRAK kolumny CRPS. BRAK silnika champion/challenger.

**Odrzucone:**

- C3 live VIES/GUS (HTTP).
- C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO.
- F2b Decimal / F3 period lock / F4 CAMT.
- `circle_sim` / kg / F1 KSeF / C1 filing / C8 409.
- packing group / live IMO leftover 192.0.
- `plan_snapshot` / what-if V8 / TT z actuals.
- Silnik CRPS/MAE w Pythonie (LLM albo NumPy).
- Champion/challenger, drift, GPS, AIS wieży, scoring osoby.
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `133_prediction_ledger` (po `132_dangerous_good_adr`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist + `crps`/`mae` >= 0 + `interval_high >= interval_low`.
2. POST/GET append-only. Brak CRPS → 400. Serwis nie importuje trips / stops / charges / extraction i nie woła HTTP. Brak UPDATE/DELETE.
3. UI `/prediction-ledgers`. `just api-types`. How-to `docs/operator/ledger-predykcji.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `LedgerDesk` / `ledgerWrite` / `persistLedgerMark`.

## Poza zakresem

C3 live lookup · C4 eCMR · C9 Trans.eu · C2 AIS · C6 BDO · F2b · F3 · F4 · G2.20 · kg · F1 · C1 · C8 409 · packing group · live IMO · plan_snapshot · TT actuals · silnik CRPS · champion/challenger · GPS · scoring osoby · kwota · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `prediction_ledger` z przedziałem, `crps` i `mae`, bez wyliczenia metryki w serwisie
- [x] brak `crps` → 400 (`crps`)
- [x] zły `prediction_kind` → 400 (`rodzaj`)
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM-predykcji / live HTTP / scrape / scoringu osoby

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST przedział + CRPS/MAE; 400 `crps` / `rodzaj` / `przedział` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_prediction_ledger_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover `plan_snapshot` / champion / drift |
| Proza operatora | 5–15 zdań | `ledger-predykcji.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN B0b/V1, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
