# Plaster 261.0 · T6 mapa planistyczna

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T6 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § poza Falą T · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) § N10 · [decyzja-operatora-wizja.md](../../analysis/decyzja-operatora-wizja.md) #9
**Zależy od:** 150.0 `stop` · 152.0 `trip` · 94.0 watchtower mapa lazy (inny job)
**Status:** Zamknięty `/noc` 2026-09-10. Nie GPS. Nie OSM. Nie SQL na `charge`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T6 mapa + leftover T6 board 4 widoki + leftover N10 + leftover T6b select&drop + leftover T6c pre-planning + leftover T6d polygon + leftover X8 podkłady + leftover T4b SQL `charge` + leftover T5 + leftover T7b + leftover N3 + S53 + PIN):** leftover 4 widoki Timeline/Blocks/Table/Legs parked. leftover N10 klawiatura parked. leftover T6b–d parked. leftover X8 katalog podkładów parked. leftover T4b parked. leftover T5 parked. leftover T7b parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **trasa `/planning`** — leniwy panel mapy (chunk poza initial 250 kB, dopiero po kliknięciu), overlay etykiet z GET `trip` tenanta (numer / `route_label`), bez lat/lng i bez kafelków. Nie ten sam plik co `watchtower/map-panel`. Brak migracji. `SHIPPED_CHARGE_ROUTES["261.0"]` = `/planning`. Nowy job OPS (109). `BUSINESS_LISTS.planning`. Sidebar i paleta. OpenFGA bez nowej relacji (`can_manage_shipments` jak T2). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `trip` / `stop`. ISTNIEJE wieża — leniwy panel mapy bez kafelków, job wyjątków. ISTNIEJE `fetchStops(shipmentId)` — N+1 przy liście zleceń. BRAK `/planning`. BRAK leaflet / mapbox w `package.json`. BRAK 4 widoków boardu.

**Odrzucone:**

- leaflet / mapbox / `tile.openstreetmap.org` / live GPS / AIS.
- 4 widoki boardu · N10 · select&drop · pre-planning · rectangle/polygon · X8 BYO.
- nowa tabela · GET wszystkich `stop` (brak listy tenanta bez `shipment_id`).
- SQL na `charge` · T5 · Auth0 · live HTTP.

## Zakres

1. `frontend/src/routes/planning.tsx` + strona z bramką mapy (`lazy` + `Suspense`). Nie `create_table`.
2. Overlay GET `/trips` jako etykiety. Zero float / lat / lng.
3. `BUSINESS_LISTS` / job OPS 109 / `SHIPPED_CHARGE_ROUTES["261.0"]` = `/planning`. How-to; fold w `katalogi.md` (40 linii).

## Poza zakresem

4 widoki · N10 · T6b–d · X8 · leaflet · GPS · T4b SQL `charge` · T5 · Auth0 · live HTTP · nowy list `stop`

## Kryteria akceptacji

- [x] GET `/planning` pokazuje bramkę mapy; chunk ładuje się po kliknięciu (`data-planning="map"`)
- [x] Panel nie zawiera `leaflet` / `mapbox` / `tile.openstreetmap.org`
- [x] Overlay to istniejące `trip`, bez lat/lng
- [x] `SHIPPED_CHARGE_ROUTES["261.0"]` = `/planning`; job OPS ma tę trasę
- [x] Zero nowej tabeli / float / marży / live HTTP / 4 widoków

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/planning` bramka + lazy overlay etykiet `trip`; zero kafelków |
| Szybkość | N/A ten plaster | lazy chunk, nie ścieżka z budżetu AGENTS |
| SQL | N/A | brak nowej migracji i zapytania; GET `/trips` bez zmian |
| N+1 | OK | jeden GET listy `trip`; bez `fetchStops` w pętli |
| Dług / człowiek | OK | inny plik niż wieża; leftover 4 widoki / N10 / T6b–d / X8 |
| Proza operatora | 5–15 zdań | `planowanie.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T6, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
