# Plaster 28.0 · M-35 Zlecenie (`shipment` tablica odczytu)

**Spec źródłowa:** [docs/spec/shipment.md](../../spec/shipment.md)  
**Zależy od:** 5.1 `quotation.party_id` · 22.0 `offer_acceptance` · 0.6 DataTableShell

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie tracking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 5 M-35):** zlecenie **nie ma tabeli** w repo. Operator widzi wyceny z `party_id` na `/shipments` jako pracę handlową. Token `shipment` (UI, nie tabela). Żywy ID **M-35**. 22.0 i 27.0 zostają; 28.0 = jeden odczyt pod nazwą zlecenia.

**Łowca:** BRAK `shipment` / `shipment_leg` w modelach. ISTNIEJE: `quotation` z `party_id`, `quotationAcceptancePending`. PODOBNE: 15.0 `/finance`, 27.0 `/notifications`. GLOSSARY już ma `shipment`. Makieta design ma HBL / odcinki / dwie marże — leftover, nie ten plaster.

**Odrzucone:**

- Tabela `shipment` + numer `GD/2026/…` — zgadywanie schematu (CURRENT).
- `shipment_leg` / tracking / wyjątki — M-36 / M-37.
- Druga marża (plan vs prognoza) — `charge` zostaje prawdą; JS nie odejmuje.
- LLM nadający numer zlecenia.

## Zakres

Read-only `/shipments`. `data-shipment="board"`. Brak migracji. Linki do `/quotations`. `<Money/>` z pól wyceny.

## Poza zakresem

Tabela `shipment` · odcinki · HBL · kontenery · Temporal · ExtractionService

## Ustalenia

- Token: `shipment` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations`.
- RLS: bez nowej tabeli.
- Job: `/shipments`. `SHIPPED_CHARGE_ROUTES["28.0"]` = `/shipments`. OPS_JOBS +1.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/shipments` ma `data-shipment="board"` i woła `fetchQuotations` oraz filtr `party_id`
- [x] Brak nowej migracji; brak odejmowania kwot w JS; brak `shipment_leg`
- [x] `SHIPPED_CHARGE_ROUTES["28.0"]` = `/shipments`
- [x] `just dup` ≤ 3% (2,71%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | tablica wycen z `party_id`; brak tabeli `shipment` |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | `just dup` 2,71%; tabela zlecenia / odcinki / tracking zostają |
| Docs/OS | PRZESZŁO | spec `shipment.md`; CURRENT → Plan M-36 |

## Knowledge

Nie RAG. Nie silnik trackingu.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela zlecenia, odcinki i tracking świadomie leftover.
