# Plaster 559.0 Â· F11 leftover â€” HITL `postal_epo_mark`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala F Â· F11

**ZaleĹĽy od:** tenancy Â· wzorzec HITL mark Â· F11 `postal_dispatch_mark` (obok)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy F11 leftover. Nowa tabela `postal_epo_mark`: `mark_code` (snake 2â€“32) + `epo_kind` `register|label|track|other` + `source_ref`. Append-only. RLS. Katalog stancji EPO/PP â€” nie live PP HTTP, nie e-DorÄ™czenia.
- **Wybrane:** POST/GET `/api/v1/postal-epo-marks`. OpenFGA `can_manage_postal_epo_marks`. UI `/postal-epo-marks`. `SHIPPED["559.0"]`.
- **Odrzucone:** live PP / e-DorÄ™czenia HTTP (`REJECTED` â†’ leftover). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / ranking SQL / PUESC.

**Ĺowca:** BRAK `postal_epo_mark`. ISTNIEJE `postal_dispatch_mark` (kanaĹ‚ EN/USS/EPO). ISTNIEJE `e_doreczenia_mark` / `e_delivery_mark`.

## Zakres

1. Migracja `439_postal_epo_mark` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["559.0"]` â†’ `/postal-epo-marks`.

## Poza zakresem

live PP Â· e-DorÄ™czenia Â· AI3 Â· G0 Â· kwota

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount

## Leftover

live PP Â· e-DorÄ™czenia HTTP
