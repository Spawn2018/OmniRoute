# Plaster 191.0 · EXP0.8 `cargo_claim` CMR + OS&D

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP0.8 / EXP1 `cargo_claim`
**Zależy od:** 113.0 `cargo_claim` (reklamacja na zleceniu już jest)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie kwota. Nie silnik 7/21/365.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover C3–C9 + F2b–F4 + G2.20–G2.22 + F1 + C1 + C8 409):** leftover C3 live VIES/GUS parked (HTTP). leftover C4 eCMR/eFTI parked (2027 / Selenium). leftover C9 Trans.eu parked. leftover C2 AIS/PUESC parked. leftover C6 BDO parked. leftover F2b Decimal parked. leftover F3 period lock parked. leftover F4 CAMT parked. leftover `circle_sim`/km/P parked. leftover kg/CBAM parked. F1 live FA(3) parked. C1 filing XML parked. C8 409 parked. Żywy plaster: **pogłębienie istniejącej** `cargo_claim` — HITL słownik OS&D i dwa terminy CMR jako dane, nie kalkulator dni i nie nowa tabela. Wiersz dostaje `damage_code` (`overage` | `shortage` | `damage` | `loss`) + `cmr_notice_window` (`notice_7` | `notice_21`) + `notice_due_at` (`DATE`) + `suit_due_at` (`DATE`). Operator wpisuje kod szkody i dni, które sam odczytał z konwencji; serwis nie dodaje 7/21/365 do daty dostawy, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. 365 żyje jako `suit_due_at`, nie jako `timedelta`. Zły `damage_code` → 400 (`osd`). Złe okno → 400 (`okno`). Zła data zawiadomienia → 400 (`zawiadomienie`). Zła data pozwu → 400 (`pozew`). `suit_due_at` przed `notice_due_at` → 400 (`kolejność`). `claim_kind` i `source_ref` zostają z 113.0. GET listy SQL bez filtra overdue. UI zostaje `/claims` (pola w tym samym formularzu). `SHIPPED_CHARGE_ROUTES["191.0"]` = `/claims`. OpenFGA bez nowej relacji (`can_manage_cargo_claims`). Migracja `131_cargo_claim_cmr`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `cargo_claim` — `claim_kind` damage/shortage/other, **nie** słownik OS&D i **nie** terminy CMR. ISTNIEJE `tender.deadline_at` — termin przetargu, **nie** reklamacja. ISTNIEJE `operational_exception` — wyjątek operacyjny, **nie** OS&D. ISTNIEJE `carrier_inquiry.no_reply_after` — cisza zapytania, **nie** CMR. BRAK `damage_code` / `notice_due_at` / `suit_due_at` na `cargo_claim`. BRAK silnika Deadline Engine. BRAK `liable_party_id`. BRAK evidence GPS/temp/photo.

**Odrzucone:**

- C3 live VIES/GUS (HTTP).
- C4 eCMR/eFTI / Selenium.
- C9 Trans.eu snapshot / scrape opinii.
- C2 AIS/AES/Intrastat / live PUESC.
- C6 BDO/KPO / MOS HTTP.
- F2b Decimal / F3 period lock / F4 CAMT.
- `circle_sim` / km / P (leftover G2.20–G2.22).
- kg / CBAM (leftover G2.15–G2.18).
- F1 live FA(3) / C1 SENT XML / C8 409.
- `timedelta` 7/21/365 od daty dostawy (Deadline Engine).
- `evidence_gps` / `evidence_temp` / `evidence_photo` (leftover EXP1).
- `liable_party_id` (szkic S11).
- Kwota / druga marża / float / T-SQL / live HTTP / scrape / LLM.

## Zakres

1. Migracja `131_cargo_claim_cmr` (po `130_carbon_method`): kolumny NOT NULL po backfillu starych wierszy (`damage_code` z `claim_kind`, okno `notice_7`, daty = `created_at::date` — nie silnik). CHECK na `damage_code` i `cmr_notice_window`.
2. POST wymaga nowych pól. Zły kod / okno / data → 400. Serwis nie importuje shipments / charges / quotations / extraction i nie woła HTTP. API nadal składa odczyt `shipment`.
3. UI `/claims`. `just api-types`. How-to `docs/operator/reklamacja.md` (uzupełnić, bez nowego pliku); fold w `katalogi.md` (40 linii, bez nowej linii).

## Poza zakresem

C3 live lookup · C4 eCMR · C9 Trans.eu · C2 AIS · C6 BDO · F2b Decimal · F3 noty · F4 CAMT · G2.20 circle_sim · kg · F1 KSeF · C1 filing · C8 409 · Deadline Engine · evidence · liable_party · kwota · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `cargo_claim` z `damage_code`, `cmr_notice_window`, `notice_due_at` i `suit_due_at`, bez kwoty
- [x] zły `damage_code` → 400 (`osd`)
- [x] złe `cmr_notice_window` → 400 (`okno`)
- [x] GET zwraca terminy tenanta
- [x] Test izolacji tabeli (wiersz z nowymi kolumnami)
- [x] Zero float / marży / timedelta / LLM / live HTTP / scrape / silnika 7/21/365

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST OS&D + okno + dwa dni; 400 `osd` / `okno` / `kolejność`; GET lista SQL |
| Szybkość | N/A ten plaster | INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` — `ix_cargo_claim_organization_id` |
| N+1 | OK | GET jedna lista; POST = odczyt zlecenia + INSERT |
| Dług / człowiek | OK | kolumny na istniejącej tabeli; leftover Deadline Engine / evidence / S11 |
| Proza operatora | 5–15 zdań | `reklamacja.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN EXP0.8, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
