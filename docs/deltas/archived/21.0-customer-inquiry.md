# Plaster 21.0 · M-28 Zapytania od klientów (`customer_inquiry` przy `quotation`)

**Spec źródłowa:** [docs/spec/customer-inquiry.md](../../spec/customer-inquiry.md)  
**Zależy od:** 2.0/5.1 `quotation` · 5.0 `party` · 0.25 `<Money/>`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie IMAP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-28):** zapytanie klienta **to dziś wycena z `party_id`**. Panel na `/quotations` grupuje istniejące wiersze po kontrahencie. Token `customer_inquiry` (UI, nie tabela). Żywy ID **M-28**.

**Łowca:** BRAK `customer_inquiry` / `rfq` / `enquiry` w kodzie i GLOSSARY. ISTNIEJE: `quotation.party_id` + filtr listy + `quotationPartyIds` + katalog `party`. PODOBNE: 17.0/19.0 panele odczytu na `/quotations`.

**Odrzucone:**

- Tabela zapytań / status RFQ — zgadywanie schematu (CURRENT).
- IMAP / M-32 — Fala 4; parked wcześniej niż skrzynka.
- Auto-INSERT `quotation` z tekstu / LLM — HITL; model nie liczy.
- M-30 wysyłka do agenta/armatora — kolejna pozycja Fali 3.
- Sumowanie `amount` w JS — LLM/JS nie liczy kwot.

## Zakres

Panel na `/quotations`. `data-customer-inquiry="trail"`. Brak migracji. Brak nowego routera.

## Poza zakresem

Tabela RFQ · skrzynka · workflow statusu · ExtractionService · mnożenie/suma kwot

## Ustalenia

- Token: `customer_inquiry` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations` na liście.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["21.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/quotations` ma `data-customer-inquiry="trail"` i grupuje wyceny po `party_id`
- [x] Kwota w grupie przez `<Money/>`, nie suma w JS
- [x] Brak nowej migracji i brak `POST` zapytań
- [x] `just dup` ≤ 3% (2,70%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | panel grupuje istniejące wyceny per party; `<Money/>` per wiersz; brak sumy |
| Szybkość | N/A ten plaster | brak nowej ścieżki wyceny |
| Dług w diffie | leftover | `just dup` 2,70%; tabela RFQ i IMAP zostają |
| Docs/OS | PRZESZŁO | spec `customer-inquiry.md`; CURRENT → Plan M-29 |

## Knowledge

Nie RAG. Wzorzec = 17.0/19.0 panel odczytu.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela RFQ i IMAP świadomie leftover.
