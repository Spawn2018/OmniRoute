# Plaster 122.0 · leftover S7 UN na RFQ (M-52)

**Spec źródłowa:** [docs/spec/dangerous-good.md](../../spec/dangerous-good.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S7  
**Zależy od:** 7.0 `dangerous_good` · 70.0 `commodity_code_id` na RFQ/wycenie · 67.0 `customer_rfq`

**Status:** Zamknięty `/noc` 2026-09-04. Żywy ID **M-52** (FK na M-28/M-21). Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam wzór co 70.0 HS: nullable `dangerous_good_id` na `customer_rfq` i `quotation`. FK złożone tenanta. API składa katalog (`DangerousGoodService.get` po id). Serwis katalogu nie zapisuje RFQ/wyceny. Serwis RFQ i wyceny nie importują `dangerous_goods`. Kwota wyceny nadal z `QUOTE_FROM_CURRENT_SQL` — UN nie filtruje stawki. LLM nie nadaje numeru UN ani klasy IMDG. UI: `/mail` „Podpnij UN”, `/quotations` picker. `SHIPPED_CHARGE_ROUTES["122.0"]` = `/mail`. OPS_JOBS 57 → 58.

**Łowca:** ISTNIEJE 7.0 katalog UN + 70.0 HS na RFQ. BRAK `dangerous_good_id`. PODOBNE: `inherit_rfq_commodity_code_id` + PATCH HS.

**Odrzucone:**

- Nadawanie UN/klasy przez LLM / extract.
- Import `dangerous_goods` w serwisie RFQ/wyceny; zapis `quotation` z `DangerousGoodService`.
- Filtrowanie `rate_line` po UN. Live IMO.
- Auth0 · portale · OTel · katalog 71–212 · konsument outboxa.

## Zakres

1. Migracja `071_dangerous_good_on_rfq`: `uq_dangerous_good_org_id`; `dangerous_good_id` na RFQ i wycenie; FK złożone; indeksy. Bez nowej tabeli, bez nowego RLS.
2. `get` w katalogu UN. PATCH RFQ z `dangerous_good_id`. POST wyceny z opcjonalnym id; puste + RFQ = dziedziczenie.
3. Test izolacji: UN tenanta B nie siada na RFQ A. Nieznany id = błąd. Kwota ze stawki.
4. `/mail` Podpnij UN. `/quotations` picker. How-to w `poczta.md` (nie nowa linia `katalogi.md`).

## Poza zakresem

Live IMO · ADN/ADR osobno · send · F9.1

## Ustalenia

- Token katalogu zostaje `dangerous_good`. HITL extract bez zmian.
- LLM nie liczy i nie nadaje UN. `charge` zostaje prawdą o marży.

## Schemat

`customer_rfq.dangerous_good_id` nullable. `quotation.dangerous_good_id` nullable. FK `(organization_id, dangerous_good_id)` → `dangerous_good`.

## Kryteria akceptacji

- [x] FK tenanta; izolacja
- [x] Kwota z INSERT…SELECT, nie z UN / LLM
- [x] Zero zapisu quotation z serwisu katalogu UN
- [x] OPS_JOBS = 58; `122.0` → `/mail`
- [x] `katalogi.md` ≤ 40

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | FK UN na RFQ/wycenie + Podpnij UN; kwota ze stawki |
| Szybkość | N/A ten plaster | etykieta, nie ścieżka 50k stawek |
| SQL | N/A | ten sam INSERT…SELECT; bind UUID; bez JOIN `dangerous_good` |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | druga kopia inherit/PATCH; fieldset UN ≠ label HS |
| Proza operatora | 5–15 zdań | `poczta.md` + zdanie w `ścieżka-pieniędzy.md`; `katalogi.md` 40 |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
