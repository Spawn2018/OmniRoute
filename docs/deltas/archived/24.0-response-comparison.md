# Plaster 24.0 · M-31 Porównanie odpowiedzi (`response_comparison` przy `quotation`)

**Spec źródłowa:** [docs/spec/response-comparison.md](../../spec/response-comparison.md)  
**Zależy od:** 13.0 `channel_quote` · 2.0/5.1 `quotation` · 18.0 resolve · 23.0 ślad

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie spread w JS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-31):** porównanie odpowiedzi **to dziś zestawienie istniejących kwot**. Panel na `/quotations` składa lane wyceny (POL/POD) z wszystkimi `channel_quote` na tej samej parze portów — także innych armatorów niż `quotation.party_id`. Token `response_comparison` (UI, nie tabela). Żywy ID **M-31**. 18.0 zostaje resolve jednej oferty; 23.0 zostaje ślad per party; 24.0 = zestawienie geograficzne.

**Łowca:** BRAK `response_comparison` / tabeli spread / odejmowania kwot w kodzie i GLOSSARY. ISTNIEJE: 18.0 `resolveChannelQuote` + `<Money/>` osobno; 23.0 `quotationCarrierInquiries` (party+POL+POD). PODOBNE: 21.0 grupowanie wycen.

**Odrzucone:**

- Tabela wyniku / kolumna spread — zgadywanie schematu (CURRENT).
- Odejmowanie `quotation.amount − channel.amount` w JS — PLAN: LLM nie liczy; nie drugi `margin()`. Leftover (Decimal w SQL/Python, nie ten plaster).
- Zapis do `rate_line` / `charge` / mutacja kwoty wyceny.
- Import `channel_quotes` z `quotations` service.
- Live HTTP / tabela RFQ — leftover 13.0/23.0.

## Zakres

Panel na `/quotations`. `data-response-comparison="lanes"`. Brak migracji. Brak endpointu spread.

## Poza zakresem

Silnik różnicy Decimal · katalog won/lost · IMAP · ExtractionService · live HTTP

## Ustalenia

- Token: `response_comparison` (GLOSSARY, UI).
- Klucz zestawienia: `origin_port_id` + `destination_port_id`. Nie `party_id` (wycena = klient, oferta kanału = armator).
- OpenFGA: istniejące `can_manage_rate_lines` na liście `channel_quote`; `can_manage_quotations` na liście wycen.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["24.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/quotations` ma `data-response-comparison="lanes"` i woła `quotationResponseComparisons`
- [x] Kwoty wyceny i kanału przez `<Money/>`; brak odejmowania / `parseFloat`
- [x] Zestawienie łączy `channel_quote` po POL/POD także przy innym `party_id` niż wycena
- [x] Brak nowej migracji
- [x] `just dup` ≤ 3% (2,67%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | zestawienie POL/POD; `<Money/>`; brak odejmowania |
| Szybkość | N/A ten plaster | odczyt istniejącego katalogu |
| Dług w diffie | leftover | `just dup` 2,67%; silnik różnicy Decimal zostaje |
| Docs/OS | PRZESZŁO | spec `response-comparison.md`; CURRENT → Plan M-32 |

## Knowledge

Nie RAG. Wzorzec = 18.0/23.0 odczyt katalogu przy wycenie. Nie spread.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Silnik różnicy Decimal i tabela wyniku świadomie leftover.
