# Plaster 246.0 · leftover `quote_recorded` z API

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Oś leftoverów `/noc` (2a/2b) · [245.0](../archived/245.0-inquiry-sent-event.md) leftover `quote_recorded` · [133.0](../archived/133.0-entity-event.md)
**Zależy od:** 245.0 `inquiry_sent` z API
**Status:** Zamknięty `/noc` 2026-09-10. Nie live HTTP. Nie Temporal.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `quote_recorded`/`channel_quote`/batch + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. POST `channel_quote` parked. batch parked. PATCH parked. Żywy plaster: **to samo składanie API co 244.0/245.0** — po POST `/carrier-inquiries` (jeden wiersz), gdy `status=answered`, API woła `EntityEventService.create_event` (`event_kind=quote_recorded`, `subject_kind=carrier_inquiry`). Rozszerzyć mapper w `_append_queued_event` o `answered`. **CarrierInquiryService nie importuje `entity_events`.** `draft` / `queued` / `sent` bez zmian. `declined` zero zdarzeń. Brak nowej tabeli / migracji. `SHIPPED_CHARGE_ROUTES["246.0"]` = `/entity-events`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE 244.0/245.0 `_append_queued_event` (`queued`/`sent`). ISTNIEJE kind `quote_recorded` w allowliście 133.0. ISTNIEJE POST `answered` z kwotą. BRAK append przy `answered`. PODOBNE: ten sam helper. Nie nowa tabela. Nie import BC. POST `/channel-quotes` nie składa zdarzenia — leftover.

**Odrzucone:**

- Import `entity_events` w serwisie inquiries.
- POST `channel_quote` / batch / PATCH status.
- T1b · T2c · T7b · T5 · N3 · S53 · PIN · live HTTP · Temporal.

## Zakres

1. API: `answered` → `quote_recorded` w tej samej transakcji co POST.
2. Test HTTP: answered → jeden INSERT; queued/sent bez zmian; draft/declined zero.
3. Fold w `entity-event.md`. Job OPS zostaje 106.

## Poza zakresem

POST `channel_quote` · batch · PATCH · FK subject · outbox consumer · Temporal · PIN · km · mapa · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST ze `status=answered` (kwota + waluta) dopisuje `quote_recorded`
- [x] POST `queued` nadal `inquiry_queued`; `sent` nadal `inquiry_sent`; `draft` i `declined` zero
- [x] Zero importu `entity_events` w serwisie inquiries
- [x] Zero float / marży / live HTTP / Temporal

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | answered → `quote_recorded`; queued/sent bez zmian; draft/declined → zero |
| Szybkość | N/A ten plaster | jeden INSERT zdarzenia, nie ścieżka z budżetu AGENTS |
| SQL | N/A | brak nowej tabeli / migracji |
| N+1 | OK | jeden append po jednym INSERT |
| Dług / człowiek | OK | leftover POST `channel_quote`; PIN parked |
| Proza operatora | 5–15 zdań | fold w `entity-event.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN B0a, how-to |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
