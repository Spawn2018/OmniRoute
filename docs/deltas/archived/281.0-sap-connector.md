# Plaster 281.0 · CT6 `sap_connector` HITL (SAP/Oracle fixture)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT6 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md)
**Zależy od:** 0.3 tenancy · 268.0 F9 `erp_connector` Optima (wzorzec; ten Q nie mutuje Optima)
**Status:** Zamknięty `/noc` 2026-09-11. Nie live SOAP. Nie SQL do SAP. Nie sekrety.

## Wybrane / odrzucone / dlaczego

**Wybrane:** leftover CT6 HITL katalog konektora SAP/Oracle jak F9. leftover live SOAP/RFC parked. leftover SQL do SAP parked. leftover CT12 CAPA parked (następny Q). leftover 409 / auto shipment / CT2 parked. Żywy plaster: **nowa tabela** `sap_connector` — `connector_code` + `system_kind` (`sap`|`oracle`) + `source_ref`. Nie rozszerza CHECK Optima. UI `/sap-connectors`. OpenFGA `can_manage_sap_connectors`. Migracja `214_sap_connector`. Job OPS 126. BC `sap_connectors`.

**Łowca:** BRAK `sap_connector`. ISTNIEJE `erp_connector` kind `optima` — F9, **nie** CT6. Nie mutować Optima.

**Odrzucone:** live SOAP · SQL `sa` · sekrety · ciphertext · base_url · XL · Optima · kwota · float · UPDATE/DELETE.

## Zakres

1. Migracja `214_sap_connector` po `213_otif_mark`: RLS FORCE, izolacja.
2. POST/GET `/sap-connectors`. OpenFGA. Deny-list extract.
3. UI `/sap-connectors`. How-to. Job OPS 126.

## Poza zakresem

live SOAP · SQL do SAP · sekrety · Optima · CT12 · 409 · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `sap_connector` z kodem, kind sap|oracle i `source_ref`
- [x] zły kod → 400 (`oznaczenie`); zły kind → 400 (`system`); obce `source_ref` → 400 (`obce`)
- [x] POST z `base_url` / `api_key` / `amount` → 422
- [x] GET lista tenanta; test izolacji; zero float/HTTP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400; 422; RLS; OpenFGA |
| Szybkość | N/A ten plaster | INSERT/GET katalog |
| SQL | EXPLAIN | lista po organization_id |
| N+1 | OK | jeden SELECT / INSERT |
| Dług / człowiek | OK | leftover live SOAP / CT12 / 409 |
| Proza operatora | 5–15 zdań | `sap-oracle.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN, GLOSSARY, MODULES |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Leftover po 281.0

live SOAP/RFC/SQL do SAP. CT12 CAPA. Egzekucja 409. Auto shipment. CT2 CI5. CT5/CT8 live.
