# Plaster 17.0 · M-24 Ryzyko oferty (`offer_risk` przy `quotation`)

**Spec źródłowa:** [docs/spec/offer-risk.md](../../spec/offer-risk.md)  
**Zależy od:** 5.1 `quotation.party_id` · 10.0 `party_scorecard` · 14.0 `credit_review` · 0.6 DataTableShell

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-24):** oferta **czyta** fakty kontrahenta, które już są w katalogach. Panel na `/quotations`. Token `offer_risk` (UI, nie tabela). Żywy ID **M-24**.

**Łowca:** BRAK `offer_risk` / `quotation_risk` w kodzie i GLOSSARY. ISTNIEJE: `quotation.party_id` (5.1), `resolveCreditReview` (14.0), `fetchPartyScorecard` (10.0). PODOBNE: 15.0 `finance_board` i 16.0 panel NBP na `/quotations` — compose API, zero nowej tabeli.

**Odrzucone:**

- Nowa tabela score/rating — AI Act + PLAN: nie scoring osoby; 14.0 leftover.
- Silnik „ryzyko = f(…)” w Pythonie/JS — LLM nie liczy; nie drugi `margin()`.
- Zapis `credit_limit` / `credit_review` z tego panelu — mutacje zostają na `/parties` i `/credit-reviews`.
- Import `app.services.parties` z `quotations` — independence import-lintera.

## Zakres

Panel na istniejącym `/quotations`. `resolveCreditReview` + `fetchPartyScorecard`. Brak migracji.

## Poza zakresem

Scoring · biuro BIK/KRD · nowy router backendu · nowy BC · ExtractionService→parties · LLM

## Ustalenia

- Token: `offer_risk` (GLOSSARY, UI). 17.0 = użycie przy ofercie, nie nowy byt w bazie.
- OpenFGA: istniejące `can_manage_parties` na recenzji/karcie; 403 zostaje.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["17.0"]` = `/quotations`.
- `just dup` ≤ 3% — własny fieldset, nie kopia `/finance` ani `/credit-reviews`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] `/quotations` woła `resolveCreditReview` i `fetchPartyScorecard`
- [x] Brak nowej migracji
- [x] Brak scoringu / `parseFloat` na karcie jako „ocena oferty”
- [x] `quotations` service nie importuje `parties`
- [x] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | `/quotations` czyta recenzję i kartę; brak zapisu; brak nowej tabeli |
| Szybkość | N/A ten plaster | brak nowej ścieżki wyceny |
| Dług w diffie | leftover | `just dup` 2,75%; scoring zostaje leftover 14.0 |
| Docs/OS | PRZESZŁO | spec `offer-risk.md`; CURRENT → Plan M-25 |

## Knowledge

Wzorzec = 15.0 tablica odczytu. Nie RAG na scoring.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Scoring świadomie leftover (14.0).
