# Plaster 624.0 · D4b rodzaj dokumentu `rod`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D4 · [158.0](../archived/158.0-cod-instruction.md)
**Zależy od:** `shipment_document.document_kind` (`noted`|`attached`|`other`), kolumna `String(16)`
**Status:** Zamknięty `/noc` 2026-09-22. Nie skan. Nie token `pod`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D4b POD/ROD):** wskazanie dokumentu na zleceniu już jest. Skan i bajty zostają. Żywy plaster: czwarty `document_kind` = `rod` (3 znaki, mieści się w 16). CHECK i domena: `noted`|`attached`|`other`|`rod`. `pod` zostaje odrzucone — w słowniku POD to port wyładunku, nie dowód dostawy. UI `/shipment-documents` przyjmuje wpisany token; zły rodzaj → 400 (`rodzaj`). `SHIPPED_CHARGE_ROUTES["624.0"]` = `/shipment-documents`. OpenFGA bez nowej relacji. Migracja `496_shipment_document_rod` po `495_pallet_balance_epal`: podmiana CHECK, bez nowej tabeli. Marża zostaje na `charge`. Zero bajtów. Zero HTTP.

**REQUIREMENT:** leftover 158.0 „POD/ROD jako document_kind”. Token `rod`, nie `pod`. Źródło: `docs/deltas/archived/158.0-cod-instruction.md`; GLOSSARY `destination_port_id` = POD.

**Łowca:** ISTNIEJE `require_document_kind` ze zbiorem `noted`|`attached`|`other`. ISTNIEJE CHECK `ck_shipment_document_kind`. ISTNIEJE `cod_instruction` — pobranie bez kwoty, nie rodzaj dokumentu. BRAK `rod` na `shipment_document`.

**Odrzucone:**

- token `pod` (kolizja z portem wyładunku)
- `proof_of_delivery` (18 znaków, kolumna ma 16)
- bajty skanu / kamera
- kwota / COD / marża

## Zakres

1. Migracja podmienia CHECK. Domena i model to samo.
2. POST `document_kind=rod` zapisuje wskazanie; `pod` → 400.
3. Zdanie w `docs/operator/dokument-zlecenia.md`.

## Poza zakresem

Skan · kamera · token `pod` · kwota COD · float · live HTTP

## Plan plików

- `backend/alembic/versions/496_shipment_document_rod.py`
- `backend/app/domain/shipment_document.py`
- `backend/app/models/shipment_document.py`
- testy domeny i HTTP dokumentu zlecenia
- `frontend/src/features/shipment-document/catalog-page.tsx` (placeholder)
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- `docs/operator/dokument-zlecenia.md`

## Kryteria akceptacji

- [x] POST `document_kind=rod` zapisuje wiersz
- [x] `pod` i rodzaj spoza czwórki → 400 (`rodzaj`)
- [x] zero bajtów / marży / nowej tabeli / nowej relacji OpenFGA

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST `rod`; 400 na `pod`; CHECK `496` |
| Szybkość | N/A ten plaster | katalog INSERT |
| SQL | N/A | podmiana CHECK |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | poszerzenie allowlisty |
| Proza operatora | 5–15 zdań | `dokument-zlecenia.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D4, GLOSSARY, VISION |
| Pushy do zielonego | N/A | CI po tym pushu |
| Minuty plastra | 20 | od delty do zamknięcia |
| Bench rzemiosła | PRZESZŁO | `docs/_bench/cases/624.0-closed.md` |
