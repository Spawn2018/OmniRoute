# Plaster 186.0 · G2.23 `kreptd_licence`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.23 · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) § G2.23
**Zależy od:** 5.0 `party` (kontrahent już jest)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie scrape KREPTD. Nie Citizen API.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.20–G2.22 + G2.23):** leftover `circle_sim`/km/P (G2.20–G2.22) zostaje leftover — nie silnik 500k w Pythonie i nie LLM-VRP. leftover kg/CBAM (G2.15–G2.18) zostaje leftover. Żywy plaster: jedna tabela `kreptd_licence` per tenant — **HITL numer licencji KREPTD/GITD z `source_ref`**, nie klient HTTP do kreptd.gitd.gov.pl i nie `party_document` (C8). Wiersz = `party_id` (FK tenanta do `party`) + `licence_no` (tekst wpisany przez operatora) + `source_ref` (`tenant:manual` albo `fixture://kreptd-licence/…`). Operator zapisuje, że kontrahent ma numer licencji ITD; serwis nie woła sieci, nie woła LLM, nie mutuje `party` i nie czyta `charge`. Zły numer → 400 (`licencja`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, party_id)` — jeden numer na kontrahenta. GET listy SQL. API składa odczyt kontrahenta (`PartyService.get_party`). UI `/kreptd-licences` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["186.0"]` = `/kreptd-licences`. OpenFGA: `can_manage_kreptd_licences`. Migracja `126_kreptd_licence`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tender_ted_notice` — numer TED na `tender`, **nie** KREPTD. ISTNIEJE `party` — brak `kreptd_licence_no`. BRAK `party_document` (C8 leftover). BRAK tabeli `kreptd_licence`. BRAK `/kreptd-licences`. BRAK klienta HTTP KREPTD.

**Odrzucone:**

- `circle_sim` / km / P (leftover G2.20–G2.22).
- kg / CBAM (leftover G2.15–G2.18).
- `party_document` + 409 na `POST shipment` (C8 leftover).
- Scrape kreptd.gitd.gov.pl / Citizen API / certyfikat / live HTTP.
- Auto-award / scoring osoby / kolumny na `party`.
- Kwota / druga marża / float / T-SQL.

## Zakres

1. Migracja `126_kreptd_licence`: RLS, izolacja, indeks `(organization_id, licence_no)`, unique `(organization_id, party_id)`.
2. POST/GET. Zły `licence_no` / `source_ref` → 400. API składa odczyt `party`, serwis nie importuje parties / charges / extraction i nie woła HTTP.
3. UI `/kreptd-licences`. `just api-types`. How-to `docs/operator/licencja-kreptd.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `KreptdDesk` / `kreptdWrite` / `persistKreptdMark`.

## Poza zakresem

G2.20–G2.22 circle_sim/km/P · G2.15–G2.18 kg/CBAM · C8 `party_document` · Citizen API · scrape HTML · auto-award · scoring osoby · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST zapisuje `kreptd_licence` z numerem licencji i `source_ref` na istniejącym `party`, bez scrape
- [x] zły `licence_no` → 400
- [x] GET zwraca licencje tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / LLM / live HTTP / scrape / kolumn na `party`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST numer licencji + `source_ref` na `party`; 400 `licencja` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `(organization_id, licence_no)` — `ix_kreptd_licence_org_licence` albo unique prefix |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover Citizen API / C8 `party_document`; leftover G2.20–G2.22; leftover G2.15–G2.18 |
| Proza operatora | 5–15 zdań | `licencja-kreptd.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN G2.23, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
