# Plaster 201.0 · W3 `memory_edge` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala W · [karty-pol-fala-w.md](../../analysis/karty-pol-fala-w.md) § W3
**Zależy od:** 0.3 tenancy (katalog per tenant) · 127.0 `entity_event` (ledger zostaje; krawędź nie zapisuje zdarzeń)
**Status:** Zamknięty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie RAG. Nie wektor.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover W3 krawędzie na `entity_event` + RAG + leftover W2 N8 + leftover V7 tacho + V8 what-if + leftover V6 EBITDA + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7 tacho TO_VERIFY parked. leftover V8 what-if parked. leftover V6 silnik EBITDA / `sla_clause` parked. leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b parked. leftover V4 AIS parked. leftover V2b myto parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. leftover N8 / T8 / widok W2 parked. Graf wektorowy, pgvector i RAG na stawkach / umowach CI zostają leftover. Żywy plaster: jedna tabela `memory_edge` per tenant — **HITL znacznik rodzaju krawędzi pamięci jako dane**, nie graf na `entity_event` i nie wyszukiwanie. Wiersz = `edge_kind` (`recalls` | `follows` | `blocks` | `cites` | `other`) + `source_ref` (`tenant:manual` albo `fixture://memory-edge/…`). Operator wpisuje rodzaj krawędzi; serwis nie woła LLM, nie czyta `entity_event` / `charge` / `rate_line` / umów CI, nie liczy podobieństwa i nie woła HTTP. Zły rodzaj → 400 (`krawędź`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/memory-edges` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["201.0"]` = `/memory-edges`. OpenFGA: `can_manage_memory_edges`. Migracja `141_memory_edge`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `entity_event` B0a — ledger append-only, **nie** krawędź. ISTNIEJE `prediction_ledger` — CRPS/MAE, **nie** graf. ISTNIEJE `war_room_mark` — incydent sali, **nie** pamięć. ISTNIEJE `operator_notice` — inbox, **nie** krawędź. BRAK tabeli `memory_edge`. BRAK RAG. BRAK pgvector w runtime.

**Odrzucone:**

- Krawędzie FK na `entity_event` / graf / wektor / pgvector.
- RAG na stawkach, wycenach, umowach CI.
- Silnik EBITDA / fizyka twinów / N8 / drugi czat.
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `141_memory_edge` (po `140_war_room_mark`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind.
2. POST/GET. Zły rodzaj → 400. Serwis nie importuje entity_events / charges / extraction i nie woła HTTP.
3. UI `/memory-edges`. `just api-types`. How-to `docs/operator/memory-edge.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `EdgeDesk` / `edgeWrite` / `persistEdgeMark`. Formularz radio (nie `<select>` klonów HITL).

## Poza zakresem

RAG · pgvector · FK entity_event · graf wektorowy · N8 · drugi czat · silnik EBITDA · fizyka twinów · live GPS · AIS · myto → charge · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [x] POST zapisuje `memory_edge` z `edge_kind`, bez HTTP
- [x] zły `edge_kind` → 400 (`krawędź`)
- [x] obce `source_ref` → 400 (`obce`)
- [x] GET zwraca wiersze tenanta
- [x] Test izolacji tabeli
- [x] Zero float / marży / RAG / live HTTP / scrape / LLM

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST `edge_kind`; 400 `krawędź` / `obce`; GET lista SQL |
| Szybkość | N/A ten plaster | katalog INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | `organization_id` — `ix_memory_edge_organization_id` albo unique source_ref |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | nowa tabela; leftover graf / pgvector / RAG; formularz chip-radio + lista `ol` |
| Proza operatora | 5–15 zdań | `memory-edge.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN W3, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
