# Plaster 516.0 · AI7.0 Cost Allocation — `allocation_key` HITL

**Status:** DONE 2026-09-15 `/noc 7`.

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI7.0 · [VISION.md](../../VISION.md) § B.6 silnik kosztu obsługi

**Zależy od:** tenancy RLS · wzorzec otwartego słownika (`autonomy_level` / `twin_kind`) · `cost_allocation_mark` (340.0) zostaje osobno

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `allocation_keys`. Tabela `allocation_key`: `key_code` (snake 2–32, **bez** CHECK listy 23) + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z AI7.0 — katalog HITL **kluczy podziału kosztów** jako dane (PDF §13j / VISION: 23 klucze), nie silnik SQL i nie `TRUE CONTRIBUTION MARGIN`.
- **Wybrane:** POST/GET `/api/v1/allocation-keys`. OpenFGA `can_manage_allocation_keys` (member). UI `/allocation-keys`. `SHIPPED["516.0"]` → `/allocation-keys`.
- **Wybrane:** how-to `docs/operator/allocation-key.md` (job: zapis klucza bez liczenia marży).
- **Odrzucone:** mutacja / rozszerzenie `cost_allocation_mark` (`REJECTED` — EXP2.3 tryb direct|abc|shared|other; tu osobny słownik kluczy).
- **Odrzucone:** allocation SQL / ABC / mnożenie na `charge` (`REJECTED` — leftover silnika; marża zostaje w `margin()`).
- **Odrzucone:** `TRUE CONTRIBUTION MARGIN` jako kolumna / widok / druga marża (`REJECTED` — HC; leftover po kluczach + kategoriach).
- **Odrzucone:** CHECK/ENUM 12 poziomów albo 6 kategorii albo listy 23 (`REJECTED` — nowy klucz = INSERT, jak autonomy_level).
- **Odrzucone:** live ERP / AI7.1 Cyfrowy CFO (`REJECTED` — osobny wiersz / park).
- **Odrzucone:** float / amount w payloadzie (`REJECTED`).

**Łowca:** ISTNIEJE `cost_allocation_mark` (tryb alokacji HITL). ISTNIEJE `make_or_buy_mark` / `dual_ledger_mark`. PODOBNE `autonomy_level` / `twin_kind` (otwarty słownik). BRAK tabeli `allocation_key`.

## Zakres

HITL `allocation_key` — migracja + model + repo + serwis + API + OpenFGA + test izolacji + FE + how-to + SHIPPED 516.0.

## Poza zakresem

allocation SQL · ABC · TRUE CONTRIBUTION MARGIN · druga marża · 12 poziomów jako osobna tabela · 6 kategorii CHECK · Cyfrowy CFO · live ERP · kwota · UPDATE/DELETE wiersza

## Kryteria akceptacji

- [x] migracja + FORCE RLS + test izolacji tenantów
- [x] POST z `key_code` (snake 2–32) + `source_ref`; zły kod/source_ref → 400; amount/margin → 422
- [x] brak CHECK listy 23 kluczy w migracji
- [x] UI `/allocation-keys`; OpenFGA; `SHIPPED["516.0"]`
- [x] craft_style; how-to operatora

## Leftover

12 poziomów (osobny katalog) · 6 kategorii PDF · silnik SQL alokacji · TRUE CONTRIBUTION MARGIN · AI7.1 Cyfrowy CFO · live ERP

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL allocation_key POST/GET + RLS |
| Szybkość | N/A ten plaster | |
| SQL | N/A | list/insert |
| N+1 | OK | |
| Dług / człowiek | OK | klon autonomy_level |
| Proza operatora | 5–15 | allocation-key.md |
| Docs/OS | PRZESZŁO | |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | `/noc 7` |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
