# Plaster 432.0 · AI1.0 `suggestion_ledger` HITL

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.0

**Zależy od:** 431.0 leftover EXP1 quotation zamknięty · 193.0 `prediction_ledger` (analog przedziału, nie ta tabela) · 129.0 `charge.source_ref` (AI0 DONE)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `suggestion_ledger` per tenant — append-only HITL. Wiersz = `target_bc` (snake 2–32) + `entity_id` (UUID jako dana, bez FK) + `suggestion_kind` `eta|rate|route|other` + `interval_low` / `interval_high` (`Numeric(14,4)`, `high >= low`, zakaz float) + `model_version` + `prompt_version` (snake 2–32) + `reaction` `accept|modify|reject` + `changed_to` + `source_ref`. Operator wpisuje podpowiedź i reakcję po fakcie. Serwis nie woła LLM i nie liczy metryk.
- **Wybrane:** `changed_to` = `none` przy `accept`/`reject`; przy `modify` tekst 1–256 inny niż `none`. To jest pole „na co zmienił” z B.2.
- **Odrzucone:** zapis z modelu na L0–2 — HC-04; ten plaster nie ma extractora.
- **Odrzucone:** rozszerzenie `prediction_ledger` — tam CRPS/MAE jako dane; tu reakcja człowieka. AI2 złączy później.
- **Odrzucone:** zapis do `operator_decision` — szyna extract/oferty, nie substrat podpowiedzi.
- **Odrzucone:** `outcome_ledger` / `counterfactual_run` / `benefit_ledger` / słowniki AI1.4 — kolejne wiersze Fali AI.
- **Odrzucone:** trzy BC piszące do substratu — warunek końca fali AI1, nie tego plastra.
- **Odrzucone:** CRPS/MAE liczone · JSON payload · UPDATE/DELETE · FK · kwota / marża / float.

**Łowca:** PODOBNE `prediction_ledger` (przedział jako dana). PODOBNE `operator_decision` (accept/modify/reject). PODOBNE `entity_event` (append-only). BRAK `suggestion_ledger`.

## Zakres

HITL `suggestion_ledger`. Migracja `342` (revises `341_quote_validity_mark`). UI `/suggestion-ledgers`.
OpenFGA `can_manage_suggestion_ledgers`. `SHIPPED["432.0"]`. OPS_JOBS length 249.

## Poza zakresem

outcome_ledger · counterfactual_run · benefit_ledger · twin_kind/data_source/autonomy_level/suggestion_kind jako tabele · zapis LLM · CRPS SQL · trzy BC autosave · FK do shipment/trip · prediction_ledger mutate

## Kryteria akceptacji

- [x] POST z target_bc, entity_id, kind, przedziałem Decimal, wersjami, reaction, changed_to, source_ref
- [x] float / amount / margin → 422; zły kind/reaction/source_ref / high < low / modify+none → 400
- [x] izolacja RLS; REQUIRED `/suggestion-ledgers`
- [x] OPS_JOBS length 249; SHIPPED `"432.0"`
- [x] craft_style funkcje>40: 0

## Leftover

AI1.1 outcome_ledger · AI1.4 słowniki otwarte · trzy BC piszą do substratu · CRPS liczone (AI2)

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | HITL ledger + RLS + UI + OpenFGA |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | N/A | list/order bez nowego resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | analog prediction_ledger; craft_style 0 |
| Proza operatora | 5–15 zdań | `docs/operator/suggestion-ledger.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | CI billing limit jak 431 |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
