# Plaster 636.0 · D9f rest `output_kind` HITL (`pdf` / `zpl`)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D9 · [162.0](../archived/162.0-document-template.md) · [627.0](../archived/627.0-document-template-branding.md)
**Zależy od:** 627.0 `branding_ref` już jest; `html_print` zostaje
**Status:** Zamknięty `/noc 17` 2026-09-23. Nie silnik PDF/ZPL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D9f rest `output_kind`):** `layout_ref`, `branding_ref` i `html_print` już są. Live generacja PDF/ZPL/QR zostaje park (D9f pdf-zpl live / D9d). Żywy plaster: **pogłębienie istniejącej** `document_template` — allowlista `output_kind` rozszerzona o `pdf` i `zpl` obok `html_print`. To HITL **zamierzony** rodzaj wyjścia (dana), nie bajty i nie renderer. CHECK w bazie + domena + select UI. Zły token → 400 (`wyjście`). `SHIPPED_CHARGE_ROUTES["636.0"]` = `/document-templates`. OpenFGA bez nowej relacji (`can_manage_document_templates`). Migracja `508_dt_output_kind` po `507_local_charge_warning_mark`. Marża zostaje na `charge`. Zero bajtów. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 162.0 / 627.0 „`output_kind` `pdf`/`zpl`” — tu tylko etykieta intended output, nie silnik. Źródło: `docs/deltas/archived/162.0-document-template.md` · `docs/ops/docs-debt.md` § 162.0 · PLAN wiersz D9.

**Łowca:** ISTNIEJE `document_template.output_kind` z CHECK/`_OUTPUTS` = tylko `html_print`. ISTNIEJE `branding_ref` (627.0). BRAK allowlisty `pdf`/`zpl`. BRAK silnika bajtów PDF/ZPL (park).

**Odrzucone:**

- live generacja PDF / ZPL / QR bitmap (D9d / D9f pdf-zpl live)
- BYTEA / WeasyPrint / zebra / upload layoutu
- HTTP 409 wyjazdu (live 409 park)
- skan HITL bez Omni (D9e park)
- kwota / marża / float

## Zakres

1. Migracja: drop CHECK `html_print` only → CHECK `html_print|pdf|zpl`; model + domena `_OUTPUTS`.
2. POST z `output_kind=pdf` / `zpl` zapisuje etykietę; `html_print` bez regresji; nieznany → 400 (`wyjście`).
3. Select na `/document-templates` + zdanie w how-to: intended output ≠ plik.

## Poza zakresem

PDF silnik · ZPL silnik · QR · 409 · upload · C8 · WMS · mapa · kwota

## Plan plików

- `backend/alembic/versions/508_dt_output_kind.py`
- `backend/app/domain/document_template.py`
- `backend/app/models/document_template.py`
- testy domeny i HTTP (pdf/zpl OK; unknown 400)
- `frontend/src/features/document-template/sheet-panel.tsx` (+ catalog.test)
- `frontend/src/features/ops/ops-index.ts` (`"636.0"`)
- `docs/operator/szablon-wydruku.md`
- GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST `output_kind=pdf` / `zpl` zapisuje etykietę; zero bajtów w odpowiedzi
- [x] `html_print` bez regresji; nieznany token → 400 (`wyjście`)
- [x] zero silnika PDF/ZPL / marży / float / 409

## Leftover

D9d PDF-QR · D9e skan · D9f pdf-zpl live · live 409

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST pdf/zpl 201; docx 400 |
| Szybkość | N/A ten plaster | CHECK + INSERT |
| SQL | N/A | drop/create CHECK |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | rozszerzenie allowlisty |
| Proza operatora | 5–15 zdań | `szablon-wydruku.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D9, GLOSSARY, VISION |
| Pushy do zielonego | N/A | CI po tym pushu |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
