# Plaster 619.0 · U3c pule HAWB/MAWB z M-03

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § U3 leftover · [209.0](209.0-air-hawb.md) · [72.0](72.0-offer-document-number.md) · [karty-pol-fala-u.md](../../analysis/karty-pol-fala-u.md) § U3
**Zależy od:** 209.0 `hawb_no` / `mawb_no` na `shipment_leg` air · 71.0/72.0 wzorzec prefiksu M-03 + SQL `MAX`+1
**Status:** Zamknięty `/noc` 2026-09-22. Nie cyfra kontrolna IATA. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover U3c pule IATA/air):** tokeny HAWB/MAWB już są (209.0). Live IATA i cyfra kontrolna zostają. Żywy plaster: **pule numerów w M-03** — klucze `hawb_number_prefix` i `mawb_number_prefix` na allowliście `organization_setting` (ten sam normalizator prefiksu co `quotation_number_prefix`). Operator klika „Nadaj HAWB” / „Nadaj MAWB” na odcinku `air` z pustym polem: API składa — odczyt prefiksu z `OrganizationSettingService`, serwis `shipment_legs` dostaje sam string i zapisuje numer. Kolejny numer liczy **Postgres** (`MAX` ogona cyfr + 1), nie pętla w Pythonie. Format: znormalizowany prefiks + cztery cyfry. Drugie nadanie na tym samym polu zwraca ten sam numer (niemutowalny). Ręczne wklejenie numeru przy POST (209.0) zostaje. Brak prefiksu → 400 (`prefiks`). Noga nie-`air` albo nieznana → 400/404 jak dziś. Unique per tenant już jest. UI `/air`. `SHIPPED_CHARGE_ROUTES["619.0"]` = `/air`. OpenFGA bez nowej relacji (`can_manage_shipment_legs`). Serwis `shipment_legs` nie importuje `organization_settings`. Marża zostaje na `charge`. Zero float. Zero HTTP IATA.

**REQUIREMENT:** karta U3 — „HAWB/MAWB, pule M-03”. Źródło: `docs/analysis/karty-pol-fala-u.md` § U3; leftover z 154.0/209.0.

**Łowca:** ISTNIEJE `hawb_no` / `mawb_no` + unique. ISTNIEJE `quotation.document_number` + `ISSUE_DOCUMENT_NUMBER_SQL` (wzorzec). ISTNIEJE allowlista M-03. BRAK kluczy air-prefix. BRAK endpointu nadania na nodze. BRAK IATA HTTP.

**Odrzucone:**

- cyfra kontrolna IATA / live HTTP IATA / e-rates / `channel_quote`
- osobna tabela puli / generator spoza M-03
- druga tabela listu / `ocean_bill.bill_kind=hawb`
- znacznik HITL bez nadawania numeru (zostawia leftover U3c)
- import `organization_settings` w serwisie nóg · float · matching T5 · T8 live

## Zakres

1. Dwa klucze w allowliście M-03 + normalizacja prefiksu (współdzielona z ofertą).
2. SQL nadania HAWB i MAWB na `shipment_leg` (tylko `leg_kind=air`, tylko gdy pole NULL).
3. POST compose w API nóg: ustawienie → string → serwis. UI `/air` + zdanie w `lot.md`.

## Poza zakresem

IATA live · cyfra kontrolna · e-rates · ocean_bill · PDF · matching T5 · T8 · float

## Kryteria akceptacji

- [x] brak prefiksu → 400 (`prefiks`); z prefiksem nadaje `PREFIX`+4 cyfry; drugie kliknięcie bez zmiany
- [x] ręczne POST z 209.0 nadal działa; unique per tenant; izolacja
- [x] serwis `shipment_legs` nie importuje `organization_settings`; Postgres liczy kolejny numer
- [x] zero float / marży / IATA HTTP / cyfry kontrolnej

## Leftover

e-rates IATA · cyfra kontrolna IATA · live booking

## Post-plaster

| Check | Wynik | Dowód |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | pytest waybill pools + vitest `/air` |
| Szybkość | N/A | nadanie numeru, nie ścieżka z budżetu |
| SQL | EXPLAIN | `MAX`+`LPAD` na `hawb_no`/`mawb_no` |
| N+1 | OK | jeden UPDATE |
| Dług / człowiek | OK | compose w API; bez importu settings w serwisie |
| Proza operatora | 5–15 | `lot.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN U3, GLOSSARY |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc 7` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
