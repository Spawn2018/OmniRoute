# Plaster 223.0 · T3 `packaging_code` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [222.0](../archived/222.0-container-cargo.md) leftover `packaging_code` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 222.0 `cargo_description`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `packaging_code`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `quantity` / `weight_kg` / `volume_m3` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `packaging_code` (1–32 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`opakowanie`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["223.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `162_container_pack`. Marża zostaje na `charge`. Kod to tekst HITL, nie FK słownika.

**Łowca:** ISTNIEJE `cargo_description` — opis ładunku, nie kod opakowania. BRAK `packaging_code` na `container`. BRAK tabeli słownika opakowań. BRAK `quantity`. BRAK PIN. BRAK VGM.

**Odrzucone:**

- FK słownika opakowań.
- `quantity` / `weight_kg` / `volume_m3` / VGM / TARA (Decimal).
- `pin_code` / ciphertext.
- `booking_no` / live HTTP.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `162_container_pack` (po `161_container_cargo`): `packaging_code` nullable `String(32)`.
2. POST opcjonalny `packaging_code`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · ilość · objętość · booking · km · mapa · T7b · T5 · Auth0 · live HTTP · słownik FK

## Kryteria akceptacji

- [x] POST z `packaging_code` zapisuje kod opakowania, bez PIN i bez wagi
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`opakowanie`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `packaging_code`; 400 `opakowanie`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(32)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie waga; leftover T3 PIN/VGM/`ref_1`; `record_container` = 40 linii |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
