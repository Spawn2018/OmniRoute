# Plaster 603.0 · T1 EXP1 `appointment_status` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [255.0](255.0-stop-pod-quality.md) leftover · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 602.0 T1b sync; 252.0 `appointment_ref`
**Status:** Zamknięty `/noc` 2026-09-20. Nie dok. Nie T8. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `appointment_status`/`no_show_at` + leftover HW + leftover UXCL + Job#2 + N3 + AI3 + G0-SH):** leftover `no_show_at` parked. leftover HW parked. leftover UXCL parked. Job#2 parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `appointment_status` (`String(16)` HITL, allowlista `noted` / `advised` / `confirmed` / `cancelled`, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`status awizacji`). `require_stop_appointment_status` w `app.domain.stop` — wzór `pod_quality` (zbioru), **nie** import `dock_appointments` / `require_appointment_status` z doku (inny BC, inny zestaw). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["603.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `478_stop_appointment_status` (po `477_stop_group_bind`). CHECK `ck_stop_appointment_status`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.appointment_ref` (252.0). ISTNIEJE `dock_appointment.appointment_status` (157.0) — inny BC. ISTNIEJE `stop.pod_quality` (255.0). BRAK `stop.appointment_status`. BRAK `stop.no_show_at`.

**Odrzucone:**

- `no_show_at` / HW / UXCL / Job#2 / N3 / AI3 / G0-SH / import `dock_appointments` / T8 / PIN / mapa / live HTTP / float / auto-status z zegara.

## Zakres

1. Migracja `478_stop_appointment_status`: `appointment_status` nullable String(16) + CHECK allowlista. Nie `create_table`.
2. POST opcjonalny `appointment_status`. Zły token → 400 (`status awizacji`). Serwis nie importuje dock/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

`no_show_at` · dok / T8 · HW · UXCL · PIN · mapa · Auth0 · live HTTP · float

## Kryteria akceptacji

- [x] POST z `appointment_status=noted|advised|confirmed|cancelled` zapisuje status HITL
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / nieznany token → 400 (`status awizacji`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / doku / live HTTP / import `dock_appointments`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `appointment_status`; 400 `status awizacji`; pusty → null |
| Szybkość | N/A ten plaster | kolumny INSERT/GET |
| SQL | N/A | String + CHECK allowlista |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | wzór `pod_quality`; leftover `no_show_at` |
| Proza operatora | 5–15 zdań | `zlecenie.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
