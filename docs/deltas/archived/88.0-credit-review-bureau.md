# Plaster 88.0 · S26 recenzja + załącznik wywiadowni (M-14)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S26  
**Zależy od:** 14.0 `credit_review` · 5.0 `party.credit_limit` (nie nadpisuj)

**Status:** Zamknięty `/noc` 2026-09-03. Ten sam żywy ID **M-14**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** załącznik = **wskazanie** raportu wywiadowni na istniejącej recenzji, nie nowy limit i nie HTTP do biura. Nullable `credit_review.bureau_attachment_ref` (tekst jak `source_ref`, nie blob). PATCH dopina URI, który operator już ma. Decyzja `ok|hold|refuse` i `party.credit_limit` nietknięte. Bez nowej tabeli — jeden raport na recenzję.

**Łowca:** ISTNIEJE 14.0 `credit_review` + `source_ref` (`tenant:manual`). BRAK osobnego wskazania raportu. PODOBNE: `source_ref` (pochodzenie wpisu, nie plik); dokumenty zlecenia to S30.

**Odrzucone:**

- Auto-limit / zapis `credit_limit` / scoring osoby.
- Nowa tabela albo upload bajtów (M-38 leftover).
- Live HTTP BIK/KRD/GUS.
- Mutacja `quotation` / `noted_credit_review_id`.
- F9.1.

## Zakres

1. Migracja `047_review_bureau_ref` (revision ≤32): kolumna TEXT NULL na `credit_review`. Bez FK. RLS zostaje z 025.
2. `PATCH /credit-reviews/{id}/attach-bureau` — `{bureau_attachment_ref}`. OpenFGA `can_manage_parties`. `PartyService` (nie osobny BC).
3. UI `/credit-reviews`: „Dołącz raport”. `SHIPPED_CHARGE_ROUTES["88.0"]` = `/credit-reviews`.

## Poza zakresem

Auto-limit · BIK/KRD HTTP · blob · score · `legal_form` · F9.1

## Ustalenia

- Token: `credit_review`. How-to: `ścieżka-pieniędzy.md`.
- Serwis `quotations` / `charges` / `extraction` nie importuje `credit_review` (wskazanie 87.0 zostaje UUID).
- Pusty ref po trim = 400. Max 256. Bez pobierania sieciowego.
- `source_ref` zostaje pochodzeniem wpisu; nie nadpisuj go załącznikiem.

## Schemat

`credit_review.bureau_attachment_ref` TEXT NULL.

## Kryteria akceptacji

- [x] Kolumna bez score i bez `credit_limit`
- [x] Pusty albo za długi ref → 400
- [x] Brak HTTP do biura i brak `risk_score` na `/credit-reviews`
- [x] `just api-types`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | PATCH zapisuje wskazanie raportu; decyzja i limit nietknięte |
| Szybkość | N/A ten plaster | jeden PATCH po kluczu |
| SQL | N/A | UPDATE jednej kolumny po PK |
| N+1 | OK | get review + zapis kolumny |
| Dług / człowiek | OK | bez nowej warstwy; HTTP wywiadowni poza zakresem |
| Proza operatora | 5–15 zdań | `ścieżka-pieniędzy.md` |
| Docs/OS | PRZESZŁO | spec, MODULES, CURRENT |
| Pushy do zielonego | N/A | przed push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Pytania

Brak. `/noc` akceptuje deltę.
