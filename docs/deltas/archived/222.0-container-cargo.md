# Plaster 222.0 · T3 `cargo_description` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [221.0](../archived/221.0-container-remarks.md) leftover `cargo_description` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 221.0 `remarks`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `cargo_description`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `weight_kg` / `quantity` / `volume_m3` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `cargo_description` (1–256 znaków po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`ładunek`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["222.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `161_container_cargo`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `remarks` — uwaga operatora, nie opis ładunku. ISTNIEJE `commodity_code` — katalog HS, nie tekst SPEED Ładunek. BRAK `cargo_description` na `container`. BRAK `weight_kg`. BRAK PIN. BRAK VGM.

**Odrzucone:**

- `weight_kg` / `quantity` / `volume_m3` / VGM / TARA (Decimal).
- `pin_code` / ciphertext.
- `booking_no` / live HTTP.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `161_container_cargo` (po `160_container_remarks`): `cargo_description` nullable `String(256)`.
2. POST opcjonalny `cargo_description`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · ilość · objętość · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `cargo_description` zapisuje opis ładunku, bez PIN i bez wagi
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`ładunek`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `cargo_description`; 400 `ładunek`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(256)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie waga; leftover T3 PIN/VGM/`packaging_code`; C901 czysty |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
