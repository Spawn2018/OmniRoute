# Plaster 204.0 · D9b `shipment_ref` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D9 leftover · [162.0](../archived/162.0-document-template.md) leftover D9b · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D9
**Zależy od:** 90.0 `shipment` (zlecenie z wyceny już jest) · 162.0 `document_template` (layout zostaje; ten plaster nie drukuje)
**Status:** Zamknięty `/noc` 2026-09-09. Nie QR. Nie PDF. Nie 409 wyjazdu.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D9b–f + leftover V7 tacho + V8 + V6 EBITDA + V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7–V8 / V6 / V5 / V4 / V2b parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. leftover D9c 409 sieci parked. leftover D9d PDF/QR/ZPL parked. leftover D9e skan HITL parked. leftover D9f branding/pdf parked. Żywy plaster: **pogłębienie istniejącej** `shipment` — HITL twardy numer `shipment_ref` jako dana, nie generator GD/2026 i nie bitmapa QR. Kolumna nullable (stare `draft` bez numeru zostają). Unique `(organization_id, shipment_ref)` tam gdzie numer jest. Format: `fixture://shipment-ref/…` albo `omni://shipment/` + token (`[a-z0-9][a-z0-9._-]{1,62}`). Operator wpisuje numer przy zapisie zlecenia; serwis nie woła LLM, nie składa QR, nie drukuje PDF i nie woła HTTP. Zły numer → 400 (`numer`). Obce wskazanie → 400 (`obce`). GET zwraca numer albo `null`. UI zostaje `/shipments`. `SHIPPED_CHARGE_ROUTES["204.0"]` = `/shipments`. OpenFGA bez nowej relacji (`can_manage_shipments`). Migracja `144_shipment_ref`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `shipment.source_ref` — pochodzenie wiersza (`fixture://shipment/` / `tenant:manual`), **nie** twardy numer wydruku. ISTNIEJE `shipment_package` — skan QR Omni paczki, **nie** `shipment_ref`. ISTNIEJE `document_template` — layout `html_print`, **nie** numer. ISTNIEJE `ocean_bill` — HBL/MBL, **nie** numer Omni. ISTNIEJE how-to „numer GD/2026” jako poza zakresem. BRAK kolumny `shipment.shipment_ref`. BRAK QR. BRAK 409 wyjazdu z tego pola.

**Odrzucone:**

- QR bitmap / ZPL / PDF / bajty (D9d).
- `network_print_requirement` / 409 wyjazdu (D9c; nie C8).
- Skan zwrotny bez kodu Omni (D9e).
- Generator `GD/2026` / szablon M-03 nadający numer.
- NOT NULL na istniejących `draft`.
- F10 drabina match FV / auto-link.
- Kwota / druga marża / T-SQL / scrape / float / live HTTP.

## Zakres

1. Migracja `144_shipment_ref` (po `143_rank_mark`): kolumna `shipment_ref` nullable, unique per tenant gdy nie NULL.
2. POST opcjonalny `shipment_ref`. Zły numer → 400. Serwis nie importuje document_templates / charges / extraction i nie woła HTTP.
3. UI `/shipments`. `just api-types`. How-to `zlecenie.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

QR · PDF · ZPL · 409 sieci · D8 etykieta · generator GD · F10 auto-link · kwota · T-SQL · live HTTP

## Kryteria akceptacji

- [x] POST z `shipment_ref` zapisuje numer, bez QR
- [x] POST bez `shipment_ref` zostaje `null` (stary draft)
- [x] zły `shipment_ref` → 400 (`numer` albo `obce`)
- [x] GET zwraca numer tenanta
- [x] Unique per tenant; izolacja (ten sam numer u B niewidoczny)
- [x] Zero float / marży / QR / PDF / 409 / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `shipment_ref`; 400 `numer` / `obce`; GET; unique per tenant |
| Szybkość | N/A ten plaster | kolumna INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | unique `(organization_id, shipment_ref)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `shipment`; leftover D9c–f QR/PDF/409; bez nowego OPS job |
| Proza operatora | 5–15 zdań | `zlecenie.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN D9, GLOSSARY, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
