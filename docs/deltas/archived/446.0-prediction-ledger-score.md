# Plaster 446.0 · AI2 leftover — `prediction_ledger` bez wpisanego CRPS/MAE

**Status:** Zamknięty `/noc`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · E tabela AI2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI2

**Zależy od:** 193.0 `prediction_ledger` · 443.0 `interval_score`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `prediction_ledger`. POST i formularz **nie** przyjmują `crps` / `mae`. Kolumny zostają, stają się nullable. Stare wiersze z wpisem zostają. Nowy INSERT zapisuje NULL. Metryka liczona zostaje na `interval_score`.
- **Wybrane:** `PredictionLedgerCreate` bez pól `crps`/`mae` (`extra=forbid` → body z tymi polami = 422). Serwis nie liczy i nie wstawia zera.
- **Wybrane:** GET nadal pokazuje historyczny wpis, gdy kolumna nie jest pusta. Brak złączenia z `interval_score` w tym API.
- **Odrzucone:** drop kolumn (`REJECTED` — gubi historię wpisu).
- **Odrzucone:** kopiowanie `interval_score` do wiersza (`REJECTED` — drugi magazyn metryki; zakaz importu BC).
- **Odrzucone:** `0` jako zaślepka (`REJECTED` — zero to legalny wynik).
- **Odrzucone:** Brier · auto-champion · detektor/próg · float · druga marża · SQL na `charge`.

**Łowca:** ISTNIEJE `prediction_ledger.crps`/`mae` NOT NULL + POST + formularz (193.0). PODOBNE `interval_score` (443.0) — metryka **liczona**, nie klonować do ledgeru. BRAK ścieżki zapisu bez wpisu. `just dup` bez wyodrębnienia.

## Zakres

Migracja `356_prediction_ledger_null` (revises `355_version_window`): `crps` i `mae` nullable; CHECK `>= 0` zostaje na wartościach niepustych. Ten sam OpenFGA `can_manage_prediction_ledgers`. UI `/prediction-ledgers` bez pól CRPS/MAE. `SHIPPED["446.0"]` → `/prediction-ledgers`. Bez nowego OPS_JOB.

## Poza zakresem

Brier · auto-champion · detektor · drop kolumn · trzy BC · `data_source` · AI4.1

## Kryteria akceptacji

- [x] POST bez `crps`/`mae` → 201, kolumny NULL
- [x] POST z `crps` albo `mae` w body → 422
- [x] GET starego wiersza z wpisem nadal zwraca te liczby
- [x] brak importu `interval_scores` / `suggestion_ledgers` / `outcome_ledgers` w serwisie
- [x] brak `float`; craft_style funkcje>40: 0

## Leftover

Brier · auto-champion · detektor/próg · trzy BC · `data_source` (AI5) · **AI3.0** PATCH szkicu

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST bez metryki; 422 na wpis; GET historii |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS |
| SQL | leftover | ALTER nullable; EXPLAIN nie w tym plasterze |
| N+1 | OK | bez nowego SELECT w pętli |
| Dług / człowiek | OK | brak Pythona na CRPS; brak zera-zaślepki |
| Proza operatora | 5–15 zdań | `docs/operator/ledger-predykcji.md` |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | billing CI jak 441–445 |
| Minuty plastra | N/A | |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
