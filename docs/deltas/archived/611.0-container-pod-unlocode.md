# Plaster 611.0 · T3 `pod_unlocode` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T3 · [610.0](610.0-container-pol-unlocode.md)
**Zależy od:** 153.0 `container`; 610.0 `pol_unlocode`
**Status:** Zamknięty `/noc` 2026-09-21. Nie FK portu. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z karty T3 `pod_port_id` / `destination_city` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** `pol_unlocode` jest w 610.0 (gate success). FK `port` odrzucone — serwis kontenera nie importuje geography. leftover `destination_city` parked. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `pod_unlocode` (`String(5)` HITL). POST bez pola zostaje `null`. Pusty trim → `null`. Nie-tekst, długość inna niż 5 albo znak spoza UN/LOCODE → 400 (`pod`). Wielkość liter składana do wielkich. `require_pod_unlocode` w `app.domain.container` — ten sam kształt co `require_pol_unlocode` (`_unlocode_shape`), **nie** resolve słownika portów, **nie** Haversine. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["611.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `486_container_pod_unlocode` (po `485_container_pol_unlocode`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.pol_unlocode` (610.0). ISTNIEJE `shipment_leg.destination_location_id` (lokalizacja nogi, nie kod POD na kontenerze). BRAK `container.pod_unlocode`. BRAK `container.pod_port_id`.

**Odrzucone:**

- `destination_city` / FK `port` / resolve geography / HW / UXCL / N3 / AI3 / G0-SH / mapa / float / marża / Auth0.

## Zakres

1. Migracja `486_container_pod_unlocode`: `pod_unlocode` nullable String(5). Nie `create_table`.
2. POST opcjonalny `pod_unlocode`. Zła wartość → 400 (`pod`). Serwis nie importuje charges ani geography.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

destination_city · FK portu · mapa · HW · UXCL · N3 · Auth0 · live HTTP · float

## Kryteria akceptacji

- [x] POST z `pod_unlocode` zapisuje pięć znaków UN/LOCODE, bez resolve portu
- [x] POST bez pola / pusty trim zostaje `null`
- [x] małe litery → wielkie
- [x] nie-tekst / zła długość / zły znak → 400 (`pod`)
- [x] RLS istniejącego `container`; izolacja bez nowej tabeli
- [x] Zero float / marży / live HTTP / import charges / import geography

## Leftover

`destination_city` · HW · UXCL · N3 · AI3 · G0-SH

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | CI po push | POST opcjonalny `pod_unlocode`; 400 `pod`; puste → null; wielkie litery. PRZESZŁO dopiero gdy gate na SHA = success |
| Szybkość | N/A | brak ścieżki z budżetu AGENTS |
| SQL | N/A | add_column; bez nowego SELECT |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | `_unlocode_shape` współdzielone z pol; wzór 610 |
| Proza operatora | 5–15 zdań | `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | po `--close` | `factory_cycle.py --close` |
