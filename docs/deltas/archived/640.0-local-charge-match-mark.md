# Plaster 640.0 · leftover P4c matching stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 · [166.0](../archived/166.0-local-charge.md) · [635.0](../archived/635.0-local-charge-warning-mark.md)
**Zależy od:** 635.0 `local_charge_warning_mark` już jest
**Status:** Zamknięty `/noc 17` 2026-09-23. Nie matching SQL. Nie warning-jako-fakt.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P4c „matching braków vs local_charge”):** katalog `local_charge` zostaje prawdą o dopłacie Decimal. Warning stance zostaje w `local_charge_warning_mark` (635). Silnik matching SQL vs braki / warning-jako-fakt / 409 wyceny zostają park. Żywy plaster: **nowa tabela** `local_charge_match_mark` per tenant — HITL stance dopasowania dopłaty lokalnej. Wiersz = `mark_code` (snake 1–32) + `match_kind` (`match`|`gap`|`waive`|`other`) + `source_ref` (`tenant:manual` albo `fixture://local-charge-match/…`). Operator zapisuje, jak traktuje lukę dopłaty względem katalogu (dopasuj / luka / zwolnij / inne); serwis **nie** porównuje do `local_charge`, nie czyta `charge` i nie zwraca 409. Zły kind → 400 (`dopasowanie`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `local_charges` / `local_charge_warning_marks` / charges / quotations. UI `/local-charge-match-marks`. `SHIPPED_CHARGE_ROUTES["640.0"]` = `/local-charge-match-marks`. OpenFGA: `can_manage_local_charge_match_marks`. Migracja `512_local_charge_match_mark` po `511_margin_match_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 166.0 / 635.0 P4c matching braków — tu tylko HITL stance, nie silnik matching. Źródło: `docs/deltas/archived/166.0-local-charge.md`; PLAN wiersz P4; `docs/ops/docs-debt.md` § 635.0.

**Łowca:** ISTNIEJE `local_charge` — dopłata Decimal, nie stance. ISTNIEJE `local_charge_warning_mark` — ostrzeżenie, nie matching. ISTNIEJE `margin_match_mark` — wzorzec stance match. BRAK `local_charge_match_mark`. BRAK `/local-charge-match-marks`. BRAK matching SQL vs local_charge.

**Odrzucone:**

- matching SQL vs `local_charge` / warning-jako-fakt
- 409 na wycenie / auto-linia `charge`
- FK UUID do `local_charge` / `quotation` / `local_charge_warning_mark`
- float / marża / live HTTP

## Zakres

1. Migracja `512_local_charge_match_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `match_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/local-charge-match-marks` + zdanie w how-to dopłat lokalnych.

## Poza zakresem

matching SQL · warning-jako-fakt · 409 wyceny · FK · float · live HTTP

## Plan plików

- `backend/alembic/versions/512_local_charge_match_mark.py`
- `backend/app/models/local_charge_match_mark.py`
- `backend/app/domain/local_charge_match_mark.py`
- `backend/app/repositories/local_charge_match_marks/`
- `backend/app/services/local_charge_match_marks/` (+ AGENTS.md)
- `backend/app/api/local_charge_match_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/local-charge-match-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `match_kind=match` zapisuje wiersz; `local_charge` i `charge` bez zmian
- [x] kind spoza `match`|`gap`|`waive`|`other` → 400 (`dopasowanie`); amount → 422; duplikat kodu → 409
- [x] serwis nie importuje `local_charges` / `charges` i nie woła HTTP
- [x] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / matching SQL

## Leftover

warning-jako-fakt (compose) · matching SQL · FK local_charge · P5c · live HTTP

## Hotfix

Type hint migracji bez `sa.SchemaItem` (CI alembic load).
