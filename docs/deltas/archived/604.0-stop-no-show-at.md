# Plaster 604.0 · T1 EXP1 `no_show_at` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [603.0](603.0-stop-appointment-status.md) leftover · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 603.0 `appointment_status`; 252.0 `appointment_ref`
**Status:** Zamknięty `/noc` 2026-09-20. Nie dok. Nie charge no_show. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `no_show_at` + leftover weigh_in/out + leftover HW + leftover UXCL + Job#2 + N3 + AI3 + G0-SH):** leftover `weigh_in_kg` / `weigh_out_kg` parked. leftover HW parked. leftover UXCL parked. Job#2 parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `no_show_at` (`DateTime(timezone=True)` HITL, ISO-8601 ze strefą, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`niestawiennictwo`). `require_stop_no_show_at` w `app.domain.stop` — opcjonalny zegar, reużywa `_require_eta_clock`, **nie** import `dock_appointments` / `charges` / auto-`appointment_status`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["604.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `479_stop_no_show_at` (po `478_stop_appointment_status`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.appointment_ref` (252.0). ISTNIEJE `stop.appointment_status` (603.0). ISTNIEJE `stop.waiting_started_at` (254.0) — wzór zegara. BRAK `stop.no_show_at`. BRAK `weigh_in_kg` / `weigh_out_kg`.

**Odrzucone:**

- `weigh_in_kg` / `weigh_out_kg` / HW / UXCL / Job#2 / N3 / AI3 / G0-SH / szkic `charge` no_show / auto-status awizacji / T8 / PIN / mapa / Auth0 / live HTTP / float.

## Zakres

1. Migracja `479_stop_no_show_at`: `no_show_at` nullable timestamptz. Nie `create_table`.
2. POST opcjonalny `no_show_at`. Zły token → 400 (`niestawiennictwo`). Serwis nie importuje dock/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

weigh_in/out · dok / T8 · HW · UXCL · PIN · mapa · Auth0 · live HTTP · float · charge no_show

## Kryteria akceptacji

- [x] POST z `no_show_at` zapisuje chwilę niestawiennictwa HITL, bez auto-statusu
- [x] POST bez pola / pusty trim zostaje `null`
- [x] nie-tekst / bez strefy / nie ISO → 400 (`niestawiennictwo`)
- [x] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [x] Zero float / marży / mapy / doku / live HTTP / import `dock_appointments`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | POST opcjonalny `no_show_at`; 400 `niestawiennictwo`; pusty → null |
| Szybkość | N/A ten plaster | kolumny INSERT/GET |
| SQL | N/A | timestamptz nullable |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | wzór `waiting_started_at`; leftover weigh_in/out |
| Proza operatora | 5–15 zdań | `zlecenie.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T1, GLOSSARY |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
