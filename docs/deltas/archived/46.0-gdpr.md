# Plaster 46.0 · M-56 RODO (`gdpr`)

**Spec źródłowa:** [docs/spec/gdpr.md](../../spec/gdpr.md)  
**Zależy od:** 0.3 `app_user.email`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 9. LLM nie liczy. Nie nowa tabela. Nie usuwanie.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 8 — następna nazwana pozycja):** PLAN wymienia w Fali 8 **M-53 sankcje** (45.0) i **M-56 RODO**. M-54 i M-55 **nie mają żywej nazwy** w PLAN/MODULES — nie zgaduję z archiwum. RODO **nie ma tabeli wniosków**. Operator widzi użytkowników tenanta (`email`, `display_name`) jako inwentarz danych w `/gdpr`. Token `gdpr` (UI, nie tabela). Żywy ID **M-56**. 0.5 `/tenancy/users` zostaje administracją kont.

**Łowca:** BRAK `gdpr` / `erasure` / `consent`. ISTNIEJE: `fetchTenancyUsers`, `email`. PODOBNE: 0.5 lista users; 45.0 tablica party. Postgres: zero kolumn RODO.

**Odrzucone:**

- Tabela wniosków / DPIA / zgód — zgadywanie schematu (CURRENT).
- Zgadywanie nazwy M-54/M-55 z archiwum.
- Endpoint usuwania konta w tym plasterze.
- Pokazywanie `password_hash`.
- LLM czytający treść ekstrakcji jako inwentarz.

## Zakres

Read-only `/gdpr`. `data-gdpr="board"`. Brak migracji. `gdprSubjects(users)`. Link do `/tenancy/users`.

## Poza zakresem

Tabela wniosków · usuwanie · DPIA · ExtractionService

## Ustalenia

- Token: `gdpr` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia tenancy users.
- RLS: bez nowej tabeli.
- Job: `/gdpr`. `SHIPPED_CHARGE_ROUTES["46.0"]` = `/gdpr`. OPS_JOBS +1 → 43.
- `just dup` ≤ 3% (2,85%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 46.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/gdpr` ma `data-gdpr="board"` i woła `gdprSubjects`
- [x] Widać `email` i `display_name`; brak `password_hash` na ekranie
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["46.0"]` = `/gdpr`
- [x] `just dup` ≤ 3% (2,85%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | emaile kont; brak wniosków |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | wnioski / usuwanie / M-54 nazwa |
| Docs/OS | PRZESZŁO | spec `gdpr.md`; CURRENT → Plan M-57 |

## Knowledge

Nie RAG. Nie silnik DPIA.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. M-54/M-55 bez nazwy świadomie pominięte. Wnioski leftover.
