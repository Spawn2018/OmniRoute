# Plaster 60.0 · Q-E1 katalogi Grupa A (`catalog-parts`)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Cel jakości + § Kolejka Q-E1  
**Zależy od:** `components/catalog/catalog-parts.tsx` (4.0) + leftover U-catalog-parts

**Status:** Zamknięty 2026-09-02. Nie nowy M-xx. Nie mixin modeli. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** trzecie powtórzenie chrome katalogu (nagłówek, notka sesji, błąd, tabela) na trzech katalogach zapisu Grupa A: `/charges`, `/organization-settings`, `/rate-lines`. Formularze pól zostają lokalne — `CatalogCreateForm` jest pod kod+nazwa+aliasy, nie pod kwoty/`source_ref`.

**Łowca:** ISTNIEJE: `CatalogHeading`, `TenantSessionNotice`, `CatalogError`, `CatalogLoadedTable`. PODOBNE: `/quotations` (własny chrome + panele jobów). BRAK: mixin `organization_id` na modelach — zakaz Q-E1.

**Odrzucone:**

- Mixin wszystkich modeli SQLAlchemy (python 3,48% = nagłówki `id`/`organization_id`).
- Przepięcie `/quotations` (panele NBP/kredyt/kanał — nie trzecie powtórzenie tabeli).
- `CatalogLoadedTable` na `/rate-lines` — test 53.0 czyta `DataTableShell` i `allowCondensed` ze źródła strony.

## Zakres

Trzy ruchy, zachowanie jobu bez zmian, testy bez zmiany asercji.

1. `/charges` → `CatalogHeading` + `TenantSessionNotice` + `CatalogError` + `CatalogLoadedTable`
2. `/organization-settings` → to samo
3. `/rate-lines` → `CatalogHeading` + `TenantSessionNotice` + `CatalogError`; `DataTableShell` + `allowCondensed` zostaje

## Poza zakresem

Q-E2 Alembic/EXPLAIN · mixin modeli · `/quotations` · F9.1 · S1

## Ustalenia

- C901 czyste. `just dup` 2,81% → 2,78% total (tsx 2,85% → 2,76%).
- `refactor_ratio` 0,6% / 10% za 4 tyg. — ten slot spłaca chrome, nie ratio okna.
- OpenFGA / RLS / API: bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [x] Trzy katalogi Grupa A bez własnego h2 / notki sesji / banneru błędu
- [x] `/rate-lines` nadal ma `allowCondensed` i `DataTableShell` w źródle
- [x] Testy bez zmiany asercji; `just test` po każdym ruchu
- [x] Brak mixin modeli

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | chrome na `catalog-parts`; formularze i mutacje bez zmian |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu AGENTS w diffie |
| SQL | N/A | brak SELECT/INSERT/UPDATE/resolve |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | leftover | `/quotations` nadal własny chrome; nagłówki modeli bez mixinu (zakaz Q-E1); `rate-lines` bez `CatalogLoadedTable` przez test 53.0 |
| Proza operatora | N/A | brak nowego jobu zapisu |
| Docs/OS | PRZESZŁO | CURRENT → Q-E2 Plan; `just docs` |

## Pytania

Brak. Następny: Q-E2 `/plan-modul`.
