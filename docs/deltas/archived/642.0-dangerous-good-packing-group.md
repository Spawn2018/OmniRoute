# Plaster 642.0 · leftover EXP0.9 packing group HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § EXP0.9 · [192.0](../archived/192.0-dangerous-good-adr.md) · [7.0](../archived/7.0-dangerous-good.md)
**Zależy od:** 192.0 `dangerous_good` z `adr_tunnel_code` + `segregation_group`
**Status:** Zamknięty `/noc 21` 2026-09-23. Nie live IMO. Nie LLM klasy.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP0.9 „packing group”):** katalog `dangerous_good` zostaje prawdą o UN + IMDG + tunel ADR + SG. Grupy zgodności 1.xA / live IMO / LLM klasa zostają park. Żywy plaster: **pogłębienie istniejącej** `dangerous_good` — HITL `packing_group` jako dana (`I`|`II`|`III`), nie nadanie klasy przez LLM i nie live IMO. Operator wpisuje grupę pakowania z tabeli IMDG/ADR; serwis nie zmienia `imdg_class`, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. Zła grupa → 400 (`pakowanie`). Istniejące wiersze: backfill `II` (środek allowlisty; nie zgadujemy I/III). GET listy SQL. UI zostaje `/dangerous-goods`. `SHIPPED_CHARGE_ROUTES["642.0"]` = `/dangerous-goods`. OpenFGA bez nowej relacji (`can_manage_dangerous_goods`). Migracja `514_dangerous_good_packing_group` po `513_local_charge_bind_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 192.0 / PLAN EXP0.9 packing group HITL. Źródło: `docs/deltas/archived/192.0-dangerous-good-adr.md`; PLAN wiersz EXP0.9; `docs/ops/docs-debt.md` § 642.0 PLAN.

**Łowca:** ISTNIEJE `dangerous_good` — UN + IMDG + tunel + SG, **nie** packing group. ISTNIEJE `un_segregation_mark` — stance tunel/segregacja, **nie** kolumna PG na UN. BRAK `packing_group` na `dangerous_good`. BRAK live IMO. BRAK LLM klasy.

**Odrzucone:**

- live IMO / scrape / HTTP
- nadanie `imdg_class` z packing group albo z LLM
- grupy zgodności 1.xA / marine pollutant / limited quantity
- nowa tabela mark / FK do `un_segregation_mark`
- kwota / druga marża / float / T-SQL

## Zakres

1. Migracja `514_dangerous_good_packing_group`: kolumna NOT NULL po backfillu (`II`). CHECK `I`|`II`|`III`.
2. POST wymaga `packing_group`. Zła wartość → 400 (`pakowanie`). Serwis nie importuje quotations / charges / extraction i nie woła HTTP.
3. UI `/dangerous-goods` (pole w formularzu). How-to / fold w katalogach DG. `just api-types`.

## Poza zakresem

live IMO · LLM klasa · grupy 1.xA · marine pollutant · LQ · kwota · float · T-SQL · live HTTP · nowa tabela mark

## Plan plików

- `backend/alembic/versions/514_dangerous_good_packing_group.py`
- `backend/app/models/dangerous_good.py` (+ CHECK)
- `backend/app/domain/dangerous_good.py` (`normalize_packing_group`)
- serwis / API / testy HTTP + izolacja (istniejące BC)
- `frontend/src/features/dangerous-goods/` + `dangerous-goods-api.ts`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts` (`642.0` → `/dangerous-goods`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt · spec

## Kryteria akceptacji

- [x] POST z `packing_group=II` zapisuje wiersz; `imdg_class` bez zmian z LLM
- [x] wartość spoza `I`|`II`|`III` → 400 (`pakowanie`)
- [x] serwis nie importuje charges / quotations / extraction i nie woła HTTP
- [x] RLS: tenant B nie widzi UN tenanta A; zero float / marży / live IMO

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | packing_group HITL; imdg bez LLM |
| Szybkość | N/A | ten plaster |
| SQL | N/A | INSERT/UPDATE kolumna; bez nowego SELECT silnika |
| N+1 | OK | `_reject_taken` jak wcześniej |
| Dług / człowiek | OK | `create_good` rozbite pod sufit 40 |
| Proza operatora | 5-15 | fold `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc 21` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |

## Leftover

live IMO · grupy zgodności 1.xA · marine pollutant · LQ
