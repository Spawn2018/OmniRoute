# Plaster 39.0 · M-46 Koszt obsługi klienta (`cost_to_serve`)

**Spec źródłowa:** [docs/spec/cost-to-serve.md](../../spec/cost-to-serve.md)  
**Zależy od:** 11.0 `customer_sop` · 5.1 `quotation.party_id`

**Status:** Zamknięty `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie suma.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 6 M-46):** koszt obsługi **nie ma tabeli stawek godzinowych**. Operator wybiera jednego kontrahenta i widzi jego `customer_sop` oraz wyceny z `party_id` — bez sumowania kwot. Token `cost_to_serve` (UI, nie tabela). Żywy ID **M-46**. 11.0 zostaje katalogiem SOP; 2.0 zostaje katalogiem wyceny.

**Łowca:** BRAK `cost_to_serve` / ABC / tabeli kosztu obsługi. ISTNIEJE: `fetchParties`, `fetchCustomerSops`, `fetchQuotations` z filtrem `partyId`, `<Money/>`. PODOBNE: 11.0 SOP; 25.0 jeden kontrahent; 28.0 wyceny z `party_id`.

**Odrzucone:**

- Tabela kosztu / stawka godziny / ABC — zgadywanie schematu (CURRENT).
- Suma kwot wycen w JS — leftover Decimal w SQL; LLM nie liczy.
- `charge` na tym ekranie — brak `party_id` na `charge` (zgadywanie FK).

## Zakres

Read-only `/cost-to-serve`. `data-cost-to-serve="board"`. Brak migracji. `customerSopsForParty(sops, partyId)` + `fetchQuotations` dla wybranego `party_id`. Linki do `/customer-sops` i `/quotations`.

## Poza zakresem

Tabela ABC · suma wycen · stawka godziny · `charge` bez `party_id` · ExtractionService

## Ustalenia

- Token: `cost_to_serve` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia party / SOP / quotation.
- RLS: bez nowej tabeli.
- Job: `/cost-to-serve`. `SHIPPED_CHARGE_ROUTES["39.0"]` = `/cost-to-serve`. OPS_JOBS +1 → 36.
- `just dup` ≤ 3% (2,92%).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 39.0 nie dodaje tabeli.

## Kryteria akceptacji

- [x] `/cost-to-serve` ma `data-cost-to-serve="board"` i woła `customerSopsForParty`
- [x] Kwoty wycen przez `<Money/>`; brak sumy / `parseFloat`
- [x] Brak nowej migracji
- [x] `SHIPPED_CHARGE_ROUTES["39.0"]` = `/cost-to-serve`
- [x] `just dup` ≤ 3% (2,92%)

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | SOP + wyceny wybranego party; brak sumy |
| Szybkość | N/A ten plaster | odczyt lokalnych list |
| Dług w diffie | leftover | ABC / suma wycen / party_id na charge zostaje |
| Docs/OS | PRZESZŁO | spec `cost-to-serve.md`; CURRENT → Plan M-47 |

## Knowledge

Nie RAG. Nie silnik ABC.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. ABC i suma wycen świadomie leftover.
