# Plaster 556.0 Â· C8 leftover â€” HITL `create_block_mark`

**Spec ĹşrĂłdĹ‚owa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) Â§ Fala C Â· C8 Â· [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**ZaleĹĽy od:** tenancy Â· wzorzec HITL mark Â· C8 `relation_document_requirement` (obok, nie to samo)

**Status:** zaakceptowany `/noc` â€” wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** wÄ™ĹĽszy C8 leftover. Nowa tabela `create_block_mark`: `mark_code` (snake 2â€“32) + `block_kind` `block|warn|allow|other` + `source_ref`. Append-only. RLS. Katalog stancji bramki tworzenia zlecenia â€” nie live 409 na `POST shipment`.
- **Wybrane:** POST/GET `/api/v1/create-block-marks`. OpenFGA `can_manage_create_block_marks`. UI `/create-block-marks`. `SHIPPED["556.0"]`.
- **Odrzucone:** live `blocks_create` / HTTP 409 na `POST shipment` (`REJECTED` â†’ leftover). Para `document_kind` / matching do `party_document` (`REJECTED` â†’ leftover). Kwota / float / scoring osoby (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / ranking SQL / PUESC / bind filing.

**Ĺowca:** BRAK `create_block_mark`. ISTNIEJE `relation_document_requirement` (zestaw relacji, nie brama create). ISTNIEJE `party_document` (rodzaj na party). ISTNIEJE `document_checklist_rule` (incotermĂ—mode).

## Zakres

1. Migracja `436_create_block_mark` + RLS + izolacja.
2. Model â†’ repo â†’ serwis â†’ API â†’ OpenFGA â†’ FE â†’ how-to.
3. `SHIPPED["556.0"]` â†’ `/create-block-marks`.

## Poza zakresem

409 shipment Â· blocks_create egzekucja Â· document_kind matching Â· PUESC Â· AI3 Â· G0 Â· kwota

## Kryteria akceptacji

- [x] POST zĹ‚y kind / obce source_ref / pusty kod â†’ 400
- [x] izolacja tenantĂłw
- [x] UI + SHIPPED
- [x] zero float / live HTTP / amount / 409

## Leftover

blocks_create egzekucja Â· 409 shipment Â· document_kind matching Â· scoring
