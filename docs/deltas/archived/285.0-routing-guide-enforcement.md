# Plaster 285.0 · CT4 `routing_guide_enforcement` HITL (katalog trybu 409)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 leftover
**Zależy od:** 0.3 tenancy · 279.0 `routing_guide` · 284.0 (oś pin)
**Status:** Zamknięty `/noc` 2026-09-11. Nie 409 na shipment. Nie wspólny SELECT.

## Wybrane / odrzucone / dlaczego

**Wybrane:** leftover egzekucja 409 jako **katalog trybu**: `record_only` | `block_409`. CT9 = już C5 `carbon_method` (park w docs). Żywy plaster: tabela `routing_guide_enforcement` — `mark_code` + `enforcement_kind` + `source_ref`. UI `/routing-guide-enforcements`. OpenFGA `can_manage_routing_guide_enforcements`. Migracja `218_routing_guide_enforcement`. Job OPS 130. BC `routing_guide_enforcements`.

**Łowca:** BRAK `routing_guide_enforcement`. ISTNIEJE `routing_guide` (katalog trasy, nie tryb 409).

**Odrzucone:** HTTP 409 na POST shipment/ASN · SQL vs guide · float · kwota · UPDATE/DELETE.

## Zakres

1. Migracja `218_routing_guide_enforcement` po `217_collaboration_mark`: RLS FORCE.
2. POST/GET `/routing-guide-enforcements`. OpenFGA. Deny-list extract.
3. UI `/routing-guide-enforcements`. How-to. Job OPS 130.

## Kryteria akceptacji

- [x] POST z kodem, kind record_only|block_409 i source_ref
- [x] zły kod → 400; zły kind → 400; obce source_ref → 400
- [x] POST z amount/shipment_id → 422
- [x] izolacja RLS; zero float/HTTP; zero 409 w API shipment

## Leftover po 285.0

Żywy HTTP 409 przy zapisie zlecenia/ASN poza guide. CT9 (już C5). CT2/CT5/CT8 live.

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; RLS; OpenFGA |
| Szybkość | N/A | katalog |
| SQL | EXPLAIN | list uses org index |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | FE segmented buttons (jscpd) |
| Proza operatora | 5–15 zdań | docs/operator/egzekucja-przewodnika.md |
| Docs/OS | PRZESZŁO | CURRENT + just docs |
| Pushy do zielonego | N/A | lokalny gate /noc |
| Minuty plastra | N/A | noc |
| Bench rzemiosła | PRZESZŁO | factory_cycle --close |
