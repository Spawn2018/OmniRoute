# Plaster 646.0 · leftover EXP1 rate_line allotment_teu HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [1.1](../archived/) rate_line
**Zależy od:** M-07 `rate_line` (niemutowalna stawka kupna)
**Status:** Zamknięty `/noc 7` 2026-09-24. Nie matching. Nie float. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 allotment TEU):** tabela `rate_line` zostaje prawdą o stawce kupna + `source_ref`. Matching WHEN/IF / T-SQL / auto-allotment / live armator zostają park. Żywy plaster: **pogłębienie istniejącej** `rate_line` — opcjonalny `allotment_teu` (`Numeric(14,4)` Decimal, ≥ 0, nullable). Operator wpisuje alokację TEU z kontraktu; serwis nie mnoży TEU × stawka, nie tworzy `charge`, nie woła sieci, nie woła LLM. POST bez pola → `null`. Istniejące wiersze: `null`. Zmiana alokacji = supersede (nowy wiersz), jak każda zmiana na `rate_line`. GET listy SQL. UI `/rate-lines`. `SHIPPED_CHARGE_ROUTES["646.0"]` = `/rate-lines`. OpenFGA bez nowej relacji. Migracja `518_rate_line_allotment` po `517_cargo_claim_evidence`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**Dlaczego na `rate_line`, nie mark:** EXP1 karty mówi „rate_line/charge: … allotment_teu”. Mark katalogowy gubi powiązanie ze stawką; kolumna nullable na niemutowalnym wierszu jest węższym HITL.

**REQUIREMENT:** leftover EXP1 po zamknięciu evidence 645.0. Źródło: `docs/analysis/karty-pol-fala-exp.md` § EXP1; CURRENT Następny **646.0**.

**Łowca:** ISTNIEJE `rate_line` — charge_code + amount Decimal + currency + source_ref + superseded_by, **nie** allotment. ISTNIEJE `fuel_index` — indeks FSC, **nie** TEU. ISTNIEJE `spot_contract_mark` — spot/contract stance, **nie** TEU na stawce. BRAK `allotment_teu` na `rate_line`. BRAK matching. BRAK float.

**Odrzucone:**

- float / mnożenie TEU × amount w Pythonie
- zapis `charge` / druga marża
- matching WHEN/IF / T-SQL
- nowa tabela mark zamiast kolumny
- live HTTP armatora / scrape MQC
- mutacja `allotment_teu` w miejscu (tylko supersede)

## Zakres

1. Migracja `518_rate_line_allotment`: kolumna `allotment_teu` NUMERIC(14,4) nullable.
2. POST opcjonalnie przyjmuje `allotment_teu`. Zła wartość (ujemna / nie Decimal) → 400. Serwis nie importuje charges / quotations / extraction.
3. UI `/rate-lines` (opcjonalne pole). How-to / fold. `just api-types`. Fixture bez pola zostają `null`.

## Poza zakresem

matching · float · charge · druga marża · live HTTP · mark · mutacja w miejscu · index_id FSC

## Plan plików

- `backend/alembic/versions/518_rate_line_allotment.py`
- model / domain / serwis / API / testy rate_lines
- `frontend/src/features/rate-lines/` + API client
- `ops-index` + `routes-breadth` (`646.0` → `/rate-lines`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [x] POST z `allotment_teu` Decimal ≥ 0 zapisuje wiersz; bez pola → null
- [x] ujemny / float-string odrzucony
- [x] serwis nie importuje charges / quotations / extraction i nie woła HTTP
- [x] RLS: tenant B nie widzi stawek tenanta A; zero float / marży / matching

## Leftover

index_id FSC na rate_line · spot_or_contract kolumna · matching · live MQC

## Pętla po plasterze

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | allotment_teu HITL; pytest + vitest |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN nullable; brak nowego SELECT |
| N+1 | OK | brak pętli z zapytaniem |
| Dług / człowiek | OK | NamedTuple nie potrzebny; domain ≤40 |
| Proza operatora | 5–15 zdań | `katalogi.md` — opcjonalny TEU |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
