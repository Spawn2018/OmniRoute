# Plaster 265.0 · B0b `plan_snapshot` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § B0b leftover `plan_snapshot` · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § `plan_snapshot`
**Zależy od:** 0.3 tenancy · 152.0 `trip` / 151.0 `resource` / 35.0 `shipment` (obiekty istnieją; ten plaster ich nie importuje)
**Status:** Zamknięty `/noc` 2026-09-10. Nie kółka. Nie what-if. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `plan_snapshot`/kółka + leftover TT z actuals + leftover what-if V8 + leftover 8 silników + leftover F9 Optima + leftover T8 + leftover instancja `task` + leftover konsument M-02 + S53 + PIN):** leftover kółka / `circle_sim` parked (oś: `plan_snapshot` **przed** kółkami, nie w jednym worku). leftover TT z actuals `stop` parked. leftover V8 what-if parked. leftover 8 silników fizyki parked. leftover F9 Optima fixture parked. leftover T8 parked. leftover instancja `task` parked. leftover konsument / worker parked. S53 parked. PIN parked. Żywy plaster: **nowa tabela** `plan_snapshot` per tenant — HITL wersja planu jako dane, nie silnik i nie mapa. Wiersz = `snapshot_code` (snake 2–32) + `shipment_id` / `trip_id` / `resource_id` (UUID jako dane, **bez FK**, bez odczytu tych BC) + `author_label` (tekst 1–64) + `recorded_at` (`timestamptz` HITL) + `source_ref` (`tenant:manual` albo `fixture://plan-snapshot/…`). Operator wpisuje trójkę zlecenie→przejazd→zasób, autora i czas; serwis nie liczy km, nie woła HTTP, nie woła LLM, nie czyta `charge` / `trip` / `shipment` / `resource` i nie nadpisuje wiersza. Append-only INSERT. Zły kod → 400 (`migawka`). Zły UUID zlecenia → 400 (`zlecenie`). Zły UUID przejazdu → 400 (`przejazd`). Zły UUID zasobu → 400 (`zasob`). Zły autor → 400 (`autor`). Zły czas → 400 (`czas`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, snapshot_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/plan-snapshots`. `SHIPPED_CHARGE_ROUTES["265.0"]` = `/plan-snapshots`. OpenFGA: `can_manage_plan_snapshots`. Migracja `198_plan_snapshot` (po `197_outbox_task_template_kind`). Job OPS 111. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `prediction_ledger` 193.0 — przedział CRPS/MAE, **nie** wersja planu. ISTNIEJE `twin_mark` 199.0 — rodzaj bliźniaka w tym `plan`, **nie** snapshot przypisań. ISTNIEJE `trip.expected_buy` 167.0 — freeze kosztu kupna, **nie** trójka zlecenie→przejazd→zasób. ISTNIEJE `entity_event` — ledger inquiry/quote, **nie** wersje planu. ISTNIEJE `shipment` / `trip` / `resource` — żywe obiekty, **nie** historia planu. BRAK tabeli `plan_snapshot` przed tym plasterem. BRAK `circle_sim`. BRAK what-if V8.

**Odrzucone:**

- kółka / `circle_sim` / km ładowny (G2.20–G2.22).
- TT z actuals `stop`.
- what-if V8 / OptiPlaner / F9 Optima fixture.
- 8 silników fizyki / tacho V7.
- FK do `shipment` / `trip` / `resource` i import tych BC w serwisie.
- UPDATE/DELETE wiersza (wersja = nowy INSERT).
- T8 live API · instancja `task` · konsument outboxa · Auth0 · PIN.
- kwota / marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `198_plan_snapshot`: RLS FORCE, izolacja, unique kod + `source_ref`. UUID bez FK. Nie kolumny na `trip`.
2. POST/GET append-only. Zły token → 400 (`migawka` / `zlecenie` / `przejazd` / `zasob` / `autor` / `czas` / `obce`). Serwis nie importuje trips/shipments/resources/charges i nie woła HTTP. Brak UPDATE/DELETE.
3. UI `/plan-snapshots`. How-to `docs/operator/plan-snapshot.md`; fold w `katalogi.md` (40 linii). Job OPS 111. OpenFGA `can_manage_plan_snapshots`. Nazwy: `SnapshotDesk` / `snapshot-form.tsx` / `persistSnapshot`.

## Poza zakresem

kółka · circle_sim · TT actuals · what-if V8 · F9 Optima · T8 · fizyka · FK obiektów · konsument · Auth0 · kwota · float

## Kryteria akceptacji

- [x] POST zapisuje `plan_snapshot` z kodem, trójką UUID, autorem i czasem, bez HTTP
- [x] zły kod → 400 (`migawka`); zły UUID → 400 (`zlecenie` / `przejazd` / `zasob`)
- [x] GET zwraca migawki tenanta
- [x] Test izolacji nowej tabeli
- [x] Zero float / marży / kółek / what-if / live HTTP / FK do trip/shipment/resource

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST/GET HITL; 400 tokenów PL; RLS + unique kod/`source_ref`; zero FK |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN leftover | EXPLAIN listy po `organization_id` (indeks/unique); EXPLAIN ANALYZE nie w tym plasterze |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | OK | nowa tabela append-only; leftover kółka / what-if / TT actuals |
| Proza operatora | 5–15 zdań | `plan-snapshot.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | PRZESZŁO | CURRENT, PLAN B0b, GLOSSARY, MODULES B0b, spec |
| Pushy do zielonego | N/A | GitHub watch po pushu |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |

## Implementacja (265.0)

- Migracja `198_plan_snapshot` po `197_outbox_task_template_kind`. RLS FORCE.
- `PlanSnapshotService.persist_snapshot` — append-only, bez importu trips/shipments/resources/charges.
- UI `/plan-snapshots`. `SHIPPED_CHARGE_ROUTES["265.0"]` = `/plan-snapshots`. Job OPS 111.

## Leftover po 265.0

kółka / `circle_sim` / km ładowny (G2.20–G2.22). TT z actuals `stop`. what-if V8. 8 silników fizyki. F9 Optima fixture. T8. Instancja `task`. Konsument outboxa.
