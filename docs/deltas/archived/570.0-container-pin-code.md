# Plaster 570.0 · T3 leftover — HITL `container.pin_code`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [241.0](archived/241.0-container-booking-no.md) (numer bookingu obok, nie PIN) · [569.0](archived/569.0-container-tare-kg.md)

**Zależy od:** 153.0 `container` · 569.0 `tare_kg`

**Status:** Zamknięty `/noc` 2026-09-18. Nie live terminal. Nie ciphertext. Nie TEU. Nie kalkulator VGM. Nie countdown N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover T3 po tarze. Pogłębienie istniejącej `container`: opcjonalny `pin_code` (`String(64)`, tekst HITL PIN odbioru, trim, max 64 jak `booking_no`). POST bez pola → `null`. Pusty string po trim → `null`. Nie-tekst albo za długi → 400 (`pin`). Nie mutuje `tare_kg` / `vgm_kg` / `booking_no`. GET zwraca ten sam tekst — operator czyta PIN, nie skarbiec.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["570.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1. Migracja `450_container_pin` (po `449_container_tare`). `_WriteBox` dostaje `pin` → kolumna `pin_code` (wzorzec `book` → `booking_no`). `require_pin_code` w `app.domain.container`.
- **Odrzucone:** ciphertext / BYTEA / Fernet / KEK (`REJECTED` ten ID). Karta analizy „szyfrowane w spoczynku” cytuje HC-05, a HC-05 mówi o poświadczeniach armatora/API, nie o tokenie, który operator wpisuje i odczytuje. Szyfr bez klucza tenanta to teatr; skarbiec zostaje w parku.
- **Odrzucone:** live terminal / HTTP bramki (`REJECTED`). TEU (`REJECTED`). Kalkulator VGM (`REJECTED`). Countdown N3 / szkic `charge` (`REJECTED` → park). Import `charges` / `geography` / `booking_instruction`.

**Łowca:** BRAK `container.pin_code` (kontrakty starych migracji trzymają zakaz w tych plikach). ISTNIEJE `booking_no` (241.0, ten sam BC — reuse trim/max 64, osobny komunikat `pin`). ISTNIEJE `booking_instruction` (I4, status instrukcji, nie PIN). PODOBNE: opcjonalny string. Nie nowa tabela.

## Zakres

1. Migracja `450_container_pin`: kolumna nullable `String(64)`. Stare migracje zostają bez `pin_code`.
2. Domain `require_pin_code` + API POST opcjonalny + GET. AGENTS BC: zakaz „PIN” zwęża się do live terminal / ciphertext; kolumna HITL jest dozwolona (`agentlint --write` w commicie plastra).
3. UI `/shipments` panel kontenera (`pin_code` jako string). How-to dopisek. `SHIPPED["570.0"]`.

## Poza zakresem

Ciphertext · live terminal · TEU · kalkulator VGM · `payload_kg` · countdown N3 · szkic charge · AI3 · kwota · live HTTP

## Kryteria akceptacji

- [x] POST bez pola → `null`
- [x] POST `pin_code="GATE-18"` zapisuje tekst
- [x] pusty / nie-tekst / > 64 → 400 (`pin`)
- [x] izolacja RLS (bez zmiany polityki)
- [x] zero ciphertext / live HTTP / mutacja tary i VGM / import charges

## Leftover

`payload_kg` / `quantity` na kontenerze · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO (lokalne; CI po push) | kolumna HITL tekst PIN |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | ADD COLUMN String(64) |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | `_write_from_body` na suficie 40 linii |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT + `just docs` |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | PRZESZŁO | `factory_cycle --close` |
