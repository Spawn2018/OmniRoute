# Plaster 221.0 · T3 `remarks` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [220.0](../archived/220.0-container-voyage.md) leftover `remarks` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 220.0 `voyage_no`
**Status:** Zamknięty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `remarks`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover waga parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `remarks` (1–256 znaków po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`uwaga`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["221.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `160_container_remarks`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.notes_for_driver` — notatka punktu, nie kontener. ISTNIEJE statek/rejs/plomby. BRAK `remarks` na `container`. BRAK PIN. BRAK VGM.

**Odrzucone:**

- `pin_code` / ciphertext.
- VGM / TARA / `weight_kg`.
- `booking_no` / live HTTP.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `160_container_remarks` (po `159_container_voyage`): `remarks` nullable `String(256)`.
2. POST opcjonalny `remarks`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [x] POST z `remarks` zapisuje uwagę, bez PIN i bez wagi
- [x] POST bez pola zostaje `null`
- [x] zły token → 400 (`uwaga`)
- [x] RLS istniejącego `container`; izolacja
- [x] Zero float / marży / VGM / PIN / live HTTP / scrape

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | PRZESZŁO | POST opcjonalny `remarks`; 400 `uwaga`; blank → `null`; izolacja |
| Szybkość | N/A ten plaster | kolumny INSERT/GET, nie ścieżka z budżetu AGENTS |
| SQL | N/A | nullable `String(256)`; brak nowego SELECT resolve |
| N+1 | OK | GET jedna lista; POST = INSERT |
| Dług / człowiek | OK | kolumna na `container`, nie waga; leftover T3 PIN/VGM/`cargo_description`; C901 czysty |
| Proza operatora | 5–15 zdań | fold w `katalogi.md` |
| Docs/OS | PRZESZŁO | CURRENT, PLAN T3, GLOSSARY |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | PRZESZŁO | `factory_cycle.py --close` |
