# Plaster 472.0 · BR5.1 Finansowanie zamówienia (PO Financing)

**Status:** Zamknięte `/noc`. W kodzie.

**Spec źródłowa:** [VISION.md](../../VISION.md) § A.4 Strumień 3 Trade-Tech · § C.3 Grupa 5 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR5.1 · CT1 `purchase_order` · 471.0 `factoring_connector`

**Zależy od:** tenancy RLS · wzorzec HITL `factoring_connector` (471.0) · CT1 `purchase_order` / `po_line` / `asn` już w kodzie (bez kleju) · BR1.2 Inventory Release = leftover

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `po_financing_marks`. Tabela `po_financing_mark`: `mark_code` + `financing_kind` `po|release|advance|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance finansowania zamówienia / Inventory Release Trade-Tech jako katalog HITL, nie wycena zapasu i nie live HTTP partnera.
- **Wybrane:** POST/GET `/api/v1/po-financing-marks`. OpenFGA `can_manage_po_financing_marks` (member). UI `/po-financing-marks`. `SHIPPED["472.0"]` → `/po-financing-marks`.
- **Wybrane:** Alembic `377_po_financing_mark` (`down_revision = "376_factoring_connector"`). Id ≤ 32 znaków.
- **Odrzucone:** FK do `purchase_order` / `po_line` / `asn` (`REJECTED` — CT1 zostaje nagłówek/linia/awizo; klej po BR1.2 leftover).
- **Odrzucone:** rozszerzenie `customer_po_mark` (`REJECTED` — EXP1 to referencja PO klienta na zleceniu, nie Trade-Tech PO Financing).
- **Odrzucone:** rozszerzenie `inventory_position_mark` / `working_capital_mark` (`REJECTED` — EXP3.3/EXP2.1 to WMS pozycja / DSO, nie stance finansowania PO).
- **Odrzucone:** live SMEO / bank HTTP · wycena zapasu SQL · wiekowanie · kwota / marża / float · LLM limitu.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `purchase_order` + `po_line` + `asn` (CT1). ISTNIEJE `customer_po_mark` (referencja klienta). ISTNIEJE `po_plant_mark` / `po_sku_mark` / `po_batch_mark` (EXP3.0). ISTNIEJE `inventory_position_mark` (EXP3.3). ISTNIEJE `factoring_connector` (471.0 partner). ISTNIEJE `fair_share_mark` / `make_or_buy_mark` (EXP2). PODOBNE katalog HITL stance. BRAK `po_financing_mark`. BRAK katalogu PO Financing / Inventory Release.

## Zakres

How-to: jedno zdanie — to katalog stance finansowania zamówienia Trade-Tech; wycena zapasu i live partner zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (radio fieldset jak 469, nie select 471 ani toggle 470) pod jscpd < 3%.

## Poza zakresem

FK do `purchase_order` · wycena zapasu BR1.2 · live SMEO HTTP · kwota · BR5.2 giełdy · WMS live

## Kryteria akceptacji

- [x] migracja `varchar(32)` + FORCE RLS + test izolacji
- [x] POST zły kind/kod → 400; amount → 422
- [x] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

FK do `purchase_order` · wycena / wiekowanie zapasu BR1.2 · live partner HTTP · workflow wypłaty · qty float · BR5.2 giełdy
