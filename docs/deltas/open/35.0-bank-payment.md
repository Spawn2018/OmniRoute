# Plaster 35.0 · M-42 Bank i płatności (`bank_payment`)

**Spec źródłowa:** [docs/spec/bank-payment.md](../../spec/bank-payment.md)  
**Zależy od:** 5.0 `party_bank_account` · 5.0 `iban-lookup` · 1.2 `charge.sell` · 33.0 `/invoices`

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 6 M-42):** bank **nie ma tabeli płatności**. Operator wybiera kontrahenta, widzi IBAN z `party_bank_account`, szkic białej listy (`iban-lookup`) i `sell` z `charge` — jedyne znane kwoty do zapłaty. Token `bank_payment` (UI, nie tabela). Żywy ID **M-42**. 5.0 zostaje zapisem rachunku; 33.0 zostaje tablicą faktur.

**Łowca:** BRAK `payment` / `bank_transfer` / SEPA / wyciągu. ISTNIEJE: `fetchBankAccounts`, `fetchParties`, `fetchCharges`, `POST /parties/iban-lookup`, `<Money/>`. PODOBNE: 25.0 `/mail` (wybór party, bez N+1); 33.0 sell z charge.

**Odrzucone:**

- Tabela płatności / wyciągu / SEPA XML — zgadywanie schematu (CURRENT).
- Pętla fetch rachunków po całym katalogu (N+1) — jeden wybrany kontrahent.
- Odejmowanie `sell − zapłacone` w JS — nie drugi `margin()`.
- Zapis IBAN z tego ekranu — mutacja zostaje na `/parties`.
- Live bank / PSD2.

## Zakres

Read-only `/payments`. `data-bank-payment="board"`. Brak migracji. Wybór `party` + `fetchBankAccounts` + `iban-lookup` + `fetchCharges` / `<Money/>` na `sell_amount`. Linki do `/parties` i `/invoices`.

## Poza zakresem

Tabela płatności · SEPA · wyciąg · PSD2 · N+1 · ExtractionService

## Ustalenia

- Token: `bank_payment` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_parties` i `can_manage_charges`.
- RLS: bez nowej tabeli.
- Job: `/payments`. `SHIPPED_CHARGE_ROUTES["35.0"]` = `/payments`. OPS_JOBS +1 → 32.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 35.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/payments` ma `data-bank-payment="board"` i woła `fetchBankAccounts`
- [ ] Kwoty `sell` przez `<Money/>`; brak odejmowania / `parseFloat`
- [ ] Brak pętli fetch po wszystkich `party`; brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["35.0"]` = `/payments`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

Wypełnić przy `/zamknij`.

## Knowledge

Nie RAG. Nie silnik płatności.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela płatności, SEPA i wyciąg świadomie leftover.
