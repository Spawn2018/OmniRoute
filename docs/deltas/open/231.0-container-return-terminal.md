# Plaster 231.0 · T3 `return_terminal` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [230.0](../archived/230.0-container-pickup-terminal.md) leftover `return_terminal` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 230.0 `pickup_terminal`
**Status:** Otwarty `/noc` 2026-09-10. Nie FK. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `return_terminal`/party/PIN/VGM/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `return_terminal` (ta sama reguła co `pickup_terminal`: 1–32 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`terminal`). `require_return_terminal` deleguje do `require_pickup_terminal`. HITL tekst, nie FK. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["231.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `170_container_return_terminal` (po `169_container_pickup_terminal`). `_WriteBox` — dodać `yard`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `pickup_terminal`. BRAK `return_terminal`. BRAK FK. PODOBNE: `require_pickup_terminal`. Nie duplikować geography `terminal`.

**Odrzucone:**

- FK `return_terminal_id` / `carrier_party_id` / live HTTP.
- PIN / VGM / `booking_no`.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `170_container_return_terminal` (po `169_container_pickup_terminal`): `return_terminal` nullable `String(32)`.
2. POST opcjonalny `return_terminal`. Zły token → 400. Serwis nie importuje geography/parties/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

FK · party · PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `return_terminal` zapisuje token terminalu zwrotu, bez FK i bez PIN
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`terminal`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape
