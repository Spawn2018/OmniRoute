# Plaster 628.0 · T7 daty bazowe HITL na zleceniu

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T7 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T7 · [618.0](../archived/618.0-charge-fx-sql.md) · [211.0](../archived/211.0-fx-rate-basis.md)
**Zależy od:** 90.0 `shipment` · 211.0 tokeny `fx_rate_basis` · 618.0 `charge_sell_in_pln` (osobno)
**Status:** Delta zaakceptowana `/noc 7` 2026-09-22. Wolno `/plaster`. Nie fx×FV SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T7 „daty bazowe” + park fx×FV):** polityka `fx_rate_basis` już wskazuje `etd` / `loading_date` / `unloading_date` / `invoice_date`, ale na `shipment` tych kolumn nie ma (potwierdzone w 618.0). Mnożenie przy FV i złożenie z `fx_rate_day` zostają parked. Żywy plaster: **cztery opcjonalne DATE** na istniejącej `shipment` — `etd`, `loading_date`, `unloading_date`, `invoice_date`. Operator wpisuje daty kotwic kursu; serwis nie czyta `nbp_rate`, nie woła `fx_rate_day`, nie mnoży i nie zapisuje FV. Puste / brak = `null`. Zły kształt daty → 400 (`data`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["628.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `500_shipment_fx_anchor_dates` po `499_document_template_branding`. Marża zostaje na `charge`. Zero float.

**REQUIREMENT:** karta T7 — `fx_rate_basis` wybiera datę zlecenia; bez kolumn basis jest martwy. Źródło: `docs/analysis/karty-pol-fala-t.md` § T7; leftover 618.0.

**Łowca:** ISTNIEJE `shipment` bez dat T7. ISTNIEJE allowlista / tokeny `fx_rate_basis`. ISTNIEJE `charge_sell_in_pln(on_date)`. BRAK kolumn kotwic na zleceniu. BRAK SQL fx×FV składającego basis→offset→kurs→kwota.

**Odrzucone:**

- fx×FV: złożenie basis + `fx_rate_day` + `charge_sell_in_pln` w jednym wywołaniu
- zapis kwoty PLN na `sales_invoice` / druga marża
- override dat na `charge` (tokeny `fx_rate_*` już są w 615.0)
- live HTTP NBP · matching T5 · FK kontekstu · T8 live · D9c/d/e/f park
- float / mnożenie w Pythonie

## Zakres

1. Migracja: cztery nullable `Date` na `shipment`. Bez nowej tabeli.
2. POST opcjonalne daty; GET zwraca. Zły kształt → 400 (`data`). Serwis `shipments` nie importuje `nbp_rates` / `organization_calendars` / `charges`.
3. Formularz `/shipments` + zdanie w how-to zlecenia.

## Poza zakresem

fx×FV SQL · zapis przy FV · live NBP · matching T5 · FK kontekstu · T8 · float

## Plan plików

- `backend/alembic/versions/500_shipment_fx_anchor_dates.py`
- `backend/app/models/shipment.py`
- `backend/app/domain/shipment.py` (walidacja dat opcjonalnych)
- serwis / repo / API `shipments`
- testy domeny, HTTP, izolacja RLS bez nowej tabeli
- `frontend/src/features/shipment/catalog-page.tsx`
- `frontend/src/features/ops/ops-index.ts` (`"628.0"`)
- `docs/operator/` how-to zlecenia (albo fold w istniejącym)
- GLOSSARY · CURRENT · PLAN · VISION · PROGRESS

## Kryteria akceptacji

- [ ] POST bez dat zostawia cztery pola `null`; z poprawnymi datami zapisuje
- [ ] zły kształt → 400 (`data`); serwis nie importuje `nbp_rates` / kalendarza / `charges`
- [ ] GET zwraca daty tenanta; inny tenant nie widzi
- [ ] zero float / mnożenia / fx×FV / live HTTP / nowej tabeli / nowej relacji OpenFGA

## Leftover

fx×FV (basis → `fx_rate_day` → `charge_sell_in_pln`) · D9c 409 · D9d/e/f · matching T5 · FK kontekstu · T8 live
