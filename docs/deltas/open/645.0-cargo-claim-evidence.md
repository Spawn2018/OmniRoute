# Plaster 645.0 · leftover EXP0.8/EXP1 cargo_claim evidence HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § EXP0.8 · [191.0](../archived/191.0-cargo-claim-cmr.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP0.8 / EXP1 `cargo_claim`
**Zależy od:** 191.0 `cargo_claim` z OS&D + terminami CMR
**Status:** Delta zaakceptowana `/noc 7` 2026-09-24. Nie Deadline Engine. Nie live GPS. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 evidence):** tabela `cargo_claim` zostaje prawdą o reklamacji na zleceniu (OS&D + CMR). Deadline Engine / `timedelta` 7/21/365 / live GPS / bajty zdjęć / `liable_party_id` S11 zostają park. Żywy plaster: **pogłębienie istniejącej** `cargo_claim` — trzy niezależne bool HITL: `evidence_gps`, `evidence_temp`, `evidence_photo` (`true`|`false` każde). Operator zaznacza, jakie dowody ma przy reklamacji; serwis nie pobiera GPS, nie czyta temperatury z V5, nie przechowuje bajtów zdjęcia, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. Istniejące wiersze: backfill `false` × 3. GET listy SQL. UI zostaje `/claims` (trzy checkboxy w tym samym formularzu). `SHIPPED_CHARGE_ROUTES["645.0"]` = `/claims`. OpenFGA bez nowej relacji (`can_manage_cargo_claims`). Migracja `517_cargo_claim_evidence` po `516_dg_limited_quantity`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**Dlaczego trzy bool, nie jeden stance:** GPS / temperatura / zdjęcie nie wykluczają się — operator może mieć GPS+foto. Exclusive stance `gps|temp|photo|other` gubi kombinacje i wymusza sztuczne `other`.

**REQUIREMENT:** leftover 191.0 / PLAN EXP0.8 evidence HITL. Źródło: `docs/deltas/archived/191.0-cargo-claim-cmr.md` (odrzut evidence); PLAN wiersz EXP0.8; CURRENT Następny **645.0**.

**Łowca:** ISTNIEJE `cargo_claim` — claim_kind + damage_code + CMR dates + source_ref, **nie** evidence. ISTNIEJE `tracking_event` / `position_event` — śledzenie / pozycja, **nie** znacznik dowodu na reklamacji. ISTNIEJE `shipment_document` — wskazanie dokumentu, **nie** evidence bool. BRAK `evidence_gps` / `evidence_temp` / `evidence_photo` na `cargo_claim`. BRAK Deadline Engine. BRAK `liable_party_id`.

**Odrzucone:**

- jeden exclusive stance `gps|temp|photo|other` (gubi kombinacje)
- Deadline Engine / `timedelta` 7/21/365 od daty dostawy
- live GPS / poll / lat/lng / V5 telemetry
- bajty zdjęcia / upload / storage
- `liable_party_id` (szkic S11)
- nowa tabela mark
- kwota / druga marża / float / T-SQL / live HTTP

## Zakres

1. Migracja `517_cargo_claim_evidence`: trzy kolumny BOOLEAN NOT NULL po backfillu (`false`).
2. POST wymaga `evidence_gps`, `evidence_temp`, `evidence_photo`. Serwis nie importuje shipments / charges / quotations / extraction i nie woła HTTP. Przy suficie 40 linii — NamedTuple draft jak 644.0.
3. UI `/claims` (trzy checkboxy). How-to `docs/operator/reklamacja.md` (uzupełnić); fold w `katalogi.md`. `just api-types`. Fixture `CargoClaim(` dostają trzy `False`.

## Poza zakresem

Deadline Engine · live GPS · bajty foto · liable_party · kwota · float · T-SQL · live HTTP · nowa tabela mark · exclusive stance

## Plan plików

- `backend/alembic/versions/517_cargo_claim_evidence.py`
- model / domain / serwis / API / testy cargo_claims
- `frontend/src/features/cargo-claim/` + API client
- `ops-index` + `routes-breadth` (`645.0` → `/claims`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z trzema bool zapisuje wiersz; brak live GPS / bajtów
- [ ] POST z dowolną kombinacją true/false (w tym wszystkie false) zapisuje wiersz
- [ ] serwis nie importuje charges / quotations / extraction i nie woła HTTP
- [ ] RLS: tenant B nie widzi reklamacji tenanta A; zero float / marży / Deadline Engine

## Leftover

Deadline Engine · liable_party S11 · live GPS / foto storage
