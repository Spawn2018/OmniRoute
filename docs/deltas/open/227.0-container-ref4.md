# Plaster 227.0 · T3 `ref_4` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [226.0](../archived/226.0-container-ref3.md) leftover `ref_4` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 226.0 `ref_3`
**Status:** Otwarty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `ref_4`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `ref_5` parked. leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `ref_4` (ta sama reguła co `ref_1`: 1–64 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`referencja`). `require_container_ref_4` deleguje do `require_container_ref_1`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["227.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `166_container_ref4`. `record_container` jest na 39 liniach — spakować pola zapisu do `_WriteBox`, żeby dodać `ref_4` bez przekroczenia 40. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `ref_1` / `ref_2` / `ref_3`. BRAK `ref_4`. BRAK PIN. BRAK VGM. BRAK `booking_no`.

**Odrzucone:**

- `ref_5` w tym plasterze.
- `booking_no` / live HTTP.
- `quantity` / `weight_kg` / VGM / TARA.
- `pin_code` / ciphertext.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `166_container_ref4` (po `165_container_ref3`): `ref_4` nullable `String(64)`.
2. POST opcjonalny `ref_4`. Zły token → 400. Serwis nie importuje charges.
3. `_WriteBox` na polach zapisu — `record_container` zostaje ≤ 40.
4. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · `ref_5` · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `ref_4` zapisuje czwartą referencję, bez PIN i bez wagi
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`referencja`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape
