# Plaster 179.0 · G2.9b `draft_kind=tender_rfp` accept

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.9 leftover kind/accept · [178.0](../archived/178.0-tender-rfp-intake.md)
**Zależy od:** 178.0 `tender_rfp_intake` · 0.8 / 082 `extraction_draft.draft_kind` · 1.3 `AcceptExtractionToRates`
**Status:** Delta zaakceptowana `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.9b):** `extraction_draft.draft_kind` dopuszcza **`tender_rfp`**. Operator składa szkic na `/ai` (payload `tender_id` + `intake_code`). **Akceptacja HITL** w `AcceptExtractionToRates` (warstwa API) zapisuje `tender_rfp_intake`; `source_ref` zapisu = `fixture://tender-rfp-intake/{draft.id}` (ślad akceptu, nie treść z LLM). `ExtractionService` nadal nie importuje tenders / intakes / rates / quotations. Zły kind → 400 (`allowlist`). Zły payload → 400 (`przyjęcie`). Nie nowa tabela. Nie nowa trasa katalogu. `SHIPPED_CHARGE_ROUTES["179.0"]` = `/ai`. OpenFGA: istniejące `can_manage_extractions`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `draft_kind` (`rate_line` | `carrier_quote`) + CHECK `ck_extraction_draft_kind`. ISTNIEJE `AcceptExtractionToRates` — zapis `rate_line` / `channel_quote` po akceptcie, nie intake. ISTNIEJE `tender_rfp_intake` (178.0) — ręczny POST, nie gałąź accept. ISTNIEJE `/ai`. BRAK `tender_rfp` w allowliście. BRAK zapisu intake z accept.

**Odrzucone:**

- Prospecting HITL / bid/no-bid / award four-eyes / TED / CO₂ (leftover G2.10–G2.18).
- LLM extractor produkujący `tender_rfp` albo INSERT intake bez akceptu.
- Auto-award / scrape TED / kwota na szkicu / druga marża / UPDATE `tender.status`.
- Import `tender_rfp_intakes` w `ExtractionService`.
- Nowa tabela / nowy ekran katalogu / float / T-SQL / live HTTP.

## Zakres

1. Migracja `119_extraction_draft_tender_rfp`: CHECK `draft_kind IN ('rate_line','carrier_quote','tender_rfp')`. Model + domena allowlista.
2. Payload HITL `{tender_id, intake_code}`. Accept (API) woła `TenderRfpIntakeService.persist_intake`. GET intake bez zmian 178.0.
3. UI `/ai`: `draft_kind=tender_rfp`. `just api-types`. How-to fold w `przyjecie-rfp.md` + bullet `/ai` w `katalogi.md` (40 linii, bez nowej linii).

## Poza zakresem

G2.10–G2.18 prospecting/four-eyes/TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · zapis z LLM · kwota na wierszu · T-SQL · live HTTP · nowa tabela intake

## Kryteria akceptacji

- [ ] POST szkicu `draft_kind=tender_rfp` przechodzi; `purchase_invoice` nadal 400
- [ ] Accept HITL zapisuje `tender_rfp_intake` z `fixture://tender-rfp-intake/{draft.id}`
- [ ] `ExtractionService` nie importuje tenders / intakes / rates
- [ ] Zero float / marży / LLM write / live HTTP / auto-award
