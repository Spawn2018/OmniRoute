# Plaster 524.0 · AI5.1 — `model_feature_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI5.1 · po [523.0](../archived/523.0-ingest-gate-mark.md)

**Zależy od:** AI5.0 `data_source` + `ingest_gate_mark` · cechy jako etykiety, nie live train

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `model_feature_marks`. Tabela `model_feature_mark`: `mark_code` + `feature_kind` `numeric|categorical|derived|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja AI5.1 — **etykieta cechy modelu jako dana HITL**, nie live train / feature store.
- **Wybrane:** POST/GET `/api/v1/model-feature-marks`. OpenFGA `can_manage_model_feature_marks`. UI `/model-feature-marks`. `SHIPPED["524.0"]`.
- **Wybrane:** how-to `docs/operator/model-feature-mark.md`.
- **Odrzucone:** live train · feature store SQL · embedding / pgvector · auto-champion (`REJECTED`).
- **Odrzucone:** float / amount / score cech / FK do `data_source` w tym plasterze (`REJECTED`).

**Łowca:** ISTNIEJE `data_source` · `ingest_gate_mark` · `suggestion_kind`. BRAK `model_feature_mark`.

## Zakres

HITL `model_feature_mark` — migracja 414 + warstwy + OpenFGA + FE + how-to + SHIPPED 524.0.

## Poza zakresem

live train · AI6 SQL grafu · KPI wzór z modelu · float

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] UI `/model-feature-marks`; `SHIPPED["524.0"]`
- [ ] craft_style; how-to

## Leftover

live train · wiring cech do modelu · AI6 leftover SQL
