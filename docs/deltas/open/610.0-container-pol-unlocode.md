# Plaster 610.0 · T3 `pol_unlocode` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T3 · [609.0](../archived/609.0-container-alliance-service.md)
**Zależy od:** 153.0 `container`; 609.0 `alliance_service`
**Status:** Delta zaakceptowana `/noc` 2026-09-21. Wolno `/plaster`. Nie FK portu. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z karty T3 `pol_port_id` / `pod_port_id` / `destination_city` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** EXP1 kontenera (`grade`, `vessel_imo`, `alliance_service`, rejs) jest w kolumnach. PIN jest w 570.0. HBL/MBL odrzucone — `ocean_bill.bill_no` i HAWB/MAWB na nodze. FK `port` odrzucone w tym plasterze — serwis kontenera nie importuje geography; bind jak `carrier_party_id` zostaje na później. leftover `pod_unlocode` parked. leftover `destination_city` parked. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `pol_unlocode` (`String(5)` HITL). POST bez pola zostaje `null`. Pusty trim → `null`. Nie-tekst, długość inna niż 5 albo znak spoza UN/LOCODE → 400 (`pol`). Wielkość liter składana do wielkich. `require_pol_unlocode` w `app.domain.container` — wzór `require_alliance_service`, **nie** resolve słownika portów, **nie** Haversine, **nie** cyfra kontrolna. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["610.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `485_container_pol_unlocode` (po `484_container_alliance_service`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.alliance_service` (609.0). ISTNIEJE `container.vessel_imo` (608.0). ISTNIEJE `container.pickup_terminal` (tekst, nie port). ISTNIEJE `shipment_leg.origin_location_id` (lokalizacja nogi, nie kod POL na kontenerze). ISTNIEJE `ocean_bill.bill_no`. BRAK `container.pol_unlocode`. BRAK `container.pol_port_id`.

**Odrzucone:**

- `pod_unlocode` / `destination_city` / FK `port` / resolve geography / HBL / HW / UXCL / N3 / AI3 / G0-SH / mapa / float / marża / Auth0.

## Zakres

1. Migracja `485_container_pol_unlocode`: `pol_unlocode` nullable String(5). Nie `create_table`.
2. POST opcjonalny `pol_unlocode`. Zła wartość → 400 (`pol`). Serwis nie importuje charges ani geography.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

pod_unlocode · destination_city · FK portu · mapa · HW · UXCL · N3 · Auth0 · live HTTP · float

## Kryteria akceptacji

- [ ] POST z `pol_unlocode` zapisuje pięć znaków UN/LOCODE, bez resolve portu
- [ ] POST bez pola / pusty trim zostaje `null`
- [ ] małe litery → wielkie
- [ ] nie-tekst / zła długość / zły znak → 400 (`pol`)
- [ ] RLS istniejącego `container`; izolacja bez nowej tabeli
- [ ] Zero float / marży / live HTTP / import charges / import geography

## Leftover

`pod_unlocode` · `destination_city` · HW · UXCL · N3 · AI3 · G0-SH
