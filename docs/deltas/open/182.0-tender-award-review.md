# Plaster 182.0 · G2.12 `tender_award_review`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.12 · leftover award four-eyes
**Zależy od:** 169.0 `tender` (nagłówek już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.12–G2.18):** jedna tabela `tender_award_review` per tenant — **HITL ślad czterech oczu na nagrodę z `source_ref`**, nie silnik auto-award i nie szyna A/Z/O. Wiersz = `tender_id` (FK tenanta do `tender`) + `review_code` (`countersign` | `challenge`) + `source_ref` (`tenant:manual` albo `fixture://tender-award-review/…`). Operator wpisuje, że druga para oczu podpisała albo zakwestionowała nagrodę; serwis nie woła LLM, nie mutuje `tender.status`, nie zapisuje `tender_win_loss` i nie czyta `charge`. Zły kod → 400 (`przegląd`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id)` — jeden przegląd na przetarg. GET listy SQL. API składa odczyt nagłówka (`TenderService.get_board`). UI `/tender-award-reviews` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["182.0"]` = `/tender-award-reviews`. OpenFGA: `can_manage_tender_award_reviews`. Migracja `122_tender_award_review`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `operator_decision` — szyna Akceptuj/Zmień/Odrzuć z `lock_version`, **nie** katalog G2 czterech oczu. ISTNIEJE `tender_win_loss` — wynik po fakcie, nie drugi podpis. ISTNIEJE `tender.status` (`awarded`) — nagłówek; **nie** mutujemy. BRAK `tender_award_review`. BRAK `/tender-award-reviews`.

**Odrzucone:**

- TED capability / CO₂ (leftover G2.13–G2.18).
- Auto-award / scrape / UPDATE `tender.status` / INSERT `tender_win_loss` / import `operator_decisions`.
- Wymuszenie dwóch różnych `user_id` w tym plasterze (leftover).
- Kwota / druga marża / scoring.
- Float / T-SQL / live HTTP.

## Zakres

1. Migracja `122_tender_award_review`: RLS, izolacja, indeks `(organization_id, review_code)`, unique `(organization_id, tender_id)`, CHECK `review_code`.
2. POST/GET. Zły `review_code` / `source_ref` → 400. API składa odczyt `tender`, serwis nie importuje tenders / win_losses / operator_decisions / charges / extraction.
3. UI `/tender-award-reviews`. `just api-types`. How-to `docs/operator/cztery-oczy-przetargu.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `AwardReviewDesk` / `awardReviewWrite` / `persistAwardReviewMark`.

## Poza zakresem

G2.13–G2.18 TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · scrape · LLM · kwota na wierszu · mutacja `tender.status` · dwa user_id · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `tender_award_review` z kodem przeglądu i `source_ref` na istniejącym `tender`, bez auto-award
- [ ] zły `review_code` → 400
- [ ] GET zwraca przeglądy tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / LLM / live HTTP / scrape / UPDATE `tender.status`
