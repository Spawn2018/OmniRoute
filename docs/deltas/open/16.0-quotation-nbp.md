# Plaster 16.0 · M-23 Waluty w ofercie (`nbp_rate` przy `quotation`)

**Spec źródłowa:** [docs/spec/quotation-nbp.md](../../spec/quotation-nbp.md)  
**Zależy od:** 6.0 `nbp_rate` · 2.0/5.1 `quotation` · 0.6 DataTableShell

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie drugi katalog kursów.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-23):** wycena **czyta** katalog 6.0. Panel na `/quotations` woła `GET /nbp-rates/resolve`. Żywy ID katalogu zostaje **M-23** `nbp_rate` (6.0). 16.0 nie nadaje nowego M-xx.

**Łowca:** BRAK drugiego katalogu FX. ISTNIEJE: `nbp_rate.resolve(currency, on_date)` + `quotation.currency` (CHAR(3) ze stawki). PODOBNE: panel recenzji na `/parties` (odczyt/zapis innego BC z UI). Tu tylko odczyt.

**Odrzucone:**

- Druga tabela `fx_rate` / `quotation_fx` — PLAN: czyta `nbp_rate`, nie drugi katalog.
- Import `app.services.nbp_rates` z `quotations` — independence import-lintera.
- Mnożenie kwoty w Pythonie/JS — leftover silnika walut; 16.0 pokazuje `mid`, nie nową kwotę PLN.
- FK `nbp_rate_id` na `quotation` — zgadywanie schematu; snapshot w 16.0 zbędny przy resolve as-of.
- Live HTTP NBP, ECB, tabela C, marża w `charge`.

## Zakres

Panel na istniejącym `/quotations`. `resolveNbpRate`. PLN bez requestu. Brak migracji.

## Poza zakresem

Przeliczenie oferty do PLN · zapis kursu na wierszu wyceny · nowy router backendu · nowy BC · ExtractionService→nbp · LLM

## Ustalenia

- Token katalogu: `nbp_rate` (GLOSSARY). 16.0 = użycie przy ofercie, nie nowy byt.
- OpenFGA: istniejące `can_manage_nbp_rates` na resolve; 403 zostaje błędem API.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["16.0"]` = `/quotations`.
- `just dup` ≤ 3% — własny fieldset, nie kopia katalogu `/nbp-rates`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/quotations` woła `resolveNbpRate`; pokazuje `mid` i `source_ref`
- [ ] Brak nowej migracji
- [ ] Brak `amount * mid` / `parseFloat` na kwocie
- [ ] `quotations` service nie importuje `nbp_rates`
- [ ] PLN bez resolve (katalog 6.0 odrzuca PLN)
- [ ] `just dup` ≤ 3%

## Knowledge

Karta NBP = katalog 6.0. Nie RAG na mnożenie.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Przeliczenie kwoty świadomie leftover.
