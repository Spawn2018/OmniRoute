# Plaster 571.0 · T3 leftover — HITL `container.payload_kg`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [569.0](../archived/569.0-container-tare-kg.md) (`tare_kg` obok, nie to samo) · [239.0](../archived/239.0-container-vgm.md) (VGM obok, nie kalkulator)

**Zależy od:** 153.0 `container` · 569.0 `tare_kg` · 239.0 `vgm_kg`

**Status:** Zaakceptowana `/noc` 2026-09-18. Nie quantity. Nie TEU. Nie kalkulator VGM. Nie countdown N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover T3 po PIN. Pogłębienie istniejącej `container`: opcjonalny `payload_kg` (`Numeric(14,4)`, ten sam typ co `tare_kg` / `vgm_kg` w `backend/app/models/container.py`). Decimal HITL ładowności, > 0, nie float, nie waluta, nie `charge`. POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`ładowność`). Nie mutuje `tare_kg` / `vgm_kg` / `pin_code`. Nie liczy `vgm − tara` w Pythonie ani JS.
- **Wybrane:** `require_payload_kg` woła istniejące `_require_positive_kg` z etykietą `ładowność` (żeńska, pasuje do „dodatnia” bez zmiany wspólnego przymiotnika). UI zostaje `/shipments`. `SHIPPED["571.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1. Migracja `451_container_payload` (po `450_container_pin`). `_write_from_body` jest na suficie 40 linii — nowe pole w istniejącej linii argumentów, nie w nowej.
- **Odrzucone:** kalkulator VGM = tara + ładowność (`REJECTED`). float / `parseFloat` (`REJECTED`). `quantity` (`REJECTED`). TEU (`REJECTED`). countdown N3 / szkic charge (`REJECTED` → park). ciphertext (`REJECTED`). AI3-payload / G0-SH (`REJECTED` → park). Import `charges` / `money` / `geography`.

**Łowca:** BRAK `payload_kg` w `backend/`. ISTNIEJE `tare_kg` (569.0) i `vgm_kg` (239.0, `Numeric(14,4)`). ISTNIEJE `_require_positive_kg`. PODOBNE: `require_tare_kg`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `451_container_payload`: kolumna nullable `Numeric(14,4)`. Stare migracje zostają bez `payload_kg`.
2. Domain `require_payload_kg` + API POST opcjonalny + GET.
3. UI `/shipments` panel kontenera (`payload_kg` jako string). How-to dopisek. `SHIPPED["571.0"]`.

## Poza zakresem

quantity · TEU · kalkulator VGM · countdown N3 · szkic charge · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `payload_kg="28000.5"` zapisuje Decimal
- [ ] float / ≤0 / bool / nie-liczba → 400 (`ładowność`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero float / live HTTP / mutacja tary, VGM i PIN / import charges

## Leftover

TEU · `quantity` na kontenerze · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH
