# Plaster 455.0 · BR3.0 znacznik planu trasy

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E Fala BR · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR3.0 · Q9 start HHL

**Zależy od:** tenancy RLS · wzorzec HITL mark (G6 `load_plan_mark`)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `route_plan_marks`. Tabela `route_plan_mark`: `mark_code` + `plan_kind` `route|stop|window|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/route-plan-marks`. OpenFGA `can_manage_route_plan_marks` (member). UI `/route-plan-marks`. `SHIPPED["455.0"]` → `/route-plan-marks`.
- **Wybrane:** Alembic `360_route_plan_mk` (`down_revision = "359_circle_pair"`). Id ≤ 32 znaków.
- **Odrzucone:** live Valhalla / ORS / VROOM / HTTP mapy (`REJECTED` — leftover solvera).
- **Odrzucone:** LLM-VRP / km / float / kwota / marża / osie Decimal.
- **Odrzucone:** klej do `load_plan_mark` / `routing_guide` / `circle_sim` / `trip` (`REJECTED` — inny BC; G6 = załadunek, CT4 = przewodnik, G2.20 = kółko).
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind · nowy silnik.

**Łowca:** ISTNIEJE `load_plan_mark` (G6 osie/tunel — załadunek). ISTNIEJE `routing_guide` + enforcement/match (CT4 przewodnik, nie solver). ISTNIEJE `circle_sim` / `circle_sim_pair` (kółko, nie plan trasy). PODOBNE katalog HITL mark. BRAK `route_plan_mark`. BRAK Valhalla.

## Zakres

How-to: jedno zdanie — to katalog rodzaju planu trasy; solver zostaje leftover. Bez +1 OPS_JOB poza nową listą.

## Poza zakresem

Valhalla · VROOM · mapa · km · `load_plan_mark` · `routing_guide` · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount/km → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

Valhalla live · VRP · km · tacho jako constraint (BR3.3) · BR3.1 załadunek

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
