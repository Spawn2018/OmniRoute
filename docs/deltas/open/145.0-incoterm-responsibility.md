# Plaster 145.0 · I1 `incoterm_responsibility`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § I1 · [karty-pol-fala-i.md](../../analysis/karty-pol-fala-i.md) § I1
**Zależy od:** 137.0 `quotation.incoterm` / `trade_side`
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Nie I2. Nie I3. Nie cytat ICC.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z I1):** jedna tabela katalogu per tenant. Para `(incoterm, trade_side)` → role odprawy i kto bookuje główny przewóz. Seed 11×2 to **dane Omni** (`source_ref=omni:incoterms2020:ops`), nie oficjalny tekst ICC. Override tenanta = nowy wiersz + `superseded_by` (jak stawka). GET filtr SQL po parze. `booking_scope` to tablica tokenów (`precarriage` | `ocean` | `oncarriage` | `contact_exchange` | `none`), nie silnik bookingu.

UI `/quotations`. `SHIPPED_CHARGE_ROUTES["145.0"]` = `/quotations`. OpenFGA: `can_manage_quotations`. Migracja `085_incoterm_responsibility`. Seed: POST `/incoterm-responsibilities/seed` dopisuje brakujące pary; istniejące nie nadpisuje.

**Łowca:** ISTNIEJE `quotation.incoterm` / `trade_side` (137.0). ISTNIEJE GLOSSARY `incoterm_responsibility`. ISTNIEJE `ARRAY` ról na `party`. BRAK tabeli macierzy. BRAK seedu Omni. BRAK cytatu ICC w kodzie.

**Odrzucone:**

- Cytat / skan tabeli ICC w UI albo w seedzie.
- I2 `shipment_stakeholder`, I3 dispatch, I4 booking_instruction.
- LLM wybierający Incoterms albo adresata.
- Import serwisu quotations w tym BC.
- Python liczący obowiązki z Incoterms w locie — to wiersz w bazie.

## Zakres

1. Migracja `085_incoterm_responsibility`: RLS, izolacja, unique bieżącej pary.
2. POST/GET + seed Omni 11×2. Override = nowy wiersz.
3. UI `/quotations`. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

I2–I4 · U4 · C8 · booking HTTP · NBP

## Kryteria akceptacji

- [ ] POST zapisuje wiersz; obcy incoterm / rola → 400
- [ ] Seed `omni:incoterms2020:ops` daje 22 bieżące pary; DDP × import ma `import_clearance_role=seller`
- [ ] Override tej samej pary = nowy wiersz, stary `superseded_by`
- [ ] GET po parze zwraca bieżący wiersz
- [ ] Serwis bez importu quotations / charges
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / cytatu ICC
