# Plaster 18.0 · M-25 Negocjacja i wynik (`offer_negotiation` przy `quotation`)

**Spec źródłowa:** [docs/spec/offer-negotiation.md](../../spec/offer-negotiation.md)  
**Zależy od:** 5.1 `quotation` lane · 13.0 `channel_quote` · 0.6 DataTableShell

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-25):** wycena **czyta** ofertę kanału na tej samej lane (party + POL + POD). Token `offer_negotiation` (UI, nie tabela). Żywy ID **M-25**.

**Łowca:** BRAK `offer_outcome` / `quotation_outcome` / won-lost w kodzie i GLOSSARY. ISTNIEJE: `quotation` z lane (5.1), `channel_quote.resolve` (13.0). PODOBNE: 16.0/17.0 panele na `/quotations` — compose API, zero nowej tabeli.

**Odrzucone:**

- Tabela wyniku won/lost — zgadywanie schematu (CURRENT). Leftover.
- Odejmowanie `quotation.amount − channel_quote.amount` w JS — PLAN: LLM nie liczy; nie drugi `margin()`.
- Zapis do `rate_line` / `charge` / mutacja kwoty wyceny.
- Import `app.services.channel_quotes` z `quotations` — independence import-lintera.
- Live HTTP armatora (leftover 13.0).

## Zakres

Panel na istniejącym `/quotations`. `resolveChannelQuote`. Brak migracji.

## Poza zakresem

Katalog wyniku · silnik negocjacji · IMAP · ExtractionService · LLM

## Ustalenia

- Token: `offer_negotiation` (GLOSSARY, UI). 18.0 = odczyt przy ofercie, nie nowy byt w bazie.
- OpenFGA: istniejące `can_manage_rate_lines` na resolve kanału; 403 zostaje.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["18.0"]` = `/quotations`.
- `just dup` ≤ 3% — własny panel, nie kopia `/channel-quotes`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/quotations` woła `resolveChannelQuote`
- [ ] Brak nowej migracji
- [ ] Brak odejmowania kwot / `parseFloat` jako spread
- [ ] `quotations` service nie importuje `channel_quotes`
- [ ] `just dup` ≤ 3%

## Knowledge

Wzorzec = 16.0/17.0 odczyt katalogu przy wycenie. Nie RAG na spread.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Zapis wyniku won/lost świadomie leftover.
