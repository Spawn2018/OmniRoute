# Plaster 522.0 · AI5.0 — `data_source` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI5.0 · VISION B.3 · po [521.0](../archived/521.0-article50-mark.md)

**Zależy od:** AI1.4 leftover `data_source` · badania `09` / `10` jako katalog nazw, nie 22 plastry

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `data_sources`. Tabela `data_source`: `source_code` (snake 2–32, **bez** CHECK listy) + `license_label` (tekst 2–64) + `rights_scope` (tekst 2–128) + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja AI5.0 — **słownik źródeł z licencją jako dana HITL**, nie live ingest 22 dostępów.
- **Wybrane:** POST/GET `/api/v1/data-sources`. OpenFGA `can_manage_data_sources`. UI `/data-sources`. `SHIPPED["522.0"]`.
- **Wybrane:** how-to `docs/operator/data-source.md`.
- **Odrzucone:** live ingest / HTTP do zewnętrznych API / 22 dostępy P0 jako osobne plastry (`REJECTED` — leftover bramy „źródło + właściciel + wyjątek”).
- **Odrzucone:** CHECK/ENUM listy licencji · OMNI READINESS ENGINE · pasek 72% · KPI wzór z modelu (`REJECTED`).
- **Odrzucone:** float / amount / sekrety / `api_key` / `base_url` (`REJECTED`).

**Łowca:** ISTNIEJE `twin_kind` · `suggestion_kind` · `outcome_kind` · `autonomy_level`. BRAK `data_source`.

## Zakres

HITL `data_source` — migracja 412 + warstwy + OpenFGA + FE + how-to + SHIPPED 522.0.

## Poza zakresem

live ingest · brama produkcyjna · cechy modelu AI5.1 · KPI OTD/OTIF wzór · float

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja; brak CHECK listy źródeł/licencji
- [ ] POST zły kod / puste etykiety → 400; amount → 422
- [ ] UI `/data-sources`; `SHIPPED["522.0"]`
- [ ] craft_style; how-to

## Leftover

brama „źródło prawdy + właściciel + wyjątek” · live ingest · AI5.1
