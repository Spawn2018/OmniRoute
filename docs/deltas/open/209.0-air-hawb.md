# Plaster 209.0 · U3b `hawb_no` / `mawb_no` na `shipment_leg` air

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § U3 leftover · [154.0](../archived/154.0-air.md) leftover HAWB/MAWB · [karty-pol-fala-u.md](../../analysis/karty-pol-fala-u.md) § U3
**Zależy od:** 154.0 `leg_kind=air` już jest · 160.0 `ocean_bill` (wzorzec tokenu listu; ten plaster nie importuje ocean_bills)
**Status:** Otwarty `/noc` 2026-09-09. Nie pula M-03. Nie IATA live. Nie PDF. Nie ocean_bill.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover U3b HAWB/MAWB + leftover U3c pule + leftover P4c warning + leftover P4b armator/serwis FK + leftover T4 `parent_shipment_id` + S53 Auth0):** leftover P4c warning-jako-fakt parked. leftover P4b armator/serwis FK parked. leftover U3c pule M-03 parked. leftover T4 rentowność SQL na `charge` parked w tym cyklu (T4 zostaje następnym Q po U3b). S53 Auth0 parked. Żywy plaster: **pogłębienie istniejącej** `shipment_leg` — opcjonalne `hawb_no` i `mawb_no` jako dana na odcinku `air`, nie generator puli i nie list morski. POST bez pól zostaje `null`. Zły token → 400 (`numer`). Token na `road`/`rail`/`china_rail`/`ocean_lcl` → 400 (`list`). Unique per tenant gdy numer jest. UI `/air`. `SHIPPED_CHARGE_ROUTES["209.0"]` = `/air`. OpenFGA bez nowej relacji. Migracja `149_shipment_leg_air_waybill`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `shipment_leg` `air` — odcinek lotnisk; testy 154.0 świadomie bez HAWB. ISTNIEJE `ocean_bill` — HBL/MBL, **nie** HAWB (`bill_kind=hawb` → 400). ISTNIEJE `shipment_document` — wskazanie dokumentu, nie numer listu. ISTNIEJE `quotation.document_number` — pula oferty, leftover U3c. BRAK `hawb_no` / `mawb_no` na nodze. BRAK IATA HTTP.

**Odrzucone:**

- Pula numerów M-03 / generator (leftover U3c).
- Cyfra kontrolna IATA / live HTTP IATA / e-rates.
- `ocean_bill.bill_kind=hawb` / druga tabela listu.
- PDF / QR / ZPL.
- P4c warning · P4b armator FK · T4 SQL na `charge` · S53 Auth0.
- Import ocean_bills / charges / quotations.

## Zakres

1. Migracja `149_shipment_leg_air_waybill` (po `148_local_charge_iso`): kolumny `hawb_no` / `mawb_no` nullable, CHECK tylko przy `leg_kind=air`, unique per tenant gdy numer jest.
2. POST opcjonalny `hawb_no` / `mawb_no`. Zły token → 400. Serwis nie importuje ocean_bills i nie woła HTTP.
3. UI `/air`. How-to `lot.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

Pule M-03 · IATA live · ocean_bill · PDF · T4 · P4c · Auth0 · T-SQL · charge · live HTTP

## Kryteria akceptacji

- [ ] POST `air` z `hawb_no` / `mawb_no` zapisuje token, bez FK i bez ocean_bill
- [ ] POST `air` bez pól zostaje `null`
- [ ] zły token → 400 (`numer`); token na innym `leg_kind` → 400 (`list`)
- [ ] unique per tenant gdy numer jest; izolacja
- [ ] Zero float / marży / puli / IATA HTTP / scrape / PDF
