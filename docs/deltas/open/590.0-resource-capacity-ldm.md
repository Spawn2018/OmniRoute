# Plaster 590.0 · T2 leftover — HITL `resource.capacity_ldm`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [589.0](../archived/589.0-resource-capacity-kg.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2

**Zależy od:** 151.0 `resource` · 589.0 `capacity_kg`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie `capacity_m3`. Nie `document_expiries`. Nie floating trailer. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po kg. Opcjonalny `capacity_ldm` na `resource` (`Numeric(14,4)`, Decimal HITL metrów ładunkowych, > 0, nie float, nie waluta, nie `charge`). POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`ldm`). Zmiana LDM przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z kg i nr). Nie liczy m³.
- **Wybrane:** UI `/fleet`. `SHIPPED["590.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `466_resource_capacity_ldm` (po `465_resource_capacity_kg`).
- **Odrzucone:** `capacity_m3` (`REJECTED` w tym plasterze). `document_expiries` / floating / HW (`REJECTED`). float (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.capacity_ldm`. ISTNIEJE `capacity_kg` (589.0 — reuse walidacji Decimal, osobny komunikat `ldm`). PODOBNE: opcjonalny Decimal + supersede.

## Zakres

1. Migracja `466_resource_capacity_ldm`.
2. Domain `require_capacity_ldm` + serwis + API + GET.
3. UI `/fleet` pole LDM. How-to. `SHIPPED["590.0"]`.

## Poza zakresem

`capacity_m3` · `document_expiries` · floating · HW · UXCL · Job#2 · N3 · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `capacity_ldm="13.6"` zapisuje Decimal
- [ ] float / ≤0 → 400 (`ldm`)
- [ ] zmiana LDM → supersede
- [ ] izolacja RLS
- [ ] zero float / import charges

## Leftover

`capacity_m3` · `document_expiries` · floating trailer · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
