# Plaster 135.0 · O2 ręczny `channel_quote` z wyceny

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O2 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § O2
**Zależy od:** 13.0 / 134.0 `channel_quote` · 85.0 wskazanie
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Ten sam żywy ID **M-19**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki O2):** ten sam POST `/channel-quotes`. `source_ref = tenant:manual:{user_id}` (nie goły `tenant:manual`). Konflikt dnia → `ChannelQuoteConflict` HTTP **409**. Formularz zapisu na `/quotations` (lane wyceny: party + POL + POD + dzień + kwota + waluta + opcjonalny TT). Nie nowy endpoint. Nie mutacja kwoty. `charge` zostaje marżą.

**Łowca:** ISTNIEJE create katalogu i unikat dnia. ISTNIEJE panel porównania (zapis marży = `charge`). BRAK wpisu oferty z `/quotations` i 409.

**Odrzucone:** nowy URL · UPDATE kwoty · INSERT `rate_line`/`charge` z tego formularza · live HTTP · O3.

## Zakres

1. `source_ref` z user_id. Konflikt → 409.
2. Formularz na `/quotations`. `SHIPPED_CHARGE_ROUTES["135.0"]` = `/quotations`.
3. `just api-types`. How-to w `ścieżka-pieniędzy.md` bez nowej linii `katalogi.md`.

## Poza zakresem

O3–O8 · 1.3 accept · T7

## Kryteria akceptacji

- [ ] `source_ref` zawiera `user_id`
- [ ] Drugi POST tego samego dnia/lane/armatora → 409
- [ ] `/quotations` zapisuje ofertę kanału
- [ ] Brak UPDATE `amount`
- [ ] `just api-types`
