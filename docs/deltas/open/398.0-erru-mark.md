# Plaster 398.0 · EXP4.20 `erru_mark` HITL (katalog, bez live ERRU)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala EXP / EXP4.20 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) · ERRU TO_VERIFY
**Status:** Delta zaakceptowana `/noc` 2026-09-12. Wolno `/plaster`. Nie live ERRU. Nie scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki EXP4.20 — tylko HITL katalog `erru_mark` jako dane. `mark_code` + `check_kind` (`to_verify` | `clear` | `hit` | `other`) + `source_ref`. UI `/erru-marks`. OpenFGA `can_manage_erru_marks`. Migracja `309_erru_mark`. Job OPS. Deny-list extract. Live ERRU / Citizen API = leftover.

**Łowca:** BRAK tabeli `erru_mark`. ISTNIEJE `kreptd_licence` — licencja GITD, nie ERRU. ISTNIEJE `registry_poll_mark` — poll CEIDG/KRS, nie ERRU.

**Odrzucone:** live ERRU HTTP · scrape · scoring osoby · kwota · marża · float · UPDATE/DELETE.

## Zakres

1. Migracja `309_erru_mark` RLS FORCE.
2. POST/GET `/erru-marks`. OpenFGA. Deny-list.
3. UI `/erru-marks`. How-to. `SHIPPED_CHARGE_ROUTES["398.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, check_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/score → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
