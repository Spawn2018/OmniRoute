# Plaster 486.0 · AI8.2 leftover auto-zejście jakości — katalog HITL

**Status:** Delta zaakceptowana `/noc 8` 2026-09-14 — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) · [GROUNDING.md](../../../GROUNDING.md) HC auto-zejście · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI8.2 leftover

**Zależy od:** tenancy RLS · `l3_gate_mark` (483.0) · nie L3 write · nie silnik MAE

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `quality_descent_marks`. Tabela `quality_descent_mark`: `mark_code` + `descent_kind` `mae|crps|brier|manual|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja leftover AI8.2 — katalog HITL **powodu zejścia jakości** (MAE / CRPS / Brier / ręcznie), nie auto-przełączenie poziomu i nie wyliczanie metryk.
- **Wybrane:** POST/GET `/api/v1/quality-descent-marks`. OpenFGA `can_manage_quality_descent_marks` (member). UI `/quality-descent-marks`. `SHIPPED["486.0"]` → `/quality-descent-marks`.
- **Wybrane:** Alembic `391_quality_descent_mark` (`down_revision = "390_style_fidelity_mark"`).
- **Wybrane:** how-to `docs/operator/quality-descent-mark.md`.
- **Odrzucone:** silnik auto-zejścia / mutacja `autonomy_level` (`REJECTED` — HC-04; park L3 write).
- **Odrzucone:** wyliczanie MAE/CRPS/Brier w Pythonie (`REJECTED` — Postgres/ledgery; tu tylko stancja).
- **Odrzucone:** scoring osoby / Pain×Frequency (`REJECTED`).
- **Odrzucone:** FK do `l3_gate_mark` / `autonomy_level` / `interval_score` (`REJECTED`).
- **Odrzucone:** AI5.0 `data_source` (`REJECTED` na tym wierszu — park licencja).

**Łowca:** ISTNIEJE `l3_gate_mark`. ISTNIEJE `interval_score` (widok). BRAK `quality_descent_mark`.

## Zakres

How-to + OPS_JOB. FE: `descent_kind` jako `<select>` z jawnymi option (różne od segmented 485 / radio 484).

## Poza zakresem

auto-descent silnik · L3 write · MAE SQL · scoring · kwota · AI5 ingest

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

silnik auto-zejścia · L3 write · AI5.0 ingest (park)
