# Plaster 75.0 · S12 tabela powiadomień (M-34)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S12  
**Zależy od:** 0.3 RLS · 0.4 OpenFGA · 27.0 tablica odczytu

**Status:** Plan zaakceptowany (`/noc` 2026-09-03). Ten sam żywy ID M-34. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-34**, token `operator_notice`. Nowa tabela `operator_notice` z RLS FORCE + test izolacji. Wiersz jest powiadomieniem zapisanym, nie filtrem pending z wycen. `kind` na start tylko `manual`. Status: `unread` | `read`. Create = `unread`. Odczyt: jawny POST `read`. `source_ref` obowiązkowy. Bez FK do `quotation` / `extraction_draft` / `inbound_message`. OpenFGA: nowa relacja `can_manage_operator_notices` = member. Ekran: ten sam `/notifications` — lista zapisanych + utwórz. Tablica 27.0 (HITL + wyceny pending) zostaje leftover na tym ekranie, nie jest źródłem wierszy tabeli. Serwis nie importuje quotations / extraction / inbound. Nie send. Nie outbox.

**Łowca:** BRAK tabeli `operator_notice` / `notification` w backendzie. ISTNIEJE: 27.0 `/notifications` składa `fetchExtractionDrafts("pending")` i `quotationAcceptancePending` w `operatorNotices()` — to filtr, nie inbox. PODOBNE: 74.0 `operator_decision` to szyna werdyktu, nie powiadomienie.

**Odrzucone:**

- Zapis filtra pending z wycen do tabeli (S12 = nie ten job).
- Auto-INSERT z HITL / `quotation` / `operator_decision`.
- Send / Graph / SMTP / push (S18).
- Outbox / M-02 (S16).
- Draft maila (S13). Accept extractu 1.3.
- Nowy żywy ID (M-34 już jest).
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `035_operator_notice_rls`: tabela + RLS FORCE. Test izolacji.
2. Serwis create + list + mark read. `GET/POST /operator-notices`. OpenFGA nowa relacja.
3. UI `/notifications`: lista zapisanych, utwórz unread, oznacz przeczytane. Nie licz w JS.

## Poza zakresem

Filtr 27.0 jako źródło wierszy · send · outbox · S13 draft maila · F9.1

## Ustalenia

- Żywy ID: **M-34**. Token: `operator_notice`.
- HITL zostaje na `/extractions`. LLM nie liczy i nie pisze treści.
- How-to: `docs/operator/katalogi.md`.

## Schemat

`operator_notice`: `id`, `organization_id`, `kind`, `body`, `status`, `read_at`, `source_ref`, timestamps. CHECK kind/status.

## Kryteria akceptacji

- [ ] RLS FORCE + izolacja
- [ ] Create = unread; POST read z unread; ponowny read = błąd albo idempotencja (wybrać w plasterze: idempotentny)
- [ ] `can_manage_operator_notices`; 403 bez tuple
- [ ] UI `/notifications` zapisuje wiersz tabeli
- [ ] Serwis nie importuje quotations / extraction / inbound
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę. Ponowny read = idempotentny (zostaje `read`).
