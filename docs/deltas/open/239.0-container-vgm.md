# Plaster 239.0 · T3 VGM bundle na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [238.0](../archived/238.0-container-cfs-cutoff.md) leftover VGM · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 238.0 `cfs_cutoff_at`
**Status:** Otwarty `/noc` 2026-09-10. Nie live HTTP. Nie kalkulator kg. Nie PIN. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 VGM/PIN/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (osobna waga, nie VGM). leftover `last_survey_at` parked. leftover `shipment_leg_id` parked. Żywy plaster: **pogłębienie istniejącej** `container` — trzy opcjonalne pola HITL razem (oś: bundle, nie trzy plastry): `vgm_kg` (`Numeric(14,4)`, Decimal, nie float, nie waluta, nie `charge`), `vgm_method` (allowlista SOLAS `method1` | `method2`), `vgm_cutoff_at` (`timestamptz`, ISO-8601 ze strefą). Każde pole osobno opcjonalne — brak CHECK „wszystkie albo żadne”. POST bez pola zostaje `null`. Zła waga → 400 (`vgm`). Zły token metody → 400 (`vgm`). Zły cutoff → 400 (`vgm`). `require_vgm_kg` / `require_vgm_method` / `require_vgm_cutoff_at` w `app.domain.container` — nie import `money` / `groupage_tariff` / `stop`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["239.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `178_container_vgm` (po `177_container_cfs_cutoff`). `_WriteBox` — dodać `mass` / `weigh` / `vgm`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `groupage_tariff.chargeable_weight` (D5, inny BC, Decimal progu). ISTNIEJE `app.domain.money` (kwota+waluta, nie kg). ISTNIEJE `container.cfs_cutoff_at` / `si_cutoff_at` (238/235, parse czasu — reuse na `vgm_cutoff_at`). ISTNIEJE `container.bl_kind` allowlista (232.0, inny token). BRAK `container.vgm_kg` / `vgm_method` / `vgm_cutoff_at`. PODOBNE: Decimal bez float jak `require_chargeable_weight`; nie importować groupage. Nie kalkulator Tara+cargo. Nie live HTTP.

**Odrzucone:**

- Live HTTP / scrape VGM z portalu armatora.
- Kalkulator kg / TARA / suma w Pythonie albo JS (`parseFloat` zakaz).
- PIN / `booking_no` / FK / `last_survey_at` / `weight_kg` osobno od VGM.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `178_container_vgm` (po `177_container_cfs_cutoff`): `vgm_kg` nullable Numeric(14,4); `vgm_method` nullable String(16); `vgm_cutoff_at` nullable DateTime(timezone=True).
2. POST opcjonalny bundle. Zła wartość → 400. Serwis nie importuje stops/charges/money. `_container_row` zostaje ≤40 linii.
3. UI `/shipments` panel kontenera. `vgm_kg` jako string (wzorzec `chargeable_weight`). Fold w how-to. Test `catalog.test.ts` dziś zabrania słowa `vgm` — zdjąć ten zakaz, zostawić zakaz `parseFloat`. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · booking · `last_survey_at` · `weight_kg` · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown · kalkulator

## Kryteria akceptacji

- [ ] POST z Decimal `vgm_kg` + `method1`/`method2` + ISO `vgm_cutoff_at` zapisuje dane, bez live HTTP i bez PIN
- [ ] POST bez pól zostaje `null` / `null` / `null`
- [ ] float / zły token / cutoff bez strefy → 400 (`vgm`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / PIN / live HTTP / scrape / import `money` / import `stop`
