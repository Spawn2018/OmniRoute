# Plaster 493.0 · BR6.2 leftover shipper_round_mark

**Status:** Delta zaakceptowana `/noc 8` 2026-09-15. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § BR6.2 leftover · leftover `461.0`

**Zależy od:** `shipper_tender_mark` (461.0) · tenancy RLS · nie Alpega

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `shipper_round_marks`. Tabela `shipper_round_mark`: `mark_code` + `round_kind` `first|second|final|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/shipper-round-marks`. OpenFGA `can_manage_shipper_round_marks` (member). UI `/shipper-round-marks`. `SHIPPED["493.0"]` → `/shipper-round-marks`.
- **Wybrane:** Alembic `398_shipper_round_mark` (`down_revision = "397_sales_lane_volume"`).
- **Odrzucone:** FK do `shipper_tender_mark` / `tender` / `tender_round` (`REJECTED`).
- **Odrzucone:** silnik multi-round · like-for-like SQL · Alpega live · auto-award · kwota / marża / float (`REJECTED`).
- **Odrzucone:** rozszerzenie CHECK `shipper_kind` zamiast osobnego katalogu rund (`REJECTED` — tryb vs runda to dwie osie).

**Łowca:** ISTNIEJE `shipper_tender_mark` (tryb round|bench|spot). ISTNIEJE `tender_round` (G2 sell/buy). BRAK `shipper_round_mark` (runda HITL po stronie załadowcy).

## Zakres

How-to. +1 OPS_JOB. Klon wzorca HITL mark.

## Poza zakresem

Alpega · like-for-like · FK · auto-award · Expo BR6.3

## Kryteria akceptacji

- [ ] migracja varchar + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style; operator how-to

## Leftover

like-for-like · Alpega live · FK · auto-award · BR6.3 Expo (park)
