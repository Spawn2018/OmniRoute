# Plaster 147.0 · I3 `document_dispatch_rule`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § I3 · [karty-pol-fala-i.md](../../analysis/karty-pol-fala-i.md) § I3
**Zależy od:** 137.0 `incoterm` / `trade_side` · U5 `document_kind` checklisty · I2 role
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Nie send. Nie I4.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z I3):** jedna tabela katalogu per tenant. Trójka `(trade_side, incoterm, document_kind)` → `recipient_role`. Rodzaje dokumentu = te same tokeny co checklista (`commercial_invoice` | `packing_list` | `bill_of_lading` | `export_declaration`), nie `noted`/`attached`/`other` z `shipment_document`. Rola adresata = role I2 (`shipper` | `consignee` | `origin_agent` | `dest_agent` | `ocean_carrier` | `omni_customs` | `client_customs`). `source_ref` obowiązkowe. Zmiana roli przy tej samej trójce = nowy wiersz + `superseded_by`. GET SQL po parze incoterm×strona (bieżące), opcjonalnie `document_kind`.

UI `/quotations`. `SHIPPED_CHARGE_ROUTES["147.0"]` = `/quotations`. OpenFGA: `can_manage_quotations`. Migracja `087_document_dispatch_rule`.

**Łowca:** ISTNIEJE `document_checklist_rule` — trójka z `mode` → `document_kind` + `blocks_dispatch`; **inny** job (czy dokument jest na liście), nie rozszerzać o adresata. ISTNIEJE `shipment_document.document_kind` (`noted`/`attached`/`other`) — **inny** katalog. ISTNIEJE GLOSSARY `document_dispatch` (wysyłka + `mail_draft`) — leftover następnego plastra I3. BRAK tabeli `document_dispatch_rule`. BRAK send.

**Odrzucone:**

- Tabela `document_dispatch` (batch mail, `mail_draft_id`, status `draft`/`sent`) — leftover po S11/S18; tu tylko reguła adresata.
- Auto-send, Selenium na portal celny, I4 `booking_instruction`.
- Rozszerzenie checklisty o `recipient_role`.
- Import serwisów quotations / shipments / mail_drafts / charges w tym BC.
- LLM wybierający adresata.

## Zakres

1. Migracja `087_document_dispatch_rule`: RLS, izolacja, indeks `(organization_id, incoterm, trade_side, document_kind)`.
2. POST/GET. Override trójki = nowy wiersz. Obcy kind / rola / brak source_ref → 400.
3. UI `/quotations`. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

`document_dispatch` · send / S18 · I4 · C8 · NBP · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje wiersz; `document_kind=noted` albo `recipient_role=sold_to` → 400
- [ ] Ta sama trójka z inną rolą = nowy wiersz, stary `superseded_by`
- [ ] GET po parze zwraca bieżące wiersze
- [ ] Serwis bez importu quotations / shipments / mail_drafts / charges
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / HTTP send
