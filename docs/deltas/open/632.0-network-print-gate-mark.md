# Plaster 632.0 · leftover D9c 409 stance HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D9 · [626.0](../archived/626.0-network-print-requirement.md) · [162.0](../archived/162.0-document-template.md)
**Zależy od:** 626.0 `network_print_requirement`
**Status:** Delta zaakceptowana `/noc 7` 2026-09-23 — wolno `/plaster`. Nie live 409. Nie PDF.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D9c „409 wyjazdu”):** katalog `network_print_requirement` zostaje wymogiem etykiety sieci. Live HTTP 409 na wyjeździe / zleceniu oraz PDF/QR/ZPL zostają park. Żywy plaster: **nowa tabela** `network_print_gate_mark` per tenant — HITL stance trybu bramy wydruku sieci. Wiersz = `mark_code` (snake 1–32) + `gate_kind` (`block_409`|`warn_only`|`record_only`|`other`) + `source_ref` (`tenant:manual` albo `fixture://network-print-gate/…`). Operator zapisuje, czy uważa brak wymogu za blokadę / ostrzeżenie / tylko zapis; serwis **nie** zwraca 409 na `shipment` / wyjazd i nie woła matching SQL. Zły kind → 400 (`gate`). Obce `source_ref` → 400 (`obce`). Duplikat `mark_code` w tenancie → 409. GET listy SQL. Serwis nie importuje `network_print_requirements` / shipments / charges. UI `/network-print-gate-marks`. `SHIPPED_CHARGE_ROUTES["632.0"]` = `/network-print-gate-marks`. OpenFGA: `can_manage_network_print_gate_marks`. Migracja `504_network_print_gate_mark` po `503_clone_carry_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 626.0 / 162.0 „409 wyjazdu (D9c)” — tu tylko HITL stance, nie egzekucja 409. Źródło: `docs/deltas/archived/626.0-network-print-requirement.md`; PLAN wiersz D9; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `network_print_requirement` — katalog wymogu, nie gate. ISTNIEJE `blocks_create_enforcement_mark` — brama create C8, nie wydruk sieci. BRAK `network_print_gate_mark`. BRAK `/network-print-gate-marks`. BRAK stance block_409|warn_only|record_only dla D9c.

**Odrzucone:**

- live HTTP 409 na POST shipment / wyjeździe
- wiring matching do `network_print_requirement`
- PDF / QR / ZPL / bajty
- FK UUID do `network_print_requirement` / `shipment`
- kwota / float

## Zakres

1. Migracja `504_network_print_gate_mark`: RLS, izolacja, unique `(organization_id, mark_code)` + `(organization_id, source_ref)`, CHECK `gate_kind`.
2. POST/GET. Zły kind / `source_ref` → 400. Duplikat kodu → 409. Amount → 422.
3. UI `/network-print-gate-marks` + zdanie w how-to wydruku / sieci.

## Poza zakresem

live 409 · PDF · QR · ZPL · D9d/e/f · matching T5 · FK UUID · live HTTP

## Plan plików

- `backend/alembic/versions/504_network_print_gate_mark.py`
- `backend/app/models/network_print_gate_mark.py`
- `backend/app/domain/network_print_gate_mark.py`
- `backend/app/repositories/network_print_gate_marks/`
- `backend/app/services/network_print_gate_marks/` (+ AGENTS.md)
- `backend/app/api/network_print_gate_marks.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/network-print-gate-mark/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z `gate_kind=block_409` zapisuje wiersz; `shipment` i `network_print_requirement` bez zmian
- [ ] kind spoza `block_409`|`warn_only`|`record_only`|`other` → 400 (`gate`); amount → 422; duplikat kodu → 409
- [ ] serwis nie importuje `network_print_requirements` / `shipments` / `charges` i nie woła HTTP
- [ ] RLS: tenant B nie widzi znacznika tenanta A; zero float / marży / live 409

## Leftover

live 409 wyjazdu (compose API) · D9d PDF-QR · D9e skan · D9f pdf-zpl · matching T5 · FK kontekstu · T8 live · D7b · auto-resync salda · auto-copy U1
