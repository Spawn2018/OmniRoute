# Plaster 264.0 · T5 outbox `task_template_saved`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T5 leftover outbox · [79.0](../archived/79.0-outbox.md) · [263.0](../archived/263.0-task-template.md)
**Zależy od:** 79.0 `outbox_event` · 263.0 `task_template`
**Status:** Plan `/noc` 2026-09-10. Nie konsument. Nie Temporal. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover outbox T5 + leftover instancja `task` + leftover matching SQL + leftover konsument M-02 + leftover plan_snapshot + leftover T7b + leftover N3 + S53 + PIN):** leftover instancja `task` parked. leftover matching SQL parked. leftover konsument / worker / `processed` parked (live M-02 konsument nie jest tym Q). leftover `plan_snapshot` parked. leftover T7b parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **drugi `event_kind` na istniejącej** `outbox_event` — `task_template_saved`, `subject_id` = UUID szablonu **bez FK** (skład w API). Unikat `(organization_id, event_kind, subject_id)`: ten sam szablon = ten sam wiersz. Zapis składa **API** `task_templates` po POST — **OutboxEventService nie importuje task_templates**; **TaskTemplateService nie importuje outbox**. Status zostaje `pending`. Brak nowej tabeli. `SHIPPED_CHARGE_ROUTES["264.0"]` = `/outbox`. OpenFGA bez nowej relacji. Job OPS bez zmian (110).

**Łowca:** ISTNIEJE `outbox_event` — kind tylko `inbound_message_saved`; CHECK w modelu i migracji. ISTNIEJE skład w API poczty (`record_message_saved`). ISTNIEJE `task_template` 263.0. ISTNIEJE `entity_event` — inny ledger, nie outbox. BRAK `task_template_saved`. BRAK konsumenta.

**Odrzucone:**

- worker / dispatcher / Temporal / Hatchet / status `processed`.
- JSON payload.
- import `task_templates` w serwisie outbox albo outbox w serwisie szablonów.
- nowa tabela · instancja `task` · matching `applies_when`.
- live HTTP · Auth0 · kwota / marża / float.

## Zakres

1. Migracja `197_outbox_task_template_kind` (po `196_task_template`): CHECK `event_kind IN ('inbound_message_saved','task_template_saved')`. Nie nowa tabela.
2. POST `/task-templates` po zapisie woła outbox w warstwie API. Ponowny ten sam `subject_id` = ten sam wiersz.
3. How-to fold w `szablon-zadania.md` / linia outbox w `katalogi.md`. Nie nowa linia katalogu (40). `SHIPPED_CHARGE_ROUTES["264.0"]` = `/outbox`.

## Poza zakresem

konsument · Temporal · instancja `task` · matching SQL · live HTTP · JSON payload · nowa tabela

## Kryteria akceptacji

- [ ] POST szablonu zapisuje `outbox_event` kind `task_template_saved` bez HTTP
- [ ] ten sam `subject_id` = ten sam wiersz outbox
- [ ] OutboxEventService nie importuje task_templates; TaskTemplateService nie importuje outbox
- [ ] Zero nowej tabeli / konsumenta / float / marży / instancji `task`
