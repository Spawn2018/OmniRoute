# Plaster 53.0 · U-condensed (`table_density_condensed`)

**Spec źródłowa:** [docs/spec/table-condensed.md](../../spec/table-condensed.md)  
**Zależy od:** DataTableShell compact/comfortable + katalog `rate_line`

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. Nie gęstość globalna. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN § Wave FE leftover U-condensed, ADR-0003 §3):** trzeci tryb **tylko** na gridzie stawek. Token `table_density_condensed` (UI, nie tabela). Żywy ID leftover **U-condensed**, plaster **53.0**. `allowCondensed` na `DataTableShell`; `/rate-lines` ustawia true. Inne katalogi zostają compact/comfortable.

**Łowca:** ISTNIEJE: `TableDensity` compact|comfortable, select gęstości, `estimateSize` 32/40. BRAK `condensed`. PODOBNE: canvas ui-01 Density. Postgres: `table_view.config` JSON — bez nowej kolumny.

**Odrzucone:**

- Condensed na każdym DataTableShell (gate: condensed globalnie).
- Czwarty silnik tabeli.
- Nowa tabela / nowa trasa.
- Zmiana wysokości HITL / sidebara.

## Zakres

`TableDensity` + `condensed`. Opcja w selectcie tylko gdy `allowCondensed`. Wiersz `py-0.5` / 24 px virtualizer. Test: shell użytkowników bez `condensed`; `/rate-lines` z `allowCondensed`. Zero migracji. Zero nowej trasy.

## Poza zakresem

U-primitives-json · freeze kolumn · i18n

## Ustalenia

- Token: `table_density_condensed` (GLOSSARY, UI).
- OpenFGA / RLS: bez zmian (widok tabeli już tenantowy).
- Job: ten sam `/rate-lines`. `SHIPPED_CHARGE_ROUTES` bez 53.0.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 53.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/rate-lines` ma `allowCondensed` i opcję `condensed`
- [ ] Domyślny DataTableShell (users) **nie** ma opcji `condensed`
- [ ] Brak nowej migracji i nowej trasy
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | wysokość wiersza |
| Dług w diffie | leftover | freeze / primitives |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. U-primitives-json leftover.
