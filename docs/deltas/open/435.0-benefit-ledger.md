# Plaster 435.0 · AI1.3 `benefit_ledger` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.3

**Zależy od:** 434.0 `counterfactual_run` (AI1.2 DONE) · AI0 `charge.source_ref` (129.0 / 072)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `benefit_ledger` per tenant — append-only HITL. Wiersz = `benefit_code` (snake 2–32) + `method_label` (metoda punktu odniesienia, tekst 1–256, obowiązkowa) + `hours_saved` (`Numeric(14,4)`, zakaz float) + `saved_amount` (`Numeric(14,4)`) + `saved_currency` (ISO 4217) + `source_ref`. Operator wpisuje oszczędność. Serwis nie liczy z `charge` i nie woła LLM.
- **Wybrane:** `method_label` obowiązkowy — bez metody liczba jest nieweryfikowalna (VISION B.2).
- **Odrzucone:** kolumna `margin` / buy+sell — `charge` zostaje jedynym miejscem marży. Dump `04b` druga marża = zakaz.
- **Odrzucone:** wyliczenie z `charge` / `outcome_ledger` / `counterfactual_run`.
- **Odrzucone:** słowniki AI1.4 · trzy BC autosave · FK · JSON · UPDATE/DELETE · float.

**Łowca:** PODOBNE `outcome_ledger` (Decimal HITL). PODOBNE `intervention_outcome` (wynik bez kwoty — inny BC). PODOBNE `charge` (nie rozszerzać). BRAK `benefit_ledger`.

## Zakres

HITL `benefit_ledger`. Migracja `345` (revises `344_counterfactual_run`). UI `/benefit-ledgers`.
OpenFGA `can_manage_benefit_ledgers`. `SHIPPED["435.0"]`. OPS_JOBS length 252.

## Poza zakresem

druga marża · SQL z charge · słowniki AI1.4 · trzy BC autosave · FK · CRPS · silnik AI4.1

## Kryteria akceptacji

- [ ] POST z benefit_code, method_label, hours_saved Decimal, saved_amount+currency, source_ref
- [ ] float / margin / extra → 422; zły kod / metoda / waluta / source_ref → 400
- [ ] izolacja RLS; REQUIRED `/benefit-ledgers`
- [ ] OPS_JOBS length 252; SHIPPED `"435.0"`
- [ ] craft_style funkcje>40: 0

## Leftover

AI1.4 słowniki · trzy BC piszą do substratu · silnik AI4.1 · CRPS liczone (AI2)
