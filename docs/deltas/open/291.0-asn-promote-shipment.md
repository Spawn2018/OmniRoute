# Plaster 291.0 · CT1 HITL promote ASN → shipment

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § CT1 leftover auto shipment · U1
**Zależy od:** 278.0 asn · 290.0 shipment labels · M-35 shipment · U1 field_carry optional later
**Status:** Plan `/noc` 2026-09-11. Nie live EDI. Nie auto przy POST asn.

## Wybrane / odrzucone / dlaczego

**Wybrane:** operator świadomie promuje awizo do zlecenia: `POST /asns/{id}/promote` z `quotation_id` (+ opcjonalny `source_ref`). API czyta ASN (guide_code, plant_label, carrier_label), odpala tę samą bramkę przewodnika co POST `/shipments`, tworzy `shipment` przez `ShipmentService`. Zapis powiązania: kolumna `shipment.asn_id` nullable UUID (FK org-scoped) **albo** osobna tabela `asn_shipment_link` — wybór: **nullable `shipment.asn_id`** (jeden ASN → max jedno zlecenie w tym plasterze; unique org+asn_id).

**Łowca:** BRAK promote / `asn_id` na shipment. ISTNIEJE ASN + shipment + gate CT4.

**Odrzucone:** auto INSERT shipment przy POST asn · qty float · live EDI 856 · U1 overwrite wyceny · BC purchase_orders import shipments service (składanie w API).

## Zakres

1. Migracja `223_shipment_asn_id`: nullable `asn_id` + FK + unique (org, asn_id).
2. Domain/API promote + testy 201/404/409 guide.
3. FE przycisk na awizie / how-to.

## Kryteria akceptacji

- [ ] promote znanego asn + quotation z party → 201 shipment z guide/etykietami z ASN
- [ ] block_409 jak na POST shipments
- [ ] drugi promote tego asn → 409/conflict
- [ ] BC services bez krzyżowych importów shipment↔purchase_orders
- [ ] zero float

## Leftover po 291.0

U1 field carry masowy. qty. Live EDI. Wspólny SELECT CT11. CT2.
