# Plaster 551.0 · F10 leftover — HITL `invoice_alloc_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala F · F10 leftover po 550.0

**Zależy od:** tenancy · `purchase_invoice` (549.0) · `invoice_match_mark` (550.0)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover F10. Nowa tabela `invoice_alloc_mark`: `mark_code` (snake 2–32) + `alloc_kind` `line|header|batch|other` + `source_ref`. Append-only. RLS. Katalog stancji alokacji FV zakupu — nie `purchase_invoice_allocation` z kwotą i nie SQL.
- **Wybrane:** POST/GET `/api/v1/invoice-alloc-marks`. OpenFGA `can_manage_invoice_alloc_marks`. UI `/invoice-alloc-marks`. `SHIPPED["551.0"]`.
- **Odrzucone:** tabela `purchase_invoice_allocation` z kwotą / FK charge (`REJECTED` → leftover). Ranking SQL / `invoice_match_candidate` (`REJECTED` → leftover). Auto-link do `charge` / `purchase_invoice` (`REJECTED`). Kwota / marża / float (`REJECTED`). Live ERP (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13.

**Łowca:** BRAK `invoice_alloc_mark` / `purchase_invoice_allocation` w models. ISTNIEJE `invoice_match_mark` (stancja match, nie allocation). ISTNIEJE `cost_allocation_mark` (koszt ABC, nie FV zakupu). ISTNIEJE `purchase_invoice` (ingest).

## Zakres

1. Migracja `431_invoice_alloc_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["551.0"]` → `/invoice-alloc-marks`.

## Poza zakresem

`purchase_invoice_allocation` · ranking SQL · `invoice_match_candidate` · auto-link · kwota · live ERP · AI3 · G0

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount / FK purchase_invoice

## Leftover

`purchase_invoice_allocation` · ranking SQL · `invoice_match_candidate` · auto-link · kwota · live ERP
