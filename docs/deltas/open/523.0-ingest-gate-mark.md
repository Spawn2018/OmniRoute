# Plaster 523.0 · AI5.0 leftover — `ingest_gate_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI5.0 leftover · po [522.0](../archived/522.0-data-source.md)

**Zależy od:** AI5.0 `data_source` · brama przed pierwszym ingestem produkcyjnym

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `ingest_gate_marks`. Tabela `ingest_gate_mark`: `mark_code` + `gate_kind` `truth|owner|exception|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja leftoveru AI5 — **brama jako dana HITL**, nie live ingest.
- **Wybrane:** POST/GET `/api/v1/ingest-gate-marks`. OpenFGA `can_manage_ingest_gate_marks`. UI `/ingest-gate-marks`. `SHIPPED["523.0"]`.
- **Wybrane:** how-to `docs/operator/ingest-gate-mark.md`.
- **Odrzucone:** live ingest HTTP · mutacja `data_source` · 22 plastry dostępów (`REJECTED`).
- **Odrzucone:** OMNI READINESS · pasek 72% · KPI wzór z modelu · float / amount (`REJECTED`).

**Łowca:** ISTNIEJE `data_source` · `l3_gate_mark`. BRAK `ingest_gate_mark`.

## Zakres

HITL `ingest_gate_mark` — migracja 413 + warstwy + OpenFGA + FE + how-to + SHIPPED 523.0.

## Poza zakresem

live ingest · AI5.1 cechy · KPI OTD/OTIF wzór

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] UI `/ingest-gate-marks`; `SHIPPED["523.0"]`
- [ ] craft_style; how-to

## Leftover

live ingest · AI5.1 · definicja KPI per strona
