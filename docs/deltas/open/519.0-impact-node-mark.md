# Plaster 519.0 · AI6.0 — `impact_node_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI6.0 · po [518.0](../archived/518.0-allocation-level.md)

**Zależy od:** `tower_impact` (V6) · `impact_scenario` (CI6) · `line_impact_mark` zostają osobno

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `impact_node_marks`. Tabela `impact_node_mark`: `mark_code` + `node_kind` `shipment|inventory|sku|line|order|revenue|margin|cash|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja AI6.0 — **węzły kaskady** Business Impact Graph jako dane HITL (VISION B.6), nie SQL i nie EBITDA.
- **Wybrane:** POST/GET `/api/v1/impact-node-marks`. OpenFGA `can_manage_impact_node_marks`. UI `/impact-node-marks`. `SHIPPED["519.0"]`.
- **Wybrane:** how-to `docs/operator/impact-node-mark.md`.
- **Odrzucone:** mutacja `tower_impact` / `impact_scenario` / `line_impact_mark` (`REJECTED` — osobne BC: etap V6 vs scenariusz CI6 vs skutek linii vs węzeł AI6).
- **Odrzucone:** SQL grafu / EBITDA / Watch Tower silnik / live plant (`REJECTED`).
- **Odrzucone:** krawędzie grafu / FK między węzłami (`REJECTED` — leftover osobnego plastra).
- **Odrzucone:** float / amount / druga marża (`REJECTED`).

**Łowca:** ISTNIEJE `tower_impact` (stock|production|sales|ebitda). ISTNIEJE `impact_scenario` (chain_label). ISTNIEJE `line_impact_mark`. BRAK `impact_node_mark` z 8 węzłami kaskady AI6.

## Zakres

HITL `impact_node_mark` — migracja 409 + warstwy + OpenFGA + FE + how-to + SHIPPED 519.0.

## Poza zakresem

krawędzie · SQL grafu · EBITDA · Watch Tower silnik · live plant · kwota · UPDATE/DELETE

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] UI `/impact-node-marks`; `SHIPPED["519.0"]`
- [ ] craft_style; how-to

## Leftover

impact_edge · SQL grafu · EBITDA · AI6 silnik · live plant
