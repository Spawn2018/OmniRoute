# Plaster 613.0 · T4 `shipment_id` na `charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T4 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T4 · [262.0](../archived/262.0-charge-sql-margin.md)
**Zależy od:** 1.2 `charge`; 210.0 `parent_shipment_id`; 262.0 marża listy z SQL
**Status:** Delta zaakceptowana `/noc` 2026-09-21. Wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T4 rollup drzewa + karta T3 zamknięta w 612.0):** karta T3 (POL/POD/miasto) jest w kodzie. `hbl_no` / `mbl_no` odrzucone — HBL/MBL jest `ocean_bill` (160.0). FK `port` odrzucone. Rollup SQL główne+podzlecenia czeka w 262.0 na wiązanie `charge` ze zleceniem: na `charge` nie ma `shipment_id`. Widok rentowności w tym plasterze zostałby bez klucza. leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `charge` — opcjonalny `shipment_id` (UUID, złożone FK tenanta, `ON DELETE RESTRICT`). POST bez pola zostaje `null`. Stare wiersze zostają `null`. Obcy UUID albo zlecenie spoza tenanta → 400 (`zlecenie`) przez `IntegrityError` → `InvalidShipment` (handler `DomainError` już jest 400; bez nowej klasy i bez zmiany HTTP `ShipmentConflict`). Serwis `charges` nie importuje `app.services.shipments`. Marża zostaje `margin(buy, sell)` na jednym rekordzie. UI `/charges`. `SHIPPED_CHARGE_ROUTES["613.0"]` = `/charges`. OpenFGA bez nowej relacji (`can_manage_charges`). Migracja `488_charge_shipment_id` (po `487_container_destination_city`).

**Łowca:** ISTNIEJE `shipment.parent_shipment_id` + `relation_kind` (210.0). ISTNIEJE GET listy `charge` z marżą SQL (262.0) bez FK do zlecenia. ISTNIEJE `container.shipment_id` jako złożone FK. BRAK `charge.shipment_id`. ISTNIEJE `ocean_bill` na HBL/MBL.

**Odrzucone:**

- widok rollup drzewa, kolumna magazynu marży, suma w Pythonie, `charge.quotation_id`
- `hbl_no` na `container`, FK portu, import serwisu shipments
- HW, UXCL, N3, AI3, G0-SH, float, Auth0, live HTTP

## Zakres

1. Migracja `488_charge_shipment_id`: `shipment_id` nullable UUID + `ForeignKeyConstraint` (`organization_id`, `shipment_id`) → `shipment` (`organization_id`, `id`), `ON DELETE RESTRICT`. Nie `create_table`.
2. POST opcjonalny `shipment_id`. Brak pola i `null` zostają `null`. Zły UUID → 422. Nieznane zlecenie → 400 (`zlecenie`).
3. UI `/charges` opcjonalne pole. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

Rollup SQL drzewa · generated marża · `quotation_id` · HBL/MBL · FK portu · HW · UXCL · N3 · Auth0 · live HTTP · float

## Kryteria akceptacji

- [ ] POST z `shipment_id` tego tenanta zapisuje UUID
- [ ] POST bez pola / `null` zostaje `null`; stare wiersze bez backfillu
- [ ] obcy UUID → 400 (`zlecenie`); serwis nie importuje `app.services.shipments`
- [ ] RLS istniejącego `charge`; izolacja bez nowej tabeli (cudze zlecenie nie wchodzi)
- [ ] Zero float / drugiej kolumny marży / widoku rollup / live HTTP

## Leftover

Rollup SQL drzewa `parent_shipment_id` · HW · UXCL · N3 · AI3 · G0-SH

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
