# Plaster 57.0 · U-print (`print_sheet`)

**Spec źródłowa:** [docs/spec/print-sheet.md](../../spec/print-sheet.md)  
**Zależy od:** ADR-0003 §9 + 19.0/31.0/33.0 panele dokumentu

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. Nie PDF-teatr. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN leftover U-print):** jeden blok `@media print` w `frontend/src/index.css`. Token `print_sheet`. Żywy ID leftover **U-print**, plaster **57.0**. Druk przeglądarki: chowa chrome shella (`aside`, `header`, ⌘K). Panele oferty / FV / dokumentów zlecenia zostają. Gate leftover: chaos `@media print` albo PDF-teatr.

**Łowca:** BRAK `@media print`. ISTNIEJE: `data-offer-document`, `data-sales-invoice`, `data-shipment-document`, `aside` w sidebar, `header` w AppShell. PODOBNE: `a11y.test.ts` czyta CSS.

**Odrzucone:**

- jsPDF / pdf-lib / blob w bazie.
- Nowa tabela HBL / numer FV.
- Nowa trasa.
- `window.print` w kodzie (operator: Ctrl+P).

## Zakres

`index.css` `@media print`. Test. Zero migracji. `SHIPPED_CHARGE_ROUTES` bez 57.0.

## Poza zakresem

PDF · KSeF · letterhead · mapa Watchtower

## Ustalenia

- Token: `print_sheet` (GLOSSARY).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `index.css` ma `@media print` chowające `aside` i `header`
- [ ] Brak `jspdf` / `pdf-lib` w `package.json`
- [ ] Panele dokumentu nie są `display: none` w druku
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | CSS |
| Dług w diffie | leftover | Wave FE leftover table pusta |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Wave FE leftover U-* po tym plasterze pusta.
