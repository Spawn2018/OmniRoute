# Plaster 262.0 · T4b SQL marży na `charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T4 leftover T4b · [210.0](../archived/210.0-shipment-parent.md) leftover SQL · HC-03 / HC-11
**Zależy od:** 1.2 `charge` · 210.0 `parent_shipment_id` (poza zapisem)
**Status:** Plan `/noc` 2026-09-10. Nie druga kolumna marży. Nie T-SQL. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T4b SQL `charge` + leftover rollup drzewa shipment + leftover lookup/KSeF TE + leftover T5 + leftover T7b + leftover N3 + S53 + PIN):** leftover rollup główne+podzlecenia parked — na `charge` nie ma `shipment_id`. leftover lookup/KSeF parked. leftover T5 parked. leftover T7b parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **GET listy `charge`** zwraca `margin_amount` / `margin_currency` z Postgres (`sell_amount - buy_amount`, ta sama waluta już w CHECK), nie z `margin()` w `ChargeResponse.from_row`. POST nadal woła `margin()` w serwisie (odrzut mieszanej waluty zanim INSERT). Brak nowej tabeli i brak kolumny magazynu marży. `SHIPPED_CHARGE_ROUTES["262.0"]` = `/charges`. OpenFGA bez nowej relacji. UI `/charges` bez odejmowania w JS.

**Łowca:** ISTNIEJE `margin()` w domenie i w `ChargeResponse.from_row` (Python na GET). ISTNIEJE CHECK `buy_currency = sell_currency`. ISTNIEJE kolumna `margin_amount` w JSON, nie w tabeli. BRAK `shipment_id` na `charge`. BRAK `select(sell - buy)` w repozytorium.

**Odrzucone:**

- kolumna / generated column magazynu marży (HC-03).
- rollup SQL po `parent_shipment_id` (brak FK `charge`→`shipment`).
- float · T-SQL · mnożenie kursem · live HTTP · druga tabela.

## Zakres

1. SELECT listy: `sell_amount - buy_amount` w SQL. GET nie woła `margin()` przy mapowaniu wiersza.
2. Test: lista HTTP = różnica Decimal; kontrakt że GET nie importuje `margin` do mapowania listy. POST zostaje z `margin()`.
3. How-to fold w `tablica-finansowa.md` / `ścieżka-pieniędzy.md`. Nie nowa linia w `katalogi.md`.

## Poza zakresem

rollup drzewa · `charge.shipment_id` · generated column · T5 · KSeF · Auth0 · live HTTP · float

## Kryteria akceptacji

- [ ] GET `/charges` zwraca `margin_amount` zgodne z `sell - buy` z SQL, nie z pętli Python `margin()` na liście
- [ ] POST nadal waliduje parę przez `margin()`; mieszana waluta odpada
- [ ] Zero nowej kolumny / tabeli / float / JS `parseFloat` / live HTTP / rollup drzewa
