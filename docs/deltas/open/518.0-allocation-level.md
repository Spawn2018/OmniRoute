# Plaster 518.0 · AI7.0 leftover — `allocation_level` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI7.0 leftover · po [517.0](../archived/517.0-cost-category-mark.md)

**Zależy od:** 516.0 `allocation_key` · 517.0 `cost_category_mark`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `allocation_levels`. Tabela `allocation_level`: `level_code` (snake 2–32, **bez** CHECK listy 12) + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja leftover AI7.0 — **12 poziomów** jako otwarty słownik (VISION), nie SQL i nie TCM.
- **Wybrane:** POST/GET `/api/v1/allocation-levels`. OpenFGA `can_manage_allocation_levels`. UI `/allocation-levels`. `SHIPPED["518.0"]`.
- **Wybrane:** how-to `docs/operator/allocation-level.md`.
- **Odrzucone:** CHECK/ENUM listy 12 (`REJECTED` — nowy poziom = INSERT).
- **Odrzucone:** mutacja `allocation_key` / `cost_category_mark` / `autonomy_level` (`REJECTED` — osobne BC).
- **Odrzucone:** SQL alokacji / TCM / druga marża / AI7.1 / live ERP (`REJECTED`).
- **Odrzucone:** float / amount (`REJECTED`).

**Łowca:** ISTNIEJE `allocation_key` · `cost_category_mark` · `autonomy_level`. BRAK `allocation_level`.

## Zakres

HITL `allocation_level` — migracja 408 + warstwy + OpenFGA + FE + how-to + SHIPPED 518.0.

## Poza zakresem

SQL alokacji · TCM · Cyfrowy CFO · live ERP · kwota · UPDATE/DELETE

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kod/source_ref → 400; amount → 422
- [ ] UI `/allocation-levels`; `SHIPPED["518.0"]`
- [ ] craft_style; how-to

## Leftover

silnik SQL · TCM · AI7.1 · live ERP
