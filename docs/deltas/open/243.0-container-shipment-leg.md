# Plaster 243.0 · T3 `shipment_leg_id` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [242.0](../archived/242.0-container-carrier-party.md) leftover `shipment_leg_id` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md)
**Zależy od:** 242.0 `carrier_party_id`
**Status:** Otwarty `/noc` 2026-09-10. Nie live HTTP. Nie PIN. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `shipment_leg_id`/PIN/TARA/TEU + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + eventy 2a/2b):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover TARA/TEU parked. eventy 2a/2b parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `shipment_leg_id` (UUID HITL odcinka). Złożone FK `(organization_id, shipment_leg_id)` → `shipment_leg` jak `carrier_party_id` / `shipment_id` na tym samym wierszu. `shipment_leg` nie ma `uq_(organization_id, id)` — migracja doda `uq_shipment_leg_org_id` (wzorzec `uq_party_org_id` / `uq_shipment_org_id`), inaczej Postgres nie przyjmie złożonego FK. POST bez pola zostaje `null`. Nieznany UUID → 404. `require_container_shipment_leg_id` jak `require_carrier_party_id`. API składa bind: nowy odczyt `ShipmentLegService.get_leg` + `ShipmentLegRepository.get` (wzorzec `get_shipment` / `get_party`) — **ContainerService nie importuje `shipment_legs`**. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["243.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `182_container_shipment_leg` (po `181_container_carrier_party`). `_WriteBox` — dodać `leg`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.shipment_id` (złożone FK + bind w API). ISTNIEJE `container.carrier_party_id` (242.0, złożone FK + `PartyService.get_party` w API). ISTNIEJE tabela `shipment_leg` (065+, RLS). BRAK `container.shipment_leg_id`. BRAK `ShipmentLegService.get_leg`. BRAK unique `(organization_id, id)` na `shipment_leg`. PODOBNE: bind w warstwie API. Nie import `app.services.shipment_legs` w BC container. Nie live HTTP. Nie nowa tabela.

**Odrzucone:**

- Import `shipment_legs` w `ContainerService`.
- FK tylko po `id` bez tenanta (słabsze niż 242.0).
- PIN / TARA / TEU / eventy 2a/2b / live HTTP / mapa.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `182_container_shipment_leg`: `uq_shipment_leg_org_id` na `shipment_leg`; `shipment_leg_id` nullable UUID na `container` + FK `(organization_id, shipment_leg_id)` → `shipment_leg`.
2. Odczyt `get_leg` w BC `shipment_legs` (404). POST kontenera opcjonalny. Nieznany → 404. Serwis kontenera nie importuje shipment_legs/charges.
3. UI `/shipments`. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · TARA · TEU · eventy 2a/2b · S21 · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown

## Kryteria akceptacji

- [ ] POST z UUID istniejącego `shipment_leg` zapisuje FK, bez live HTTP i bez PIN
- [ ] POST bez pola zostaje `null`
- [ ] nieznany UUID → 404
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / PIN / live HTTP / scrape / import `shipment_legs` w serwisie kontenera
