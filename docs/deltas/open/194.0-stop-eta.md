# Plaster 194.0 · V2 `stop` ETA fizyczne / prawne

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) § V2 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) leftover T1
**Zależy od:** 150.0 `stop` (punkt na zleceniu już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie mapa. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover C3–C9 + F2b–F4 + G2.20 + V2/V2b):** leftover C3 live VIES parked. leftover C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO parked. leftover F2b–F4 parked. leftover `circle_sim` parked. leftover kg parked. leftover `plan_snapshot` / champion parked. F1 / C1 / C8 409 parked. Żywy plaster: **pogłębienie istniejącej** `stop` — HITL dwa czasy ETA jako dane, nie GPS i nie pogoda. Wiersz dostaje `eta_physical` i `eta_legal` (`timestamptz` NOT NULL po backfillu `created_at`). Operator wpisuje ISO-8601; serwis nie liczy ETA z mapy, nie woła Open-Meteo, nie woła LLM i nie czyta `charge` / `trip`. Zły czas fizyczny → 400 (`fizyczny`). Zły czas prawny → 400 (`prawny`). UI zostaje `/shipments`. `SHIPPED_CHARGE_ROUTES["194.0"]` = `/shipments`. OpenFGA bez nowej relacji (`can_manage_shipments`). Migracja `134_stop_eta`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop` — punkt + IANA, **nie** dwa ETA. ISTNIEJE GLOSSARY `eta_physical` / `eta_legal` bez kolumn. ISTNIEJE `prediction_ledger` 193.0 — CRPS, **nie** ETA na stopie. BRAK `weather_observation`. BRAK myta na `charge` z tego BC. BRAK silnika ETA.

**Odrzucone:**

- C3 live VIES / C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO.
- F2b / F3 / F4 / G2.20 / kg / F1 / C1 / C8 409.
- `plan_snapshot` / champion leftover 193.0.
- Pogoda Open-Meteo / `weather_observation` (leftover V2).
- Myto → `charge` (leftover V2b).
- GPS / tacho / mapa / leaflet / liczenie ETA w Pythonie.
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `134_stop_eta` (po `133_prediction_ledger`): kolumny NOT NULL po backfillu (`created_at`).
2. POST wymaga obu ETA. Zły ISO → 400. Serwis nie importuje trips / charges / extraction / geography i nie woła HTTP.
3. UI `/shipments`. `just api-types`. How-to istniejący punkt; fold w `katalogi.md` (40 linii, bez nowej linii).

## Poza zakresem

C3 live lookup · pogoda · myto · GPS · mapa · plan_snapshot · silnik ETA · kwota · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `stop` z `eta_physical` i `eta_legal`, bez wyliczenia z GPS
- [ ] zły `eta_physical` → 400 (`fizyczny`)
- [ ] zły `eta_legal` → 400 (`prawny`)
- [ ] GET zwraca oba czasy tenanta
- [ ] Test izolacji (wiersz z nowymi kolumnami)
- [ ] Zero float / marży / mapy / live HTTP / scrape
