# Plaster 495.0 · BR6.0 leftover crm_dedup_mark

**Status:** Delta zaakceptowana `/noc 8` 2026-09-15. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § BR6.0 leftover · leftover `490.0`

**Zależy od:** `crm_opportunity` / `crm_activity` / `crm_pipeline_mark` · nie cold-send

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `crm_dedup_marks`. Tabela `crm_dedup_mark`: `mark_code` + `dedup_kind` `nip|vat|email|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/crm-dedup-marks`. OpenFGA `can_manage_crm_dedup_marks`. UI `/crm-dedup-marks`. `SHIPPED["495.0"]`.
- **Wybrane:** Alembic `400_crm_dedup_mark` (`down_revision = "399_shipper_like_mark"`).
- **Odrzucone:** dedup SQL / merge kont / cold-send / FK lead/opportunity (`REJECTED`).

**Łowca:** ISTNIEJE crm_opportunity/activity/pipeline. BRAK stance dedup NIP.

## Zakres

How-to. +1 OPS_JOB.

## Poza zakresem

cold-send · merge · FK · scoring osoby

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; operator how-to

## Leftover

FK · cold-send · merge SQL
