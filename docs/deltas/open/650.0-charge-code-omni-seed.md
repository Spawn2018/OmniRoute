# Plaster 650.0 · leftover EXP1 seed Omni charge_code WAITING/NO_SHOW/DIVERSION/STAMP

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [649.0](../archived/649.0-charge-code-source-ref.md)
**Zależy od:** M-06 `charge_code` + 649.0 `source_ref`
**Status:** Delta zaakceptowana `/noc 17` 2026-09-24. Nie matching. Nie float. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 seed Omni czterech kodów):** katalog `charge_code` zostaje prawdą o tokenie. Żywy plaster: **seed Omni** czterech kodów `WAITING` / `NO_SHOW` / `DIVERSION` / `STAMP` per tenant — INSERT tylko gdy brakuje (jak `incoterm_responsibility` seed). `source_ref=omni:charge-code:exp1`. Nazwy PL/EN krótkie. **Bez** CHECK allowlisty — operator nadal może dodać inne kody. Serwis nie tworzy `rate_line` / `charge`, nie woła HTTP, nie woła LLM. Endpoint `POST /charge-codes/seed`. UI `/charge-codes` (przycisk seed). `SHIPPED_CHARGE_ROUTES["650.0"]` = `/charge-codes`. OpenFGA bez nowej relacji. Brak nowej migracji schematu (dane). Marża zostaje na `charge`. Zero float.

**Dlaczego seed endpoint, nie migracja INSERT:** seed per tenant przy pierwszym użyciu (jak I1); migracja nie zna żywych `organization_id`. Idempotentne — istniejące kody nie nadpisuje.

**REQUIREMENT:** leftover EXP1 po 649.0. Źródło: `docs/analysis/karty-pol-fala-exp.md` § EXP1; CURRENT Następny **650.0**.

**Łowca:** ISTNIEJE `charge_code` + `source_ref` (649.0). ISTNIEJE wzorzec seed Omni (`incoterm_responsibility` POST `/seed`). BRAK seedu czterech kodów EXP1. BRAK CHECK allowlisty. BRAK matching.

**Odrzucone:**

- CHECK allowlisty tylko cztery kody
- float / druga marża / zapis `charge` / `rate_line`
- nadpisywanie istniejących wierszy
- live HTTP / FK fuel_index / spot_or_contract na quotation

## Zakres

1. Domain/service: cztery pary code+name+source_ref; `seed_omni_exp1` INSERT brakujących.
2. POST `/charge-codes/seed` → lista utworzonych (lub pusta gdy już są).
3. UI `/charge-codes` — przycisk „Seed Omni EXP1”. How-to. `SHIPPED_CHARGE_ROUTES["650.0"]`.

## Poza zakresem

CHECK allowlisty · FK fuel_index · spot_or_contract na quotation · matching · float · charge · live HTTP · nowa kolumna

## Plan plików

- domain / serwis / API charge_codes (+ AGENTS: seed Omni dozwolony)
- testy unit + HTTP seed
- `frontend/src/features/charge-codes/` + API client
- `ops-index` + `routes-breadth` (`650.0` → `/charge-codes`)
- how-to · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST seed tworzy cztery kody z `omni:charge-code:exp1` gdy brak
- [ ] ponowny seed nie duplikuje / nie nadpisuje
- [ ] serwis nie importuje rate_lines / charges i nie woła HTTP
- [ ] RLS: seed tenanta A nie widać u B
- [ ] zero float / marży / matching / CHECK allowlisty
