# Plaster 278.0 · CT1 `asn` HITL (awizo, bez EDI 856 live / bez auto shipment)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT1 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`ASN`, nie live EDI CT5)
**Zależy od:** 0.3 tenancy · 276.0 `purchase_order` (nagłówek; FK w tym samym BC) · 277.0 `po_line` (linia już w katalogu; ten Q nie rozszerza linii)
**Status:** Zaakceptowany `/noc` 2026-09-11. 276.0 = nagłówek; 277.0 = linia; 278.0 = awizo HITL. To nie jest live EDI 856 i nie jest auto `shipment`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CT1 / pin 2026-09-08c: katalog HITL awiza wysyłki jako dane; nie live EDI i nie auto shipment):** leftover CT5 EDI 856 live / scrape parked (park live HTTP — brak sekretu i testu). leftover auto-create `shipment` (U1) parked. leftover CT8 AIS live parked (TO_VERIFY licencja). leftover CI1 extract parked. leftover live p44 parked. leftover qty float parked na stałe. Żywy plaster: **nowa tabela** `asn` per tenant w **tym samym BC** `purchase_orders` — HITL katalog awiza na istniejącym nagłówku zamówienia, nie komunikat EDI i nie zlecenie. GLOSSARY: **awizo wysyłki** = `asn` (nie kolumny na `purchase_order` / `po_line`, nie `edi_message`, nie `shipment`). Wiersz = `asn_code` (snake 2–32) + `purchase_order_id` (złożone FK `(organization_id, purchase_order_id)` → `purchase_order`, ten sam tenant) + opcjonalne etykiety `plant_label` / `carrier_label` / `ship_ref_label` (tekst 1–128 albo brak; **nie** FK `party` / geography / `shipment`) + `source_ref` (`tenant:manual` albo `fixture://asn/…`). Zero kolumn `shipment_id` / EDI payload / kwoty / waluty / qty. Operator wpisuje awizo na znanym nagłówku; serwis nie woła HTTP, nie scrapuje 856, nie woła LLM, nie importuje `shipments` / `charges` / `field_carry_forwards` / `edi_messages` / `extraction` i nie tworzy zlecenia. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`awizo`). Zła etykieta → 400 (`zakład` / `przewoźnik` / `referencja`). Obce `source_ref` → 400 (`obce`). Nieznany albo cudzy `purchase_order_id` → 404 (`nieznane zamówienie`) — RLS ukrywa wiersz tenanta B. Body z `shipment_id` / `edi` / `amount` / `currency` / `buy_amount` / `payload` → 422 (`extra=forbid`). Unique `(organization_id, purchase_order_id, asn_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/asns`. `SHIPPED_CHARGE_ROUTES["278.0"]` = `/asns`. OpenFGA: **reuse** `can_manage_purchase_orders` — ten sam job CT1 katalogu, nie nowy obiekt AuthZ. Migracja `211_asn` (po `210_po_line`). Job OPS 123. Deny-list: dodać `app.models.asn`. Marża zostaje na `charge`. To nie jest live EDI i nie jest auto shipment.

**Łowca:** BRAK tabeli `asn` / `advance_ship_notice`. BRAK `asn_code` w schemacie. BRAK nazwy w GLOSSARY — ten plan dopisuje **awizo wysyłki**. ISTNIEJE `purchase_order` 276.0 — nagłówek, **ten sam BC, FK OK**. ISTNIEJE `po_line` 277.0 — linia SKU, **nie** awizo (nie rozszerzać). ISTNIEJE `edi_message` — komunikat **na** `shipment`, **nie** ASN CT1 (nie FK, nie rozszerzać). ISTNIEJE `shipment` / `field_carry_forward` — zlecenie i U1, **nie** awizo PO. ISTNIEJE `booking_instruction` I4 — booking, **nie** ASN. PODOBNE: dziecko z FK tenanta jak `po_line` / `consignment`. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- Live EDI 856 / scrape / CT5 HTTP / parser X12.
- Auto-create `shipment` / zapis `field_carry_forward` / FK `shipment_id`.
- CT8 AIS live / live p44 / CI1 extract LLM.
- qty / kwota / waluta / marża / float / EDI payload / BYTEA.
- Import shipments/charges/edi_messages/field_carry_forwards/extraction; HTTP; UPDATE/DELETE.
- Nowa relacja OpenFGA — ten sam job co nagłówek/linia CT1.

## Zakres

1. Migracja `211_asn` po `210_po_line`: RLS FORCE, izolacja, złożone FK do `purchase_order`, unique awizo-na-PO + `source_ref`. Zero kolumn `shipment_id` / EDI / kwoty.
2. POST/GET `/asns`. Zły token → 400 (`awizo` / `zakład` / `przewoźnik` / `referencja` / `obce`). Cudzy albo nieznany nagłówek → 404 (`nieznane zamówienie`). Body z `shipment_id`/EDI/kwotą → 422. Serwis w BC `purchase_orders` czyta nagłówek z tego samego repozytorium. Brak UPDATE/DELETE. Zero `float()`. ExtractionService nie importuje `asn`.
3. UI `/asns`. How-to `docs/operator/awizo-wysylki.md`; fold w `katalogi.md` (40 linii). Job OPS 123. OpenFGA `can_manage_purchase_orders`. Nazwy: `AsnDesk` / `asn-form.tsx` / `persistAsn`. Brak `<Money/>`. Brak przycisku „Utwórz zlecenie” / „Wyślij EDI”.

## Poza zakresem

live EDI 856 · CT5 scrape · auto `shipment` · CT8 AIS · live p44 · CI1 extract · kwota · float · FK shipment · payload EDI

## Kryteria akceptacji

- [ ] POST zapisuje `asn` z kodem, FK nagłówka, opcjonalnymi etykietami i `source_ref`, bez `shipment_id`
- [ ] zły kod → 400 (`awizo`); obce `source_ref` → 400 (`obce`)
- [ ] POST z `purchase_order_id` tenanta B → 404 (`nieznane zamówienie`)
- [ ] POST z `shipment_id` / `edi` / `amount` / `payload` **nie** zapisuje awiza jako zlecenia (422)
- [ ] GET zwraca awiza tenanta
- [ ] Test izolacji nowej tabeli
- [ ] ExtractionService / `extraction-no-rates` nie importuje `asn`
- [ ] Zero float / marży / live HTTP / importu shipments/charges/edi_messages
- [ ] Zero twierdzenia, że live EDI 856 albo auto shipment istnieje

## Leftover po 278.0

Auto `shipment` (U1) z ASN. CT5 EDI 856 live. CT8 AIS live (TO_VERIFY licencja). Live p44. CI1 extract. `wrapped_dek` / KMS. Reszta pinu 2026-09-08c.
