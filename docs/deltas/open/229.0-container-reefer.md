# Plaster 229.0 · T3 `reefer` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [228.0](../archived/228.0-container-ref5.md) leftover `reefer` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 228.0 `ref_5`
**Status:** Otwarty `/noc` 2026-09-09. Nie temperatura. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `reefer`/temp/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `temp_min` / `temp_max` parked (Decimal). leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — HITL flaga `reefer` (bool, nie nullable). POST bez pola zostaje `false`. `true`/`false` zapisuje flagę. Zły token (nie bool) → 400 (`chłodniczy`). `require_container_reefer` jak `require_blocks_dispatch`: `type is bool` albo 400. UI `/shipments` checkbox. `SHIPPED_CHARGE_ROUTES["229.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `168_container_reefer` (po `167_container_ref5`): `Boolean` NOT NULL `server_default false`. `_WriteBox` — dodać `cold`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container` + `ref_1`…`ref_5`. BRAK kolumny `reefer` (zero trafień w `backend/app`). BRAK `temp_min` / `temp_max` / `needs_external_power`. PODOBNE: `require_blocks_dispatch` (flaga bool). Nie duplikować T2 `resource` — tam też BRAK `reefer`.

**Odrzucone:**

- `temp_min` / `temp_max` / Decimal / float.
- `needs_external_power` w tym plasterze.
- PIN / VGM / `booking_no` / live HTTP.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `168_container_reefer` (po `167_container_ref5`): `reefer` Boolean NOT NULL, default `false`.
2. POST opcjonalny `reefer`. Brak pola → `false`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

temperatura · zasilanie · PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `reefer: true` zapisuje flagę chłodniczego, bez temperatury i bez PIN
- [ ] POST bez pola zostaje `false`
- [ ] zły token → 400 (`chłodniczy`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape
