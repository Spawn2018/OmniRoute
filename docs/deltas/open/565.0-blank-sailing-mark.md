# Plaster 565.0 · V3 leftover — HITL `blank_sailing_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · V3 · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) · leftover z [196.0](archived/196.0-free-time-clock.md)

**Zależy od:** tenancy · wzorzec HITL mark · 196.0 `free_time_clock` (obok, nie to samo) · 344.0 `schedule_exception_mark` (inny zakres: delay/cancel/reroute)

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy V3 leftover po `free_time_clock`. Nowa tabela `blank_sailing_mark`: `mark_code` (snake 2–32) + `sailing_kind` `blank|congestion|gate|other` + `source_ref`. Append-only. RLS. Katalog stancji blank sailing / congestion / Gate OS — nie predykcja, nie countdown N3, nie szkic `charge`.
- **Wybrane:** POST/GET `/api/v1/blank-sailing-marks`. OpenFGA `can_manage_blank_sailing_marks`. UI `/blank-sailing-marks`. `SHIPPED["565.0"]`. OPS_JOBS 335→336.
- **Odrzucone:** silnik predykcji blank sailing / live armator HTTP (`REJECTED` → park). Countdown N3 `remaining` + szkic `charge` (`REJECTED` → leftover). Mutacja `schedule_exception_mark` / `free_time_clock` (`REJECTED`). Kwota / float / scoring (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / PUESC / MOS live / live FK UUID / live 409.

**Łowca:** BRAK `blank_sailing_mark`. ISTNIEJE `schedule_exception_mark` (delay|cancel|reroute — wyjątek planu, nie blank sailing). ISTNIEJE `free_time_clock` (D&D dni). ISTNIEJE `war_room_mark.incident_kind` z tokenem `congestion` (sala, nie ocean blank). ISTNIEJE `prediction_ledger` (V1 — nie blank sailing).

## Zakres

1. Migracja `445_blank_sailing_mark` (po `444_shipment_is_waste`) + RLS + izolacja + unique `(organization_id, source_ref)` + CHECK `sailing_kind`.
2. Model → domain → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["565.0"]` → `/blank-sailing-marks`. OPS +1.

## Poza zakresem

Predykcja blank sailing · live armator · countdown N3 · szkic charge · AI3 · G0 · kwota · FK shipment/container

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED + OPS 336
- [ ] zero float / live HTTP / amount / predykcja

## Leftover

N3 countdown remaining · szkic charge D&D · live armator blank sailing · scoring

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | DO WYPEŁNIENIA | |
| Szybkość | N/A ten plaster | brak ścieżki z budżetu |
| SQL | N/A | INSERT/SELECT standard RLS |
| N+1 | OK | brak pętli |
| Dług / człowiek | OK | wzorzec HITL mark |
| Proza operatora | 5–15 zdań | `docs/operator/blank-sailing-mark.md` |
| Docs/OS | DO WYPEŁNIENIA | |
| Pushy do zielonego | N/A | po push |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | DO WYPEŁNIENIA | |
