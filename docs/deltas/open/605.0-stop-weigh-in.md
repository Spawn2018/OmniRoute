# Plaster 605.0 · T1 EXP1 `weigh_in_kg` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [604.0](../archived/604.0-stop-no-show-at.md) leftover · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 604.0 `no_show_at`; 247.0 `weight_kg`
**Status:** Delta zaakceptowana `/noc` 2026-09-20. Wolno `/plaster`. Nie VGM. Nie weigh_out. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover `weigh_in_kg`/`weigh_out_kg` + leftover HW + leftover UXCL + Job#2 + N3 + AI3 + G0-SH):** leftover `weigh_out_kg` parked. leftover HW parked. leftover UXCL parked. Job#2 parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `weigh_in_kg` (`Numeric(14,4)` Decimal HITL, ≥ 0, pusty → `null`). POST bez pola zostaje `null`. JSON float / bool / ujemna → 400 (`waga wjazdu`). `require_stop_weigh_in_kg` w `app.domain.stop` — wzór `require_stop_weight_kg`, komunikat `waga wjazdu`, **nie** import `container` / VGM / `charges`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["605.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `480_stop_weigh_in` (po `479_stop_no_show_at`). CHECK `ck_stop_weigh_in`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.weight_kg` (247.0) — waga HITL, nie para. ISTNIEJE `container.vgm_kg` (T3, inny BC). BRAK `stop.weigh_in_kg`. BRAK `stop.weigh_out_kg`.

**Odrzucone:**

- `weigh_out_kg` / HW / UXCL / Job#2 / N3 / AI3 / G0-SH / VGM / kalkulator / float / import `container` / T8 / PIN / mapa / Auth0 / live HTTP.

## Zakres

1. Migracja `480_stop_weigh_in`: `weigh_in_kg` nullable Numeric(14,4) + CHECK ≥ 0. Nie `create_table`.
2. POST opcjonalny `weigh_in_kg` (string jak `weight_kg`). Zła wartość → 400 (`waga wjazdu`). Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

weigh_out · VGM · dok / T8 · HW · UXCL · PIN · mapa · Auth0 · live HTTP · float

## Kryteria akceptacji

- [ ] POST z Decimal `weigh_in_kg` zapisuje wagę wjazdu HITL, bez VGM
- [ ] POST bez pola / pusty trim zostaje `null`
- [ ] float / ujemna / nie-liczba → 400 (`waga wjazdu`)
- [ ] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / doku / live HTTP / import `container`

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | — |
| Szybkość | N/A ten plaster | kolumny INSERT/GET |
| SQL | N/A | Numeric + CHECK |
| N+1 | — | — |
| Dług / człowiek | — | wzór `weight_kg`; leftover weigh_out |
| Proza operatora | 5–15 zdań | `zlecenie.md` |
| Docs/OS | — | — |
| Pushy do zielonego | N/A | CI po push |
| Minuty plastra | N/A | — |
| Bench rzemiosła | — | `factory_cycle.py --close` |
