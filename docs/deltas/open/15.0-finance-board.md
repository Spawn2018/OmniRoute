# Plaster 15.0 · M-15 Wirtualny Dyrektor Finansowy (`finance_board`)

**Spec źródłowa:** [docs/spec/finance-board.md](../../spec/finance-board.md)  
**Zależy od:** 1.2 `charge` / `margin()` · 5.0 `party.credit_limit` · 6.0 `nbp_rate` · 14.0 `credit_review` · 0.6 DataTableShell

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z M-15):** tablica faktów finansowych — ekran `/finance`, który **czyta** to, co już jest prawdą w katalogach (`charge.margin` z API, `nbp_rate`, `party.credit_limit`, `credit_review`). Żywy ID **M-15**. Token: `finance_board` (UI, nie tabela). Bez nowego BC.

**Łowca:** BRAK `vdf` / `finance_memo` / `finance_board`. ISTNIEJE: `charge.margin_amount` w API (1.2), `nbp_rate` (6.0), `party.credit_limit` (5.0), `credit_review` (14.0). PODOBNE: pulpit OPS (joby), nie silnik.

**Odrzucone:**

- Nowa tabela / kolumna score / „VDF liczy marżę” — PLAN: LLM nie liczy; `charge` zostaje prawdą o marży. Zgadywanie schematu z archiwum = zakaz CURRENT.
- LLM komentarz do wyceny — extract only; HITL zostaje.
- Zapis `credit_limit` albo `charge` z tego ekranu — mutacje zostają na `/parties` i `/charges`.
- Drugi `margin()` w TypeScript (odejmowanie buy/sell w JS).
- M-02, Auth0, portal.

## Zakres

Read-only `/finance`: cztery bloki (opłaty z marżą, kursy NBP, limity kontrahentów, recenzje kredytowe). DataTableShell albo CatalogLoadedTable na istniejących fetchach. OpenFGA = uprawnienia już zadeklarowane na tych API (strona nie ma własnego routera backendu).

## Poza zakresem

Nowa migracja · nowy serwis · LLM · zapis · auto-scoring · liczenie kwot w JS/Python · ExtractionService

## Ustalenia

- Żywy ID: **M-15**. Token: `finance_board` (GLOSSARY). Archiwum: VDF; w 15.0 jako **tablica odczytu**, nie asystent liczący.
- Brak tabeli `finance_*`. Brak RLS nowej — nie ma nowego wiersza.
- Front: `features/finance-board/` + `routes/finance.tsx`. Własny układ (nie `CatalogCreateForm`).
- Fetch: `fetchCharges`, `fetchNbpRates`, `fetchParties`, `fetchCreditReviews` — bez nowej funkcji liczącej marżę.
- `<Money/>` na `margin_amount`+`margin_currency` oraz `credit_limit`+`credit_currency`.
- OpenFGA: brak nowego `can_manage_finance`. 403 z istniejących API zostaje.
- import-linter: bez zmian pakietów serwisów. Extraction nie importuje finance.
- `just dup` ≤ 3%.
- OPS_JOBS length **22**. `SHIPPED_CHARGE_ROUTES["15.0"]` = `/finance`.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 15.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] UI `/finance` czyta charge / nbp_rate / party limit / credit_review
- [ ] Brak nowej migracji Alembic
- [ ] Brak odejmowania kwot w TS/Python na tej stronie
- [ ] Brak zapisu `credit_limit` / `charge` z `/finance`
- [ ] Brak kolumny score; brak promptu LLM
- [ ] `just dup` ≤ 3%

## Knowledge

Brak karty VDF w `docs/_knowledge/`. Wzorzec = pulpit OPS + odczyt katalogów. Nie RAG na liczenie.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Silnik AI VDF świadomie leftover.
