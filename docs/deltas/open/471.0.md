# Plaster 471.0 · BR5.0 Faktoring — katalog konektora partnera

**Status:** Otwarta delta `/noc`. Zero kodu do akceptacji.

**Spec źródłowa:** [VISION.md](../../VISION.md) § A.4 Strumień 3 Trade-Tech · § A.5 SMEO · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR5.0 · F7 faktoring · [erp-fk-adapter.md](../../analysis/erp-fk-adapter.md) (wzorzec katalogu konektora, nie live)

**Zależy od:** tenancy RLS · wzorzec HITL `erp_connector` (268.0) · `exchange_connector` (S55) · F: `sales_invoice` / `bank_payment` / `working_capital_mark` już w kodzie (bez kleju)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `factoring_connectors`. Tabela `factoring_connector`: `connector_code` + `system_kind` `smeo|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — HITL katalog partnera faktoringowego (SMEO w osi wejścia), nie workflow wypłaty i nie live HTTP SMEO.
- **Wybrane:** POST/GET `/api/v1/factoring-connectors`. OpenFGA `can_manage_factoring_connectors` (member). UI `/factoring-connectors`. `SHIPPED["471.0"]` → `/factoring-connectors`.
- **Wybrane:** Alembic `376_factoring_connector` (`down_revision = "375_silk_corridor_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** live SMEO HTTP / OAuth / webhook wypłaty (`REJECTED` — brak sekretu i testu live; CURRENT: nie live HTTP na tym wierszu mimo nazwy SMEO).
- **Odrzucone:** kolumny sekretu / `base_url` / ciphertext (`REJECTED` — HC-09; wzorzec erp_connector).
- **Odrzucone:** FK do `sales_invoice` / `bank_payment` / `cash_flow` / `quote_invoice_settlement` (`REJECTED` — F7 workflow wypłaty = leftover po katalogu).
- **Odrzucone:** rozszerzenie `working_capital_mark` (DSO/aging) (`REJECTED` — EXP2.1 to znacznik kapitału obrotowego, nie konektor partnera).
- **Odrzucone:** kwota / marża / float / workflow POD→wypłata / LLM limitu kredytowego.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `erp_connector` (Optima fixture). ISTNIEJE `exchange_connector` (Trans.eu fixture). ISTNIEJE `idp_connector` / `visibility_connector` / `sap_connector`. ISTNIEJE `working_capital_mark` (DSO/cash_at_risk). ISTNIEJE `sales_invoice` + `bank_payment` + `cash_flow` (łańcuch F). ISTNIEJE `credit_review` (M-14). PODOBNE katalog konektora bez live HTTP. BRAK `factoring_connector`. BRAK katalogu partnera faktoringowego / SMEO.

## Zakres

How-to: jedno zdanie — to katalog konektora partnera faktoringowego (SMEO); wypłata i live API zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (select dla system_kind, nie radio 469 ani toggle 470) pod jscpd < 3%.

## Poza zakresem

live SMEO HTTP · webhook wypłaty · FK do `sales_invoice` · workflow F7 · sekrety · BR5.1 PO Financing · BR5.2 giełdy

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live SMEO HTTP · webhook / OAuth · FK do `sales_invoice` / `bank_payment` · workflow POD→wypłata · BR5.1 PO Financing · inne marki faktoringu poza `smeo|other`
