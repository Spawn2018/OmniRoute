# Plaster 449.0 · AI3.2 ścieżka obraz vs tekst

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI3.2

**Zależy od:** 0.8 / 447.0 `extraction_draft` · `can_review_extractions`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `extraction`. `payload.extract_path` = `text` | `image`. Domyślnie `text`. Bez nowej tabeli.
- **Wybrane:** POST `/api/v1/extractions` przyjmuje opcjonalne `extract_path`. Ścieżka `image` to **etykieta challengera**, nie wołanie modelu na pikselach. Dokument i tak idzie przez obecny parser do tekstu.
- **Wybrane:** GET i UI `/extractions` pokazują ścieżkę. Operator wybiera przed ekstrakcją. How-to `docs/operator/ekstrakcja.md`.
- **Odrzucone:** live vision / instructor na obrazie (`REJECTED` — brak klucza i pomiaru u siebie).
- **Odrzucone:** zmiana `parser_challenger` A/B (`REJECTED` — to inna para parserów tekstu).
- **Odrzucone:** Excel (`REJECTED` — AI3.4) · golden (`REJECTED` — AI3.3) · zapis `rate_line` z serwisu · float · auto-accept.

**Łowca:** ISTNIEJE `document_base64` + `parser_name` / `parser_challenger`. PODOBNE A/B długości tekstu. BRAK `extract_path` text|image. `just dup` bez wyodrębnienia.

## Zakres

OpenFGA to samo `can_review_extractions`. `SHIPPED["449.0"]` → `/extractions`. Bez +1 OPS_JOB.

## Poza zakresem

AI3.3 golden · AI3.4 Excel · live obraz · cytat preprintu jako fakt Omni

## Kryteria akceptacji

- [ ] POST bez pola → `extract_path=text`; POST `image` zapisuje etykietę
- [ ] POST `extract_path` spoza allowlisty → 422; extra pole → 422
- [ ] ścieżka `image` nie woła extractora na bajtach obrazu (ten sam parser tekstu)
- [ ] UI pozwala wybrać text/image przed ekstrakcją
- [ ] brak `float`; craft_style funkcje>40: 0

## Leftover

live vision · AI3.3–3.4 · PATCH quote/rfp · historia szkicu

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
