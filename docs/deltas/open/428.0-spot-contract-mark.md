# Plaster 428.0 · EXP1 `spot_contract_mark` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `spot_contract_mark` — `mark_code` + `deal_kind` `spot|contract|other` + `source_ref`. EXP1 quotation `spot_or_contract`.
- **Odrzucone:** `temp_control_mark` — duplikat `reefer_mark` (już `setpoint|genset`).
- **Odrzucone:** `cargo_value` — kwota/waluta.
- **Odrzucone:** `mqc_teu` — pokryte `mqc_mark`; nie tu.

## Zakres

HITL `spot_contract_mark`: mark_code + deal_kind spot|contract|other + source_ref.
Migracja `338` (revises `337_diversion_mark`). UI `/spot-contract-marks`.
OpenFGA `can_manage_spot_contract_marks` w `_organization_exp1_shipment_catalog_relations` (albo nowy helper ≤40 linii).

## Poza zakresem

kolumna na quotation · matching SQL · marża · MQC egzekucja

## Kryteria

- [ ] POST z kodem, deal_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/margin/score → 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/spot-contract-marks`
- [ ] OPS_JOBS length 245; SHIPPED `"428.0"`
- [ ] craft_style funkcje>40: 0 (OpenFGA — nie puchnij jednej funkcji)

## Leftover

kolumna spot_or_contract na quotation · bid_decision
