# Plaster 399.0 · Mob `mobile_client_mark` HITL (katalog, bez Expo)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Mob · pin → Mob po EXP
**Status:** Delta zaakceptowana `/noc` 2026-09-12. Wolno `/plaster`. Nie Expo. Nie EAS.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki Mob — tylko HITL katalog `mobile_client_mark` jako dane. `mark_code` + `client_kind` (`ios` | `android` | `ota` | `other`) + `source_ref`. UI `/mobile-client-marks`. OpenFGA `can_manage_mobile_client_marks`. Migracja `310_mobile_client_mark`. Job OPS. Deny-list extract. Expo/EAS live = leftover.

**Łowca:** BRAK tabeli `mobile_client_mark`. ISTNIEJE `idp_connector` — Auth0, nie mobile. ISTNIEJE Fala X — portale web, nie Expo.

**Odrzucone:** Expo app · EAS OTA live · store submit · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `310_mobile_client_mark` RLS FORCE.
2. POST/GET `/mobile-client-marks`. OpenFGA. Deny-list.
3. UI `/mobile-client-marks`. How-to. `SHIPPED_CHARGE_ROUTES["399.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, client_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/expo → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] `/mobile-client-marks` w REQUIRED_BUSINESS_LIST_ROUTES
