# Plaster 269.0 · T8 `terminal_slot_connector` HITL (capability + godziny N4)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala T wiersz T8 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) N4 · [incoterms-booking-customs-ux.md](../../analysis/incoterms-booking-customs-ux.md) (capability `mode`, nie gwarancja)
**Zależy od:** 0.3 tenancy · 268.0 `erp_connector` (wzorzec katalogu HITL) · 4.2 `terminal` (kod jako dana, nie FK)
**Status:** Delta zaakceptowana `/noc` 2026-09-10. Wolno `/plaster`. Nie live T8 HTTP. Nie Selenium. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover T8: slot capability + godziny terminalu N4; nie `terminal_appointment`):** leftover live T8 API / Navis N4 / PCS HTTP parked (brak sekretu i testu live). leftover Selenium / bot na portal parked. leftover `terminal_appointment` (`requested`/`confirmed`/`rejected`) parked — `confirmed` tylko z przyszłego adaptera API, nie z formularza. leftover slot optimizer / yard / D3b drzwi parked. leftover G2.22 P/lista parked. leftover S53 Auth0 / portale parked. Żywy plaster: **nowa tabela** `terminal_slot_connector` per tenant — HITL katalog capability slotu i godzin otwarcia jako dane, nie rezerwacja i nie gwarancja prawna. GLOSSARY: **konektor slotu** = `terminal_slot_connector` (nie `slot_capability`, nie `terminal_hours`, nie `dock_appointment`). Wiersz = `connector_code` (snake 2–32) + `terminal_code` (snake 2–32, kod terminalu jako dana, **bez FK** do `terminal` / geography) + `mode` (`api` / `email_hitl` / `portal_task` / `unsupported`) + `opens_local` + `closes_local` + `cutoff_local` (TIME, N4 jako wpis operatora, nie countdown) + `source_ref` (`tenant:manual` albo `fixture://terminal-slot-connector/…`). Kolumna `confirmed` **nie istnieje**. Create `extra=forbid` — body z `confirmed` → 422, nic nie zapisuje. Operator nie ma checkboxa. Serwis nie woła HTTP, nie woła Selenium, nie woła LLM, nie importuje `dock_appointments` / `geography` / `shipments` / `charges` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły `terminal_code` → 400 (`terminal`). Zły `mode` → 400 (`tryb`). Złe godziny → 400 (`godziny`). Zły cutoff → 400 (`odcięcie`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, connector_code)`, `(organization_id, terminal_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/terminal-slot-connectors`. `SHIPPED_CHARGE_ROUTES["269.0"]` = `/terminal-slot-connectors`. OpenFGA: `can_manage_terminal_slot_connectors`. Migracja `202_terminal_slot_connector` (po `201_erp_connector`). Job OPS 115. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `terminal_slot_connector`. BRAK `terminal_appointment`. BRAK `slot_capability` / `terminal_hours` w `backend/` i `frontend/src`. ISTNIEJE nazwa w GLOSSARY (`terminal_slot_connector`, `terminal_appointment`) — **nie** w kodzie. ISTNIEJE `dock_appointment` 157.0 — okno TIME na `stop` magazynu (`postal_zone`/`address`), **nie** capability terminalu; leftover D3b/T8 w docs-debt; **nie rozszerzać**, nie import. ISTNIEJE `terminal` 4.2 — katalog ISPS/WPI, **bez** godzin otwarcia; kod tu jest daną, nie FK. ISTNIEJE `organization_calendar` U4 — dzień roboczy/święto kraju, **nie** godziny bramy terminalu. ISTNIEJE `groupage_line.cutoff_local` — odcięcie linii drobnicy, **nie** N4 terminalu. ISTNIEJE `erp_connector` 268.0 / `telematics_connector` 197.0 — katalog adaptera HITL, **inny** job (ERP / GPS). ISTNIEJE `container.pickup_terminal` / `return_terminal` — tekst na kontenerze, **nie** capability. PODOBNE: TIME jak D3 i D1, INNY agregat. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- live T8 HTTP / Navis N4 / PCS / poll.
- Selenium / bot na portal.
- `terminal_appointment` + status `confirmed` z formularza / checkbox HITL.
- klon `dock_appointment` (D3) / yard optimizer / G15.
- kolumny godzin na `terminal` (geography) albo na `organization_calendar`.
- FK do `terminal` / `stop` / `shipment`.
- Auth0 / portale / G2.22 P/lista.
- import dock_appointments / terminals / shipments / charges.
- kwota / marża / float / T-SQL / scrape / LLM / gwarancja prawna slotu.

## Zakres

1. Migracja `202_terminal_slot_connector`: RLS FORCE, izolacja, unique kod + terminal + `source_ref`, CHECK `mode IN ('api','email_hitl','portal_task','unsupported')`. Zero kolumny `confirmed` / kwoty / URL / sekretu.
2. POST/GET. Zły token → 400 (`oznaczenie` / `terminal` / `tryb` / `godziny` / `odcięcie` / `obce`). Body z `confirmed` → 422 (pole nie istnieje). Serwis nie importuje dock_appointments/geography/shipments/charges i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/terminal-slot-connectors`. How-to `docs/operator/konektor-slotu.md`; fold w `katalogi.md` (40 linii). Job OPS 115. OpenFGA `can_manage_terminal_slot_connectors`. Nazwy: `TerminalSlotConnectorDesk` / `terminal-slot-connector-form.tsx` / `persistTerminalSlotConnector`. Brak przycisku „Potwierdź”.

## Poza zakresem

live T8 API · Selenium · `confirmed` z POST · `terminal_appointment` · D3 dock · yard · Auth0 · portale · G2.22 · kwota · float · gwarancja slotu

## Kryteria akceptacji

- [ ] POST zapisuje `terminal_slot_connector` z kodem, `terminal_code`, `mode`, godzinami N4 i `source_ref`, bez HTTP
- [ ] zły kod → 400 (`oznaczenie`); zły terminal → 400 (`terminal`); zły mode → 400 (`tryb`); złe godziny → 400 (`godziny`); zły cutoff → 400 (`odcięcie`); obce `source_ref` → 400 (`obce`)
- [ ] POST z `confirmed=true` w body **nie** zapisuje potwierdzenia (422 albo pole nie istnieje); odpowiedź bez `confirmed`
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] Zero float / marży / live HTTP / Selenium / importu dock_appointments/geography/shipments/charges

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique) |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | | `just dup` na diffie |
| Proza operatora | 5–15 zdań | `konektor-slotu.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | | CURRENT, PLAN T8, GLOSSARY, MODULES T8, spec |
| Pushy do zielonego | | |
| Minuty plastra | N/A | — |
| Bench rzemiosła | | `factory_cycle.py --close` |
