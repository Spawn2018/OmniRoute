# Plaster 23.0 · M-30 Zapytania do agentów/armatorów (`carrier_inquiry` przy `quotation`)

**Spec źródłowa:** [docs/spec/carrier-inquiry.md](../../spec/carrier-inquiry.md)  
**Zależy od:** 13.0 `channel_quote` · 2.0/5.1 `quotation` · 18.0 resolve na lane

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-30):** zapytanie do armatora **to dziś wiersz `channel_quote`**. Panel na `/quotations` pokazuje katalog 13.0 dopasowany do lane wyceny. Token `carrier_inquiry` (UI, nie tabela). Żywy ID **M-30**. 18.0 zostaje resolve jednej oferty; 23.0 = ślad listy.

**Łowca:** BRAK `carrier_inquiry` / RFQ outbound / wysyłki HTTP. ISTNIEJE: `fetchChannelQuotes` + `quotationLanes` + 18.0 `resolveChannelQuote`. PODOBNE: 21.0 ślad per party.

**Odrzucone:**

- Tabela RFQ / job wysyłki — zgadywanie schematu (CURRENT); Temporal parked (M-02).
- Live HTTP armatora — leftover 13.0.
- IMAP / M-32.
- Odejmowanie `quotation.amount − channel.amount` — leftover 18.0; M-31 porównanie.
- Import `channel_quotes` z `quotations` service.

## Zakres

Panel na `/quotations`. `data-carrier-inquiry="trail"`. Brak migracji. Brak POST RFQ.

## Poza zakresem

Wysyłka · sekrety · M-31 silnik · ExtractionService · spread w JS

## Ustalenia

- Token: `carrier_inquiry` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_rate_lines` na liście `channel_quote`.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["23.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/quotations` ma `data-carrier-inquiry="trail"` i woła `fetchChannelQuotes`
- [ ] Kwota kanału przez `<Money/>`; brak odejmowania od wyceny
- [ ] Brak nowej migracji i brak live HTTP
- [ ] `just dup` ≤ 3%

## Knowledge

Nie RAG. Wzorzec = 13.0 katalog + 18.0 lane.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Wysyłka i tabela RFQ świadomie leftover.
