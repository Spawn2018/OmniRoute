# Plaster 414.0 · EXP3.0c `po_sku_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP3 (PO line SKU); kolejka pin 2026-09-08c
**Zależy od:** 413.0 `po_plant_mark` (wzorzec HITL; bez współdzielenia tabeli)

## Zakres

HITL `po_sku_mark`: mark_code + sku_kind sku|gtin|customer_sku|other + source_ref.
Migracja `324`. UI `/po-sku-marks`. OpenFGA `can_manage_po_sku_marks`.

Wybrane: osobny katalog SKU na linii PO (węższa pozycja z kolejki EXP3.0c).
Odrzucone: rozszerzenie `po_plant_mark` o kolejne kindy; kolumna SKU na `po_line`; live EDI.
Dlaczego: CURRENT i PLAN wskazują osobny obiekt HITL; plant już ma własny katalog.

## Poza zakresem

live EDI · auto shipment · qty/kwota · FK do `purchase_order` / `po_line` · WMS

## Ustalenia

Append-only INSERT. Bez UPDATE/DELETE wiersza. Bez float/marży. Bez HTTP zewnętrznego.

## Kryteria akceptacji

- [ ] POST z kodem, sku_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/qty → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0

## Leftover

live EDI · auto shipment · qty na linii PO
