# Plaster 163.0 · P1 `rate_card`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P1 · [karty-pol-fala-p.md](../../analysis/karty-pol-fala-p.md) § P1
**Zależy od:** 1.2 `charge` (marża zostaje) · 12.0 `port_surcharge` już jest (inny job)
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Nie silnik WHEN/IF. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z P1):** jedna tabela `rate_card` per tenant — **warunek jako dane + kwota Decimal**, nie matching SQL i nie T-SQL. Wiersz = `card_code` (snake 2–32) + `applies_when` (1–512, tekst warunku, nie AST) + `amount` (`Numeric(14,4)` > 0) + `currency` (`CHAR(3)`) + `source_ref` (`tenant:manual` albo `fixture://rate-card/…`). Zły `card_code` → 400 (detail zawiera `kod`). Pusty `applies_when` → 400 (detail zawiera `warunek`). `amount` ≤ 0 / float / bool → 400 (detail zawiera `kwota`). GET listy SQL. Serwis karty nie importuje port_surcharges / rate_lines / charges / groupage_tariffs. UI `/rate-cards`. `SHIPPED_CHARGE_ROUTES["163.0"]` = `/rate-cards`. OpenFGA: `can_manage_rate_cards`. Migracja `103_rate_card`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `port_surcharge` + 69.0 matching `applies_when` — extra **portowe**; **nie** rozszerzamy o kartę stawek. ISTNIEJE `rate_line` — niemutowalna stawka kupna na wycenie; **nie** rozszerzamy (inna oś). ISTNIEJE `groupage_tariff` — próg wagi na strefie, leftover P1 w D5; **nie** rozszerzamy. ISTNIEJE `charge` — jedyna marża. BRAK `rate_card`. BRAK `/rate-cards`.

**Odrzucone:**

- Matching GET / równość SQL jak 69.0 (leftover P1b).
- WHEN/IF/CALC/MIN/MAX parser (leftover P1c).
- Daty ważności / exclusion (leftover P1d / P2).
- FSC / indeks BAF (P3).
- Kolumny na `port_surcharge` / `rate_line` / `groupage_tariff`.
- Zapis do `charge` / float / T-SQL.

## Zakres

1. Migracja `103_rate_card`: RLS, izolacja, indeks `(organization_id, card_code)`.
2. POST/GET. Zły kod / warunek / kwota / `source_ref` → 400.
3. UI `/rate-cards`. `just api-types`. How-to `docs/operator/karta-stawek.md`.

## Poza zakresem

P1b matching SQL · P1c CALC · P1d ważność · P2 szablony · P3 FSC · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `rate_card` z `applies_when` i kwotą Decimal, bez matching
- [ ] zły `card_code` → 400
- [ ] GET zwraca karty tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / parsera WHEN
