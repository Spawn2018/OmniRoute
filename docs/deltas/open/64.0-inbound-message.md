# Plaster 64.0 · S1 inbound_message (M-32)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S1  
**Zależy od:** 25.0 `/mail` tablica adresów · `party_email_domain` · leftover „tabela skrzynki”

**Status:** Delta zaakceptowana `/noc` 2026-09-02. Nie Graph. Nie IMAP. Nie send. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** jedna tabela `inbound_message` per tenant: treść + `source_ref` + status `draft`. Zapis z **fixture** (operator wkleja / test wstawia), nie z sieci. Ekran: ten sam `/mail` — lista wiadomości obok znanych adresów. OpenFGA `can_manage_inbound_messages` = member.

**Łowca:** ISTNIEJE: `/mail` odczyt kontaktów i `resolve_email`. PODOBNE: `extraction_draft` (status + `source_ref`). BRAK: tabela skrzynki; IMAP; Graph.

**Odrzucone:**

- Graph / IMAP / send (S15 / S17 / S18).
- Podpięcie `resolve_email` na wiersz (S2).
- Treść → extract (S3).
- `shipment`, outbox (M-02 parked).
- F9.1.

## Zakres

1. Migracja `inbound_message`: `organization_id`, RLS FORCE, `source_ref`, `from_address`, `subject`, `body_text`, `status` (`draft`), timestamps.
2. Model → repo → serwis → `GET/POST /inbound-messages`.
3. Test izolacji + fixture bez sieci.
4. `/mail`: lista i formularz zapisu fixture.

## Poza zakresem

S2 `resolve_email` na wiadomości · S3 extract z maila · IMAP · Graph · send · `party_id` na wierszu · załączniki blob · outbox

## Ustalenia

- EN: `inbound_message`. PL: wiadomość przychodząca. GLOSSARY dopisany w tym plasterze.
- Fixture `source_ref` jak `synth://` / `fixture://inbound-mail/...`.
- Lookup GUS nie dotyczy. HITL extract bez zmian.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

`inbound_message` (nowa). Brak FK do `party` w S1.

## Kryteria akceptacji

- [ ] Tabela z `organization_id`, RLS FORCE, test izolacji
- [ ] INSERT wymaga `source_ref`; status `draft`
- [ ] Endpoint z `can_manage_inbound_messages`; brak uprawnienia = 403
- [ ] `/mail` pokazuje listę i zapis fixture; zero HTTP do Graph/IMAP
- [ ] GLOSSARY: `inbound_message`

## Pytania

Brak. `/noc` akceptuje deltę.
