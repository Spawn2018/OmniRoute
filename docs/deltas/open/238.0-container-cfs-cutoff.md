# Plaster 238.0 · T3 `cfs_cutoff_at` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [237.0](../archived/237.0-container-cy-cutoff.md) leftover `cfs_cutoff_at` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 237.0 `cy_cutoff_at`
**Status:** Otwarty `/noc` 2026-09-10. Nie live HTTP. Nie VGM. Nie PIN. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `cfs_cutoff_at`/VGM/PIN/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked (`vgm_kg` / `vgm_method` / `vgm_cutoff_at` razem). leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (Decimal). leftover `last_survey_at` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `cfs_cutoff_at` (`timestamptz`, ISO-8601 ze strefą, dana HITL cutoffu CFS). POST bez pola zostaje `null`. Zła wartość → 400 (`cfs`). `require_cfs_cutoff_at` deleguje do `require_si_cutoff_at` w `app.domain.container` — nie import `stop` / `require_eta_*`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["238.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `177_container_cfs_cutoff` (po `176_container_cy_cutoff`). `_WriteBox` — dodać `cfs` obok `cy`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.si_cutoff_at` / `ams_cutoff_at` / `cy_cutoff_at` (235–237, inny cutoff, ten sam BC — reuse parse). ISTNIEJE `stop.eta_physical` / `eta_legal` (194.0, inny BC). ISTNIEJE `groupage_line.cutoff_local` (155.0, inny obiekt). BRAK `container.cfs_cutoff_at`. PODOBNE: parse ISO ze strefą. Nie kopiować importem z `stop`. Nie live HTTP. Nie VGM w tym plasterze.

**Odrzucone:**

- Live HTTP / scrape cutoffu z portalu.
- `vgm_kg` / `vgm_method` / `vgm_cutoff_at` (następny pakiet).
- PIN / `booking_no` / FK / import `stop`.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `177_container_cfs_cutoff` (po `176_container_cy_cutoff`): `cfs_cutoff_at` nullable `DateTime(timezone=True)`.
2. POST opcjonalny `cfs_cutoff_at`. Zła wartość → 400. Serwis nie importuje stops/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

VGM · PIN · booking · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown

## Kryteria akceptacji

- [ ] POST z ISO `cfs_cutoff_at` zapisuje czas, bez live HTTP i bez PIN
- [ ] POST bez pola zostaje `null`
- [ ] zły / bez strefy → 400 (`cfs`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape / import `stop`
