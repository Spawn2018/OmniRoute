# Plaster 456.0 · BR6.0 okazja CRM

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 / E Fala BR · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR6.0 · leftover G1 `323.0`

**Zależy od:** tenancy RLS · wzorzec HITL `crm_lead` (G1)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `crm_opportunities`. Tabela `crm_opportunity`: `opportunity_code` + `stage_kind` `open|won|lost|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/crm-opportunities`. OpenFGA `can_manage_crm_opportunities` (member). UI `/crm-opportunities`. `SHIPPED["456.0"]` → `/crm-opportunities`.
- **Wybrane:** Alembic `361_crm_opportunity` (`down_revision = "360_route_plan_mk"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `crm_lead` o szansę (`REJECTED` — G1 leftover nazywa osobny obiekt).
- **Odrzucone:** klej FK do `crm_lead` / `party` / `quotation` (`REJECTED` na tym wierszu).
- **Odrzucone:** activity · pipeline silnik · cold-send · dedup NIP · kwota / marża / float.
- **Odrzucone:** klej do `funnel_mark` (EXP4.14 lejek X7, nie okazja).
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind · auto-award.

**Łowca:** ISTNIEJE `crm_lead` (G1 lead + stage). ISTNIEJE `funnel_mark` (X7 lejek). PODOBNE katalog HITL. BRAK `crm_opportunity`. BRAK activity.

## Zakres

How-to: jedno zdanie — to katalog okazji; pipeline i activity zostają leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

activity · pipeline silnik · `crm_lead` kolumny · Valhalla · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

activity · pipeline · FK do leada/party · dedup NIP · cold-send · BR6.1 korytarz

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
