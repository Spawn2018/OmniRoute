# Plaster 597.0 · T2 leftover — HITL `resource.phone`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [596.0](../archived/596.0-resource-tail-lift.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`phone`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-20. Nie karta kierowcy. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `tail_lift`. Opcjonalny `phone` na `resource` (`String(64)`, tekst HITL, nie float). Szerokość jak `party_contact.phone`, ale bez FK i bez zapisu kontaktu. POST bez pola → `null`. Pusty string → `null`. Tekst dłuższy niż 64 → 400. Zmiana numeru przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / inwentarz / kg / LDM / m³ / ADR / chłodnia / winda). Nie SMS. Nie `party_contact`.
- **Wybrane:** UI `/fleet`. `SHIPPED["597.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `473_resource_phone` (po `472_resource_tail_lift`). Bez nowej tabeli.
- **Odrzucone:** `driver_card_no` (`REJECTED`). HW (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.phone`. ISTNIEJE `party_contact.phone` (inny BC, nie reuse). PODOBNE: `inventory_no` + supersede.

## Zakres

1. Migracja `473_resource_phone`.
2. Domain `require_phone` + serwis + API + GET.
3. UI `/fleet` pole telefonu. How-to. `SHIPPED["597.0"]`.

## Poza zakresem

karta kierowcy · SMS · `party_contact` · HW · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `phone` zapisuje tekst
- [ ] pusty string → `null`; >64 → 400
- [ ] zmiana numeru → supersede
- [ ] izolacja RLS (tabela `resource`)
- [ ] zero float / import charges

## Leftover

karta kierowcy · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
