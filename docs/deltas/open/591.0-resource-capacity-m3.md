# Plaster 591.0 · T2 leftover — HITL `resource.capacity_m3`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [590.0](../archived/590.0-resource-capacity-ldm.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2

**Zależy od:** 151.0 `resource` · 590.0 `capacity_ldm`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie `document_expiries`. Nie floating trailer. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po LDM. Opcjonalny `capacity_m3` na `resource` (`Numeric(14,4)`, Decimal HITL metrów sześciennych, > 0, nie float, nie waluta, nie `charge`). POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`m3`). Zmiana m³ przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z kg/LDM/nr). Nie liczy z LDM.
- **Wybrane:** UI `/fleet`. `SHIPPED["591.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `467_resource_capacity_m3` (po `466_resource_capacity_ldm`).
- **Odrzucone:** `document_expiries` / floating / HW (`REJECTED`). float (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.capacity_m3`. ISTNIEJE `capacity_ldm` (590.0 — reuse `_require_positive_decimal`, komunikat `m3`). ISTNIEJE `container.volume_m3` (inny BC). PODOBNE: opcjonalny Decimal + supersede.

## Zakres

1. Migracja `467_resource_capacity_m3`.
2. Domain `require_capacity_m3` + serwis + API + GET.
3. UI `/fleet` pole m³. How-to. `SHIPPED["591.0"]`.

## Poza zakresem

`document_expiries` · floating · HW · UXCL · Job#2 · N3 · kwota · live HTTP · kalkulator z LDM

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `capacity_m3="90"` zapisuje Decimal
- [ ] float / ≤0 → 400 (`m3`)
- [ ] zmiana m³ → supersede
- [ ] izolacja RLS
- [ ] zero float / import charges

## Leftover

`document_expiries` · floating trailer · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
