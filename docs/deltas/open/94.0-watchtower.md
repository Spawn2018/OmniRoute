# Plaster 94.0 · S32 Watchtower (lista + S11 + lazy mapa)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S32  
**Zależy od:** 93.0 `operational_exception` · 74.0 S11 `operator_decision` · 0.7 HITL `/ai` · ADR-0003

**Status:** Zaakceptowany `/noc` 2026-09-03. Nie nowy M-xx. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** jeden ekran `/watchtower` składa już istniejące joby: lista `operational_exception` + pending `operator_decision` (S11: Akceptuj/Odrzuć przez istniejące API). Canvas 06 HITL = link do `/ai`, nie drugi extract. Mapa = **leniwy** chunk bez leaflet/mapbox (panel „mapa poza paczką początkową”), żeby nie weszła do initial JS (ADR-0003). Zero nowej tabeli. Zero AIS.

**Łowca:** BRAK `/watchtower` w routes/sidebar. ISTNIEJE `/exceptions` (93.0 zapis), `/decisions` (S11), `/ai` (HITL), `decideOperatorDecision`. PODOBNE: tablice-składanki finance; nie duplikować BC. Canvas `docs/design/ui-06-vision.canvas.tsx` to makieta — nie kopiować HTML do `frontend/`.

**Odrzucone:**

- leaflet / mapbox / kafelki / AIS / project44 w paczce początkowej albo w tym plasterze.
- Nowa tabela / nowy M-xx.
- ETA liczona w JS/Python/LLM.
- Kopiowanie canvas 06 do SPA.
- Drugi silnik decyzji obok S11.
- F9.1.

## Zakres

1. Trasa `/watchtower`, nav, `BUSINESS_LISTS.watchtower`, `SHIPPED_CHARGE_ROUTES["94.0"]` = `/watchtower`. `data-watchtower="board"`.
2. Lista wyjątków (`fetchOperationalExceptions`) + pending decyzji (`fetchOperatorDecisions` status `pending`) + Akceptuj/Odrzuć (`decideOperatorDecision`). Link HITL do `/ai`.
3. Leniwy panel mapy (`React.lazy`) **bez** `leaflet` / `mapbox`. Asercja: strona i panel bez tych stringów; `just perf` (initial JS) zostaje.
4. How-to `docs/operator/wieza.md`. Brak migracji.

## Poza zakresem

S33 EDI · kafelki mapy · AIS · ETA · nowy BC · F9.1

## Ustalenia

- Token UI: `watchtower`. How-to: `wieza.md`.
- Uprawnienia: istniejące `can_manage_exceptions` + `can_manage_operator_decisions` (front woła te same endpointy).
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL extract bez zmian.

## Schemat

Brak nowej tabeli.

## Kryteria akceptacji

- [ ] `/watchtower` składa wyjątki i pending S11; Akceptuj/Odrzuć woła istniejące API
- [ ] Brak `leaflet` / `mapbox` / `eta` / `ais` na stronie i w panelu mapy
- [ ] Mapa wchodzi tylko przez `React.lazy` (nie static import w stronie)
- [ ] `SHIPPED_CHARGE_ROUTES["94.0"]` = `/watchtower`
- [ ] `just perf` (limit initial JS) bez regresji

## Pytania

Brak. `/noc` akceptuje deltę.
