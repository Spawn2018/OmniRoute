# Plaster 412.0 · Demo-1b `demo_sim_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `demo_sim_mark` per tenant: `mark_code` + `sim_kind`
`fleet_150`|`months_10`|`other` + `source_ref` (`tenant:manual` | `fixture://demo-sim-mark/…`).

Klon wzorca `demo_wipe_mark`:
- migracja `322` (revises `321_demo_wipe_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_demo_sim_marks`
- UI `/demo-sim-marks`, business-lists + ops-index `SHIPPED["412.0"]`
- operator `docs/operator/demo-sim-mark.md`
- izolacja RLS; POST `amount`/`vehicles`/`score` → 422

Znacznik demo_sim (150 aut / 10 mies.) jako dana. Bez live symulacji floty.

## Poza zakresem

live demo_sim · 150 aut generator · wipe live · GPS live · PDF umów

## Kryteria

- [ ] POST z kodem, sim_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/vehicles → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live demo_sim 150 aut · 10 mies. seed · flota klienta na sali
