# Plaster 441.0 · AI1.4 leftover HITL `outcome_kind` (słownik otwarty)

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 436.0 `suggestion_kind` (wzorzec) · 433.0 `outcome_ledger` (CHECK zostaje)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `outcome_kind` per tenant — append-only HITL. Wiersz = `kind_code` (snake 2–32, **bez** listy CHECK/ENUM) + `source_ref` (`tenant:manual` albo `fixture://outcome-kind/…`). Nowy rodzaj wyniku = INSERT, nie migracja (VISION B.3).
- **Wybrane:** czwarty słownik osi AI1.4 po `suggestion_kind` / `twin_kind` / `autonomy_level`. `data_source` zostaje w AI5 (licencja).
- **Odrzucone:** zmiana CHECK na `outcome_ledger.outcome_kind` / FK — leftover osobnego plastra (wzorzec 439.0).
- **Odrzucone:** `data_source` · CRPS (AI2) · silnik L3+ · import `outcome_ledgers` z tego BC.
- **Odrzucone:** seed globalny · UPDATE/DELETE · float / kwota.

**Łowca:** PODOBNE `suggestion_kind` (436.0). PODOBNE CHECK na `outcome_ledger` (433.0). BRAK tabeli `outcome_kind`. Nie klon ledgeru.

## Zakres

HITL `outcome_kind`. Migracja `351` (revises `350_twin_mark_kind_fk`). UI `/outcome-kinds`.
OpenFGA `can_manage_outcome_kinds`. `SHIPPED["441.0"]`. OPS_JOBS length 256.

## Poza zakresem

FK z `outcome_ledger` · zdjęcie CHECK · data_source · CRPS · trzy BC autosave

## Kryteria akceptacji

- [ ] POST z kind_code (dowolny snake 2–32, np. tender) + source_ref
- [ ] extra amount/margin/score → 422; zły kod / source_ref → 400
- [ ] brak CHECK listy `eta|rate|route` w migracji 351; izolacja RLS; REQUIRED `/outcome-kinds`
- [ ] OPS_JOBS length 256; SHIPPED `"441.0"`
- [ ] craft_style funkcje>40: 0

## Leftover

FK `outcome_ledger` → `outcome_kind` · `data_source` (AI5) · trzy BC · AI4.1 · CRPS (AI2)
