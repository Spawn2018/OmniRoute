# Plaster 55.0 · U-i18n-structure (`ui_message`)

**Spec źródłowa:** [docs/spec/i18n-structure.md](../../spec/i18n-structure.md)  
**Zależy od:** ADR-0003 §7 + trasy `/quality` `/rollout`

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. Nie drugi język. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN leftover U-i18n-structure):** warstwa kluczy `t()` + katalog `pl` w `frontend/src/lib/i18n.ts`. Token `ui_message`. Żywy ID leftover **U-i18n-structure**, plaster **55.0**. Gate leftover: hardcoded string na **nowym** ekranie — nagłówki `/quality` i `/rollout` przez `t()`. Format dat: `formatInstant` + `Intl.DateTimeFormat("pl-PL")`. Kwoty zostają na osi 52.0.

**Łowca:** BRAK katalogu i18n / `t()` / i18next / `toLocaleDateString`. ISTNIEJE: hardcoded `CatalogHeading` na quality i rollout; `<Money/>` bez grouping. PODOBNE: `CatalogHeading` zostaje — dostaje string z `t()`, nie drugi komponent.

**Odrzucone:**

- i18next / formatjs (paczka, drugi runtime).
- Drugi język EN.
- Migracja wszystkich katalogów.
- `toLocaleString` / `Number` na kwocie (52.0 leftover grouping).
- Nowa tabela / nowa trasa.

## Zakres

`frontend/src/lib/i18n.ts`: katalog `pl`, `t(key)`, `formatInstant`. Nagłówki quality i rollout. Test. Zero migracji. Zero nowej trasy. `SHIPPED_CHARGE_ROUTES` bez 55.0.

## Poza zakresem

U-playwright-axe · U-print · EN · grouping `<Money/>`

## Ustalenia

- Token: `ui_message` (GLOSSARY, klucz w katalogu).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `t("extraction_quality.title")` zwraca polski napis z katalogu
- [ ] Brak klucza pali test
- [ ] `/quality` i `/rollout` nie mają hardcoded tytułu w JSX (jest `t(`)
- [ ] `formatInstant` formatuje ISO przez `pl-PL`; `i18n.ts` bez `parseFloat` / `Number(`
- [ ] Brak i18next w `package.json`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | katalog stringów |
| Dług w diffie | leftover | Playwright axe |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. U-playwright-axe leftover.
