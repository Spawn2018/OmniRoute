# Plaster 52.0 · U-money-align (`money_axis`)

**Spec źródłowa:** [docs/spec/money-align.md](../../spec/money-align.md)  
**Zależy od:** 0.25 `<Money/>` + `parseMoney`

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. LLM nie liczy. Nie `parseFloat`.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN § Wave FE leftover U-money-align):** `<Money/>` **już istnieje**. Operator widzi kwotę w siatce integer / ułamek / kod ISO (ADR-0003 §4). Token `money_axis` (UI, nie tabela). Żywy ID leftover **U-money-align**, plaster **52.0**. Split tekstu z `parseMoney` (BigInt), nie `Number`.

**Łowca:** ISTNIEJE: `formatMoney`, `<Money/>` z `tabular-nums` (jeden span). BRAK osi dziesiętnej (siate integer/fraction). PODOBNE: `index.css` `.tabular-nums`. Postgres: bez zmian.

**Odrzucone:**

- `parseFloat` / `Number` / `toLocaleString` na kwocie.
- Grupowanie locale przez float.
- Nowa tabela / nowa trasa.
- Optymistyczny update kwoty.

## Zakres

`moneyAxis` + siatka w `money.tsx`. `data-money="axis"`. Test w `money.test.ts`. Zero migracji. Zero nowej trasy. `SHIPPED_CHARGE_ROUTES` bez 52.0.

## Poza zakresem

U-condensed · i18n grouping · Playwright

## Ustalenia

- Token: `money_axis` (GLOSSARY, UI).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `<Money/>` ma `data-money="axis"` i trzy części (integer / fraction / currency) dla pary z `parseMoney`
- [ ] Brak `parseFloat` / `Number(` na kwocie w `money.tsx`
- [ ] HITL nie-decimal zostaje jednym spanem (formatMoney raw)
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | render stringa |
| Dług w diffie | leftover | grouping locale |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Grupowanie leftover.
