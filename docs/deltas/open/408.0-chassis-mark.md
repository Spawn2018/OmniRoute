# Plaster 408.0 · EXP4.5b `chassis_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `chassis_mark` per tenant: `mark_code` + `chassis_kind`
`chassis`|`trailer`|`other` + `source_ref` (`tenant:manual` | `fixture://chassis-mark/…`).

Klon wzorca `ocean_feeder_mark`:
- migracja `318` (revises `317_ocean_feeder_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_chassis_marks`
- UI `/chassis-marks`, business-lists + ops-index `SHIPPED["408.0"]`
- operator `docs/operator/chassis-mark.md`
- izolacja RLS; POST `amount`/`teu`/`score` → 422

Obok `empty_depot_mark` — tu chassis/trailer, nie depot live.

## Poza zakresem

live chassis pool · TEU · kwota · yard live · WMS

## Kryteria

- [ ] POST z kodem, chassis_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/teu → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live chassis pool · yard · TEU
