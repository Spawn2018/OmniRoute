# Plaster 437.0 · AI1.4 leftover `twin_kind` HITL (słownik otwarty)

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · C.2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 436.0 `suggestion_kind` (pierwszy słownik DONE) · `twin_mark` (CHECK zostaje)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `twin_kind` per tenant — append-only HITL. Wiersz = `kind_code` (snake 2–32, **bez** listy CHECK/ENUM) + `source_ref` (`tenant:manual` albo `fixture://twin-kind/…`). Nowy rodzaj bliźniaka = INSERT, nie migracja (VISION C.2).
- **Wybrane:** drugi z czterech słowników AI1.4 — C.2 każe przenieść `twin_kind` na tabelę słownikową.
- **Odrzucone:** zmiana CHECK na `twin_mark.twin_kind` / FK z `twin_mark` — leftover osobnego plastra.
- **Odrzucone:** `data_source` (licencja, AI5) · `autonomy_level` (per klient).
- **Odrzucone:** pięć osi taksonomii jako kolumny / CHECK · JSON · UPDATE/DELETE · float / kwota.
- **Odrzucone:** rozszerzenie `twin_mark` — tam zamknięty rodzaj, nie otwarty słownik.

**Łowca:** PODOBNE `twin_mark` (CHECK 8 wartości — nie rozszerzać). PODOBNE `suggestion_kind` (wzorzec otwartego słownika). BRAK tabeli `twin_kind`.

## Zakres

HITL `twin_kind`. Migracja `347` (revises `346_suggestion_kind`). UI `/twin-kinds`.
OpenFGA `can_manage_twin_kinds`. `SHIPPED["437.0"]`. OPS_JOBS length 254.

## Poza zakresem

data_source · autonomy_level · zdjęcie CHECK na twin_mark · FK · CRPS · silnik AI4.1 · fizyka bliźniaka

## Kryteria akceptacji

- [ ] POST z kind_code (dowolny snake 2–32, np. tender) + source_ref
- [ ] extra amount/margin/score → 422; zły kod / source_ref → 400
- [ ] brak CHECK listy 8 rodzajów w migracji; izolacja RLS; REQUIRED `/twin-kinds`
- [ ] OPS_JOBS length 254; SHIPPED `"437.0"`
- [ ] craft_style funkcje>40: 0

## Leftover

data_source · autonomy_level · zdjęcie CHECK na twin_mark i suggestion_ledger + FK · trzy BC piszą · silnik AI4.1 · CRPS (AI2)
