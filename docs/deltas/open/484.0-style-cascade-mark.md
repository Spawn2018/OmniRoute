# Plaster 484.0 · AI8.0 kaskada stylu — katalog HITL

**Status:** Delta zaakceptowana `/noc 8` 2026-09-14 — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.5 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI8.0

**Zależy od:** tenancy RLS · wzorzec HITL mark · nie AI8.1 fidelity · nie twin fizyka

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `style_cascade_marks`. Tabela `style_cascade_mark`: `mark_code` + `cascade_kind` `global|company|department|user|customer|person|context|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z AI8.0 leftover — katalog HITL **poziomów kaskady stylu** (Global → Company → Department → User → Customer → Person-to-Person → Context), nie silnik stylu i nie ocena osoby.
- **Wybrane:** POST/GET `/api/v1/style-cascade-marks`. OpenFGA `can_manage_style_cascade_marks` (member). UI `/style-cascade-marks`. `SHIPPED["484.0"]` → `/style-cascade-marks`.
- **Wybrane:** Alembic `389_style_cascade_mark` (`down_revision = "388_l3_gate_mark"`).
- **Wybrane:** how-to `docs/operator/style-cascade-mark.md` (job operatora: zapis poziomu kaskady bez scoringu).
- **Odrzucone:** `STYLE FIDELITY SCORE` / bramka 85% (`REJECTED` — to AI8.1, osobny wiersz).
- **Odrzucone:** scoring osoby / ocena wyników / Pain×Frequency (`REJECTED` — VISION B.5; bliźniak = styl i relacja).
- **Odrzucone:** silnik generowania stylu / LLM piszący treść maila (`REJECTED` — HC-04; katalog HITL tylko).
- **Odrzucone:** FK do `party` / `app_user` / `twin_mark` (`REJECTED` — poziom to dana katalogowa, nie wiązanie osoby).
- **Odrzucone:** AI5.0 `data_source` (`REJECTED` na tym wierszu — park licencja).

**Łowca:** ISTNIEJE `twin_mark` (rodzaj bliźniaka). ISTNIEJE `l3_gate_mark`. BRAK `style_cascade_mark`.

## Zakres

How-to + OPS_JOB. FE: `cascade_kind` jako pionowe przyciski radio (różne od select 482 / segmented 481), kolejność pól source→kod→kind; model: `cascade_kind` przed `mark_code` (jscpd < 3.0%).

## Poza zakresem

fidelity score · silnik stylu · scoring osoby · twin fizyka · kwota · L3 write · AI5 ingest

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

AI8.1 STYLE FIDELITY SCORE · AI5.0 ingest (park licencja) · auto-zejście jakości L3
