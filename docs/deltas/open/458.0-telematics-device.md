# Plaster 458.0 · BR2.1 urządzenie telematyczne

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR2.1

**Zależy od:** tenancy RLS · wzorzec HITL (V5 `telematics_connector` / BR2.0 `position_event` jako odrębne BC)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `telematics_devices`. Tabela `telematics_device`: `device_code` + `device_kind` `tracker|fault|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/telematics-devices`. OpenFGA `can_manage_telematics_devices` (member). UI `/telematics-devices`. `SHIPPED["458.0"]` → `/telematics-devices`.
- **Wybrane:** Alembic `363_telematics_device` (`down_revision = "362_position_event"`). Id ≤ 32 znaków.
- **Odrzucone:** klej FK do `resource` / `trip` / `telematics_connector` / `position_event` (`REJECTED` — parowanie leftover).
- **Odrzucone:** live GPS poll · GBOX/IKOL HTTP · mapa · silnik awarii.
- **Odrzucone:** rozszerzenie `telematics_connector` o urządzenie (`REJECTED` — V5 to adapter, nie sprzęt).
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind · kwota / float.

**Łowca:** ISTNIEJE `telematics_connector` (V5 adapter, nie sprzęt). ISTNIEJE `position_event` (BR2.0 zdarzenie, nie urządzenie). ISTNIEJE `resource` (flota, nie tracker). PODOBNE katalog HITL. BRAK `telematics_device`. BRAK parowania.

## Zakres

How-to: jedno zdanie — to katalog urządzenia; parowanie z pojazdem i live zostają leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

parowanie · live poll · `position_event` · mapa · AI5 · Valhalla

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

parowanie z `resource` · cykl życia poza kind · silnik awarii · live poll · BR2.2 zgoda

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
