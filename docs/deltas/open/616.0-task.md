# Plaster 616.0 · T5 instancja `task` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T5 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) T5 · [263.0](../archived/263.0-task-template.md)
**Zależy od:** 263.0 `task_template`; 0.3 tenancy
**Status:** Delta zaakceptowana `/noc` 2026-09-21. Wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T5 instancja task, po T7b 615.0):** szablon jest. Matching SQL i outbox zostają. Żywy plaster: **nowa tabela** `task` per tenant — HITL wpis zadania. Wiersz = `task_code` (snake 2–32) + `template_code` (tekst snake, nie FK UUID w tym plasterze) + `status_kind` `open|done|skipped|other` + `source_ref`. Operator wpisuje kod i status; serwis nie ewaluuje `applies_when`, nie tworzy FK do shipment/trip/stop, nie woła M-71, nie woła HTTP. Zły kod → 400 (`zadanie`). Zły status → 400 (`status`). Unique `(organization_id, task_code)` i `(organization_id, source_ref)`. GET lista. UI `/tasks`. `SHIPPED_CHARGE_ROUTES["616.0"]` = `/tasks`. OpenFGA `can_manage_tasks`. Migracja `491_task` (po `490_charge_fx_rate`). RLS FORCE. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `task_template` (263.0). ISTNIEJE `product_ticket` jako wzór wpisu HITL. BRAK tabeli `task`. BRAK matching SQL.

**Odrzucone:**

- matching `applies_when` · FK shipment/trip/stop · assignee · outbox/Temporal
- M-71 done auto · T7c · HW · UXCL · N3 · float · Auth0

## Zakres

1. Migracja `491_task`: tabela + RLS FORCE + izolacja + unique. Nie kolumny na `shipment`.
2. POST/GET. Zły token → 400. Serwis nie importuje shipments/charges/outbox/task_templates (odczyt szablonu nie w tym plasterze).
3. UI `/tasks`. How-to. OpenFGA `can_manage_tasks`.

## Poza zakresem

matching SQL · FK kontekstu · outbox · M-71 · T7c · live HTTP · float

## Kryteria akceptacji

- [ ] POST zapisuje `task` z kodem, `template_code`, `status_kind`, `source_ref`
- [ ] zły kod → 400 (`zadanie`); zły status → 400 (`status`)
- [ ] GET zwraca zadania tenanta; test izolacji nowej tabeli
- [ ] serwis nie importuje shipments / charges / outbox / task_templates
- [ ] Zero float / matchingu / live HTTP

## Leftover

matching SQL · FK shipment/trip/stop · assignee · outbox · M-71 · T7c

## Post-plaster

| Check | Wynik | Dowód |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
