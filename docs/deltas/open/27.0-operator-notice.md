# Plaster 27.0 · M-34 Powiadomienia (`operator_notice` tablica odczytu)

**Spec źródłowa:** [docs/spec/operator-notice.md](../../spec/operator-notice.md)  
**Zależy od:** 0.7 `extraction_draft` pending · 22.0 `offer_acceptance` · 0.6 DataTableShell

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie push/email.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 4 M-34):** powiadomienie **to dziś istniejąca praca do zrobienia**. Ekran `/notifications` składa szkice HITL `pending` i wyceny oczekujące na akceptację. Token `operator_notice` (UI, nie tabela). Żywy ID **M-34**. Kolejka `/extractions` i panel 22.0 zostają; 27.0 = jeden odczyt.

**Łowca:** BRAK `notification` / outbox / push / SMTP. ISTNIEJE: `fetchExtractionDrafts("pending")`, `quotationAcceptancePending`. PODOBNE: 15.0 `/finance`, 25.0 `/mail`.

**Odrzucone:**

- Tabela `notification` / kolejka push — zgadywanie schematu (CURRENT).
- Wysyłka maila / IMAP — leftover 25.0.
- Accept/reject HITL z tego ekranu — HITL zostaje na `/extractions`.
- LLM piszący treść powiadomienia.

## Zakres

Read-only `/notifications`. `data-operator-notice="board"`. Brak migracji. Linki do `/extractions` i `/quotations`.

## Poza zakresem

Tabela zdarzeń · push · SMTP · outbox · Temporal · ExtractionService

## Ustalenia

- Token: `operator_notice` (GLOSSARY, UI).
- OpenFGA: istniejące `can_review_extractions` i `can_manage_quotations`.
- RLS: bez nowej tabeli.
- Job: `/notifications`. `SHIPPED_CHARGE_ROUTES["27.0"]` = `/notifications`. OPS_JOBS +1.
- Szkice HITL: etykieta Art. 50 (system AI, recenzja człowieka).
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/notifications` ma `data-operator-notice="board"` i woła `fetchExtractionDrafts` oraz `quotationAcceptancePending`
- [ ] Brak `imap` / `smtp` / `acceptExtractionDraft` na ekranie; brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["27.0"]` = `/notifications`
- [ ] `just dup` ≤ 3%

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela powiadomień i wysyłka świadomie leftover.
