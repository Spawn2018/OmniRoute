# Plaster 86.0 · S24 accept oferty przez S11 (M-29)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S24  
**Zależy od:** 22.0 ślad `offer_acceptance` · 74.0 `operator_decision` · 2.0 `quotation`

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-29**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** wynik oferty = decyzja S11 na podmiocie `quotation`. Rozszerzyć `operator_decision.subject_kind` o `quotation`. Przyjmij / Odrzuć idzie istniejącym `POST` decyzji, nie nową kolumną won/lost i nie HITL extract. 22.0 pending zostaje odczytem: wycena z `party_id` bez decyzji `accepted`. Etykiety po polsku (`przyjęta` / `odrzucona`) — katalog `/quotations` nadal nie zawiera podciągu `won`.

**Łowca:** ISTNIEJE 22.0 panel pending + 74.0 szyna + `frontend/src/lib/operator-decisions-api.ts` + `has_accepted`. BRAK `subject_kind = quotation`. PODOBNE: HITL accept 1.3 (inny byt). 85.0 wskazanie kanału (inne BC).

**Odrzucone:**

- Tabela `offer_result` / kolumna `won`/`lost` na `quotation`.
- `acceptExtractionDraft` przy ofercie.
- Angielskie `won`/`lost` na `/quotations` (strażnik 22.0).
- CSV (M-27).
- Live HTTP · F9.1.

## Zakres

1. Migracja `045_operator_decision_quotation`: CHECK `subject_kind` o `quotation` (rewizja `ck_operator_decision_subject_kind` z 036).
2. Domena `_SUBJECTS` + ten sam OpenFGA `can_manage_operator_decisions`. Serwis decyzji nie importuje `quotations`.
3. UI `/quotations` w panelu 22.0: „Przyjmij” / „Odrzuć” przez istniejące API decyzji. `SHIPPED_CHARGE_ROUTES["86.0"]` = `/quotations`. `OPS_JOBS` zostaje 49.

## Poza zakresem

CSV · `changed` · accept extractu · mutacja kwoty · F9.1

## Ustalenia

- Token: `offer_acceptance`. How-to: `ścieżka-pieniędzy.md`.
- API składa: znać `quotation.id`, zapisać decyzję. Izolacja zostaje na `operator_decision`.
- `just api-types` jeśli OpenAPI nie zmienia kontraktu — tylko gdy CHECK/enum w schemacie odpowiedzi.

## Schemat

`operator_decision.subject_kind IN ('inbound_message', 'mail_draft', 'quotation')`.

## Kryteria akceptacji

- [ ] `quotation` jako subject; nieznana wycena nie jest wymagana w serwisie decyzji (brak FK)
- [ ] Przyjęta wycena znika z pending 22.0
- [ ] Brak `won` i `acceptExtractionDraft` na `/quotations`
- [ ] Serwis `operator_decisions` bez importu `quotations` / `extraction`

## Pytania

Brak. `/noc` akceptuje deltę.
