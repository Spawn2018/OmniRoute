# Plaster 454.0 · AI4.2 kółka w SQL

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI4.2 · 266.0 `circle_sim` · 453.0 widok powtórki

**Zależy od:** 266.0 `circle_sim` HITL

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `circle_sims`. Widok SQL `circle_sim_pair` — samozłączenie dwóch wierszy HITL tego tenanta, gdy `a.unload_unlocode = b.load_unlocode` i `a.load_unlocode = b.unload_unlocode` oraz `a.id < b.id` (para raz). `WITH (security_invoker = true)`.
- **Wybrane:** GET `/api/v1/circle-sim-pairs` — odczyt widoku, to samo OpenFGA `can_manage_circle_sims`. Bez +1 OPS_JOB. UI zostaje na `/circle-sims` (lista par uzupełniających).
- **Wybrane:** `SHIPPED["454.0"]` → `/circle-sims`. How-to: jedno zdanie — para zamyka się w SQL; silnik 500k i km zostają leftover.
- **Wybrane:** Alembic `359_circle_pair` (`down_revision = "358_cf_run_snap"`). Id ≤ 32 znaków. Down zdejmuje widok.
- **Odrzucone:** pętla w Pythonie po kółkach (`REJECTED` — zasada 11: Postgres z indeksem).
- **Odrzucone:** generator 500k wierszy / VRP / OR-Tools / LLM-VRP (`REJECTED` na tym wierszu — leftover pojemności).
- **Odrzucone:** km / Haversine / `lane_km` / float / kwota / marża / `n_overlap`.
- **Odrzucone:** import `geography` / `lane_kms` / `plan_snapshots` / `what_if_marks` · UPDATE/DELETE · live HTTP · nowy BC.

**Łowca:** ISTNIEJE `circle_sim` HITL kod + para UN/LOCODE (266.0). ISTNIEJE unique pary per tenant. ISTNIEJE wzorzec widoku `security_invoker` (443–445, 453.0). ISTNIEJE `lane_pattern` / `lane_km` (inne BC). PODOBNE samozłączenie jak powtórka 453.0. BRAK widoku pary uzupełniającej. BRAK silnika 500k. BRAK km.

## Zakres

OpenFGA to samo. Widok bez kolumny kwoty / km / float. Test izolacji: A nie widzi par B. Dwa HITL wiersze uzupełniające → jedna para w GET.

## Poza zakresem

silnik 500k · km · VRP · `lane_km` · what-if liczby · mapa

## Kryteria akceptacji

- [ ] migracja `varchar(32)`; down zdejmuje widok
- [ ] dwa uzupełniające kółka tenanta → GET 200 z jedną parą; tenant B nie widzi
- [ ] widok bez amount / km / float; craft_style funkcje>40: 0
- [ ] brak pętli Python po `circle_sim`

## Leftover

silnik 500k wariantów · km ładowny/pusty · `n_overlap` · VRP · EXPLAIN p95 na 500k

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
