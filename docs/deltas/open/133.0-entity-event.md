# Plaster 133.0 · B0a `entity_event` append-only

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § B0a · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § B0a
**Zależy od:** 0.3 RLS · 0.4 OpenFGA
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Nowy obiekt, nie nowy M-xx. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki B0a):** nowa tabela `entity_event` per tenant, wyłącznie INSERT. Kind na tej fali: `inquiry_queued` / `inquiry_sent` / `quote_recorded`. `subject_kind` allowlista: `carrier_inquiry` / `quotation` / `channel_quote`. `source_ref` obowiązkowe. Job na `/entity-events`. Zero FK do innych BC (wskazanie UUID, API nie importuje ich serwisów). Zero JSON payload.

**Łowca:** ISTNIEJE `outbox_event` (79.0) — szyna między BC, inny job. PODOBNE wzorzec RLS + `source_ref`. BRAK ledgeru zdarzeń podmiotu. Nie reuse outbox (konsument parked).

**Odrzucone:**

- `prediction_ledger` / what-if — B0b po T2.
- UPDATE/DELETE, Temporal, worker.
- Import `app.services.quotations` / `carrier_inquiries` / `channel_quotes`.
- JSON payload, kwota, marża.

## Zakres

1. Migracja `076_entity_event`: tabela + RLS FORCE + test izolacji.
2. Serwis `create_event` append-only. OpenFGA `can_manage_entity_events` = member.
3. UI `/entity-events`. `SHIPPED_CHARGE_ROUTES["133.0"]` = `/entity-events`.
4. `just api-types`. How-to w `katalogi.md` albo osobna karta jeśli nie mieści się w 40 liniach.

## Poza zakresem

O1–O8 · B0b · live HTTP · outbox consumer · F9.1

## Kryteria akceptacji

- [ ] INSERT z kind spoza allowlisty → 400
- [ ] Brak `source_ref` → 400
- [ ] Brak UPDATE/DELETE w serwisie
- [ ] Izolacja tenantów
- [ ] `/entity-events` zapisuje
- [ ] `just api-types`
