# Plaster 63.0 · Q-E4 threat model tenant+HITL + CodeQL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka Q-E4  
**Zależy od:** RLS + HITL w kodzie · leftover 58.0 (STRIDE/CodeQL poza tabelą co plaster) · repo **private** na GitHub Free

**Status:** Delta zaakceptowana `/noc` 2026-09-02. Nie nowy M-xx. Nie GHAS. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** karta STRIDE **tylko** na izolację tenanta i ścieżkę HITL (extract → accept → `rate_line`). Workflow CodeQL w `.github/workflows/` (python + JS/TS), **poza** `gate.yml` / `just gate`. Na prywatnym Free Code Scanning wymaga GitHub Code Security — pierwszy run to pokaże; czerwony 403 ≠ DoD.

**Łowca:** ISTNIEJE: RLS FORCE, `can_review_extractions`, ExtractionService bez rates, matryca S1–S6. PODOBNE: `docs/ops/docs-debt.md` odkłada STRIDE/CodeQL. BRAK: karta threat model; workflow `codeql.yml`.

**Odrzucone:**

- CodeQL w `just gate` (lokalnie nie ma CodeQL CLI; GHAS).
- STRIDE 70 modułów.
- Mutmut / vulture.
- Publiczność repo, żeby dostać darmowy CodeQL.
- F9.1, S1, Auth0.

## Zakres

1. `docs/ops/threat-model-tenant-hitl.md` — tabela STRIDE + istniejące mitygacje (RLS, HITL, OpenFGA).
2. `.github/workflows/codeql.yml` — analyze python i javascript-typescript; nie job `gate`.
3. Test: karta i workflow istnieją; karta wymienia `organization_id` i zakaz zapisu z extractu.

## Poza zakresem

Fala S / S1 · F9.1 · zmiana RLS/HITL w produkcie (chyba że test spadnie) · k6 · mixin · 70 stubów

## Ustalenia

- Job operatora: brak. OpenFGA/RLS: bez zmian w kodzie.
- CodeQL **nie** jest required check. Leftover, gdy run = GHAS/403.
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL zostaje.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] Karta STRIDE tenant+HITL w `docs/ops/threat-model-tenant-hitl.md`
- [ ] `.github/workflows/codeql.yml` z `python` i `javascript-typescript`
- [ ] `just gate` / job `gate` **nie** woła CodeQL
- [ ] Po pierwszym runie: sukces albo leftover z powodem GHAS — nie milczenie

## Pytania

Brak. `/noc` akceptuje deltę.
