# Plaster 36.0 · M-43 Koszt pieniądza (`money_cost`)

**Spec źródłowa:** [docs/spec/money-cost.md](../../spec/money-cost.md)  
**Zależy od:** 6.0 `nbp_rate` · 1.2 `charge.buy` · 15.0 `/finance`

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 6 M-43):** koszt pieniądza **nie ma tabeli odsetek**. Operator widzi kurs średni NBP (ustawowy fakt) i `buy_amount` z `charge` (gotówka po stronie kupna) — bez mnożenia. Token `money_cost` (UI, nie tabela). Żywy ID **M-43**. 15.0 zostaje tablicą marży/limitu; 6.0 zostaje katalogiem kursu.

**Łowca:** BRAK `interest` / WACC / `money_cost` tabeli. ISTNIEJE: `fetchNbpRates`, `fetchCharges`, `buy_amount`, `<Money/>`, `/nbp-rates`, `/finance`. PODOBNE: 15.0 pokazuje `margin` + NBP; 33.0 pokazuje `sell`.

**Odrzucone:**

- Tabela odsetek / WACC / dni finansowania — zgadywanie schematu (CURRENT).
- Mnożenie `buy × mid` w JS — nie drugi `margin()`; leftover Decimal w SQL.
- `payment_terms_days` w API — kolumna jest, ale nie w kontrakcie HTTP (zgadywanie powierzchni).
- Zapis `nbp_rate` / `charge` z tego ekranu.

## Zakres

Read-only `/money-cost`. `data-money-cost="board"`. Brak migracji. `fetchNbpRates` + `fetchCharges` / `<Money/>` na `buy_amount`. Linki do `/nbp-rates` i `/charges`.

## Poza zakresem

Tabela odsetek · WACC · mnożenie kursem · `payment_terms_days` w API · ExtractionService

## Ustalenia

- Token: `money_cost` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia `nbp_rate` i `charge`.
- RLS: bez nowej tabeli.
- Job: `/money-cost`. `SHIPPED_CHARGE_ROUTES["36.0"]` = `/money-cost`. OPS_JOBS +1 → 33.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 36.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/money-cost` ma `data-money-cost="board"` i woła `fetchNbpRates`
- [ ] `buy_amount` przez `<Money/>`; brak `*` / `parseFloat` / `mid`
- [ ] Brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["36.0"]` = `/money-cost`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

Wypełnić przy `/zamknij`.

## Knowledge

Nie RAG. Nie silnik odsetek.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Odsetki, WACC i mnożenie kursem świadomie leftover.
