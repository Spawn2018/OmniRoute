# Plaster 634.0 · leftover BR2.2 kolumna zgody na kontakcie

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § BR2.2 · [459.0](../archived/459.0-tracking-consent.md)
**Zależy od:** 459.0 `tracking_consent` katalog · M-10 `party_contact`
**Status:** Delta zaakceptowana `/noc 7` 2026-09-23 — wolno `/plaster`. Nie FK. Nie live poll.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover 459 „kolumna na party_contact”):** katalog `tracking_consent` zostaje. Live poll, egzekucja zgody i FK UUID zostają park. Żywy plaster: **pogłębienie istniejącej** `party_contact` — opcjonalny bool `tracking_consent` (`Boolean NOT NULL DEFAULT false`). POST/PATCH kontaktu bez pola zostaje `false`. To flaga HITL „kontakt ma zgodę na śledzenie”, **nie** FK do katalogu `tracking_consent` i nie egzekucja GPS. Serwis `parties` nie importuje `tracking_consents`. UI `/parties` panel kontaktów. `SHIPPED_CHARGE_ROUTES["634.0"]` = `/parties`. OpenFGA bez nowej relacji. Migracja `506_party_contact_tracking_consent` po `505_local_charge_carrier_service`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 459.0 „kolumna na party_contact” — tu tylko bool HITL, nie FK i nie live. Źródło: `docs/deltas/archived/459.0-tracking-consent.md`; PLAN BR2.2; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `tracking_consent` katalog (459.0). ISTNIEJE `party_contact` bez zgody. BRAK `party_contact.tracking_consent`. BRAK FK kontaktu→katalogu.

**Odrzucone:**

- FK UUID do `tracking_consent` / `party` poza istniejącym
- live poll / mapa / egzekucja
- BR2.3 Expo
- float / marża

## Zakres

1. Migracja `506_party_contact_tracking_consent`: kolumna bool DEFAULT false.
2. API create/update kontaktu przyjmuje opcjonalny bool. Serwis nie importuje tracking_consents.
3. UI panel kontaktów + zdanie w how-to. Aktualizacja `AGENTS.md` parties / tracking_consents (kolumna dozwolona; FK/live nadal zakaz).

## Poza zakresem

FK · live poll · Expo BR2.3 · egzekucja

## Plan plików

- `backend/alembic/versions/506_party_contact_tracking_consent.py`
- `backend/app/models/party_contact.py`
- domena / serwis / API parties (kontakty)
- testy HTTP/izolacji
- FE panel kontaktów
- how-to · CURRENT · PLAN · VISION · PROGRESS · docs-debt · GLOSSARY · AGENTS

## Kryteria akceptacji

- [ ] POST kontaktu z `tracking_consent=true` zapisuje flagę; katalog `tracking_consent` bez zmian
- [ ] POST bez pola → `false`
- [ ] serwis parties nie importuje `tracking_consents` i nie woła HTTP/GPS
- [ ] RLS istniejącego `party_contact`; zero float / marży / live poll

## Leftover

FK do katalogu · egzekucja zgody · live poll · BR2.3 Expo
