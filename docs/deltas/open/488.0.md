# Plaster 488.0 · AI9.2 leftover program zgodności PDF — katalog HITL

**Status:** Delta zaakceptowana `/noc 8` 2026-09-15 — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § AI9 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI9.2 leftover

**Zależy od:** tenancy RLS · `risk_register_mark` (481.0) · nie U-art50 · nie bajty PDF

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `compliance_program_marks`. Tabela `compliance_program_mark`: `mark_code` + `program_kind` `draft|review|signed|exempt|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja leftover AI9.2 — katalog HITL **stancji programu zgodności** (szkic / przegląd / podpisany / zwolniony), nie generator PDF i nie przebudowa U-art50.
- **Wybrane:** POST/GET `/api/v1/compliance-program-marks`. OpenFGA `can_manage_compliance_program_marks` (member). UI `/compliance-program-marks`. `SHIPPED["488.0"]` → `/compliance-program-marks`.
- **Wybrane:** Alembic `393_compliance_program_mark` (`down_revision = "392_field_confidence_mark"`).
- **Wybrane:** how-to `docs/operator/compliance-program-mark.md`.
- **Odrzucone:** bajty PDF / upload / render (`REJECTED` — tu stancja, nie dokument).
- **Odrzucone:** przebudowa U-art50 label (`REJECTED` — label już DONE).
- **Odrzucone:** scoring osoby · L3 write · FK do `risk_register_mark` (`REJECTED`).

**Łowca:** ISTNIEJE `risk_register_mark`. ISTNIEJE U-art50. BRAK `compliance_program_mark`.

## Zakres

How-to + OPS_JOB. FE: `program_kind` jako `<select>` z jawnymi option.

## Poza zakresem

PDF bytes · U-art50 · L3 write · silnik auto-zejścia · kwota

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

silnik auto-zejścia · L3 write (park) · AI5.0 ingest (park)
