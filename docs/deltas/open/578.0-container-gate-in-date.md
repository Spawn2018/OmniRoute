# Plaster 578.0 · T3 leftover — HITL `container.gate_in_date`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`pickup_date` / `return_date` / `gate_in_date` / `delivery_date` / `unload_date`, typ `date`) · [577.0](../archived/577.0-container-return-date.md)

**Zależy od:** 153.0 `container` · 577.0 `return_date`

**Status:** Zaakceptowana `/noc` 2026-09-19. Nie dwie pozostałe daty. Nie cutoff timestamptz. Nie `temp_min`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po `return_date` karta T3 zostawia `gate_in_date`. W `backend/app` kolumny nie ma. Ten sam typ `date`: ISO `YYYY-MM-DD`, pusty string → `null`, POST bez pola → `null`. Zła wartość → 400 (`data wjazdu`). Nie liczy countdown. Nie mutuje `return_date`.
- **Wybrane:** trzecia kopia walidacji daty. `require_container_pickup_date` i `require_container_return_date` wołają jeden `_require_calendar_day` w domenie kontenera; `require_container_gate_in_date` też. UI `/shipments`, string (`optionalToken`). `SHIPPED["578.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `458_container_gate_in_date` (po `457_container_return_date`), kolumna `Date` nullable. Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `delivery_date` / `unload_date` (`REJECTED` w tym plasterze — węższa data). Reuse cutoffu timestamptz (`REJECTED`). `temp_min` (`REJECTED`). N3 countdown (`REJECTED` → park). float / ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `gate_in_date` w `backend/app`. ISTNIEJE `require_container_return_date`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `458_container_gate_in_date`: kolumna nullable `Date`.
2. Domain `require_container_gate_in_date` + wspólny `_require_calendar_day` + API POST opcjonalny + GET.
3. UI `/shipments`. How-to dopisek. `SHIPPED["578.0"]`.

## Poza zakresem

`delivery_date` · `unload_date` · cutoff timestamptz · `temp_min` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `gate_in_date="2026-09-21"` zapisuje date
- [ ] data z godziną / zły format / nie-string → 400 (`data wjazdu`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero countdown / mutacja `return_date`

## Leftover

`delivery_date` i `unload_date` · `temp_min` gdy reefer · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH
