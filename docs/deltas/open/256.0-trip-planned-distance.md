# Plaster 256.0 · T2c `planned_distance_km` na `trip`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 leftover T2c · [214.0](../archived/214.0-trip-route-label.md) leftover km · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2
**Zależy od:** 214.0 `route_label`
**Status:** Delta zaakceptowana `/noc`. Nie actual. Nie `/fleet`. Nie `party`. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T2c km/`party`/`/fleet` + leftover T1b + leftover `appointment_status` + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover `actual_distance_km` parked. leftover `subcontractor_party_id` parked. leftover `/fleet` parked. leftover T1b parked. leftover `appointment_status` / `no_show_at` parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `trip` — opcjonalny `planned_distance_km` (`Numeric(14,4)` HITL, ≥ 0, pusty → `null`). POST bez pola zostaje `null`. JSON float / bool / ujemna → 400 (`km`). `require_trip_planned_distance_km` w `app.domain.trip` — wzór `weight_kg`, nie liczenie z GPS. UI `/shipments` panel przejazdu. `SHIPPED_CHARGE_ROUTES["256.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `192_trip_planned_distance` (po `191_stop_pod_quality`). CHECK `ck_trip_planned_distance`. Marża zostaje na `charge`. W plasterze: zakaz BC trip „km” → zakaz **liczenia** km / GPS; pole HITL wolno.

**Łowca:** ISTNIEJE `trip.route_label` (214.0). ISTNIEJE `stop.weight_kg` (247.0, Decimal HITL). BRAK `trip.planned_distance_km`. BRAK `actual_distance_km`. BRAK `/fleet`.

**Odrzucone:**

- `actual_distance_km` / liczenie z GPS / freeze kosztu.
- `subcontractor_party_id` / `/fleet` / HW / import `parties` / mapa / T5 / S53 / live HTTP / float.

## Zakres

1. Migracja `192_trip_planned_distance`: `planned_distance_km` nullable Numeric(14,4) + CHECK ≥ 0. Nie `create_table`.
2. POST opcjonalny `planned_distance_km`. Zły token → 400 (`km`). Serwis nie importuje charges/parties i nie woła HTTP.
3. UI `/shipments` panel przejazdu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`actual_distance_km` · `subcontractor_party_id` · `/fleet` · GPS · mapa · T7b · T5 · Auth0 · live HTTP · float

## Kryteria akceptacji

- [ ] POST z `planned_distance_km` zapisuje odległość planowaną HITL, bez liczenia
- [ ] POST bez pola / pusty trim zostaje `null`; `0` jest legalne
- [ ] float / bool / ujemna / nie-Decimal → 400 (`km`)
- [ ] RLS istniejącego `trip`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / `/fleet` / live HTTP / import `parties`
