# Plaster 468.0 · BR4.2 konsolidacja morska LCL: konsole i CFS

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR4.2 · D6 `ocean_bill` · 111.0 `ocean_lcl`

**Zależy od:** tenancy RLS · wzorzec HITL `oog_permit_mark` (467.0) · `ocean_bill` i odcinek `ocean_lcl` już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `lcl_console_marks`. Tabela `lcl_console_mark`: `mark_code` + `console_kind` `console|cfs|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance konsoli/CFS, nie druga tabela LCL i nie live CFS.
- **Wybrane:** POST/GET `/api/v1/lcl-console-marks`. OpenFGA `can_manage_lcl_console_marks` (member). UI `/lcl-console-marks`. `SHIPPED["468.0"]` → `/lcl-console-marks`.
- **Wybrane:** Alembic `373_lcl_console_mark` (`down_revision = "372_oog_permit_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `ocean_bill` (`REJECTED` — D6 zostaje HBL/MBL, nie konsola).
- **Odrzucone:** druga tabela LCL obok `shipment_leg.ocean_lcl` (`REJECTED` — GLOSSARY: odcinek zostaje, nie tabela LCL).
- **Odrzucone:** klej FK do `ocean_bill` / `shipment_leg` / `nvocc_mark` (`REJECTED` na tym wierszu).
- **Odrzucone:** live CFS HTTP · kalkulacja CBM · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `ocean_bill` (D6 HBL/MBL). ISTNIEJE `shipment_leg` tryb `ocean_lcl` (111.0). ISTNIEJE `nvocc_mark` (nvocc/house/master). ISTNIEJE `groupage_dispatcher_mark` (drobnica drogowa). PODOBNE katalog HITL. BRAK `lcl_console_mark`. BRAK katalogu konsoli/CFS.

## Zakres

How-to: jedno zdanie — to katalog stance konsoli LCL; live CFS i CBM zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (lista + select, nie klon 464–467) pod jscpd < 3%.

## Poza zakresem

live CFS · CBM · druga tabela LCL · FK do `ocean_bill` · NVOCC live · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live CFS · kalkulacja CBM · FK do `ocean_bill` / `shipment_leg` / `nvocc_mark` · BR4.3 NAC
