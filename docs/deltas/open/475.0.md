# Plaster 475.0 · BR1.1 RFID — katalog identyfikacji automatycznej

**Status:** Otwarte `/noc 17` 2026-09-14. Czeka na plaster.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 1 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR1.1 · BR1.2

**Zależy od:** tenancy RLS · wzorzec HITL `wms_flow_mark` (474.0) · `shipment_package` (skan QR, nie RFID)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `rfid_marks`. Tabela `rfid_mark`: `mark_code` + `rfid_kind` `reader|gate|tag|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR1.0 — katalog HITL czytników, bramek i znakowania (VISION § C.3), nie live poll RFID i nie odczyt EPC.
- **Wybrane:** POST/GET `/api/v1/rfid-marks`. OpenFGA `can_manage_rfid_marks` (member). UI `/rfid-marks`. `SHIPPED["475.0"]` → `/rfid-marks`.
- **Wybrane:** Alembic `380_rfid_mark` (`down_revision = "379_wms_flow_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `telematics_device` (`REJECTED` — BR2.1 to urządzenie GPS/floty, nie bramka magazynowa).
- **Odrzucone:** rozszerzenie `wms_flow_mark` (`REJECTED` — 474.0 to rodzaj operacji WMS, nie sprzęt RFID).
- **Odrzucone:** rozszerzenie `shipment_package` (`REJECTED` — D2 to paczka na zleceniu ze skanem Omni QR).
- **Odrzucone:** live RFID poll / Impinj / Zebra HTTP · bajty EPC · parowanie z lokalizacją bin · FK shipment (`REJECTED` — leftover; CURRENT park live HTTP).
- **Odrzucone:** kwota / marża / float / UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `telematics_device` (device_kind tracker/fault). ISTNIEJE `shipment_package` (QR Omni). ISTNIEJE `wms_flow_mark` (flow_kind operacji). PODOBNE katalog HITL stance per tenant. BRAK `rfid_mark`. BRAK katalogu czytnik/bramka/znakowanie RFID.

## Zakres

How-to: jedno zdanie — to katalog sprzętu/stancji RFID w magazynie; live poll i odczyt tagów zostają leftover. +1 OPS_JOB. Klient FE: `rfid_kind` jako segmented control (jak 474.0 `flow_kind`).

## Poza zakresem

live RFID HTTP · EPC/tag bytes · FK location/bin · parowanie z `wms_flow_mark` · qty · wycena zapasu BR1.2 · sekrety · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live Impinj/Zebra/bramki HTTP · odczyt EPC · parowanie z bin · integracja z `wms_flow_mark` · Inventory Release BR1.2
