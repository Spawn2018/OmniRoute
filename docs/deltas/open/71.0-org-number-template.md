# Plaster 71.0 · S8 numer i szablon w organization_setting (M-03)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S8  
**Zależy od:** 3.0 `organization_setting` · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Arch. M-84 scala się w M-03. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama tabela `organization_setting`. Allowlista dostaje dwa klucze: `quotation_number_prefix` (prefiks etykiety, nie licznik) i `quotation_print_template` (token `plain` albo `letter`). Wartość zostaje tekstem w `setting_value` (64 znaki wystarczą). Zero nowej tabeli, zero nowego RLS. OpenFGA bez nowej relacji: `can_manage_organization_settings`. Ekran: ten sam `/organization-settings`. Serwis ustawień nie importuje `quotations`. Serwis wyceny nie czyta tych kluczy — S8 = konfiguracja jako dane, nie numer na ofercie.

**Łowca:** ISTNIEJE: `ALLOWED_SETTING_KEYS` = `default_currency`, `normalize_setting_key` / `normalize_setting_value`, upsert, UI waluty, RLS. PODOBNE: 50.0 odczyt `default_currency` na `/rollout`. BRAK: klucz prefiksu; klucz szablonu; walidacja wartości poza ISO 4217.

**Odrzucone:**

- Nowy moduł / tabela arch. M-84 (PLAN: scalać w M-03).
- Tabela serii numerów / `nextval` / licznik w Pythonie (to silnik numeracji, nie ustawienie).
- Kolumna `quotation.document_number` w tym plasterze (S9 dokument oferty).
- HTML/PDF/letterhead blob (57.0 leftover; S9).
- Sekrety, Infisical, env.
- F9.1 · Auth0 · portale · M-02.

## Zakres

1. Allowlista + normalizacja dwóch kluczy. Bez migracji, bez nowego RLS.
2. Test: nieznany klucz pada; sekret pada; prefiks i token szablonu zapisują się; tenant B nie widzi ustawień A (istniejący test izolacji zostaje).
3. `/organization-settings`: formularze prefiksu i szablonu obok waluty. Nie licz w JS.

## Poza zakresem

Numer na `quotation` · PDF / send · IMAP · F9.1 · nowa tabela

## Ustalenia

- Żywy ID: **M-03**. Arch. M-84 nie dostaje osobnego kodu.
- HITL bez zmian. LLM nie liczy. `charge` zostaje prawdą o marży.
- How-to: dopisać do `docs/operator/katalogi.md` (job zapisu prefiksu/szablonu).

## Schemat

Bez zmian tabeli. Klucze w allowliście domeny.

## Kryteria akceptacji

- [ ] Tylko klucze z allowlisty; sekret nadal odrzut
- [ ] Prefiks i szablon zapisują się per tenant
- [ ] Izolacja tabeli bez zmian (FORCE zostaje)
- [ ] `can_manage_organization_settings`; 403 bez tuple
- [ ] UI na `/organization-settings`
- [ ] Serwis ustawień nie importuje `quotations` / `charges`
- [ ] Serwis wyceny nie czyta nowych kluczy

## Pytania

Brak. `/noc` akceptuje deltę.
