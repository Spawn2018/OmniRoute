# Plaster 592.0 · T2 leftover — HITL `resource_document` (ważność floty)

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [591.0](../archived/591.0-resource-capacity-m3.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`document_expiries`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie floating trailer. Nie HW. Nie UXCL. Nie Job#2. Nie N3. Nie 409 na trip.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po m³. Nowa tabela `resource_document` per tenant: `resource_id` (złożone FK `organization_id`+`id` RESTRICT) + `document_kind` (`licence`|`insurance`|`other`) + `valid_until` (date HITL, nie countdown) + `source_ref`. Append-only. Nie `party_document` (to C8 na kontrahencie). Nie kolumna JSON na `resource`.
- **Wybrane:** UI `/fleet`. `SHIPPED["592.0"]` → `/fleet`. OpenFGA bez nowej relacji (`can_manage_shipments`). Migracja `468_resource_document` (po `467_resource_capacity_m3`) + RLS FORCE + test izolacji.
- **Odrzucone:** floating trailer / HW (`REJECTED`). 409 przy przypisaniu do `trip` (`REJECTED` — leftover). countdown SQL (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park). `party_document` (`REJECTED` — inny BC).

**Łowca:** BRAK `resource_document`. ISTNIEJE `party_document` (C8 — inny podmiot, nie reuse). ISTNIEJE `resource` (151.0 — FK). PODOBNE: HITL katalog + data. Nie nowa kolumna na `resource`.

## Zakres

1. Migracja `468_resource_document` + RLS.
2. Model + repo + serwis + API POST/GET.
3. UI `/fleet` pole ważności. How-to. `SHIPPED["592.0"]`. Test izolacji.

## Poza zakresem

floating trailer · HW · 409 na trip · countdown · `party_document` · UXCL · Job#2 · N3 · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST `document_kind=licence` + `valid_until` zapisuje wiersz
- [ ] zły kind / obcy `resource_id` → 400
- [ ] izolacja RLS (dwa tenanty)
- [ ] zero UPDATE/DELETE · zero import charges · zero 409 na trip

## Leftover

floating trailer · HW · 409 przy planowaniu · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
