# Plaster 6.0 · M-23 Kurs NBP (`nbp_rate`)

**Spec źródłowa:** [docs/spec/nbp-rate.md](../../spec/nbp-rate.md)  
**Zależy od:** 0.3 RLS · 0.4 OpenFGA · 0.6 DataTableShell · 3.0 `default_currency` (zostaje na `organization_setting`)

**Status:** Delta zaakceptowana `/noc` 2026-09-01. Wolno `/plaster`. Nie Q6.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki Q5):** jeden katalog kursu średniego tabeli A per tenant — tabela `nbp_rate`. `resolve(currency, on_date)` bierze najnowszy opublikowany dzień `<= on_date` (NBP i tak nie publikuje w weekend). UI `/nbp-rates`. Żywy ID **M-23** (token `nbp_rate` z GLOSSARY). Nie M-07 — żywe M-07 = `rate_line`.

**Odrzucone:**

- Przeliczanie kwot wyceny / marży w tym plastrze — kolejka Q5 to katalog kursu, nie drugi silnik `charge`. Fala 3 „Waluty w ofercie” czyta ten katalog później.
- Nadpisanie żywego M-07 `rate_line` albo drugiej tabeli stawek.
- Tabela C (kupno/sprzedaż NBP), ECB, live HTTP do api.nbp.pl w CI — jak 4.0/5.0: fixture + ręczny wpis; operator odpala ingest, gdy ma sieć.
- Kalendarz świąt RP w Pythonie — data pochodzi z tabeli NBP, nie z naszego liczenia D-1.
- Lista walut ISO jako osobny katalog — `default_currency` zostaje w M-03; tu tylko kurs do CHAR(3).

## Zakres

Katalog `nbp_rate` per tenant. Lista, dodanie ręczne, `resolve`. RLS FORCE. OpenFGA `can_manage_nbp_rates` = member. Ingest w teście = fixture.

## Poza zakresem

Q6 · Fala 3 M-23-oferta (czytanie kursu w `quotation`) · `charge.margin()` w obcej walucie · live NBP w gate · Auth0 · M-02 · portale · Exit Wave FE · ExtractionService→kursy · LLM liczący przeliczenie

## Ustalenia

- Żywy ID: **M-23**. Token: `nbp_rate`. Archiwum M-07 „waluty i czas” nie używa numeru M-07 w kodzie.
- Fala 3 „Waluty w ofercie” (archiwum M-23) = późniejszy plaster na `quotation`, nie drugi katalog FX.
- `currency`: ISO 4217, 3 litery A–Z. Inne = `InvalidCurrency`. Brak wiersza PLN (NBP nie kwotuje PLN/PLN).
- `mid`: `Numeric(14,4)` — skala tabeli A NBP. Nigdy float.
- `source_ref` obowiązkowy; ręczny wpis = `tenant:manual`; fixture = `nbp:A:<date>:<table_no>`.
- Unikat `(organization_id, currency, rate_date)`.
- `resolve`: SQL `rate_date <= on_date` ORDER BY `rate_date` DESC LIMIT 1; pusto = `UnknownNbpRate`.
- OpenFGA: `can_manage_nbp_rates` = `member`. Brak tuple na wiersz.
- RLS FORCE + test izolacji dwóch organizacji.
- Job operatora: `/nbp-rates` — DataTableShell, dodanie, pole resolve (waluta + data).
- import-linter: `app.services.nbp_rates` niezależny BC. `quotations` / `charges` / `rate_lines` / `extraction` nie importują.
- Migracja `018_nbp_rate_rls` (w górę i w dół).
- LLM nie liczy. `charge` = marża zostaje.

## Schemat

`nbp_rate`: `id`, `organization_id`, `currency` CHAR(3), `rate_date` DATE, `mid` Numeric(14,4), `source_ref`, timestamps.

## Kryteria akceptacji

- [ ] Migracja `018_nbp_rate_rls`: tabela + RLS FORCE; w górę i w dół
- [ ] Test izolacji tenantów
- [ ] `resolve` po walucie i dacie; brak wiersza = `UnknownNbpRate`
- [ ] Token waluty poza ISO 4217 (3 litery) = `InvalidCurrency`
- [ ] `mid` nie jest float (typ Numeric / Decimal)
- [ ] OpenFGA `can_manage_nbp_rates`; brak deklaracji = deny
- [ ] UI `/nbp-rates` (DataTableShell): lista, dodanie, resolve
- [ ] Extraction / quotations / charges / rate_lines nie importują `nbp_rates`
- [ ] `just api-types` po zmianie OpenAPI
