# Plaster 22.0 · M-29 Wykrywanie akceptacji (`offer_acceptance` przy `quotation`)

**Spec źródłowa:** [docs/spec/offer-acceptance.md](../../spec/offer-acceptance.md)  
**Zależy od:** 2.0/5.1 `quotation` · 0.25 `<Money/>`

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie HITL accept. Nie IMAP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-29):** nie ma w bazie znacznika akceptacji oferty, więc **wykrycie = pending**. Panel na `/quotations` pokazuje wyceny z `party_id` jako oczekujące. Token `offer_acceptance` (UI, nie tabela). Żywy ID **M-29**.

**Łowca:** BRAK `offer_acceptance` / statusu wyceny / won-lost. ISTNIEJE: `quotation.party_id` + `<Money/>`. PODOBNE: HITL `accept` na ekstrakcji (M-20) — **inny byt**, nie reuse. 18.0 leftover: katalog wyniku = osobny Plan, nie zgadywać tabeli.

**Odrzucone:**

- Tabela `offer_result` / kolumna statusu — zgadywanie schematu (CURRENT, leftover 18.0).
- Etykiety `won`/`lost` na `/quotations` — test katalogu 18.0 tego broni.
- `ExtractionService.accept` / przycisk HITL przy ofercie — inna domena; HITL zostaje przy draftcie.
- IMAP / auto-parse maila — M-32.
- LLM czytający „akceptuję”.

## Zakres

Panel na `/quotations`. `data-offer-acceptance="pending"`. Brak migracji. Brak POST wyniku.

## Poza zakresem

Zapis decyzji · skrzynka · M-30 RFQ outbound · ExtractionService · suma kwot

## Ustalenia

- Token: `offer_acceptance` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations` na liście.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["22.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/quotations` ma `data-offer-acceptance="pending"` i listuje wyceny z `party_id`
- [ ] Kwota przez `<Money/>`; brak `won`/`lost`; brak `acceptExtractionDraft`
- [ ] Brak nowej migracji
- [ ] `just dup` ≤ 3%

## Knowledge

Nie RAG. Nie reuse HITL accept.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Zapis wyniku i IMAP świadomie leftover.
