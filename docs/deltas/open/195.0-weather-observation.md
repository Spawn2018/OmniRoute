# Plaster 195.0 · V2 `weather_observation` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) § V2 · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § 3.8
**Zależy od:** 0.3 tenancy (katalog per tenant) · 194.0 `stop` ETA (pogoda była leftover)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie Open-Meteo. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover V2 pogoda + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V2b myto→`charge` parked (druga marża). leftover C3 live VIES parked. leftover C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO parked. leftover F2b–F4 parked. leftover `circle_sim` parked. leftover kg parked. F1 / C1 / C8 409 parked. Open-Meteo wzdłuż `trip.route_geometry` (lat/lng, WMO, HTTP) zostaje leftover. Żywy plaster: jedna tabela `weather_observation` per tenant — **HITL znacznik pogody jako dane**, nie feed i nie silnik ETA. Wiersz = `condition_code` (`clear` | `rain` | `snow` | `wind` | `fog` | `ice` | `other`) + `station_unlocode` (5 znaków A–Z0–9, dana, nie resolve geography) + `observed_at` (ISO-8601 ze strefą) + `provider_code` (`hitl`) + `source_ref` (`tenant:manual` albo `fixture://weather-observation/…`). Operator wpisuje warunek i stację; serwis nie woła Open-Meteo, nie woła IMGW/DWD, nie liczy ETA, nie woła LLM i nie czyta `charge` / `trip` / `stop`. Zły warunek → 400 (`warunek`). Zła stacja → 400 (`stacja`). Zły czas → 400 (`obserwacja`). Obcy provider → 400 (`dostawca`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/weather-observations` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["195.0"]` = `/weather-observations`. OpenFGA: `can_manage_weather_observations`. Migracja `135_weather_observation`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.eta_physical` / `eta_legal` — dwa zegary HITL, **nie** pogoda. ISTNIEJE `entity_event` — append-only job, **nie** obserwacja WMO. ISTNIEJE `prediction_ledger` — CRPS, **nie** Open-Meteo. ISTNIEJE GLOSSARY `observation_kind` (V5 GPS) — **inny** byt. BRAK tabeli `weather_observation`. BRAK lat/lng na pogodzie. BRAK HTTP pogody.

**Odrzucone:**

- Open-Meteo / IMGW / DWD / Météo-France live HTTP.
- `trip_id` / geometria / lat/lng / WMO int z API.
- Myto → `charge` (leftover V2b).
- C3 live VIES / C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO.
- F2b / F3 / F4 / G2.20 / kg / F1 / C1 / C8 409.
- Silnik ETA / GPS / mapa / float.
- Kwota / druga marża / T-SQL / scrape.

## Zakres

1. Migracja `135_weather_observation` (po `134_stop_eta`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist.
2. POST/GET. Zły warunek / stacja / czas → 400. Serwis nie importuje trips / stops / charges / extraction / geography i nie woła HTTP.
3. UI `/weather-observations`. `just api-types`. How-to `docs/operator/pogoda.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `WeatherDesk` / `weatherWrite` / `persistWeatherMark`.

## Poza zakresem

Open-Meteo · IMGW · DWD · geometria trip · lat/lng · myto → charge · GPS · silnik ETA · C3 live lookup · kwota · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `weather_observation` z `condition_code`, `station_unlocode` i `observed_at`, bez HTTP
- [ ] zły `condition_code` → 400 (`warunek`)
- [ ] zły `observed_at` → 400 (`obserwacja`)
- [ ] GET zwraca wiersze tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / mapy / live HTTP / scrape
