# Plaster 120.0 · S27b karta z decyzji oferty (M-13)

**Spec źródłowa:** [docs/spec/party-scorecard.md](../../spec/party-scorecard.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S27 (S27b)  
**Zależy od:** 10.0 `party_scorecard` · 86.0 decyzja S11 na `quotation` · 5.1 `quotation.party_id`

**Status:** Delta zaakceptowana `/noc` 2026-09-04. Ten sam żywy ID **M-13**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** `/party-scorecards` **czyta** istniejące decyzje S11 na wycenie (`fetchOperatorDecisions` + `fetchQuotations`) i pokazuje przy danym kontrahencie, które oferty są przyjęte, a które odrzucone. Zero zapisu wskaźników. Zero nowej tabeli. Zero `won`/`lost` po angielsku. `SHIPPED_CHARGE_ROUTES["120.0"]` = `/party-scorecards`. OPS_JOBS 55 → 56.

**Łowca:** ISTNIEJE 10.0 snapshot ręczny. ISTNIEJE 86.0 Przyjmij/Odrzuć na `quotation`. BRAK tych werdyktów na karcie. PODOBNE: 106.0 FV na `/finance`. Dwa joby: zapis karty vs odczyt decyzji.

**Odrzucone:**

- SQL-refresh zapisujący `response_rate` / medianę w Pythonie albo w JS.
- Scoring osoby / JDG / auto-limit.
- Kolumna `won`/`lost` na wycenie.
- Import `quotations` / `operator_decisions` w serwisie `parties`.
- Auth0 · portale · OTel · katalog 71–212.

## Zakres

1. `/party-scorecards`: sekcja `data-scorecard="offer-outcomes"`. Link do `/decisions` i `/quotations`.
2. Test powierzchni 120.0 (zostaw asercje 10.0). How-to w `katalogi.md` bez nowej linii (footer / karta).
3. `SHIPPED_CHARGE_ROUTES["120.0"]` = `/party-scorecards`. OPS_JOBS = 56.

## Poza zakresem

Zapis KPI z COUNT · karta per lane · `changed` · UN na RFQ · F9.1

## Ustalenia

- Token zostaje `party_scorecard`. Werdykt zostaje `operator_decision`.
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL extract bez zmian.
- Ręczny upsert 10.0 zostaje.

## Schemat

Bez nowej tabeli.

## Kryteria akceptacji

- [ ] Karta listuje przyjęte/odrzucone oferty; brak openai / parseFloat / scoring
- [ ] Zero nowej migracji
- [ ] OPS_JOBS = 56; `120.0` → `/party-scorecards`
- [ ] `katalogi.md` ≤ 40

## Pytania

Brak. `/noc` akceptuje deltę.
