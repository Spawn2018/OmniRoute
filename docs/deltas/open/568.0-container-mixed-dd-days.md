# Plaster 568.0 · V3 leftover — HITL `container.mixed_dd_days`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · V3 · leftover z [567.0](archived/567.0-container-detention-days.md) · [196.0](archived/196.0-free-time-clock.md)

**Zależy od:** 153.0 `container` · 566.0 demurrage · 567.0 detention (obok, nie to samo)

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover V3 po detention. Pogłębienie istniejącej `container`: opcjonalny `mixed_dd_days` (`Integer` nullable, ≥ 0, dana w **dniach** mixed demurrage+detention, nie `now() − gate`). POST bez pola → `null`. Zła wartość → 400 (`dni`). Nie mutuje `free_time_clock` / demurrage / detention.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["568.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1.
- **Odrzucone:** countdown N3 / remaining (`REJECTED` → park). Szkic `charge` D&D (`REJECTED` → park). Import `free_time_clocks` (`REJECTED`). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live armator / PIN / VGM.

**Łowca:** BRAK `mixed_dd_days`. ISTNIEJE `demurrage_free_days` / `detention_free_days`. ISTNIEJE `free_time_clock.free_days`.

## Zakres

1. Migracja `448_container_mixed_dd` (po `447_container_detention`): kolumna nullable Integer.
2. Domain `require_mixed_dd_days` + API POST opcjonalny + GET.
3. UI `/shipments` panel kontenera. How-to dopisek. `SHIPPED["568.0"]`.

## Poza zakresem

countdown N3 · szkic charge · free_time_clock mutacja · AI3 · kwota

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `mixed_dd_days=3` zapisuje dni
- [ ] ujemne / float / bool → 400 (`dni`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero float / live HTTP / import free_time_clocks

## Leftover

N3 countdown · szkic charge · live armator

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | DO WYPEŁNIENIA | |
| Szybkość | N/A | kolumna |
| SQL | N/A | ADD COLUMN |
| N+1 | OK | |
| Dług / człowiek | OK | leftover N3 |
| Proza operatora | 5–15 zdań | dopisek how-to |
| Docs/OS | DO WYPEŁNIENIA | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | DO WYPEŁNIENIA | |
