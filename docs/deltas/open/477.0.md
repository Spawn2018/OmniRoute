# Plaster 477.0 · BR1.3 zabezpieczenie na towarze — katalog HITL

**Status:** Plan zaakceptowany `/noc 20` 2026-09-14. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 1 · § A.4 Trade-Tech · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR1.3

**Zależy od:** tenancy RLS · wzorzec HITL `inventory_finance_mark` (476.0) · `inventory_position_mark` (EXP3.3) · `po_financing_mark` (472.0)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `inventory_collateral_marks`. Tabela `inventory_collateral_mark`: `mark_code` + `collateral_kind` `pledge|lien|hold|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR1.2 — katalog HITL stancji zabezpieczenia na towarze (VISION: powiązanie pozycji z finansowaniem), nie FK do pozycji i nie live zastaw.
- **Wybrane:** POST/GET `/api/v1/inventory-collateral-marks`. OpenFGA `can_manage_inventory_collateral_marks` (member). UI `/inventory-collateral-marks`. `SHIPPED["477.0"]` → `/inventory-collateral-marks`.
- **Wybrane:** Alembic `382_inventory_collateral_mark` (`down_revision = "381_inventory_finance_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `inventory_finance_mark` (`REJECTED` — 476.0 to wycena/wiekowanie/release jako obiekt finansowy, nie zastaw).
- **Odrzucone:** rozszerzenie `po_financing_mark` (`REJECTED` — 472.0 to stance finansowania PO, nie zabezpieczenie magazynowe).
- **Odrzucone:** rozszerzenie `inventory_position_mark` (`REJECTED` — EXP3.3 to pozycja plant/sku, nie zastaw).
- **Odrzucone:** FK do `inventory_position_mark` / `po_financing_mark` / `purchase_order` · live zastaw HTTP · wartość Decimal (`REJECTED` — leftover Trade-Tech; CURRENT park).
- **Odrzucone:** kwota / marża / float / UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `inventory_finance_mark` (valuation/aging/release). ISTNIEJE `po_financing_mark` (po/release/advance). ISTNIEJE `inventory_position_mark`. ISTNIEJE `wms_flow_mark` / `rfid_mark`. PODOBNE katalog HITL stance. BRAK `inventory_collateral_mark`. BRAK katalogu zastawu/hold na towarze.

## Zakres

How-to: jedno zdanie — to katalog stancji zabezpieczenia na towarze Trade-Tech; klej do pozycji i live zastaw zostają leftover. +1 OPS_JOB. Klient FE: `collateral_kind` jako segmented control (jak 476.0).

## Poza zakresem

FK position / PO / financing · live zastaw · wartość Decimal · wycena SQL · WMS live · sekrety · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

FK `inventory_position_mark` · klej do `po_financing_mark` · live zastaw Trade-Tech · wartość · BR5.1 FK PO
