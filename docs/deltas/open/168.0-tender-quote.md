# Plaster 168.0 · P6 `tender_quote`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P6 · [karty-pol-fala-p.md](../../analysis/karty-pol-fala-p.md) § P6
**Zależy od:** 2.0 `quotation` (oferta sprzedaży już jest) · G2 `tender` zostaje później
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Nie auto-award. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z P6):** jedna tabela `tender_quote` per tenant — **ważność i limit orderów z oferty jako dane**, nie obiekt przetargu G2 i nie auto-award. Wiersz = `quotation_id` (FK tenanta do istniejącej wyceny) + `valid_until` (`DATE`) + `order_limit` (`INTEGER` > 0, liczba zleceń, nie kwota) + `source_ref` (`tenant:manual` albo `fixture://tender-quote/…`). Zła data → 400 (detail zawiera `data`). `order_limit` ≤ 0 / float / bool → 400 (detail zawiera `limit`). Obce `source_ref` → 400 (`obce`). GET listy SQL. Serwis nie importuje quotations / charges / channel_quotes / trips. UI `/tender-quotes`. `SHIPPED_CHARGE_ROUTES["168.0"]` = `/tender-quotes`. OpenFGA: `can_manage_tender_quotes`. Migracja `108_tender_quote`. Marża zostaje na `charge`. Zero Money na tym ekranie (to nie kwota).

**Łowca:** ISTNIEJE `quotation` — wycena sprzedaży + `negotiated_channel_quote_id`; **nie** dokładamy ważności tam (osobny job P6). ISTNIEJE `channel_quote` — oferta kanału z kwotą; **nie** rozszerzamy o limit orderów. ISTNIEJE `charge_template` — daty na kolekcji kodów; inna oś. ISTNIEJE GLOSSARY `tender` = G2; **nie** tworzymy tabeli `tender`. BRAK `tender_quote`. BRAK `/tender-quotes`.

**Odrzucone:**

- Obiekt `tender` / loty / rundy / award (leftover P6b = G2).
- Auto-award (leftover P6c).
- Kwota / marża na tym wierszu (zostaje `quotation` / `channel_quote` / `charge`).
- Kolumny na `quotation` / `channel_quote`.
- Float / T-SQL / live HTTP / TED scrape.

## Zakres

1. Migracja `108_tender_quote`: RLS, izolacja, indeks `(organization_id, quotation_id)`.
2. POST/GET. Zła data / limit / `source_ref` → 400. API składa odczyt wyceny (FK), serwis nie importuje quotations.
3. UI `/tender-quotes`. `just api-types`. How-to `docs/operator/oferta-przetargowa.md`; fold w `katalogi.md` (40 linii).

## Poza zakresem

P6b G2 `tender` · P6c auto-award · kwota na wierszu · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `tender_quote` z `valid_until` i `order_limit` > 0, bez award
- [ ] `order_limit` ≤ 0 → 400
- [ ] GET zwraca oferty tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / auto-award / live HTTP
