# Plaster 526.0 · AI5.0 leftover — `kpi_definition_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI5.0 leftover · po [525.0](../archived/525.0-cfo-narrative-mark.md)

**Zależy od:** AI5.0 `data_source` / brama · definicja KPI jako dana, nie wzór z modelu

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `kpi_definition_marks`. Tabela `kpi_definition_mark`: `mark_code` + `kpi_kind` `otd|otif|custom|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja leftoveru AI5 — **definicja KPI per strona jako dana HITL**, nie wzór OTD/OTIF z modelu.
- **Wybrane:** POST/GET `/api/v1/kpi-definition-marks`. OpenFGA `can_manage_kpi_definition_marks`. UI `/kpi-definition-marks`. `SHIPPED["526.0"]`.
- **Wybrane:** how-to `docs/operator/kpi-definition-mark.md`.
- **Odrzucone:** wzór KPI z modelu · OMNI READINESS · pasek 72% · live scoring (`REJECTED`).
- **Odrzucone:** float / amount / SQL OTIF% (`REJECTED`).

**Łowca:** ISTNIEJE `otif_mark` · `job_metric_mark` · `cfo_narrative_mark`. BRAK `kpi_definition_mark`.

## Zakres

HITL `kpi_definition_mark` — migracja 416 + warstwy + OpenFGA + FE + how-to + SHIPPED 526.0.

## Poza zakresem

wzór KPI · live ingest · float

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] UI `/kpi-definition-marks`; `SHIPPED["526.0"]`
- [ ] craft_style; how-to

## Leftover

live ingest · wzór KPI egzekucja
