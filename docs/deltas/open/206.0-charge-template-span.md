# Plaster 206.0 · P2c exclusion daterange `charge_template`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P2 leftover · [164.0](../archived/164.0-charge-template.md) leftover P2c · [4.1](../archived/4.1-location-zones.md) (wzorzec GiST; ten plaster nie importuje geography)
**Zależy od:** 164.0 `charge_template` (kolekcja + daty już są) · 013 `btree_gist` (rozszerzenie już jest)
**Status:** Otwarty `/noc` 2026-09-09. Nie T-SQL. Nie druga marża. Nie parser overlap w Pythonie.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P2c exclusion + P1c parser + P1d daty karty + leftover D9c–f + V7–V8 + C3–C9 + F2b–F4 + G2.20):** leftover P1c WHEN/IF/CALC parked. leftover P1d exclusion na `rate_card` parked. leftover D9c–f / V7–V8 / V6 / V5 / V4 / V2b / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. Żywy plaster: **pogłębienie istniejącej** `charge_template` — Postgres blokuje nakładające się okna `valid_from`/`valid_until` tego samego kodu w tym samym szablonie tenanta (exclusion GiST na `daterange`), jak 4.1 strefy pocztowe, nie pętla w Pythonie. Zdejmujemy unique `(organization_id, template_code, charge_code)`, żeby kolejne, nienakładające się okresy tego samego kodu były legalne. Nakładanie → 400 (`nakładanie`). UI zostaje `/charge-templates`. `SHIPPED_CHARGE_ROUTES["206.0"]` = `/charge-templates`. OpenFGA bez nowej relacji (`can_manage_charge_templates`). Migracja `146_charge_template_span`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `location_zone_member` exclusion GiST na `postal_span` — zakresy pocztowe, **nie** szablon opłat; nie importujemy geography. ISTNIEJE `charge_template` unique `(organization_id, template_code, charge_code)` — jeden kod raz, **nie** sekwencja okresów. ISTNIEJE `party_charge_override` z datami i kwotą — inna oś. ISTNIEJE `btree_gist` w 013. BRAK exclusion daterange na `charge_template`. BRAK overlap w Pythonie.

**Odrzucone:**

- Parser WHEN/IF/CALC (P1c) i daty na `rate_card` (P1d).
- Import `geography` / `charges` / `rate_cards`.
- `CREATE EXTENSION btree_gist` (już 013).
- Druga tabela header–line (P2b).
- Kwota / marża / float / T-SQL / live HTTP / 409 na shipment.

## Zakres

1. Migracja `146_charge_template_span` (po `145_rate_card_match`): kolumna generowana `validity_span` (`daterange` zamknięty `[]`), drop unique członka, `EXCLUDE USING gist` `(organization_id, template_code, charge_code, validity_span &&)`.
2. POST: sekwencja nienakładających się okien OK; nakładanie łapie IntegrityError → 400. Serwis nie porównuje dat w pętli i nie woła HTTP.
3. UI `/charge-templates`. How-to `szablon-oplat.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

P1c parser · P1d daty karty · T-SQL · FSC · charge · live HTTP · float

## Kryteria akceptacji

- [ ] dwa nienakładające się okna tego samego kodu w szablonie zapisują się
- [ ] nakładające się okna → 400 (`nakładanie`); baza, nie Python
- [ ] izolacja: nakładanie u A nie widać u B; ten sam zakres u B jest legalny
- [ ] unique 164.0 już nie blokuje sekwencji okresów
- [ ] Zero float / marży / T-SQL / live HTTP / scrape
