# Plaster 401.0 · EXP4.21 `job_metric_mark` HITL (katalog, bez scoringu osoby)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala EXP · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) · M-72 Job-metric
**Status:** Delta zaakceptowana `/noc` 2026-09-12. Wolno `/plaster`. Nie scoring osoby. Nie metryka SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki EXP4.21 / M-72 — tylko HITL katalog `job_metric_mark` jako dane. `mark_code` + `metric_kind` (`time_to_fix` | `touches` | `rework` | `other`) + `source_ref`. UI `/job-metric-marks`. OpenFGA `can_manage_job_metric_marks`. Migracja `311_job_metric_mark`. Job OPS. Deny-list. Scoring osoby / SQL job = leftover.

**Łowca:** BRAK tabeli `job_metric_mark`. ISTNIEJE `entity_event` — ledger, nie metryka jobu. ISTNIEJE `rank_mark` — oś zakupu, nie M-72.

**Odrzucone:** scoring osoby · SQL job engine · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `311_job_metric_mark` RLS FORCE.
2. POST/GET `/job-metric-marks`. OpenFGA. Deny-list.
3. UI `/job-metric-marks`. How-to. `SHIPPED_CHARGE_ROUTES["401.0"]` + REQUIRED_BUSINESS_LIST_ROUTES.

## Kryteria akceptacji

- [ ] POST z kodem, metric_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/score → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
