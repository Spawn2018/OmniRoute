# Plaster 276.0 · CT1 `purchase_order` HITL (nagłówek, bez linii)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT1 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`purchase_order`, nie `po_line` / ASN w tym Q)
**Zależy od:** 0.3 tenancy · 272.0 `customer_contract` (wzorzec nagłówka HITL) · 275.0 `visibility_connector` (katalog unique)
**Status:** Delta przyjęta `/noc` 2026-09-10. Wolno `/plaster`. Nie `po_line`. Nie ASN. Nie auto `shipment`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CT1 / pin 2026-09-08c: katalog HITL nagłówka zamówienia zakupu jako dane; nie linia SKU i nie ASN):** leftover `po_line` (sku, qty, uom, plant, batch, serial, coo) parked — karta CT1, nie ten Q. leftover ASN / EDI 856 parked. leftover auto-create `shipment` (U1) parked — `field_carry_forward` już przenosi pola oferty→zlecenie, to **nie** jest PO. leftover CT8 AIS live parked (TO_VERIFY licencja). leftover CI1 extract parked. leftover live p44 parked (275.0 = fixture katalogu). leftover qty jako float parked na stałe. Żywy plaster: **nowa tabela** `purchase_order` per tenant — HITL katalog nagłówka zamówienia zakupu jako dane, nie linia i nie zlecenie. GLOSSARY: **zamówienie zakupu** = `purchase_order` (nie `po_helper`, nie kolumny na `shipment`, nie `field_carry_forward`). Wiersz = `po_code` (snake 2–32) + opcjonalny `plant_label` (tekst 1–128 albo brak; etykieta, **nie** FK geography/`party`) + `source_ref` (`tenant:manual` albo `fixture://purchase-order/…`). Zero kolumn SKU / qty / uom / batch / serial / coo / ASN / `shipment_id` / kwoty. Operator wpisuje kod i opcjonalny zakład; serwis nie woła HTTP, nie woła LLM, nie importuje `shipments` / `charges` / `field_carry_forwards` / `extraction` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zła etykieta zakładu → 400 (`zakład`). Obce `source_ref` → 400 (`obce`). Body z linią/ASN/kwotą (`sku`, `qty`, `quantity`, `uom`, `asn`, `shipment_id`, `amount`) → 422 (`extra=forbid`). Unique `(organization_id, po_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/purchase-orders`. `SHIPPED_CHARGE_ROUTES["276.0"]` = `/purchase-orders`. OpenFGA: `can_manage_purchase_orders`. Migracja `209_purchase_order` (po `208_visibility_connector`). Job OPS 121. Deny-list: `extraction-no-rates` dostaje `app.services.purchase_orders` / `app.models.purchase_order`. Marża zostaje na `charge`. To nie jest ASN i nie jest live widoczność.

**Łowca:** BRAK tabeli `purchase_order`. BRAK `po_line` / `po_code` / ASN w schemacie i w GLOSSARY — ten plan dopisał **zamówienie zakupu**. ISTNIEJE `shipment` 35.0 / 90.0 — zlecenie z wyceny, **nie** PO (nie rozszerzać, nie FK w tym Q). ISTNIEJE `field_carry_forward` U1 — snapshot pola oferta→zlecenie, **nie** zamówienie zakupu. ISTNIEJE `customer_contract` 272.0 — nagłówek HITL, **inny** job. ISTNIEJE `visibility_connector` 275.0 / konektory 268.0–271.0 — katalog HITL, **inny** job. ISTNIEJE `edi_message` 32.0 — komunikat na zleceniu, **nie** ASN 856. ISTNIEJE `booking_instruction` I4 — scope bookingu, **nie** PO. PODOBNE: kod + opcjonalna etykieta + `source_ref` jak CI9 nagłówek. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- `po_line` / SKU / qty / uom / batch / serial / coo w tym Q.
- ASN / EDI 856 / auto-create `shipment` / zapis `field_carry_forward`.
- CT8 AIS live / kongestia / live p44 / CI1 extract LLM.
- qty jako float / kwota / marża / Decimal pieniężny.
- FK do `shipment` / `party` / geography; import shipments/charges/field_carry_forwards/extraction.
- HTTP / mapa / live widoczność / UPDATE/DELETE.

## Zakres

1. Migracja `209_purchase_order` po `208_visibility_connector`: RLS FORCE, izolacja, unique kod + `source_ref`. Zero kolumn linii / ASN / kwoty / `shipment_id`.
2. POST/GET. Zły token → 400 (`oznaczenie` / `zakład` / `obce`). Body z linią/ASN/kwotą → 422. Serwis nie importuje shipments/charges/field_carry_forwards/extraction i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`. ExtractionService nie importuje tego BC (`extraction-no-rates`).
3. UI `/purchase-orders`. How-to `docs/operator/zamowienie-zakupu.md`; fold w `katalogi.md` (40 linii). Job OPS 121. OpenFGA `can_manage_purchase_orders`. Nazwy: `PurchaseOrderDesk` / `purchase-order-form.tsx` / `persistPurchaseOrder`. Brak przycisku „Utwórz zlecenie” / ASN / SKU.

## Poza zakresem

`po_line` · ASN · EDI 856 · auto `shipment` · CT8 AIS · live p44 · CI1 extract · kwota · float · FK shipment

## Kryteria akceptacji

- [ ] POST zapisuje `purchase_order` z kodem, opcjonalnym `plant_label` i `source_ref`, bez linii i bez ASN
- [ ] zły kod → 400 (`oznaczenie`); zła etykieta zakładu → 400 (`zakład`); obce `source_ref` → 400 (`obce`)
- [ ] POST z `sku` / `qty` / `asn` / `shipment_id` / `amount` **nie** zapisuje linii (422)
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] ExtractionService / `extraction-no-rates` nie importuje `purchase_orders`
- [ ] Zero float / marży / live HTTP / importu shipments/charges/field_carry_forwards/extraction
- [ ] Zero twierdzenia, że ASN albo live widoczność istnieje

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | POST/GET HITL; 400 tokenów PL; 422 na linię/ASN; RLS + unique kod/`source_ref` |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | | lista po `organization_id`; izolacja SELECT pod RLS |
| N+1 | | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | | nowa tabela unique; leftover `po_line` / ASN |
| Proza operatora | | `zamowienie-zakupu.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | | CURRENT, PLAN CT1, GLOSSARY, MODULES, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate jest dowodem |
| Minuty plastra | N/A | — |
| Bench rzemiosła | | `factory_cycle.py --close` |

## Implementacja (276.0)

- Migracja `209_purchase_order` po `208_visibility_connector`. RLS FORCE.
- `PurchaseOrderService.persist_purchase_order` — katalog unique, bez importu shipments/charges/field_carry_forwards/extraction.
- UI `/purchase-orders`. `SHIPPED_CHARGE_ROUTES["276.0"]` = `/purchase-orders`. Job OPS 121.

## Leftover po 276.0

CT1 `po_line` (sku, qty Decimal, uom, plant, batch, serial, coo). ASN / EDI 856. Auto `shipment` (U1). CT8 AIS live (TO_VERIFY licencja). Live p44. CI1 extract. `wrapped_dek` / KMS.
