# Plaster 410.0 · EXP3.3c `three_way_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `three_way_mark` per tenant: `mark_code` + `way_kind`
`buyer`|`seller`|`carrier`|`other` + `source_ref` (`tenant:manual` | `fixture://three-way-mark/…`).

Klon wzorca `line_impact_mark`:
- migracja `320` (revises `319_line_impact_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_three_way_marks`
- UI `/three-way-marks`, business-lists + ops-index `SHIPPED["410.0"]`
- operator `docs/operator/three-way-mark.md`
- izolacja RLS; POST `amount`/`tuple`/`score` → 422

Znacznik 3-way (buyer/seller/carrier) jako dana. Nie tuple OpenFGA per strona (CT11 leftover).

## Poza zakresem

tuple OpenFGA per strona · wspólny SELECT · kwota · live Auth0

## Kryteria

- [ ] POST z kodem, way_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/tuple → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

tuple per strona · wspólny SELECT cross-shipper
