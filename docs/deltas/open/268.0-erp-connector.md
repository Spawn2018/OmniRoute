# Plaster 268.0 · F9 `erp_connector` HITL (Optima fixture)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Oś leftoverów `/noc` · [erp-fk-adapter.md](../../analysis/erp-fk-adapter.md) (tylko Comarch Optima) · [konkurencja-funkcje-2026-09.md](../../analysis/konkurencja-funkcje-2026-09.md) (Optima fixture → XL → …) · [api-dostawy-operatora.md](../../ops/api-dostawy-operatora.md) (jeden plaster F9 = Optima fixture)
**Zależy od:** 0.3 tenancy · 197.0 `telematics_connector` (wzorzec katalogu adaptera, nie poll)
**Status:** Delta accepted `/noc` 2026-09-10. Wolno `/plaster`. Nie live SOAP/WebAPI. Nie sekrety. Nie XL. Nie G2.22.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover F9 Optima fixture; nie F9.1 M-58–M-60 / S56–S58):** leftover T8 parked. leftover G2.22 P/lista parked. leftover XL / Symfonia / nexo / GT / enova / WAPRO parked (łańcuch po tym leftoverze). leftover live SOAP/WebAPI / agent on-prem parked (brak sekretu i testu live). leftover `purchase_invoice` / FS+FZ / KSeF / CAMT parked. leftover `erp_series_map` / `erp_export` / `credential_ciphertext` / `base_url` parked. Żywy plaster: **nowa tabela** `erp_connector` per tenant — HITL katalog konektora Comarch Optima jako dane, nie agent i nie posting. Wiersz = `connector_code` (snake 2–32) + `system_kind` (`optima` — jedyny token w tym Q) + `source_ref` (`tenant:manual` albo `fixture://optima/…`). GLOSSARY: **konektor ERP** = `erp_connector` (obok `telematics_connector`; nie `bookkeeping`, bo to para `charge`+`sales_invoice`). Operator wpisuje kod i kind Optima; serwis nie woła HTTP, nie woła SOAP, nie woła LLM, nie czyta `charge` / `sales_invoice` / `bookkeeping` / `party` i nie liczy marży. Zero kolumn sekretu / URL / ciphertext. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły kind → 400 (`system`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, connector_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/erp-connectors`. `SHIPPED_CHARGE_ROUTES["268.0"]` = `/erp-connectors`. OpenFGA: `can_manage_erp_connectors`. Migracja `201_erp_connector` (po `200_lane_km`). Job OPS 114. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `erp_connector`. BRAK `optima` w `backend/` i `frontend/src`. BRAK nazwy w GLOSSARY. ISTNIEJE `telematics_connector` 197.0 — katalog adaptera GPS (reżim + dostawca), **nie** ERP. ISTNIEJE `bookkeeping` 104.0 — para `charge`+`sales_invoice`, **nie** konektor FK. ISTNIEJE `sales_invoice` / `ksef_ref` — numer sesji Omni, **nie** export FS. ISTNIEJE `terminal_slot_connector` w GLOSSARY (T8) — **nie** ten leftover. Analiza §6.1 `erp_connector` ma pięć brandów + ciphertext + URL — ten plaster tnie do tokenu `optima` i bez sekretów.

**Odrzucone:**

- live SOAP / WebAPI / agent on-prem / ping.
- `credential_ciphertext` / `base_url` / kolumny sekretu.
- SQL `sa` do MSSQL klienta.
- XL / nexo / GT / Symfonia / enova / WAPRO w CHECK (łańcuch po leftoverze).
- `purchase_invoice` / FS+FZ posting / KSeF / CAMT.
- F9.1 M-58–M-60 (bez żywej nazwy).
- G2.22 P/lista. T8. Auth0 / portale.
- import charges / sales_invoices / bookkeeping / parties.
- kwota / marża / float / T-SQL / scrape / LLM.

## Zakres

1. Migracja `201_erp_connector`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK `system_kind = 'optima'`. Zero kolumn sekretu / URL / kwoty.
2. POST/GET. Zły token → 400 (`oznaczenie` / `system` / `obce`). Serwis nie importuje charges/sales_invoices/bookkeeping/parties i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/erp-connectors`. How-to `docs/operator/erp-optima.md`; fold w `katalogi.md` (40 linii). Job OPS 114. OpenFGA `can_manage_erp_connectors`. Nazwy: `ErpConnectorDesk` / `erp-connector-form.tsx` / `persistErpConnector`.

## Poza zakresem

live SOAP · XL/nexo/GT/Symfonia · SQL `sa` · FS+FZ · purchase_invoice · sekrety · T8 · G2.22 · F9.1 · kwota · float

## Kryteria akceptacji

- [ ] POST zapisuje `erp_connector` z kodem, kind `optima` i `source_ref`, bez HTTP
- [ ] zły kod → 400 (`oznaczenie`); zły kind (m.in. `xl`) → 400 (`system`); obce `source_ref` → 400 (`obce`)
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] Zero float / marży / ciphertext / live HTTP / importu charges/sales_invoices/bookkeeping/parties

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | INSERT/GET katalog, nie ścieżka z budżetu AGENTS |
| SQL | EXPLAIN | lista po `organization_id` (indeks/unique) |
| N+1 | OK | GET = jeden SELECT; POST = jeden INSERT |
| Dług / człowiek | — | leftover XL / live / FS+FZ / T8 |
| Proza operatora | 5–15 zdań | `erp-optima.md`; fold w `katalogi.md` (40 linii) |
| Docs/OS | — | CURRENT, PLAN F9, GLOSSARY, MODULES F9, spec |
| Pushy do zielonego | N/A | GitHub billing; lokalny gate |
| Minuty plastra | N/A | — |
| Bench rzemiosła | — | `factory_cycle.py --close` |

## Implementacja (268.0)

- Migracja `201_erp_connector` po `200_lane_km`. RLS FORCE.
- `ErpConnectorService.persist_erp_connector` — katalog unique, bez importu charges/sales_invoices/bookkeeping/parties.
- UI `/erp-connectors`. `SHIPPED_CHARGE_ROUTES["268.0"]` = `/erp-connectors`. Job OPS 114.

## Leftover po 268.0

XL → Symfonia → nexo → GT → enova/WAPRO. live SOAP/agent. `purchase_invoice` / FS+FZ. sekrety. T8 (następny na osi `/noc`). G2.22 P/lista.
