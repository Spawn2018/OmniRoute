# Plaster 327.0 · G6 `load_plan_mark` HITL (katalog, bez solvera OR)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G6 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie OR. Nie LLM-VRP.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G6 — tylko HITL katalog `load_plan_mark` jako dane. `mark_code` + `stance_kind` (`axes` | `tunnel` | `other`) + `source_ref`. UI `/load-plan-marks`. OpenFGA `can_manage_load_plan_marks`. Migracja `239_load_plan_mark`. Job OPS. Deny-list extract. Solver OR / osie Decimal / ADR tunel egzekucja / klej T2+M-52 = leftover.

**Łowca:** BRAK tabeli `load_plan_mark`. ISTNIEJE `oog_mark` — eskort, nie plan załadunku. ISTNIEJE `dangerous_good` — IMDG, nie tunel ADR.

**Odrzucone:** solver OR · LLM-VRP · osie Decimal · egzekucja tunelu · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `239_load_plan_mark` RLS FORCE.
2. POST/GET `/load-plan-marks`. OpenFGA. Deny-list.
3. UI `/load-plan-marks`. How-to. `SHIPPED_CHARGE_ROUTES["327.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, stance_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/axle → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
