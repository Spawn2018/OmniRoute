# Plaster 89.0 · S27 sankcje HTTP na party (M-53)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S27  
**Zależy od:** 45.0 tablica `/sanctions` · 5.0 `party` (nie nadpisuj `credit_limit`)

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-53**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** HTTP = **zapis sprawdzenia** na `party`, nie live lista i nie auto-match. Operator podaje URI listy, którą już ma (`fixture://sanctions/` albo `eu://`). Serwis ustawia `sanctions_list_ref` + `sanctions_checked_at`. Wzorzec jak ingest Graph (S15) i jak `vies_checked_at`. Tablica 45.0 zostaje. Bez nowej tabeli hitów.

**Łowca:** ISTNIEJE 45.0 `/sanctions` + `party.tax_id`/`country_code` + `vies_checked_at`/`gus_synced_at` + `PARTY_LOOKUP_BACKEND=fixture`. BRAK kolumny sprawdzenia sankcji. PODOBNE: wskazanie raportu 88.0 (URI, nie blob); lookup GUS (szkic, nie zapis siecią).

**Odrzucone:**

- Live HTTP OFAC/EU/UN (teatr bez umowy; S21 ten sam zakaz; sekrety).
- Tabela hitów i auto-match / auto-blokada.
- S27b karta M-13 z won/lost.
- Scoring osoby / `legal_form` / zmiana `credit_limit` / `is_active`.
- Nowy BC. F9.1. Zlecenie (S28).

## Zakres

1. Migracja `048_party_sanctions_screen` (po `047_review_bureau_ref`): `sanctions_list_ref` TEXT NULL, `sanctions_checked_at` timestamptz NULL na `party`. RLS z 015.
2. `POST /parties/{id}/screen-sanctions` — `{sanctions_list_ref}`. OpenFGA `can_manage_parties`. `PartyService`.
3. UI `/sanctions`: „Zapisz sprawdzenie”. `SHIPPED_CHARGE_ROUTES["89.0"]` = `/sanctions`. Asercja 45.0 bez `ofac` w stronie zostaje.

## Poza zakresem

Live lista · auto-match · tabela hitów · S27b · `credit_limit` · F9.1 · S28

## Ustalenia

- Token: `sanctions`. How-to: `kontrahent.md`.
- Prefix URI: `fixture://sanctions/` albo `eu://`. Pusty po trim = 400. Max 256. Brak `httpx`/`requests` w serwisie.
- `party.source_ref` nietknięty (pochodzenie karty, nie lista).
- UI nie zawiera stringu `ofac` (test 45.0).

## Schemat

`party.sanctions_list_ref` TEXT NULL. `party.sanctions_checked_at` timestamptz NULL.

## Kryteria akceptacji

- [ ] Kolumny bez score i bez `credit_limit`
- [ ] Pusty albo zły prefix URI → 400
- [ ] Serwis nie woła sieci; brak `risk_score` na `/parties` i `/sanctions`
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
