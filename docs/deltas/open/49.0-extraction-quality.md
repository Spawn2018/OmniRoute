# Plaster 49.0 · M-69 Jakość (`extraction_quality`)

**Spec źródłowa:** [docs/spec/extraction-quality.md](../../spec/extraction-quality.md)  
**Zależy od:** 0.7 `extraction_draft.payload.unparsed_regions`

**Status:** Otwarty `/noc` 2026-09-02. Nie Fala 10. LLM nie liczy. Nie nowa tabela. Nie scoring.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 11 M-69):** jakość **nie ma tabeli defektów**. Operator widzi szkice pending, których `unparsed_regions` nie jest puste (GLOSSARY: region nierozpoznany). Token `extraction_quality` (UI, nie tabela). Żywy ID **M-69**. 47.0 zostaje listą pending; 0.7 zostaje HITL. Nie `ab_delta_chars` w JS jako ocena.

**Łowca:** BRAK `quality` / `defect` / `qa_score`. ISTNIEJE: `fetchExtractionDrafts`, `unparsed_regions`. PODOBNE: 47.0 `/ai` (pending bez regionów). Postgres: zero tabel jakości.

**Odrzucone:**

- Tabela scoringu / defektów — zgadywanie schematu (CURRENT).
- Liczenie `ab_delta_chars` jako ocena — LLM/JS nie liczy jakości.
- Accept z tej tablicy.
- Pokazywanie `input_text`.

## Zakres

Read-only `/quality`. `data-extraction-quality="board"`. Brak migracji. `qualityGaps(drafts)`. Link do `/extractions` i `/ai`.

## Poza zakresem

Tabela QA · scoring · promptfoo live · ExtractionService zapis rates

## Ustalenia

- Token: `extraction_quality` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia extractions.
- RLS: bez nowej tabeli.
- Job: `/quality`. `SHIPPED_CHARGE_ROUTES["49.0"]` = `/quality`. OPS_JOBS +1 → 46.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 49.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/quality` ma `data-extraction-quality="board"` i woła `qualityGaps`
- [ ] Filtr niepustych `unparsed_regions`; brak `input_text` na ekranie
- [ ] Brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["49.0"]` = `/quality`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | scoring / tabela QA |
| Docs/OS | — | po zamknięciu |

## Knowledge

Nie RAG. Nie silnik scoringu.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Scoring leftover.
