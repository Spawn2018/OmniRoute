# Plaster 536.0 · O8 leftover — rfc822 Message-ID / In-Reply-To na `inbound_message`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O8 leftover rfc822 · [533.0](../archived/533.0-buy-desk-groupby.md) · poczta [142.0](../archived/142.0-mail-group-by.md)

**Zależy od:** `inbound_message` (M-32) · buy-desk O8 bez wątku mailowego · `external_id` już jest (nie mylić z Message-ID)

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover O8. Opcjonalne HITL pola tekstowe na istniejącej `inbound_message`: `rfc822_message_id` + `in_reply_to` (String, trim, pusty → null). Operator wpisuje nagłówki; **nie** parser Graph/IMAP, **nie** drugi czat, **nie** group-by thread z tych pól w tym plasterze (thread desk zostaje POL/POD z 533.0).
- **Wybrane:** migracja `421_inbound_rfc822` po `420_trip_bill_mark`. OpenFGA bez nowej relacji. UI `/mail` — dwa opcjonalne pola w formularzu. `SHIPPED["536.0"]` → `/mail`.
- **Odrzucone:** live Graph HTTP · auto-fill z bajtów · FK self-thread · nowa tabela wątków (`REJECTED`).
- **Odrzucone:** zmiana `group_by=thread` buy-desk na rfc822 (`REJECTED` na tym wierszu — leftover osobny).
- **Odrzucone:** float / marża / Temporal (`REJECTED`).

**Łowca:** ISTNIEJE `inbound_message.external_id` (idempotencja skrzynki). ISTNIEJE UI `/mail`. BRAK kolumn Message-ID / In-Reply-To. PODOBNE: opcjonalne String jak `pickup_terminal` / HITL etykiety — nie nowa SoR.

## Zakres

1. Migracja RLS-safe: dwie kolumny nullable + ewentualny indeks `(organization_id, rfc822_message_id)` WHERE NOT NULL.
2. Domena + model + serwis + API create/list/response.
3. FE `/mail` pola opcjonalne.
4. Testy: izolacja · HTTP · contracts · vitest powierzchni.
5. How-to fold.

## Poza zakresem

live Graph · parser MIME · buy-desk group-by po Message-ID · Temporal · auto-thread SQL

## Kryteria akceptacji

- [ ] POST bez pól → null; z polami → zapis trim
- [ ] pusty string → null; zły token (za długi) → 400 PL
- [ ] zero live HTTP / float / marży
- [ ] test izolacji RLS; SHIPPED `536.0` → `/mail`

## Leftover

group-by buy-desk po rfc822 · live Graph fill · self-FK wątku
