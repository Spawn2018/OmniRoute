# Plaster 76.0 · S13 draft maila obok extract (M-57)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S13  
**Zależy od:** 0.3 RLS · 0.4 OpenFGA · 47.0 tablica `/ai` · 74.0 `operator_decision`

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID M-57. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-57**. Nowa tabela `mail_draft` z RLS FORCE + test izolacji. Wiersz to szkic **wychodzącej** odpowiedzi obok extractu — nie czat, nie drugi `rate_line`, nie `inbound_message.status = draft`. `subject_kind` na start tylko `extraction_draft`, `subject_id` UUID **bez FK**. `status` na start tylko `draft`. Create = `draft`. `source_ref` obowiązkowy. Treść pisze operator — LLM nie liczy i nie składa body. Accept przez **S11**: CHECK `operator_decision.subject_kind` dopuszcza też `mail_draft`; `subject_id` = `mail_draft.id`. Werdykt zostaje na `/decisions` (pending + accept/reject). OpenFGA: nowa relacja `can_manage_mail_drafts` = member. Ekran: ten sam `/ai` — tablica 47.0 zostaje + lista/utwórz `mail_draft`. Serwis `mail_drafts` nie importuje extraction / inbound / quotations / operator_decisions / rate_lines. Nie send. Nie outbox.

**Łowca:** BRAK tabeli `mail_draft` / `outbound_message` w backendzie. ISTNIEJE: 47.0 `/ai` = filtr `extraction_draft` pending (`aiProposals`) — odczyt, nie szkic maila. ISTNIEJE: 64.0 `inbound_message` ze statusem `draft` — przychodzące fixture, nie wychodzący szkic. ISTNIEJE: 74.0 `operator_decision` — szyna werdyktu, `subject_kind` tylko `inbound_message`. ISTNIEJE: accept extractu 1.3 → `rate_line` na `/extractions`. PODOBNE: `/mail` to ingest + HITL z inbound; `operator_notice` to inbox, nie compose.

**Odrzucone:**

- Tabela czatu / wątku / MCP (żywe M-57 zostaje; S56 = pogłębienie, nie nowy czat).
- Accept extractu 1.3 i drugi `rate_line` z tej tabeli.
- Kolumna na `inbound_message` (to przychodzące; S13 = wychodzący szkic).
- FK do `extraction_draft` / `inbound_message` (serwis nie woła innych BC).
- Send / Graph / SMTP / `mailto:` (S18).
- Outbox / M-02 (S16).
- Zapis `changed` na decyzji (leftover S14).
- Auto-skład body z payloadu extractu (LLM nie pisze treści).
- Nowy żywy ID (M-57 już jest).
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `036_mail_draft_rls`: tabela + RLS FORCE. Test izolacji. CHECK `operator_decision.subject_kind` ∈ (`inbound_message`, `mail_draft`).
2. Serwis create + list. `GET/POST /mail-drafts`. OpenFGA nowa relacja.
3. UI `/ai`: lista zapisanych, utwórz draft przy `extraction_draft` UUID, zgłoś pending na `/operator-decisions`. Werdykt na `/decisions`. Nie licz w JS.

## Poza zakresem

Czat · send · outbox · `changed` · lock S14 · accept extractu · F9.1

## Ustalenia

- Żywy ID: **M-57**. Token: `mail_draft` (tabela). Token UI 47.0 `ai_copilot` zostaje na tablicy pending.
- HITL extract zostaje na `/extractions`. LLM nie liczy.
- How-to: `docs/operator/katalogi.md`.

## Schemat

`mail_draft`: `id`, `organization_id`, `subject_kind`, `subject_id`, `body`, `status`, `source_ref`, timestamps. CHECK kind/status.

## Kryteria akceptacji

- [ ] RLS FORCE + izolacja
- [ ] Create = draft; B nie widzi A
- [ ] `can_manage_mail_drafts`; 403 bez tuple
- [ ] `operator_decision` przyjmuje `subject_kind=mail_draft`
- [ ] UI `/ai` zapisuje wiersz tabeli; tablica 47.0 zostaje
- [ ] Serwis nie importuje extraction / inbound / quotations / operator_decisions
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
