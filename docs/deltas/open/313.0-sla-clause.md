# Plaster 313.0 · CI1 `sla_clause` HITL (klauzula, bez ciphertext / extract)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CI / CI1 · [karty-pol-fala-ci.md](../../analysis/karty-pol-fala-ci.md) · [GLOSSARY.md](../../GLOSSARY.md) klauzula SLA
**Zależy od:** 0.3 tenancy · 272.0 `customer_contract` (CI9 nagłówek)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie extract. Nie kara SQL. Nie X1 portal.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CI1 / pin 2026-09-08c po wyczerpaniu label-form SourceRef):** leftover X1–X5 portal parked (po S53+T2; nie ten Q — oś pinu ma CI1 zaraz po CI9). leftover upload PDF / Langfuse parked. leftover extract LLM parked (zakaz AI na umowach). leftover `penalty_ciphertext` / `obligation_ciphertext` parked (jak CI9 blob — najpierw wpis ręczny bez szyfru). leftover CI5 kara SQL / CI2 spend / CT2 parked. Żywy plaster: **nowa tabela** `sla_clause` per tenant — HITL klauzula SLA jako dane na istniejącej umowie, nie treść PDF i nie kara. GLOSSARY: **klauzula SLA** = `sla_clause`. Wiersz = `clause_code` (snake 2–32) + `customer_contract_id` (FK ten sam tenant) + `metric_kind` (`otif`|`delay`|`damage`|`other`) + `threshold_label` (tekst 1–128, próg jako dana, **nie** liczenie) + `source_ref`. Zero kolumn ciphertext / kwoty / kary Decimal. Operator wpisuje kod, wybiera umowę i metrykę; serwis nie woła HTTP, nie woła LLM, nie importuje `charges` / `extraction` / `tower_impacts` i nie liczy kary. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. UI `/sla-clauses`. OpenFGA `can_manage_sla_clauses`. Migracja `225_sla_clause` (po `224_visibility_connector_vendors`). Job OPS 128. Deny-list extract. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `sla_clause`. BRAK serwisu `sla_clauses`. ISTNIEJE `customer_contract` 272–273 — nagłówek umowy, **nie** klauzula. ISTNIEJE `tower_impact.contract_data_status` token `sla_clause` — status „brak danych”, **nie** tabela klauzul. ISTNIEJE `party_document` — dokument na party, nie SLA. PODOBNE: kod + kind + source_ref + FK jak asn na purchase_order. Nie wyodrębniać wspólnej warstwy.

**Odrzucone:**

- Extract LLM / upload PDF / Langfuse na umowach.
- `penalty_ciphertext` / `obligation_ciphertext` / KMS / unwrap.
- CI5 kara SQL / CI2 spend leakage / CT2 impact chain.
- X1 portal SPA / OAuth2.
- Kwota / marża / float / live HTTP / UPDATE/DELETE.

## Zakres

1. Migracja `225_sla_clause`: RLS FORCE, FK do `customer_contract`, unique kod + source_ref.
2. POST/GET `/sla-clauses`. OpenFGA. Deny-list extract. 400 tokenów PL. 422 na ciphertext/kwotę.
3. UI `/sla-clauses`. How-to. Job OPS 128. `SHIPPED_CHARGE_ROUTES["313.0"]`.

## Poza zakresem

extract · upload · ciphertext · kara SQL · X1 · CT2 · kwota · float

## Kryteria akceptacji

- [ ] POST z clause_code, customer_contract_id, metric_kind, threshold_label, source_ref
- [ ] zły kod → 400 (`oznaczenie`); zły kind → 400 (`metryka`); zły próg → 400 (`prog`); obce source_ref → 400 (`obce`); obca umowa → 404/400
- [ ] POST z amount / penalty_ciphertext → 422
- [ ] izolacja RLS; ExtractionService nie importuje BC
- [ ] Zero float / marży / HTTP / extract
