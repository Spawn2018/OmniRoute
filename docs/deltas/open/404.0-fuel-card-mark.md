# Plaster 404.0 · EXP2.14 `fuel_card_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `fuel_card_mark` per tenant: `mark_code` + `card_kind`
`fuel`|`anomaly`|`other` + `source_ref` (`tenant:manual` | `fixture://fuel-card-mark/…`).

Klon wzorca `impersonate_guard_mark`:
- migracja `314` (revises `313_impersonate_guard_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_fuel_card_marks`
- UI `/fuel-card-marks`, business-lists + ops-index `SHIPPED["404.0"]`
- operator `docs/operator/fuel-card-mark.md`
- izolacja RLS; POST `amount`/`liters`/`score` → 422

Znacznik karty paliwowej / anomalia jako dana. Bez live fleetu, bez SQL anomalia.

## Poza zakresem

live fuel card API · litry · kwota · anomaly SQL · TCO · CMMS

## Kryteria

- [ ] POST z kodem, card_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/liters → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live fuel card · anomaly engine · litry/kwota
