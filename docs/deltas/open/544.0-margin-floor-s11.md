# Plaster 544.0 · N6 leftover — S11 przy breach `margin_floor`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § N6 leftover · [539.0](../archived/539.0-margin-floor-409.md) · [527.0](../archived/527.0-margin-floor.md)

**Zależy od:** `margin_floor` HITL (527.0) · 409 API (539.0) · `operator_decision` M-71 (74.0 / 121.0)

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover N6 S11. Przy `POST /charges` z parą UN/LOCODE, gdy marża < `margin_floor`: API **tworzy albo wznawia** pending `operator_decision` (`subject_kind=margin_floor`, `subject_id=floor.id`, `source_ref` z body) i nadal zwraca **409** z `decision_id` w treści — operator widzi sprawę na `/decisions` (szyna S11), nie tylko ślepy konflikt.
- **Wybrane:** opcjonalne `floor_decision_id` na body. Gdy wskazuje **accepted** decyzję dla tego samego `margin_floor.id` — pomija `require_margin_above_floor` i zapisuje `charge`. Retry po Akceptuj na `/decisions`.
- **Wybrane:** migracja CHECK `subject_kind` + domena `_SUBJECTS`; compose w `charges.py` (jak 539), nie w `ChargeService` / `OperatorDecisionService` (HC granic BC).
- **Odrzucone:** auto INSERT `charge` przy accept decyzji (`REJECTED` — serwis S11 nie importuje charges). Usunięcie 409 bez ścieżki override (`REJECTED` — łamie kontrakt 539). Mail/Graph/S18 (`REJECTED`). Matching fuzzy / Haversine (`REJECTED`). Kolumny lane na `charge` (`REJECTED`). Float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload (żywy OpenAI CI = park), Plat-HD-flow (osobny Q), G0-SH (host/IdP), SH-R16-13 (Dependabot — zakaz w `/noc`).

**Łowca:** ISTNIEJE 409 `MarginFloorBreach` · ISTNIEJE `operator_decision` + `has_accepted` · ISTNIEJE `/decisions`. BRAK `subject_kind=margin_floor`. BRAK `floor_decision_id` / decyzji przy breach.

## Zakres

1. Alembic: rozszerzenie CHECK `ck_operator_decision_subject_kind` o `margin_floor` (model + domena).
2. API create charge: breach → pending decision + 409 z `decision_id`; accepted `floor_decision_id` → INSERT.
3. Testy HTTP: 409 z decision_id · retry po accept → 201 · zły/pending decision_id → 409/400 · regresja 539 bez pary.
4. How-to `margin-floor.md` (zamiast „S11 leftover”).
5. `SHIPPED` zostaje na `/charges` (539) — bez nowej trasy FE obowiązkowej; lista `/decisions` już czyta pending.

## Poza zakresem

auto-create charge na accept · matching lane · UI pól UN na `/charges` · mail · Plat-HD · AI3-payload · G0-SH

## Kryteria akceptacji

- [ ] POST poniżej podłogi → 409 + pending `operator_decision` (`margin_floor`) + `decision_id` w odpowiedzi
- [ ] drugi POST poniżej podłogi (ten sam floor) → ten sam pending (unikat), 409
- [ ] POST z `floor_decision_id` accepted → 201 poniżej podłogi
- [ ] POST z pending / obcym / rejected id → odmowa (bez INSERT)
- [ ] zero float / auto charge z serwisu decyzji / matching

## Leftover

pola UN na UI `/charges` · matching lane na wycenie · auto charge po accept (park — granica BC)
