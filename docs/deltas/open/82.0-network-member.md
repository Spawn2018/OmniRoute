# Plaster 82.0 · S19 katalog network_member (M-12)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S19  
**Zależy od:** 9.0 `network` · 0.3 RLS · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-12**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-12**. Nowa tabela `network_member` — ręczny katalog agenta **w sieci tenanta**, nie scraping. `network_id` FK do `network` (RESTRICT). `member_code` + `legal_name` + `source_ref`. Unikat `(organization_id, network_id, member_code)`. `party_id` **nie** wchodzi (serwis networks nie importuje parties). RLS FORCE + test izolacji. OpenFGA: istniejące `can_manage_networks`. `GET/POST /networks/{network_id}/members`. Ekran `/networks`: lista i dodanie członka wybranej sieci. LLM nie zgaduje agenta.

**Łowca:** ISTNIEJE 9.0 `network` + `/networks`. BRAK tabeli `network_member`. PODOBNE: katalog `commodity_code` (kod + nazwa). Odrzucone w 9.0: seed tysięcy / WCA.

**Odrzucone:**

- Scraping WCA / Globalia / JCtrans (sui generis).
- `network_member_link` / RapidFuzz / dedupe.
- FK / import `party`.
- Import Excel/PDF.
- `network_membership` (numer członkowski tenanta) — leftover kolumn na `network`.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `042_network_member_rls`: tabela + RLS FORCE. Test izolacji.
2. Serwis list + create w BC `networks`. `GET/POST /networks/{network_id}/members`.
3. UI `/networks`: członkowie wybranej sieci. Nie licz w JS.

## Poza zakresem

Scraping · RapidFuzz · FK party · Excel · M-30 · F9.1

## Ustalenia

- Żywy ID: **M-12**. Token: `network_member`.
- How-to: `docs/operator/katalogi.md`.
- Scraping / link do party = leftover.

## Schemat

`network_member`: `id`, `organization_id`, `network_id`, `member_code`, `legal_name`, `source_ref`, timestamps.

## Kryteria akceptacji

- [ ] RLS FORCE + izolacja (B nie widzi członka A)
- [ ] Create w nieznanej sieci → 404; duplikat kodu w sieci → 400
- [ ] `can_manage_networks`; 403 bez tuple
- [ ] Serwis bez `httpx` / `requests` / `scrap`
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
