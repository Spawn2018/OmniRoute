# Plaster 113.0 · S51 reklamacja ładunku (M-55)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S51  
**Zależy od:** 90.0 `shipment` · 0.3 RLS · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-04. Żywy ID **M-55**. Token `cargo_claim`. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** nowa tabela `cargo_claim` per tenant na **zleceniu**. Operator na `/claims` zapisuje reklamację (`damage` / `shortage` / `other`) z `source_ref`. Wiele reklamacji na jedno `shipment`. API składa: odczyt `shipment`, zapis reklamacji. Serwis nie importuje `shipments` / `charges` / `quotations`. Żywa nazwa: `cargo_claim` (nie JWT `claim`, nie `claim_pending` szyny decyzji). `SHIPPED_CHARGE_ROUTES["113.0"]` = `/claims`. OPS_JOBS 50 → 51.

**Łowca:** ISTNIEJE 93.0 `operational_exception` (inny byt: wyjątek operacyjny). ISTNIEJE `claim_pending` na `operator_decision` (lock, nie reklamacja). BRAK tabeli reklamacji / cargo claim. PODOBNE: 93.0 / 92.0 zapis na `shipment`. Dwa joby: `/exceptions` vs `/claims`.

**Odrzucone:**

- Scoring osoby / fraud S52.
- Kwota / marża / float na wierszu.
- Auto-INSERT z wyjątku albo z faktury.
- Klasyfikacja przez LLM.
- Druga tabela szkody / ubezpieczenia.
- Flota S50 (parked).
- F9.1.

## Zakres

1. Migracja `069_cargo_claim_rls` (po `068_shipment_leg_ocean_lcl_kind`): tabela `cargo_claim` RLS ENABLE+FORCE; FK tenanta do `shipment`.
2. `GET/POST /cargo-claims` — OpenFGA `can_manage_cargo_claims` = member. POST: `{shipment_id, claim_kind, source_ref}`. Nieznane zlecenie → 404.
3. UI `/claims`: lista + zapis. `data-cargo-claim="board"`. `SHIPPED_CHARGE_ROUTES["113.0"]` = `/claims`. OPS_JOBS = 51.
4. Test izolacji. How-to `docs/operator/reklamacja.md`. `katalogi.md` ≤ 40 linii (wtopić w wyjątki).

## Poza zakresem

S52 oszustwo · kwota · ubezpieczenie · LLM · F9.1

## Ustalenia

- Token: `cargo_claim` (tabela). How-to: `reklamacja.md`.
- `claim_kind` CHECK: `damage` | `shortage` | `other`.
- `source_ref`: `fixture://cargo-claim/` albo `tenant:manual`. Max 256.
- Serwis bez `httpx`. ExtractionService nie importuje reklamacji.
- LLM nie klasyfikuje. `charge` zostaje prawdą o marży. HITL bez zmian.

## Schemat

`cargo_claim` (nowa): `id`, `organization_id`, `shipment_id`, `claim_kind`, `source_ref`, timestamps, `created_by`. FK tenanta do `shipment`. Indeks org+shipment.

## Kryteria akceptacji

- [ ] Tabela z `organization_id`, RLS FORCE, test izolacji
- [ ] Brak `amount` / leaflet
- [ ] Zły kind / puste źródło → 400; brak uprawnienia → 403
- [ ] `/claims` zapisuje i listuje; OPS_JOBS = 51; `katalogi.md` ≤ 40

## Pytania

Brak. `/noc` akceptuje deltę.
