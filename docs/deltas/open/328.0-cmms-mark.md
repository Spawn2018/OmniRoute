# Plaster 328.0 · G7 `cmms_mark` HITL (katalog, bez work_order)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G7 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie kara kierowcy. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G7 — tylko HITL katalog `cmms_mark` jako dane. `mark_code` + `work_kind` (`work_order` | `dtc` | `other`) + `source_ref`. UI `/cmms-marks`. OpenFGA `can_manage_cmms_marks`. Migracja `240_cmms_mark`. Job OPS. Deny-list extract. Tabela `work_order` / `dtc_code` z V5 / klej T2 / kara kierowcy = leftover.

**Łowca:** BRAK tabeli `cmms_mark`. ISTNIEJE `telematics_connector` (V5) — adapter GPS, nie DTC. ISTNIEJE `resource` — flota, nie CMMS.

**Odrzucone:** work_order silnik · dtc z V5 live · kara kierowcy · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `240_cmms_mark` RLS FORCE.
2. POST/GET `/cmms-marks`. OpenFGA. Deny-list.
3. UI `/cmms-marks`. How-to. `SHIPPED_CHARGE_ROUTES["328.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, work_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/penalty → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
