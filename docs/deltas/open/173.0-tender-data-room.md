# Plaster 173.0 · G2.4 `tender_data_room`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.4 · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) § Dalej `tender_data_room`
**Zależy od:** 169.0 `tender` (nagłówek już jest)
**Status:** Delta zaakceptowana `/noc` 2026-09-09. Nie auto-award. Nie T-SQL.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.4–G2.18):** jedna tabela `tender_data_room` per tenant — **pokój danych + NDA jako dane**, nie extract RFP, nie bajty i nie kwota. OpenFGA = uprawnienie katalogu `can_manage_tender_data_rooms`, nie tuple per plik. Wiersz = `tender_id` (FK tenanta do `tender`) + `nda_mark` (allowlista `signed`) + `source_ref` (`tenant:manual` albo `fixture://tender-data-room/…`). Jeden pokój na przetarg: unique `(organization_id, tender_id)`. Zły znacznik NDA → 400 (detail zawiera `nda`). Obce `source_ref` → 400 (`obce`). GET listy SQL. Serwis nie importuje tenders / tender_lots / tender_lanes / tender_rounds / quotations / charges. API składa odczyt nagłówka (`TenderService.get_board`). UI `/tender-data-rooms`. `SHIPPED_CHARGE_ROUTES["173.0"]` = `/tender-data-rooms`. Migracja `113_tender_data_room`. Marża zostaje na `charge`. Zero Money na tym ekranie.

**Łowca:** ISTNIEJE `tender` — nagłówek; **nie** dokładamy NDA jako kolumny tam. ISTNIEJE `shipment_document` — wskazanie dokumentu na zleceniu, nie pokój przetargu. ISTNIEJE `tender_lot` / `tender_lane` / `tender_round` — partia / korytarz / runda; **nie** NDA. ISTNIEJE OpenFGA `can_manage_*` na katalogach; **nie** ACL plików. BRAK `tender_data_room`. BRAK `/tender-data-rooms`.

**Odrzucone:**

- Bajty / S3 / skan / PDF (leftover: wskazanie, nie treść).
- Extract RFP HITL / matryca Excel (`tender_matrix_cell`) / playbook / four-eyes / win/loss / TED (leftover G2.5–G2.18).
- Tuple OpenFGA per dokument (leftover ACL).
- Auto-award.
- Kwota / marża na tym wierszu.
- Kolumny na `tender` / `tender_round`.
- Float / T-SQL / live HTTP / portal.

## Zakres

1. Migracja `113_tender_data_room`: RLS, izolacja, indeks `(organization_id, tender_id)`, unique `(organization_id, tender_id)`, CHECK `nda_mark = 'signed'`.
2. POST/GET. Zły `nda_mark` / `source_ref` → 400. API składa odczyt `tender`, serwis room nie importuje tenders.
3. UI `/tender-data-rooms`. `just api-types`. How-to `docs/operator/pokoj-danych.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `RoomDesk` / `ndaWrite` / `persistRoomMark`.

## Poza zakresem

G2.5–G2.18 extract/desk · G2.19–G2.22 · G2.23 · auto-award · bajty · ACL per plik · kwota na wierszu · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `tender_data_room` z `nda_mark=signed` na istniejącym `tender`, bez auto-award
- [ ] `nda_mark` spoza allowlisty → 400
- [ ] GET zwraca pokoje tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / auto-award / live HTTP / bajtów
