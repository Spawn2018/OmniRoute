# Plaster 500.0 · AI3.0 leftover PATCH quote/rfp

**Status:** Delta zaakceptowana `/noc 8` 2026-09-15 — wolno `/plaster` (kolejna sesja / kontynuacja nocy).

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI3.0 · leftover po [447.0](archived/447.0-extraction-draft-patch.md)

**Zależy od:** `extraction_draft` · `can_review_extractions`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** rozszerzyć istniejący `PATCH /api/v1/extractions/{draft_id}` na `draft_kind` `quotation` oraz `customer_rfq` (pending). Te same reguły co 447.0: podmiana `candidates` w payload, reszta bez zmian, 409 przy złym statusie/kind.
- **Wybrane:** testy HTTP + izolacja; how-to w `docs/operator/ekstrakcja.md` (dopisek). Bez nowej tabeli. Bez +1 OPS_JOB. `SHIPPED["500.0"]` → `/extractions`.
- **Odrzucone:** próg 70% pewności zbiorczej (`REJECTED` na tym plasterze) · live vision · AI3.1 historia wierszy · nowy endpoint (`REJECTED`).

**Łowca:** ISTNIEJE PATCH rate_line (447.0). BRAK quote/rfp w allowliście kind.

## Zakres

Dopisek operator. Testy czerwone→zielone.

## Poza zakresem

70% · vision · bbox · openpyxl · AI3.1

## Kryteria akceptacji

- [ ] PATCH pending `quotation` / `customer_rfq` podmienia `candidates`
- [ ] PATCH zły kind/status → 409; amount float → 422
- [ ] izolacja tenantów bez zmiany
- [ ] how-to dopisek

## Leftover

próg 70% · live vision · pełna historia PATCH
