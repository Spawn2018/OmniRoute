# Plaster 517.0 · AI7.0 leftover — `cost_category_mark` HITL

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI7.0 leftover · po [516.0](../archived/516.0-allocation-key.md)

**Zależy od:** 516.0 `allocation_key` · `cost_allocation_mark` (340.0) zostaje osobno

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `cost_category_marks`. Tabela `cost_category_mark`: `mark_code` + `category_kind` `direct|shared|allocated|overhead|capital|risk|other` + `source_ref`. Append-only INSERT. RLS + izolacja. Węższa opcja leftover AI7.0 — **6 kategorii PDF §13j** jako dane HITL (plus `other`), nie SQL i nie TCM.
- **Wybrane:** POST/GET `/api/v1/cost-category-marks`. OpenFGA `can_manage_cost_category_marks`. UI `/cost-category-marks`. `SHIPPED["517.0"]`.
- **Wybrane:** how-to `docs/operator/cost-category-mark.md`.
- **Odrzucone:** mutacja `cost_allocation_mark` / `allocation_key` (`REJECTED` — osobne BC: tryb EXP2.3 vs klucz vs kategoria).
- **Odrzucone:** allocation SQL / TRUE CONTRIBUTION MARGIN / druga marża (`REJECTED`).
- **Odrzucone:** 12 poziomów (`REJECTED` — leftover osobnego plastra).
- **Odrzucone:** AI7.1 Cyfrowy CFO / live ERP (`REJECTED`).
- **Odrzucone:** float / amount (`REJECTED`).

**Łowca:** ISTNIEJE `cost_allocation_mark` (direct|abc|shared|other). ISTNIEJE `allocation_key` (otwarty słownik). BRAK `cost_category_mark` z kategorią PDF.

## Zakres

HITL `cost_category_mark` — migracja + warstwy + OpenFGA + FE + how-to + SHIPPED 517.0.

## Poza zakresem

12 poziomów · SQL alokacji · TCM · Cyfrowy CFO · live ERP · kwota · UPDATE/DELETE

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] UI `/cost-category-marks`; `SHIPPED["517.0"]`
- [ ] craft_style; how-to

## Leftover

12 poziomów · silnik SQL · TCM · AI7.1 · live ERP
