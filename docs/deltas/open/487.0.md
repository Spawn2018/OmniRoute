# Plaster 487.0 · AI9.1 leftover ui-04 pewność per pole — katalog HITL

**Status:** Delta zaakceptowana `/noc 8` 2026-09-15 — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI9.1 leftover · makieta ui-04 (pasmowanie, nie przebudowa)

**Zależy od:** tenancy RLS · `automation_bias_mark` (482.0) · nie przebudowa `hitl-review-split` · nie float auto-accept

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `field_confidence_marks`. Tabela `field_confidence_mark`: `mark_code` + `band_kind` `green|yellow|orange|hold|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja leftover AI9.1 — katalog HITL **pasma pewności per pole** (zielone / żółte / pomarańcz / wstrzymaj), zgodny z makietą ui-04 (próg 0,7 jako **opis**, nie silnik), nie przebudowa splitu HITL i nie float w bazie.
- **Wybrane:** POST/GET `/api/v1/field-confidence-marks`. OpenFGA `can_manage_field_confidence_marks` (member). UI `/field-confidence-marks`. `SHIPPED["487.0"]` → `/field-confidence-marks`.
- **Wybrane:** Alembic `392_field_confidence_mark` (`down_revision = "391_quality_descent_mark"`).
- **Wybrane:** how-to `docs/operator/field-confidence-mark.md`.
- **Odrzucone:** przebudowa `hitl-review-split` / canvas ui-04 (`REJECTED` — CURRENT: katalog HITL; split już jest).
- **Odrzucone:** kolumna Numeric/float pewności, próg auto-accept, optimistic accept (`REJECTED` — HC-04 / AI Act; `confidence_text` zostaje tekstem na kandydacie).
- **Odrzucone:** FK do `extraction_draft` / `automation_bias_mark` (`REJECTED`).
- **Odrzucone:** scoring osoby · U-art50 przebudowa (`REJECTED` — label już DONE).

**Łowca:** ISTNIEJE `automation_bias_mark`. ISTNIEJE `confidence_text` (tekst na kandydacie). BRAK `field_confidence_mark`.

## Zakres

How-to + OPS_JOB. FE: `band_kind` jako `<select>` z jawnymi option (różne od segmented/radio wcześniejszych AI8).

## Poza zakresem

przebudowa ui-04 / split · float confidence · auto-accept · L3 write · program PDF AI9.2 · kwota

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

AI9.2 program zgodności PDF · silnik auto-zejścia · L3 write (park) · live preprocess X9
