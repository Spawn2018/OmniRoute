# Plaster 65.0 · S2 resolve_email na inbound_message (M-11 × M-32)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S2  
**Zależy od:** 64.0 `inbound_message` · 8.0 `resolve_email` · `party_email_domain`

**Status:** Delta zaakceptowana `/noc` 2026-09-02. Nie IMAP. Nie Graph. Nie send. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane:** operator na `/mail` dopina nadawcę wiadomości do istniejącego `party` przez **ten sam** `resolve_email` (domena po `@` → `party_email_domain`). Kolumna `party_id` nullable na `inbound_message`. API woła matcher, BC `inbound_messages` tylko zapisuje UUID — bez importu `app.services.parties`.

**Łowca:** ISTNIEJE: `PartyService.resolve_email`, GET/POST `/inbound-messages`, `/mail` fixture. PODOBNE: resolve na `/parties` i na tablicy adresów. BRAK: `party_id` na wiadomości; auto-dopięcie przy INSERT.

**Odrzucone:**

- Auto-`resolve_email` przy tworzeniu fixture (zapis bez kliknięcia).
- Import `parties` w `app.services.inbound_messages` (niezależność BC).
- Nowy matcher / druga tabela domen.
- IMAP, Graph, send, extract z treści (S3).
- INSERT `party_contact` z maila.
- F9.1.

## Zakres

1. Migracja: `inbound_message.party_id` UUID NULL, FK `party(id)`, RLS bez zmian.
2. `POST /inbound-messages/{id}/resolve-email` — `can_manage_inbound_messages`; 403 bez tuple.
3. `/mail`: przycisk na wierszu — dopnij nadawcę; pokaż `party_id` albo komunikat nieznanej domeny.
4. Test: wiadomość tenanta A nie dostaje `party` tenanta B; nieznana domena = błąd domenowy, nie INSERT.

## Poza zakresem

S3 extract · IMAP · Graph · send · `shipment` · outbox · auto-kontakt · LLM

## Ustalenia

- EN: `resolve_email` na `inbound_message`. PL: dopasowanie nadawcy.
- `party_id` puste = jeszcze nie dopięte. Nie zgadujemy nazwy.
- HITL extract bez zmian. Lookup GUS nie dotyczy.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

`inbound_message.party_id` (nowa, nullable). Brak nowej tabeli.

## Kryteria akceptacji

- [ ] `party_id` nullable, FK, test izolacji (wiadomość A ≠ party B)
- [ ] Resolve tylko na żądanie operatora; INSERT fixture bez `party_id`
- [ ] Brak importu `app.services.parties` w BC inbound_messages
- [ ] `/mail` pokazuje dopięcie; zero HTTP do Graph/IMAP
- [ ] Nieznana domena nie tworzy `party`

## Pytania

Brak. `/noc` akceptuje deltę.
