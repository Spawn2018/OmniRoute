# Plaster 620.0 · U3 leftover e-rates air na `channel_quote`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § U3 leftover · [154.0](../archived/154.0-air.md) · [13.0](../archived/13.0-channel-quote.md) · [karty-pol-fala-u.md](../../analysis/karty-pol-fala-u.md) § U3
**Zależy od:** M-19 `channel_quote` · `port.function_flags` z `airport`
**Status:** Otwarty — delta zaakceptowana w `/noc` 2026-09-22. Wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover e-rates IATA):** katalog `channel_quote` już jest (kwota Decimal + `source_ref`). Live IATA zostaje. Żywy plaster: **tryb transportu** na ofercie z kanału — kolumna `transport_mode` `air`|`other` (default `other`, wstecznie zgodne). Przy `air` API składa odczyt obu portów i wymaga `airport` w `function_flags` (jak 154.0 na nodze). Zły tryb → 400 (`tryb`). Port bez lotniska przy `air` → 400 (`lotnisko`). Unikat rozszerzony o `transport_mode` (ta sama para portów w tym samym dniu może mieć ocean i air osobno). Serwis `channel_quotes` nie importuje `shipment_legs`. UI `/channel-quotes` + zdanie i link z `/air`. `SHIPPED_CHARGE_ROUTES["620.0"]` = `/channel-quotes`. OpenFGA bez zmian. Zero float. Zero HTTP IATA. Marża zostaje na `charge`.

**REQUIREMENT:** karta U3 — „e-rates = `channel_quote`+`source_ref`”. Źródło: `docs/analysis/karty-pol-fala-u.md` § U3.

**Łowca:** ISTNIEJE `channel_quote` + POL/POD + amount Decimal. ISTNIEJE `airport` w flags. BRAK `transport_mode`. BRAK wymogu lotniska przy e-rate air.

**Odrzucone:**

- live HTTP IATA / scrape / cyfra kontrolna
- druga tabela e-rate / mark HITL zamiast `channel_quote`
- FK do `shipment_leg` / auto-link
- matching T5 · T8 · float · druga marża

## Zakres

1. Migracja: `transport_mode` + CHECK + unique z trybem.
2. POST z trybem; `air` → lotniska na obu końcach (compose API).
3. UI formularza + proza `/air` i katalogi.

## Poza zakresem

IATA live · cyfra kontrolna · booking · matching T5 · T8 · float

## Kryteria akceptacji

- [ ] `other` bez zmiany zachowania; `air` z lotniskami zapisuje
- [ ] `air` bez `airport` → 400 (`lotnisko`); zły tryb → 400 (`tryb`)
- [ ] unique per tenant z trybem; izolacja
- [ ] zero float / marży / IATA HTTP

## Leftover

cyfra kontrolna IATA · live booking / e-rates API
