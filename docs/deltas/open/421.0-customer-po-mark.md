# Plaster 421.0 · EXP1 `customer_po_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP1 shipment customer_po/release/call_off
**Zależy od:** —

## Zakres

HITL katalog `customer_po_mark` per tenant: `mark_code` + `ref_kind`
(`customer_po`|`release`|`call_off`|`other`) + `source_ref` (`tenant:manual` |
`fixture://customer-po-mark/`). Migracja RLS FORCE. UI `/customer-po-marks`.
OpenFGA `can_manage_customer_po_marks`.

**Wybrane:** węższy HITL katalog referencji PO klienta jako dane.
**Odrzucone:** kolumny na `shipment`; live EDI; kwota.
**Dlaczego:** EXP1 leftover po freight_term; nie dubluj `purchase_order` CT1.

## Poza zakresem

kolumna na shipment · purchase_order CT1 · EDI live · kwota / marża · extract LLM

## Ustalenia

Łowca: ISTNIEJE `purchase_order` / `po_line` / `asn` (CT1). BRAK `customer_po_mark`.
BRAK kolumn customer_po na shipment.

## Kryteria akceptacji

- [ ] POST z kodem, ref_kind, source_ref
- [ ] zły kod / kind / source_ref → 400; amount / margin → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] UI lista + formularz HITL; proza operatora 5–15 zdań
