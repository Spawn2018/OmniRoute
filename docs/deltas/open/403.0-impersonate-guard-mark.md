# Plaster 403.0 · EXP0.12 `impersonate_guard_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `impersonate_guard_mark` per tenant: `mark_code` + `guard_kind`
`impersonate`|`unwrap_denied`|`other` + `source_ref` (`tenant:manual` | `fixture://impersonate-guard-mark/…`).

Klon wzorca `demo_gps_mark`:
- migracja `313` (revises `312_demo_gps_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_impersonate_guard_marks`
- UI `/impersonate-guard-marks`, business-lists + ops-index `SHIPPED["403.0"]`
- operator `docs/operator/impersonate-guard-mark.md`
- izolacja RLS; POST `unwrap`/`ciphertext`/`amount` → 422

Znacznik reguły Admin-P: impersonate ≠ decrypt/unwrap umów. Bez crypto.

## Poza zakresem

unwrap KEK · Fernet · KMS · Auth0 impersonate live · ciphertext · kwota

## Kryteria

- [ ] POST z kodem, guard_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; unwrap/ciphertext/amount → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live Auth0 impersonate · unwrap umów · decrypt w CI
