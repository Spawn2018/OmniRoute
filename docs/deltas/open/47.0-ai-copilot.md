# Plaster 47.0 · M-57 AI (`ai_copilot`)

**Spec źródłowa:** [docs/spec/ai-copilot.md](../../spec/ai-copilot.md)  
**Zależy od:** 0.7 `extraction_draft.status`

**Status:** Otwarty `/noc` 2026-09-02. Nie Fala 10. LLM nie liczy. Nie nowa tabela. Nie czat.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 9 — HITL i Art. 50 już w PLAN):** M-57 **nie ma tabeli czatu**. Operator widzi szkice `pending` (propozycja AI, Art. 50) z `source_ref`. Token `ai_copilot` (UI, nie tabela). Żywy ID **M-57**. 0.7 `/extractions` zostaje recenzją HITL (accept/reject).

**Łowca:** BRAK `copilot` / `chat` / `prompt_log`. ISTNIEJE: `fetchExtractionDrafts`, `status` pending, label Art. 50 na HITL. PODOBNE: 0.7 kolejka; 27.0 pending notice. Postgres: brak tabeli copilot.

**Odrzucone:**

- Tabela wątku / czatu / promptów — zgadywanie schematu (CURRENT).
- Accept z tej tablicy — HITL zostaje na `/extractions`.
- Scoring osoby (zakaz Fali 9).
- LLM liczący kwoty z kandydatów.

## Zakres

Read-only `/ai`. `data-ai-copilot="board"`. Brak migracji. `aiProposals(drafts)`. Link do `/extractions`. Nie renderuje `input_text`.

## Poza zakresem

Tabela czatu · accept · scoring · ExtractionService zapis rates

## Ustalenia

- Token: `ai_copilot` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia extractions.
- RLS: bez nowej tabeli.
- Job: `/ai`. `SHIPPED_CHARGE_ROUTES["47.0"]` = `/ai`. OPS_JOBS +1 → 44.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 47.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/ai` ma `data-ai-copilot="board"` i woła `aiProposals`
- [ ] Filtr `status === "pending"`; brak `input_text` na ekranie
- [ ] Brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["47.0"]` = `/ai`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | czat / M-58 nazwa |
| Docs/OS | — | po zamknięciu |

## Knowledge

Nie RAG. Nie silnik czatu.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Czat leftover. Nazwa M-57 z Fali 9 (HITL / Art. 50), nie z archiwum.
