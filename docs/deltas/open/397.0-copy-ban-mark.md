# Plaster 397.0 · EXP4.19 `copy_ban_mark` HITL (katalog, bez copy marketingu)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala EXP / EXP4.19 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) · EXP0.4 zakaz 500k
**Status:** Delta zaakceptowana `/noc` 2026-09-12. Wolno `/plaster`. Nie copy claimów. Nie silnik.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki EXP4.19 — tylko HITL katalog `copy_ban_mark` jako dane. `mark_code` + `ban_kind` (`eight_min` | `fifteen_k` | `five_hundred_k` | `other`) + `source_ref`. UI `/copy-ban-marks`. OpenFGA `can_manage_copy_ban_marks`. Migracja `308_copy_ban_mark`. Job OPS. Deny-list extract. Bayer jako etykieta w `other` / leftover copy-claim live.

**Łowca:** BRAK tabeli `copy_ban_mark`. ISTNIEJE `rag_sop_mark` — inny katalog. ISTNIEJE EXP0.4 w analizie — nie tabela.

**Odrzucone:** copy claimów 8min/15k/500k do UI · silnik banów · Bayer live scrape · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `308_copy_ban_mark` RLS FORCE.
2. POST/GET `/copy-ban-marks`. OpenFGA. Deny-list.
3. UI `/copy-ban-marks`. How-to. `SHIPPED_CHARGE_ROUTES["397.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, ban_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/claim → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
