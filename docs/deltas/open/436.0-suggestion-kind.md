# Plaster 436.0 · AI1.4 `suggestion_kind` HITL (słownik otwarty)

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI1.4

**Zależy od:** 435.0 `benefit_ledger` (AI1.3 DONE) · 432.0 `suggestion_ledger` (kolumna `suggestion_kind` zostaje CHECK)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** jedna tabela `suggestion_kind` per tenant — append-only HITL. Wiersz = `kind_code` (snake 2–32, **bez** listy CHECK/ENUM) + `source_ref` (`tenant:manual` albo `fixture://suggestion-kind/…`). Nowy rodzaj podpowiedzi = INSERT, nie migracja (VISION B.3).
- **Wybrane:** pierwszy z czterech słowników AI1.4 — `suggestion_kind` jest wzorcem w B.3 („jak `suggestion_kind`”).
- **Odrzucone:** cztery tabele w jednym plasterze (`twin_kind`, `data_source`, `autonomy_level` zostają leftover).
- **Odrzucone:** `data_source` — wymaga kolumny licencji (AI5 / dokument `10`), szersze niż ten Q.
- **Odrzucone:** `autonomy_level` — dana per tenant **i per klient** (B.4), wymaga strony; szersze.
- **Odrzucone:** tabela `twin_kind` w tym plasterze — `twin_mark` już kataloguje rodzaj z CHECK; zdjęcie CHECK + FK = leftover C.2.
- **Odrzucone:** jedna tabela z `axis` w CHECK — to znowu zamyka rodzaj w CHECK (B.3).
- **Odrzucone:** zmiana CHECK na `suggestion_ledger.suggestion_kind` / FK do nowej tabeli / JSON / UPDATE/DELETE / float / kwota.

**Łowca:** PODOBNE `twin_mark` (rodzaj z CHECK — nie rozszerzać). PODOBNE kolumna `suggestion_ledger.suggestion_kind` (zamknięty zbiór — nie ruszać). BRAK tabeli słownikowej bez listy CHECK.

## Zakres

HITL `suggestion_kind`. Migracja `346` (revises `345_benefit_ledger`). UI `/suggestion-kinds`.
OpenFGA `can_manage_suggestion_kinds`. `SHIPPED["436.0"]`. OPS_JOBS length 253.

## Poza zakresem

twin_kind tabela · data_source · autonomy_level · FK z suggestion_ledger · zdjęcie CHECK · CRPS · silnik AI4.1

## Kryteria akceptacji

- [ ] POST z kind_code (dowolny snake 2–32) + source_ref
- [ ] extra amount/margin/score → 422; zły kod / source_ref → 400
- [ ] brak CHECK `eta|rate|route` w migracji; izolacja RLS; REQUIRED `/suggestion-kinds`
- [ ] OPS_JOBS length 253; SHIPPED `"436.0"`
- [ ] craft_style funkcje>40: 0

## Leftover

tabele `twin_kind` / `data_source` / `autonomy_level` · zdjęcie CHECK na `suggestion_ledger` i `twin_mark` + FK · trzy BC piszą do substratu · silnik AI4.1 · CRPS liczone (AI2)
