# Plaster 426.0 · EXP1 `haulier_role_mark` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `haulier_role_mark` — `mark_code` + `role_kind` `booked|actual|other` + `source_ref`. EXP1 booked_carrier vs actual_haulier bez FK party.
- **Odrzucone:** FK `party_id` na shipment — cięższe; nie w tym plasterze.
- **Odrzucone:** `cargo_value` — kwota.
- **Odrzucone:** `diversion_of_shipment_id` — osobny leftover.

## Zakres

HITL `haulier_role_mark`: mark_code + role_kind booked|actual|other + source_ref.
Migracja `336` (revises `335_language_code_mark`). UI `/haulier-role-marks`.
OpenFGA `can_manage_haulier_role_marks` (exp4).

## Poza zakresem

FK party na shipment · scoring · kwota · live dispatch

## Kryteria

- [ ] POST z kodem, role_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/margin/score → 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/haulier-role-marks`
- [ ] OPS_JOBS length 243; SHIPPED `"426.0"`

## Leftover

FK booked/actual haulier na shipment · diversion
