# Plaster 188.0 · C8 `party_document`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md) § C8
**Zależy od:** 5.0 `party` (kontrahent już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie 409 na shipment. Nie extract.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.20–G2.22 + F + C1 + C8):** leftover `circle_sim`/km/P (G2.20–G2.22) zostaje leftover — nie silnik 500k w Pythonie. leftover kg/CBAM zostaje leftover. F1 live FA(3) parked (live HTTP). C1 `shipment_monitoring_filing` / XML PUESC parked (live HTTP). Żywy plaster: jedna tabela `party_document` per tenant — **HITL rodzaj dokumentu kontrahenta z `source_ref`**, nie 409 na `POST shipment` i nie `relation_document_requirement`. Wiersz = `party_id` (FK tenanta do `party`) + `document_kind` (snake 2–32, np. `ocp`, `ocs`, `bdo_number`) + `source_ref` (`tenant:manual` albo `fixture://party-document/…`). Operator wpisuje, że kontrahent ma ten rodzaj dokumentu; serwis nie woła sieci, nie woła LLM, nie mutuje `party` / `shipment` i nie czyta `charge`. Zły kind → 400 (`dokument`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, party_id, document_kind)`. GET listy SQL. API składa odczyt kontrahenta (`PartyService.get_party`). UI `/party-documents` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["188.0"]` = `/party-documents`. OpenFGA: `can_manage_party_documents`. Migracja `128_party_document`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `kreptd_licence` — numer licencji KREPTD na `party`, **nie** rodzaj dokumentu. ISTNIEJE `shipment_document` — wskazanie na zleceniu, **nie** kontrahent. ISTNIEJE `monitoring_scheme` — kod schematu tenanta, **nie** dokument. BRAK tabeli `party_document`. BRAK `relation_document_requirement`. BRAK `/party-documents`. BRAK 409 na `POST shipment`.

**Odrzucone:**

- `circle_sim` / km / P (leftover G2.20–G2.22).
- kg / CBAM (leftover G2.15–G2.18).
- F1 live FA(3) / XML KSeF (live HTTP; S35 już jest).
- C1 SENT XML / `shipment_monitoring_filing`.
- `relation_document_requirement` + 409 na `POST shipment`.
- Daty ważności / `premium_status` / bajty / HITL extract zapisujący dokument.
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `128_party_document`: RLS, izolacja, unique `(organization_id, party_id, document_kind)`.
2. POST/GET. Zły `document_kind` / `source_ref` → 400. API składa odczyt `party`, serwis nie importuje parties / shipments / charges / extraction i nie woła HTTP.
3. UI `/party-documents`. `just api-types`. How-to `docs/operator/dokument-kontrahenta.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `PartyDocDesk` / `partyDocWrite` / `persistPartyDocMark`.

## Poza zakresem

G2.20–G2.22 circle_sim · G2.15–G2.18 kg · F1 live KSeF · C1 filing XML · `relation_document_requirement` · 409 na shipment · daty / składka · extract · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `party_document` z `document_kind` i `source_ref` na istniejącym `party`, bez 409 na zleceniu
- [ ] zły `document_kind` → 400
- [ ] GET zwraca dokumenty tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / LLM / live HTTP / scrape / blokady shipment
