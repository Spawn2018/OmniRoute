# Plaster 589.0 · T2 leftover — HITL `resource.capacity_kg`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [151.0](../archived/151.0-resource.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2

**Zależy od:** 151.0 `resource` · 259.0 `/fleet`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie `capacity_ldm` / `capacity_m3`. Nie `document_expiries`. Nie floating trailer. Nie HW. Nie UXCL banner (588 = park PROC-UXCL §2). Nie Job#2. Nie N3. Nie AI3. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po domknięciu T3 (587.0) i parku SH-R16-4 UXCL w `/noc` — węższy leftover T2 z karty: opcjonalny `capacity_kg` na istniejącej `resource` (`Numeric(14,4)`, Decimal HITL pojemności masowej, > 0, nie float, nie waluta, nie `charge`). POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`pojemność`). Zmiana kg przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (jak nr rejestracyjny). Nie liczy LDM/m³.
- **Wybrane:** UI `/fleet` + panel floty na `/shipments`. `SHIPPED["589.0"]` → `/fleet`. OpenFGA bez nowej relacji. OPS bez +1. Migracja `465_resource_capacity_kg` (po `464_container_release`).
- **Odrzucone:** SH-R16-4 banner UXCL w tej nocy (`REJECTED` — park PROC-UXCL §2 / idle-Ask). Job#2 charge (`REJECTED` — po bannerze). `capacity_ldm` / `capacity_m3` (`REJECTED` w tym plasterze — węższe kg). `document_expiries` (`REJECTED`). floating trailer / HW / mapa / GPS (`REJECTED`). float (`REJECTED`). N3 / AI3 / G0-SH / ciphertext (`REJECTED` → park).

**Łowca:** BRAK `resource.capacity_kg`. ISTNIEJE `resource` (151.0). ISTNIEJE `container.tare_kg` / `payload_kg` (Decimal kg, inny BC — reuse wzorca walidacji, osobny komunikat). ISTNIEJE `stop.weight_kg` (inny BC). PODOBNE: opcjonalny Decimal + supersede. Nie nowa tabela.

## Zakres

1. Migracja `465_resource_capacity_kg`: kolumna nullable `Numeric(14,4)`.
2. Domain `require_capacity_kg` + serwis w `_FleetDraft` + API POST opcjonalny + GET.
3. UI `/fleet` + `fleet-resource-panel` pole kg. How-to dopisek. `SHIPPED["589.0"]`.

## Poza zakresem

`capacity_ldm` · `capacity_m3` · `document_expiries` · floating trailer · HW · UXCL banner · Job#2 · N3 · AI3 · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `capacity_kg="24000"` zapisuje Decimal
- [ ] float / ≤0 / bool / nie-liczba → 400 (`pojemność`)
- [ ] zmiana kg przy tej samej nazwie → nowy wiersz, stary `superseded_by`
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero float / live HTTP / mutacja registration / import charges

## Leftover

`capacity_ldm` · `capacity_m3` · `document_expiries` · floating trailer · SH-R16-4 UXCL banner (park `/noc`) · Job#2 charge · N3 · AI3 · G0-SH
