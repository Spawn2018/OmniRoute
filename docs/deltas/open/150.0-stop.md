# Plaster 150.0 · T1 `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1
**Zależy od:** 28.0 `shipment` · 4.1 `location`
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Nie mapa. Nie T2.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z T1):** jedna tabela punktu na zleceniu per tenant. Wiersz = `shipment_id` + `stop_kind` (`loading` | `unloading` | `customs` | `ferry` | `terminal` | `depot` | `other`) + `sequence_no` + `location_id` + `time_zone` (IANA `Continent/City`) + `status` (`pending` | `at_stop` | `completed` | `failed`) + `source_ref`. GET SQL po `shipment_id` (bieżące). Zmiana kind/statusu/miejsca/strefy = nowy wiersz + `superseded_by`. Złożone FK `(organization_id, shipment_id)` jak `cargo_claim` oraz `(organization_id, location_id)` jak `location`→`port`. `location_id` wymagane — nieznane miejsce = 400, nie luźny adres. Unikat `sequence_no` w ramach zlecenia tylko wśród bieżących (bez partial unique — override jak `rate_line`).

UI `/shipments`. `SHIPPED_CHARGE_ROUTES["150.0"]` = `/shipments`. OpenFGA: `can_manage_shipments`. Migracja `090_stop`.

**Łowca:** ISTNIEJE `shipment_leg` (odcinek road/rail/lcl) — **inny** agregat, nie punkt ZA/WY. ISTNIEJE `location` / `port`. ISTNIEJE GLOSSARY `eta_physical` / `eta_legal` (V2). BRAK tabeli `stop`. BRAK `stop_group`. BRAK timezone na zleceniu.

**Odrzucone:**

- `stop_group` / trip / resource (T2).
- `eta_physical` / `eta_legal` (V2; GPS/tacho).
- Waga / ilość / opakowanie / plomba / awizacja / waiting / POD quality (EXP1 leftover).
- Mapa / leaflet / GPS / float na współrzędnych.
- Import serwisów shipments / geography / charges.

## Zakres

1. Migracja `090_stop`: RLS, izolacja, złożone FK, indeks `(organization_id, shipment_id)`.
2. POST/GET. Obcy kind / status / strefa / brak source_ref → 400.
3. UI `/shipments`. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

T2 trip · V2 ETA · mapa · EXP1 waga/plomba · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje punkt; `stop_kind=pickup` albo `status=queued` → 400
- [ ] Ten sam `sequence_no` na zleceniu z innym statusem = nowy wiersz, stary `superseded_by`
- [ ] GET po `shipment_id` zwraca bieżące wiersze
- [ ] Serwis bez importu shipments / geography / charges
- [ ] Test izolacji tabeli + obcy `location_id` / `shipment_id`
- [ ] Zero float / marży / mapy
