# Plaster 582.0 · T3 leftover — HITL `container.temp_max`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [581.0](../archived/581.0-container-temp-min.md)

**Zależy od:** 153.0 `container` · 581.0 `temp_min`

**Status:** Zaakceptowana `/noc` 2026-09-19. Nie `needs_external_power`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** para SPEED po `temp_min`: `temp_max` Decimal °C. W `backend/app` kolumny nie ma. Reuse `_require_temp_c` + `require_container_temp_max`. Pusty → `null`. Float/bool → 400 (`temp. max`). Nie porównuje z `temp_min`.
- **Wybrane:** UI `/shipments`. `SHIPPED["582.0"]` → `/shipments`. Migracja `462_container_temp_max` (po `461_container_temp_min`).
- **Odrzucone:** `needs_external_power` (`REJECTED` — węższe pole). Porównanie min≤max (`REJECTED`). 409 vs reefer (`REJECTED`). float / N3 / ciphertext / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `temp_max` w `backend/app`. ISTNIEJE `_require_temp_c`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `462_container_temp_max`: `Numeric(14, 4)` nullable.
2. Domain + API GET string + UI + SHIPPED.

## Poza zakresem

`needs_external_power` · min≤max SQL · 409 vs reefer · N3 · ciphertext · AI3 · G0-SH · kwota

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `temp_max="2"` zapisuje Decimal
- [ ] float / bool → 400 (`temp. max`)
- [ ] izolacja RLS
- [ ] zero mutacji `temp_min`

## Leftover

`needs_external_power` · min≤max · 409 vs reefer · N3 · ciphertext PIN · AI3-payload · G0-SH
