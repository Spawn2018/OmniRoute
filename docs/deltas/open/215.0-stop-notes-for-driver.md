# Plaster 215.0 · T1 `notes_for_driver` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover · [150.0](../archived/150.0-stop.md) leftover · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1
**Zależy od:** 150.0 `stop` + 213.0 `stop_group_code`
**Status:** Otwarty `/noc` 2026-09-09. Nie waga. Nie plomba. Nie tabela `stop_group`. Nie mapa.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T1 `notes_for_driver`/T1b tabela/EXP1 waga + leftover T2c km/`party`/`/fleet` + leftover T7b na `charge` + leftover T5 + S53):** leftover T1b tabela parked. leftover T1 EXP1 waga/ilość/opakowanie/plomba/awizacja parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. Karta T1: `notes_for_driver` = SPEED info dla kierowcy na punkcie, **nie** waga i **nie** etykieta trasy `trip.route_label`. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `notes_for_driver` (1–256 znaków po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token (nie tekst, za długi) → 400 (`notatka`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["215.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `154_stop_notes`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop` — punkt ZA/WY + IANA + ETA + `stop_group_code`, **nie** notatka. ISTNIEJE `trip.route_label` — etykieta przejazdu, **nie** info na punkcie. BRAK `notes_for_driver`. BRAK wagi/plomby. BRAK tabeli `stop_group`.

**Odrzucone:**

- Nowa tabela `stop_group` (leftover T1b).
- EXP1 waga / ilość / opakowanie / plomba / awizacja / waiting / POD.
- `trip_id` na `stop` (import trips w BC stop).
- `route_label` na `stop` (to jest T2 na `trip`, 214.0).
- T2c km · T7b `charge` · T5 outbox · mapa · GPS · S53 Auth0.

## Zakres

1. Migracja `154_stop_notes` (po `153_trip_route_label`): `notes_for_driver` nullable `String(256)`.
2. POST opcjonalny `notes_for_driver`. Zły token → 400. Serwis nie importuje trips/charges i nie woła HTTP.
3. UI `/shipments` panel punktu. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

tabela `stop_group` · EXP1 · `trip_id` · km · mapa · T7b · T5 · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST z `notes_for_driver` zapisuje tekst, bez wagi
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`notatka`)
- [ ] RLS istniejącego `stop`; izolacja
- [ ] Zero float / marży / mapy / wagi / `/fleet` / live HTTP / scrape
