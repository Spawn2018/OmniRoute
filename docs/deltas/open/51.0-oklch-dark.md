# Plaster 51.0 · U-oklch-dark (tokeny)

**Spec źródłowa:** [docs/spec/oklch-dark.md](../../spec/oklch-dark.md)  
**Zależy od:** ADR-0003 + `frontend/src/index.css`

**Status:** Otwarty `/noc` 2026-09-02. Nie Fala 10. Nie nowa tabela. Nie zmiana jobów operatora.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN § Wave FE leftover, po zamknięciu Fali 11):** hex w `:root` → `oklch()`. Klasa `.dark` z parą tło/tekst AA. Tokeny zostają w `index.css`. Operator widzi motyw jasny i ciemny (klasa na `html`, bez nowej tabeli). Żywy ID leftover **U-oklch-dark**, plaster **51.0**.

**Łowca:** BRAK `.dark` / `oklch(` w `frontend/src`. ISTNIEJE: paleta hex ADR-0002 w `index.css`; test `:focus-visible`. PODOBNE: brak drugiego arkusza motywów.

**Odrzucone:**

- Nowa tabela / ustawienie motywu w bazie — zgadywanie schematu.
- Druga paleta poza `index.css`.
- Zmiana jobów `/quality` `/rollout`.
- Base UI.

## Zakres

`index.css`: wartości kolorów jako `oklch()`, blok `.dark`. Test w `a11y.test.ts` (oklch + `.dark`, brak `#` na parach tło/tekst w `:root`). Zero migracji. Zero nowej trasy.

## Poza zakresem

U-money-align · U-condensed · Playwright axe · mapa

## Ustalenia

- Tokeny zostają CSS, nie tabela.
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy. `SHIPPED_CHARGE_ROUTES` bez 51.0. OPS_JOBS bez +1.
- `just dup` ≤ 3%.
- LLM nie liczy marży. Kontrast AA to warunek leftover, nie kwota.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `:root` i `.dark` używają `oklch(` na parach tło/tekst
- [ ] Test pali hex-only na tych parach i brak `.dark`
- [ ] Brak nowej migracji i nowej trasy
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | CSS |
| Dług w diffie | leftover | U-money-align |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. U-money-align leftover.
