# Plaster 196.0 · V3 `free_time_clock` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · [karty-pol-fala-v.md](../../analysis/karty-pol-fala-v.md) § V3 · [karty-pol-fala-n.md](../../analysis/karty-pol-fala-n.md) § N3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 0.3 tenancy (katalog per tenant) · 153.0 `container` (D&D było leftover)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie countdown. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover V3 D&D + N3 zegar + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V2b myto→`charge` parked (druga marża). leftover C3 live VIES parked. leftover C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO parked. leftover F2b–F4 parked. leftover `circle_sim` parked. leftover kg parked. F1 / C1 / C8 409 parked. N3 countdown `free_days → remaining` + szkic `charge` zostaje leftover. Blank sailing / congestion / Gate OS leftover. Żywy plaster: jedna tabela `free_time_clock` per tenant — **HITL zegar D&D/rollover jako dane**, nie watchdog i nie opłata. Wiersz = `clock_kind` (`demurrage` | `detention` | `mixed` | `rollover`) + `free_days` (integer ≥ 0, dana, nie `now() − gate_in`) + `source_ref` (`tenant:manual` albo `fixture://free-time-clock/…`). Operator wpisuje rodzaj i dni wolne; serwis nie liczy pozostałych dni, nie woła HTTP armatora, nie woła LLM i nie czyta `charge` / `container` / `trip`. Zły rodzaj → 400 (`rodzaj`). Złe dni → 400 (`dni`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/free-time-clocks` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["196.0"]` = `/free-time-clocks`. OpenFGA: `can_manage_free_time_clocks`. Migracja `136_free_time_clock`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container` 153.0 — ISO 6346, **nie** free days. ISTNIEJE `party_scorecard.rollover_count` — karta oferty, **nie** ETD/ETA rollover. ISTNIEJE `charge` — jedyna marża, **nie** szkic D&D. ISTNIEJE `local_charge` — THC/ISPS, **nie** detention. ISTNIEJE `stop.eta_*` — zegary ETA, **nie** D&D. BRAK tabeli `free_time_clock`. BRAK countdown N3. BRAK blank sailing.

**Odrzucone:**

- Countdown / remaining days w Pythonie albo SQL (`now() − gate_in`).
- Szkic `charge` z D&D (N3 leftover).
- Kolumny `demurrage_free_days` na `container` (leftover T3).
- Blank sailing / congestion / Gate OS / live HTTP armatora.
- Myto → `charge` (leftover V2b).
- C3 live VIES / C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO.
- F2b / F3 / F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `136_free_time_clock` (po `135_weather_observation`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind + `free_days >= 0`.
2. POST/GET. Zły rodzaj / dni → 400. Serwis nie importuje containers / charges / extraction / trips i nie woła HTTP.
3. UI `/free-time-clocks`. `just api-types`. How-to `docs/operator/zegar-dd.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `ClockDesk` / `clockWrite` / `persistClockMark`.

## Poza zakresem

Countdown N3 · szkic charge · kolumny na container · blank sailing · live HTTP armatora · myto → charge · GPS · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [ ] POST zapisuje `free_time_clock` z `clock_kind` i `free_days`, bez HTTP
- [ ] zły `clock_kind` → 400 (`rodzaj`)
- [ ] ujemne `free_days` → 400 (`dni`)
- [ ] GET zwraca wiersze tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / countdown / live HTTP / scrape
