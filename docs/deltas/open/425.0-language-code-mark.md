# Plaster 425.0 · EXP1 `language_code_mark` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `language_code_mark` — `mark_code` + `locale_kind` `pl|en|de|other` + `source_ref`. EXP1 `language_code` na shipment bez kolumny na zleceniu.
- **Odrzucone:** `cargo_value`+waluta — kwota; nie katalog bez Decimal.
- **Odrzucone:** mutacja i18n UI / `preferred_language` na party — osobne leftover; ten plaster nie rusza frontowego i18n ani party.
- **Odrzucone:** `booked_carrier` / diversion — cięższe FK; węższy Q = locale katalog.

## Zakres

HITL `language_code_mark`: mark_code + locale_kind pl|en|de|other + source_ref.
Migracja `335` (revises `334_payment_terms_mark`). UI `/language-code-marks`.
OpenFGA `can_manage_language_code_marks` (exp4).

## Poza zakresem

kolumna na shipment · party.preferred_language · i18n UI · tłumaczenie LLM

## Kryteria

- [ ] POST z kodem, locale_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/margin/score → 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/language-code-marks`
- [ ] OPS_JOBS length 242; SHIPPED `"425.0"`

## Leftover

FK language na shipment · sync z i18n UI
