# Plaster 279.0 · CT4 `routing_guide` HITL (katalog, bez 409 egzekucji)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md) (`routing_guide` → 409)
**Zależy od:** 0.3 tenancy · 278.0 CT1 `asn` (oś pin; ten Q nie rusza PO)
**Status:** Zaakceptowany `/noc` 2026-09-11. To nie jest egzekucja 409 na shipment. To nie jest auto shipment. To nie jest CT2 (klej CI5 — CI5 brak).

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover Fala CT / pin 2026-09-08c po zamknięciu CT1 asn):** leftover CT1 auto `shipment` (U1) **parked z powodem** — `shipment` wymaga `quotation_id`; BC `purchase_orders` zakazuje zapisu/`FK` do `shipment`; „auto” bez HITL łamie HC. Osobny Q compose API gdy będzie decyzja. leftover CT2 klej CI5 **parked** — CI5 (`sla_clause` kara SQL) jeszcze nie żyje; V6 `tower_impact.contract_data_status` już niesie „brak danych umowy”. leftover CT5 EDI live parked. leftover CT8 AIS live parked. leftover CT3 OTIF parked (metryka, nie ten Q). Żywy plaster: **nowa tabela** `routing_guide` per tenant — HITL katalog przewodnika routingu jako dane, nie 409 i nie mapa. GLOSSARY: **przewodnik routingu** = `routing_guide` (nie `route_helper`, nie kolumny na `trip` / `shipment_leg`). Wiersz = `guide_code` (snake 2–32) + opcjonalne etykiety `lane_label` / `mode_label` (tekst 1–128 albo brak; **nie** FK geography / `party` / `shipment`) + `source_ref` (`tenant:manual` albo `fixture://routing-guide/…`). Zero kolumn 409 / `blocks_dispatch` egzekucji / kwoty. Operator wpisuje kod i etykiety; serwis nie woła HTTP, nie blokuje shipment, nie woła LLM, nie importuje `shipments` / `charges` / `trips` / `extraction`. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zła etykieta → 400 (`korytarz` / `tryb`). Obce `source_ref` → 400 (`obce`). Body z `shipment_id` / `amount` / `blocks_dispatch` → 422. Unique `(organization_id, guide_code)` i `(organization_id, source_ref)`. UI `/routing-guides`. `SHIPPED_CHARGE_ROUTES["279.0"]` = `/routing-guides`. OpenFGA: `can_manage_routing_guides`. Migracja `212_routing_guide` (po `211_asn`). Job OPS 124. Nowy BC `routing_guides`. Marża zostaje na `charge`.

**Łowca:** BRAK tabeli `routing_guide`. BRAK nazwy w GLOSSARY — ten plan dopisuje **przewodnik routingu**. ISTNIEJE `trip.route_label` 214.0 — etykieta przejazdu, **nie** przewodnik CT4. ISTNIEJE `document_checklist_rule.blocks_dispatch` — checklista, **nie** routing 409. ISTNIEJE `lane_pattern` — para UN/LOCODE, **nie** przewodnik. ISTNIEJE `asn` 278.0 — awizo CT1, **nie** ten Q. PODOBNE: kod + etykiety + `source_ref` jak `purchase_order` / konektory. Nie wyodrębniać wspólnej warstwy.

**Odrzucone:**

- Egzekucja 409 na POST shipment / auto stop produkcji.
- Auto-create `shipment` z ASN / FK shipment w BC purchase_orders.
- CT2 klej CI5 / CT3 OTIF liczony / CT5 EDI live / CT8 AIS live.
- kwota / marża / float / HTTP / mapa / UPDATE/DELETE.

## Zakres

1. Migracja `212_routing_guide` po `211_asn`: RLS FORCE, izolacja, unique kod + `source_ref`. Zero kolumn 409 / shipment / kwoty.
2. POST/GET `/routing-guides`. Zły token → 400 (`oznaczenie` / `korytarz` / `tryb` / `obce`). Body z shipment/kwotą/blocks → 422. Nowy BC. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/routing-guides`. How-to `docs/operator/przewodnik-routingu.md`; fold w `katalogi.md`. Job OPS 124. OpenFGA `can_manage_routing_guides`. Nazwy: `RoutingGuideDesk` / `routing-guide-form.tsx` / `persistRoutingGuide`. Brak przycisku „Blokuj zlecenie”.

## Poza zakresem

409 na shipment · auto shipment · CT2 CI5 · OTIF · live EDI · AIS · kwota · float · mapa

## Kryteria akceptacji

- [ ] POST zapisuje `routing_guide` z kodem, opcjonalnymi etykietami i `source_ref`
- [ ] zły kod → 400 (`oznaczenie`); obce `source_ref` → 400 (`obce`)
- [ ] POST z `shipment_id` / `amount` / `blocks_dispatch` → 422
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] Zero float / marży / live HTTP / 409 egzekucji
- [ ] Zero twierdzenia, że auto shipment albo CI5 żyje

## Leftover po 279.0

Egzekucja 409 z `routing_guide`. CT1 auto shipment (compose + quotation). CT2 klej CI5. CT3 OTIF. CT5 EDI live. CT8 AIS live. CI1 extract.
