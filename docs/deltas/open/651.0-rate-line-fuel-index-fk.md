# Plaster 651.0 · leftover EXP1 rate_line FK fuel_index

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [648.0](../archived/648.0-rate-line-index-id.md) · [165.0](../archived/165.0-fuel-index.md)
**Zależy od:** M-07 `rate_line` (648.0 `index_id` pin); P3 `fuel_index` (katalog)
**Status:** Delta zaakceptowana `/noc 17` 2026-09-24. Wolno `/plaster`. Nie float. Nie mnożenie FSC.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 FK fuel_index na pin↔katalog):** tabela `rate_line` zostaje prawdą o stawce kupna. Tekstowy pin `index_id` (648.0) **zostaje**. Żywy plaster: **opcjonalny** `fuel_index_id` UUID — złożone FK `(organization_id, fuel_index_id) → fuel_index(organization_id, id)` `ON DELETE RESTRICT` (wzorzec `container.carrier_party_id`). Operator wskazuje wiersz katalogu indeksu tego tenanta; serwis weryfikuje istnienie przez **odczyt** `FuelIndexRepository` (jak `charge_codes`), **nie** import `fuel_indexes` service, nie mnoży FSC × amount, nie tworzy `charge`, nie woła HTTP/LLM. POST bez pola → `null`. Nieznany UUID / inny tenant → 400. Istniejące wiersze: `null`. Zmiana = supersede. GET listy SQL. UI `/rate-lines`. `SHIPPED_CHARGE_ROUTES["651.0"]` = `/rate-lines`. OpenFGA bez nowej relacji. Migracja `522_rate_line_fuel_index_id` po `521_charge_code_source_ref`. Marża zostaje na `charge`. Zero float.

**Dlaczego osobna kolumna UUID, nie zamiana `index_id`:** pin tekstowy = etykieta z kontraktu (648.0); FK = wiązanie do katalogu P3. Jedna kolumna nie unosi obu semantyk. Matching pin↔kind odrzucony — operator wskazuje UUID świadomie.

**REQUIREMENT:** leftover EXP1 po 650.0. Źródło: CURRENT Następny **651.0**; 648.0 Leftover „FK do fuel_index”.

**Łowca:** ISTNIEJE `rate_line.index_id` tekst (648.0). ISTNIEJE `fuel_index` + `uq_fuel_index_org_id`. BRAK `fuel_index_id` na `rate_line`. BRAK złożonego FK. BRAK mnożenia FSC.

**Odrzucone:**

- float / mnożenie FSC × amount / SQL przeliczenia na `charge`
- zamiana tekstowego `index_id` na UUID (łamie 648.0)
- matching pin tekst ↔ `index_kind` automatyczny
- import `app.services.fuel_indexes` w BC rate_lines
- live HTTP indeksu
- mutacja w miejscu (tylko supersede)

## Zakres

1. Migracja `522_rate_line_fuel_index_id`: kolumna UUID nullable + FK złożone; trigger niemutowalności obejmuje `fuel_index_id`.
2. Domain `require_fuel_index_id`; serwis: odczyt repo fuel_indexes; POST/supersede opcjonalnie; zły UUID / brak wiersza → 400.
3. UI `/rate-lines` (opcjonalne pole). How-to / fold. `just api-types`.
4. AGENTS rate_lines: dozwolony odczyt `repositories.fuel_indexes`; zakaz mnożenia FSC zostaje.

## Poza zakresem

mnożenie FSC · matching pin↔kind · float · charge · live HTTP · mutacja w miejscu · spot_or_contract na quotation

## Plan plików

- `backend/alembic/versions/522_rate_line_fuel_index_id.py`
- model / domain / serwis / repo fuel_indexes `get` / API / testy rate_lines
- `frontend/src/features/rate-lines/` + API client
- `ops-index` + `routes-breadth` (`651.0` → `/rate-lines`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z `fuel_index_id` istniejącego indeksu tenanta zapisuje wiersz; bez pola → null
- [ ] nieznany UUID / obcy tenant → 400; pin `index_id` nadal działa niezależnie
- [ ] serwis nie importuje `fuel_indexes` service / charges i nie mnoży FSC
- [ ] RLS: tenant B nie wiąże indeksu A; zero float / marży / matching

## Leftover

spot_or_contract na quotation · matching · live MQC · mnożenie FSC · P3b SQL na charge
