# Plaster 407.0 · EXP4.3b `ocean_feeder_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `ocean_feeder_mark` per tenant: `mark_code` + `feeder_kind`
`feeder`|`short_sea`|`other` + `source_ref` (`tenant:manual` | `fixture://ocean-feeder-mark/…`).

Klon wzorca `rail_uic_mark`:
- migracja `317` (revises `316_rail_uic_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_ocean_feeder_marks`
- UI `/ocean-feeder-marks`, business-lists + ops-index `SHIPPED["407.0"]`
- operator `docs/operator/ocean-feeder-mark.md`
- izolacja RLS; POST `amount`/`teu`/`score` → 422

Obok `ocean_alliance_mark` — tu feeder/short-sea, nie alliance live.

## Poza zakresem

live feeder schedule · TEU · kwota · AIS · alliance HTTP

## Kryteria

- [ ] POST z kodem, feeder_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/teu → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live feeder · AIS · TEU
