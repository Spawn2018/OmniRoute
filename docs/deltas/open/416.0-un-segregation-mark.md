# Plaster 416.0 · EXP0.9 `un_segregation_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP0.9 (tunel/segregacja UN); kolejka pin 2026-09-08c
**Zależy od:** 415.0 (wzorzec HITL mark; bez współdzielenia z load_plan/oog)

## Zakres

HITL `un_segregation_mark`: mark_code + segregate_kind tunnel|segregation|compat|other + source_ref.
Migracja `326`. UI `/un-segregation-marks`. OpenFGA `can_manage_un_segregation_marks`.

Wybrane: osobny katalog tunel/segregacja UN (EXP0.9 z karty; nie solver).
Odrzucone: rozszerzenie `load_plan_mark` / `oog_mark`; OR/VRP; live ADR.
Dlaczego: CURRENT i EXP0.9; load_plan ma tylko stance tunnel, nie katalog segregacji UN.

## Poza zakresem

solver OR · LLM-VRP · osie Decimal · live ADR · kwota

## Ustalenia

Append-only INSERT. Bez UPDATE/DELETE. Bez float/marży. Bez HTTP.

## Kryteria akceptacji

- [ ] POST z kodem, segregate_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/axle → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0

## Leftover

solver OR · LLM-VRP · egzekucja tunelu na trip
