# Plaster 466.0 · BR4.0 promy: rezerwacja i okna

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR4.0 · EXP2.13 `ferry_art9_mark`

**Zależy od:** tenancy RLS · wzorzec HITL `tacho_plan_mark` (465.0) · `ferry_art9_mark` już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `ferry_booking_marks`. Tabela `ferry_booking_mark`: `mark_code` + `booking_kind` `booking|window|sailing|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance rezerwacji/okna, nie art. 9 i nie bilet HTTP.
- **Wybrane:** POST/GET `/api/v1/ferry-booking-marks`. OpenFGA `can_manage_ferry_booking_marks` (member). UI `/ferry-booking-marks`. `SHIPPED["466.0"]` → `/ferry-booking-marks`.
- **Wybrane:** Alembic `371_ferry_booking_mark` (`down_revision = "370_tacho_plan_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumny na `ferry_art9_mark` (`REJECTED` — EXP2.13 zostaje rest/watchdog/crossing, nie rezerwacja).
- **Odrzucone:** klej FK do `trip` / `stop` / `route_plan_mark` (`REJECTED` na tym wierszu).
- **Odrzucone:** live rezerwacja promu · bilet HTTP · solver art. 9 · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `ferry_art9_mark` (EXP2.13 rest/watchdog/crossing). ISTNIEJE `booking_instruction` (I4 scope bookingu). PODOBNE katalog HITL. BRAK `ferry_booking_mark`. BRAK katalogu rezerwacji/okna promu.

## Zakres

How-to: jedno zdanie — to katalog stance rezerwacji promu; live bilet i art. 9 zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (lista + select, nie klon tabeli 464/465) pod jscpd < 3%.

## Poza zakresem

live rezerwacja · bilet HTTP · solver art. 9 · FK do trip · Valhalla · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services
