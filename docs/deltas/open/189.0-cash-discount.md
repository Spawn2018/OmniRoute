# Plaster 189.0 · F2 `cash_discount`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala F · [karty-pol-fala-f.md](../../analysis/karty-pol-fala-f.md) § F2
**Zależy od:** 96.0 `sales_invoice` (faktura sprzedaży już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie kwota. Nie CAMT. Nie live KSeF.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.20–G2.22 + F1 + C1 + C8 409 + F2–F4):** leftover `circle_sim`/km/P (G2.20–G2.22) zostaje leftover — nie silnik 500k w Pythonie. leftover kg/CBAM zostaje leftover. F1 live FA(3) parked (live HTTP; numer sesji to już S35 `ksef_ref`). C1 `shipment_monitoring_filing` / XML PUESC parked. C8 409 / `relation_document_requirement` parked. F3 noty + period lock parked. F4 CAMT/MT940 + biała lista przy przelewie parked. Żywy plaster: jedna tabela `cash_discount` per tenant — **HITL rodzaj skonta/rezerwy/rozliczenia wewnętrznego z `source_ref` na fakturze**, nie Decimal na wierszu i nie druga marża. Wiersz = `sales_invoice_id` (FK tenanta do `sales_invoice`) + `discount_kind` (snake 2–32, np. `skonto`, `reserve`, `internal_settlement`) + `source_ref` (`tenant:manual` albo `fixture://cash-discount/…`). Operator wpisuje, że ta faktura ma ten rodzaj skonta; serwis nie woła sieci, nie woła LLM, nie mutuje `sales_invoice` / `charge` / `bank_payment` i nie mnoży kwoty. Zły kind → 400 (`skonto`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, sales_invoice_id, discount_kind)`. GET listy SQL. API składa odczyt faktury (`SalesInvoiceService.get_invoice`). UI `/cash-discounts` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["189.0"]` = `/cash-discounts`. OpenFGA: `can_manage_cash_discounts`. Migracja `129_cash_discount`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `sales_invoice` — faktura na zleceniu, **nie** skonto. ISTNIEJE `cash_flow` — para wycena+płatność, **nie** rodzaj skonta. ISTNIEJE `quote_invoice_settlement` — para wycena+faktura, **nie** rezerwa. ISTNIEJE `bank_payment` — para faktura+rachunek, **nie** CAMT. BRAK tabeli `cash_discount`. BRAK `/cash-discounts`. BRAK Decimal na skonto. BRAK period lock. BRAK ingestu CAMT.

**Odrzucone:**

- `circle_sim` / km / P (leftover G2.20–G2.22).
- kg / CBAM (leftover G2.15–G2.18).
- F1 live FA(3) / XML KSeF (live HTTP; S35 już jest).
- C1 SENT XML / `shipment_monitoring_filing`.
- C8 `relation_document_requirement` + 409 na `POST shipment`.
- Decimal / waluta / mnożenie skonta (leftover F2b).
- Noty korygujące + period lock N13 (leftover F3).
- CAMT/MT940 + HITL rekoncyliacja + biała lista (leftover F4).
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `129_cash_discount`: RLS, izolacja, unique `(organization_id, sales_invoice_id, discount_kind)`.
2. POST/GET. Zły `discount_kind` / `source_ref` → 400. API składa odczyt `sales_invoice`, serwis nie importuje sales_invoices / charges / bank_payments / extraction i nie woła HTTP.
3. UI `/cash-discounts`. `just api-types`. How-to `docs/operator/skonto.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `SkontoDesk` / `skontoWrite` / `persistSkontoMark`.

## Poza zakresem

G2.20–G2.22 circle_sim · G2.15–G2.18 kg · F1 live KSeF · C1 filing XML · C8 409 · Decimal skonta · F3 noty/period lock · F4 CAMT · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `cash_discount` z `discount_kind` i `source_ref` na istniejącej `sales_invoice`, bez kwoty
- [ ] zły `discount_kind` → 400
- [ ] GET zwraca skonta tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / LLM / live HTTP / scrape / CAMT / period lock
