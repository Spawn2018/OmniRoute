# Plaster 56.0 · U-playwright-axe (`e2e_axe_route`)

**Spec źródłowa:** [docs/spec/playwright-axe.md](../../spec/playwright-axe.md)  
**Zależy od:** ADR-0003 §8 + U-oklch-dark (51.0)

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. Nie live HTTP do armatorów. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN leftover U-playwright-axe):** Playwright Chromium + `@axe-core/playwright` na trzech trasach SPA z `vite preview` po `just perf`. Token `e2e_axe_route`. Żywy ID leftover **U-playwright-axe**, plaster **56.0**. `just frontend-e2e` wchodzi do `code-gate` **po** `perf`, żeby reuse `frontend/dist`. CI instaluje Chromium. Gate leftover: brak Playwright w gate / axe poza CI.

**Łowca:** BRAK `playwright.config`, BRAK `e2e/`, BRAK axe w CI, BRAK `just frontend-e2e`. ISTNIEJE: vitest, `just perf` (`pnpm build`), trasy `/session` `/rate-lines` `/extractions` `/quotations`, przycisk Akceptuj w HITL. PODOBNE: vitest to logika, nie E2E.

**Odrzucone:**

- Firefox / WebKit (czas gate, nie trzy ścieżki).
- Live login JWT + klik Akceptuj (API+seed; HC optimistic accept).
- Osobny job CI zamiast `code-gate` (gate leftover wymaga Playwright **w** gate).
- Nowa tabela / nowa trasa.

## Zakres

Trzy specy: sesja→stawki; kolejka HITL; wycena. axe na każdej. `playwright.config.ts` `webServer` = `pnpm preview`. `SHIPPED_CHARGE_ROUTES` bez 56.0. Zero migracji.

## Poza zakresem

U-print · pełny accept HITL w E2E · drugi browser

## Ustalenia

- Token: `e2e_axe_route` (GLOSSARY).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3% (`e2e/` poza `src`, jscpd nie skanuje).
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `just code-gate` odpala Playwright
- [ ] Trzy ścieżki: `/session`→`/rate-lines`, `/extractions`, `/quotations`
- [ ] axe na każdej (impact serious/critical = fail)
- [ ] CI instaluje Chromium; axe nie jest poza CI
- [ ] Test nie klika Akceptuj i nie woła `parseFloat`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | E2E nie p95 wyceny |
| Dług w diffie | leftover | U-print |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. U-print leftover.
