# Plaster 623.0 · D7d rodzaj palety `epal` na saldzie

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D7 · [161.0](../archived/161.0-pallet-balance.md)
**Zależy od:** 161.0 `pallet_balance` (`chep`|`lpr`) · `pallet_pool_mark` ma już `epal` jako znacznik puli, nie saldo
**Status:** Otwarty — delta zaakceptowana w `/noc` 2026-09-22. Wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D7d):** saldo sztuk już jest. Giełda D7b i ledger ujemny D7c zostają. Żywy plaster: trzeci `pallet_kind` = `epal` na istniejącej `pallet_balance`. CHECK i domena przyjmują `chep`|`lpr`|`epal`. `unit_count` zostaje nieujemnym integerem. `epal` mieści się w `String(8)`. Zły rodzaj → 400 (`rodzaj`), jak dziś. UI `/pallet-balances` dostaje ten token na liście. `SHIPPED_CHARGE_ROUTES["623.0"]` = `/pallet-balances`. OpenFGA bez nowej relacji. Migracja `495_pallet_balance_epal` po `494_ocean_bill_number_pool`: podmiana CHECK, bez nowej tabeli. Marża zostaje na `charge`. Zero float. Zero HTTP giełdy.

**REQUIREMENT:** leftover 161.0 „Euro / EPAL jako trzeci kind”. Token `epal` jak w `pallet_pool_mark`, nie osobny napis `euro`. Źródło: `docs/deltas/archived/161.0-pallet-balance.md`; PLAN wiersz D7.

**Łowca:** ISTNIEJE `require_pallet_kind` ze zbiorem `chep`|`lpr`. ISTNIEJE CHECK `ck_pallet_balance_kind`. ISTNIEJE `pallet_pool_mark.pool_kind` z `epal` — inna tabela, nie saldo. BRAK `epal` na `pallet_balance`.

**Odrzucone:**

- giełda Chep/LPR/EPAL i live HTTP (D7b)
- saldo ujemne / ledger ruchu (D7c)
- osobny kind `euro`
- kwota / Decimal / depozyt na `charge`
- import `pallet_pool_marks` w serwisie salda

## Zakres

1. Migracja podmienia CHECK na `chep`,`lpr`,`epal`. Domena i model to samo.
2. POST `epal` zapisuje sztuki; rodzaj spoza trójki → 400.
3. Lista w UI i zdanie w `docs/operator/palety.md`.

## Poza zakresem

D7b giełda · D7c ledger · kind `euro` · float · WMS · mapa

## Plan plików

- `backend/alembic/versions/495_pallet_balance_epal.py`
- `backend/app/domain/pallet_balance.py`
- `backend/app/models/pallet_balance.py`
- `backend/app/services/pallet_balances/AGENTS.md`
- testy domeny i HTTP salda
- `frontend/src/features/pallet-balance/pool-panel.tsx`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- `docs/operator/palety.md`

## Kryteria akceptacji

- [ ] POST `pallet_kind=epal` z nieujemną liczbą sztuk zapisuje wiersz
- [ ] rodzaj spoza `chep`|`lpr`|`epal` → 400 (`rodzaj`); ujemna liczba → 400 jak dziś
- [ ] serwis nie importuje `pallet_pool_marks` i nie woła HTTP
- [ ] zero float / marży / nowej tabeli / nowej relacji OpenFGA
