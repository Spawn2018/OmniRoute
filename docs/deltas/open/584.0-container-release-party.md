# Plaster 584.0 · T3 leftover — HITL `container.container_release_party_id`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [583.0](../archived/583.0-container-needs-external-power.md) · [242.0](../archived/242.0-container-carrier-party.md)

**Zależy od:** 153.0 `container` · 242.0 `carrier_party_id` · 583.0 `needs_external_power`

**Status:** Zaakceptowana `/noc` 2026-09-19. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ostatnie pole SPEED z karty T3 „Zwolnienie kontenera dla”: opcjonalny `container_release_party_id` (UUID HITL → `party`). Wzorzec `carrier_party_id` (242.0): złożone FK `(organization_id, container_release_party_id)` → `party`, `ON DELETE RESTRICT`. `require_container_release_party_id` jak `require_carrier_party_id`. POST bez pola → `null`. Nieznany UUID → 404. API składa `PartyService.get_party` — **serwis** nie importuje `parties`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["584.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `464_container_release` (po `463_container_ext_power`, revision ≤32 znaków).
- **Odrzucone:** N3 countdown · ciphertext PIN · AI3-payload · G0-SH · min≤max · 409 vs reefer · live HTTP · import `parties` w `ContainerService` · druga marża / float.

**Łowca:** BRAK `container_release_party_id` przed plasterem. ISTNIEJE `carrier_party_id` (242.0) + bind w API. PODOBNE: ten sam wzorzec FK — nie wspólna abstrakcja (drugie powtórzenie, nie trzecie).

## Zakres

1. Migracja `464_container_release`: `container_release_party_id` nullable UUID + FK złożone → `party`.
2. Domain + API bind + UI `/shipments` + SHIPPED. Fold w how-to.

## Poza zakresem

N3 countdown · ciphertext PIN · AI3-payload · G0-SH · min≤max · 409 vs reefer · live HTTP · S21 · kwota

## Kryteria akceptacji

- [ ] POST z UUID istniejącego `party` zapisuje FK
- [ ] POST bez pola → `null`
- [ ] nieznany UUID → 404
- [ ] RLS / izolacja istniejącego `container`
- [ ] zero float / marży / live HTTP / import `parties` w serwisie
- [ ] zero mutacji `carrier_party_id`

## Leftover

N3 countdown · min≤max · 409 vs reefer · ciphertext PIN · AI3-payload · G0-SH

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po plasterze |
| Szybkość | N/A ten plaster | kolumna INSERT/GET |
| SQL | N/A | nullable UUID + złożone FK |
| N+1 | — | |
| Dług / człowiek | — | reuse wzorca 242.0 |
| Proza operatora | 5–15 zdań | dopisek `katalogi.md` |
| Docs/OS | — | |
| Pushy do zielonego | — | |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | — | |
