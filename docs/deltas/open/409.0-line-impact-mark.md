# Plaster 409.0 · EXP3.3b `line_impact_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `line_impact_mark` per tenant: `mark_code` + `impact_kind`
`line`|`plant`|`sku`|`other` + `source_ref` (`tenant:manual` | `fixture://line-impact-mark/…`).

Klon wzorca `chassis_mark`:
- migracja `319` (revises `318_chassis_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_line_impact_marks`
- UI `/line-impact-marks`, business-lists + ops-index `SHIPPED["409.0"]`
- operator `docs/operator/line-impact-mark.md`
- izolacja RLS; POST `amount`/`score`/`ebitda` → 422

Znacznik skutku linii gdy dane — bez SQL impact / EBITDA.

## Poza zakresem

SQL line impact · EBITDA · kwota · scoring · live plant feed

## Kryteria

- [ ] POST z kodem, impact_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/ebitda → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

SQL line impact · EBITDA · plant feed
