# Plaster 84.0 · S22 porównanie → `charge` (M-31)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S22  
**Zależy od:** 24.0 ślad `response_comparison` · 1.2 `charge` / `margin()`

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-31**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** spread **nie** jest nową kolumną ani odejmowaniem w JS. Operator na `/quotations` (panel 24.0) zapisuje istniejącą opłatę: kupno = kwota `channel_quote`, sprzedaż = kwota wyceny, kod z wyceny. `POST /charges` + `margin()` w domenie. 24.0 zestawienie zostaje. Zero nowej tabeli.

**Łowca:** ISTNIEJE 24.0 panel + `createCharge` + `margin()`. BRAK przycisku zapisu z porównania. PODOBNE: formularz `/charges`.

**Odrzucone:**

- `quotation.amount − channel.amount` w JS / nowy endpoint różnicy.
- Druga kolumna spread.
- Live HTTP (S21 parked).
- won/lost (S24).
- F9.1.

## Zakres

1. Helper `comparisonChargeBody` — kopiuje stringi kwot, nie odejmuje.
2. UI `/quotations`: „Zapisz marżę” w `data-response-comparison="lanes"`. Marża z odpowiedzi API przez `<Money/>`.
3. `SHIPPED_CHARGE_ROUTES["84.0"]` = `/quotations`.

## Poza zakresem

Nowy endpoint silnika · won/lost · live HTTP · nowa tabela · F9.1

## Ustalenia

- Żywy ID: **M-31**. Token: `response_comparison`.
- How-to: `docs/operator/ścieżka-pieniędzy.md` albo `katalogi.md`.
- OpenFGA: istniejące `can_manage_charges`.
- Różne waluty = 400 z `margin()`.

## Schemat

Bez migracji. Zapis do `charge`.

## Kryteria akceptacji

- [ ] Brak `-` na `amount` w panelu / helperze
- [ ] Odpowiedź pokazuje `margin_amount` przez `<Money/>`
- [ ] `just api-types` nie rusza (brak nowego endpointu)
- [ ] `OPS_JOBS` = 49

## Pytania

Brak. `/noc` akceptuje deltę.
