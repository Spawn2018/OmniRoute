# Plaster 271.0 · S55 `exchange_connector` HITL (Trans.eu fixture)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § S55 / Fala X · [dostepy-do-zdobycia.md](../../analysis/dostepy-do-zdobycia.md) § 4 P0 Trans.eu · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § 3.5 `exchange_code`
**Zależy od:** 0.3 tenancy · 270.0 `idp_connector` / 268.0 `erp_connector` (wzorzec katalogu HITL)
**Status:** Zaakceptowany `/noc` 2026-09-10. Wolno `/plaster`. Nie live HTTP. Nie sekrety. Nie SPA. Nie OAuth2.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover S55 / Fala X: katalog HITL fixture jednej giełdy jako dane; nie live giełda i nie X1–X5 SPA):** leftover live Trans.eu / TIMOCOM / Transporeon / Teleroute HTTP parked (brak umowy, brak sekretu, brak testu live). leftover auto-post frachtu parked. leftover scrape / czat giełd parked (zakaz na stałe). leftover X1 portal klienta / X2 portal przewoźnika / X3 apka / X4 konsument outboxa / X5 OAuth2 parked (CURRENT: nie budować SPA ani logowania). leftover CT7 p44/FourKites/Shippeo parked (następny na osi po tym Q). leftover CI9 parked. leftover S53 ponownie parked (270.0 zamknięty). leftover C9 snapshot Trans.eu parked. leftover V5b `exchange_message` parked. leftover diada jako marka parked — w kartach **diada** = jeden pracownik × jeden odbiorca (`mail_draft` 47.0), nie firma. Żywy plaster: **nowa tabela** `exchange_connector` per tenant — HITL katalog konektora giełdy jako dane, nie posting i nie publiczny portal. GLOSSARY nie ma `portal_connector` / `exchange_connector`; kanon jak `erp_connector`: **konektor giełdy** = `exchange_connector` (nie `portal_helper`, nie `trans_eu_utils`, nie kolumny na `network`). Token `system_kind` = `trans_eu` — pierwsza nazwana giełda P0 w [dostepy-do-zdobycia.md](../../analysis/dostepy-do-zdobycia.md) § 4 i pierwszy token `exchange_code` w pola-wizja § 3.5. Nie `portal_connector`: X1/X2 to SPA klienta/przewoźnika (odrzucone); „portale” w leftover S55 = tablica giełd, nie login. Wiersz = `connector_code` (snake 2–32) + `system_kind` (`trans_eu` — jedyny token w tym Q) + `source_ref` (`tenant:manual` albo `fixture://portal/…`). Zero kolumn sekretu / ciphertext / client_secret / URL. Operator wpisuje kod i token `trans_eu`; serwis nie woła HTTP, nie scrapuje, nie postuje frachtu, nie woła LLM, nie importuje `charges` / `networks` / `mail_drafts` / `outbox_events` / `idp_connectors` i nie liczy marży. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły kind → 400 (`system`). Obce `source_ref` → 400 (`obce`). Body z polem sekretu (`client_secret`, `ciphertext`, `base_url`) → 422 (`extra=forbid`). Unique `(organization_id, connector_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/exchange-connectors`. `SHIPPED_CHARGE_ROUTES["271.0"]` = `/exchange-connectors`. OpenFGA: `can_manage_exchange_connectors`. Migracja `204_exchange_connector` (po `203_idp_connector`). Job OPS 117. Marża zostaje na `charge`. JWT hello / `idp_connector` bez zmian.

**Łowca:** BRAK tabeli `exchange_connector`. BRAK `portal_connector`. BRAK `exchange_message` w kodzie (tylko karta V5b). BRAK nazwy w GLOSSARY (`exchange_connector`, `portal_connector`) — ten plan dopisuje **konektor giełdy**. ISTNIEJE `telematics_connector` 197.0 — GPS; token `trans_eu` tam jest **odrzucony** (nie giełda). ISTNIEJE `network` / `network_member` — stowarzyszenie WCA, nie giełda frachtu. ISTNIEJE `idp_connector` 270.0 / `erp_connector` 268.0 / `terminal_slot_connector` 269.0 — katalog adaptera HITL, **inny** job (IdP / ERP / slot). ISTNIEJE `mail_draft` — diada pracownik×odbiorca, nie tablica giełd. ISTNIEJE `/session` JWT hello — nie ruszać. PODOBNE: kod + kind + `source_ref` jak F9. Nie wyodrębniać wspólnej warstwy (drugie, nie trzecie powtórzenie).

**Odrzucone:**

- live HTTP Trans.eu / TIMOCOM / Transporeon / Teleroute / auto-post frachtu.
- scrape HTML / czat giełd / Citizen API.
- pięć tablic w jednym CHECK (`timocom`, `transporeon`, `teleroute`, `transporeon_insights`).
- X1–X5 SPA klienta/przewoźnika, apka, konsument outboxa, OAuth2 login.
- `portal_connector` jako publiczny portal / atrapa logowania.
- kolumny sekretu / ciphertext / `base_url` / live issuer.
- S53 ponownie / zmiana `/session` / `idp_connector`.
- CT7 p44/FourKites/Shippeo; CI9; C9 snapshot; V5b `exchange_message`.
- firma „Diada”; import charges/networks/mail_drafts/outbox/idp; kwota / marża / float / HTTP.

## Zakres

1. Migracja `204_exchange_connector`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK `system_kind = 'trans_eu'`. Zero kolumn sekretu / URL / kwoty.
2. POST/GET. Zły token → 400 (`oznaczenie` / `system` / `obce`). Body z sekretem → 422. Serwis nie importuje charges/networks/mail_drafts/outbox/idp i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/exchange-connectors`. How-to `docs/operator/konektor-gieldy.md`; fold w `katalogi.md` (40 linii). Job OPS 117. OpenFGA `can_manage_exchange_connectors`. Nazwy: `ExchangeConnectorDesk` / `exchange-connector-form.tsx` / `persistExchangeConnector`. Brak przycisku „Wystaw fracht” / OAuth / publicznego portalu.

## Poza zakresem

live giełda · scrape · X1–X5 SPA · OAuth2 · outbox consumer · pięć tablic · S53 · CT7/CI9 · sekrety · kwota · float

## Kryteria akceptacji

- [ ] POST zapisuje `exchange_connector` z kodem, `system_kind=trans_eu` i `source_ref`, bez HTTP
- [ ] zły kod → 400 (`oznaczenie`); zły kind (m.in. `timocom`) → 400 (`system`); obce `source_ref` → 400 (`obce`)
- [ ] POST z `client_secret` / `ciphertext` / `base_url` **nie** zapisuje sekretu (422)
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] `/session` i `idp_connector` bez zmian w tym plasterze
- [ ] Zero float / marży / ciphertext / live HTTP / importu charges/networks/mail_drafts/outbox/idp

## Leftover po 271.0

Live giełda (gdy umowa + sekret + test). TIMOCOM / Transporeon / Teleroute. X1–X5 SPA / OAuth2 / konsument outboxa. C9 snapshot. V5b `exchange_message`. CT7/CI9 / reszta pinu (następny na osi `/noc`).
