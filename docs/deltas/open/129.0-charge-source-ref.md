# Plaster 129.0 · P0 leftover `charge.source_ref` (M-08)

**Spec źródłowa:** [docs/spec/charge.md](../../spec/charge.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Leftover P0 · [karty-pol-fala-p.md](../../analysis/karty-pol-fala-p.md)
**Zależy od:** 1.2 tabela `charge` · 0.3 RLS · 0.4 OpenFGA
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Ten sam żywy ID **M-08**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki P0):** kolumna `source_ref` na **istniejącym** `charge`. W bazie **nullable** (stare fixture bez zmyślonego pochodzenia). Nowy INSERT — `POST /charges` i helper porównania — **wymaga** niepustego tekstu przez istniejące `require_source_ref`. Job operatora na tym samym `/charges`. Zero nowej tabeli. Zero nowej marży.

**Łowca:** ISTNIEJE 1.2 `charge` + `ChargeService.create_charge` + `POST /charges` + UI `/charges` + `can_manage_charges`. ISTNIEJE `app.domain.rate_line.require_source_ref` + `InvalidSourceRef`. BRAK kolumny `charge.source_ref`. PODOBNE: `rate_line.source_ref`, `port_surcharge.source_ref` — rozszerzyć CREATE, nie pisać drugiego walidatora. `rate_line_id` to wskazanie stawki kupna, nie pochodzenie wiersza opłaty.

**Odrzucone:**

- Nowa tabela / kolumna magazynu marży — HC-02 zostaje na `charge`.
- `NOT NULL` na całej kolumnie + backfill — zmyślałoby `source_ref` starym fixture.
- CHECK prefixu w SQL — zablokowałoby V2b (myto) i F11 (znaczek PP), których prefiksów ten plaster nie zna.
- Silnik myta V2b, znaczek PP / F11, accept HITL → `charge` — ci pisarze **użyją** kolumny później; tu tylko obowiązek na dzisiejszym INSERT.
- ExtractionService zapisujący `charge` / `rate_line`.
- Nowe OpenFGA. Nowa trasa. Kwota / float / LLM.

## Zakres

1. Nowa migracja ALTER (numer z head `alembic/versions/`, nie zgadywać w kodzie planu): `charge.source_ref` TEXT NULL, max jak `require_source_ref` (512). RLS z 009 bez zmian.
2. Model + `ChargeService.create_charge` + `ChargeCreate` / `ChargeResponse`: `source_ref` obowiązkowy na INSERT (pusty / nie-tekst → 400). Lista oddaje `null` na starych wierszach.
3. UI `/charges`: pole pochodzenia przy „Dodaj opłatę” + kolumna. `chargeCreateBody` i `comparisonChargeBody` przekazują `source_ref` (porównanie: `tenant:manual:comparison` albo pole operatora — nie puste). `SHIPPED_CHARGE_ROUTES["129.0"]` = `/charges`. Bez nowej trasy. OPS_JOBS bez +1.
4. `just api-types`. Spec `charge.md`. How-to: zdanie w `docs/operator/katalogi.md` + `ścieżka-pieniędzy.md` (pochodzenie na opłacie).

## Poza zakresem

V2b myto · F11 znaczek PP · accept HITL → `charge` · mutacja `source_ref` w miejscu · backfill starych wierszy · druga tabela marży · ExtractionService · F9.1 · O0

## Ustalenia

- Token: `source_ref` (GLOSSARY: pochodzenie). Walidacja = `require_source_ref` (tekst, trim, niepusty, max 512). Bez allowlisty prefixu.
- Stare wiersze: NULL w bazie, `null` w JSON. Nowy INSERT bez ref = 400.
- OpenFGA: `can_manage_charges`. Brak nowej relacji.
- RLS: ta sama polityka 009. Nie nowa tabela — istniejący test izolacji `charge` zostaje.
- LLM nie liczy. `margin(buy, sell)` jedyna prawda o marży. HITL extract bez zmian.
- `rate_line_id` nietknięty (wskazanie kupna, nie origin).

## Schemat

`charge.source_ref` TEXT NULL.

## Kryteria akceptacji

- [ ] Kolumna nullable; nowy INSERT bez `source_ref` / pusty po trim → 400
- [ ] `require_source_ref` z `domain.rate_line` — brak drugiej kopii walidatora
- [ ] Istniejący test izolacji `charge` nadal przechodzi; brak nowej tabeli
- [ ] `/charges` pokazuje i wymaga pochodzenia przy zapisie
- [ ] `comparisonChargeBody` nie wysyła opłaty bez `source_ref`
- [ ] `just api-types`
- [ ] Zero `float` / drugiej marży / importu ExtractionService → charges
