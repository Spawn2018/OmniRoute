# Plaster 106.0 · S44 tablica faktów z FV (M-15)

**Spec źródłowa:** [docs/spec/finance-board.md](../../spec/finance-board.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S44  
**Zależy od:** 15.0 `/finance` · 96.0 `sales_invoice` · 97.0 `ksef_ref` · 1.2 `charge`

**Status:** Zaakceptowany `/noc` 2026-09-03. Ten sam żywy ID **M-15**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** `/finance` **czyta** też faktury sprzedaży (`fetchSalesInvoices`: `invoice_ref`, `invoice_kind`, `ksef_ref`). Kwoty zostają na `charge`. Zero nowej tabeli. Zero nowego BC. Zero nowego OpenFGA. Tablica 15.0 przestaje udawać, że finanse kończą się na marży, kursie i limicie. Link do `/invoices`. `SHIPPED_CHARGE_ROUTES["106.0"]` = `/finance`. OPS_JOBS długość 50.

**Łowca:** ISTNIEJE 15.0 `/finance` + `fetchCharges` / `fetchNbpRates` / `fetchParties` / `fetchCreditReviews`. ISTNIEJE 96.0 `fetchSalesInvoices`. BRAK FV na tablicy faktów. PODOBNE: 97.0 KSeF na `/invoices`. Dwa joby (zapis FV vs odczyt faktów) = `/invoices` vs `/finance`.

**Odrzucone:**

- Narracja NL / komentarz LLM — S57.
- Silnik limitu / auto-scoring.
- Suma faktur / odejmowanie / kopia `sell`.
- Zbiorcze członki na tablicy — leftover; `/invoices` zostaje zapisem.
- Nowa tabela `finance_*`.
- F9.1.

## Zakres

1. `/finance`: blok „Faktury” z `fetchSalesInvoices`. Bez `CatalogCreateForm`. Bez `parseFloat`.
2. Asercje 15.0 bez odejmowania / LLM zostają. Nowa asercja 106.0: `fetchSalesInvoices` na tablicy, zero `sell_amount`.
3. How-to `docs/operator/tablica-finansowa.md`. `just api-types` nie jest wymagane (brak nowego DTO).

## Poza zakresem

S57 narracja · silnik limitu · suma FV · collective_invoice na tablicy · nowa migracja · F9.1

## Ustalenia

- Token: `finance_board` (UI). How-to: `tablica-finansowa.md`.
- Blok FV: `<dl>` (nie `ul` z NBP, nie `flex flex-wrap` z limitów).
- LLM nie liczy. `charge` zostaje prawdą o marży. HITL extract bez zmian.
- OPS_JOBS zostaje długości 50.

## Schemat

Brak nowej tabeli.

## Kryteria akceptacji

- [ ] `/finance` listuje `sales_invoice` bez kwoty / `parseFloat` / `CatalogCreateForm`
- [ ] `SHIPPED_CHARGE_ROUTES["106.0"]` = `/finance`; OPS_JOBS = 50
- [ ] Zero nowej migracji / serwisu / relacji OpenFGA

## Pytania

Brak. `/noc` akceptuje deltę.
