# Plaster 534.0 · O6 leftover — `quote_recorded` przy accept `carrier_quote`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O6 leftover · [531.0](../archived/531.0-carrier-quote-hitl.md) leftover `quote_recorded` · [246.0](../archived/246.0-quote-recorded-event.md) · [133.0](../archived/133.0-entity-event.md)

**Zależy od:** 531.0 `mark_answered` w `AcceptExtractionToRates` · 246.0 POST `answered` → `quote_recorded` · kind `quote_recorded` w allowliście 133.0

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover O6 po **531.0**. Po `mark_answered` w `AcceptExtractionToRates._write_channel_quote` warstwa API dopisuje `entity_event` (`event_kind=quote_recorded`, `subject_kind=carrier_inquiry`, `subject_id` = inquiry, `source_ref` z inquiry). Ten sam kształt co 246.0. Bez `carrier_inquiry_id` — zero zdarzenia (regres 140.0 / 531.0).
- **Wybrane:** składanie w API (`AcceptExtractionToRates`), nie w `CarrierInquiryService` / `ExtractionService` (`CONFIRMED` — HC-04 / wzorzec 244–246).
- **Wybrane:** brak nowej tabeli / migracji. OpenFGA bez nowej relacji (`can_review_extractions`). Fold w [entity-event.md](../../operator/entity-event.md) + [ścieżka-pieniędzy.md](../../operator/ścieżka-pieniędzy.md). `SHIPPED` / job OPS bez nowego ekranu.
- **Odrzucone:** import `entity_events` w serwisie inquiries / extraction (`REJECTED`).
- **Odrzucone:** zdarzenie przy accept **bez** `carrier_inquiry_id` · zdarzenie na `channel_quote` jako subject · PATCH statusu inquiry · wymóg zawsze `carrier_inquiry_id` (`REJECTED` na tym wierszu).
- **Odrzucone:** float / marża / live HTTP / Temporal / auto-accept (`REJECTED`).
- **Odrzucone na tym wierszu:** leftover O7/O8 EXPLAIN · rfc822 · POST `channel_quote` → event · SH-R16 rytuały.

**Łowca:** ISTNIEJE `_append_queued_event` + `answered`→`quote_recorded` w `carrier_inquiries.py` (246.0). ISTNIEJE `mark_answered` w accept (531.0). ISTNIEJE `EntityEventService.create_event`. BRAK append `quote_recorded` po accept HITL. PODOBNE: cienkie wołanie `EntityEventService` w API po `mark_answered` (nie nowa tabela, nie import BC w services).

## Zakres

1. `AcceptExtractionToRates._write_channel_quote`: po udanym `mark_answered` → INSERT `entity_event` `quote_recorded`.
2. Testy: accept z inquiry → jeden `quote_recorded`; bez id → zero zdarzenia; regres POST answered (246.0); ExtractionService / CarrierInquiryService bez importu `entity_events`.
3. How-to fold + `SHIPPED` / docs pętli post-plaster.

## Poza zakresem

PATCH inquiry · obowiązkowe `carrier_inquiry_id` · event z POST `channel_quote` · EXPLAIN O7/O8 · rfc822 · live HTTP · Temporal · UI buy-desk poza istniejącym `/ai`

## Kryteria akceptacji

- [ ] Accept `carrier_quote` z `carrier_inquiry_id` → `channel_quote` + `answered` **oraz** jeden `entity_event` `quote_recorded`
- [ ] Accept bez `carrier_inquiry_id` → tylko `channel_quote`; zero `entity_event`
- [ ] Zero importu `entity_events` w `CarrierInquiryService` / `ExtractionService`
- [ ] Zero float / marży / live HTTP / Temporal; brak nowej migracji
- [ ] How-to: accept HITL też zapisuje `quote_recorded` (nie tylko POST answered)

## Leftover

wymóg zawsze `carrier_inquiry_id` · PATCH statusu inquiry · event z POST `/channel-quotes` · UI buy-desk poza `/ai`
