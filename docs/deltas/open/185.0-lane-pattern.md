# Plaster 185.0 · G2.19 `lane_pattern`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.19 · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) § G2.19–G2.22
**Zależy od:** 171.0 `tender_lane` (korytarz partii już jest — to nie ten obiekt)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie kalkulator kg. Nie circle_sim.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.15–G2.18 + G2.19–G2.22):** leftover kg/CBAM (G2.15–G2.18) zostaje leftover — nie silnik kg×współczynnik i nie drugi katalog towarowy (kolizja M-52 `dangerous_good`). Żywy plaster: jedna tabela `lane_pattern` per tenant — **HITL wzorzec korytarza z `source_ref`**, nie `circle_sim`, nie km i nie P z actuals. Wiersz = `origin_unlocode` + `destination_unlocode` (token UN/LOCODE 5 znaków przez istniejące `normalize_unlocode`, bez resolve `port`) + `source_ref` (`tenant:manual` albo `fixture://lane-pattern/…`). To nie jest `tender_lane` (tam FK do `tender_lot`). To nie jest `party_charge_override.lane_pattern` (luźny tekst przy wyjątku stawki). Operator wpisuje parę końców korytarza, z której później G2.20+ policzy nakładanie w SQL; serwis nie liczy km, nie woła LLM i nie czyta `charge` / `trip`. Zła para → 400 (`wzorzec`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, origin_unlocode, destination_unlocode)`. GET listy SQL. Serwis nie importuje tenders / tender_lanes / geography / charges / extraction. UI `/lane-patterns` bez `<Money/>`, bez `sum()` i bez mnożenia w JS. `SHIPPED_CHARGE_ROUTES["185.0"]` = `/lane-patterns`. OpenFGA: `can_manage_lane_patterns`. Migracja `125_lane_pattern`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tender_lane` — para UN/LOCODE na `tender_lot`, **nie** wzorzec z actuals. ISTNIEJE `party_charge_override.lane_pattern` — tekst przy wyjątku stawki, **nie** tabela G2.19. ISTNIEJE `party_lane_scorecard` — karta POL/POD kontrahenta. ISTNIEJE `normalize_unlocode` — format tokenu; **nie** drugi parser. ISTNIEJE `tender_carbon_mark` — znacznik śladu, **nie** kg. BRAK tabeli `lane_pattern`. BRAK `/lane-patterns`. BRAK `circle_sim`.

**Odrzucone:**

- kg / tCO₂e / CBAM katalog (leftover G2.15–G2.18; kolizja M-52).
- `circle_sim` ≥500k / km ładowny/pusty/dolot / P=n_overlap (leftover G2.20–G2.22).
- Kolumna na `tender_lane` / `party_charge_override`.
- Auto-award / scrape / LLM-VRP / copy „natychmiast”.
- Kwota / druga marża / float / T-SQL / live HTTP / resolve `port`.

## Zakres

1. Migracja `125_lane_pattern`: RLS, izolacja, indeks `(organization_id, origin_unlocode)`, unique `(organization_id, origin_unlocode, destination_unlocode)`, CHECK końce różne + format UN/LOCODE.
2. POST/GET. Zła para / `source_ref` → 400. Serwis nie importuje tenders / geography / charges / extraction i nie liczy km.
3. UI `/lane-patterns`. `just api-types`. How-to `docs/operator/wzorzec-korytarza.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `PatternDesk` / `patternWrite` / `persistPatternMark`.

## Poza zakresem

G2.15–G2.18 kg/CBAM · G2.20–G2.22 circle_sim/km/P · G2.23 · auto-award · scrape · LLM-VRP · kwota/km na wierszu · resolve `port` · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `lane_pattern` z parą UN/LOCODE i `source_ref`, bez km i bez circle_sim
- [ ] zła para (pusty / zły token / te same końce) → 400
- [ ] GET zwraca wzorce tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / kg / LLM / live HTTP / scrape / circle_sim
