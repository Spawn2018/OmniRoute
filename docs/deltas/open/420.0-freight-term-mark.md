# Plaster 420.0 · EXP1 `freight_term_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP1 shipment `freight_term`
**Zależy od:** —

## Zakres

HITL katalog `freight_term_mark` per tenant: `mark_code` + `term_kind`
(`prepaid`|`collect`|`third_party`|`other`) + `source_ref` (`tenant:manual` |
`fixture://freight-term-mark/`). Migracja RLS FORCE. UI `/freight-term-marks`.
OpenFGA `can_manage_freight_term_marks`.

**Wybrane:** węższy HITL katalog freight term jako dane.
**Odrzucone (EXP0.4):** nowy ban „500k” — **ISTNIEJE** `copy_ban_mark.ban_kind`
`five_hundred_k` (397.0). Nie dubluj.
**Dlaczego:** oś EXP0 domknięta (0.3 właśnie; 0.4/0.7 = skip duplikat); EXP1
`freight_term` jeszcze bez katalogu HITL.

## Poza zakresem

kolumna na `shipment` · kwota / marża · auto z Incoterms · extract LLM

## Ustalenia

Łowca: ISTNIEJE `copy_ban_mark` (EXP0.4/EXP4.19). BRAK `freight_term_mark`.
BRAK kolumny `freight_term` na shipment (leftover EXP1 pole — nie ten plaster).

## Kryteria akceptacji

- [ ] POST z kodem, term_kind, source_ref
- [ ] zły kod / kind / source_ref → 400; amount / margin → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] UI lista + formularz HITL; proza operatora 5–15 zdań
