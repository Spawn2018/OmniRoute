# Plaster 419.0 · EXP0.3 `slot_guarantee_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP0.3; leftover T8 „nie gwarancja”
**Zależy od:** 269.0 `terminal_slot_connector` (osobny byt — capability/godziny, nie stance)

## Zakres

HITL katalog `slot_guarantee_mark` per tenant: `mark_code` + `stance_kind`
(`capability`|`non_guarantee`|`other`) + `source_ref` (`tenant:manual` |
`fixture://slot-guarantee-mark/`). Migracja RLS FORCE. UI `/slot-guarantee-marks`.
OpenFGA `can_manage_slot_guarantee_marks`.

**Wybrane:** węższy HITL znacznik „slot ≠ gwarancja prawna” jako dane katalogu.
**Odrzucone (EXP0.7 holding):** nowa tabela `holding_mark` — **ISTNIEJE**
`company_mark.seat_kind` (hq|branch|other), `role_view_mark`, `party.parent_party_id`.
**Dlaczego:** łowca + zakaz dublowania jak `otif_split` vs `otif_mark`. EXP0.3
nadal bez osobnego znacznika stance; `terminal_slot_connector` to capability/godziny.

## Poza zakresem

live T8 · `confirmed` z POST · terminal_appointment · yard · kwota / marża · drugi tenant

## Ustalenia

Łowca: ISTNIEJE `terminal_slot_connector` (mode+godziny). ISTNIEJE `dock_appointment`.
BRAK `slot_guarantee_mark`. BRAK `holding_mark` (i nie budujemy — dubluje G10).

## Kryteria akceptacji

- [ ] POST z kodem, stance_kind, source_ref
- [ ] zły kod / kind / source_ref → 400; amount / confirmed → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] UI lista + formularz HITL; proza operatora 5–15 zdań
