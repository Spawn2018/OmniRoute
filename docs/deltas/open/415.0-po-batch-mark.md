# Plaster 415.0 · EXP3.0d `po_batch_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP3 (PO line batch); kolejka pin 2026-09-08c
**Zależy od:** 414.0 `po_sku_mark` (wzorzec HITL; bez współdzielenia tabeli)

## Zakres

HITL `po_batch_mark`: mark_code + batch_kind batch|lot|serial|other + source_ref.
Migracja `325`. UI `/po-batch-marks`. OpenFGA `can_manage_po_batch_marks`.

Wybrane: osobny katalog batch/lot na linii PO (domknięcie triad plant/SKU/batch z EXP3.0).
Odrzucone: rozszerzenie `po_plant_mark` o kolejne kindy; kolumna batch na `po_line`; live EDI.
Dlaczego: CURRENT wskazuje osobny obiekt HITL; plant i SKU mają własne katalogi.

## Poza zakresem

live EDI · auto shipment · qty/kwota · FK do `purchase_order` / `po_line` · WMS

## Ustalenia

Append-only INSERT. Bez UPDATE/DELETE wiersza. Bez float/marży. Bez HTTP zewnętrznego.

## Kryteria akceptacji

- [ ] POST z kodem, batch_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/qty → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0

## Leftover

live EDI · auto shipment · qty na linii PO
