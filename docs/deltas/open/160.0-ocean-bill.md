# Plaster 160.0 · D6 `ocean_bill`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D6 · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D6
**Zależy od:** 90.0 `shipment` · 111.0 `shipment_leg` `ocean_lcl` już jest (poza tym plasterem)
**Status:** Delta zaakceptowana `/noc` 2026-09-08. Nie booking HZ/S21. Nie pula.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z D6):** jedna tabela `ocean_bill` per tenant — **znacznik HBL/MBL na zleceniu**, bez PDF i bez bookingu. Konsolidacja (wiele house pod jeden master) i pula numerów M-03 = leftover. Wiersz = `bill_no` (2–32, `[A-Za-z0-9-]+`, numer listu jako dane, nie snake katalogu) + `bill_kind` (`hbl` | `mbl`) + FK złożone do `shipment` + `source_ref` (`tenant:manual` albo `fixture://ocean-bill/…`). `bill_kind` spoza allowlisty → 400 (detail zawiera `rodzaj`). Zły `bill_no` → 400 (detail zawiera `numer`). GET listy SQL. API składa odczyt `shipment`; serwis konosamentu nie importuje shipments / charges / geography. UI `/ocean-bills`. `SHIPPED_CHARGE_ROUTES["160.0"]` = `/ocean-bills`. OpenFGA: `can_manage_ocean_bills`. Migracja `100_ocean_bill`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `shipment_leg` `ocean_lcl` + `/lcl` — odcinek drobnicy, **nie** list; **nie** rozszerzamy (nie druga tabela LCL). ISTNIEJE `shipment_document` (`noted`/`attached`/`other`) — wskazanie dokumentu, testy bez `hbl`; **nie** rozszerzamy. ISTNIEJE `document_checklist_rule` / `document_dispatch_rule` kind `bill_of_lading` — wymóg na wycenie, nie numer HBL/MBL na zleceniu; **nie** rozszerzamy. ISTNIEJE `container` — 153.0 świadomie bez `bl_kind`. ISTNIEJE `booking_instruction` — instrukcja bookingu, nie list. ISTNIEJE `quotation.document_number` / M-03 prefiks — pula oferty, nie HBL. ISTNIEJE `edi_message` — komunikat, nie konosament. BRAK `ocean_bill`. BRAK `/ocean-bills`. BRAK `bill_kind` hbl/mbl na zleceniu.

**Odrzucone:**

- Booking armatorski / live HTTP HZ/S21.
- Druga tabela LCL / CFS / duplikat `ocean_lcl`.
- Kolumny HBL na `shipment_document` / `container` / `shipment`.
- Konsolidacja wielu house pod jeden MBL (leftover D6b).
- Pula numerów z prefiksu M-03 (leftover D6c; analog 72.0).
- PDF / bajty / wydruk D9 / HAWB (154.0 leftover air).

## Zakres

1. Migracja `100_ocean_bill`: RLS, izolacja, indeks `(organization_id, shipment_id)`.
2. POST/GET. Zły rodzaj / numer / `source_ref` → 400.
3. UI `/ocean-bills`. `just api-types`. How-to `docs/operator/konosament.md`.

## Poza zakresem

D6b konsolidacja · D6c pula M-03 · HZ/S21 · PDF · WMS · mapa · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje HBL albo MBL na zleceniu bez kwoty i bez PDF
- [ ] `bill_kind` spoza `hbl`/`mbl` → 400
- [ ] GET zwraca konosamenty tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / bookingu
