# Plaster 652.0 · leftover EXP1 quotation valid_until HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `quotation` · [431.0](../archived/431.0-quote-validity-mark.md)
**Zależy od:** M- wycena `quotation`; katalog `quote_validity_mark` (431.0) zostaje osobno
**Status:** Delta zaakceptowana `/noc 17` 2026-09-24. Wolno `/plaster`. Nie float. Nie NBP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 kolumn ważności wyceny):** tabela `quotation` zostaje prawdą o ofercie. Katalog `quote_validity_mark` (431.0) zostaje katalogiem stance — **nie** FK. Żywy plaster: **opcjonalny** `valid_until` (`DATE` nullable) na `quotation` — kalendarzowa ważność HITL. Operator ustawia datę; serwis nie liczy D+N, nie mnoży NBP, nie tworzy `charge`, nie woła HTTP/LLM. POST/PATCH bez pola → `null`. Zła data → 400. Istniejące wiersze: `null`. GET listy SQL. UI `/quotations`. `SHIPPED_CHARGE_ROUTES["652.0"]` = `/quotations`. OpenFGA bez nowej relacji. Migracja `523_quotation_valid_until` po `522_rate_line_fuel_index_id`. Marża zostaje na `charge`. Zero float.

**Dlaczego tylko `valid_until`, nie trójka:** leftover 431.0 wymienia trzy kolumny. Najwęższy plaster = jedna data ważności; `revision_no` / `supersedes_id` = osobne leftovery.

**REQUIREMENT:** leftover EXP1 po 651.0 / 431.0. Źródło: CURRENT Następny **652.0**; docs-debt 431.0.

**Łowca:** ISTNIEJE `quotation`. ISTNIEJE `quote_validity_mark` (stance). ISTNIEJE `tender_quote.valid_until` — inny BC. BRAK `valid_until` na `quotation`. BRAK float / NBP.

**Odrzucone:**

- `revision_no` / `supersedes_id` w tym plasterze (leftover)
- FK do `quote_validity_mark`
- float / NBP / mnożenie / Deadline Engine
- zapis `charge` / auto-award
- live HTTP

## Zakres

1. Migracja `523_quotation_valid_until`: kolumna `DATE` nullable.
2. Domain `require_valid_until`; API create/update opcjonalnie; UI `/quotations`.
3. How-to / fold. `just api-types`. `SHIPPED_CHARGE_ROUTES["652.0"]`.

## Poza zakresem

revision_no · supersedes_id · spot_or_contract na quotation (park) · Deadline Engine · NBP · float · charge

## Plan plików

- `backend/alembic/versions/523_quotation_valid_until.py`
- model / domain / serwis / API / testy quotations
- `frontend/src/features/quotations/` + API client
- `ops-index` + `routes-breadth`
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST/PATCH z `valid_until` ISO date zapisuje; bez pola → null
- [ ] zła data → 400; serwis nie mnoży NBP i nie tworzy charge
- [ ] RLS: tenant B nie widzi wycen A; zero float / marży
- [ ] `quote_validity_mark` bez zmian

## Leftover

revision_no · supersedes_id · spot_or_contract na quotation (park) · matching · Deadline Engine
