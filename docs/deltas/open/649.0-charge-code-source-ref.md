# Plaster 649.0 · leftover EXP1 charge_code source_ref + kody waiting/no_show/diversion/stamp

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [648.0](../archived/648.0-rate-line-index-id.md) · [1.0](../archived/) charge_code
**Zależy od:** M-06 `charge_code` (katalog); 648.0 zamknięty pin indeksu na stawce
**Status:** Delta zaakceptowana `/noc 17` 2026-09-24. Nie matching. Nie float. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 charge_code waiting/no_show/diversion/stamp + source_ref):** katalog `charge_code` zostaje prawdą o tokenie opłaty. Żywy plaster: **pogłębienie istniejącej** `charge_code` — wymagany `source_ref` (tekst 1–512, `tenant:manual` albo `fixture://charge-code/…`) przy POST. Istniejące wiersze: backfill `fixture://charge-code/legacy` w migracji (NOT NULL). Operator może utworzyć kody `WAITING` / `NO_SHOW` / `DIVERSION` / `STAMP` jak każdy inny token — **bez** CHECK allowlisty i bez seedu Omni w tym plasterze (seed = leftover). Serwis nie tworzy `rate_line` / `charge`, nie woła sieci, nie woła LLM. GET listy SQL. UI `/charge-codes`. `SHIPPED_CHARGE_ROUTES["649.0"]` = `/charge-codes`. OpenFGA bez nowej relacji. Migracja `521_charge_code_source_ref` po `520_rate_line_index_id`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**Dlaczego source_ref najpierw, nie seed czterech kodów:** karty EXP1 wiążą tokeny waiting/… z `source_ref`. Bez pochodzenia katalog łamie HC stawek. Seed Omni czterech kodów to osobny leftover (dane, nie schemat). CHECK listy zamknąłby THC/FREIGHT.

**REQUIREMENT:** leftover EXP1 po 648.0. Źródło: `docs/analysis/karty-pol-fala-exp.md` § EXP1; CURRENT Następny **649.0**.

**Łowca:** ISTNIEJE `charge_code` — code + name + aliases, **nie** source_ref. ISTNIEJE `rate_line.source_ref`. BRAK `source_ref` na `charge_code`. BRAK matching. BRAK float.

**Odrzucone:**

- float / druga marża / zapis `charge` / `rate_line` z tego serwisu
- CHECK allowlisty tylko WAITING|NO_SHOW|DIVERSION|STAMP
- seed Omni czterech kodów w tym plasterze (leftover)
- live HTTP / mutacja wiersza (katalog bez supersede — jak dziś)

## Zakres

1. Migracja `521_charge_code_source_ref`: kolumna `source_ref` TEXT NOT NULL; backfill legacy; CHECK długości opcjonalny.
2. POST wymaga `source_ref` (1–512 po trim). Pusty → 400. Serwis nie importuje rate_lines / charges.
3. UI `/charge-codes` (pole pochodzenia). How-to wymienia kody EXP1 WAITING/NO_SHOW/DIVERSION/STAMP jako konwencję. `just api-types` jeśli dotyczy.

## Poza zakresem

seed Omni czterech kodów · CHECK allowlisty · FK · matching · float · charge · live HTTP · supersede charge_code

## Plan plików

- `backend/alembic/versions/521_charge_code_source_ref.py`
- model / domain / serwis / API / testy charge_codes
- `frontend/src/features/charge-codes/` + API client
- `ops-index` + `routes-breadth` (`649.0` → `/charge-codes`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z `source_ref` zapisuje wiersz; brak / pusty → 400
- [ ] istniejące wiersze mają `fixture://charge-code/legacy` po migracji
- [ ] serwis nie importuje rate_lines / charges i nie woła HTTP
- [ ] RLS: tenant B nie widzi kodów tenanta A; zero float / marży / matching
- [ ] WAITING/NO_SHOW/DIVERSION/STAMP wolno utworzyć jak każdy inny kod (bez CHECK)

## Leftover

seed Omni WAITING/NO_SHOW/DIVERSION/STAMP · FK fuel_index · spot_or_contract na quotation · matching · live MQC
