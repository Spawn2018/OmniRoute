# Plaster 19.0 · M-26 Dokument oferty (`offer_document` przy `quotation`)

**Spec źródłowa:** [docs/spec/offer-document.md](../../spec/offer-document.md)  
**Zależy od:** 2.0/5.1 `quotation` · 0.25 `<Money/>`

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie U-print.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-26):** operator widzi **dokument z faktów**, które już są na wycenie. Token `offer_document` (UI, nie tabela). Żywy ID **M-26**.

**Łowca:** BRAK `offer_document` / `quotation_pdf` w kodzie. ISTNIEJE: `Quotation` + `<Money/>`. PODOBNE: 15.0 tablica odczytu. U-print w PLAN = B/L/FV **Fala 5/6**, nie ten plaster.

**Odrzucone:**

- Tabela blob/PDF — zgadywanie schematu; PDF-teatr (PLAN U-print).
- `window.print` / `@media print` — leftover U-print, nie M-26.
- LLM generujący treść oferty — extract only; HITL zostaje.
- Mutacja `quotation.amount` / `charge`.

## Zakres

Panel na `/quotations`. `data-offer-document="preview"`. Brak migracji.

## Poza zakresem

PDF · druk B/L/FV · szablon · ExtractionService · LLM

## Ustalenia

- Token: `offer_document` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations` na liście.
- RLS: bez nowej tabeli.
- Job: ten sam `/quotations`. `SHIPPED_CHARGE_ROUTES["19.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/quotations` ma `data-offer-document="preview"` i pokazuje `<Money/>` z wiersza
- [ ] Brak nowej migracji
- [ ] Brak `window.print` / jspdf / html2canvas
- [ ] `just dup` ≤ 3%

## Knowledge

Nie RAG na treść oferty. Nie karta PDF.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. PDF i U-print świadomie leftover.
