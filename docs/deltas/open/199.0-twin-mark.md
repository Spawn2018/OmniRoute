# Plaster 199.0 · W1 `twin_mark` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala W · [karty-pol-fala-w.md](../../analysis/karty-pol-fala-w.md) § W1
**Zależy od:** 0.3 tenancy (katalog per tenant) · 151.0 `resource` / 153.0 `container` (powiązanie leftover)
**Status:** Otwarty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie fizyka. Nie circle_sim.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover W1 8 twinów + `plan_snapshot` + V7 tacho + V8 what-if + leftover V6 EBITDA + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7 tacho TO_VERIFY parked. leftover V8 what-if parked. leftover V6 silnik EBITDA / `sla_clause` parked. leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b parked. leftover V4 AIS parked. leftover V2b myto parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. `plan_snapshot` i 8 silników fizyki zostają leftover. Żywy plaster: jedna tabela `twin_mark` per tenant — **HITL znacznik rodzaju bliźniaka jako dane**, nie symulacja i nie kółka. Wiersz = `twin_kind` (`vehicle` | `driver` | `container` | `shipment` | `network` | `plan` | `office` | `cargo`) + `source_ref` (`tenant:manual` albo `fixture://twin-mark/…`). Operator wpisuje postać; serwis nie liczy fizyki, nie woła HTTP, nie woła LLM i nie czyta `charge` / `resource` / `trip` / `plan_snapshot`. Zły rodzaj → 400 (`postać`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/twin-marks` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["199.0"]` = `/twin-marks`. OpenFGA: `can_manage_twin_marks`. Migracja `139_twin_mark`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `resource` 151.0 — flota, **nie** twin. ISTNIEJE `container` 153.0 — ISO 6346, **nie** twin. ISTNIEJE `shipment` / `network` — joby operacyjne, **nie** odzwierciedlenie. ISTNIEJE `entity_event` — ledger, **nie** stan 8. ISTNIEJE `tower_impact` — etap łańcucha, **nie** twin. BRAK tabeli `twin_mark`. BRAK `plan_snapshot`. BRAK silnika fizyki.

**Odrzucone:**

- 8 silników symulacji / fizyka / kółka G2.20.
- Tabela `plan_snapshot` i what-if V8.
- Tacho / posting (leftover V7 TO_VERIFY).
- Silnik EBITDA / sla_clause / scoring osoby.
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `139_twin_mark` (po `138_tower_impact`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind.
2. POST/GET. Zły rodzaj → 400. Serwis nie importuje resources / trips / charges / extraction i nie woła HTTP.
3. UI `/twin-marks`. `just api-types`. How-to `docs/operator/twin.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `TwinDesk` / `twinWrite` / `persistTwinMark`. Formularz radio (nie `<select>` klonów HITL).

## Poza zakresem

plan_snapshot · fizyka · circle_sim · tacho · EBITDA · live GPS · AIS · myto → charge · kwota · auto-award · T-SQL

## Kryteria akceptacji

- [ ] POST zapisuje `twin_mark` z `twin_kind`, bez HTTP
- [ ] zły `twin_kind` → 400 (`postać`)
- [ ] obce `source_ref` → 400 (`obce`)
- [ ] GET zwraca wiersze tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / fizyki / live HTTP / scrape / circle_sim
