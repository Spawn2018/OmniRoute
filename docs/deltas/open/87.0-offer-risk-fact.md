# Plaster 87.0 · S25 fakty ryzyka (M-24)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S25  
**Zależy od:** 17.0 ślad `offer_risk` · 14.0 `credit_review` · 2.0 `quotation`

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-24**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** fakt ryzyka = **wskazanie** recenzji kredytowej na wycenie, nie scoring. Nullable `quotation.noted_credit_review_id`. PATCH zapisuje UUID. 17.0 resolve/karta zostaje odczytem. Kwota i `margin()` nietknięte. Bez FK — API składa (jak 85.0).

**Łowca:** ISTNIEJE 17.0 panel `resolveCreditReview` + `fetchPartyScorecard`. BRAK kolumny faktu. PODOBNE: 85.0 wskazanie kanału; 14.0 zapis recenzji na `/credit-reviews`.

**Odrzucone:**

- Silnik score / `risk_score` / ocena osoby.
- Druga kwota / nadpisanie `quotation.amount`.
- Zapis `credit_review` z panelu wyceny.
- Live HTTP biura · F9.1.

## Zakres

1. Migracja `046_quotation_noted_credit_review`: kolumna UUID NULL.
2. `PATCH /quotations/{id}/note-risk` — `credit_review_id`. OpenFGA `can_manage_quotations`. API: `CreditReviewService.get`, potem UUID w serwisie wyceny.
3. UI `/quotations` w panelu 17.0: „Zapisz fakt”. `SHIPPED_CHARGE_ROUTES["87.0"]` = `/quotations`. `OPS_JOBS` = 49.

## Poza zakresem

Scoring · BIK/KRD · mutacja recenzji · S26 załącznik wywiadowni · F9.1

## Ustalenia

- Token: `offer_risk`. How-to: `ścieżka-pieniędzy.md`.
- Serwis `quotations` nie importuje `credit_reviews` / `parties` / `charges`.
- Katalog `/quotations` nadal bez `risk_score`.

## Schemat

`quotation.noted_credit_review_id` UUID NULL.

## Kryteria akceptacji

- [ ] Kolumna bez score; izolacja zostaje na `quotation`
- [ ] Nieznana recenzja → 400
- [ ] Brak `risk_score` i odejmowania na `/quotations`
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
