# Plaster 483.0 · AI8.2 brama L3 — katalog HITL

**Status:** Delta zaakceptowana `/noc 20` 2026-09-14 (bez osobnego kliknięcia).

**Spec źródłowa:** [VISION.md](../../VISION.md) § D.7 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI8.2

**Zależy od:** tenancy RLS · `autonomy_level` (słownik poziomów zostaje) · nie L3 write

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `l3_gate_marks`. Tabela `l3_gate_mark`: `mark_code` + `gate_kind` `sot|owner|exception|rollback|blast|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z AI8.2 leftover — katalog HITL **checklisty bramy przed L3** (źródło prawdy / właściciel / wyjątek / rollback / promień wybuchu), nie silnik zapisu AI i nie mutacja `autonomy_level`.
- **Wybrane:** POST/GET `/api/v1/l3-gate-marks`. OpenFGA `can_manage_l3_gate_marks` (member). UI `/l3-gate-marks`. `SHIPPED["483.0"]` → `/l3-gate-marks`.
- **Wybrane:** Alembic `388_l3_gate_mark` (`down_revision = "387_automation_bias_mark"`).
- **Wybrane:** how-to `docs/operator/l3-gate-mark.md` + jedna strona SOP agenta `docs/operator/agent-l3-sop.md` (po co / dane / read-write / kiedy człowiek / log / rollback / właściciel) — job 3 z D.7 jako proza, nie BC.
- **Odrzucone:** silnik L3 write / auto-zejście jakości jako kod (`REJECTED` — HC-04; park).
- **Odrzucone:** scoring Pain×Frequency · Bertha · FK `autonomy_level` → party (`REJECTED`).
- **Odrzucone:** AI5.0 `data_source` (`REJECTED` na tym wierszu — park licencja).
- **Odrzucone:** rozszerzenie samego `autonomy_level` o CHECK 0–5 (`REJECTED` — słownik otwarty zostaje; tu osobna checklista).

**Łowca:** ISTNIEJE `autonomy_level`. ISTNIEJE `automation_bias_mark`. BRAK `l3_gate_mark`.

## Zakres

How-to + OPS_JOB + SOP 1 strona. FE: `gate_kind` jako pionowe przyciski (różne od select 482 / segmented 481 / radio 480).

## Poza zakresem

L3 write · quality auto-down · ingest AI5 · scoring · kwota · Bertha

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to + agent-l3-sop

## Leftover

auto-zejście jakości przed L3 · AI5.0 ingest · program PDF AI9.2 · ui-04 pewność
