# Plaster 581.0 · T3 leftover — HITL `container.temp_min`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [580.0](../archived/580.0-container-unload-date.md) · [229.0](../archived/229.0-container-reefer.md)

**Zależy od:** 153.0 `container` · 229.0 `reefer` · 580.0 `unload_date`

**Status:** Zaakceptowana `/noc` 2026-09-19. Nie `temp_max`. Nie `needs_external_power`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po pięciu datach SPEED karta T3 zostawia `temp_min` (Decimal °C). W `backend/app` kolumny nie ma. Temperatura może być ujemna — **nie** `_require_positive_kg`. Nowy helper `_require_temp_c` + `require_container_temp_min`: `None`/pusty → `null`, float/bool → 400, nie-Decimal → 400, etykieta `temp. min`. POST bez pola → `null`. Nie egzekwuje `reefer=true` w tym plasterze (dane HITL; karta „temp tylko gdy reefer” = proza, nie 409).
- **Wybrane:** UI `/shipments`, string (`optionalToken`). `SHIPPED["581.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `461_container_temp_min` (po `460_container_unload_date`). Pole w istniejącej linii `_write_from_body`.
- **Odrzucone:** `temp_max` / `needs_external_power` (`REJECTED` — węższe pole). float (`REJECTED`). N3 countdown (`REJECTED` → park). 409 gdy `reefer=false` (`REJECTED` w tym plasterze). ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `temp_min` w `backend/app`. ISTNIEJE `reefer` 229.0. ISTNIEJE `_require_positive_kg` — **nie reuse** (ujemne °C). Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `461_container_temp_min`: kolumna nullable `Numeric(14, 4)`.
2. Domain `require_container_temp_min` + API POST opcjonalny + GET (string jak kg).
3. UI `/shipments`. How-to dopisek. `SHIPPED["581.0"]`.

## Poza zakresem

`temp_max` · `needs_external_power` · N3 · ciphertext · AI3-payload · G0-SH · kwota · live HTTP · 409 vs reefer

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `temp_min="-18"` zapisuje Decimal
- [ ] float / bool / zły token → 400 (`temp. min`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero float / countdown / mutacja `reefer`

## Leftover

`temp_max` · `needs_external_power` · egzekucja „tylko gdy reefer” · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH
