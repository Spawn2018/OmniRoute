# Plaster 294.0 · CT7 `visibility_connector` tokeny FourKites / Shippeo

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT7 · [karty-pol-fala-ct.md](../../analysis/karty-pol-fala-ct.md)  
**Zależy od:** 275.0 `visibility_connector` (kind `p44`) · 0.3 tenancy  
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie live HTTP. Nie sekrety. Nie AIS.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe leftover CT / pin po 293.0):** leftover CT7 FourKites / Shippeo jako **osobne tokeny** na istniejącej tabeli `visibility_connector` (275.0 świadomie zostawił tylko `p44`). U1 masowy / wspólny SELECT / CT2 / SQL FV vs charge / workflow CAPA / live SOAP / CT5–CT8 live / live p44 / paper-form `source_ref` (reszta) / CI1 extract = **park** (CURRENT). CT9 = już C5 `carbon_method` (park w docs-debt 285.0). Żywy plaster: rozszerzyć CHECK + domenę + UI — `system_kind IN ('p44', 'fourkites', 'shippeo')`. Nadal zero HTTP, zero sekretów, zero UPDATE/DELETE. Operator wybiera markę; wiersze różnych kind mogą współistnieć w katalogu (jak `sap`|`oracle`); „drugi vendor jako prawda” live zostaje zakazem karty CT7.

**Łowca:** ISTNIEJE `visibility_connector` + `parse_visibility_row` (tylko `p44`). ISTNIEJE UI `/visibility-connectors`. BRAK tokenów `fourkites` / `shippeo` w CHECK. PODOBNE: `sap_connector` dwa kind. Nie nowa tabela. Nie wspólna warstwa connectorów.

**Odrzucone:**

- live HTTP p44 / FourKites / Shippeo / track feed
- nowa tabela / drugi BC
- U1 masowy / wspólny SELECT / CT2 / CT9 silnik kg / CI1 extract
- paper-form `source_ref` (to `/refaktor`, nie ten Q)
- sekrety / `api_key` / `base_url` / ciphertext
- float / marża / UPDATE/DELETE

## Zakres

1. Migracja `224_visibility_connector_vendors` po `223_shipment_asn_id`: drop/recreate `ck_visibility_connector_kind` → `p44|fourkites|shippeo`. Model + domena allowlista. Bez nowej tabeli RLS.
2. Testy: POST `fourkites` / `shippeo` OK; zły kind → 400; sekrety → 422; izolacja bez zmian zachowania.
3. UI radio/segment trzech kind; how-to + GLOSSARY + AGENTS BC. Job OPS bez nowego ekranu (ta sama trasa). `SHIPPED_CHARGE_ROUTES` bez nowej ścieżki.

## Poza zakresem

live p44/FourKites/Shippeo · CT2 · U1 masowy · wspólny SELECT · CT5/CT8 live · paper-form source_ref · CI1 · sekrety · float

## Ustalenia

- Tokeny: `fourkites`, `shippeo` (jak asercje 275.0, bez podkreślnika).
- Unique kod / `source_ref` bez zmian.
- OpenFGA: bez nowej relacji (ta sama `can_manage_visibility_connectors`).

## Kryteria akceptacji

- [ ] POST z `system_kind=fourkites` albo `shippeo` zapisuje wiersz bez HTTP
- [ ] POST `p44` nadal działa
- [ ] zły kind → 400 (`system`); obce `source_ref` → 400; sekrety w body → 422
- [ ] CHECK w bazie dopuszcza trzy tokeny; zero float / live / nowej tabeli
- [ ] UI pozwala wybrać trzy marki; how-to + GLOSSARY zaktualizowane

## Pytania

Brak.
