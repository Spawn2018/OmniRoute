# Plaster 247.0 · T1 EXP1 `weight_kg` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [215.0](../archived/215.0-stop-notes-for-driver.md) leftover waga · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 215.0 `notes_for_driver`
**Status:** Delta zaakceptowana `/noc`. Nie tabela `stop_group`. Nie mapa. Nie para weigh_in/out. Nie VGM.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T1b tabela `stop_group` + leftover EXP1 waga/ilość/opakowanie/plomba/awizacja + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover T1b tabela parked (213.0: kolumna, nie tabela). leftover ilość/opakowanie/plomba/awizacja/waiting/POD parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. POST `channel_quote` parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `weight_kg` (`Numeric(14,4)`, Decimal HITL, ≥ 0, nie float, nie waluta, nie `charge`, nie VGM). POST bez pola zostaje `null`. Zła waga → 400 (`waga`). `require_stop_weight_kg` w `app.domain.stop` — nie import `container` / `money` / `groupage_tariff`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["247.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `183_stop_weight` (po `182_container_shipment_leg`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop` — ZA/WY + IANA + ETA + `stop_group_code` + `notes_for_driver`, **nie** waga. ISTNIEJE `container.vgm_kg` (T3, inny BC, SOLAS). ISTNIEJE `groupage_tariff.chargeable_weight` (D5, inny BC). BRAK `stop.weight_kg`. BRAK `weigh_in_kg` / `weigh_out_kg`. BRAK tabeli `stop_group`. PODOBNE: Decimal jak VGM, bez importu container.

**Odrzucone:**

- Nowa tabela `stop_group` (leftover T1b; 213.0 = kolumna).
- Para `weigh_in_kg` / `weigh_out_kg`.
- Ilość / opakowanie / plomba / awizacja / waiting / POD.
- VGM / kalkulator / float / `charge`.
- T2c km · T7b · T5 · N3 · S53 · PIN · live HTTP · mapa.

## Zakres

1. Migracja `183_stop_weight`: `weight_kg` nullable Numeric(14,4), CHECK ≥ 0 albo NULL.
2. POST opcjonalny `weight_kg` (string jak `vgm_kg`). Zła wartość → 400 (`waga`). Serwis nie importuje container/charges i nie woła HTTP.
3. UI `/shipments` panel punktu. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

tabela `stop_group` · ilość · opakowanie · plomba · awizacja · waiting · POD · weigh_in/out · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z Decimal `weight_kg` zapisuje wagę, bez VGM i bez nowej tabeli
- [ ] POST bez pola zostaje `null`
- [ ] float / ujemna / nie-liczba → 400 (`waga`)
- [ ] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / `/fleet` / live HTTP / scrape / import `container`
