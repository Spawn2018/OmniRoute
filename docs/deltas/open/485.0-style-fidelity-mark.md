# Plaster 485.0 · AI8.1 STYLE FIDELITY — katalog HITL

**Status:** Delta zaakceptowana `/noc 8` 2026-09-14 — wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § B.5 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI8.1

**Zależy od:** tenancy RLS · `style_cascade_mark` (484.0) · nie silnik score · nie scoring osoby

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `style_fidelity_marks`. Tabela `style_fidelity_mark`: `mark_code` + `fidelity_kind` `pass|hold|reject|exempt|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z AI8.1 — katalog HITL **stancji bramki fidelity** (przepuść / wstrzymaj / odrzuć / zwolnij), nie wyliczanie 85% i nie porównanie LLM.
- **Wybrane:** POST/GET `/api/v1/style-fidelity-marks`. OpenFGA `can_manage_style_fidelity_marks` (member). UI `/style-fidelity-marks`. `SHIPPED["485.0"]` → `/style-fidelity-marks`.
- **Wybrane:** Alembic `390_style_fidelity_mark` (`down_revision = "389_style_cascade_mark"`).
- **Wybrane:** how-to `docs/operator/style-fidelity-mark.md`.
- **Odrzucone:** wyliczanie SCORE 85% w Pythonie / SQL / LLM (`REJECTED` — model nie liczy; bramka jako HITL stancja).
- **Odrzucone:** auto-blokada mail_draft / szkicu bez operatora (`REJECTED` — HC-04).
- **Odrzucone:** scoring osoby / ocena wyników (`REJECTED` — VISION B.5).
- **Odrzucone:** FK do `style_cascade_mark` / `party` / `mail_draft` (`REJECTED` — katalog niezależny).
- **Odrzucone:** AI5.0 `data_source` (`REJECTED` na tym wierszu — park licencja).

**Łowca:** ISTNIEJE `style_cascade_mark`. ISTNIEJE `automation_bias_mark`. BRAK `style_fidelity_mark`.

## Zakres

How-to + OPS_JOB. FE: `fidelity_kind` jako segmented buttons w jednym rzędzie (różne od radio 484 / Button kolumna 483 / select 482).

## Poza zakresem

score 85% · silnik porównania stylu · scoring osoby · auto-block mail · kwota · AI5 ingest

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

silnik porównania stylu · auto-bramka na szkicu · AI5.0 ingest (park)
