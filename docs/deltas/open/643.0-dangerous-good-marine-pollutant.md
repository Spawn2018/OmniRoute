# Plaster 643.0 · leftover EXP0.9 marine pollutant HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § EXP0.9 · [642.0](../archived/642.0-dangerous-good-packing-group.md) · [192.0](../archived/192.0-dangerous-good-adr.md) · [7.0](../archived/7.0-dangerous-good.md)
**Zależy od:** 642.0 `dangerous_good` z `packing_group`
**Status:** Delta zaakceptowana `/noc 7` 2026-09-23. Wolno `/plaster`. Nie live IMO. Nie LLM klasy.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP0.9 „marine pollutant”):** katalog `dangerous_good` zostaje prawdą o UN + IMDG + tunel ADR + SG + packing group. Live IMO / grupy zgodności 1.xA / LQ / LLM klasa zostają park. Żywy plaster: **pogłębienie istniejącej** `dangerous_good` — HITL `marine_pollutant` jako bool (`true`|`false`), nie nadanie klasy przez LLM i nie live IMO. Operator zaznacza zanieczyszczenie morza z tabeli IMDG; serwis nie zmienia `imdg_class`, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. Istniejące wiersze: backfill `false` (nie zgadujemy MP). GET listy SQL. UI zostaje `/dangerous-goods`. `SHIPPED_CHARGE_ROUTES["643.0"]` = `/dangerous-goods`. OpenFGA bez nowej relacji (`can_manage_dangerous_goods`). Migracja `515_dangerous_good_marine_pollutant` po `514_dangerous_good_packing_group`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 642.0 / PLAN EXP0.9 marine pollutant HITL. Źródło: `docs/deltas/archived/642.0-dangerous-good-packing-group.md`; PLAN wiersz EXP0.9; `docs/ops/docs-debt.md` § 643.0 PLAN.

**Łowca:** ISTNIEJE `dangerous_good` — UN + IMDG + tunel + SG + packing_group, **nie** marine pollutant. ISTNIEJE `un_segregation_mark` — stance tunel/segregacja, **nie** bool MP na UN. BRAK `marine_pollutant` na `dangerous_good`. BRAK live IMO. BRAK LLM klasy.

**Odrzucone:**

- live IMO / scrape / HTTP
- nadanie `imdg_class` z marine pollutant albo z LLM
- grupy zgodności 1.xA / limited quantity
- nowa tabela mark / FK do `un_segregation_mark`
- kwota / druga marża / float / T-SQL

## Zakres

1. Migracja `515_dangerous_good_marine_pollutant`: kolumna `marine_pollutant` BOOLEAN NOT NULL po backfillu (`false`).
2. POST wymaga `marine_pollutant`. Serwis nie importuje quotations / charges / extraction i nie woła HTTP.
3. UI `/dangerous-goods` (checkbox w formularzu). How-to / fold w katalogach DG. `just api-types`.

## Poza zakresem

live IMO · LLM klasa · grupy 1.xA · LQ · kwota · float · T-SQL · live HTTP · nowa tabela mark

## Plan plików

- `backend/alembic/versions/515_dangerous_good_marine_pollutant.py`
- `backend/app/models/dangerous_good.py`
- `backend/app/domain/dangerous_good.py` (walidacja bool jeśli potrzeba)
- serwis / API / testy HTTP + izolacja (istniejące BC)
- `frontend/src/features/dangerous-goods/` + `dangerous-goods-api.ts`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts` (`643.0` → `/dangerous-goods`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt · spec

## Kryteria akceptacji

- [ ] POST z `marine_pollutant=true` zapisuje wiersz; `imdg_class` bez zmian z LLM
- [ ] POST z `marine_pollutant=false` zapisuje wiersz
- [ ] serwis nie importuje charges / quotations / extraction i nie woła HTTP
- [ ] RLS: tenant B nie widzi UN tenanta A; zero float / marży / live IMO

## Leftover

live IMO · grupy zgodności 1.xA · LQ
