# Plaster 453.0 · AI4.1 what-if replay

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI4.1 · 434.0 `counterfactual_run` · 452.0 FK snapshot

**Zależy od:** 434.0 `counterfactual_run` · 452.0 `plan_snapshot` FK

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `counterfactual_runs`. Nowa kolumna `plan_snapshot_id` + FK złożone `(organization_id, plan_snapshot_id)` → `plan_snapshot (organization_id, id)`. `ON DELETE RESTRICT`. Bez CASCADE.
- **Wybrane:** widok SQL `what_if_replay` — złączenie przebiegu z migawką (kody, etykiety, trójka UUID). Odtwarzalność = ten sam SELECT, nie liczenie w Pythonie. Bez kwoty, bez km, bez marży.
- **Wybrane:** POST `/api/v1/counterfactual-runs` wymaga istniejącej migawki tego tenanta. Bind bez importu `app.services.plan_snapshots`. `IntegrityError` → 400 („migawka musi istnieć w tym tenancie”).
- **Wybrane:** GET `/api/v1/what-if-replays` — odczyt widoku, to samo OpenFGA `can_manage_counterfactual_runs`. Bez +1 OPS_JOB. UI zostaje na `/counterfactual-runs` (pole UUID migawki + lista powtórek).
- **Odrzucone:** solver / mnożenie / druga marża (`REJECTED` — LLM nie liczy; `charge` zostaje jedyną marżą).
- **Odrzucone:** kółka 500k w SQL (`REJECTED` na tym wierszu — to AI4.2).
- **Odrzucone:** JSON dźwigni · import `what_if_marks` · UPDATE/DELETE przebiegu · CASCADE · float · live HTTP.

**Łowca:** ISTNIEJE `counterfactual_run` HITL etykiety (434.0). ISTNIEJE `plan_snapshot` z FK trójki (452.0). ISTNIEJE `what_if_mark` (rodzaj fuel|port|bankruptcy, inny BC). PODOBNE FK org+id z 452.0. BRAK `plan_snapshot_id` na przebiegu. BRAK widoku powtórki. BRAK solvera.

## Zakres

OpenFGA to samo. `SHIPPED["453.0"]` → `/counterfactual-runs`. How-to: jedno zdanie — przebieg wskazuje istniejącą migawkę; powtórka nie liczy.

## Poza zakresem

AI4.2 · solver · JSON dźwigni · `what_if_mark` · `circle_sim` · kwota

## Kryteria akceptacji

- [ ] migracja `varchar(32)`; down zdejmuje FK i widok
- [ ] POST bez istniejącej migawki → 400; POST z migawką tenanta → 201
- [ ] izolacja: A nie wiąże migawki B; A nie widzi powtórek B
- [ ] widok bez kolumny kwoty / float; craft_style funkcje>40: 0

## Leftover

AI4.2 kółka SQL · solver liczb · JSON dźwigni · CASCADE

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
