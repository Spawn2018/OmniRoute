# Plaster 240.0 · T3 `last_survey_at` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [239.0](../archived/239.0-container-vgm.md) leftover `last_survey_at` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 239.0 VGM bundle
**Status:** Otwarty `/noc` 2026-09-10. Nie live HTTP. Nie PIN. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `last_survey_at`/PIN/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `shipment_leg_id` parked. leftover `quantity` / `weight_kg` parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `last_survey_at` (`timestamptz`, ISO-8601 ze strefą, dana HITL ostatniego przeglądu). POST bez pola zostaje `null`. Zła wartość → 400 (`survey`). `require_last_survey_at` deleguje do `require_si_cutoff_at` w `app.domain.container` — nie import `stop` / `require_eta_*`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["240.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `179_container_last_survey` (po `178_container_vgm`). `_WriteBox` — dodać `survey`. `_container_row` zostaje ≤40 linii. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.si_cutoff_at` / `vgm_cutoff_at` (235–239, parse ISO — reuse). ISTNIEJE `stop.eta_physical` (194.0, inny BC). BRAK `container.last_survey_at`. PODOBNE: parse ze strefą. Nie kopiować importem z `stop`. Nie live HTTP. Nie booking w tym plasterze.

**Odrzucone:**

- Live HTTP / scrape przeglądu z portalu.
- PIN / `booking_no` / FK / GPS / silnik daty.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `179_container_last_survey` (po `178_container_vgm`): `last_survey_at` nullable `DateTime(timezone=True)`.
2. POST opcjonalny `last_survey_at`. Zła wartość → 400. Serwis nie importuje stops/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · booking · km · mapa · T7b · T5 · Auth0 · live HTTP · countdown · GPS

## Kryteria akceptacji

- [ ] POST z ISO `last_survey_at` zapisuje czas, bez live HTTP i bez PIN
- [ ] POST bez pola zostaje `null`
- [ ] zły / bez strefy → 400 (`survey`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / PIN / live HTTP / scrape / import `stop`
