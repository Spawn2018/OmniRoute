# Plaster 54.0 · U-primitives-json (`shadcn_radix_base`)

**Spec źródłowa:** [docs/spec/shadcn-radix-base.md](../../spec/shadcn-radix-base.md)  
**Zależy od:** ADR-0003 §1 + istniejące `@radix-ui/*`

**Status:** Otwarty `/noc` 2026-09-02. Nie nowa tabela. Nie migracja na Base UI. LLM nie liczy.

## Wybrane / odrzucone / dlaczego

**Wybrane (PLAN leftover U-primitives-json):** pin CLI w `frontend/components.json` — pole `base: "radix"`. Token `shadcn_radix_base` (plik konfiguracji, nie tabela). Żywy ID leftover **U-primitives-json**, plaster **54.0**. Istniejące Button/Input na Radix Slot zostają. `rsc: false` (Vite SPA).

**Łowca:** BRAK `components.json`. ISTNIEJE: `@radix-ui/react-slot`, `@radix-ui/react-dialog`, `src/components/ui`. PODOBNE: ADR-0003 `-b radix`. Zero `@base-ui`.

**Odrzucone:**

- Base UI / dwa primitywy.
- `shadcn init` nadpisujący tokeny OKLCH.
- Nowa tabela / nowa trasa.
- Instalacja pakietu `shadcn` do runtime.

## Zakres

`frontend/components.json` z `"base": "radix"`. Test czyta plik (brak `base-ui`, jest radix). Zero migracji. Zero nowej trasy. `SHIPPED_CHARGE_ROUTES` bez 54.0.

## Poza zakresem

U-i18n-structure · dodawanie nowych komponentów CLI · Base UI ADR

## Ustalenia

- Token: `shadcn_radix_base` (GLOSSARY, plik).
- OpenFGA / RLS: bez zmian.
- Job: bez nowej trasy.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `frontend/components.json` ma `"base": "radix"`
- [ ] Test pali brak pliku albo `base-ui` jako base
- [ ] Brak `@base-ui` w `package.json`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | plik JSON |
| Dług w diffie | leftover | i18n |
| Docs/OS | — | po zamknięciu |

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. U-i18n-structure leftover.
