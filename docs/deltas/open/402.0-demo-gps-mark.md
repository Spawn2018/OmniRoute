# Plaster 402.0 · EXP0.11 `demo_gps_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `demo_gps_mark` per tenant: `mark_code` + `demo_kind`
`seven_day`|`fleet_demo`|`other` + `source_ref` (`tenant:manual` | `fixture://demo-gps-mark/…`).

Klon wzorca `job_metric_mark` / `mobile_client_mark`:
- migracja `312` (revises `311_job_metric_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_demo_gps_marks`
- UI `/demo-gps-marks`, business-lists + ops-index `SHIPPED["402.0"]`
- operator `docs/operator/demo-gps-mark.md`
- izolacja RLS; POST `lat`/`lng`/`amount` → 422

## Poza zakresem

live GPS poll · lat/lng · wipe demo · Expo · kwota · marża

## Kryteria

- [ ] POST z kodem, demo_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; lat/lng/amount → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

GPS live 7 dni · wipe `USUN` · flota klienta na sali
