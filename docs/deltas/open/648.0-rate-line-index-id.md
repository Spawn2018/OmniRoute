# Plaster 648.0 · leftover EXP1 rate_line index_id FSC HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP1 `rate_line/charge` · [647.0](../archived/647.0-rate-line-spot-contract.md) · [165.0](../archived/) fuel_index
**Zależy od:** M-07 `rate_line` (647.0 spot_or_contract); P3 `fuel_index` (katalog, nie FK w tym plasterze)
**Status:** Delta zaakceptowana `/noc 7` 2026-09-24. Nie matching. Nie float. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 index_id FSC/BAF/CAF):** tabela `rate_line` zostaje prawdą o stawce kupna. Katalog `fuel_index` (P3) zostaje osobnym indeksem — **nie** FK UUID w tym plasterze (binding = późniejszy leftover). Żywy plaster: **pogłębienie istniejącej** `rate_line` — opcjonalny `index_id` (tekst 1–64, pin HITL etykiety indeksu FSC/BAF/CAF z kontraktu, nullable). Operator wpisuje pin indeksu; serwis nie mnoży FSC, nie tworzy `charge`, nie woła sieci, nie woła LLM, nie JOIN `fuel_index`. POST bez pola → `null`. Istniejące wiersze: `null`. Zmiana = supersede. GET listy SQL. UI `/rate-lines`. `SHIPPED_CHARGE_ROUTES["648.0"]` = `/rate-lines`. OpenFGA bez nowej relacji. Migracja `520_rate_line_index_id` po `519_rate_line_spot_contract`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**Dlaczego tekst pin, nie FK:** karty EXP1 mówią `index_id (FSC/BAF/CAF)`. FK do `fuel_index` wymusza istnienie wiersza indeksu i import BC — szersze. Pin tekstowy wiąże etykietę z kontraktu bez matching SQL.

**REQUIREMENT:** leftover EXP1 po 647.0. Źródło: `docs/analysis/karty-pol-fala-exp.md` § EXP1; CURRENT Następny **648.0**.

**Łowca:** ISTNIEJE `rate_line` — allotment + spot_or_contract, **nie** index_id. ISTNIEJE `fuel_index` — katalog FSC, **nie** pin na stawce. BRAK `index_id` na `rate_line`. BRAK matching. BRAK float.

**Odrzucone:**

- float / mnożenie FSC × amount
- zapis `charge` / druga marża
- FK UUID do `fuel_index` / matching SQL
- live HTTP indeksu
- mutacja w miejscu (tylko supersede)

## Zakres

1. Migracja `520_rate_line_index_id`: kolumna `index_id` TEXT nullable; trigger niemutowalności obejmuje `index_id`.
2. POST opcjonalnie przyjmuje `index_id` (1–64 po trim). Puste → null. Zły typ → 400. Serwis nie importuje fuel_indexes / charges.
3. UI `/rate-lines` (opcjonalne pole). How-to / fold. `just api-types`.

## Poza zakresem

FK fuel_index · matching · float · charge · live HTTP · mutacja w miejscu · mnożenie FSC

## Plan plików

- `backend/alembic/versions/520_rate_line_index_id.py`
- model / domain / serwis / API / testy rate_lines
- `frontend/src/features/rate-lines/` + API client
- `ops-index` + `routes-breadth` (`648.0` → `/rate-lines`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z `index_id` tekst 1–64 zapisuje wiersz; bez pola → null
- [ ] pusty / za długi → 400 lub null wg reguł domeny
- [ ] serwis nie importuje fuel_indexes / charges / quotations i nie woła HTTP
- [ ] RLS: tenant B nie widzi stawek tenanta A; zero float / marży / matching

## Leftover

FK do fuel_index · spot_or_contract na quotation · matching · live MQC · mnożenie FSC
