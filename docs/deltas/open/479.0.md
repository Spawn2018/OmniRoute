# Plaster 479.0 · BR7.1 wpływ na linię — warstwa liczona (katalog HITL)

**Status:** Delta zaakceptowana `/noc 20` 2026-09-14. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 7 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR7.1

**Zależy od:** tenancy RLS · wzorzec HITL `line_impact_mark` (409.0) · leftover SQL/EBITDA zostaje park

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `line_impact_layer_marks`. Tabela `line_impact_layer_mark`: `mark_code` + `layer_kind` `scored|forecast|actual|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR7.0 — katalog HITL **warstwy liczonej** wpływu na linię (VISION: dziś tylko katalog znacznika skutku), nie SQL EBITDA i nie plant live feed.
- **Wybrane:** POST/GET `/api/v1/line-impact-layer-marks`. OpenFGA `can_manage_line_impact_layer_marks` (member). UI `/line-impact-layer-marks`. `SHIPPED["479.0"]` → `/line-impact-layer-marks`.
- **Wybrane:** Alembic `384_line_impact_layer_mark` (`down_revision = "383_ops_room_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `line_impact_mark` (`REJECTED` — 409.0 to rodzaj skutku line/plant/sku, nie warstwa scored/forecast/actual).
- **Odrzucone:** rozszerzenie `delay_forecast` / `prediction_ledger` / `benefit_ledger` (`REJECTED` — inne joby; nie EBITDA linii).
- **Odrzucone:** SQL line impact · EBITDA · plant live feed · amount jako wynik (`REJECTED` — leftover 409.0 / CURRENT park; LLM nie liczy).
- **Odrzucone:** kwota / marża / float / UPDATE/DELETE · ENUM poza CHECK kind · FK do `line_impact_mark`.

**Łowca:** ISTNIEJE `line_impact_mark` (impact_kind). ISTNIEJE `ops_room_mark` (BR7.0 warstwa sali). ISTNIEJE `delay_forecast` / `prediction_ledger`. PODOBNE katalog HITL stance. BRAK `line_impact_layer_mark`. BRAK katalogu warstwy liczonej wpływu na linię.

## Zakres

How-to: jedno zdanie — to katalog warstwy liczonej wpływu na linię; SQL/EBITDA zostają leftover. +1 OPS_JOB. Klient FE: `layer_kind` jako **segmented buttons** (nie select 478, nie radio war_room — rozróżnienie jscpd).

## Poza zakresem

SQL line impact · EBITDA · plant live · amount wyniku · FK line_impact_mark · AI6 silnik

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services
- [ ] jscpd total < 3.0%

## Leftover

SQL line impact · EBITDA · plant feed · AI6 warstwa liczona runtime

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | katalog |
| SQL | N/A | list/insert |
| N+1 | — | |
| Dług / człowiek | — | |
| Proza operatora | — | |
| Docs/OS | — | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | `/noc 20` |
| Bench rzemiosła | — | |
