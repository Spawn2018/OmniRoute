# Plaster 286.0 · CT4 żywy 409 na ASN poza `routing_guide`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 leftover · [285.0](../archived/285.0-routing-guide-enforcement.md)
**Zależy od:** 285.0 `routing_guide_enforcement` · 279.0 `routing_guide` · 278.0 `asn`
**Status:** Plan `/noc` 2026-09-11. Nie 409 na shipment. Nie live EDI.

## Wybrane / odrzucone / dlaczego

**Wybrane:** gdy tenant ma wiersz `routing_guide_enforcement` z `enforcement_kind=block_409`, POST `/asns` wymaga `guide_code` obecnego w katalogu `routing_guide` tego tenanta; brak / nieznany → **HTTP 409** (`RoutingGuideOffGuide`). Składanie w warstwie API (BC `purchase_orders` nie importuje services przewodnika). Opcjonalna kolumna `asn.guide_code`. `record_only` = brak blokady. Leftover: 409 na shipment.

**Łowca:** ISTNIEJE katalog `routing_guide` + tryb `routing_guide_enforcement`. BRAK egzekucji 409 na ASN.

**Odrzucone:** 409 na shipment · matching lane/mode w Pythonie poza equality guide_code · live EDI · auto shipment · float.

## Zakres

1. Migracja `219_asn_guide_code`: nullable `guide_code` + check snake.
2. Domain `assert_asn_on_routing_guide` + `RoutingGuideOffGuide` → handler 409.
3. API `POST /asns`: odczyt enforcements + guides, assert, potem `AsnService`.
4. FE pole `guide_code`; testy domain/http; how-to update.

## Kryteria akceptacji

- [ ] bez `block_409` → POST ASN bez guide_code = 201
- [ ] z `block_409` i brak/nieznany guide_code → 409
- [ ] z `block_409` i znany guide_code → 201 + zapis guide_code
- [ ] BC purchase_orders bez importu `routing_guide*` services
- [ ] zero float; izolacja bez zmian schematu RLS (ta sama tabela asn)

## Leftover po 286.0

409 na POST shipment. Matching lane/mode. Live EDI. Auto shipment.
