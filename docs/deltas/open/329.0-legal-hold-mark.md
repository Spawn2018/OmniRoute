# Plaster 329.0 · G8 `legal_hold_mark` HITL (katalog, bez eIDAS crypto)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G8 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie F1 KSeF. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G8 — tylko HITL katalog `legal_hold_mark` jako dane. `mark_code` + `hold_kind` (`retention` | `legal_hold` | `eidas`) + `source_ref`. UI `/legal-hold-marks`. OpenFGA `can_manage_legal_hold_marks`. Migracja `241_legal_hold_mark`. Job OPS. Deny-list extract. Wipe / ciphertext offboarding / podpis crypto / F1 KSeF = leftover.

**Łowca:** BRAK tabeli `legal_hold_mark`. ISTNIEJE `gdpr_request` — wniosek RODO, nie hold. ISTNIEJE `customer_contract` — umowa, nie retencja.

**Odrzucone:** eIDAS crypto · wipe ciphertext · F1 KSeF live · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `241_legal_hold_mark` RLS FORCE.
2. POST/GET `/legal-hold-marks`. OpenFGA. Deny-list.
3. UI `/legal-hold-marks`. How-to. `SHIPPED_CHARGE_ROUTES["329.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, hold_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/wipe → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
