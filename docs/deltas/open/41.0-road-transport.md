# Plaster 41.0 · M-48 Transport drogowy (`road_transport`)

**Spec źródłowa:** [docs/spec/road-transport.md](../../spec/road-transport.md)  
**Zależy od:** 4.1 `location.kind`

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 7 M-48):** transport drogowy **nie ma tabeli TMS**. Operator widzi lokalizacje `postal_zone` i `address` (ląd, nie UN/LOCODE portu). Token `road_transport` (UI, nie tabela). Żywy ID **M-48**. 4.1 zostaje katalogiem lokalizacji; 4.0 zostaje portami.

**Łowca:** BRAK `road_leg` / TMS / tabeli drogówki. ISTNIEJE: `fetchLocations`, `kind` `postal_zone` | `address` | `unlocode`. PODOBNE: 4.1 `/locations`; 29.0 lane POL/POD.

**Odrzucone:**

- Tabela zlecenia drogowego / TMS / naczepa — zgadywanie schematu (CURRENT).
- Filtrowanie `unlocode` jako drogi — to port M-05.
- Nowa trasa live GPS — leftover trackingu.

## Zakres

Read-only `/road`. `data-road-transport="board"`. Brak migracji. `roadLocations(locations)`. Link do `/locations`.

## Poza zakresem

Tabela TMS · GPS · `unlocode` jako droga · ExtractionService

## Ustalenia

- Token: `road_transport` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia geography.
- RLS: bez nowej tabeli.
- Job: `/road`. `SHIPPED_CHARGE_ROUTES["41.0"]` = `/road`. OPS_JOBS +1 → 38.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 41.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/road` ma `data-road-transport="board"` i woła `roadLocations`
- [ ] Brak `unlocode` na tablicy (filtr `kind`)
- [ ] Brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["41.0"]` = `/road`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

Wypełnić przy `/zamknij`.

## Knowledge

Nie RAG. Nie silnik TMS.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. TMS i GPS świadomie leftover.
