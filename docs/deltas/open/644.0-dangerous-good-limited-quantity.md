# Plaster 644.0 · leftover EXP0.9 limited quantity HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § EXP0.9 · [643.0](../archived/643.0-dangerous-good-marine-pollutant.md) · [642.0](../archived/642.0-dangerous-good-packing-group.md) · [7.0](../archived/7.0-dangerous-good.md)
**Zależy od:** 643.0 `dangerous_good` z `marine_pollutant`
**Status:** Delta zaakceptowana `/noc 7` 2026-09-24. Wolno `/plaster`. Nie live IMO. Nie LLM klasy.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP0.9 „limited quantity”):** katalog `dangerous_good` zostaje prawdą o UN + IMDG + tunel + SG + packing + MP. Live IMO / grupy zgodności 1.xA / LLM klasa zostają park. Żywy plaster: **pogłębienie istniejącej** `dangerous_good` — HITL `limited_quantity` jako bool (`true`|`false`), nie nadanie klasy przez LLM i nie live IMO. Operator zaznacza LQ z tabeli IMDG; serwis nie zmienia `imdg_class`, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. Istniejące wiersze: backfill `false`. GET listy SQL. UI zostaje `/dangerous-goods`. `SHIPPED_CHARGE_ROUTES["644.0"]` = `/dangerous-goods`. OpenFGA bez nowej relacji. Migracja `516_dg_limited_quantity` po `515_dg_marine_pollutant`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 643.0 / PLAN EXP0.9 limited quantity HITL. Źródło: `docs/deltas/archived/643.0-dangerous-good-marine-pollutant.md`; PLAN wiersz EXP0.9; `docs/ops/docs-debt.md` § 644.0 PLAN.

**Łowca:** ISTNIEJE `dangerous_good` — UN + IMDG + tunel + SG + packing + marine_pollutant, **nie** limited quantity. BRAK `limited_quantity` na `dangerous_good`. BRAK live IMO. BRAK LLM klasy.

**Odrzucone:**

- live IMO / scrape / HTTP
- nadanie `imdg_class` z LQ albo z LLM
- grupy zgodności 1.xA
- nowa tabela mark
- kwota / druga marża / float / T-SQL

## Zakres

1. Migracja `516_dg_limited_quantity`: kolumna `limited_quantity` BOOLEAN NOT NULL po backfillu (`false`).
2. POST wymaga `limited_quantity`. Serwis nie importuje quotations / charges / extraction i nie woła HTTP.
3. UI `/dangerous-goods` (checkbox). How-to / fold. `just api-types`. Wszystkie fixture `DangerousGood(` dostają `limited_quantity=False`.

## Poza zakresem

live IMO · LLM klasa · grupy 1.xA · kwota · float · T-SQL · live HTTP · nowa tabela mark

## Plan plików

- `backend/alembic/versions/516_dg_limited_quantity.py`
- model / domain / serwis / API / testy (w tym RFQ/quotation isolation fixtures)
- `frontend/src/features/dangerous-goods/` + `dangerous-goods-api.ts`
- `ops-index` + `routes-breadth` (`644.0` → `/dangerous-goods`)
- how-to · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS · docs-debt

## Kryteria akceptacji

- [ ] POST z `limited_quantity=true` zapisuje wiersz; `imdg_class` bez zmian z LLM
- [ ] POST z `limited_quantity=false` zapisuje wiersz
- [ ] serwis nie importuje charges / quotations / extraction i nie woła HTTP
- [ ] RLS: tenant B nie widzi UN tenanta A; zero float / marży / live IMO

## Leftover

live IMO · grupy zgodności 1.xA
