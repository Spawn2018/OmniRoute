# Plaster 211.0 · T7 `fx_rate_basis` w `organization_setting`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T7 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T7
**Zależy od:** 3.0 `organization_setting` allowlista · 16.0 `nbp_rate` (odczyt katalogu; ten plaster nie mnoży)
**Status:** Otwarty `/noc` 2026-09-09. Nie przeliczenie SQL. Nie `charge`. Nie U4 weekday.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T7 polityka kursu + leftover T7b override na `charge`/`shipment` + leftover T7c D-1 roboczy U4 + leftover T4b SQL marży + leftover T5 outbox + leftover T6 mapa + leftover T8 live API + S53):** leftover T7b nadpisanie na `charge`/`shipment` parked. leftover T7c offset jako dzień roboczy U4 parked. leftover T4b SQL na `charge` parked. leftover T5 outbox M-02 parked. leftover T6 mapa parked. leftover T8 live API parked. S53 Auth0 parked. Żywy plaster: **pogłębienie istniejącej** `organization_setting` — trzy klucze allowlisty: `fx_rate_basis` (`etd` / `loading_date` / `unloading_date` / `invoice_date`), `fx_rate_offset_days` (`0` / `-1`), `fx_rate_table` (`nbp_a` / `nbp_b`). To dana polityki tenanta, nie mnożenie kursem. Zły basis/tabela → 400 (`kurs`). Zły offset → 400 (`dni`). UI `/organization-settings`. `SHIPPED_CHARGE_ROUTES["211.0"]` = `/organization-settings`. OpenFGA bez nowej relacji. Bez nowej migracji (KV już jest). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `organization_setting` — allowlista kluczy, **nie** polityka kursu. ISTNIEJE `nbp_rate` — katalog D-1, **nie** mnożenie. ISTNIEJE `charge` — buy+sell, **nie** `fx_rate_basis`. ISTNIEJE `organization_calendar` — `is_working_day` SQL, **nie** offset T7. BRAK kluczy `fx_rate_*`. BRAK SQL przeliczenia przy fakturze.

**Odrzucone:**

- Przeliczenie SQL / mnożenie `nbp_rate` × kwota (leftover T7b/T7d).
- Nadpisanie na `charge` albo `shipment` (leftover T7b).
- Offset liczony dniem roboczym U4 (leftover T7c).
- Live HTTP NBP · tabela B ingest.
- T4b SQL marży · T5 outbox · T6 mapa · T8 API · S53 Auth0.
- Import charges / nbp_rates w serwisie ustawień.

## Zakres

1. Allowlista `organization_setting`: trzy klucze + walidacja tokenów. Bez nowej tabeli.
2. POST upsert. Zły token → 400. Serwis nie importuje charges / nbp_rates i nie woła HTTP.
3. UI `/organization-settings`. Fold w `katalogi.md`. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

SQL mnożenia · override na `charge` · U4 weekday · live NBP · T5 · T6 · T8 · Auth0 · T-SQL

## Kryteria akceptacji

- [ ] upsert `fx_rate_basis` / `fx_rate_offset_days` / `fx_rate_table` zapisuje token, bez `charge`
- [ ] zły basis/tabela → 400 (`kurs`); zły offset → 400 (`dni`)
- [ ] klucz spoza trójki T7 nadal poza allowlistą
- [ ] GET zwraca wartość tenanta; izolacja ustawień bez zmian tabeli
- [ ] Zero float / marży / mnożenia / live HTTP / scrape
