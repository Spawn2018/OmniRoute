# Plaster 549.0 · F10 — HITL `purchase_invoice`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala F · F10

**Zależy od:** tenancy · `sales_invoice` (wzorzec nagłówka, nie kopia kwoty) · F9 `erp_connector`

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy F10. Nowa tabela `purchase_invoice`: `invoice_ref` (1–64) + `invoice_kind` `noted|other` + `source_ref`. Append-only. RLS. HITL ingest FV zakupu — nie auto-link do `charge`.
- **Wybrane:** POST/GET `/api/v1/purchase-invoices`. OpenFGA `can_manage_purchase_invoices`. UI `/purchase-invoices`. `SHIPPED["549.0"]`.
- **Odrzucone:** `invoice_match_candidate` / ranking SQL / `purchase_invoice_allocation` (`REJECTED` teraz → leftover). Kwota / marża / float (`REJECTED`). Auto-link do `charge` / `sales_invoice` (`REJECTED`). Live ERP / KSeF XML (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / SH-R16-13 / N14 leftovers live.

**Łowca:** BRAK `purchase_invoice` w models. ISTNIEJE `sales_invoice` (sprzedaż + FK shipment). ISTNIEJE `freight_audit_mark` (rodzaj audytu, nie ingest). ISTNIEJE `quote_invoice_settlement` (wiązanie wyceny↔FV sprzedaży). BRAK `invoice_match_candidate`.

## Zakres

1. Migracja `429_purchase_invoice` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["549.0"]` → `/purchase-invoices`.

## Poza zakresem

ranking SQL · allocation · auto-link · kwota · live ERP · AI3 · G0

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty ref → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount

## Leftover

`invoice_match_candidate` · ranking SQL · `purchase_invoice_allocation` · kwota · live ERP
