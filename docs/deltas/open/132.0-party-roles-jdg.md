# Plaster 132.0 · M10-2 role, JDG, parent (M-10)

**Spec źródłowa:** [docs/spec/parties.md](../../spec/parties.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § M10-2 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § M10-2
**Zależy od:** 5.0 / 131.0 `party` · 14.0 `credit_review` · 0.3 RLS
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Ten sam żywy ID **M-10**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki M10-2):** kolumny `is_sole_trader` i `parent_party_id` na **istniejącym** `party`. Nowa tabela `party_role_assignment` (org + party + role) z RLS. Rola `subcontractor` wchodzi do allowlisty. `party.roles` zostaje projekcją (CHECK 5.0) — serwis zapisuje assignment i tablicę razem. JDG + `credit_limit` na INSERT → 400 (kredyt tylko HITL recenzją, nie w tym plasterze). `parent_party_id` FK tenanta, nie sam siebie. Job na `/parties`.

**Łowca:** ISTNIEJE `party.roles` ARRAY + `ALLOWED_ROLES` + `credit_review`. BRAK `is_sole_trader` / `parent_party_id` / tabeli assignment. PODOBNE: `network_member.party_id` FK tenanta — ten sam wzorzec. Nie trzy tabele agent/armator/podwykonawca.

**Odrzucone:**

- Osobne tabele `party_agent` / `party_carrier` / `party_subcontractor`.
- Wyrzucenie `party.roles` w tym plasterze (zostawiłoby martwy CHECK i UI).
- Auto-limit / scoring / zapis `credit_review` z create.
- Cykl w grafie parent (poza `parent != id`) — leftover jeśli trzeba.
- Portal, M10-1 wstecz, F9.1.

## Zakres

1. Migracja `075_party_roles_jdg`: kolumny + tabela + RLS FORCE + FK `(organization_id, parent_party_id)` + CHECK `subcontractor` + unikat `(organization_id, party_id, role)`.
2. Domenowe `is_sole_trader` + `require_no_auto_credit_for_jdg`. `parent_party_id != id`.
3. `create_party`: flagi; INSERT assignment z ról; JDG+limit → 400; nieznany parent → 404.
4. UI `/parties`: checkbox JDG, pole `parent_party_id`. `SHIPPED_CHARGE_ROUTES["132.0"]` = `/parties`.
5. Test izolacji nowej tabeli. `just api-types`. How-to w `kontrahent.md`.

## Poza zakresem

B0a · O1–O8 · live GUS · scoring · nowa recenzja kredytowa · portal · F9.1

## Ustalenia

- Role: istniejące + `subcontractor`. Nie trzy tabele.
- JDG: `is_sole_trader=true` zabrania `credit_limit` na create. Recenzja 14.0 zostaje HITL.
- Parent: ten sam tenant; NULL na starych i na nowym bez grupy.
- OpenFGA: `can_manage_parties`. RLS jak 015.

## Schemat

`party.is_sole_trader` bool NOT NULL default false. `party.parent_party_id` UUID NULL.

`party_role_assignment`: `id`, `organization_id`, `party_id`, `role`, `source_ref`, timestamps, `created_by`.

## Kryteria akceptacji

- [ ] JDG + credit_limit → 400
- [ ] `parent_party_id` = własny id → 400; obcy tenant → IntegrityError
- [ ] `subcontractor` przechodzi; nieznana rola → 400
- [ ] Assignment izolowany RLS
- [ ] `/parties` ma JDG i parent
- [ ] `just api-types`
