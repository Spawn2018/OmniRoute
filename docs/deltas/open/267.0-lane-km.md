# Plaster 267.0 · G2.21 `lane_km` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.19–G2.22 leftover km ładowny/pusty/dolot · [karty-pol-g2-tender.md](../../analysis/karty-pol-g2-tender.md) § G2.19–G2.22 · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) (brak osobnej tabeli pól km ładowny — karta G2)
**Zależy od:** 0.3 tenancy · 185.0 `lane_pattern` (para origin/destination już jest — to nie ten obiekt) · 266.0 `circle_sim` (kółko już jest — to nie ten obiekt)
**Status:** Otwarty `/noc` 2026-09-10. Nie Haversine. Nie mapa. Nie suma `circle_sim`. Nie T2c na `trip`. Nie live HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover G2.21–G2.22 km/P + leftover silnik ≥500k + leftover F9 Optima + leftover T8 + leftover what-if V8 + leftover 8 silników):** leftover P=`n_overlap`/`n_similar` i lista przewoźników parked (G2.22). leftover silnik ≥500k w Postgres parked. leftover F9 Optima fixture parked. leftover T8 parked. leftover V8 what-if parked. leftover fizyka twinów parked. leftover kolumny km na `trip` (256.0/257.0) **nie ten leftover** — T2c to plan/actual jednego przejazdu. Żywy plaster: **nowa tabela** `lane_km` per tenant — HITL km ładowny / pusty / dolot jako dane Decimal, nie liczenie z GPS i nie suma kółek. Wiersz = `km_code` (snake 2–32) + `loaded_km` + `empty_km` + `approach_km` (`Numeric(14,4)`, ≥ 0, `0` legalne) + `source_ref` (`tenant:manual` albo `fixture://lane-km/…`). GLOSSARY: **km ładowny** = `lane_km` (obok `lane_pattern`; nie helper `circle_km`, bo to nie suma kółka). To nie jest `trip.planned_distance_km` / `actual_distance_km` (T2c, jeden odcinek przejazdu, token 400 `km`). To nie jest `circle_sim` (tam para UN/LOCODE, zero km). To nie jest `lane_pattern` (tam para origin→destination, zero km). Operator wpisuje nazwane trójkąt km; serwis nie liczy Haversine, nie woła HTTP, nie woła LLM, nie czyta `charge` / `trip` / `shipment` / `resource` / `tender` / `geography` / `circle_sim` i nie sumuje kółek. Katalog unique, POST INSERT, GET lista, bez UPDATE/DELETE. Zły kod → 400 (`oznaczenie`). Zły ładowny → 400 (`ladowny`). Zły pusty → 400 (`pusty`). Zły dolot → 400 (`dolot`). Obce `source_ref` → 400 (`obce`). JSON float / bool → 400 (ten sam token pola). Unique `(organization_id, km_code)` i `(organization_id, source_ref)`. GET listy SQL. UI `/lane-kms`. `SHIPPED_CHARGE_ROUTES["267.0"]` = `/lane-kms`. OpenFGA: `can_manage_lane_kms`. Migracja `200_lane_km` (po `199_circle_sim`). Job OPS 113. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `trip.planned_distance_km` 256.0 i `trip.actual_distance_km` 257.0 — HITL km **przejazdu** (T2c), token 400 `km`, **nie** trójkąt ładowny/pusty/dolot G2.21. ISTNIEJE `require_trip_planned_distance_km` — parser Decimal T2c; **nie** ten katalog (własne tokeny, bez importu `trip`). ISTNIEJE `circle_sim` 266.0 — kółko unload/load, testy świadomie **bez** `loaded_km` / `empty_km` / `approach_km`. ISTNIEJE `lane_pattern` 185.0 — para UN/LOCODE korytarza, **nie** km. ISTNIEJE `party_charge_override.lane_pattern` — tekst wyjątku stawki. BRAK tabeli `lane_km`. BRAK `circle_km`. BRAK `/lane-kms`. BRAK kolumn km na `circle_sim`. BRAK silnika Haversine / VRP / myto.

**Odrzucone:**

- kolumny `loaded_km` / `empty_km` / `approach_km` na `circle_sim` albo `trip`.
- Haversine / mapa / GPS / suma `circle_sim` / liczenie w Pythonie.
- P=`n_overlap`/`n_similar` / lista przewoźników (leftover G2.22).
- silnik `circle_sim` ≥500k w Postgres (leftover G2.20 engine).
- F9 Optima fixture / T8 / what-if V8 / fizyka twinów / myto / paliwo.
- import trips / shipments / resources / charges / geography / circle_sims / tenders.
- kwota / marża / float / T-SQL / live HTTP / scrape / LLM-VRP.

## Zakres

1. Migracja `200_lane_km`: RLS FORCE, izolacja, unique kod + `source_ref`, CHECK trzy km ≥ 0 (`Numeric(14,4)`). Nie kolumny na `trip` / `circle_sim`.
2. POST/GET. Zły token → 400 (`oznaczenie` / `ladowny` / `pusty` / `dolot` / `obce`). Serwis nie importuje trips/shipments/resources/charges/geography/circle_sims/tenders i nie woła HTTP. Brak UPDATE/DELETE. Zero `float()`.
3. UI `/lane-kms`. How-to `docs/operator/lane-km.md`; fold w `katalogi.md` (40 linii). Job OPS 113. OpenFGA `can_manage_lane_kms`. Nazwy: `LaneKmDesk` / `lane-km-form.tsx` / `persistLaneKm`.

## Poza zakresem

G2.22 P/lista · silnik 500k · Haversine · mapa · T2c na `trip` · F9 Optima · T8 · what-if · fizyka · myto · kwota · float

## Kryteria akceptacji

- [ ] POST zapisuje `lane_km` z kodem, trójką Decimal ładowny/pusty/dolot i `source_ref`, bez liczenia km
- [ ] zły kod → 400 (`oznaczenie`); float / bool / ujemna → 400 (`ladowny` / `pusty` / `dolot`); `0` legalne
- [ ] GET zwraca wpisy tenanta
- [ ] Test izolacji nowej tabeli
- [ ] Zero float / marży / Haversine / sumy circle_sim / kolumn na trip / live HTTP / importu trips/shipments/charges/circle_sims

## Implementacja (267.0)

- Migracja `200_lane_km` po `199_circle_sim`. RLS FORCE.
- `LaneKmService.persist_lane_km` — katalog unique, bez importu trips/shipments/charges/geography/circle_sims/tenders.
- UI `/lane-kms`. `SHIPPED_CHARGE_ROUTES["267.0"]` = `/lane-kms`. Job OPS 113.

## Leftover po 267.0

P=`n_overlap` / lista P (G2.22). silnik ≥500k. F9 Optima fixture (następny na osi `/noc`). T8. what-if V8. 8 silników fizyki.
