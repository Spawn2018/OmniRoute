# Plaster 136.0 · O3 `carrier_inquiry` batch + statusy + lane

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O3 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § O3
**Zależy od:** 83.0 `carrier_inquiry` · O0 `network_member.party_id`
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Ten sam żywy ID **M-30**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki O3):** ta sama tabela. CHECK status: `draft` \| `queued` \| `sent` \| `answered` \| `declined`. Lane: `origin_port_id` / `destination_port_id` FK `port` w tenancie. `quoted_amount` + `quoted_currency` + `quoted_transit_days` tylko przy `answered` (Decimal, nie float). Batch: jeden POST wielu `network_member_id` (1 / wielu / wszyscy z sieci). Wskazanie wyceny składa **API**, serwis nie importuje `quotations`. `source_ref` obowiązkowe. Job zostaje na `/networks` (istniejący zapis zapytania) + lista.

**Łowca:** ISTNIEJE `carrier_inquiry` (83.0) — tylko `draft`, FK członek, zero kwoty, zero lane. PODOBNE POST pojedynczy. BRAK batch i statusów Fali O.

**Odrzucone:**

- Kolumna `quotation_id` w serwisie buy (karta: API składa).
- Live HTTP / scraping.
- Auto-send (O4/S18).
- Import `app.services.quotations`.

## Zakres

1. Migracja `078_carrier_inquiry_batch`: statusy, lane, kwota odpowiedzi, CHECK answered.
2. Create pojedynczy i batch. OpenFGA bez nowej relacji (to samo co 83.0).
3. UI batch na `/networks`. `SHIPPED_CHARGE_ROUTES["136.0"]` = `/networks`.
4. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

O4–O8 · Graph HTTP · NBP

## Kryteria akceptacji

- [ ] Status spoza allowlisty → 400
- [ ] Kwota przy `draft` → 400
- [ ] Batch trzech członków = trzy wiersze
- [ ] Serwis bez importu quotations
- [ ] Izolacja RLS zostaje
- [ ] `just api-types`
