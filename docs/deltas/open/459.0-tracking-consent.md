# Plaster 459.0 · BR2.2 zgoda na śledzenie

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 2 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR2.2

**Zależy od:** tenancy RLS · wzorzec HITL (`party_contact` jako osobny BC)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `tracking_consents`. Tabela `tracking_consent`: `consent_code` + `consent_kind` `party|driver|other` + `source_ref`. Append-only INSERT. RLS + test izolacji.
- **Wybrane:** POST/GET `/api/v1/tracking-consents`. OpenFGA `can_manage_tracking_consents` (member). UI `/tracking-consents`. `SHIPPED["459.0"]` → `/tracking-consents`.
- **Wybrane:** Alembic `364_tracking_consent` (`down_revision = "363_telematics_device"`). Id ≤ 32 znaków.
- **Odrzucone:** kolumna `party_contact.tracking_consent` na tym wierszu (`REJECTED` — leftover PLAN; osobny katalog, nie mutacja M-10).
- **Odrzucone:** klej FK do `party` / `party_contact` / `resource` (`REJECTED` na tym wierszu).
- **Odrzucone:** live GPS poll · mapa · egzekucja zgody.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind · kwota / float.

**Łowca:** ISTNIEJE `party_contact` (M-10 kontakt, bez zgody). ISTNIEJE `position_event` / `telematics_device` (ślad i sprzęt, nie zgoda). PODOBNE katalog HITL. BRAK `tracking_consent`. BRAK kolumny na kontakcie.

## Zakres

How-to: jedno zdanie — to katalog zgody; kolumna na kontakcie i live zostają leftover. +1 OPS_JOB na nową listę.

## Poza zakresem

`party_contact.tracking_consent` · live poll · mapa · AI5 · parowanie urządzenia

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

kolumna na `party_contact` · FK do party · egzekucja · live poll · BR2.3 app kierowcy

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
