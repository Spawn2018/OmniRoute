# Plaster 31.0 · M-38 Dokumenty zlecenia (`shipment_document` tablica odczytu)

**Spec źródłowa:** [docs/spec/shipment-document.md](../../spec/shipment-document.md)  
**Zależy od:** 5.1 `quotation.party_id` · 19.0 `offer_document` / `source_ref` · 28.0 `/shipments`

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela. Nie PDF.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 5 M-38):** dokument zlecenia **nie ma tabeli HBL/B/L** w repo. Operator widzi `source_ref` wycen z `party_id` na `/shipment-documents` jako jedyny znany dokument. Token `shipment_document` (UI, nie tabela). Żywy ID **M-38**. 19.0 zostaje panelem na `/quotations`; 28.0 zostaje tablicą pracy. U-print (B/L, FV) leftover.

**Łowca:** BRAK `shipment_document` / HBL / B/L / waybill w modelach. ISTNIEJE: `quotationAcceptancePending`, `source_ref`, `<Money/>`, panel 19.0. PODOBNE: 19.0 dokument oferty, 28.0 `/shipments`.

**Odrzucone:**

- Tabela dokumentów / numer HBL — zgadywanie schematu (CURRENT).
- PDF / `@media print` / U-print — leftover Fala 5/6.
- Druga marża. LLM piszący treść dokumentu.

## Zakres

Read-only `/shipment-documents`. `data-shipment-document="board"`. Brak migracji. `quotationAcceptancePending` + `source_ref` + `<Money/>`. Linki do `/shipments` i `/quotations`.

## Poza zakresem

Tabela dokumentów · HBL · PDF · U-print · EDI (M-39) · ExtractionService

## Ustalenia

- Token: `shipment_document` (GLOSSARY, UI).
- OpenFGA: istniejące `can_manage_quotations`.
- RLS: bez nowej tabeli.
- Job: `/shipment-documents`. `SHIPPED_CHARGE_ROUTES["31.0"]` = `/shipment-documents`. OPS_JOBS +1 → 28.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] `/shipment-documents` ma `data-shipment-document="board"` i woła `quotationAcceptancePending`
- [ ] Brak nowej migracji; brak `pdf` / `print` / `hbl` w ekranie
- [ ] `SHIPPED_CHARGE_ROUTES["31.0"]` = `/shipment-documents`
- [ ] `just dup` ≤ 3%

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. PDF, HBL i U-print świadomie leftover.
