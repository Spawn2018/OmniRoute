# Plaster 503.0 · AI3.1 leftover historia wierszy w JSONB

**Status:** Zaakceptowane `/noc 15` — wolno `/plaster`.

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI3.1 leftover · [VISION.md](../../VISION.md) E AI3 · po [448.0](../archived/448.0-extraction-draft-revision.md) · [502.0](../archived/502.0-extraction-prompt-mark.md)

**Zależy od:** 447.0/500.0 PATCH `candidates` · 448.0 `payload.revision` · `can_review_extractions`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `extraction`. Bez nowej tabeli. Przy każdym `PATCH` pending: do `payload.history` (lista) dorzuć snapshot poprzedniego stanu: `{ "revision": <int przed bump>, "candidates": <poprzednia lista> }`. Potem podmiana `candidates` i `revision` +1 jak dziś.
- **Wybrane:** `ExtractionPayload.history` = `list[ExtractionHistoryEntry]`, default `[]`. Entry: `revision: int` + `candidates: list[ExtractedChargeCandidate]`. `extra=forbid`. Brak float.
- **Wybrane:** nowy szkic ma `history: []`. GET zwraca `history`. Accept 1.3 nadal czyta tylko bieżące `candidates` (kod / amount_text / currency) — nie historię.
- **Wybrane:** UI `/extractions` — krótka lista poprzednich wersji (revision + liczba kandydatów); bez cofania do starej wersji w tym plasterze. How-to `docs/operator/ekstrakcja.md`.
- **Wybrane:** OpenFGA bez zmian (`can_review_extractions`). `SHIPPED["503.0"]` → `/extractions`. Bez +1 OPS_JOB.
- **Odrzucone:** tabela historii / `parent_id` (`REJECTED` — jak 448.0; ślad = JSONB).
- **Odrzucone:** cofanie PATCH do starej wersji / silnik diff (`REJECTED` — leftover).
- **Odrzucone:** live vision · wiring Instructor · partial write · float (`REJECTED`).

**Łowca:** ISTNIEJE `payload.revision` + PATCH. PODOBNE append-only w JSONB. BRAK `payload.history`. `just dup` bez wyodrębnienia.

## Zakres

Domena + serwis `patch_candidates` + schemat Pydantic + testy + GET + UI lista historii + how-to. Bez Alembic (kolumna `payload` już jest).

## Poza zakresem

Tabela historii · undo · Instructor · AI3.2 obraz · M-03 `hitl_confidence_*` · partial write

## Kryteria akceptacji

- [ ] nowy szkic: `history` = `[]`, `revision` = 0
- [ ] pierwszy PATCH: `history` ma 1 wpis z `revision` 0 i starymi `candidates`; bieżące `revision` = 1
- [ ] drugi PATCH: `history` ma 2 wpisy; bieżące `revision` = 2
- [ ] GET pokazuje historię; accept nie zależy od `history`
- [ ] UI pokazuje listę poprzednich wersji; brak float; craft_style funkcje>40: 0

## Leftover

undo do wersji · wiring Instructor · live vision · partial write · M-03
