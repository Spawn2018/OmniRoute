# Plaster 208.0 · P4b rest `iso_size_type` na `local_charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 leftover · [207.0](../archived/207.0-local-charge-port.md) leftover P4b rest · [166.0](../archived/166.0-local-charge.md)
**Zależy od:** 207.0 `port_unlocode` już jest · 153.0 `container.iso_size_type` (wzorzec tokenu; ten plaster nie importuje containers)
**Status:** Otwarty `/noc` 2026-09-09. Nie numer kontenera. Nie VGM. Nie FK. Nie warning braków.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P4b rest armator/serwis/kontener + P4c warning + P5b–c + P6c):** leftover P4c warning-jako-fakt parked. leftover P5b–c SQL na `charge` parked. leftover P6c auto-award parked. leftover armator/serwis FK parked. Żywy plaster: **pogłębienie istniejącej** `local_charge` — opcjonalny `iso_size_type` (4 znaki, jak na `container`), nie numer BIC i nie FK. POST bez pola zostaje `null`. Zły token → 400 (`typ`). Unique per tenant `(charge_kind, port_unlocode, iso_size_type)` zastępuje unique 207.0. UI `/local-charges`. `SHIPPED_CHARGE_ROUTES["208.0"]` = `/local-charges`. OpenFGA bez nowej relacji. Migracja `148_local_charge_iso`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `local_charge.port_unlocode` — port jako dana, **nie** typ kontenera. ISTNIEJE `container.iso_size_type` — na konkretnym pudle; **nie** importujemy services.containers. BRAK `iso_size_type` na dopłacie lokalnej. BRAK warning-jako-fakt.

**Odrzucone:**

- Armator / serwis / FK `container` / `party`.
- Numer BIC / VGM / TEU.
- P4c warning · P5b–c na `charge` · P6c auto-award.
- Import containers / geography / charges.
- T-SQL / float / live HTTP.

## Zakres

1. Migracja `148_local_charge_iso` (po `147_local_charge_port`): kolumna `iso_size_type` nullable, drop unique 207.0, unique `(organization_id, charge_kind, port_unlocode, iso_size_type)`.
2. POST opcjonalny `iso_size_type`. Zły token → 400. Serwis nie importuje containers i nie woła HTTP.
3. UI `/local-charges`. How-to `oplata-lokalna.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

Armator · serwis · FK · P4c · P5 · P6c · T-SQL · charge · live HTTP

## Kryteria akceptacji

- [ ] POST z `iso_size_type` zapisuje token, bez FK container
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`typ`)
- [ ] unique per tenant na trójce kind+port+typ; izolacja
- [ ] Zero float / marży / warning-jako-fakt / live HTTP / scrape
