# Plaster 502.0 · AI3.1 leftover prompt jako dana

**Status:** delta zaakceptowana `/noc 15` — wolno `/plaster`.

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § AI3.1 leftover · [VISION.md](../../VISION.md) C.5 · po [448.0](../archived/448.0-extraction-draft-revision.md)

**Zależy od:** tenancy RLS · `extraction_draft` · nie live Instructor

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `extraction_prompt_marks`. Tabela `extraction_prompt_mark`: `mark_code` + `prompt_kind` `extract|system|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja leftover AI3.1 — **prompt jako dana** (katalog HITL wersji promptu per tenant), nie tabela historii wierszy (448.0: tabela historii `REJECTED`).
- **Wybrane:** POST/GET `/api/v1/extraction-prompt-marks`. OpenFGA `can_manage_extraction_prompt_marks` (member). UI `/extraction-prompt-marks`. `SHIPPED["502.0"]` → `/extraction-prompt-marks`.
- **Wybrane:** Alembic `405_extraction_prompt_mark` (`down_revision = "404_shipper_award_mark"`).
- **Wybrane:** how-to `docs/operator/extraction-prompt-mark.md`.
- **Odrzucone:** tabela historii kopii kandydatów / `parent_id` (`REJECTED` — potwierdzenie 448.0; leftover osobny jeśli kiedyś JSONB).
- **Odrzucone:** podpięcie Instructor / live LLM do odczytu z katalogu (`REJECTED` — ten plaster = katalog HITL).
- **Odrzucone:** bajty promptu / ciphertext / kolumna sekretu (`REJECTED`).
- **Odrzucone:** live vision · auto-accept · kwota / float.

**Łowca:** ISTNIEJE `document_template` (layout jako dana). ISTNIEJE `revision` na szkicu. BRAK katalogu promptu ekstrakcji. `just dup` bez wyodrębnienia.

## Zakres

How-to + OPS_JOB. FE: `prompt_kind` jako `<select>`.

## Poza zakresem

wiring LLM · historia JSONB · live vision · partial write · M-03 progi

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%
- [ ] operator how-to

## Leftover

historia wierszy (JSONB, nie tabela) · wiring Instructor do katalogu · live vision
