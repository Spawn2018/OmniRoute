# Plaster 566.0 · V3 leftover — HITL `container.demurrage_free_days`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala V · V3 · leftover z [196.0](archived/196.0-free-time-clock.md) · [233.0](archived/233.0-container-free-time-origin.md)

**Zależy od:** 153.0 `container` · 233.0 `free_time_origin_h` (godziny — obok, nie to samo) · 196.0 `free_time_clock` (katalog dni — obok)

**Status:** delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover V3 po blank sailing. Pogłębienie istniejącej `container`: opcjonalny `demurrage_free_days` (`Integer` nullable, ≥ 0, dana w **dniach**, nie `now() − gate_in`). POST bez pola → `null`. Zła wartość → 400 (`dni`). Nie mutuje `free_time_clock`.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["566.0"]` → `/shipments`. OpenFGA bez nowej relacji. OPS bez +1.
- **Odrzucone:** countdown N3 / remaining (`REJECTED` → park). Szkic `charge` D&D (`REJECTED` → park). `detention_free_days` / `mixed_dd_days` w tym plasterze (`REJECTED` → leftover). Import `free_time_clocks` (`REJECTED`). Kwota / float (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live armator / PIN / VGM.

**Łowca:** BRAK `demurrage_free_days`. ISTNIEJE `free_time_origin_h` / `free_time_dest_h` (godziny EXP1). ISTNIEJE `free_time_clock.free_days` (katalog V3, nie kolumna kontenera). ISTNIEJE `blank_sailing_mark` (565.0).

## Zakres

1. Migracja `446_container_demurrage_days` (po `445_blank_sailing_mark`): kolumna nullable Integer.
2. Domain `require_demurrage_free_days` + API POST opcjonalny + GET.
3. UI `/shipments` panel kontenera. How-to dopisek. `SHIPPED["566.0"]`.

## Poza zakresem

countdown N3 · detention/mixed · szkic charge · free_time_clock mutacja · AI3 · kwota

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `demurrage_free_days=7` zapisuje dni
- [ ] ujemne / float / bool → 400 (`dni`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero float / live HTTP / import free_time_clocks

## Leftover

`detention_free_days` · `mixed_dd_days` · N3 countdown · szkic charge · live armator

## Karta post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | DO WYPEŁNIENIA | |
| Szybkość | N/A | kolumna |
| SQL | N/A | ADD COLUMN |
| N+1 | OK | |
| Dług / człowiek | OK | leftover detention/mixed |
| Proza operatora | 5–15 zdań | dopisek how-to kontener |
| Docs/OS | DO WYPEŁNIENIA | |
| Pushy do zielonego | N/A | |
| Minuty plastra | N/A | `/noc` |
| Bench rzemiosła | DO WYPEŁNIENIA | |
