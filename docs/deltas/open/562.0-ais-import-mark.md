# Plaster 562.0 · C2 leftover — HITL `ais_import_mark`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C2 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md)

**Zależy od:** tenancy · wzorzec HITL mark · C1 filing (obok, nie to samo) · EXP2.21 `sid_import_mark` (inny kanał)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C2 leftover. Nowa tabela `ais_import_mark`: `mark_code` (snake 2–32) + `import_kind` `ais|aes|intrastat|other` + `source_ref`. Append-only. RLS. Katalog stancji zgłoszenia AIS-IMPORT / AES / Intrastat — nie PUESC live.
- **Wybrane:** POST/GET `/api/v1/ais-import-marks`. OpenFGA `can_manage_ais_import_marks`. UI `/ais-import-marks`. `SHIPPED["562.0"]`.
- **Odrzucone:** PUESC HTTP / XML AIS-IMPORT / AES / Intrastat live (`REJECTED` → park TO_VERIFY konto). Kwota / float / scoring (`REJECTED`). Mutacja `sid_import_mark` / `filing_scheme_mark` (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / live FK UUID / live 409 blocks_create.

**Łowca:** BRAK `ais_import_mark`. ISTNIEJE `sid_import_mark` (SID/EDI, nie celne AIS). ISTNIEJE `filing_scheme_mark` (ICS2/CBAM/eFTI). ISTNIEJE `shipment_monitoring_filing` (SENT/C1).

## Zakres

1. Migracja `442_ais_import_mark` + RLS + izolacja.
2. Model → repo → serwis → API → OpenFGA → FE → how-to.
3. `SHIPPED["562.0"]` → `/ais-import-marks`.

## Poza zakresem

PUESC live · XML AIS/AES · Intrastat HTTP · AI3 · G0 · kwota · FK shipment

## Kryteria akceptacji

- [ ] POST zły kind / obce source_ref / pusty kod → 400
- [ ] izolacja tenantów
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / amount / PUESC

## Leftover

PUESC live AIS/AES/Intrastat · konto TO_VERIFY · scoring
