# Plaster 427.0 · EXP1 `diversion_mark` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `diversion_mark` — `mark_code` + `stance_kind` `diversion|reroute|other` + `source_ref`. EXP1 `diversion_of_shipment_id` bez FK self-shipment.
- **Odrzucone:** FK `diversion_of_shipment_id` na shipment — leftover osobny.
- **Odrzucone:** `cargo_value` — kwota.
- **Odrzucone:** temperature/logger — osobny leftover (reefer_mark już pokrywa część).

## Zakres

HITL `diversion_mark`: mark_code + stance_kind diversion|reroute|other + source_ref.
Migracja `337` (revises `336_haulier_role_mark`). UI `/diversion-marks`.
OpenFGA `can_manage_diversion_marks` (exp4).

## Poza zakresem

FK shipment · mapa · kwota · auto-reroute

## Kryteria

- [ ] POST z kodem, stance_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/margin/score → 422
- [ ] izolacja RLS; `__init__.py`; REQUIRED `/diversion-marks`
- [ ] OPS_JOBS length 244; SHIPPED `"427.0"`

## Leftover

FK diversion_of_shipment_id · temperature fields
