# Plaster 413.0 · EXP3.0b `po_plant_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `po_plant_mark` per tenant: `mark_code` + `plant_kind`
`plant`|`batch`|`sku`|`other` + `source_ref` (`tenant:manual` | `fixture://po-plant-mark/…`).

Klon wzorca `demo_sim_mark`:
- migracja `323` (revises `322_demo_sim_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_po_plant_marks`
- UI `/po-plant-marks`, business-lists + ops-index `SHIPPED["413.0"]`
- operator `docs/operator/po-plant-mark.md`
- izolacja RLS; POST `amount`/`qty`/`score` → 422

Znacznik plant/batch/SKU na PO jako dana. Bez live EDI / auto shipment.

## Poza zakresem

live EDI · auto shipment · qty/kwota · FK purchase_order zapis

## Kryteria

- [ ] POST z kodem, plant_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/qty → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live EDI · auto shipment · qty
