# Plaster 249.0 · T1 EXP1 `packaging_code` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [248.0](../archived/248.0-stop-quantity.md) leftover opakowanie · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1
**Zależy od:** 248.0 `quantity`
**Status:** Delta zaakceptowana `/noc`. Nie plomba. Nie awizacja. Nie mapa. Nie FK słownika.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 opakowanie/plomba/awizacja + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover plomba/awizacja/waiting/POD parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `packaging_code` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`opakowanie`). `require_stop_packaging_code` w `app.domain.stop` — nie import `container` / `require_packaging_code`. Kod to tekst, nie FK słownika (jak 223.0 na kontenerze). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["249.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `185_stop_pack` (po `184_stop_quantity`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.quantity` (248.0). ISTNIEJE `container.packaging_code` (223.0, inny BC). BRAK `stop.packaging_code`. BRAK tabeli słownika opakowań. BRAK plomby / awizacji na `stop`.

**Odrzucone:**

- FK słownika opakowań.
- Plomba / awizacja / waiting / POD.
- Tabela `stop_group`.
- Import `container` / `charge` / float / km / mapa / T5 / S53 / live HTTP.

## Zakres

1. Migracja `185_stop_pack`: `packaging_code` nullable String(32).
2. POST opcjonalny `packaging_code`. Zły token → 400 (`opakowanie`). Serwis nie importuje container/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

plomba · awizacja · waiting · POD · tabela `stop_group` · słownik FK · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `packaging_code` zapisuje kod, bez plomby
- [ ] POST bez pola / pusty trim zostaje `null`
- [ ] nie-tekst / za długi → 400 (`opakowanie`)
- [ ] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / `/fleet` / live HTTP / import `container`
