# Plaster 564.0 · C6 leftover — HITL `shipment.is_waste`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · C6 · [karty-pol-fala-c.md](../../analysis/karty-pol-fala-c.md) · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § `is_waste`

**Zależy od:** 90.0 `shipment` · 563.0 `waste_mark` (katalog stancji — obok, nie to samo)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy C6 leftover po katalogu `waste_mark`. Pogłębienie istniejącej `shipment`: kolumna `is_waste` boolean NOT NULL DEFAULT `false`. Operator ustawia przy zapisie zlecenia (POST opcjonalny `is_waste`). Nie MOS live. Nie druga tabela.
- **Wybrane:** UI zostaje `/shipments`. `SHIPPED["564.0"]` → `/shipments`. OpenFGA bez nowej relacji (`can_manage_shipments`).
- **Odrzucone:** MOS/BDO HTTP live (`REJECTED` → park). Auto-ustawianie z `waste_mark` (`REJECTED`). Kwota / float / scoring (`REJECTED`).
- **Odrzucone teraz:** AI3-payload / G0-SH / PUESC / live 409 / C3–C5 live.

**Łowca:** ISTNIEJE `waste_mark` (563.0) — katalog stancji BDO/KPO/WSR, **nie** flaga na zleceniu. ISTNIEJE `shipment.shipment_ref` (204.0) — wzorzec kolumny HITL. ISTNIEJE `relation_document_requirement` relation_kind `waste`. BRAK `shipment.is_waste`.

## Zakres

1. Migracja `444_shipment_is_waste`: kolumna boolean NOT NULL DEFAULT false.
2. Model + domena + API POST opcjonalny `is_waste` + GET zwraca flagę.
3. UI `/shipments` (checkbox HITL). How-to dopisek. `SHIPPED["564.0"]`.

## Poza zakresem

MOS live · BDO HTTP · auto z `waste_mark` · AI3 · G0 · kwota · 409

## Kryteria akceptacji

- [ ] POST bez `is_waste` → `false`
- [ ] POST `is_waste=true` zapisuje flagę
- [ ] izolacja tenantów (bez zmiany RLS)
- [ ] UI + SHIPPED
- [ ] zero float / live HTTP / MOS / auto z katalogu

## Leftover

MOS/BDO live · scoring · auto-link waste_mark ↔ shipment
