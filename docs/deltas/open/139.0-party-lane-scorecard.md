# Plaster 139.0 · O5 `party_lane_scorecard` snapshot per lane

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O5 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § O5
**Zależy od:** 10.0 `party_scorecard` · 4.0 `port` · 130.0 `network_member.party_id`
**Status:** Plan zaakceptowany `/noc` 2026-09-08. Żywy ID **M-13**. Nie O6. Nie scoring osoby.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z kolejki O5):** nowa tabela `party_lane_scorecard` per tenant. Snapshot jak 10.0, nie silnik. Unikat `(organization_id, party_id, origin_port_id, destination_port_id, window_days)`. Pola: `sample_size` ≥ 0, `answered_inquiry_count` ≥ 0, `shipment_count` ≥ 0, `cheapest_count` ≥ 0, `median_response_hours` Numeric nullable, `computed_at`, `source_ref`. `window_days` z M-03 (`lane_scorecard_window_days`, 1–365) kopiowane na wiersz przy zapisie. Tekst podpowiedzi = szablon w domenie z tych pól (nie LLM): przy `sample_size=0` stały komunikat o braku historii. UI `/party-scorecards`. OpenFGA `can_manage_parties`. `SHIPPED_CHARGE_ROUTES["139.0"]` = `/party-scorecards`. Refresh SQL z `carrier_inquiry` / `shipment` / `channel_quote` = leftover (serwis parties nie importuje innych BC).

**Łowca:** ISTNIEJE `party_scorecard` (10.0) — globalny snapshot, bez POL/POD. ISTNIEJE GLOSSARY `party_lane_scorecard`. ISTNIEJE `/party-scorecards`. PODOBNE upsert snapshotu. BRAK tabeli lane, szablonu podpowiedzi, klucza okna.

**Odrzucone:**

- Kolumny lane na `party_scorecard`.
- Liczenie mediany / min oferty w Pythonie.
- Import `carrier_inquiries` / `shipments` / `channel_quotes` w serwisie parties.
- `sent_at` / `answered_at` na `carrier_inquiry` (brak kolumn — mediana zostaje NULL aż osobny plaster).
- LLM w tekście podpowiedzi.
- Scoring osoby.

## Zakres

1. Migracja `081_party_lane_scorecard`: RLS + izolacja.
2. POST/GET `/party-lane-scorecards`. Klucz `lane_scorecard_window_days` w M-03.
3. Szablon podpowiedzi w domenie. UI na `/party-scorecards`.
4. `just api-types`. How-to w `katalogi.md` wtopione (40 linii).

## Poza zakresem

O6–O8 · live SQL z innych BC · NBP · Graph

## Kryteria akceptacji

- [ ] `sample_size=0` nadal zapisuje wiersz i daje tekst o braku historii
- [ ] `median_response_hours` ujemne → 400
- [ ] Ten sam party+lane+okno = ten sam wiersz (409 albo upsert)
- [ ] Serwis parties bez importu inquiries / shipments / quotes
- [ ] Test izolacji nowej tabeli
- [ ] `just api-types`
