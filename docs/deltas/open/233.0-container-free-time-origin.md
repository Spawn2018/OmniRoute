# Plaster 233.0 · T3 `free_time_origin_h` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [232.0](../archived/232.0-container-bl-kind.md) leftover `free_time_origin_h` · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container
**Zależy od:** 232.0 `bl_kind`
**Status:** Otwarty `/noc` 2026-09-10. Nie countdown. Nie `free_time_clock`. Nie PIN. Nie VGM. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `free_time_origin_h`/party/PIN/VGM/booking + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53):** leftover T1b parked. leftover T2c km/`party`/`/fleet` parked. leftover T7b parked. leftover T5 parked. leftover N3 countdown D&D parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `carrier_party_id` parked (FK). leftover `quantity` / `weight_kg` parked (Decimal). leftover `free_time_dest_h` parked (następne pole). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `free_time_origin_h` (integer ≥ 0, dana w godzinach EXP1, nie `now() − gate_in`). POST bez pola zostaje `null`. Zła wartość → 400 (`godziny`). `require_free_time_origin_h` w `app.domain.container` — nie wołać `require_free_days` z V3. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["233.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `172_container_free_time_origin` (po `171_container_bl_kind`). `_WriteBox` — dodać `idle`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `free_time_clock` 196.0 (`clock_kind` + `free_days`, UI `/free-time-clocks`) — nie rozszerzać, nie import. ISTNIEJE leftover 196 `demurrage_free_days` na `container` (odrzut 196.0). BRAK `container.free_time_origin_h`. PODOBNE: integer ≥ 0, INNA jednostka (godziny vs dni), INNY obiekt. Nie countdown. Nie szkic `charge`.

**Odrzucone:**

- Countdown / remaining / `now() − gate_in`.
- Import `free_time_clocks` / kolumna `demurrage_free_days`.
- `free_time_dest_h` w tym plasterze.
- PIN / VGM / `booking_no` / FK.
- T1b · T2c km · T7b · T5 · N3 · S53 Auth0.

## Zakres

1. Migracja `172_container_free_time_origin` (po `171_container_bl_kind`): `free_time_origin_h` nullable `Integer`.
2. POST opcjonalny `free_time_origin_h`. Zła wartość → 400. Serwis nie importuje free_time_clocks/charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

countdown N3 · free_time_clock · dest hours · PIN · VGM · waga · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `free_time_origin_h=48` zapisuje godziny, bez countdown i bez PIN
- [ ] POST bez pola zostaje `null`
- [ ] ujemne / nie-int → 400 (`godziny`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape / import free_time_clocks
