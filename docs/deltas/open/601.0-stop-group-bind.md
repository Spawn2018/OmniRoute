# Plaster 601.0 · T1b leftover — HITL bind `stop.stop_group_id`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover T1b bind · [600.0](../archived/600.0-stop-group-table.md) · [542.0](../archived/542.0-consignment-stop.md)

**Zależy od:** 600.0 `stop_group` · 150.0 `stop`

**Status:** Delta zaakceptowana `/noc` 2026-09-20. Nie mapa. Nie kasowanie `stop_group_code`. Nie HW. Nie UXCL.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover T1b bind po tabeli 600.0. Opcjonalny `stop_group_id` na `stop` — złożone FK `(organization_id, shipment_id, stop_group_id)` → `stop_group` (jak `consignment.stop_id` 542.0 wymaga unique na grupie: migracja doda `uq_stop_group_org_shipment_id`). POST bez pola → `null`. Obca grupa / grupa innego zlecenia → 400 (`grupa`). Nieznana → 404. Kolumna `stop_group_code` **zostaje** (luźny token 213.0) — bez sync.
- **Wybrane:** UI `/shipments` panel punktu. `SHIPPED["601.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `477_stop_group_bind` (po `476_stop_group`). API składa `StopGroupService` / repo — **StopService nie importuje stop_groups**.
- **Odrzucone:** sync `stop_group_code` ↔ tabela (`REJECTED` → leftover). Usunięcie `stop_group_code` (`REJECTED`). M:N członkostwo (`REJECTED`). Mapa / HW / UXCL / Job#2 / N3 (`REJECTED` → park).

**Łowca:** ISTNIEJE `stop_group` (600.0). ISTNIEJE `stop.stop_group_code`. BRAK `stop.stop_group_id`. PODOBNE: `consignment.stop_id` (542.0).

## Zakres

1. Migracja `477_stop_group_bind`: unique `(organization_id, shipment_id, id)` na `stop_group`; kolumna `stop_group_id` + FK złożone.
2. Domain + serwis stop + API POST/GET. Test HTTP bind / mismatch / omit.
3. UI `/shipments`. How-to. `SHIPPED["601.0"]`.

## Poza zakresem

sync kod↔FK · kasowanie `stop_group_code` · mapa · HW · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [ ] POST bez `stop_group_id` → 201, `null`
- [ ] POST grupa tego samego zlecenia → 201
- [ ] POST grupa obcego zlecenia → 400 (`grupa`)
- [ ] nieznana grupa → 404
- [ ] zero float / import charges / mapa

## Leftover

sync `stop_group_code` · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
