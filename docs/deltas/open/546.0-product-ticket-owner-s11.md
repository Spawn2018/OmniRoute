# Plaster 546.0 · Plat-HD leftover — S11 owner na `product_ticket`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Plat-HD-flow leftover · [545.0](../archived/545.0-product-ticket.md) · [544.0](../archived/544.0-margin-floor-s11.md)

**Zależy od:** `product_ticket` HITL (545.0) · `operator_decision` M-71 (74.0 / 121.0 / 544.0)

**Status:** zaakceptowany `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover Plat-HD S11 owner. `POST /product-tickets` z `ticket_kind=owner_ok` wymaga `reviewed_ticket_id` (istniejący ticket tenanta). Bez accepted `owner_decision_id`: API tworzy/wznawia pending `operator_decision` (`subject_kind=product_ticket`, `subject_id=reviewed_ticket_id`) i zwraca **409** z `decision_id`. Z accepted `owner_decision_id` dla tego ticketu → INSERT `owner_ok`.
- **Wybrane:** inne kind (`report|triage|other`) bez zmian; `owner_decision_id` / `reviewed_ticket_id` wtedy zabronione.
- **Wybrane:** compose w `product_tickets.py` (jak 544 charges) — nie import charges/decisions w `ProductTicketService`.
- **Odrzucone:** auto-fix / agent PR (`REJECTED`). UPDATE ticketu w miejscu (`REJECTED`). Mob Expo (`REJECTED`). FK do `product_ticket_mark` (`REJECTED`). CAPA (`REJECTED`).
- **Odrzucone teraz:** AI3-payload (park OpenAI CI), G0-SH (host/IdP), SH-R16-13 (Dependabot).

**Łowca:** ISTNIEJE `product_ticket` · ISTNIEJE `operator_decision` + pending/unique · ISTNIEJE wzorzec 544. BRAK `subject_kind=product_ticket`. BRAK 409 owner przy `owner_ok`.

## Zakres

1. Alembic `427_od_product_ticket`: CHECK + domena `_SUBJECTS`.
2. API create: `owner_ok` → pending + 409 / accepted override → 201.
3. Testy HTTP: 409+decision_id · retry po accept → 201 · regresja report bez decyzji.
4. How-to `product-ticket.md`.
5. `SHIPPED["546.0"]` → `/product-tickets`.

## Poza zakresem

auto-fix · Mob · FK mark · CAPA · mail · AI3-payload · G0-SH

## Kryteria akceptacji

- [ ] POST `owner_ok` + `reviewed_ticket_id` bez decyzji → 409 + pending + `decision_id`
- [ ] drugi POST (ten sam reviewed) → ten sam pending, 409
- [ ] POST z `owner_decision_id` accepted → 201
- [ ] POST `report` bez pól owner → 201 (regresja 545)
- [ ] zero auto-fix / UPDATE / float

## Leftover

FK mark · Mob · auto-fix / agent PR
