# Plaster 593.0 · T2 leftover — HITL `resource.inventory_no`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [592.0](../archived/592.0-resource-document.md) · [pola-wizja-2026-09.md](../../analysis/pola-wizja-2026-09.md) § 2.2 (`inventory_no`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie floating trailer. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `resource_document`. Opcjonalny `inventory_no` na `resource` (`String(32)`, tekst HITL, nie float, nie waluta). POST bez pola → `null`. Pusty string → `null`. Tekst dłuższy niż 32 → 400. Zmiana numeru przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / kg / LDM / m³). Nie unikalny. Nie klucz GPS.
- **Wybrane:** UI `/fleet`. `SHIPPED["593.0"]` → `/fleet`. OpenFGA bez nowej relacji (`can_manage_shipments`). Migracja `469_resource_inventory_no` (po `468_resource_document`). Bez nowej tabeli — RLS już na `resource`.
- **Odrzucone:** floating trailer jako nowa kolumna (`REJECTED` — `trip.trailer_id` jest od 152.0). `gvm_kg` / osie (`REJECTED` — V2b myto). flagi ADR / telefon / karta kierowcy (`REJECTED` w tym plasterze). HW (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.inventory_no`. ISTNIEJE `registration_no` (151.0 — ten sam wzorzec tekstu 32). ISTNIEJE `trip.trailer_id` (152.0 — naczepa na przejeździe, nie reuse). PODOBNE: opcjonalny tekst + supersede.

## Zakres

1. Migracja `469_resource_inventory_no`.
2. Domain `require_inventory_no` + serwis + API + GET.
3. UI `/fleet` pole numeru inwentarzowego. How-to. `SHIPPED["593.0"]`.

## Poza zakresem

floating trailer · `gvm_kg` · ADR · telefon · karta kierowcy · HW · UXCL · Job#2 · N3 · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `inventory_no="INV-1"` zapisuje tekst
- [ ] pusty string → `null`; >32 → 400
- [ ] zmiana numeru → supersede
- [ ] izolacja RLS (tabela `resource`, bez nowej)
- [ ] zero float / import charges

## Leftover

flagi ADR / telefon / karta kierowcy · `gvm_kg` · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
