# Plaster 600.0 · T1b leftover — HITL tabela `stop_group`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover T1b · [213.0](../archived/213.0-stop-group.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1

**Zależy od:** 150.0 `stop` · 90.0 `shipment` · 213.0 `stop_group_code` (kolumna zostaje)

**Status:** Delta zaakceptowana `/noc` 2026-09-20. Nie mapa. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover T1b po zamknięciu T2 (599.0). **Nowa tabela** `stop_group` per tenant — HITL nagłówek grupy punktów (groupage / dostawy razem), nie kolumna i nie mapa. Wiersz = `group_code` (2–32 `[A-Za-z0-9_-]`, ta sama reguła co `stop_group_code`) + złożone FK `(organization_id, shipment_id)` → `shipment` + `source_ref` (`tenant:manual` albo `fixture://stop-group/…`). N grup na jedno zlecenie. Unique `(organization_id, shipment_id, group_code)` i `(organization_id, source_ref)`. POST bez `shipment_id` → 400 (`zlecenie`). Nieznane zlecenie → 404. Zły kod → 400 (`grupa`). Obce `source_ref` → 400 (`obce`). Kolumna `stop.stop_group_code` **zostaje** (luźny token 213.0).
- **Wybrane:** UI `/stop-groups`. `SHIPPED["600.0"]` → `/stop-groups`. OpenFGA `can_manage_shipments` (bez nowej relacji — grupa żyje przy zleceniu). Migracja `476_stop_group` (po `475_resource_vehicle_profile`) + RLS FORCE + test izolacji. How-to fold w `zlecenie.md` / `katalogi.md`. Job OPS jak inne katalogi przy zleceniu.
- **Odrzucone:** członkostwo / FK `stop.stop_group_id` (`REJECTED` → leftover T1b bind). Usunięcie `stop_group_code` (`REJECTED`). Osobna tabela członkostwa M:N (`REJECTED`). Mapa / T6 / GPS (`REJECTED`). HW / UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park). Kwota / `charge` / float (`REJECTED`).

**Łowca:** ISTNIEJE `stop.stop_group_code` (213.0) — luźny token na punkcie, **nie** tabela. ISTNIEJE `consignment` — przesyłka N1, **nie** grupa stopów. ISTNIEJE `shipment_leg` — odcinek, **nie** grupa. BRAK tabeli `stop_group`. BRAK `/stop-groups`.

## Zakres

1. Migracja `476_stop_group` + RLS FORCE + indeks `(organization_id, shipment_id)`.
2. Model + repo + serwis + API POST/GET. Serwis nie importuje stops/charges i nie woła HTTP; API składa odczyt zlecenia.
3. UI `/stop-groups`. How-to. `SHIPPED["600.0"]`. Test izolacji.

## Poza zakresem

członkostwo `stop_group_id` · kasowanie `stop_group_code` · mapa · GPS · HW · UXCL · Job#2 · N3 · AI3 · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST z `shipment_id` + `group_code` zapisuje wiersz
- [ ] POST bez `shipment_id` → 400 (`zlecenie`); nieznane → 404; zły kod → 400 (`grupa`)
- [ ] GET lista per tenant
- [ ] izolacja RLS (dwa tenanty)
- [ ] zero float / import charges / FK na `stop` / mapa

## Leftover

T1b bind (`stop.stop_group_id` albo sync z `stop_group_code`) · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
