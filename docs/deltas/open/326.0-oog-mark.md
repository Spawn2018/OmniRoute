# Plaster 326.0 · G5 `oog_mark` HITL (katalog, bez wymiarów)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G5 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie wymiary. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G5 — tylko HITL katalog `oog_mark` jako dane. `mark_code` + `escort_kind` (`oog` | `lashing` | `escort` | `other`) + `source_ref`. UI `/oog-marks`. OpenFGA `can_manage_oog_marks`. Migracja `238_oog_mark`. Job OPS. Deny-list extract. Wymiary / cert / zezwolenie / klej T1 = leftover.

**Łowca:** BRAK tabeli `oog_mark`. ISTNIEJE `dangerous_good` — IMDG, nie OOG. ISTNIEJE `stop` — nie eskort.

**Odrzucone:** wymiary Decimal · lashing_cert bajty · escort party FK · zezwolenie_kraj · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `238_oog_mark` RLS FORCE.
2. POST/GET `/oog-marks`. OpenFGA. Deny-list.
3. UI `/oog-marks`. How-to. `SHIPPED_CHARGE_ROUTES["326.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, escort_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/length → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
