# Plaster 396.0 · EXP4.18 `rag_sop_mark` HITL (katalog, bez pgvector)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala EXP / EXP4.18 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) · [ai-gdzie-uzasadnione.md](../../analysis/ai-gdzie-uzasadnione.md) § RAG
**Status:** Delta zaakceptowana `/noc` 2026-09-12. Wolno `/plaster`. Nie pgvector. Nie RAG na wycenie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki EXP4.18 — tylko HITL katalog `rag_sop_mark` jako dane. `mark_code` + `scope_kind` (`sop` | `adr` | `mail` | `other`) + `source_ref`. UI `/rag-sop-marks`. OpenFGA `can_manage_rag_sop_marks`. Migracja `307_rag_sop_mark`. Job OPS. Deny-list extract. Live pgvector / RAG na wycenie-VAT-schemacie = leftover (HC-08).

**Łowca:** BRAK tabeli `rag_sop_mark`. ISTNIEJE `role_view_mark` / `mail_accept_mark` — inne katalogi HITL. ISTNIEJE Knowledge Base w analizie — nie tabela RAG.

**Odrzucone:** pgvector · embedding live · RAG na wycenie/VAT/schemacie DB · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `307_rag_sop_mark` RLS FORCE.
2. POST/GET `/rag-sop-marks`. OpenFGA. Deny-list.
3. UI `/rag-sop-marks`. How-to. `SHIPPED_CHARGE_ROUTES["396.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, scope_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/vector → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
