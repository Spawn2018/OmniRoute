# Plaster 68.0 · S5 istniejący silnik wyceny na customer_rfq (M-21)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S5  
**Zależy od:** 2.0 / 5.1 / 20.0 `quotation` · 67.0 `customer_rfq` · 0.3 RLS · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Nie nowy silnik. LLM nie liczy. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam `QUOTE_FROM_CURRENT_SQL` (kod + `superseded_by IS NULL`). Kolumna `quotation.customer_rfq_id` nullable, FK tenanta do `customer_rfq`. `POST /quotations` dostaje opcjonalne `customer_rfq_id`. API składa: ładuje RFQ, `party_id` musi siedzieć na RFQ i zgadzać się z body; POL/POD i kod podaje operator. Kwota tylko ze stawki. Serwis wyceny nie importuje `customer_rfqs` / inbound. Serwis RFQ nie importuje `quotations`. Ekran: `/quotations` — picker RFQ; ścieżka bez RFQ zostaje. Panel 21.0 zostaje śladem. Wiele wycen na jeden RFQ (kody). OpenFGA bez nowej relacji: `can_manage_quotations`.

**Łowca:** ISTNIEJE: `QuotationService.quote_from_current_rate`, `QUOTE_FROM_CURRENT_SQL`, `POST /quotations` + wsad, `customer_rfq.party_id`, panel 21.0, `/mail` „Utwórz RFQ”. PODOBNE: API składa inbound+RFQ w `customer_rfqs.py`; snapshot `party_id` na wycenie. BRAK: `quotation.customer_rfq_id`, `get_rfq`, picker RFQ na wycenie.

**Odrzucone:**

- Nowy silnik albo drugi SQL doboru stawki (COVERED arch. M-17 / M-22; S5 = ten sam INSERT…SELECT).
- Kwota na `customer_rfq` albo z body / LLM / JS.
- Import `quotations` w serwisie RFQ; import inbound / `customer_rfqs` w serwisie wyceny.
- Auto-wycena przy tworzeniu RFQ.
- `applies_when` (S6) · HS/CN (S7).
- Zastąpienie panelu 21.0.
- F9.1 · Auth0 · portale · M-02.
- Nowa relacja OpenFGA.
- Unikat „jedna wycena na RFQ” — wsad kodów 20.0 zostaje.

## Zakres

1. Migracja `029_quotation_customer_rfq`: `customer_rfq_id` na `quotation`; `uq_customer_rfq_org_id` na `(organization_id, id)`; FK złożone; indeks `(organization_id, customer_rfq_id)`. Bez nowej tabeli, bez nowego RLS (`quotation` już FORCE).
2. `get_rfq` w serwisie RFQ (odczyt). `POST /quotations` (i wsad) z opcjonalnym `customer_rfq_id`. `GET` filtr po `customer_rfq_id`.
3. Test: RFQ tenanta B nie wchodzi w wycenę A; brak `party_id` na RFQ = błąd; kwota = `rate_line`, nie body; ścieżka bez RFQ bez zmian; `quotation_gap` zostaje.
4. `/quotations`: picker RFQ, kolumna i filtr `customer_rfq_id`, `<Money/>`. `/mail`: link „Wycena” na `/quotations?rfq=`.

## Poza zakresem

S6 `applies_when` · S7 HS/CN · nowy silnik · kwota na RFQ · IMAP / Graph · auto przy RFQ · outbox · LLM liczący kwoty · F9.1

## Ustalenia

- Żywy ID: **M-21**. M-28 zostaje obiektem RFQ, nie drugim silnikiem.
- API składa BC jak 67.0. `source_ref` wyceny zostaje ze stawki; korelacja = `customer_rfq_id`.
- HITL extract bez zmian. LLM nie liczy. `charge` zostaje prawdą o marży.
- How-to: dopisać do `docs/operator/ścieżka-pieniędzy.md` (job zapisu wyceny z RFQ).

## Schemat

`quotation.customer_rfq_id` nullable. FK `(organization_id, customer_rfq_id)` → `customer_rfq`.

## Kryteria akceptacji

- [ ] FK tenanta; test izolacji (RFQ B nie wchodzi w wycenę A)
- [ ] Kwota z INSERT…SELECT, nie z body / LLM
- [ ] RFQ bez `party_id` = błąd domenowy
- [ ] `can_manage_quotations`; 403 bez tuple
- [ ] UI picker RFQ; panel 21.0 zostaje
- [ ] Serwis RFQ nie importuje quotations; serwis wyceny nie importuje RFQ/inbound
- [ ] `just api-types` po zmianie OpenAPI

## Pytania

Brak. `/noc` akceptuje deltę.
