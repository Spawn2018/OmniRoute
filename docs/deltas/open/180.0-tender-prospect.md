# Plaster 180.0 · G2.10 `tender_prospect`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § G2.10 · leftover prospecting HITL
**Zależy od:** 169.0 `tender` · `party` (firma, nie scrape)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie TED scrape.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z G2.10–G2.18):** jedna tabela `tender_prospect` per tenant — **HITL ślad prospectingu firmy z `source_ref`**, nie scrape kontaktów i nie bid/no-bid silnik. Wiersz = `tender_id` (FK tenanta do `tender`) + `party_id` (FK tenanta do `party`) + `outreach_code` (snake 2–32) + `source_ref` (`tenant:manual` albo `fixture://tender-prospect/…`). Operator wpisuje, że kontaktował firmę; serwis nie woła LLM, nie czyta `charge` i nie importuje extraction. Zły kod → 400 (`prospekt`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, tender_id, party_id)`. GET listy SQL. API składa odczyt nagłówka i kontrahenta (`TenderService.get_board`, `PartyService.get_party`). UI `/tender-prospects` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["180.0"]` = `/tender-prospects`. OpenFGA: `can_manage_tender_prospects`. Migracja `120_tender_prospect`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `tender_consortium_member` — fotel konsorcjum, nie ślad outreach. ISTNIEJE `carrier_inquiry` — zapytanie do `network_member`, nie przetarg G2. ISTNIEJE `tender_rfp_intake` — przyjęcie RFP, nie firma. ISTNIEJE `party` — katalog; **nie** dokładamy prospectingu jako JSON tam. BRAK `tender_prospect`. BRAK `/tender-prospects`.

**Odrzucone:**

- Bid/no-bid silnik / award four-eyes / TED capability / CO₂ (leftover G2.11–G2.18).
- Scrape kontaktów / TED HTTP / auto-award / INSERT `party` z lookupu.
- Kwota / druga marża / UPDATE `tender.status` / `draft_kind` prospecting.
- Float / T-SQL / live HTTP.

## Zakres

1. Migracja `120_tender_prospect`: RLS, izolacja, indeks `(organization_id, tender_id)`, unique `(organization_id, tender_id, party_id)`, CHECK `outreach_code`.
2. POST/GET. Zły `outreach_code` / `source_ref` → 400. API składa odczyt `tender` i `party`, serwis nie importuje tenders / parties / charges / extraction.
3. UI `/tender-prospects`. `just api-types`. How-to `docs/operator/prospekt-przetargu.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `ProspectDesk` / `prospectWrite` / `persistProspectMark`.

## Poza zakresem

G2.11–G2.18 bid/no-bid/four-eyes/TED/CO₂ · G2.19–G2.22 · G2.23 · auto-award · scrape · LLM · kwota na wierszu · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `tender_prospect` z kodem outreach i `source_ref` na istniejącym `tender` i `party`, bez auto-award
- [ ] zły `outreach_code` → 400
- [ ] GET zwraca prospekty tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / LLM / live HTTP / scrape
