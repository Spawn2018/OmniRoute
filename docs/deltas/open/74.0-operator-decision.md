# Plaster 74.0 · S11 szyna Akceptuj/Zmień/Odrzuć (M-71)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S11  
**Zależy od:** 0.3 RLS · 0.4 OpenFGA

**Status:** Plan zaakceptowany (`/noc` 2026-09-03). Nowy żywy ID. Nie M-57. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** nowy żywy ID **M-71**, token `operator_decision`. Nowa tabela `operator_decision` z RLS FORCE + test izolacji. Wiersz: `subject_kind` (na start tylko `inbound_message`) + `subject_id` UUID bez FK do innego BC (API nie woła inbound). Status: `pending` | `accepted` | `changed` | `rejected`. Create = `pending`. Decyzja jawnym POST — `accepted` albo `rejected` w tym plasterze; `changed` = leftover (S13/S14). Unikat pending na `(organization_id, subject_kind, subject_id)`. OpenFGA: nowa relacja `can_manage_operator_decisions` = member. Ekran `/decisions`. Serwis nie importuje inbound / quotations / extraction. Nie woła accept HITL 1.3. Nie send.

**Łowca:** BRAK `operator_decision` / szyny decyzji w kodzie. ISTNIEJE: HITL extract `can_review_extractions` + accept→`rate_line` (1.3) — to nie ta szyna. PODOBNE: 22.0 `offer_acceptance` to tablica odczytu, nie decyzja. M-57 = tablica pending extract, nie accept.

**Odrzucone:**

- Numerowanie jako M-57 / nadpisanie copilot extract.
- Wołanie `ExtractionService` / accept 1.3 z tej szyny.
- Send / Graph / `mailto:` (S18).
- Lock optymistyczny (S14). Draft maila (S13).
- FK do `inbound_message` w tym plasterze (skład API = leftover S13).
- Status `changed` w zapisie (kolumna status dopuszcza wartość; POST nie nadaje).
- F9.1 · Auth0 · portale · M-02.

## Zakres

1. Migracja `034_operator_decision_rls`: tabela + RLS FORCE + unikat pending. Test izolacji.
2. Serwis create + decide accept/reject. `GET /operator-decisions`. OpenFGA nowa relacja.
3. UI `/decisions`: lista, utwórz pending, Akceptuj / Odrzuć. Nie licz w JS.

## Poza zakresem

S13 draft maila · S14 lock · S18 send · accept extractu · F9.1

## Ustalenia

- Żywy ID: **M-71**. Arch. M-179 nie dostaje osobnego kodu.
- HITL extract 1.3 bez zmian. LLM nie liczy. `charge` zostaje prawdą o marży.
- How-to: `docs/operator/katalogi.md`.

## Schemat

`operator_decision`: `id`, `organization_id`, `subject_kind`, `subject_id`, `status`, `decided_at`, `source_ref`, timestamps. CHECK kind/status. Unikat pending.

## Kryteria akceptacji

- [ ] RLS FORCE + izolacja
- [ ] Create = pending; accept/reject z pending; ponowna decyzja = błąd
- [ ] `can_manage_operator_decisions`; 403 bez tuple
- [ ] UI `/decisions`
- [ ] Serwis nie importuje inbound / quotations / extraction
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
