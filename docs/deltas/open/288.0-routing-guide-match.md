# Plaster 288.0 · CT4 `routing_guide_match` HITL (katalog trybu dopasowania)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala CT / CT4 leftover matching
**Zależy od:** 287.0 shipment 409 · 286.0 ASN 409 · 285.0 enforcement
**Status:** Plan `/noc` 2026-09-11. Nie silnik matching. Nie 409 zmiana.

## Wybrane / odrzucone / dlaczego

**Wybrane:** leftover matching lane/mode jako **katalog trybu**: `guide_code_only` | `lane_label` | `mode_label`. Żywy plaster: tabela `routing_guide_match` — `mark_code` + `match_kind` + `source_ref`. UI `/routing-guide-matches`. OpenFGA `can_manage_routing_guide_matches`. Migracja `221_routing_guide_match`. Job OPS 131. BC `routing_guide_matches`. Egzekucja 409 zostaje na guide_code (286/287).

**Łowca:** BRAK `routing_guide_match`. ISTNIEJE gate guide_code + katalog guide/enforcement.

**Odrzucone:** silnik porównujący lane/mode na POST · zmiana 409 · float · HTTP live · UPDATE/DELETE.

## Zakres

1. Migracja `221_routing_guide_match` po `220_shipment_guide_code`: RLS FORCE.
2. POST/GET `/routing-guide-matches`. OpenFGA. Deny-list extract.
3. UI `/routing-guide-matches`. How-to. Job OPS 131.

## Kryteria akceptacji

- [ ] POST z kodem, kind guide_code_only|lane_label|mode_label i source_ref
- [ ] zły kod/kind/source_ref → 400
- [ ] POST z amount → 422
- [ ] izolacja RLS; zero float; zero zmiany bramki 409

## Leftover po 288.0

Silnik matching lane/mode na POST. Wspólny SELECT. Auto shipment. CT2/CT5/CT8 live.
