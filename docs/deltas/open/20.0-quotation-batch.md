# Plaster 20.0 · M-27 Wycena wsadowa (`quotation_batch`)

**Spec źródłowa:** [docs/spec/quotation-batch.md](../../spec/quotation-batch.md)  
**Zależy od:** 2.0/5.1 `quotation` INSERT…SELECT

**Status:** Plan `/noc` 2026-09-02. Nie Fala 8. LLM nie liczy. Nie nowa tabela.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 3 M-27):** wiele kodów na **tej samej lane**, jedno HTTP, istniejący `quote_from_current_rate`. Token `quotation_batch` (API, nie tabela). Żywy ID **M-27** jako pogłębienie M-21.

**Łowca:** BRAK `quotation_batch` / CSV. ISTNIEJE: `POST /quotations` + SQL ze stawki. PODOBNE: `normalize_aliases` (unikaty kodów).

**Odrzucone:**

- Tabela jobów / CSV — zgadywanie schematu; Temporal parked (M-02).
- N requestów z UI — to nie wsad.
- Set-based INSERT — leftover; 20.0 reuse istniejącego SQL.
- Kwota w body. LLM.

## Zakres

`POST /api/v1/quotations/batch`. OpenFGA `can_manage_quotations`. UI `/quotations`.

## Poza zakresem

CSV · outbox · ExtractionService · mnożenie kwot

## Ustalenia

- Max 20 kodów. Dedup jak aliasy.
- `SHIPPED_CHARGE_ROUTES["20.0"]` = `/quotations`.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak.

## Kryteria akceptacji

- [ ] Batch 201 zwraca listę wycen; kwota z SQL
- [ ] Pusty wsad / >20 → 400
- [ ] Gap na jednym kodzie → 400, bez zapisu
- [ ] Brak `amount` w body
- [ ] `just dup` ≤ 3%

## Knowledge

Nie RAG. Wzorzec = 2.0 INSERT…SELECT.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. CSV i set-based SQL leftover.
