# Plaster 480.0 · Plat-HD help desk produktu — katalog HITL

**Status:** Delta zaakceptowana `/noc 20` 2026-09-14. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 8 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Plat-HD

**Zależy od:** tenancy RLS · wzorzec HITL mark · nie CI8 / CAPA / operator_notice

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `product_ticket_marks`. Tabela `product_ticket_mark`: `mark_code` + `ticket_kind` `report|triage|owner_ok|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR7.1 — katalog HITL stancji ticketu produktu (VISION: zgłoszenie błędu programu → analiza → akceptacja właściciela), nie workflow naprawy i nie auto-merge.
- **Wybrane:** POST/GET `/api/v1/product-ticket-marks`. OpenFGA `can_manage_product_ticket_marks` (member). UI `/product-ticket-marks`. `SHIPPED["480.0"]` → `/product-ticket-marks`.
- **Wybrane:** Alembic `385_product_ticket_mark` (`down_revision = "384_line_impact_layer_mark"`).
- **Odrzucone:** rozszerzenie `operator_notice` / `capa_mark` / `repair_playbook` (`REJECTED` — VISION: nie Operational Service / CAPA / CI8).
- **Odrzucone:** auto-naprawa bez akceptacji właściciela · live agent deploy · kwota (`REJECTED`).
- **Odrzucone:** Mob Expo / EAS live (`REJECTED` — osobny wiersz Mob; CURRENT park).

**Łowca:** ISTNIEJE `operator_notice` / `capa_mark` / `repair_playbook` / `mobile_client_mark`. BRAK `product_ticket_mark`. BRAK katalogu ticketu błędu programu.

## Zakres

How-to + OPS_JOB. FE: `ticket_kind` jako radio fieldset (rozróżnienie jscpd).

## Poza zakresem

auto-fix · deploy bez HITL · CAPA workflow · operator_notice · Expo Mob · kwota

## Kryteria akceptacji

- [ ] migracja + FORCE RLS + izolacja
- [ ] POST zły kind → 400; amount → 422
- [ ] craft_style; jscpd < 3.0%

## Leftover

workflow ticket→analiza→naprawa · Mob akceptacja z telefonu · agent auto-PR

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | |
| Szybkość | N/A | |
| SQL | N/A | |
| N+1 | — | |
| Dług / człowiek | — | |
| Proza operatora | — | |
| Docs/OS | — | |
| Bench rzemiosła | — | |
