# Plaster 50.0 · M-70 Wdrożenie (`tenant_rollout`)

**Spec źródłowa:** [docs/spec/tenant-rollout.md](../../spec/tenant-rollout.md)  
**Zależy od:** 3.0 `organization_setting.setting_key`

**Status:** Otwarty `/noc` 2026-09-02. Nie Fala 10. LLM nie liczy. Nie nowa tabela. Nie deploy.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z Fali 11 M-70):** wdrożenie **nie ma tabeli rolloutu**. Operator widzi wiersze `organization_setting` z kluczem allowlisty `default_currency` (konfiguracja jako dane, nie env). Token `tenant_rollout` (UI, nie tabela). Żywy ID **M-70**. 3.0 `/organization-settings` zostaje zapisem. Nie procent gotowości.

**Łowca:** BRAK `rollout` / `onboarding` / `go_live` w Postgres. ISTNIEJE: `fetchOrganizationSettings`, `ALLOWED_SETTING_KEYS` = `default_currency`. PODOBNE: 3.0 katalog z upsert. Zero tabel wdrożenia.

**Odrzucone:**

- Tabela wdrożenia / checklisty / środowiska — zgadywanie schematu (CURRENT).
- Upsert z tej tablicy — zapis zostaje na `/organization-settings`.
- Scoring gotowości w JS.
- Klucze spoza allowlisty.

## Zakres

Read-only `/rollout`. `data-tenant-rollout="board"`. Brak migracji. `rolloutSettings(rows)`. Link do `/organization-settings`.

## Poza zakresem

Tabela rollout · CI/CD · sekrety · Auth0

## Ustalenia

- Token: `tenant_rollout` (GLOSSARY, UI).
- OpenFGA: istniejące uprawnienia organization_settings.
- RLS: bez nowej tabeli.
- Job: `/rollout`. `SHIPPED_CHARGE_ROUTES["50.0"]` = `/rollout`. OPS_JOBS +1 → 47.
- `just dup` ≤ 3%.
- LLM nie liczy. `charge` zostaje prawdą o marży.

## Schemat

Brak. 50.0 nie dodaje tabeli.

## Kryteria akceptacji

- [ ] `/rollout` ma `data-tenant-rollout="board"` i woła `rolloutSettings`
- [ ] Filtr `setting_key === "default_currency"`; brak upsert na ekranie
- [ ] Brak nowej migracji
- [ ] `SHIPPED_CHARGE_ROUTES["50.0"]` = `/rollout`
- [ ] `just dup` ≤ 3%

## Pętla post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | — | po kodzie |
| Szybkość | N/A ten plaster | odczyt lokalnej listy |
| Dług w diffie | leftover | tabela rollout / CI |
| Docs/OS | — | po zamknięciu |

## Knowledge

Nie RAG. Nie pipeline wdrożenia.

## Pytania (pustych nie ma — `/noc` wybiera)

Brak. Tabela rollout leftover.
