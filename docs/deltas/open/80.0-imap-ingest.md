# Plaster 80.0 · S17 ingest IMAP na inbound_message (M-32)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S17  
**Zależy od:** 64.0 / 78.0 `inbound_message` · 79.0 outbox · 0.3 RLS

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID M-32. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ten sam żywy ID **M-32**, ta sama tabela. Ingest IMAP to zapis **już pobranej** wiadomości: `source_ref` zaczyna się od `imap://`, `external_id` (UID / Message-ID) ten sam unikat per tenant co Graph. Drugi POST z tym samym `external_id` zwraca istniejący wiersz. Status `draft`. OpenFGA: `can_manage_inbound_messages`. Ekran `/mail`: formularz ingest IMAP (nie live skrzynka). Serwis nie woła IMAP/EmailEngine — w pliku serwisu nie ma słowa `imap` (kontrakt 64.0); walidacja prefiksu siedzi w domenie. API poczty składa outbox `inbound_message_saved` jak przy Graph. Sekret skrzynki nie wchodzi do bazy (HC: brak kasy klucza tenanta = leftover).

**Łowca:** ISTNIEJE `external_id` + `graph://` (78.0). ISTNIEJE ingest-graph + outbox przy zapisie (79.0). BRAK `imap://` / EmailEngine. PODOBNE: ingest Graph — ten plaster to drugi prefiks, nie drugi silnik.

**Odrzucone:**

- Live IMAP / EmailEngine HTTP (teatr bez umowy; sekret leftover).
- Zmiana Graph / drugi `external_id`.
- Send (S18).
- Nowa tabela / nowy żywy ID.
- F9.1 · Auth0 · portale.

## Zakres

1. Migracja `040_inbound_imap_ingest`: CHECK `source_ref` dopuszcza `imap://` obok fixture/synth/graph.
2. `POST /inbound-messages/ingest-imap`: body + `external_id` + `imap://`. Ten sam `external_id` = ten sam wiersz. Outbox jak przy Graph.
3. UI `/mail`: formularz ingest IMAP. Nie licz w JS. Nie live skrzynka.

## Poza zakresem

Live IMAP · EmailEngine · sekret · send · F9.1

## Ustalenia

- Żywy ID: **M-32**. Token: `inbound_message`.
- HITL extract bez zmian. LLM nie liczy.
- How-to: `docs/operator/katalogi.md`.
- Sekret IMAP / EmailEngine = leftover (brak szyfrowania kluczem tenanta).

## Schemat

CHECK `source_ref ~ '^(fixture|synth|graph|imap)://'`. `external_id` bez zmian.

## Kryteria akceptacji

- [ ] `imap://` przechodzi; live host/http nie
- [ ] Dwa ingest z tym samym `external_id` = jeden wiersz
- [ ] Tenant B nie widzi A
- [ ] Serwis inbound bez `imap` / `httpx` / `emailengine` w pliku
- [ ] Brak sekretu w diffie
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
