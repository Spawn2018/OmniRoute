# Plaster 418.0 · EXP0.6 `named_place_mark` HITL

**Spec źródłowa:** docs/analysis/karty-pol-fala-exp.md EXP0.6; GLOSSARY `named_place`
**Zależy od:** 137.0 (quotation.named_place — osobny byt, nie rozszerzamy tu)

## Zakres

HITL katalog `named_place_mark` per tenant: `mark_code` + `named_place` (tekst miejsca) +
`terms_version` (`2020`|`2010`) + `source_ref` (`tenant:manual` | `fixture://named-place-mark/`).
Migracja RLS FORCE. UI `/named-place-marks`. OpenFGA `can_manage_named_place_marks`.

**Wybrane:** węższy HITL katalog znacznika miejsca nazwanego + wersji Incoterms jako dane.
**Odrzucone:** cytat ICC w blobie; mutacja `quotation.named_place` / `incoterms_version`;
egzekucja 409 DAP (już na wycenie 137.0); FK do `quotation` / `tender`.
**Dlaczego:** kolejka EXP0.6 = leftover katalog, nie drugi zapis na wycenie.

## Poza zakresem

cytat ICC · 409 DAP na shipment · live ICC API · kwota / marża · extract LLM

## Ustalenia

Łowca: ISTNIEJE `quotation.named_place` + `incoterms_version` (137.0). ISTNIEJE przeniesienie
`field_carry_forward` klucza `named_place`. BRAK tabeli `named_place_mark`. Nie duplikat.

## Kryteria akceptacji

- [ ] POST z kodem, named_place, terms_version, source_ref
- [ ] zły kod / wersja / source_ref → 400; amount / icc_quote → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] UI lista + formularz HITL; proza operatora 5–15 zdań
