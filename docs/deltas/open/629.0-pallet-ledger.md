# Plaster 629.0 · D7c ledger ruchu palet HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D7 · [161.0](../archived/161.0-pallet-balance.md) · [623.0](../archived/623.0-pallet-balance-epal.md)
**Zależy od:** 161.0 `pallet_balance` · 5.0 `party` · 623.0 `epal` na saldzie
**Status:** Delta zaakceptowana `/noc 7` 2026-09-22 — wolno `/plaster`. Nie giełda live.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D7c „ledger ujemny / ruch”):** saldo `pallet_balance` zostaje nieujemnym snapshotem. Giełda D7b zostaje park live. Żywy plaster: **nowa tabela** `pallet_ledger` per tenant — append-only wpis ruchu. Wiersz = `movement_code` (snake 1–64) + `pallet_kind` (`chep`|`lpr`|`epal`) + FK złożone do `party` + `delta_count` (integer **ze znakiem**, ujemny dozwolony) + `source_ref` (`tenant:manual` albo `fixture://pallet-ledger/…`). Operator rejestruje wydanie / zwrot / korektę jako daną; serwis **nie** UPDATE `pallet_balance.unit_count` i nie liczy bieżącego salda z sumy. Zły rodzaj → 400 (`rodzaj`). `delta_count` float / bool / string → 400 (`sztuk`). Duplikat `movement_code` w tenancie → 409. GET listy SQL. API składa odczyt `party`; serwis ledgeru nie importuje `pallet_balances` / parties / charges / shipments. UI `/pallet-ledgers`. `SHIPPED_CHARGE_ROUTES["629.0"]` = `/pallet-ledgers`. OpenFGA: `can_manage_pallet_ledgers`. Migracja `501_pallet_ledger` po `500_shipment_fx_anchor_dates`. Marża zostaje na `charge`. Zero float. Zero HTTP giełdy.

**REQUIREMENT:** leftover 161.0 / 623.0 „Ruch podpisany / ujemne saldo / ledger (D7c)”. Źródło: `docs/deltas/archived/161.0-pallet-balance.md`; PLAN wiersz D7; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `pallet_balance` — snapshot ≥ 0, nie ruch. ISTNIEJE `pallet_pool_mark` — znacznik puli, nie sztuki. ISTNIEJE `benefit_ledger` / `suggestion_ledger` — inne BC. BRAK `pallet_ledger`. BRAK `/pallet-ledgers`. BRAK podpisanego `delta_count` na paletach.

**Odrzucone:**

- giełda Chep/LPR/EPAL i live HTTP (D7b)
- auto-przeliczenie / UPDATE `pallet_balance` z sumy ledgeru
- kwota / Decimal / depozyt na `charge`
- ujemne `unit_count` na istniejącym saldzie
- WMS / mapa / FK do `shipment`

## Zakres

1. Migracja `501_pallet_ledger`: RLS, izolacja, unique `(organization_id, movement_code)`, indeks `(organization_id, party_id)`.
2. POST/GET. Zły rodzaj / sztuki / `source_ref` → 400. Duplikat kodu → 409.
3. UI `/pallet-ledgers` + zdanie w `docs/operator/palety.md`.

## Poza zakresem

D7b giełda live · synchro salda z ledgerem · kwota · WMS · mapa · float

## Plan plików

- `backend/alembic/versions/501_pallet_ledger.py`
- `backend/app/models/pallet_ledger.py`
- `backend/app/domain/pallet_ledger.py`
- `backend/app/repositories/pallet_ledgers/`
- `backend/app/services/pallet_ledgers/` (+ AGENTS.md)
- `backend/app/api/pallet_ledgers.py`
- OpenFGA model + testy izolacji / HTTP / domeny
- `frontend/src/features/pallet-ledger/`
- `frontend/src/features/ops/ops-index.ts` + `routes-breadth.test.ts`
- `docs/operator/palety.md` · GLOSSARY · CURRENT · PLAN · VISION · PROGRESS

## Kryteria akceptacji

- [ ] POST z ujemnym `delta_count` zapisuje wiersz; saldo `pallet_balance` bez zmian
- [ ] rodzaj spoza `chep`|`lpr`|`epal` → 400 (`rodzaj`); float sztuk → 400 (`sztuk`); duplikat kodu → 409
- [ ] serwis nie importuje `pallet_balances` / `charges` i nie woła HTTP
- [ ] RLS: tenant B nie widzi ruchu tenanta A; zero float / marży / giełdy

## Leftover

D7b giełda park live · synchro salda z ledgerem (osobna decyzja) · D9c 409 / D9d/e/f · matching T5 · FK kontekstu · T8 live · fx×FV
