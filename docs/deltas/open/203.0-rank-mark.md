# Plaster 203.0 · W5 `rank_mark` HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala W · [karty-pol-fala-w.md](../../analysis/karty-pol-fala-w.md) § W5
**Zależy od:** 0.3 tenancy (katalog per tenant) · 9.0 sieci / `carrier_inquiry` (ranking answered zostaje; ten plaster nie auto-award)
**Status:** Otwarty `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie N szkiców z rankingu.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover W5 ranking SQL + N szkiców + leftover V7 tacho + V8 what-if + leftover V6 EBITDA + leftover V5 GPS + V4 AIS + V2b myto + C3–C9 + F2b–F4 + G2.20):** leftover V7 tacho TO_VERIFY parked. leftover V8 what-if parked. leftover V6 silnik EBITDA / `sla_clause` parked. leftover V5 `position_event` / ciphertext / 3 dni U4 parked. leftover V5b parked. leftover V4 AIS parked. leftover V2b myto parked. leftover C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. Ranking SQL i N szkiców `mail_draft` z Top N zostają leftover. Żywy plaster: jedna tabela `rank_mark` per tenant — **HITL znacznik osi rankingu zakupu jako dane**, nie auto-award i nie paczka szkiców. Wiersz = `rank_kind` (`price` | `transit` | `reliability` | `carbon` | `other`) + `source_ref` (`tenant:manual` albo `fixture://rank-mark/…`). Operator wpisuje oś rankingu; serwis nie woła LLM, nie czyta `network_member` / `mail_draft` / `charge`, nie sortuje ofert i nie woła HTTP. Zły rodzaj → 400 (`ranking`). Obce `source_ref` → 400 (`obce`). Unique `(organization_id, source_ref)`. GET listy SQL. UI `/rank-marks` bez `<Money/>` i bez `sum()` w JS. `SHIPPED_CHARGE_ROUTES["203.0"]` = `/rank-marks`. OpenFGA: `can_manage_rank_marks`. Migracja `143_rank_mark`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `/networks` Top N z `answered` + „Zapisz szkice” — **nie** katalog osi. ISTNIEJE `executive_mark` — pytanie zarządu, **nie** ranking zakupu. ISTNIEJE `carrier_inquiry` — ślad zapytania, **nie** ranking. BRAK tabeli `rank_mark`. BRAK auto-award. BRAK generatora N szkiców z osi.

**Odrzucone:**

- Ranking SQL / Top N / N szkiców `mail_draft` z rankingu.
- Auto-award / przetarg G2.
- Suma LLM / EBITDA / RAG / N8.
- Live GPS / AIS / myto → `charge` / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409.
- Kwota / druga marża / T-SQL / scrape / float.

## Zakres

1. Migracja `143_rank_mark` (po `142_executive_mark`): RLS FORCE, izolacja, unique `(organization_id, source_ref)`, CHECK allowlist kind.
2. POST/GET. Zły rodzaj → 400. Serwis nie importuje networks / mail_drafts / charges / extraction i nie woła HTTP.
3. UI `/rank-marks`. `just api-types`. How-to `docs/operator/rank.md`; fold w `katalogi.md` (40 linii, bez nowej linii). Nazwy: `RankDesk` / `rankWrite` / `persistRankMark`. Formularz radio (nie `<select>` klonów HITL).

## Poza zakresem

ranking SQL · N szkiców · auto-award · suma LLM · RAG · N8 · live GPS · AIS · myto → charge · kwota · T-SQL

## Kryteria akceptacji

- [ ] POST zapisuje `rank_mark` z `rank_kind`, bez HTTP
- [ ] zły `rank_kind` → 400 (`ranking`)
- [ ] obce `source_ref` → 400 (`obce`)
- [ ] GET zwraca wiersze tenanta
- [ ] Test izolacji tabeli
- [ ] Zero float / marży / auto-award / live HTTP / scrape
