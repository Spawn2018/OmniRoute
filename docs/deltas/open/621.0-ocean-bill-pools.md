# Plaster 621.0 · D6c leftover pule HBL/MBL z M-03

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § D6 leftover · [160.0](../archived/160.0-ocean-bill.md) · [619.0](../archived/619.0-air-waybill-pools.md) · [karty-pol-fala-d.md](../../analysis/karty-pol-fala-d.md) § D6
**Zależy od:** 160.0 `ocean_bill` · 619.0 wzorzec prefiksu M-03 + SQL `MAX`+1
**Status:** Otwarty — delta zaakceptowana w `/noc` 2026-09-22. Wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover D6c pule M-03):** znacznik HBL/MBL już jest (`bill_no` + `bill_kind`). Konsolidacja D6b i PDF zostają. Żywy plaster: **pule numerów w M-03** — klucze `hbl_number_prefix` i `mbl_number_prefix` na allowliście `organization_setting` (ten sam normalizator co `quotation_number_prefix` / HAWB). `bill_no` staje się **nullable** (stare wiersze z numerem zostają). Operator klika „Nadaj HBL” / „Nadaj MBL” na konosamencie z pustym `bill_no` i zgodnym `bill_kind`: API składa — odczyt prefiksu z `OrganizationSettingService`, serwis `ocean_bills` dostaje sam string i zapisuje numer. Kolejny numer liczy **Postgres** (`MAX` ogona cyfr + 1 per kind), nie pętla w Pythonie. Format: znormalizowany prefiks + cztery cyfry. Drugie nadanie bez zmiany. Ręczne wklejenie `bill_no` przy POST (160.0) zostaje. Brak prefiksu → 400 (`prefiks`). Niezgodny `bill_kind` albo już wypełniony numer → 400. Unique `(organization_id, bill_no)` gdy numer jest. UI `/ocean-bills`. `SHIPPED_CHARGE_ROUTES["621.0"]` = `/ocean-bills`. OpenFGA bez nowej relacji (`can_manage_ocean_bills`). Serwis `ocean_bills` nie importuje `organization_settings`. Marża zostaje na `charge`. Zero float. Zero HTTP armatora.

**REQUIREMENT:** karta D6 — „pule numerów (M-03 szablon)”. Źródło: `docs/analysis/karty-pol-fala-d.md` § D6; leftover z 160.0.

**Łowca:** ISTNIEJE `ocean_bill.bill_no` / `bill_kind`. ISTNIEJE `hawb_number_prefix` + SQL nadania (619.0). ISTNIEJE allowlista M-03. BRAK `hbl_number_prefix` / `mbl_number_prefix`. BRAK endpointu nadania na konosamencie. BRAK D6b konsolidacji.

**Odrzucone:**

- D6b konsolidacja wielu house pod jeden MBL
- PDF / bajty / booking armatorski / HZ/S21
- osobna tabela puli / generator spoza M-03
- import `organization_settings` w serwisie ocean_bills · float · druga marża

## Zakres

1. Dwa klucze w allowliście M-03 + `bill_no` nullable + unique gdy nie NULL.
2. SQL nadania HBL/MBL na `ocean_bill` (tylko gdy `bill_no` NULL i kind zgadza się).
3. POST compose w API: ustawienie → string → serwis. UI `/ocean-bills` + zdanie operatora.

## Poza zakresem

D6b konsolidacja · PDF · live booking · float · cyfra kontrolna

## Kryteria akceptacji

- [ ] brak prefiksu → 400 (`prefiks`); z prefiksem nadaje `PREFIX`+4 cyfry; drugie kliknięcie bez zmiany
- [ ] ręczne POST z 160.0 nadal działa; unique per tenant gdy numer; izolacja
- [ ] serwis `ocean_bills` nie importuje `organization_settings`; Postgres liczy kolejny numer
- [ ] zero float / marży / HTTP
