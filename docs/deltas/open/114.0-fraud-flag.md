# Plaster 114.0 · S52 oszustwo (M-54)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S52  
**Zależy od:** 5.0 `party` · 0.3 RLS · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-04. Żywy ID **M-54**. Token `fraud_flag`. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** nowa tabela `fraud_flag` per tenant na **kontrahencie**. Operator na `/fraud` zapisuje flagę (`billing` / `document` / `other`) z `source_ref`. Wiele flag na jedną `party`. API składa: odczyt `party`, zapis flagi. Serwis nie importuje `parties` / `charges` / `quotations`. Żywa nazwa: `fraud_flag` (nie scoring, nie JWT claim, nie `cargo_claim`). `SHIPPED_CHARGE_ROUTES["114.0"]` = `/fraud`. OPS_JOBS 51 → 52.

**Łowca:** ISTNIEJE 89.0 `party.sanctions_list_ref` (sprawdzenie listy, nie oszustwo). ISTNIEJE 113.0 `cargo_claim` (reklamacja ładunku na zleceniu). ISTNIEJE 10.0 `party_scorecard` / 14.0 `credit_review` (nie scoring osoby). BRAK tabeli oszustwa / fraud flag. PODOBNE: 113.0 zapis na rodzicu + 89.0 flaga na `party`. Dwa joby: `/sanctions` vs `/fraud`.

**Odrzucone:**

- Scoring osoby / JDG / auto-limit.
- Kwota / marża / float na wierszu.
- Kolumny na `party` zamiast własnego BC (M-54 ≠ M-10).
- FK do `sales_invoice` / `bank_payment` (sąsiad kwoty).
- Auto-INSERT z sankcji albo z reklamacji.
- Klasyfikacja przez LLM.
- Live HTTP list / auto-match.
- Named park (PLAN: Plan → plaster; jest job zapisu).
- F9.1. S53 Auth0.

## Zakres

1. Migracja `070_fraud_flag_rls` (po `069_cargo_claim_rls`): tabela `fraud_flag` RLS ENABLE+FORCE; FK tenanta do `party`.
2. `GET/POST /fraud-flags` — OpenFGA `can_manage_fraud_flags` = member. POST: `{party_id, flag_kind, source_ref}`. Nieznany kontrahent → 404.
3. UI `/fraud`: lista + zapis. `data-fraud-flag="board"`. `SHIPPED_CHARGE_ROUTES["114.0"]` = `/fraud`. OPS_JOBS = 52.
4. Test izolacji. How-to `docs/operator/oszustwo.md`. `katalogi.md` ≤ 40 linii (wtopić w sankcje).

## Poza zakresem

S53 Auth0 · scoring osoby · kwota · live lista · LLM · F9.1

## Ustalenia

- Token: `fraud_flag` (tabela). How-to: `oszustwo.md`.
- `flag_kind` CHECK: `billing` | `document` | `other`.
- `source_ref`: `fixture://fraud-flag/` albo `tenant:manual`. Max 256.
- Serwis bez `httpx`. ExtractionService nie importuje flag.
- LLM nie klasyfikuje. `charge` zostaje prawdą o marży. HITL bez zmian.

## Schemat

`fraud_flag` (nowa): `id`, `organization_id`, `party_id`, `flag_kind`, `source_ref`, timestamps, `created_by`. FK tenanta do `party`. Indeks org+party.

## Kryteria akceptacji

- [ ] Tabela z `organization_id`, RLS FORCE, test izolacji
- [ ] Brak `amount` / `risk_score` / leaflet
- [ ] Zły kind / puste źródło → 400; brak uprawnienia → 403
- [ ] `/fraud` zapisuje i listuje; OPS_JOBS = 52; `katalogi.md` ≤ 40

## Pytania

Brak. `/noc` akceptuje deltę.
