# Plaster 331.0 · G11 `bonded_mark` HITL (katalog, bez WMS)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G11 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie WMS. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G11 — tylko HITL katalog `bonded_mark` jako dane. `mark_code` + `bond_kind` (`bonded` | `recognized` | `other`) + `source_ref`. UI `/bonded-marks`. OpenFGA `can_manage_bonded_marks`. Migracja `243_bonded_mark`. Job OPS. Deny-list extract. Procedura bonded live / WMS e-com / dock yard = leftover.

**Łowca:** BRAK tabeli `bonded_mark`. ISTNIEJE `dock_appointment` — awizacja, nie bonded. ISTNIEJE `monitoring_scheme` — schemat, nie miejsce uznane.

**Odrzucone:** WMS e-com · procedura bonded live · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `243_bonded_mark` RLS FORCE.
2. POST/GET `/bonded-marks`. OpenFGA. Deny-list.
3. UI `/bonded-marks`. How-to. `SHIPPED_CHARGE_ROUTES["331.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, bond_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/wms → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
