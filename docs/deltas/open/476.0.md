# Plaster 476.0 · BR1.2 zapas jako obiekt finansowy — katalog HITL

**Status:** Plan zaakceptowany `/noc 20` 2026-09-14. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 1 · § A.4 Trade-Tech · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR1.2

**Zależy od:** tenancy RLS · wzorzec HITL `rfid_mark` (475.0) · `inventory_position_mark` (EXP3.3, pozycja WMS) · `po_financing_mark` (472.0, stance PO, nie wycena zapasu)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `inventory_finance_marks`. Tabela `inventory_finance_mark`: `mark_code` + `finance_kind` `valuation|aging|release|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q po BR1.1 — katalog HITL stancji zapasu jako obiektu finansowego (VISION: wycena, wiekowanie, Inventory Release), nie silnik wyceny SQL i nie live zastaw.
- **Wybrane:** POST/GET `/api/v1/inventory-finance-marks`. OpenFGA `can_manage_inventory_finance_marks` (member). UI `/inventory-finance-marks`. `SHIPPED["476.0"]` → `/inventory-finance-marks`.
- **Wybrane:** Alembic `381_inventory_finance_mark` (`down_revision = "380_rfid_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** rozszerzenie `inventory_position_mark` (`REJECTED` — EXP3.3 to znacznik pozycji plant/sku, nie obiekt finansowy).
- **Odrzucone:** rozszerzenie `working_capital_mark` (`REJECTED` — EXP2.1 to DSO/cash_at_risk/aging należności, nie wiekowanie zapasu).
- **Odrzucone:** rozszerzenie `po_financing_mark` (`REJECTED` — 472.0 to stance finansowania PO; Inventory Release tu jest rodzajem finansowania zamówienia, nie katalogiem zapasu).
- **Odrzucone:** wycena / wiekowanie SQL · amount / Decimal jako wartość zapasu · FK do `purchase_order` / `inventory_position_mark` · live SMEO / zastaw HTTP (`REJECTED` — CURRENT park wycena zapasu; leftover Trade-Tech).
- **Odrzucone:** kwota / marża / float / UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `inventory_position_mark` (stock_kind position/plant/sku). ISTNIEJE `working_capital_mark` (capital_kind dso/aging AR). ISTNIEJE `po_financing_mark` (financing_kind po/release/advance). ISTNIEJE `wms_flow_mark` / `rfid_mark` (Grupa 1 HITL). PODOBNE katalog HITL stance per tenant. BRAK `inventory_finance_mark`. BRAK katalogu wycena/wiekowanie/release zapasu.

## Zakres

How-to: jedno zdanie — to katalog stancji zapasu jako obiektu finansowego Trade-Tech; wycena SQL i live zastaw zostają leftover. +1 OPS_JOB. Klient FE: `finance_kind` jako segmented control (jak 475.0 `rfid_kind`).

## Poza zakresem

wycena zapasu SQL · wiekowanie SQL · amount wartości · FK PO / position · live zastaw / SMEO · qty · WMS live · sekrety · AI5

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

wycena / wiekowanie SQL · wartość Decimal · FK `inventory_position_mark` · klej do `po_financing_mark` · live zastaw Trade-Tech · BR5.1 FK PO
