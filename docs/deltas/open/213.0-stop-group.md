# Plaster 213.0 · T1 `stop_group_code` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover · [150.0](../archived/150.0-stop.md) leftover `stop_group` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T1
**Zależy od:** 150.0 `stop` + 194.0 ETA już jest
**Status:** Otwarty `/noc` 2026-09-09. Nie nowa tabela. Nie mapa. Nie EXP1 waga/plomba. Nie `trip_id`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T1 `stop_group` tabela-lub-kolumna + leftover T1 EXP1 waga/plomba + leftover T2c km/`party`/`/fleet` + leftover T7b na `charge` + leftover T5 outbox + S53):** leftover T1 EXP1 waga/ilość/opakowanie/plomba/awizacja parked. leftover T2c km parked. leftover T7b na `charge` parked. leftover T5 outbox parked. S53 Auth0 parked. Karta T1: `stop_group` = grupowanie stopów wykonywanych razem — **kolumna, nie tabela**. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `stop_group_code` (token HITL 2–32 `[A-Za-z0-9_-]`), ten sam kod na wielu punktach tego zlecenia = grupa. POST bez pola zostaje `null`. Zły token → 400 (`grupa`). Brak unique (unique zablokowałby grupę). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["213.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `152_stop_group`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop` — punkt ZA/WY + IANA + dwa ETA, **nie** grupa. ISTNIEJE `shipment_leg` — odcinek trasy, **nie** grupa stopów. ISTNIEJE `trip` — przejazd, **nie** stop_group. BRAK `stop_group` tabeli. BRAK `stop_group_code`. BRAK `trip_id` na `stop`.

**Odrzucone:**

- Nowa tabela `stop_group` + członkostwo (leftover T1b).
- EXP1 waga / ilość / opakowanie / plomba / awizacja / waiting / POD (leftover T1 EXP1).
- `trip_id` na `stop` (import trips w BC stop).
- T2c km · T7b `charge` · T5 outbox · mapa · GPS · S53 Auth0.

## Zakres

1. Migracja `152_stop_group` (po `151_trip_driver2`): `stop_group_code` nullable, CHECK regex albo NULL.
2. POST opcjonalny `stop_group_code`. Zły token → 400. Serwis nie importuje trips/charges i nie woła HTTP.
3. UI `/shipments` panel punktu. Fold w how-to zlecenia. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

tabela `stop_group` · EXP1 · `trip_id` · km · mapa · T7b · T5 · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST z `stop_group_code` zapisuje token, bez nowej tabeli
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`grupa`)
- [ ] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / wagi / `/fleet` / live HTTP / scrape
