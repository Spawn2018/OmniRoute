# Plaster 210.0 · T4 `parent_shipment_id` na `shipment`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T4 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T4
**Zależy od:** 90.0 `shipment` już jest · 204.0 `shipment_ref` (wzorzec kolumny HITL; ten plaster nie rusza numeru)
**Status:** Otwarty `/noc` 2026-09-09. Nie rentowność SQL. Nie `charge`. Nie N1 `consignment`.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T4 `parent_shipment_id` + leftover T4b rentowność SQL + leftover U3c pule + leftover P4c warning + leftover P4b armator/serwis FK + leftover N1 `consignment` + S53 Auth0):** leftover T4b widok SQL marży główne+podzlecenia parked. leftover U3c pule M-03 parked. leftover P4c warning-jako-fakt parked. leftover P4b armator/serwis FK parked. leftover N1 `consignment` parked. S53 Auth0 parked. Żywy plaster: **pogłębienie istniejącej** `shipment` — opcjonalne `parent_shipment_id` (FK tenanta na to samo `shipment`) + `relation_kind` jako dana (`drayage` / `oncarriage` / `leg_subcontract` / `other`), nie silnik rentowności i nie druga tabela przesyłki. POST bez pól zostaje `null`/`null`. Rodzic bez rodzaju → 400 (`rodzaj`). Rodzaj bez rodzica → 400 (`główne`). Zły token rodzaju → 400 (`rodzaj`). Wskazanie siebie → 400 (`główne`). UI `/shipments`. `SHIPPED_CHARGE_ROUTES["210.0"]` = `/shipments`. OpenFGA bez nowej relacji (`can_manage_shipments`). Migracja `150_shipment_parent`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `shipment` — zlecenie z wyceny; `shipment_ref` to twardy numer, **nie** rodzic. ISTNIEJE `shipment_leg` — odcinek trasy, **nie** podzlecenie. ISTNIEJE `collective_invoice` — dodatkowe zlecenie na fakturze, **nie** drzewo. ISTNIEJE słownik `consignment` (N1) — **nie** zbudowane. ISTNIEJE `ocean-bill` pole UI `consignmentToken` = alias `shipment_id`. BRAK `parent_shipment_id`. BRAK `relation_kind` na `shipment`. BRAK widoku SQL marży główne+dzieci.

**Odrzucone:**

- Rentowność SQL / widok na `charge` (leftover T4b).
- N1 tabela `consignment`.
- Cykl wielopoziomowy / PATCH rodzica / UPDATE drzewa.
- Druga marża / float / T-SQL parser.
- P4c warning · P4b armator FK · U3c pule · S53 Auth0.
- Import charges / quotations poza istniejącym składem API 90.0.

## Zakres

1. Migracja `150_shipment_parent` (po `149_shipment_leg_air_waybill`): `parent_shipment_id` nullable, `relation_kind` nullable, FK `(organization_id, parent_shipment_id)` → `uq_shipment_org_id`, CHECK para NULL albo oba ustawione + allowlista kind + `parent_shipment_id <> id`.
2. POST opcjonalny rodzic + rodzaj. Zły zestaw → 400. Serwis nie importuje charges i nie woła HTTP.
3. UI `/shipments`. How-to `zlecenie.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

T4b SQL na `charge` · N1 consignment · cykl drzewa · PATCH · pule · P4c · Auth0 · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST z `parent_shipment_id` + `relation_kind` zapisuje FK tenanta, bez `charge`
- [ ] POST bez pól zostaje `null` / `null`
- [ ] rodzic bez rodzaju → 400 (`rodzaj`); rodzaj bez rodzica → 400 (`główne`); self → 400 (`główne`)
- [ ] FK tego samego tenanta; izolacja (rodzic obcego tenanta niewidoczny)
- [ ] Zero float / marży / widoku SQL na `charge` / N1 / live HTTP / scrape
