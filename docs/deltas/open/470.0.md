# Plaster 470.0 · BR4.4 Jedwabny Szlak (korytarz Chiny–Europa)

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § C.3 Grupa 4 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR4.4 · GLOSSARY `china_rail` · G2.19 `lane_pattern` · G2.21 `lane_km`

**Zależy od:** tenancy RLS · wzorzec HITL `nac_mark` (469.0) · 110.0 `shipment_leg` china_rail już w kodzie

## Wybrane / odrzucone / dlaczego

- **Wybrane:** nowy BC `silk_corridor_marks`. Tabela `silk_corridor_mark`: `mark_code` + `corridor_kind` `silk|block_train|transit|other` + `source_ref`. Append-only INSERT. RLS + test izolacji. Węższa opcja z kolejki Q — stance korytarza Jedwabnego Szlaku / kolei Chiny–Europa jako katalog HITL, nie para UN/LOCODE i nie live CR Express HTTP.
- **Wybrane:** POST/GET `/api/v1/silk-corridor-marks`. OpenFGA `can_manage_silk_corridor_marks` (member). UI `/silk-corridor-marks`. `SHIPPED["470.0"]` → `/silk-corridor-marks`.
- **Wybrane:** Alembic `375_silk_corridor_mark` (`down_revision = "374_nac_mark"`). Id ≤ 32 znaków.
- **Odrzucone:** nowa tabela korytarza z `origin_unlocode`/`destination_unlocode` (`REJECTED` — G2.19 `lane_pattern` już jest).
- **Odrzucone:** kolumna / FK na `shipment_leg` / `china_rail` (`REJECTED` — 110.0 odcinek zostaje, nie stance korytarza).
- **Odrzucone:** klej FK do `lane_km` / `sales_lane` / `combined_transport_mark` / `rail_cim_mark` (`REJECTED` na tym wierszu).
- **Odrzucone:** live CR Express / China Railway HTTP · km / Haversine · kwota / marża / float.
- **Odrzucone:** UPDATE/DELETE · ENUM poza CHECK kind.

**Łowca:** ISTNIEJE `shipment_leg` `china_rail` (110.0 odcinek). ISTNIEJE `lane_pattern` (para UN/LOCODE). ISTNIEJE `lane_km` (km). ISTNIEJE `sales_lane` (repeat/spot sprzedaż). ISTNIEJE `combined_transport_mark` (combined/mobility/piggyback UE). ISTNIEJE `rail_uic_mark` / `rail_cim_mark` (UIC/CIM filing). PODOBNE katalog HITL stance. BRAK `silk_corridor_mark`. BRAK katalogu Jedwabnego Szlaku.

## Zakres

How-to: jedno zdanie — to katalog stance korytarza Chiny–Europa; para miejsc, km i live kolej zostają leftover. +1 OPS_JOB. Klient FE od razu rozróżniony (kind jako button toggle group, nie radio 469 ani select 468) pod jscpd < 3%.

## Poza zakresem

UN/LOCODE para · km · live CR Express · FK do `shipment_leg` · `lane_pattern` · AI5 · BR5.0 faktoring

## Kryteria akceptacji

- [ ] migracja `varchar(32)` + FORCE RLS + test izolacji
- [ ] POST zły kind/kod → 400; amount → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

para UN/LOCODE · km · live CR Express / China Railway HTTP · FK do `shipment_leg` / `lane_km` · BR5.0 faktoring
