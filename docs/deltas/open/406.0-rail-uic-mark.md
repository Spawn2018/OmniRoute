# Plaster 406.0 · EXP4.2 `rail_uic_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `rail_uic_mark` per tenant: `mark_code` + `rail_kind`
`uic`|`cim`|`smgs`|`other` + `source_ref` (`tenant:manual` | `fixture://rail-uic-mark/…`).

Klon wzorca `e_doreczenia_mark`:
- migracja `316` (revises `315_e_doreczenia_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_rail_uic_marks`
- UI `/rail-uic-marks`, business-lists + ops-index `SHIPPED["406.0"]`
- operator `docs/operator/rail-uic-mark.md`
- izolacja RLS; POST `amount`/`km`/`score` → 422

Znacznik UIC/CIM/SMGS jako dana. Bez live rail API.

## Poza zakresem

live UIC/CIM/SMGS · km · kwota · mapa · wagon live

## Kryteria

- [ ] POST z kodem, rail_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/km → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live rail API · km · wagon tracking
