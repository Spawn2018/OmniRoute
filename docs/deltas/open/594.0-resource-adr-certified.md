# Plaster 594.0 · T2 leftover — HITL `resource.adr_certified`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T2 · [593.0](../archived/593.0-resource-inventory-no.md) · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T2 (`adr_certified`)

**Zależy od:** 151.0 `resource`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Nie `reefer`. Nie `tail_lift`. Nie HW. Nie UXCL. Nie Job#2. Nie N3.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover po `inventory_no`. Opcjonalny `adr_certified` na `resource` (bool HITL, nie float). POST bez pola → `null`. `true` / `false` zapisuje. Inny typ → 400. Zmiana flagi przy tej samej parze kind×nazwa = nowy wiersz + `superseded_by` (razem z nr rej. / inwentarz / kg / LDM / m³). Nie matching ładunku ADR. Nie 409.
- **Wybrane:** UI `/fleet`. `SHIPPED["594.0"]` → `/fleet`. OpenFGA bez nowej relacji. Migracja `470_resource_adr_certified` (po `469_resource_inventory_no`). Bez nowej tabeli.
- **Odrzucone:** `reefer` / `tail_lift` (`REJECTED` w tym plasterze). telefon / karta kierowcy (`REJECTED`). HW (`REJECTED`). UXCL / Job#2 / N3 / AI3 / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `resource.adr_certified`. ISTNIEJE `container.reefer` (inny BC, nie reuse). PODOBNE: opcjonalna flaga + supersede.

## Zakres

1. Migracja `470_resource_adr_certified`.
2. Domain `require_adr_certified` + serwis + API + GET.
3. UI `/fleet` flaga ADR. How-to. `SHIPPED["594.0"]`.

## Poza zakresem

`reefer` · `tail_lift` · telefon · karta kierowcy · matching ADR · HW · UXCL · Job#2 · N3 · kwota

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `adr_certified=true` zapisuje bool
- [ ] zły typ → 400
- [ ] zmiana flagi → supersede
- [ ] izolacja RLS (tabela `resource`)
- [ ] zero float / import charges

## Leftover

`reefer` · `tail_lift` · telefon · karta kierowcy · HW · SH-R16-4 UXCL · Job#2 · N3 · AI3 · G0-SH
