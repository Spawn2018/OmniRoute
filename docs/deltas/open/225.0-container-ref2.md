# Plaster 225.0 · T3 `ref_2` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover · [224.0](../archived/224.0-container-ref1.md) leftover `ref_2` · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3
**Zależy od:** 224.0 `ref_1`
**Status:** Otwarty `/noc` 2026-09-09. Nie PIN. Nie VGM. Nie waga. Nie booking.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 `ref_2`/PIN/VGM/booking/waga + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + S53):** leftover T1b parked. leftover T2c km parked. leftover T7b parked. leftover T5 parked. S53 Auth0 parked. leftover T3 PIN parked. leftover T3 VGM parked. leftover `booking_no` parked. leftover `ref_3`…`ref_5` parked. leftover `quantity` / `weight_kg` parked (Decimal). Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `ref_2` (ta sama reguła co `ref_1`: 1–64 znaki po trim), POST bez pola zostaje `null`. Pusty po trim → `null`. Zły token → 400 (`referencja`). `require_container_ref_2` deleguje do `require_container_ref_1`. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["225.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `164_container_ref2`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `ref_1`. ISTNIEJE `source_ref`. BRAK `ref_2`. BRAK PIN. BRAK VGM. BRAK `booking_no`.

**Odrzucone:**

- `ref_3`…`ref_5` w tym plasterze.
- `booking_no` / live HTTP.
- `quantity` / `weight_kg` / VGM / TARA.
- `pin_code` / ciphertext.
- T1b · T2c km · T7b · T5 · S53 Auth0.

## Zakres

1. Migracja `164_container_ref2` (po `163_container_ref1`): `ref_2` nullable `String(64)`.
2. POST opcjonalny `ref_2`. Zły token → 400. Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Bez nowego joba OPS (zostaje 106).

## Poza zakresem

PIN · VGM · waga · `ref_3`…`ref_5` · booking · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `ref_2` zapisuje drugą referencję, bez PIN i bez wagi
- [ ] POST bez pola zostaje `null`
- [ ] zły token → 400 (`referencja`)
- [ ] RLS istniejącego `container`; izolacja
- [ ] Zero float / marży / VGM / PIN / live HTTP / scrape
