# Plaster 323.0 · G1 `crm_lead` HITL (katalog, bez szansy i cold-send)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G1 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie cold auto-send. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G1 — tylko HITL katalog `crm_lead` jako dane. `lead_code` + `stage_kind` (`new` | `qualified` | `disqualified` | `other`) + `source_ref`. UI `/crm-leads`. OpenFGA `can_manage_crm_leads`. Migracja `235_crm_lead`. Job OPS. Deny-list extract. Dedup NIP / szansa / activity = leftover kolejnych plastrów.

**Łowca:** BRAK tabeli `crm_lead` / `crm_opportunity` / CRM w GLOSSARY. ISTNIEJE `party` + M10-1 dedup — inne joby, nie lead.

**Odrzucone:** `crm_opportunity` · `activity` · dedup NIP na lead · klej X7 portal · cold auto-send · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `235_crm_lead` RLS FORCE.
2. POST/GET `/crm-leads`. OpenFGA. Deny-list.
3. UI `/crm-leads`. How-to. `SHIPPED_CHARGE_ROUTES["323.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, stage_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/pipeline → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
