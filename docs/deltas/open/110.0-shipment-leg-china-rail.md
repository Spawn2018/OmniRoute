# Plaster 110.0 · S48 odcinek kolej z Chin (M-50)

**Spec źródłowa:** [docs/spec/china-rail.md](../../spec/china-rail.md) · [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S48  
**Zależy od:** 43.0 `/china-rail` · 109.0 `shipment_leg` rail · 4.0 `port.country_code`

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-50**. Nie F9.1.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama tabela `shipment_leg`. Migracja 067 rozszerza CHECK `leg_kind IN ('road', 'rail', 'china_rail')`. Operator na `/china-rail` **zapisuje** odcinek: `shipment_id` + dwie lokalizacje `unlocode` + `source_ref` + `leg_kind=china_rail`. Port podpięty pod lokalizację musi mieć `rail` w `function_flags` **oraz** `country_code = CN`. Jeden odcinek `china_rail` na zlecenie (unikat org+zlecenie+kind zostaje — `road` / `rail` / `china_rail` mogą współistnieć). API składa: odczyt `shipment`, odczyt dwóch `location`, odczyt `port`, zapis nogi. Serwis nogi nadal nie importuje geography. `SHIPPED_CHARGE_ROUTES["110.0"]` = `/china-rail`. OPS_JOBS długość 50. POST: `leg_kind` opcjonalny, domyślnie `road` — formularze 108.0 i 109.0 bez zmian.

**Łowca:** ISTNIEJE 108.0/109.0 `shipment_leg` + `/shipment-legs` + `can_manage_shipment_legs`. ISTNIEJE 43.0 `/china-rail` + `chinaRailPorts`. ISTNIEJE 4.0 `country_code` + `function_flags`. BRAK `leg_kind=china_rail` w CHECK. PODOBNE: 109.0 druga akcja na istniejącym ekranie. Dwa joby (katalog CN vs odcinek) = `/ports` vs `/china-rail`.

**Odrzucone:**

- Druga tabela korytarza / Belt and Road / rozkład CR.
- Druga mapa / GPS / ETA / HTTP do China Railway.
- `ocean_lcl` — S49.
- Końce poza CN (to zostaje `rail` na `/rail`).
- Kwota / marża.
- Auto-INSERT przy zapisie zlecenia.
- F9.1.

## Zakres

1. Migracja `067_shipment_leg_china_rail_kind` (po `066_shipment_leg_rail_kind`): DROP `ck_shipment_leg_kind`, nowy CHECK `leg_kind IN ('road', 'rail', 'china_rail')`. Bez nowej tabeli.
2. POST `/shipment-legs` przyjmuje `china_rail`. Końce jak `rail` (UN/LOCODE + flaga `rail`) plus oba porty `country_code = CN`. Port PL z flagą `rail` albo strefa pocztowa → 400. Nieznane zlecenie / lokalizacja / port → 404.
3. UI `/china-rail`: lista odcinków `china_rail` + zapis. `data-china-rail="board"` i inwentarz 43.0 zostają. `SHIPPED_CHARGE_ROUTES["110.0"]` = `/china-rail`. OPS_JOBS = 50.
4. Test izolacji nie nowy (tabela ta sama) — asercja CHECK + HTTP `china_rail`. How-to `docs/operator/kolej-chiny.md`.

## Poza zakresem

S49 · korytarz · mapa · GPS · HTTP CR · wagon · CIM · flota · kwota · F9.1

## Ustalenia

- Token: `shipment_leg` (tabela). UI zostaje `china_rail`. How-to: `kolej-chiny.md`.
- `source_ref` bez zmian: `fixture://shipment-leg/` albo `tenant:manual`.
- Serwis nogi bez `httpx` / bez importu rodziców. ExtractionService nie importuje nogi.
- LLM nie liczy trasy. HITL extract bez zmian. `charge` zostaje prawdą o marży.
- Formularz: `data-china-rail="leg-form"`. Nie klon `Input` z `/road`, nie `RAIL_FIELDS` z `/rail`, nie `CatalogCreateForm`.
- OPS_JOBS zostaje długości 50.

## Schemat

Bez nowej tabeli. `shipment_leg.leg_kind`: `road` | `rail` | `china_rail`.

## Kryteria akceptacji

- [ ] CHECK dopuszcza `china_rail`; `road` i `rail` nadal działają
- [ ] Brak `amount` / leaflet; zero nowej tabeli
- [ ] Port bez `CN` albo bez `rail` albo `postal_zone` jako koniec → 400; brak uprawnienia → 403
- [ ] `/china-rail` zapisuje i listuje odcinki `china_rail`; `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
