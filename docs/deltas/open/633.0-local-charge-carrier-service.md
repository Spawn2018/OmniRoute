# Plaster 633.0 · leftover P4b armator/serwis HITL

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 · [208.0](../archived/208.0-local-charge-iso.md) · [166.0](../archived/166.0-local-charge.md)
**Zależy od:** 208.0 `iso_size_type` już jest
**Status:** Delta zaakceptowana `/noc 7` 2026-09-23 — wolno `/plaster`. Nie FK party. Nie warning P4c.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P4b „macierz armator/serwis”):** `port_unlocode` i `iso_size_type` zostają. P4c warning-jako-fakt oraz P5b–c SQL na `charge` zostają park. Żywy plaster: **pogłębienie istniejącej** `local_charge` — opcjonalne etykiety HITL `carrier_label` (1–64, trim, pusty → `null`) i `service_label` (1–64, trim, pusty → `null`). To tekst operatora (armator / serwis liniowy), **nie** FK do `party` / `carrier_profile` i nie live HTTP. POST bez pól zostaje `null`. Nie-tekst / > 64 → 400 (`armator` / `serwis`). Unique per tenant `(charge_kind, port_unlocode, iso_size_type, carrier_label, service_label)` zastępuje unique 208.0 (Postgres NULLS DISTINCT w unique — jak dotychczasowa czwórka). UI `/local-charges`. `SHIPPED_CHARGE_ROUTES["633.0"]` = `/local-charges`. OpenFGA bez nowej relacji. Migracja `505_local_charge_carrier_service` po `504_network_print_gate_mark`. Marża zostaje na `charge`. Zero float. Zero HTTP.

**REQUIREMENT:** leftover 166.0 / 208.0 „macierz armator/serwis” — tu tylko etykiety HITL, nie FK i nie live. Źródło: `docs/deltas/archived/208.0-local-charge-iso.md`; PLAN wiersz P4; `docs/ops/docs-debt.md`.

**Łowca:** ISTNIEJE `local_charge.port_unlocode` / `iso_size_type`. ISTNIEJE `carrier_profile` na `party` — profil, nie wymiar dopłaty. BRAK `carrier_label` / `service_label` na `local_charge`. BRAK FK armator×serwis.

**Odrzucone:**

- FK `party` / `carrier_profile` / SCAC live
- warning braku dopłaty jako fakt (P4c)
- P5b–c SQL wariancji na `charge`
- live HTTP / scrape / float / marża

## Zakres

1. Migracja `505_local_charge_carrier_service`: kolumny nullable + drop unique 208.0 + unique piątki.
2. POST opcjonalny `carrier_label` / `service_label`. Zły token → 400. Serwis nie importuje parties.
3. UI `/local-charges` + zdanie w how-to.

## Poza zakresem

P4c warning · P5b–c · FK party · live HTTP · float

## Plan plików

- `backend/alembic/versions/505_local_charge_carrier_service.py`
- `backend/app/models/local_charge.py`
- `backend/app/domain/local_charge.py`
- serwis / API / testy lokalnego BC
- `frontend/src/features/local-charge/`
- how-to · CURRENT · PLAN · VISION · PROGRESS · docs-debt · GLOSSARY

## Kryteria akceptacji

- [ ] POST z `carrier_label` / `service_label` zapisuje etykiety; bez FK party
- [ ] POST bez pól / pusty trim → `null`
- [ ] nie-tekst / > 64 → 400 (`armator` / `serwis`)
- [ ] unique per tenant na piątce; izolacja; zero float / marży / live HTTP

## Leftover

P4c warning ≠ fakt · P5b–c SQL · FK party · live HTTP
