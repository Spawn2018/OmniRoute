# Plaster 473.0 · BR5.2 Giełdy transportowe (katalog konektora)

**Status:** Otwarta delta `/noc`. Zero kodu do akceptacji.

**Spec źródłowa:** [VISION.md](../../VISION.md) § A.4 Strumień 3 Trade-Tech · § C.3 Grupa 5 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala BR BR5.2 · S55 `exchange_connector` (271.0) · [dostepy-do-zdobycia.md](../../analysis/dostepy-do-zdobycia.md) § 4 P0 giełdy

**Zależy od:** tenancy RLS · wzorzec HITL `exchange_connector` (271.0) · `partner_exchange_mark` (361.0) już w kodzie (bez kleju) · leftover live HTTP / auto-post / X1–X5 SPA parked w 271.0

## Wybrane / odrzucone / dlaczego

- **Wybrane:** rozszerzenie BC `exchange_connectors` — migracja `378_exchange_connector_kinds` (`down_revision = "377_po_financing_mark"`) **ALTER CHECK** `system_kind` na `trans_eu|timocom|teleroute|transporeon|other`. Bez nowej tabeli. Węższa opcja z kolejki Q — dokończenie katalogu konektora P0 giełd po S55, nie wymiana ofert i nie live HTTP.
- **Wybrane:** aktualizacja domeny/API/FE/testów tego samego BC: POST/GET `/api/v1/exchange-connectors` bez zmiany ścieżki. UI `/exchange-connectors`. OpenFGA `can_manage_exchange_connectors` bez nowej relacji. `SHIPPED["473.0"]` → `/exchange-connectors`.
- **Odrzucone:** nowa tabela `transport_exchange_connector` / `offer_exchange_mark` (`REJECTED` — duplikat 271.0 / 361.0; łowca potwierdza istniejące BC).
- **Odrzucone:** live Trans.eu / TIMOCOM / Transporeon / Teleroute HTTP · auto-post frachtu · scrape · OAuth2 (`REJECTED` — leftover 271.0; brak umowy/sekretu).
- **Odrzucone:** kolumny sekretu / ciphertext / `base_url` / `client_secret` (`REJECTED` — HC-09; wzorzec 271.0).
- **Odrzucone:** FK do `quotation` / `network` / `mail_draft` / `carrier_inquiry` · wymiana ofert SQL · kwota / marża / float.
- **Odrzucone:** X1–X5 SPA · konsument outboxa · pięć tablic w jednym commicie poza CHECK kind.
- **Odrzucone:** UPDATE/DELETE wiersza · rozszerzenie `partner_exchange_mark` zamiast konektora (`REJECTED` — EXP2.24 to tryb giełdy partnerskiej, nie nazwana platforma P0).

**Łowca:** ISTNIEJE `exchange_connector` (271.0, kind tylko `trans_eu`). ISTNIEJE `partner_exchange_mark` (361.0, partner|spot|board). ISTNIEJE `network` / `network_member` (WCA, nie giełda). ISTNIEJE `carrier_inquiry` (ślad RFQ kupna). ISTNIEJE `rank_mark` (W5 ranking). PODOBNE katalog konektora HITL jak `factoring_connector` rozszerzenie kind. BRAK tokenów TIMOCOM/Teleroute/Transporeon w CHECK. BRAK wymiany ofert live.

## Zakres

How-to: jedno zdanie — rozszerzamy katalog konektora giełdy o nazwy P0; posting i live API zostają leftover. +0 OPS_JOB (ta sama trasa). Klient FE: rozszerzony select `system_kind` (nie radio 472, nie toggle 470) pod jscpd < 3%.

## Poza zakresem

live giełda · auto-post · scrape · X1–X5 SPA · OAuth2 · sekrety · FK quotation/network · wymiana ofert · kwota · nowa tabela

## Kryteria akceptacji

- [ ] migracja ALTER CHECK + istniejące wiersze `trans_eu` bez regresji
- [ ] POST zły kind (poza listą) → 400; amount/sekret → 422
- [ ] craft_style funkcje>40: 0; brak importu innych BC services

## Leftover

live Trans.eu / TIMOCOM / Transporeon / Teleroute HTTP · auto-post frachtu · wymiana ofert · X1–X5 SPA · OAuth2 · outbox consumer · V5b `exchange_message`
