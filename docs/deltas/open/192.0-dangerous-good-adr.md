# Plaster 192.0 · EXP0.9 `dangerous_good` ADR tunnel + SG

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala C · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) § EXP0.9
**Zależy od:** 7.0 `dangerous_good` (katalog UN + IMDG już jest)
**Status:** Zaakceptowany `/noc` 2026-09-09. Nie auto-award. Nie T-SQL. Nie kwota. LLM nie nadaje klasy.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover C3–C9 + F2b–F4 + G2.20–G2.22 + F1 + C1 + C8 409):** leftover C3 live VIES/GUS parked (HTTP). leftover C4 eCMR parked. leftover C9 Trans.eu parked. leftover C2 AIS/PUESC parked. leftover C6 BDO parked. leftover F2b Decimal parked. leftover F3 period lock parked. leftover F4 CAMT parked. leftover `circle_sim` parked. leftover kg/CBAM parked. leftover Deadline Engine parked. F1 live FA(3) parked. C1 filing parked. C8 409 parked. Żywy plaster: **pogłębienie istniejącej** `dangerous_good` — HITL kod tunelu ADR i grupa segregacji IMDG jako dane, nie nadanie klasy przez LLM i nie live IMO. Wiersz dostaje `adr_tunnel_code` (`A` | `B` | `C` | `D` | `E`) + `segregation_group` (`none` | `sg1`…`sg18`). Operator wpisuje kod z tabeli ADR/IMDG; serwis nie zmienia `imdg_class`, nie woła sieci, nie woła LLM i nie czyta `charge` / `quotation`. Zły tunel → 400 (`tunel`). Zła grupa → 400 (`segregacja`). `un_number` / `imdg_class` / `source_ref` zostają z 7.0. GET listy SQL. UI zostaje `/dangerous-goods`. `SHIPPED_CHARGE_ROUTES["192.0"]` = `/dangerous-goods`. OpenFGA bez nowej relacji (`can_manage_dangerous_goods`). Migracja `132_dangerous_good_adr`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `dangerous_good` — UN + klasa IMDG, **nie** tunel ADR i **nie** grupa SG. ISTNIEJE `cargo_claim` 191.0 — terminy CMR, **nie** UN. BRAK `adr_tunnel_code` / `segregation_group`. BRAK live IMO. BRAK silnika zgodności 1.xA.

**Odrzucone:**

- C3 live VIES/GUS (HTTP).
- C4 eCMR / C9 Trans.eu / C2 AIS / C6 BDO.
- F2b Decimal / F3 period lock / F4 CAMT.
- `circle_sim` / kg / F1 KSeF / C1 filing / C8 409.
- Deadline Engine leftover 191.0.
- Nadanie `imdg_class` z tunelu albo z LLM.
- Grupy zgodności 1.xA / packing group / live IMO.
- Kwota / druga marża / float / T-SQL / live HTTP / scrape.

## Zakres

1. Migracja `132_dangerous_good_adr` (po `131_cargo_claim_cmr`): kolumny NOT NULL po backfillu (`A`, `none`). CHECK allowlist.
2. POST wymaga nowych pól. Zły tunel / grupa → 400. Serwis nie importuje quotations / charges / extraction i nie woła HTTP.
3. UI `/dangerous-goods`. `just api-types`. How-to istniejący katalog UN; fold w `katalogi.md` (40 linii, bez nowej linii).

## Poza zakresem

C3 live lookup · C4 eCMR · C9 Trans.eu · C2 AIS · C6 BDO · F2b · F3 · F4 · G2.20 · kg · F1 · C1 · C8 409 · packing group · live IMO · LLM klasa · kwota · auto-award · T-SQL · live HTTP

## Kryteria akceptacji

- [ ] POST zapisuje `dangerous_good` z `adr_tunnel_code` i `segregation_group`, bez zmiany klasy przez LLM
- [ ] zły `adr_tunnel_code` → 400 (`tunel`)
- [ ] zły `segregation_group` → 400 (`segregacja`)
- [ ] GET zwraca kody tenanta
- [ ] Test izolacji (wiersz z nowymi kolumnami)
- [ ] Zero float / marży / LLM-klasy / live HTTP / scrape
