# Plaster 609.0 · T3 EXP1 `alliance_service` na `container`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 leftover EXP1 · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 container · [608.0](../archived/608.0-container-vessel-imo.md)
**Zależy od:** 153.0 `container`; 608.0 `vessel_imo`
**Status:** Delta zaakceptowana `/noc` 2026-09-21 — wolno `/plaster`. Nie live alliance. Nie HTTP.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover T3 EXP1 `alliance_service` + leftover HW + leftover UXCL + N3 + AI3 + G0-SH):** leftover HW parked. leftover UXCL parked. N3 parked. AI3 parked. G0-SH parked. Żywy plaster: **pogłębienie istniejącej** `container` — opcjonalny `alliance_service` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Nie-tekst / > 32 → 400 (`alians`). `require_alliance_service` w `app.domain.container` — wzór `require_vessel_imo`, **nie** live 2M/Ocean/THE, **nie** scrape. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["609.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `484_container_alliance_service` (po `483_container_vessel_imo`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `container.vessel_name` (219.0). ISTNIEJE `container.voyage_no` (220.0). ISTNIEJE `container.vessel_imo` (608.0). ISTNIEJE `container.grade` (607.0). BRAK `container.alliance_service`.

**Odrzucone:**

- HW / UXCL / N3 / AI3 / G0-SH / live alliance HTTP / scrape / float / marża / mapa / Auth0 / CHECK listy aliansów.

## Zakres

1. Migracja `484_container_alliance_service`: `alliance_service` nullable String(32). Nie `create_table`.
2. POST opcjonalny `alliance_service`. Zła wartość → 400 (`alians`). Serwis nie importuje charges.
3. UI `/shipments` panel kontenera. Fold w how-to. Job OPS bez nowego numeru.

## Poza zakresem

live alliance · HW · UXCL · N3 · mapa · Auth0 · live HTTP · float · CHECK listy

## Kryteria akceptacji

- [ ] POST z `alliance_service` zapisuje etykietę HITL, bez live alliance
- [ ] POST bez pola / pusty trim zostaje `null`
- [ ] nie-tekst / > 32 → 400 (`alians`)
- [ ] RLS istniejącego `container`; izolacja bez nowej tabeli
- [ ] Zero float / marży / live HTTP / import charges

## Leftover

HW · UXCL · N3 · AI3 · G0-SH
