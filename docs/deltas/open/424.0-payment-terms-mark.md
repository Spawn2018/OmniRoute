# Plaster 424.0 · EXP1 `payment_terms_mark` HITL

**Status:** Delta zaakceptowana `/noc` — wolno `/plaster`.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** HITL katalog `payment_terms_mark` — `mark_code` + `terms_kind` `net|prepaid|other` + `source_ref`. Odpowiada EXP1 `payment_terms_id` na shipment bez FK na zlecenie.
- **Odrzucone:** `cargo_value`+waluta — kwota/Decimal na katalogu; koliduje z zakazem drugiej marży i z `high_value_mark` (protokół bez wartości).
- **Odrzucone:** kolumna `payment_terms_days` na `party` — już istnieje; ten plaster nie mutuje party ani nie liczy dni.
- **Odrzucone:** `language_code` / `booked_carrier` — osobne leftover; węższy Q z kolejki = terms.

## Zakres

HITL `payment_terms_mark`: mark_code + terms_kind net|prepaid|other + source_ref.
Migracja `334` (revises `333_high_value_mark`). UI `/payment-terms-marks`.
OpenFGA `can_manage_payment_terms_marks` (exp4 catalog).

## Poza zakresem

kolumna / FK na shipment · party.payment_terms_days · skonto F2 · kwota · DSO

## Kryteria

- [ ] POST z kodem, terms_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/margin/score/days → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] `__init__.py` w services; `/payment-terms-marks` w REQUIRED_BUSINESS_LIST_ROUTES
- [ ] OPS_JOBS length 241; SHIPPED `"424.0"`

## Leftover

FK payment_terms na shipment · egzekucja skonta · ERP
