# Plaster 411.0 · Demo-1 `demo_wipe_mark` HITL

**Status:** Otwarty `/noc` — plan.

## Zakres

HITL katalog `demo_wipe_mark` per tenant: `mark_code` + `wipe_kind`
`usun`|`retain`|`other` + `source_ref` (`tenant:manual` | `fixture://demo-wipe-mark/…`).

Klon wzorca `three_way_mark`:
- migracja `321` (revises `320_three_way_mark`), RLS FORCE
- model / domain / repo / service / API / OpenFGA `can_manage_demo_wipe_marks`
- UI `/demo-wipe-marks`, business-lists + ops-index `SHIPPED["411.0"]`
- operator `docs/operator/demo-wipe-mark.md`
- izolacja RLS; POST `amount`/`delete`/`score` → 422

Znacznik wipe demo (`USUN`) jako dana. Bez kasowania wierszy / live wipe.

## Poza zakresem

live wipe · DELETE tenant data · flota klienta · PDF umów · GPS live

## Kryteria

- [ ] POST z kodem, wipe_kind, source_ref
- [ ] zły kod/kind/source_ref → 400; amount/delete → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
- [ ] quality_floor isolation +1

## Leftover

live wipe `USUN` · demo_sim 150 aut · GPS 7 dni
