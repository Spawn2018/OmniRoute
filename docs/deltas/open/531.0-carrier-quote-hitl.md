# Plaster 531.0 · O6 leftover — HITL `carrier_quote` → `channel_quote` + `answered`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § O6 · [karty-pol-fala-o.md](../../analysis/karty-pol-fala-o.md) § O6 · po [140.0](../archived/140.0-extraction-carrier-quote.md) · po O2 [135.0](../archived/135.0-channel-quote-from-quote.md) · po O3 [136.0](../archived/136.0-carrier-inquiry-batch.md)

**Zależy od:** 140.0 `draft_kind=carrier_quote` + accept → `channel_quote` · O2 `create_quote` · O3 status `answered` + CHECK `ck_carrier_inquiry_answered_quote` · wzorzec 1.3 / `AcceptExtractionToRates` (API, nie ExtractionService)

## Wybrane / odrzucone / dlaczego

- **Wybrane:** węższy leftover O6 po **140.0**. `draft_kind=carrier_quote` i INSERT `channel_quote` po HITL **już są** (`AcceptExtractionToRates._write_channel_quote` → `ChannelQuoteService.create_quote`). Plaster **531.0** domyka kartę O6: po akceptcie, gdy payload ma `carrier_inquiry_id`, warstwa API ustawia istniejące `carrier_inquiry` na `status=answered` z `quoted_amount` / `quoted_currency` / opcjonalne `quoted_transit_days` z tego samego payloadu (Decimal, CHECK O3). Bez `carrier_inquiry_id` — zachowanie 140.0 (tylko `channel_quote`), zero mutacji inquiry.
- **Wybrane:** rozszerzyć `CarrierQuoteDraft` / `require_carrier_quote_payload` o opcjonalne `carrier_inquiry_id` (UUID w JSONB payloadu szkicu — nie nowa kolumna na `extraction_draft`). Nowa metoda serwisu inquiries: `mark_answered` (UPDATE wiersza `sent`|`queued`|`draft` → `answered` + kwota; odmowa gdy już `answered`/`declined` albo obce id). Składanie w `AcceptExtractionToRates` — **nie** w `ExtractionService`.
- **Wybrane:** OpenFGA bez nowej relacji (`can_review_extractions`). UI `/ai` — pokazać powiązanie inquiry gdy jest w payloadzie (minimalnie). How-to fold w [ścieżka-pieniędzy.md](../../operator/ścieżka-pieniędzy.md). `SHIPPED` / job OPS bez nowego ekranu katalogu.
- **Odrzucone:** zapis `channel_quote` / `rate_line` / `carrier_inquiry` z `ExtractionService` (`REJECTED` — HC-04 / PROGRAM 1.3 / AGENTS extraction).
- **Odrzucone:** auto-accept bez HITL · live HTTP Graph/armator · float · marża / druga tabela marży · fuzzy match inquiry po party+POL/POD bez UUID (`REJECTED`).
- **Odrzucone:** nowa migracja `draft_kind` / ponowne budowanie gałęzi accept 140.0 · kolumna FK `carrier_inquiry_id` na `extraction_draft` · PATCH osobnego endpointu bez accept (`REJECTED` na tym wierszu — leftover osobny jeśli potrzeba).
- **Odrzucone na tym wierszu (leftover):** append `entity_event` `quote_recorded` przy accept (246.0 tylko POST `/carrier-inquiries` ze `status=answered`; PATCH parked) · wymóg `carrier_inquiry_id` zawsze · UPDATE inquiry bez kwoty.

**Łowca:** ISTNIEJE `extraction_draft.draft_kind` CHECK `rate_line`|`carrier_quote`|`tender_rfp` (`backend/app/models/extraction_draft.py`). ISTNIEJE `AcceptExtractionToRates` → `channel_quote` dla `carrier_quote` (`backend/app/api/accept_extraction.py`). ISTNIEJE `CarrierQuoteDraft` bez `carrier_inquiry_id` (`backend/app/domain/extraction_draft.py`). ISTNIEJE `ChannelQuoteService.create_quote` (O2). ISTNIEJE `carrier_inquiry.status` + `quoted_*` + CHECK answered (`backend/app/models/carrier_inquiry.py`). ISTNIEJE POST `answered` przy INSERT (O3) + `quote_recorded` przy POST (246.0). PODOBNE składanie API 1.3 / 140.0 / 179.0. BRAK `mark_answered` / UPDATE statusu inquiry. BRAK powiązania accept → inquiry. BRAK `carrier_inquiry_id` w payloadzie szkicu.

## Zakres

1. Domena: opcjonalne `carrier_inquiry_id` w `CarrierQuoteDraft` + walidacja UUID.
2. `CarrierInquiryService.mark_answered` + repo UPDATE (kwota Decimal, waluta ISO, TT jak O3).
3. `AcceptExtractionToRates._write_channel_quote`: po `create_quote`, jeśli id — `mark_answered` w tej samej transakcji.
4. Testy: accept z inquiry → `channel_quote` + `answered`; bez id → tylko quote (regres 140.0); ExtractionService bez importu quotes/inquiries; zły status inquiry → 400; izolacja RLS.
5. How-to + `SHIPPED` / docs pętli post-plaster.

## Poza zakresem

ponowne `draft_kind` · live HTTP · auto-accept · float / marża · fuzzy match · FK kolumna na szkicu · `quote_recorded` z accept · F10 inne kindy · O7/O8

## Kryteria akceptacji

- [ ] Accept `draft_kind=carrier_quote` z `carrier_inquiry_id` → INSERT `channel_quote` **oraz** inquiry `status=answered` z `quoted_amount`/`quoted_currency` (TT jeśli w payloadzie)
- [ ] Accept bez `carrier_inquiry_id` → nadal tylko `channel_quote` (regres 140.0); zero mutacji inquiry
- [ ] `ExtractionService` bez zapisu `channel_quote` / `rate_line` / `carrier_inquiry`
- [ ] Obce / niedozwolone przejście statusu inquiry → 400; zero float / marży / live HTTP
- [ ] Test izolacji tenanta na ścieżce accept+answered; how-to zaktualizowane

## Leftover

`quote_recorded` przy accept (nie tylko POST answered) · wymóg zawsze `carrier_inquiry_id` · osobny PATCH statusu inquiry · UI buy-desk poza `/ai`
