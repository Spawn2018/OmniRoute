# Plaster 574.0 · T3 leftover — HITL `container.weight_kg`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · karta T3 (`cargo_description` / `quantity` / `packaging_code` / `weight_kg` / `volume_m3`) · [573.0](../archived/573.0-container-quantity.md)

**Zależy od:** 153.0 `container` · 573.0 `quantity`

**Status:** Zaakceptowana `/noc` 2026-09-18. Nie `stop.weight_kg`. Nie VGM. Nie quantity. Nie TEU. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** po quantity karta T3 zostawia `weight_kg` na kontenerze. W `container` kolumny nie ma. `stop.weight_kg` jest `Numeric(14,4)` i CHECK ≥ 0, a `require_stop_weight_kg` puszcza zero — to inny BC. Kilogramy na kontenerze (`tare_kg` / `vgm_kg` / `payload_kg`) idą przez `_require_positive_kg`: Decimal, > 0, odrzut float. Opcjonalny `weight_kg` ten sam typ. POST bez pola → `null`. Pusty string → `null`. Zła wartość → 400 (`waga`). Zero odpada (jak tara, nie jak punkt). Nie wylicza wagi z VGM−tara ani z `payload_kg`. Nie mutuje `quantity` / `teu` / `payload_kg`.
- **Wybrane:** `require_container_weight_kg` woła istniejące `_require_positive_kg` z etykietą `waga`. UI `/shipments`, string jak tara (`optionalToken`). `SHIPPED["574.0"]` → `/shipments`. OpenFGA bez nowej relacji. Migracja `454_container_weight` (po `453_container_quantity`). Pole w istniejącej linii `_write_from_body`. Bez CHECK w bazie — tara też go nie ma.
- **Odrzucone:** zapis `stop` (`REJECTED`). Zero jak na punkcie (`REJECTED` — kilogramy kontenera są dodatnie). VGM / tara / ładowność (`REJECTED`). quantity / TEU / `volume_m3` (`REJECTED` → park na `volume_m3`). float (`REJECTED`). ciphertext / AI3-payload / G0-SH (`REJECTED` → park).

**Łowca:** BRAK `weight_kg` na `container`. ISTNIEJE `stop.weight_kg` (T1, zero dozwolone). ISTNIEJE `_require_positive_kg`. Nie nowa tabela. RLS bez zmiany polityki.

## Zakres

1. Migracja `454_container_weight`: kolumna nullable `Numeric(14,4)`.
2. Domain `require_container_weight_kg` + API POST opcjonalny + GET.
3. UI `/shipments` (`weight_kg` jako string). How-to dopisek. `SHIPPED["574.0"]`.

## Poza zakresem

`stop.weight_kg` · VGM · quantity · TEU · `volume_m3` · ciphertext · AI3-payload · G0-SH · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `null`
- [ ] POST `weight_kg="1250.5"` zapisuje Decimal
- [ ] float / ≤0 / bool / nie-liczba → 400 (`waga`)
- [ ] izolacja RLS (bez zmiany polityki)
- [ ] zero wyliczenia z VGM / mutacja quantity i tary

## Leftover

`volume_m3` na kontenerze · N3 countdown · szkic charge · live armator · ciphertext PIN · AI3-payload · G0-SH
