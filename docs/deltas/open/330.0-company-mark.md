# Plaster 330.0 · G10 `company_mark` HITL (katalog, bez drugiego tenanta)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala G / G10 · [karty-pol-fala-g.md](../../analysis/karty-pol-fala-g.md)
**Status:** Delta zaakceptowana `/noc` 2026-09-11. Wolno `/plaster`. Nie drugi tenant. Nie kwota.

## Wybrane / odrzucone / dlaczego

**Wybrane:** węższa z kolejki G10 — tylko HITL katalog `company_mark` jako dane. `mark_code` + `seat_kind` (`hq` | `branch` | `other`) + `source_ref`. UI `/company-marks`. OpenFGA `can_manage_company_marks`. Migracja `242_company_mark`. Job OPS. Deny-list extract. `company_id` FK / drugi tenant / multi-ledger = leftover.

**Łowca:** BRAK tabeli `company_mark`. ISTNIEJE `organization` — tenant, nie fotel spółki. ISTNIEJE `tenant_contract_kek` — KEK, nie company seat.

**Odrzucone:** drugi tenant · company_id FK na wszystkich tabelach · kwota · marża · float · HTTP · UPDATE/DELETE.

## Zakres

1. Migracja `242_company_mark` RLS FORCE.
2. POST/GET `/company-marks`. OpenFGA. Deny-list.
3. UI `/company-marks`. How-to. `SHIPPED_CHARGE_ROUTES["330.0"]`.

## Kryteria akceptacji

- [ ] POST z kodem, seat_kind, source_ref
- [ ] zły kod → 400; zły kind → 400; obce source_ref → 400
- [ ] POST z amount/ledger → 422
- [ ] izolacja RLS; craft_style funkcje>40: 0
