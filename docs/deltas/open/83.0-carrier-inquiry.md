# Plaster 83.0 · S20 zapytanie do agenta (M-30)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Kolejka S20  
**Zależy od:** 23.0 ślad `carrier_inquiry` · 82.0 `network_member` · 0.3 RLS · 0.4 OpenFGA

**Status:** Delta zaakceptowana `/noc` 2026-09-03. Ten sam żywy ID **M-30**. Nie F9.1. Zero kodu w tym commicie.

## Wybrane / odrzucone / dlaczego

**Wybrane:** żywy ID **M-30**. Nowa tabela `carrier_inquiry` — ślad **kupna**: operator zapisuje, że zapytał agenta (`network_member`). To nie jest `customer_rfq` (strona sprzedaży). 23.0 zostaje tablicą `channel_quote` przy lane. FK tenanta do `network_member` (`organization_id` + `network_member_id`). `source_ref`. Status `draft`. Bez kwoty, bez marży, bez live HTTP. OpenFGA: istniejące `can_manage_networks`. `GET/POST /carrier-inquiries`. Ekran `/networks` (ten sam job co członek). Serwis własnego BC — nie importuje `networks` / `quotations` / `channel_quotes`. Nieznany członek = błąd bazy → 400.

**Łowca:** ISTNIEJE 23.0 token UI + lista `channel_quote`. ISTNIEJE 82.0 `network_member`. ISTNIEJE 67.0 `customer_rfq` (sell). BRAK tabeli zapytania do agenta. PODOBNE: `customer_rfq` (jeden obiekt, `draft`, bez kwoty).

**Odrzucone:**

- Live HTTP / portal armatora (S21, tylko przy umowie).
- `customer_rfq` / IMAP / Graph.
- Kwota, marża, `charge`, odejmowanie od wyceny (M-31).
- FK / import `quotation` · `party` · `channel_quote`.
- Scraping · Temporal · F9.1 · Auth0 · portale.

## Zakres

1. Migracja `043_carrier_inquiry_rls`: tabela + `UNIQUE (organization_id, id)` na `network_member` (nośnik FK tenanta) + RLS FORCE. Test izolacji.
2. BC `carrier_inquiries`: list + create. `GET/POST /carrier-inquiries`.
3. UI `/networks`: zapis zapytania do wybranego członka. Zero nowego ekranu. Nie licz w JS.

## Poza zakresem

Live HTTP · S21 kanał · porównanie M-31 · `customer_rfq` · kwoty · FK quotation/party · scraping · F9.1

## Ustalenia

- Żywy ID: **M-30**. Token: `carrier_inquiry` (teraz tabela, nie tylko UI).
- How-to: `docs/operator/katalogi.md`.
- 23.0 panel na `/quotations` zostaje śladem ofert kanału.
- Wysyłka / HTTP = leftover.

## Schemat

`carrier_inquiry`: `id`, `organization_id`, `network_member_id`, `source_ref`, `status = draft`, timestamps/`created_by`. FK tenanta do `network_member`.

## Kryteria akceptacji

- [ ] RLS FORCE + izolacja (B nie widzi zapytania A)
- [ ] Nieznany `network_member_id` → 400; brak `amount` w API
- [ ] `can_manage_networks`; 403 bez tuple
- [ ] Serwis bez `httpx` / `requests` / `scrap` / importu innych BC
- [ ] `just api-types`

## Pytania

Brak. `/noc` akceptuje deltę.
