# Plaster 451.0 · AI3.4 Excel

**Status:** Delta zaakceptowana `/noc`. Wolno `/plaster`.

**Spec źródłowa:** [VISION.md](../../VISION.md) § E AI3 · [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § Fala AI AI3.4

**Zależy od:** T0 `document_base64` · `DeterministicDocumentParser`

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ten sam BC `extraction`. Bez nowej tabeli. Bez nowego pakietu pip.
- **Wybrane:** `layout_fingerprint` rozpoznaje OOXML (`PK` + `xl/workbook.xml`). Parser `xlsx_sheet` czyta pierwszy arkusz przez `zipfile` + `xml.etree`, składa wiersze w tekst i oddaje istniejący extractor.
- **Wybrane:** `parser_name=xlsx_sheet`. HTTP `document_base64` bez zmiany kontraktu. How-to: jedno zdanie, że wolno wrzucić `.xlsx`.
- **Odrzucone:** `openpyxl` / nowa zależność (`REJECTED` — stdlib wystarczy na pierwszy arkusz).
- **Odrzucone:** `.xls` BIFF · formuły · float z komórki · wiele arkuszy do wyboru · live Graph · vision · zapis z modelu.

**Łowca:** ISTNIEJE `pdf` / `text` w `layout_fingerprint`. PODOBNE `PdfStringsParser`. BRAK `xlsx`. Brak `openpyxl` w repo.

## Zakres

OpenFGA to samo. `SHIPPED["451.0"]` → `/extractions`. Bez +1 OPS_JOB.

## Poza zakresem

xls binarny · macros · drugi arkusz · instructor na siatce · live vision

## Kryteria akceptacji

- [ ] bajty xlsx z THC/EUR dają szkic z kodem THC; `parser_name=xlsx_sheet`
- [ ] ZIP bez `xl/workbook.xml` → `UnparseableDocument` / 400
- [ ] PDF i tekst bez zmiany zachowania
- [ ] brak `float`; craft_style funkcje>40: 0

## Leftover

`.xls` · wiele arkuszy · openpyxl · formuły · live vision

## Post-plaster

| Pytanie | Werdykt | Notatka |
|---|---|---|
| Skuteczność | | |
| Szybkość | | |
| SQL | | |
| N+1 | | |
| Dług / człowiek | | |
| Proza operatora | | |
| Docs/OS | | |
| Pushy do zielonego | | |
| Minuty plastra | | |
| Bench rzemiosła | | |
