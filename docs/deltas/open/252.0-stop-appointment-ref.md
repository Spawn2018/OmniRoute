# Plaster 252.0 · T1 EXP1 `appointment_ref` na `stop`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T1 leftover EXP1 · [251.0](../archived/251.0-stop-seal-out.md) leftover awizacja · [karty-pol-fala-exp.md](../../analysis/karty-pol-fala-exp.md) EXP1 stop
**Zależy od:** 251.0 `seal_out`
**Status:** Delta zaakceptowana `/noc`. Nie D3. Nie waiting. Nie mapa. Nie PIN.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover EXP1 awizacja/waiting/POD + leftover T1b + leftover T2c km + leftover T7b + leftover T5 + leftover N3 + S53 + PIN):** leftover waiting/POD parked. leftover `appointment_status` / `no_show_at` parked. leftover T1b parked. leftover T2c parked. leftover T7b parked. leftover T5 parked. leftover N3 parked. S53 parked. PIN parked. Żywy plaster: **pogłębienie istniejącej** `stop` — opcjonalny `appointment_ref` (`String(32)` HITL, trim, pusty → `null`). POST bez pola zostaje `null`. Zły token → 400 (`awizacja`). `require_stop_appointment_ref` w `app.domain.stop` — nie import `dock_appointments` / `require_appointment_code`. `_HitlTail` z 251.0 dostaje jedno pole. UI `/shipments`. `SHIPPED_CHARGE_ROUTES["252.0"]` = `/shipments`. OpenFGA bez nowej relacji. Migracja `188_stop_appointment_ref` (po `187_stop_seal_out`). Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `stop.seal_out` (251.0). ISTNIEJE `dock_appointment` (157.0) — okno TIME + `appointment_code` na `stop` magazynu, inny BC, ekran `/dock-appointments`. ISTNIEJE GLOSSARY awizacja doku / awizacja terminalu (T8). BRAK `stop.appointment_ref`. BRAK `appointment_status` / `no_show_at` na `stop`.

**Odrzucone:**

- Druga tabela doku / FK do `dock_appointment` / kopiowanie D3.
- `appointment_status` / `no_show_at` / waiting / POD.
- Import `dock_appointments` / T8 / PIN / `charge` / float / km / mapa / T5 / S53 / live HTTP.

## Zakres

1. Migracja `188_stop_appointment_ref`: `appointment_ref` nullable String(32). Nie `create_table`.
2. POST opcjonalny `appointment_ref`. Zły token → 400 (`awizacja`). Serwis nie importuje dock/charges.
3. UI `/shipments` panel punktu. Fold w how-to. Job OPS zostaje 106.

## Poza zakresem

`appointment_status` · `no_show_at` · waiting · POD · tabela `stop_group` · D3b · T8 · PIN · km · mapa · T7b · T5 · Auth0 · live HTTP

## Kryteria akceptacji

- [ ] POST z `appointment_ref` zapisuje numer awizacji na punkcie, bez okna D3
- [ ] POST bez pola / pusty trim zostaje `null`
- [ ] nie-tekst / za długi → 400 (`awizacja`)
- [ ] RLS istniejącego `stop`; izolacja bez nowej tabeli
- [ ] Zero float / marży / mapy / `/fleet` / live HTTP / import `dock_appointments`
