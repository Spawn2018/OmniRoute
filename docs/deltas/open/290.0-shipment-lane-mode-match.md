# Plaster 290.0 · CT4 matching lane/mode na shipment (przy block_409)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § CT4 leftover · [289.0](../archived/289.0-asn-lane-mode-match.md)
**Zależy od:** 289.0 ASN label match · 288.0 match kind · 287.0 shipment guide_code
**Status:** Plan `/noc` 2026-09-11. Nie live EDI. Nie auto shipment.

## Wybrane / odrzucone / dlaczego

**Wybrane:** dodać opcjonalne `plant_label` + `carrier_label` na `shipment` (jak ASN: tekst 1–128, trim, bez FK). Gdy `block_409` **oraz** katalog `routing_guide_match` ma `lane_label` / `mode_label`, POST `/shipments` po `guide_code` woła istniejące `assert_asn_labels_on_routing_guide` (te same reguły casefold).

**Łowca:** ISTNIEJE gate etykiet i kolumny na ASN. BRAK kolumn etykiet na shipment — dziś matching shipment nie da się egzekwować.

**Odrzucone:** osobna funkcja domain „shipment_labels” · fuzzy/LLM · float · live EDI · wspólny SELECT park.

## Zakres

1. Migracja `222` + model `shipment` + domain create (walidacja etykiet jak ASN).
2. API `/shipments`: pola create/response + compose match services + assert.
3. Testy HTTP/contracts + izolacja nie wymaga nowej tabeli (ALTER istniejącej).
4. FE formularz zlecenia + how-to `zlecenie.md` + OpenAPI regen jeśli w pętli.
5. Close: CURRENT Następny = wspólny SELECT / następny wąski HITL pin.

## Kryteria akceptacji

- [ ] block_409 + guide_code_only → jak 287 (same guide_code)
- [ ] block_409 + lane_label + zły plant_label → 409
- [ ] block_409 + lane_label + zgodny plant → 201
- [ ] bez block_409 → matching nie blokuje
- [ ] BC `shipments` bez importu `routing_guide*` services (compose w API)

## Leftover po 290.0

Wspólny SELECT. Auto shipment. CT2/CT5/CT8 live. Workflow CAPA.
