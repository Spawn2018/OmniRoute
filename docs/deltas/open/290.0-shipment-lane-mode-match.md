# Plaster 290.0 · CT4 matching lane/mode na shipment (przy block_409)

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § CT4 leftover · [289.0](../archived/289.0-asn-lane-mode-match.md)
**Zależy od:** 289.0 ASN label match · 287.0 shipment 409 · 288.0 match kind
**Status:** Plan `/noc` 2026-09-11. Nie live EDI. Nie auto shipment.

## Wybrane / odrzucone / dlaczego

**Wybrane:** ta sama bramka etykiet co ASN: gdy `block_409` i katalog `routing_guide_match` ma `lane_label` / `mode_label`, POST `/shipments` po `guide_code` porównuje etykiety. Shipment nie miał `plant_label`/`carrier_label` — dodajemy opcjonalne kolumny HITL (jak ASN) + pola create. Reużywa `assert_asn_labels_on_routing_guide`. Składanie w API (BC `shipments` bez importu services przewodnika).

**Łowca:** ISTNIEJE gate etykiet 289.0 + 409 guide_code na shipment 287.0. BRAK pól etykiet i wywołania label-assert na POST shipments.

**Odrzucone:** fuzzy/LLM · float · live EDI · auto shipment z ASN · wspólny SELECT CT11.

## Zakres

1. Migracja `222_shipment_guide_labels`: nullable `plant_label`, `carrier_label` + check długości.
2. Domain bez zmian (reużywa gate). API shipments: match kinds + assert.
3. FE formularz + how-to zlecenie. Testy HTTP.

## Kryteria akceptacji

- [ ] block_409 + lane_label + zły plant → 409
- [ ] block_409 + lane_label + zgodny plant → 201 + zapis etykiet
- [ ] bez block_409 → etykiety opcjonalne, zero blokady match
- [ ] BC shipments bez importu `routing_guide*` services
- [ ] zero float

## Leftover po 290.0

Wspólny SELECT. Auto shipment. CT2/CT5/CT8 live. Live EDI.
