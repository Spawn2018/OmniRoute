# Plaster 207.0 · P4b `port_unlocode` na `local_charge`

**Spec źródłowa:** [docs/PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § P4 leftover · [166.0](../archived/166.0-local-charge.md) leftover P4b
**Zależy od:** 166.0 `local_charge` (THC/ISPS/seal/amendment + Decimal już jest)
**Status:** Otwarty `/noc` 2026-09-09. Nie warning braków. Nie FK geography. Nie druga marża.

## Wybrane / odrzucone / dlaczego

**Wybrane (węższe z leftover P3b–d + P4b macierz + P4c warning + leftover D9c–f + V7–V8 + C3–C9 + F2b–F4 + G2.20):** leftover P3b SQL indeksu na `charge` parked (`charge` zostaje prawdą o marży). leftover P3c live HTTP NBP/BAF parked. leftover P3d A11 bez żywej nazwy parked. leftover P4c warning-jako-fakt parked. leftover D9c–f / V7–V8 / V6 / V5 / V4 / V2b / C3–C9 / F2b–F4 / G2.20 / kg / F1 / C1 / C8 409 / N3 parked. Żywy plaster: **pogłębienie istniejącej** `local_charge` — opcjonalny `port_unlocode` jako dana (token 5 znaków), nie macierz 4D i nie FK do `port`. POST bez pola zostaje `null` (dopłata „globalna”). Zły kod → 400 (`port`). Unique per tenant `(charge_kind, port_unlocode)` gdy port jest. UI `/local-charges`. `SHIPPED_CHARGE_ROUTES["207.0"]` = `/local-charges`. OpenFGA bez nowej relacji (`can_manage_local_charges`). Migracja `147_local_charge_port`. Marża zostaje na `charge`.

**Łowca:** ISTNIEJE `local_charge` — rodzaj + Decimal, **bez** portu. ISTNIEJE `port_surcharge` — extra portowe + `port_id` FK; **nie** rozszerzamy, inna oś. ISTNIEJE `port` / geography — katalog UN/LOCODE; **nie** importujemy. ISTNIEJE `container` ISO 6346 — leftover reszty macierzy. BRAK `port_unlocode` na `local_charge`. BRAK warning-jako-fakt.

**Odrzucone:**

- P3b mnożenie FSC na `charge` · P3c live HTTP · P3d A11.
- Armator / serwis / typ kontenera / FK (reszta P4b).
- Warning braku dopłaty (P4c).
- Import geography / charges / port_surcharges.
- T-SQL / float / live HTTP / 409 na shipment.

## Zakres

1. Migracja `147_local_charge_port` (po `146_charge_template_span`): kolumna `port_unlocode` nullable, unique `(organization_id, charge_kind, port_unlocode)` gdy nie NULL.
2. POST opcjonalny `port_unlocode`. Zły token → 400. Serwis nie importuje geography i nie woła HTTP.
3. UI `/local-charges`. How-to `oplata-lokalna.md`; fold w `katalogi.md` (40 linii). Bez nowego joba OPS (zostaje 106).

## Poza zakresem

P3b–d · P4c warning · FK `port` · armator/serwis/kontener · T-SQL · charge · live HTTP

## Kryteria akceptacji

- [ ] POST z `port_unlocode` zapisuje token, bez FK geography
- [ ] POST bez `port_unlocode` zostaje `null`
- [ ] zły token → 400 (`port`)
- [ ] unique per tenant gdy port jest; izolacja
- [ ] Zero float / marży / warning-jako-fakt / live HTTP / scrape
