# Plaster 583.0 · T3 leftover — HITL `container.needs_external_power`

**Spec źródłowa:** [PLAN-REALIZACJA.md](../../PLAN-REALIZACJA.md) § T3 · [karty-pol-fala-t.md](../../analysis/karty-pol-fala-t.md) § T3 · [582.0](../archived/582.0-container-temp-max.md) · [229.0](../archived/229.0-container-reefer.md)

**Zależy od:** 153.0 `container` · 229.0 `reefer` · 582.0 `temp_max`

**Status:** Delta zaakceptowana `/noc` 2026-09-19. Wolno `/plaster`. Nie N3. Nie ciphertext. Nie AI3-payload. Nie G0-SH.

## Wybrane / odrzucone / dlaczego

- **Wybrane:** ostatnie pole SPEED z karty reefer/temp/zasilanie: `needs_external_power` bool HITL. Wzorzec `require_container_reefer` → `require_container_needs_external_power`. POST bez pola → `false`. `true`/`false` zapisuje. Zły token → 400 (`zasilanie`). Nie egzekwuje `reefer=true`. Nie porównuje temperatur.
- **Wybrane:** UI `/shipments` checkbox. `SHIPPED["583.0"]` → `/shipments`. Migracja `463_container_needs_external_power`.
- **Odrzucone:** 409 vs reefer · min≤max · N3 countdown · ciphertext PIN · AI3-payload · G0-SH · float · live HTTP.

**Łowca:** BRAK `needs_external_power` przed plasterem. ISTNIEJE `reefer` 229.0 + `temp_min`/`temp_max`.

## Zakres

1. Migracja `463_container_needs_external_power`: Boolean NOT NULL, server_default false.
2. Domain + API + UI + SHIPPED.

## Poza zakresem

N3 · ciphertext · AI3-payload · G0-SH · 409 vs reefer · min≤max · kwota · live HTTP

## Kryteria akceptacji

- [ ] POST bez pola → `false`
- [ ] POST `needs_external_power: true` zapisuje flagę
- [ ] zły token → 400 (`zasilanie`)
- [ ] izolacja RLS
- [ ] zero mutacji `reefer` / `temp_min` / `temp_max`

## Leftover

N3 countdown · egzekucja „tylko gdy reefer” · min≤max · ciphertext PIN · AI3-payload · G0-SH
