# Karta wiedzy (3–8 linii)

Prawo: `LEARNING-OS.md`. Po **zamknięciu** PM-FAC / AAR z jedną zmianą / Closure.  
SLA: ta sama sesja co `closed`; strop = najbliższy WBR (≤7 dni).  
Nie pisz, gdy `factory_cycle --close` już zrobił `machine-*` o tym samym (RLS/bench/CI).

- id: `memory-patterns/pm-fac-YYYYMMDD` | `memory-patterns/craft-*` | `prompts/extract-miss-…` (`24` §4)
- 1 przyczyna (fakt, nie „agent głupi”):
- 1 zakaz na następną sesję albo „nic”:
- Dwuobieg? nie | tak → osobny commit + `agentlint --write` (nie w tym commicie co karta)

Cel: `docs/_knowledge/<kategoria>/` po idle. Przy `/noc` busy zostaw treść w PM-FAC/AAR; P-V zrobi flush.

Bez PII, NIP, maila, sekretu, `input_text`. Nie wkładaj `22`/`23`/`24`/`08` / `LEARNING-OS` / `CRAFT-OS` do retrieve.
