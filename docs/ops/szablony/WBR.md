# WBR-YYYY-Www — Weekly Business Review

Amazon WBR: wyjątki i decyzje, nie teatr slajdów. Wypełnij jako **krok 4** `IDLE-SESJA.md` (P-Y) — jedyne wejście CEO.  
Wejście: inbox + arkusze FACTORY/CRAFT/SKILLS + `RECURRENCE.md` (albo `unmeasured`) + twarde zera. Nie osobne spotkanie na każdy arkusz.

Czas CEO: ≤15 min. Agent przygotowuje liczby; nie ogłasza pasma elite.

## 1. Fabryka (DORA analog)

| Sygnał | Liczba | Wyjątek? |
|---|---|---|
| `factory_df` (n deployów) | | |
| `factory_cfr` | | n<8 = bez pasma |
| `factory_recover` | | |
| `factory_rework` / STUB=DONE | | twarde jeśli >0 |
| Kolizje × `/noc` | | twarde jeśli >0 |
| `factory_ai_spend` (CEO wkleja $) | | WoW≥2× przy n≤ → FINOPS-NOC; agent nie skrobie |

## 2. Inbox

- Otwarte D2: ____ (próg 7)
- D2 >7 dni: ____
- Czeka na warunek (install / pulse / G0):

## 3. Nauka (`LEARNING-OS.md`)

- Karty zaległe >7 dni od close: ____
- `learn_recurrence` (28 dni): ____ — jeśli ≥1 → dwuobieg, nie esej
- AlwaysApply nadal ≤3 / ~2k? (prune **w tej P-Y**, nie osobny piątek)
- Kubełki friday: karta / debt / kontrakt — osobne commity? tak / nie było czego / złamane (mega-commit)

## 3b. Rzemiosło (`CRAFT-OS.md`)

- `craft_stub` / `craft_skip` / `craft_floor` / `craft_critic`: ____ (twarde jeśli >0)
- `craft_slop_recurrence` (28 dni): ____ — ≥1 → dwuobieg, nie nowy recenzent

## 3c. Skille (`SKILLS-OS.md`)

- `skill_n`: ____ (10 do INSTALL / 13 po)
- `skill_extra` / `skill_skip_lowca` / `skill_review_commit`: ____ (twarde jeśli >0)
- `skill_recurrence`: ____ — ≥1 → dwuobieg, nie skill #11

## 3d. FinOps fabryki (`FINOPS-NOC.md`)

- `factory_ai_spend` (faktura CEO, nie DORA): ____ / `unmeasured`
- Anomalia WoW ≥2× przy n≤: tak / nie / brak faktury

## 4. Jedna prośba do CEO (albo „nic”)

Apetyt: `1 decyzja` | `1 plaster` | `1 noc` | `1 tydzień` | `park`

## 5. Zakaz w tym WBR

Recenzja ruffa. Drugi `/noc`. Unpark G0 bez PRR. Claim elite. Sterowanie pod Minuty plastra. Czwarty recenzent. Obniżenie floor. Claim „kod jak człowiek”. 11. skill fabryki. `module-factory` na leftover. Drugie WBR / drugi piątek. Nowy `*-OS.md` / FRIDAY-OS. Cron D1. Rozbudowa pakietu zamiast tego WBR.
