# FACTORY-PULSE-YYYY-Www

Arkusz P-R wewnątrz `IDLE-SESJA.md` (P-Y), nie osobne spotkanie. Wzory: `DORA-FABRYKA.md`.  
Nie wczytuj podczas `/plaster`. Nie optymalizuj pod **Minuty plastra**.

- Okno: ____ – ____ (7 dni UTC albo miesiąc)
- Deploye fabryki n = ____ (push na `origin/main` + `gate` i `meta` zielone)
- Źródła: PROGRESS · wiersze post-plaster (**Pushy do zielonego**, **Skuteczność**) · `gh run list`

## Pięć analogów

| ID | Liczba / ułamek | Pasmo (unmeasured / high-factory / elite-factory / N/A) |
|---|---|---|
| `factory_df` | n = | |
| `factory_lt` | HITL: ____ · silnik/leftover: ____ (oczekiwany wzrost) | nie karć silnika |
| `factory_recover` | mediana do zielonego albo leftover | |
| `factory_cfr` | (Pushy≥2 + STUB/NIE + revert) / n = | n<8 → bez pasma |
| `factory_rework` | nieplanowane / n = | cel 0 |

## Twarde zera

| Zero | Liczba | Spalenie? |
|---|---|---|
| Kolizje × `/noc` | | tak jeśli >0 |
| Echo/STUB jako DONE | | tak jeśli >0 |
| Event PII | | tak jeśli >0 |
| `--no-verify` / force-push | | tak jeśli >0 |

Rzemiosło: osobno `szablony/CRAFT-PULSE.md` (P-W). Nie mieszaj `craft_*` z pasmem elite-factory.

## Jedno ograniczenie (DORA next steps)

1. Baseline (ten wiersz albo poprzedni pulse):
2. Tarcie (fakt, nie wina etatu):
3. Jedna zmiana: leftover / kontrakt / skrypt / „nic”:
4. Data check:

## CEO (90 s)

Pasmo ogłasza tylko człowiek. Agent nie pisze „elite” do PROGRESS.
