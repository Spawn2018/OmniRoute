# Spec: routing_guide_match (CT4 leftover)

**Moduł żywy:** CT4 leftover matching

## 288.0 katalog trybu dopasowania

- Tabela `routing_guide_match`: `mark_code`, `match_kind` (`guide_code_only`|`lane_label`|`mode_label`), `source_ref`
- POST INSERT / GET lista
- Nie silnik matching na POST ASN/shipment
