# Spec: pallet_pool_mark (EXP2.17)

HITL katalog znacznika pallet-pool per tenant.

## Pola

- `mark_code` — snake 2–32
- `pool_kind` — `chep` | `lpr` | `epal` | `other`
- `source_ref` — `tenant:manual` albo `fixture://pallet-pool-mark/…`

## Poza zakresem

giełda Chep · depozyt · HTTP Chep · kwota · wymiary Decimal
