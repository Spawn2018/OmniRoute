# what_if_mark (EXP2.10)

HITL katalog scenariusza what-if per tenant.

Pola: `mark_code`, `scenario_kind` (`fuel` | `port` | `bankruptcy` | `other`), `source_ref`.

Nie silnik what-if. Nie mutacja `plan_snapshot`. Nie kwota.
