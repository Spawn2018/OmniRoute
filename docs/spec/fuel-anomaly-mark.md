# fuel_anomaly_mark (EXP2.14)

HITL katalog znacznika karty/anomalii paliwa per tenant.

Pola: `mark_code`, `anomaly_kind` (`card` | `tank` | `spike` | `other`), `source_ref`.

Nie live fuel card. Nie telemetry. Nie mnożenie FSC. Nie kwota.
