# cabotage_mark (EXP2.11)

HITL katalog znacznika kabotażu per tenant.

Pola: `mark_code`, `cabotage_kind` (`counter` | `driver_return` | `vehicle_return` | `other`),
`source_ref`.

Nie silnik kabotażu. Nie RTPD. Nie tacho. Nie kwota.
