# Spec: sap_connector (CT6)

**Moduł żywy:** CT6 (`sap_connector` HITL katalog)

## 281.0 katalog SAP/Oracle

- Tabela `sap_connector`: `connector_code`, `system_kind` (`sap`|`oracle`), `source_ref`
- POST INSERT / GET lista; brak UPDATE/DELETE
- Nie live SOAP; nie sekrety; nie SQL do SAP
