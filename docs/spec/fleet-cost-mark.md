# fleet_cost_mark (EXP2.15)

HITL katalog znacznika kosztu floty per tenant.

Pola: `mark_code`, `cost_kind` (`tco` | `maintenance` | `lease` | `other`), `source_ref`.

Nie TCO SQL. Nie CMMS silnik (`cmms_mark` = osobny katalog work_order/DTC). Nie kwota.
