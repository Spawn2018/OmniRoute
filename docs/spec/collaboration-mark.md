# Spec: collaboration_mark (CT11)

**Moduł żywy:** CT11 (`collaboration_mark` HITL katalog)

## 284.0 katalog roli współpracy

- Tabela `collaboration_mark`: `mark_code`, `role_kind` (`shipper`|`carrier`|`consignee`), `source_ref`
- POST INSERT / GET lista
- Nie wspólny SELECT; nie tuple OpenFGA per strona
