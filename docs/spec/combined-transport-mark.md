# combined_transport_mark (EXP2.12)

HITL katalog reżimu combined transport per tenant.

Pola: `mark_code`, `regime_kind` (`combined` | `mobility` | `piggyback` | `other`), `source_ref`.

Nie silnik Mobility Package. Nie Huckepack. Nie kwota.
