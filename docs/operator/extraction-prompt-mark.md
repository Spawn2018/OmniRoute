# Prompt ekstrakcji (AI3.1)

Operator zapisuje znacznik wersji promptu: kod, `prompt_kind`
(`extract` / `system` / `other`) i `source_ref`. To dane HITL —
katalog „prompt jako dana”, nie bajty treści i nie live Instructor.

Nie liczy kwot. Marża nadal tylko na `charge`. Ekstrakcja zostaje na `/extractions`.

Ścieżka UI: `/extraction-prompt-marks`.
