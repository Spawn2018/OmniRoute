# Working capital (EXP2.1)

Operator zapisuje katalogowy znacznik working capital: kod, `capital_kind`
(`dso` / `cash_at_risk` / `aging` / `other`) i `source_ref`. To dane HITL,
nie DSO SQL i nie druga marża.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/working-capital-marks`.
