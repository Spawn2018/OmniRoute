# Like-for-like załadowcy (BR6.2 leftover)

Operator zapisuje katalogowy stance: kod, `like_kind`
(`match` / `gap` / `bench` / `other`) i `source_ref`. To dane HITL,
nie Alpega i nie SQL porównania ofert.

Nie liczy kwot. Marża nadal tylko na `charge`.
FK i Alpega live zostają leftover.

Ścieżka UI: `/shipper-like-marks`.
