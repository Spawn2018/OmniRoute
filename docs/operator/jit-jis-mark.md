# Operator: znacznik JIT/JIS

Katalog HITL dla protokołu JIT/JIS (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani silnik JIT.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
