# Etap CRM (BR6.0)

Operator zapisuje katalogowy etap lejka CRM: kod, `pipeline_kind`
(`stage` / `won` / `lost` / `hold` / `other`) i `source_ref`. To dane HITL,
nie silnik lejka i nie FK do okazji.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/crm-pipeline-marks`.
