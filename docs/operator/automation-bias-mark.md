# Automation bias

Katalog HITL `automation_bias_mark`: operator zapisuje stancję mitygacji
(confirm / delay / review / other) ze `source_ref`. To nie jest przebudowa
ui-04 ani auto-accept.

Nazwy: `automation_bias_mark` · `bias_kind` · `source_ref`.
Job: `/automation-bias-marks`.
