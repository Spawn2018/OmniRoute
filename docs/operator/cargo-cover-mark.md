# Cargo cover (EXP2.4)

Operator zapisuje katalogowy znacznik cargo cover: kod, `cover_kind`
(`cargo` / `liability` / `policy` / `other`) i `source_ref`. To dane HITL,
nie live insurance i nie polisa HTTP.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/cargo-cover-marks`.
