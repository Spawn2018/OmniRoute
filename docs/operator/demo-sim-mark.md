# Operator: znacznik demo sim

Katalog HITL dla znacznika demo sim (`fleet_150` / `months_10` / `other`).
Zapisujesz kod, `sim_kind` i `source_ref`. To nie jest live symulacja floty ani
generator 150 pojazdów.

`amount`, `vehicles` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
