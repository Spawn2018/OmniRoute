# Cost allocation (EXP2.3)

Operator zapisuje katalogowy znacznik cost allocation: kod, `alloc_kind`
(`direct` / `abc` / `shared` / `other`) i `source_ref`. To dane HITL,
nie allocation SQL i nie ABC.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/cost-allocation-marks`.
