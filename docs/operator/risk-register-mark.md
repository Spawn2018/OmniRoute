# Rejestr ryzyka

Katalog HITL `risk_register_mark`: operator zapisuje stancję wpisu
(open / mitigated / accepted / other) ze `source_ref`. To nie jest scoring
osoby, L3 silnik ani label U-art50.

Nazwy: `risk_register_mark` · `risk_kind` · `source_ref`.
Job: `/risk-register-marks`.
