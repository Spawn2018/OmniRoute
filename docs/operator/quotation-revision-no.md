# Wycena — numer rewizji

Na `/quotations` opcjonalnie wpisujesz `revision_no` (liczba całkowita ≥ 1). Puste pole zostaje puste. To nie jest automatyczna sekwencja i nie wiąże poprzedniej oferty (`supersedes_id` = park).

Nie float. Nie NBP. Marża zostaje na `charge`.
