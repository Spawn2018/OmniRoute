# Wymóg wydruku sieci (D9c)

Operator zapisuje katalogowy wymóg etykiety sieci: kod, etykietę sieci
(1–64 znaków) i `source_ref`. To dane HITL, nie blokada wyjazdu i nie PDF.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/network-print-requirements`.
