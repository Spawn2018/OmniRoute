# Lead CRM (G1)

Operator zapisuje katalogowy lead: kod, `stage_kind`
(`new` / `qualified` / `disqualified` / `other`) i `source_ref`. To dane HITL,
nie szansa sprzedażowa i nie cold auto-send.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/crm-leads`.
