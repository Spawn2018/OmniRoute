# Zlecenie

Wycena z kontrahentem to jeszcze nie zlecenie. Zlecenie powstaje dopiero gdy je **zapiszesz** na `/shipments`.

1. Przyjmij albo odrzuć ofertę na `/quotations` (szyna decyzji). To nie tworzy zlecenia samo z siebie.
2. Wejdź na zlecenia. Wklej `quotation_id` wyceny, która ma kontrahenta, oraz `source_ref` zapisu (`fixture://shipment/…` albo `tenant:manual`). Opcjonalnie wpisz twardy numer `shipment_ref` (`omni://shipment/…` albo `fixture://shipment-ref/…`). Puste pole = zlecenie bez numeru. Opcjonalnie zaznacz `is_waste`, gdy ładunek to odpad (HITL; nie woła MOS i nie bierze stancji z katalogu `waste_mark`). Opcjonalnie wskaż zlecenie główne (`parent_shipment_id`) i rodzaj (`drayage` / `oncarriage` / `leg_subcontract` / `other`). Opcjonalnie `guide_code` z `/routing-guides` — gdy tenant ma tryb `block_409` na `/routing-guide-enforcements`, brak albo nieznany kod daje HTTP 409. Gdy w `/routing-guide-matches` jest `lane_label` / `mode_label` oraz `block_409`, uzupełnij zgodne `plant_label` / `carrier_label` (jak na awizie).
3. „Zapisz zlecenie” wstawia wiersz `shipment`. Jedna wycena — jedno zlecenie. Kwota i marża zostają na wycenie i na `charge`. Rodzic bez rodzaju albo rodzaj bez rodzica jest odrzucany.
4. Wycena bez kontrahenta jest odrzucana. Drugi zapis na tę samą wycenę też. Ten sam `shipment_ref` u tego tenanta też. Rodzic musi być zleceniem tego tenanta.
5. Na tym samym ekranie zapiszesz przejazd. Drugi kierowca (`driver2_id`) jest opcjonalny i musi być innym zasobem rodzaju `driver` niż pierwszy. Puste pole = brak drugiego fotela. Ten sam UUID jest odrzucany. Opcjonalna etykieta trasy (`route_label`) to tekst. Opcjonalna odległość planowana (`planned_distance_km`) i wykonana (`actual_distance_km`) to kilometry HITL (Decimal ≥ 0), nie GPS i nie liczenie w przeglądarce. Opcjonalny podwykonawca (`subcontractor_party_id`) to UUID kontrahenta tego tenanta, nie brama C8. Katalog floty (`resource`) jest też na `/fleet` — ten sam zapis co panel na zleceniach, opcjonalne `capacity_kg` / `capacity_ldm` / `capacity_m3` (Decimal HITL) oraz opcjonalny `inventory_no` (tekst HITL) oraz opcjonalny `adr_certified` (bool HITL, nie matching ładunku) oraz ważność dokumentu (`resource_document`; `licence`|`insurance`|`other` + data, nie odliczanie), nie GPS i nie własne HW.
6. Punkt załadunku może dostać opcjonalny kod grupy (`stop_group_code`). Ten sam kod na kilku punktach tego zlecenia = jedna grupa. Puste pole = punkt bez grupy. Nie osobna tabela. Nie mapa. Opcjonalna notatka dla kierowcy (`notes_for_driver`) to tekst na punkcie. Opcjonalna waga (`weight_kg`) to kilogramy HITL (Decimal), nie VGM i nie liczenie w przeglądarce. Opcjonalna ilość (`quantity`) to liczba całkowita HITL (≥ 0), nie opakowanie i nie plomba. Opcjonalny kod opakowania (`packaging_code`) to tekst HITL (do 32 znaków), nie słownik FK i nie plomba. Opcjonalna plomba wjazdu (`seal_in`) i wyjazdu (`seal_out`) to tekst HITL (do 32 znaków), nie PIN. Opcjonalny numer awizacji (`appointment_ref`) to tekst HITL (do 32 znaków), nie okno doku D3 i nie T8. Opcjonalne minuty wolnego oczekiwania (`waiting_free_minutes`) to liczba całkowita HITL (≥ 0), nie odliczanie i nie POD. Opcjonalny początek oczekiwania (`waiting_started_at`) to zegar HITL ISO-8601 ze strefą, nie odliczanie i nie POD. Opcjonalna jakość POD (`pod_quality`) to token HITL `ok` / `retake` / `missing`, nie kamera i nie skan.

Czego tu nie ma: generator `GD/2026`, QR, PDF, ZPL, fuzzy matching, auto-booking po Przyjmij, bramka sankcji, suma przychodu z podzleceń, liczenie w przeglądarce.

Nazwy w kodzie: `shipment` · `quotation_id` · `party_id` z wyceny · `shipment_ref` · `is_waste` · `parent_shipment_id` · `relation_kind` · `guide_code` · `plant_label` · `carrier_label` · `trip` · `driver2_id` · `route_label` · `planned_distance_km` · `actual_distance_km` · `subcontractor_party_id` · `resource` · `/fleet` · `stop` · `stop_group_code` · `notes_for_driver` · `weight_kg` · `quantity` · `packaging_code` · `seal_in` · `seal_out` · `appointment_ref` · `waiting_free_minutes` · `waiting_started_at` · `pod_quality`.
