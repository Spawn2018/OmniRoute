# Operator: znacznik e-Doręczenia

Katalog HITL dla e-Doręczenia / potwierdzenia (edoreczenia / receipt / inne).
Zapisujesz kod, `delivery_kind` i `source_ref`. To nie jest live ADE ani bajty PDF.

`amount`, `pdf` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
