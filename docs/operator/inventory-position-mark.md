# Operator: znacznik Inventory position

Katalog HITL dla protokołu Inventory position (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani bilans SQL.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
