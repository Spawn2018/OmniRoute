# Operator: znacznik offboarding

Katalog HITL dla protokołu offboarding (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani kasowanie konta.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
