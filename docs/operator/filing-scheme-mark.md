# Schemat składania (G12)

Operator zapisuje katalogowy schemat składania: kod, `scheme_kind`
(`ics2` / `cbam` / `eudr` / `efti` / `other`) i `source_ref`. To dane HITL,
nie SENT-UE i nie filer live.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/filing-scheme-marks`.
