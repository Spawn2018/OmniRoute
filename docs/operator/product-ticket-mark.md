# Ticket produktu

Katalog HITL `product_ticket_mark`: operator zapisuje stancję ticketu
(report / triage / owner_ok / other) ze `source_ref`. To nie jest CAPA,
auto-naprawa ani `operator_notice`.

Nazwy: `product_ticket_mark` · `ticket_kind` · `source_ref`.
Job: `/product-ticket-marks`.
