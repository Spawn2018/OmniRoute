# Konsola LCL (BR4.2)

Operator zapisuje katalogowy stance: kod, `console_kind`
(`console` / `cfs` / `other`) i `source_ref`. To dane HITL,
nie live CFS i nie kalkulacja CBM.

Nie liczy kwot. Marża nadal tylko na `charge`. HBL/MBL zostają
w osobnym katalogu D6 `ocean_bill`; odcinek morski LCL w `shipment_leg`.

Ścieżka UI: `/lcl-console-marks`.
