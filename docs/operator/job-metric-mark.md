# Operator: znacznik metryki jobu

Katalog HITL dla rodzaju metryki jobu (time_to_fix / touches / rework / inne).
Zapisujesz kod, `metric_kind` i `source_ref`. To nie liczy scoringu osoby ani metryki SQL.

`amount` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
