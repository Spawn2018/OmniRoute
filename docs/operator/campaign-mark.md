# Kampania marketingowa (BR6.5)

Operator zapisuje katalogową kampanię: kod, `campaign_kind`
(`campaign` / `attribution` / `other`) i `source_ref`. To dane HITL,
nie lejek X7 i nie atrybucja live.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/campaign-marks`.
