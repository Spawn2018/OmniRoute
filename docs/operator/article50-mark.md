# Etykieta art. 50 (AI9.0)

Operator zapisuje katalogowy znacznik etykiety art. 50: kod, `label_kind`
(`generated` / `exempt` / `human` / `other`) i `source_ref`. To dane HITL, nie przebudowa U-art50.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/article50-marks`.
