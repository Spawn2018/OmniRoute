# Kaskada stylu (`style_cascade_mark`)

Katalog HITL poziomów kaskady: Global → Company → Department → User →
Customer → Person-to-Person → Context.

Operator zapisuje poziom jako daną tenanta. Nie liczy fidelity. Nie ocenia osoby.
Nie generuje treści maila.

Ścieżka: `/style-cascade-marks`.
