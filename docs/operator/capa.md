# Znacznik CAPA (CT12)

Ekran `/capa-marks` zapisuje rodzaj QMS jako dane: kod snake, rodzaj `capa` / `eight_d` / `recurrence`, oraz `source_ref`.

To katalog HITL. Nie uruchamia workflow CAPA ani 8D. Nie liczy kwot.

Źródło: `tenant:manual` albo `fixture://capa-mark/…`.

Czego tu nie ma: silnik CAPA, 409, live SAP, mapa. Marża zostaje na `/charges`.
