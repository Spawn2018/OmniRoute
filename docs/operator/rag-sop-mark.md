# Operator: znacznik zakresu RAG

Katalog HITL dla zakresu RAG (SOP / ADR / mail / inne).
Zapisujesz kod, `scope_kind` i `source_ref`. To nie uruchamia pgvector ani wyceny.

`amount` i `vector` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
