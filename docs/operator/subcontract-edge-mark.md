# Subcontract edge (EXP2.6)

Operator zapisuje katalogowy znacznik krawędzi podwykonawstwa: kod, `edge_kind`
(`prime` / `sub` / `broker` / `other`) i `source_ref`. To dane HITL,
nie graf live i nie matching SQL.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/subcontract-edge-marks`.
