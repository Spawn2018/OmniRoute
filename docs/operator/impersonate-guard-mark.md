# Operator: znacznik impersonate guard

Katalog HITL dla reguły Admin-P (impersonate / unwrap_denied / inne).
Zapisujesz kod, `guard_kind` i `source_ref`. To nie jest unwrap umów ani Auth0 live.

`unwrap`, `ciphertext`, `amount` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
