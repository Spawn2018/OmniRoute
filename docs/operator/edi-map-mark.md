# Mapa EDI (G13)

Operator zapisuje katalogową mapę pól EDI: kod, `map_kind`
(`field_map` / `segment` / `other`) i `source_ref`. To dane HITL,
nie silent write i nie parser live.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/edi-map-marks`.
