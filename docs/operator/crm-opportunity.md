# Okazja CRM (BR6.0)

Operator zapisuje katalogową okazję: kod, `stage_kind`
(`open` / `won` / `lost` / `other`) i `source_ref`. To dane HITL,
nie silnik pipeline i nie lista aktywności.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/crm-opportunities`.
