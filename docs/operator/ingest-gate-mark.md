# Brama ingest (AI5.0 leftover)

Operator zapisuje znacznik bramy przed ingestem: kod, `gate_kind`
(`truth` / `owner` / `exception` / `other`) i `source_ref`. To dana HITL,
nie live HTTP do zrodel z C.1.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/ingest-gate-marks`.
