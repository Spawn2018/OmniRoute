# Jedwabny Szlak (BR4.4)

Operator zapisuje katalogowy stance: kod, `corridor_kind`
(`silk` / `block_train` / `transit` / `other`) i `source_ref`. To dane HITL,
nie live CR Express i nie para UN/LOCODE.

Nie liczy kwot. Marża nadal tylko na `charge`. Odcinek kolei z Chin zostaje
w 110.0 `shipment_leg` china_rail; para miejsc w G2.19 `lane_pattern`.

Ścieżka UI: `/silk-corridor-marks`.
