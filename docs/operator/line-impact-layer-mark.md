# Warstwa liczona wpływu na linię

Katalog HITL `line_impact_layer_mark`: operator zapisuje tryb warstwy
(scored / forecast / actual / other) ze `source_ref`. To nie jest silnik SQL
ani EBITDA linii produkcyjnej.

Nazwy: `line_impact_layer_mark` · `layer_kind` · `source_ref`.
Job: `/line-impact-layer-marks`.
