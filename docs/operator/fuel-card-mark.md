# Operator: znacznik fuel card

Katalog HITL dla karty paliwowej (fuel / anomaly / inne).
Zapisujesz kod, `card_kind` i `source_ref`. To nie jest live fleet ani litry.

`amount`, `liters` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
