# Operator: znacznik widoku roli

Katalog HITL dla widoku roli (groupage / FTL / ocean / inne).
Zapisujesz kod, `view_kind` i `source_ref`. To nie uruchamia boardów T6 ani mapy.

`amount` i `board` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
