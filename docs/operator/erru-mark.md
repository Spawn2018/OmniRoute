# Operator: znacznik sprawdzenia ERRU

Katalog HITL dla statusu sprawdzenia ERRU (TO_VERIFY / CLEAR / HIT / inne).
Zapisujesz kod, `check_kind` i `source_ref`. To nie uruchamia live ERRU ani scrapingu.

`amount` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
