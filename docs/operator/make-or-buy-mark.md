# Make-or-buy (EXP2.2)

Operator zapisuje katalogowy znacznik make-or-buy: kod, `buy_kind`
(`make` / `buy` / `hybrid` / `other`) i `source_ref`. To dane HITL,
nie silnik kosztu i nie allocation SQL.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/make-or-buy-marks`.
