# Definicja KPI (AI5.0 leftover)

Operator zapisuje definicje KPI per strona: kod, `kpi_kind`
(`otd` / `otif` / `custom` / `other`) i `source_ref`. To dana HITL,
nie wzór OTD/OTIF z modelu ani OTIF%.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/kpi-definition-marks`.
