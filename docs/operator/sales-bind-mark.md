# Bind sprzedaży (BR6.1)

Operator zapisuje stance wiązania korytarza: kod, `bind_kind`
(`opportunity` / `party` / `other`) i `source_ref`. To dane HITL,
nie HubSpot live i nie FK UUID.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/sales-bind-marks`.
