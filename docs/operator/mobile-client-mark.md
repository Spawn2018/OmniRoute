# Operator: znacznik klienta mobilnego

Katalog HITL dla rodzaju klienta mobilnego (iOS / Android / OTA / inne).
Zapisujesz kod, `client_kind` i `source_ref`. To nie uruchamia Expo, EAS ani store submit.

`amount` i `expo` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
