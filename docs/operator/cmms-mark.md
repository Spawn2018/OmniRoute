# Znacznik CMMS (G7)

Operator zapisuje katalogowy znacznik CMMS: kod, `work_kind`
(`work_order` / `dtc` / `other`) i `source_ref`. To dane HITL,
nie silnik work_order i nie kara kierowcy.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/cmms-marks`.
