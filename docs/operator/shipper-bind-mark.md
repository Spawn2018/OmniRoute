# Bind załadowcy (BR6.2)

Operator zapisuje stance wiązania: kod, `bind_kind`
(`tender` / `party` / `other`) i `source_ref`. To dane HITL,
nie Alpega live i nie FK UUID do tender/party.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/shipper-bind-marks`.
