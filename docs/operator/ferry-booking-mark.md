# Rezerwacja promu (BR4.0)

Operator zapisuje katalogowy stance: kod, `booking_kind`
(`booking` / `window` / `sailing` / `other`) i `source_ref`. To dane HITL,
nie bilet z portalu i nie liczenie art. 9.

Nie liczy kwot. Marża nadal tylko na `charge`. Art. 9 zostaje
w osobnym katalogu rest/watchdog/crossing.

Ścieżka UI: `/ferry-booking-marks`.
