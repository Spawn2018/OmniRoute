# Znacznik OOG (G5)

Operator zapisuje katalogowy znacznik OOG: kod, `escort_kind`
(`oog` / `lashing` / `escort` / `other`) i `source_ref`. To dane HITL,
nie wymiary Decimal i nie zezwolenie krajowe.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/oog-marks`.
