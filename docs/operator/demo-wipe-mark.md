# Operator: znacznik wipe demo

Katalog HITL dla znacznika wipe demo (`usun` / `retain` / `other`).
Zapisujesz kod, `wipe_kind` i `source_ref`. To nie jest live wipe ani kasowanie
danych tenanta.

`amount`, `delete` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
