# Znacznik wycieku (CI2)

Operator zapisuje katalogowy rodzaj wycieku spend: kod, `leakage_kind`
(`invoice` / `clause` / `other`) i `source_ref`. To dane HITL, nie porównanie
FV do `charge` i nie druga marża.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/spend-marks`.
