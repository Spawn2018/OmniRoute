# Operator: znacznik zakazu copy claimów

Katalog HITL dla zakazu copy claimów (8 min / 15k / 500k / inne).
Zapisujesz kod, `ban_kind` i `source_ref`. To nie uruchamia copy marketingu ani silnika banów.

`amount` i `claim` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
