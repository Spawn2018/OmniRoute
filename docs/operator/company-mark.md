# Znacznik spółki (G10)

Operator zapisuje katalogowy znacznik spółki w tenancie: kod, `seat_kind`
(`hq` / `branch` / `other`) i `source_ref`. To dane HITL,
nie drugi tenant i nie `company_id` na wszystkich tabelach.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/company-marks`.
