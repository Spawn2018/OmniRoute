# Znacznik kary (CI5)

Operator zapisuje katalogowy rodzaj naruszenia: kod, `breach_kind`
(`otif` / `delay` / `damage` / `other`) i `source_ref`. To dane HITL, nie kara
SQL i nie automatyczna linia produkcyjna.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/penalty-marks`.
