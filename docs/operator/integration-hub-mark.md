# Operator: znacznik Integration Hub

Katalog HITL dla protokołu Integration Hub (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live HTTP hubu ani Selenium.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
