# Sala operacyjna — warstwa działająca

Katalog HITL `ops_room_mark`: operator zapisuje rodzaj warstwy sali
(shift / board / escalation / other) ze `source_ref`. To nie jest koalescencja
alertów N8, drugi czat ani widok sklejający wyjątki z mapą.

Nazwy w kodzie: `ops_room_mark` · `layer_kind` · `source_ref`.
Job: `/ops-room-marks`.
