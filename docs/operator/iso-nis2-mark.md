# Operator: znacznik ISO/NIS2 ops

Katalog HITL dla protokołu ISO/NIS2 ops (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani certyfikat HTTP.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
