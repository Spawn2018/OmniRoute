# Zrodlo danych (AI5.0)

Operator zapisuje katalogowe zrodlo zewnetrzne: `source_code`, etykiete licencji
(`license_label`), zakres praw (`rights_scope`) i `source_ref`. To slownik HITL
(VISION B.3), nie live ingest.

Nie liczy kwot. Marza nadal tylko na `charge`. Bez `api_key` i bez HTTP.

Sciezka UI: `/data-sources`.
