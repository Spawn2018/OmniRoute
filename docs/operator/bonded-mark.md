# Znacznik bonded (G11)

Operator zapisuje katalogowy znacznik bonded / miejsce uznane: kod, `bond_kind`
(`bonded` / `recognized` / `other`) i `source_ref`. To dane HITL,
nie WMS e-com i nie procedura live.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/bonded-marks`.
