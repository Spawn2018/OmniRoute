# Dossier AEO (G14)

Operator zapisuje katalogowy znacznik dossier AEO: kod, `dossier_kind`
(`aeo` / `authorised` / `other`) i `source_ref`. To dane HITL,
nie party_document i nie scoring osoby.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/aeo-dossier-marks`.
