# Link CRM (BR6.0)

Operator zapisuje stance powiązania CRM: kod, `link_kind`
(`lead` / `party` / `other`) i `source_ref`. To dane HITL,
nie FK UUID i nie cold-send.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/crm-link-marks`.
