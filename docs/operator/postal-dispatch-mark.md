# Ksiazka PP (F11)

Operator zapisuje katalogowy znacznik stancji ksiazki PP: kod, `dispatch_kind`
(`en` / `uss` / `epo` / `other`) i `source_ref`. To dane HITL,
nie live Poczta Polska i nie e-Doreczenia.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/postal-dispatch-marks`.
