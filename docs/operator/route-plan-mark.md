# Znacznik planu trasy (BR3.0)

Operator zapisuje katalogowy znacznik planu trasy: kod, `plan_kind`
(`route` / `stop` / `window` / `other`) i `source_ref`. To dane HITL,
nie Valhalla i nie kilometraż.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/route-plan-marks`.
