# Operator: znacznik MQC

Katalog HITL dla protokołu MQC (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani RA3 scrape.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
