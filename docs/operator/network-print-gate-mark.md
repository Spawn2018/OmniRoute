# Brama wydruku sieci (D9c leftover)

Operator zapisuje stance bramy 409 przy wydruku sieci: kod, rodzaj
`block_409` | `warn_only` | `record_only` | `other` i `source_ref`.
To dane HITL — nie live 409 na wyjeździe i nie PDF.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/network-print-gate-marks`.
