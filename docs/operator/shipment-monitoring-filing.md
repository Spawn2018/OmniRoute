# Zgloszenie SENT/BDO (C1)

Operator zapisuje katalogowy znacznik stancji zgloszenia SENT/BDO: kod, `status_kind`
(`en` / `uss` / `epo` / `other`) i `source_ref`. To dane HITL,
nie live PUESC i nie XML SENT.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/shipment-monitoring-filings`.
