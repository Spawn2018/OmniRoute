# Aktywność CRM (BR6.0 leftover)

Operator zapisuje katalogową aktywność: kod, `activity_kind`
(`call` / `meeting` / `email` / `note` / `other`) i `source_ref`. To dane HITL,
nie silnik pipeline i nie FK do okazji.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/crm-activities`.
