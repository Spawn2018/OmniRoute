# Award załadowcy (BR6.2)

Operator zapisuje stance nagrody: kod, `award_kind`
(`go` / `hold` / `no_award` / `other`) i `source_ref`. To dane HITL,
nie Alpega live i nie auto-award SQL.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/shipper-award-marks`.
