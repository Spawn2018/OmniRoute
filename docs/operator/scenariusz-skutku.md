# Scenariusz skutku (CI6)

Operator zapisuje katalogowy scenariusz skutku: kod, etykietę łańcucha (tekst) i
`source_ref`. To dane HITL, nie kalkulator EBITDA i nie wieża `tower_impact`.

Nie sumuje SQL. Nie zapisuje `charge`. Marża nadal tylko na `charge`.

Ścieżka UI: `/impact-scenarios`.
