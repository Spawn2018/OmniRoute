# Operator: znacznik UIC/CIM/SMGS

Katalog HITL dla znacznika kolejowego UIC / CIM / SMGS / inne.
Zapisujesz kod, `rail_kind` i `source_ref`. To nie jest live rail API ani km.

`amount`, `km` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
