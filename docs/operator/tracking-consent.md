# Zgoda na śledzenie (BR2.2)

Operator zapisuje katalogową zgodę: kod, `consent_kind`
(`party` / `driver` / `other`) i `source_ref`. To dane HITL,
nie kolumna na `party_contact` i nie live poll.

Nie liczy kwot. Marża nadal tylko na `charge`.
Kolumna na kontakcie zostaje leftover.

Ścieżka UI: `/tracking-consents`.
