# Narracja CFO (AI7.1)

Operator zapisuje etykiete narracji po SQL: kod, `narrative_kind`
(`anomaly` / `story` / `summary` / `other`) i `source_ref`. To dana HITL,
nie silnik narracji ani druga marza.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/cfo-narrative-marks`.
