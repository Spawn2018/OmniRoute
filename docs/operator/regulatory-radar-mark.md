# Operator: znacznik regulatory radar

Katalog HITL dla protokołu regulatory radar (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła live giełda hubu ani live feed.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
