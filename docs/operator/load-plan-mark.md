# Znacznik planu załadunku (G6)

Operator zapisuje katalogowy znacznik planu załadunku: kod, `stance_kind`
(`axes` / `tunnel` / `other`) i `source_ref`. To dane HITL,
nie solver OR i nie osie Decimal.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/load-plan-marks`.
