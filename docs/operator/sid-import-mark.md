# Operator: znacznik importu SID

Katalog HITL dla trybu importu SID (pojedynczy / paczka / ręczny).
Zapisujesz kod, rodzaj i `source_ref`. To nie woła SID HTTP ani ICS2.

`amount` i bajty są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
