# Cecha modelu (AI5.1)

Operator zapisuje etykiete cechy modelu: kod, `feature_kind`
(`numeric` / `categorical` / `derived` / `other`) i `source_ref`. To dana HITL,
nie live train ani feature store.

Nie liczy kwot. Marza nadal tylko na `charge`.

Sciezka UI: `/model-feature-marks`.
