# Schedule exception (EXP2.7)

Operator zapisuje znacznik wyjątku harmonogramu: kod, `exception_kind`
(`delay` / `cancel` / `reroute` / `other`) i `source_ref`. HITL, nie silnik schedule.

UI: `/schedule-exception-marks`.
