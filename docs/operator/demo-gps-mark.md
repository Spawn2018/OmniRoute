# Operator: znacznik demo GPS

Katalog HITL dla rodzaju demo GPS (seven_day / fleet_demo / inne).
Zapisujesz kod, `demo_kind` i `source_ref`. To nie jest live poll ani mapa.

`lat`, `lng`, `amount` i `score` są odrzucane. Zmiana wiersza = nowy rekord (brak UPDATE).
