# Szkic NCTS (G4)

Operator zapisuje katalogowy szkic NCTS: kod, `transit_kind`
(`t1` / `t2` / `other`) i `source_ref`. To dane HITL,
nie PUESC HTTP i nie generowanie XML.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/ncts-drafts`.
