# Sanctions (EXP2.5)

Operator zapisuje katalogowy znacznik listy sankcji: kod, `list_kind`
(`ofac` / `eu` / `un` / `other`) i `source_ref`. To dane HITL,
nie live scrape i nie screening HTTP.

Nie liczy kwot. Marża nadal tylko na `charge`.

Ścieżka UI: `/sanctions-marks`.
