# OpenFGA — binarka lokalna (Windows)

Pobierane przez `scripts/dev-native.ps1` (nie commitować `openfga.exe`).

```powershell
powershell -ExecutionPolicy Bypass -File scripts/dev-native.ps1 -DownloadOpenFgaOnly
.\tools\openfga\openfga.exe run
```

Wersja docelowa skryptu: v1.19.0 (windows_amd64).
